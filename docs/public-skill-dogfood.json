{
  "schema_version": 1,
  "review_required_skills": [
    "announcement",
    "create-cli",
    "create-skill",
    "debug",
    "find-skills",
    "gather",
    "handoff",
    "hitl",
    "ideation",
    "impl",
    "init-repo",
    "narrative",
    "premortem",
    "quality",
    "release",
    "retro",
    "spec"
  ],
  "cases": [
    {
      "skill_id": "announcement",
      "prompt": "Summarize the latest repo changes into a chat-ready update and keep the draft scoped to what changed.",
      "repo_shape": "repo with recent checked-in changes and one clear delivery context such as release notes or a team update",
      "expected_skill": "announcement",
      "expected_artifact": "charness-artifacts/announcement/latest.md",
      "validation_tier": "hitl-recommended",
      "adapter_requirement": "required",
      "acceptance_evidence": [
        "routes the prompt to `announcement` instead of an adjacent public skill",
        "names or refreshes `charness-artifacts/announcement/latest.md` when the skill persists durable state",
        "produces an output that a maintainer could review directly without re-deriving the whole request"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-16",
      "observed_evidence": [
        "The checked-in public core keeps `announcement` draft-first, explicitly separating human-facing communication from delivery backends and from `narrative` source-of-truth alignment work.",
        "The adapter resolver already pins the visible draft artifact to `charness-artifacts/announcement/latest.md` and the hidden delivery record to `.charness/announcement/announcements.jsonl`, with draft-only defaults when no delivery config exists.",
        "Repo-owned tests already cover both seams: `test_announcement_resolve_adapter_explains_draft_only_defaults` guards the draft-only fallback and `test_capability_explain_reports_skill_needs_and_announcement_adapter_binding` proves adapter-defined delivery capability binding."
      ]
    },
    {
      "skill_id": "create-cli",
      "prompt": "We keep adding ad hoc scripts here; normalize this into one repo-owned CLI before the command surface sprawls further.",
      "repo_shape": "tooling repo where ad hoc shell or Python entrypoints already exist and the command surface needs to be normalized",
      "expected_skill": "create-cli",
      "expected_artifact": null,
      "validation_tier": "hitl-recommended",
      "adapter_requirement": "adapter-free",
      "acceptance_evidence": [
        "routes the prompt to `create-cli` instead of an adjacent public skill",
        "does not invent host-specific adapter state when repo inspection alone should be enough",
        "produces an output that a maintainer could review directly without re-deriving the whole request"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-16",
      "observed_evidence": [
        "The checked-in public core frames `create-cli` as command-surface normalization before ad hoc entrypoints sprawl, keeping operator contract, lifecycle commands, and machine-readable output ahead of implementation detail.",
        "The current validation policy already classifies `create-cli` as `hitl-recommended` and `adapter-free`, which matches a repo-inspection-first planning skill instead of a durable-artifact or adapter-owned workflow.",
        "Repo-owned tests already pin the most load-bearing seam here: `test_quality_and_create_cli_carry_command_docs_drift_pattern` and `test_create_cli_and_create_skill_carry_authenticated_release_probe_pattern` preserve the command-doc drift and authenticated release-probe contracts."
      ]
    },
    {
      "skill_id": "create-skill",
      "prompt": "Improve this skill package first so the trigger, references, and helper surface stay portable.",
      "repo_shape": "skills repo with adjacent public/support surfaces, references, and packaging constraints already present",
      "expected_skill": "create-skill",
      "expected_artifact": null,
      "validation_tier": "evaluator-required",
      "adapter_requirement": "adapter-free",
      "acceptance_evidence": [
        "routes the prompt to `create-skill` instead of an adjacent public skill",
        "does not invent host-specific adapter state when repo inspection alone should be enough",
        "handles the skill's load-bearing contract without needing the user to restate obvious repo context",
        "treats the public skill frontmatter and core trigger as classifier input, not only documentation",
        "keeps `SKILL.md` as selection/sequence core and pushes bulky nuance into references or scripts"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-16",
      "observed_evidence": [
        "The prompt asks to improve a skill package directly, which matches `create-skill` instead of generic implementation or release work.",
        "The checked-in core now explicitly requires one realistic consumer prompt for public-skill changes and treats frontmatter plus generated AGENTS hints as classifier input.",
        "The current scaffold resolves no adapter artifact for `create-skill`, which matches the adapter-free contract instead of inventing repo-local state."
      ]
    },
    {
      "skill_id": "debug",
      "prompt": "Investigate this regression and leave a durable record of what actually failed before changing code.",
      "repo_shape": "active repo slice with a reproducible failure, existing logs or tests, and enough local state to preserve a durable debug artifact",
      "expected_skill": "debug",
      "expected_artifact": "charness-artifacts/debug/latest.md",
      "validation_tier": "evaluator-required",
      "adapter_requirement": "required",
      "acceptance_evidence": [
        "routes the prompt to `debug` instead of an adjacent public skill",
        "names or refreshes `charness-artifacts/debug/latest.md` when the skill persists durable state",
        "handles the skill's load-bearing contract without needing the user to restate obvious repo context"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-16",
      "observed_evidence": [
        "The checked-in public core explicitly keeps `debug` in diagnosis-before-repair mode, preserving the exact symptom, a falsifiable hypothesis, and at least three candidate causes before implementation work starts.",
        "Repo-owned eval coverage already exercises `debug-adapter-bootstrap`, which pins the current adapter bootstrap and canonical artifact path at `charness-artifacts/debug/latest.md`.",
        "The committed `validate-debug-artifact.py` gate plus `tests/test_debug_artifact.py` enforce the durable artifact shape, including canonical sections and the minimum three plausible causes requirement."
      ]
    },
    {
      "skill_id": "find-skills",
      "prompt": "Which skill should handle this named capability, and what existing support surface already covers it?",
      "repo_shape": "repo with multiple public and support skills where the user names a capability instead of a file path",
      "expected_skill": "find-skills",
      "expected_artifact": "charness-artifacts/find-skills/latest.md",
      "validation_tier": "evaluator-required",
      "adapter_requirement": "required",
      "acceptance_evidence": [
        "routes the prompt to `find-skills` instead of an adjacent public skill",
        "names or refreshes `charness-artifacts/find-skills/latest.md` when the skill persists durable state",
        "handles the skill's load-bearing contract without needing the user to restate obvious repo context",
        "uses the named skill or capability lookup path before falling back to broad filesystem search"
      ],
  "review_status": "reviewed",
      "reviewed_on": "2026-04-19",
      "observed_evidence": [
        "The checked-in public core now treats `charness-artifacts/find-skills/latest.*` as the canonical capability inventory artifact, while recommendation queries stay ad hoc and should not rewrite that durable surface just because the invocation shape changed.",
        "The current repo memory at `charness-artifacts/retro/recent-lessons.md` explicitly records the repeat trap here: canonical inventory and ad hoc recommendation output must stay separate so ordinary inspection does not create false dirty state or erode operator trust.",
        "That keeps `find-skills-canonical-artifact-followup` as a reviewed consumer seam worth re-promoting after the latest chatbot packet churn: the user's push to distinguish canonical evidence from query-shaped output still maps directly to checked-in `find-skills` behavior and artifact policy."
      ]
    },
    {
      "skill_id": "gather",
      "prompt": "Fetch this external source into a durable local artifact instead of giving me a one-turn summary.",
      "repo_shape": "repo that already keeps gathered artifacts and a source identity that may need refresh-in-place behavior",
      "expected_skill": "gather",
      "expected_artifact": "charness-artifacts/gather/latest.md",
      "validation_tier": "evaluator-required",
      "adapter_requirement": "required",
      "acceptance_evidence": [
        "routes the prompt to `gather` instead of an adjacent public skill",
        "names or refreshes `charness-artifacts/gather/latest.md` when the skill persists durable state",
        "handles the skill's load-bearing contract without needing the user to restate obvious repo context"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-19",
      "observed_evidence": [
        "The checked-in public core now carries the private SaaS decision ladder explicitly: prefer existing local assets and official API/export paths first, fall back to `agent-browser` only when the stronger path is unavailable or equally gated, and stop cleanly when auth/bootstrap is still missing.",
        "The prompt-affecting preserve proof at `charness-artifacts/cautilus/latest.md` stayed green while that contract widened, which is the current repo-owned evidence that the maintained first-skill routing surface did not regress when browser-mediated acquisition language was added.",
        "The long-context proposal and benchmark surfaces still both carry `gather-official-path-before-browser-followup`, and the checked-in chatbot packet now sets an explicit `limit` so the full candidate set stays emitted instead of disappearing behind the upstream default cap. That keeps the `official API/export -> browser fallback -> clean auth/bootstrap stop` correction visible as a live consumer-facing gather seam instead of a stale side packet."
      ]
    },
    {
      "skill_id": "init-repo",
      "prompt": "Normalize this partially initialized repo without pretending it needs a greenfield rewrite.",
      "repo_shape": "partially initialized mature repo with divergent but valid naming and intentionally missing optional surfaces",
      "expected_skill": "init-repo",
      "expected_artifact": "charness-artifacts/init-repo/latest.md",
      "validation_tier": "evaluator-required",
      "adapter_requirement": "required",
      "acceptance_evidence": [
        "routes the prompt to `init-repo` instead of an adjacent public skill",
        "names or refreshes `charness-artifacts/init-repo/latest.md` when the skill persists durable state",
        "handles the skill's load-bearing contract without needing the user to restate obvious repo context"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-18",
      "observed_evidence": [
        "The checked-in public core now makes repo-mode detection explicit: `GREENFIELD`, `PARTIAL`, and `NORMALIZE` are separate states, and a mature repo with one missing core surface should get a targeted repair instead of a greenfield rewrite.",
        "That same boundary is reinforced in `skills/public/init-repo/references/normalization-flow.md`, which says partially initialized repos should rewrite or repair existing truth surfaces and only scaffold the missing core seam instead of dropping fresh parallel templates.",
        "This makes the current `init-repo-partial-normalization-followup` long-context candidate a real reviewed consumer seam: the user's push against greenfield assumptions maps directly to checked-in `init-repo` behavior and normalization rules."
      ]
    },
    {
      "skill_id": "narrative",
      "prompt": "Tighten the repo's durable story first, then derive one concise brief from that source of truth.",
      "repo_shape": "repo with existing source-of-truth docs that drift from the current product or project story",
      "expected_skill": "narrative",
      "expected_artifact": "charness-artifacts/narrative/latest.md",
      "validation_tier": "hitl-recommended",
      "adapter_requirement": "required",
      "acceptance_evidence": [
        "routes the prompt to `narrative` instead of an adjacent public skill",
        "names or refreshes `charness-artifacts/narrative/latest.md` when the skill persists durable state",
        "produces an output that a maintainer could review directly without re-deriving the whole request"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-16",
      "observed_evidence": [
        "The checked-in public core keeps durable truth alignment separate from audience adaptation and explicitly hands delivery-only work to `announcement`.",
        "The adapter resolves the durable artifact to `charness-artifacts/narrative/latest.md`, which matches the reviewed expectation for the aligned truth-surface output.",
        "Repo-owned tests and docs already exercise the narrative adapter, source-map path, and narrative versus announcement boundary, which is enough local evidence to treat first-touch story alignment as a real maintained seam."
      ]
    },
    {
      "skill_id": "premortem",
      "prompt": "Stress this pending decision before we lock it in and separate real blockers from over-worry.",
      "repo_shape": "repo with a non-trivial pending decision whose main risk is choosing the wrong plan too early",
      "expected_skill": "premortem",
      "expected_artifact": null,
      "validation_tier": "hitl-recommended",
      "adapter_requirement": "adapter-free",
      "acceptance_evidence": [
        "routes the prompt to `premortem` instead of an adjacent public skill",
        "does not invent host-specific adapter state when repo inspection alone should be enough",
        "produces an output that a maintainer could review directly without re-deriving the whole request"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-19",
      "observed_evidence": [
        "The checked-in public core now makes bounded subagents the canonical premortem path and explicitly forbids silently collapsing to a same-agent local pass before the subagent capability check runs and yields a concrete block.",
        "That same policy is reinforced in `skills/public/premortem/references/subagent-capability-check.md` and in `specs/on-demand-validation.spec.md`, which keeps `premortem` as a judgment-heavy on-demand workflow rather than pretending it belongs in a standing evaluator or generic local fallback path.",
        "That keeps `premortem-canonical-subagent-followup` as a reviewed consumer seam worth re-promoting after the latest chatbot packet churn: the user's push to keep subagents canonical still maps directly to checked-in behavior, not just to a one-off wording preference."
      ]
    },
    {
      "skill_id": "quality",
      "prompt": "Review the current quality posture and install the next deterministic gate if the move is obvious.",
      "repo_shape": "mature repo with standing local gates, some drift or fragility, and at least one final stop-before-finish command",
      "expected_skill": "quality",
      "expected_artifact": "charness-artifacts/quality/latest.md",
      "validation_tier": "hitl-recommended",
      "adapter_requirement": "required",
      "acceptance_evidence": [
        "routes the prompt to `quality` instead of an adjacent public skill",
        "names or refreshes `charness-artifacts/quality/latest.md` when the skill persists durable state",
        "produces an output that a maintainer could review directly without re-deriving the whole request",
        "runs or names the existing repo-owned quality gates before proposing new ones",
        "uses one realistic consumer prompt when the risk is public-skill routing or artifact behavior"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-19",
      "observed_evidence": [
        "The current quality artifact at `charness-artifacts/quality/latest.md` now explicitly audits proof layering instead of only asking what proof is missing: public executable specs, on-demand `cautilus` proof, and the specdown viewer are treated as distinct layers with different ownership.",
        "The checked-in quality references and validation docs still preserve the same split after the `v0.4.1` release closeout: `skills/public/quality/references/public-spec-layering.md` keeps reader-facing public proof separate from lower-layer checks, while `docs/public-skill-validation.md`, `specs/on-demand-validation.spec.md`, and the repaired release gate path keep checked artifacts as the source of truth and specdown as the readable viewer.",
        "That keeps `quality-proof-layering-followup` as a reviewed consumer seam worth re-promoting in the long-context packet: the user pressure about `specdown` as SoT versus viewer still maps directly to checked-in `quality` behavior and proof surfaces, not to a one-off wording preference."
      ]
    },
    {
      "skill_id": "release",
      "prompt": "Verify and advance the checked-in release surface without hand-editing generated packaging artifacts.",
      "repo_shape": "repo with checked-in version or packaging surfaces and a maintainer-facing release workflow",
      "expected_skill": "release",
      "expected_artifact": "charness-artifacts/release/latest.md",
      "validation_tier": "hitl-recommended",
      "adapter_requirement": "required",
      "acceptance_evidence": [
        "routes the prompt to `release` instead of an adjacent public skill",
        "names or refreshes `charness-artifacts/release/latest.md` when the skill persists durable state",
        "produces an output that a maintainer could review directly without re-deriving the whole request"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-16",
      "observed_evidence": [
        "The public core tells release work to inspect `current_release.py`, `check_real_host_proof.py`, git state, and the current release artifact before mutating anything.",
        "The current repo state already reports a coherent `0.0.7` release surface across packaging and checked-in manifests through `current_release.py`, and no extra release-time host-proof trigger is active for the current slice.",
        "The checked-in release artifact at `charness-artifacts/release/latest.md` records the same `0.0.7` surface and the guardrails explicitly ban hand-editing generated plugin manifests, which matches the reviewed maintainer-facing release path."
      ]
    },
    {
      "skill_id": "handoff",
      "prompt": "Use docs/handoff.md as the pickup surface and continue the next highest-leverage workflow from there.",
      "repo_shape": "mature repo with an existing handoff artifact and enough adjacent state that the next pickup path can be ambiguous",
      "expected_skill": "handoff",
      "expected_artifact": "docs/handoff.md",
      "validation_tier": "evaluator-required",
      "adapter_requirement": "required",
      "acceptance_evidence": [
        "routes the prompt to `handoff` instead of an adjacent public skill",
        "names or refreshes `docs/handoff.md` when the skill persists durable state",
        "handles the skill's load-bearing contract without needing the user to restate obvious repo context",
        "reads the current workflow trigger before broad repo exploration and keeps the baton pass continuation-first"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-19",
      "observed_evidence": [
        "The checked-in public core now makes the `Workflow Trigger` the handoff's primary contract: mention-only pickup should continue the named workflow instead of re-deriving state from summary prose, and the trigger should be explicit enough that the next operator does not have to infer the first move.",
        "That same seam is reinforced in `skills/public/handoff/references/workflow-trigger.md`, which says the next session should name the workflow directly rather than bury it inside history or diary-like status text.",
        "That keeps `handoff-workflow-trigger-followup` as a reviewed consumer seam worth re-promoting after the latest chatbot packet churn: pushing the handoff away from history-first writing and back toward continuation-first pickup still maps directly to checked-in `handoff` behavior."
      ]
    },
    {
      "skill_id": "hitl",
      "prompt": "Set up a bounded human review loop for this target so the agent does not auto-decide the final judgment.",
      "repo_shape": "repo with a bounded review target and a decision that must stay explicitly human-owned",
      "expected_skill": "hitl",
      "expected_artifact": "charness-artifacts/hitl/latest.md",
      "validation_tier": "hitl-recommended",
      "adapter_requirement": "required",
      "acceptance_evidence": [
        "routes the prompt to `hitl` instead of an adjacent public skill",
        "names or refreshes `charness-artifacts/hitl/latest.md` when the skill persists durable state",
        "produces an output that a maintainer could review directly without re-deriving the whole request"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-16",
      "observed_evidence": [
        "The checked-in public core keeps `hitl` narrowly scoped to bounded human review, explicitly preserving what must not be auto-decided and routing state through resumable review artifacts rather than chat-only judgment.",
        "The current adapter resolver already pins the visible artifact to `charness-artifacts/hitl/latest.md` and the runtime directory to `.charness/hitl/runtime`, with portable defaults when no adapter is present.",
        "Repo-owned helper scripts make the review loop concrete: `bootstrap_review.py` creates scratchpad, state, queue, rules, and event-log files for a new session, which is the current best local evidence that resumable HITL state is a maintained seam rather than prose-only guidance."
      ]
    },
    {
      "skill_id": "ideation",
      "prompt": "The concept is still fuzzy; help shape the workflow before we commit to a spec or implementation.",
      "repo_shape": "minimal or loosely defined repo context where the request is still concept-shaping rather than implementation-ready",
      "expected_skill": "ideation",
      "expected_artifact": null,
      "validation_tier": "hitl-recommended",
      "adapter_requirement": "adapter-free",
      "acceptance_evidence": [
        "routes the prompt to `ideation` instead of an adjacent public skill",
        "does not invent host-specific adapter state when repo inspection alone should be enough",
        "produces an output that a maintainer could review directly without re-deriving the whole request"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-16",
      "observed_evidence": [
        "The checked-in public core keeps `ideation` upstream of `spec` and `impl`, forcing a living concept model, dependency-ordered questions, and explicit demand/wedge/feedback checks before downstream contract work starts.",
        "The current validation policy already classifies `ideation` as `hitl-recommended` and `adapter-free`, which matches the repo's present stance that concept-shaping quality is better judged through review than through adapter or artifact enforcement.",
        "Deferred decision D10 deliberately preserves a lightweight ideation core with detailed world-model and effectuation playbooks in references, which is the strongest current local evidence that the public trigger stays concise without losing its reasoning boundary."
      ]
    },
    {
      "skill_id": "impl",
      "prompt": "Implement the smallest meaningful slice now and verify it against the current repo contract.",
      "repo_shape": "repo with an active build slice, existing code or config surfaces, and at least one verification path",
      "expected_skill": "impl",
      "expected_artifact": "charness-artifacts/impl",
      "validation_tier": "evaluator-required",
      "adapter_requirement": "required",
      "acceptance_evidence": [
        "routes the prompt to `impl` instead of an adjacent public skill",
        "names or refreshes `charness-artifacts/impl` when the skill persists durable state",
        "handles the skill's load-bearing contract without needing the user to restate obvious repo context"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-16",
      "observed_evidence": [
        "The checked-in public core keeps `impl` downstream of `spec` while still requiring an inline honest contract when no separate spec exists, which matches a direct build-slice prompt instead of collapsing into `spec` or `quality`.",
        "Repo-owned eval coverage already exercises `impl-adapter-bootstrap`, pinning the inferred adapter defaults for `charness-artifacts/impl` and the verification/truth-surface fields that bootstrap expects.",
        "Repo-owned tests preserve the operational seams that matter most here: verification survey warnings for missing preferred tools, the explicit `Truth Surface Sync` closeout contract, and the autonomous-continuation rule for locally decidable next slices."
      ]
    },
    {
      "skill_id": "spec",
      "prompt": "Turn this vague request into a living implementation contract before code changes spread.",
      "repo_shape": "repo with an under-specified change request and enough current docs or code to refine a build contract",
      "expected_skill": "spec",
      "expected_artifact": null,
      "validation_tier": "evaluator-required",
      "adapter_requirement": "adapter-free",
      "acceptance_evidence": [
        "routes the prompt to `spec` instead of an adjacent public skill",
        "does not invent host-specific adapter state when repo inspection alone should be enough",
        "handles the skill's load-bearing contract without needing the user to restate obvious repo context"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-16",
      "observed_evidence": [
        "The checked-in public core frames `spec` as turning an under-specified request into the current build contract, which keeps this prompt out of `ideation` and `impl` unless the concept or execution state changes materially.",
        "Representative contract pins in `scripts/check-skill-contracts.py` keep the Contract Shaping section and the `Fixed Decisions`/`Probe Questions`/`Deferred Decisions`/`Acceptance Checks`/`First Implementation Slice` skeleton load-bearing.",
        "Repo-owned tests preserve the higher-cost reasoning seams around `spec`, including the premortem loop, fresh-eye review, and rejected-alternative capture, while the current scaffold remains adapter-free."
      ]
    },
    {
      "skill_id": "retro",
      "prompt": "Run a short retro on this slice and persist the repeat trap if the workflow should have caught it.",
      "repo_shape": "repo that just completed a meaningful slice or exposed a missed issue that should feed repeat-trap memory",
      "expected_skill": "retro",
      "expected_artifact": "charness-artifacts/retro/recent-lessons.md",
      "validation_tier": "hitl-recommended",
      "adapter_requirement": "required",
      "acceptance_evidence": [
        "routes the prompt to `retro` instead of an adjacent public skill",
        "names or refreshes `charness-artifacts/retro/recent-lessons.md` when the skill persists durable state",
        "produces an output that a maintainer could review directly without re-deriving the whole request"
      ],
      "review_status": "reviewed",
      "reviewed_on": "2026-04-19",
      "observed_evidence": [
        "The checked-in public core now makes a real workflow miss trigger a short `session` retro before resuming work, and it requires the retro to separate local symptom from the workflow or capability change that would prevent the repeat trap.",
        "That persistence rule is reinforced in `skills/public/retro/references/trigger-and-persistence.md`, and the current repo treats `charness-artifacts/retro/recent-lessons.md` as a first-class memory surface from both `AGENTS.md` and `docs/handoff.md`.",
        "That keeps `retro-structural-cause-followup` as a reviewed consumer seam worth re-promoting after the `v0.4.1` release follow-up work: pushing past a local explanation toward a structural cause and durable memory update is still explicit checked-in `retro` behavior, not only a conversation style preference."
      ]
    }
  ]
}
