# charness Handoff

## Workflow Trigger

- Pickup = `charness:find-skills` -> **invoke `charness:handoff`**. Bare
  `/handoff` runs chunked routing over handoff entries plus live open issues;
  `## Next Session` is sequencing judgment, not the full queue.
- Refresh live state: `git status --short --branch`,
  `git log --oneline origin/main..HEAD`, `gh issue list --state open --limit 50`.
- Before mutating code/exports/validation, read
  [implementation discipline](./conventions/implementation-discipline.md) and
  [operating contract](./conventions/operating-contract.md).

## Current State

- **Mid-track: gate buy-vs-build.** Per-item decided plan + reasoning is the
  source of truth in
  [gate buy-vs-build decisions](../charness-artifacts/audit/2026-06-19-gate-buy-vs-build-decisions.md)
  — read it first.
- **Batch done, UNPUSHED (operator HELD push)** — count via
  `git log --oneline origin/main..HEAD`: A (bootstrap + #390), B triage, B DROP
  #1/#2 (advisory), ① validate_critique_packet DELETE, ③ function-length -> ruff
  PLR0915 (bespoke arm deleted), ④ doc near-dup -> nose Markdown advisory +
  **nose now REQUIRED >=0.13.0**; plus find-skills repo-only regen and a
  critique-fix commit. All fresh-eye SHIP + verification-lock green; Cautilus
  next-action `none` throughout.
- **Batch critique (4 angles + counterweight), fixes applied:** C1 nose.json
  `constraint` prose -> `>=0.13.0` (doctor now flags old nose) + C2 `--require-nose`
  fails closed on nose `error` too. Deferred C3 (validate_integrations parse-check),
  C5 (docs-only doc-dup -> item 5). Detail in the 2026-06-19 critique artifact.
- **Env change:** this machine's nose was upgraded 0.10.0 -> 0.13.0 so the new
  required gate passes locally.
- length-config WITHDRAWN (caps stay hard). lychee on PATH, not yet managed.

## Next Session

- **Remaining gate items** (each its own slice, B1 advisory pattern, fresh-eye +
  broad closeout):
  - **2.** doc-links -> lychee BUY (managed integration; move broken-link
    resolution to lychee); keep the repo-root-placeholder + repo-boundary residue;
    demote backtick/bare-mention to advisory (keep the principle).
  - **Demotions:** validate_critique_artifacts (keep tier-honesty, demote rest);
    validate_skill_ergonomics (export-leak arm only — DELICATE adapter split).
- **Item 5 (spec'd + reviewed, slice 1 in progress):** boy-scout dup ratchet.
  Canonical [spec](../charness-artifacts/spec/boy-scout-dup-ratchet.md):
  escalation-ladder, [quality-adapter.yaml](../.agents/quality-adapter.yaml)-tuned,
  **standalone portable gate script** for consumers. **Folds in
  [nose#463](../charness-artifacts/gather/2026-06-19-nose-463-v013-output-observations.md)**
  (operator-filed from ceal): use one `families[]` enumeration for counts (NOT
  `--fail-on`; ~95 gap), classify on structural fields not dashboard labels, pin
  the nose command/shape behind a resolver. Slice 1 DONE: `family_id` (5226ad9f).
  NEXT: `dup-review.json` + `quality` seed step + tests; slice 2 = gate script +
  adapter wiring + portability/escalation tests (closes C5).
- **Untouched tracks (original primary):** **C.** SRP / skill-body reduction
  (~12 SKILL.md near the 200-line cap; separate-or-delete, NOT line-shave;
  AGENTS.md `## Skill Routing` is GENERATED by setup's
  [render_skill_routing.py](../skills/public/setup/scripts/render_skill_routing.py)
  — needs a lockstep generator edit). **D.** #391 extractions + tool_version stamp.

## Discuss

- Push timing (operator HELD; batch ready post-critique-fix).
  validate_skill_ergonomics adapter-split appetite. Untouched: #391, #387, #371.

## References

- [decisions](../charness-artifacts/audit/2026-06-19-gate-buy-vs-build-decisions.md),
  [item-5 design](../charness-artifacts/audit/2026-06-19-boy-scout-dup-ratchet-design.md),
  [recent-lessons](../charness-artifacts/retro/recent-lessons.md),
  [design north star](./design-north-star.md)
