# charness Handoff

## Workflow Trigger

- 다음 세션이 repo operating contract, prompt/skill surface, export sync, 또는 checked-in artifact policy를 건드리면 이 handoff부터 읽고 `impl`로 이어간다.
- 반복 실수 방지는 [charness-artifacts/retro/recent-lessons.md](../charness-artifacts/retro/recent-lessons.md)를 함께 읽는다.

## Current State

- startup에는 `charness:find-skills` 1회가 필수이고, durable capability inventory는 [charness-artifacts/find-skills/latest.md](../charness-artifacts/find-skills/latest.md)와 `latest.json`이 owning surface다.
- [AGENTS.md](../AGENTS.md)는 craken-style compact entry다. 매 세션 즉시 필요한 Start Here / Subagent Delegation / Phase Rules만 inline이고, 세부 운영 규칙은 [docs/conventions/](./conventions/)가 owner다.
- README-first thin bootstrap contract가 이제 public skill/reference/helper/adapters/control-plane까지 맞춰졌다. default surface는 더 이상 `INSTALL.md`/`UNINSTALL.md`를 전제하지 않고, repo-owned next step은 README Quick Start + `doctor --next-action` 류 surface가 owner다.
- README/CLI/install 분리는 closed. top-level [README.md](../README.md)는 value + Quick Start, command surface는 generated [docs/cli-reference.md](./cli-reference.md), install/update는 [packaging/bootstrap-python.json](../packaging/bootstrap-python.json) + [packaging/bootstrap-requirements.txt](../packaging/bootstrap-requirements.txt)가 owner. brew는 control plane에서 제거. install/doctor가 host install vs repo onboarding을 분리해서 본다.
- `critique`과 `quality`/`handoff`의 fresh-eye loop는 subagent가 canonical이 아니라 mandatory다. bounded capability probe 뒤에도 spawn이 안 되면 same-agent fallback으로 대체하지 말고 concrete host signal을 남긴 채 stop한다.
- `init-repo` 기본 AGENTS guidance와 checked-in [AGENTS.md](../AGENTS.md) 모두 repo-mandated bounded fresh-eye subagent review를 "already delegated" 규칙으로 surface한다. second-message 대기를 금지하고, host spawn restriction을 explicit blocker로 남긴다.
- [`docs/handoff.md`](./handoff.md)는 rolling pointer다. gate 수치, transient pre-push timing, release 상태, dogfood evidence, 긴 history는 owning artifact로 보내고 여기에는 next action을 바꾸는 사실만 남긴다.
- artifact policy는 이제 `history-default, latest optional` 쪽으로 재분류 중이다. 기준 owner는 [docs/artifact-policy.md](./artifact-policy.md)와 [charness-artifacts/spec/artifact-history-default-reclassification.md](../charness-artifacts/spec/artifact-history-default-reclassification.md)다.
- checked-in plugin export가 걸린 source를 바꾸면 validator보다 먼저 `python3 scripts/sync_root_plugin_manifests.py --repo-root .`로 sync한다.
- machine-local discovery output인 `.agents/charness-discovery/`는 checked-in surface가 아니다. support/integration dry-run은 `python3 scripts/sync_support.py --repo-root . --json`와 `python3 scripts/update_tools.py --repo-root . --json`로 확인하고, generated local stubs는 drift로 커밋하지 않는다.
- prompt-affecting surface는 [.agents/cautilus-adapter.yaml](../.agents/cautilus-adapter.yaml)의 `run_mode: auto|ask|adaptive|disabled`와 planner가 owning contract다. 현재 root adapter는 Cautilus 전면 재작업 중이라 `disabled`이며, Cautilus 바이너리/평가는 돌리지 않는다.
- Cautilus는 generic review/closeout/검증 trigger가 아니다. Re-enable 전에는 deterministic gate와 adapter-disabled proof validator가 closeout을 맡고, 재작업 후에만 `cautilus.evaluation_input.v1` + `cautilus eval test/evaluate` proof seam을 복구한다.
- Quality closeout에서 `Weak`, `Missing`, `Advisory`, delegated-review status, active next gates를 최종 응답에서 숨기지 않는다. `prompt_asset_roots: []`는 canonical asset root 미선언일 뿐 inline prompt/content bulk inventory opt-out이 아니다.
- quality/runtime 숫자와 current review posture는 [charness-artifacts/quality/latest.md](../charness-artifacts/quality/latest.md), release/version truth는 [charness-artifacts/release/latest.md](../charness-artifacts/release/latest.md)와 `python3 skills/public/release/scripts/current_release.py --repo-root .`가 owner다.
- `narrative` widening v0.5.0, AGENTS.md craken refactor, 2026-04-26 clean-clone first-move dogfood, tokei self-application, `narrative` cross-repo issue hygiene, agent-facing CLI prep/execute lens ([#48](https://github.com/corca-ai/charness/issues/48) scope a), #78 quality lesson, **#135 Leg 5 (premortem → critique rename, commit `65bb5f9`, [scripts/check_premortem_rename.py](../scripts/check_premortem_rename.py) 79/0, cite anchor 138→134→79)** 은 모두 closed durable architecture. 세부는 owning spec/retro/release 아티팩트가 가진다. PR 1 시점에 spec line 138 cite를 정정 — substrate wording owner는 `closeout-discipline.md`가 아니라 [AGENTS.md](../AGENTS.md) line 30 (+ [CLAUDE.md](../CLAUDE.md) mirror)이다.
- worktree readiness contract가 landing됐다. 사용 리포가 `.agents/worktree-adapter.yaml` (스키마는 [integrations/worktree/manifest.schema.json](../integrations/worktree/manifest.schema.json), 예시는 [integrations/worktree/adapter.example.yaml](../integrations/worktree/adapter.example.yaml))에 prepare 명령과 추가 doctor 체크를 등록하면, `charness worktree doctor`/`charness worktree prepare`가 canonical 체크(git common dir, hooksPath, lefthook shim resolution, husky `_/`)와 manifest 체크를 합쳐 실행한다. owner는 [scripts/worktree_doctor_lib.py](../scripts/worktree_doctor_lib.py), [scripts/worktree_doctor.py](../scripts/worktree_doctor.py), [scripts/worktree_prepare.py](../scripts/worktree_prepare.py), [tests/charness_cli/test_worktree_doctor.py](../tests/charness_cli/test_worktree_doctor.py)다. `impl`/`hitl` bootstrap이 `charness worktree doctor --json`을 non-fatal probe로 호출하고, 실패 시 `charness worktree prepare`를 next-action으로 surface한다 (자동 실행 X). 운영 surface는 [docs/worktree-prepare.md](./worktree-prepare.md)이고, init-repo seed helper는 [skills/public/init-repo/scripts/seed_worktree_adapter.py](../skills/public/init-repo/scripts/seed_worktree_adapter.py)다. spec은 [charness-artifacts/spec/worktree-prepare-doctor.md](../charness-artifacts/spec/worktree-prepare-doctor.md)에 산다.
- [#108](https://github.com/corca-ai/charness/issues/108)/[#109](https://github.com/corca-ai/charness/issues/109)이 닫혔다. #108 claim은 [skills/public/find-skills/references/support-consumption.md](../skills/public/find-skills/references/support-consumption.md)의 `Discoverability Claim` 섹션이 owner이고, proof seam은 [tests/test_find_skills_task_recommendations.py](../tests/test_find_skills_task_recommendations.py)다. #109은 design extraction 한정으로 [charness-artifacts/spec/issue-109-spec-authoring-discipline.md](../charness-artifacts/spec/issue-109-spec-authoring-discipline.md)만 landed했고, implementation 패스는 그 spec의 3-of-5 evidence-required gate (Cautilus rework re-enable, user/maintainer HITL maturity, narrative v0.5.0 dogfood misses, second substrate 관찰, fresh-eye review)가 통과한 다음에 시작한다.
- charness가 ship한 manifest는 사용자 리포에서도 자동 노출된다. discovery 흐름 (`quality`/`narrative` `list_tool_recommendations`, `find-skills` `list_capabilities`)은 plugin-shipped manifest를 fallback으로 머지하고, 같은 `tool_id` 충돌 시 user 우선이다. 사용자 리포가 의존성을 명시하고 싶으면 [integrations/tools/dependencies.json](../integrations/tools/dependencies.json) (스키마는 같은 디렉토리)에 staging하면 추천 payload에 `staged: true|false`로 노출된다. seeding 헬퍼는 `python3 skills/public/init-repo/scripts/seed_dependencies.py --repo-root . --from-recommendations`이고, repo-owned helper인 `python3 scripts/install_tools.py --execute --add-dependency <tool-id>`도 자동 staging한다. tests는 `CHARNESS_DISABLE_PLUGIN_FALLBACK_MANIFESTS=1` autouse fixture로 fallback을 끈다.
- [#115](https://github.com/corca-ai/charness/issues/115)/[#116](https://github.com/corca-ai/charness/issues/116) 닫힘. `charness worktree doctor`/`prepare`는 `EMBEDDED_REPO_ROOT`가 None일 때(`~/.local/bin/charness` shim 케이스) `resolve_repo_root` fallback으로 managed checkout을 자동 로드한다. owner는 [charness](../charness)의 `_resolve_worktree_lib_root`/`_load_worktree_lib`이고, 새 인자 `--home-root`/`--charness-checkout`는 다른 명령들과 일관. `charness update`는 cache 디렉토리 로테이션을 감지해 `session_staleness` 페이로드 + 사람용 경고를 출력한다 (`diff_cache_entries`/`session_staleness_payload`/`_print_session_staleness`). [resolve_skill_path.py](../skills/public/find-skills/scripts/resolve_skill_path.py)는 `--marketplace`/`--plugin`을 받아 임의 Codex 플러그인의 캐시(예: `openai-curated/github`)도 fallback resolve한다. proof seam은 [tests/charness_cli/test_worktree_doctor.py](../tests/charness_cli/test_worktree_doctor.py), [tests/charness_cli/test_codex_cache_refresh.py](../tests/charness_cli/test_codex_cache_refresh.py), [tests/test_resolve_skill_path_arbitrary_plugin.py](../tests/test_resolve_skill_path_arbitrary_plugin.py)다.
- [#122](https://github.com/corca-ai/charness/issues/122)–[#127](https://github.com/corca-ai/charness/issues/127) (commits `044d7fe`, `5ace071`) 닫힘. `issue` skill backend가 adapter-aware (`issue_backend.{id, binary, commands}`); preflight `selected_backend` 보고; HITL chunk Agent Assessment + Recommended Disposition (display-only) invariant; 5단계 proof ladder ([external-capability-proof-ladder.md](../skills/shared/references/external-capability-proof-ladder.md)) 도입; `issue new` closeout은 verified ledger에서만 렌더; target은 세션 durable state라 retry 시 `target_unavailable` fail-closed; 외부-출처 이슈는 canonical source identity 필수. owner는 [skills/public/issue/](../skills/public/issue/) 하위 backend ref + closeout-discipline.md.
- [#128](https://github.com/corca-ai/charness/issues/128)–[#134](https://github.com/corca-ai/charness/issues/134) (commits `4bc2e29`, `ca00142`, `e58988d`, `5d48956`) 닫힘. **Slice A**: issue skill `--repo-root` 기본 cwd; HITL `check_chunk_contract.py` self-check; shared `agent-assessment-invariant.md` + 7개 chunk surface cite. **Slice B**: critique `first-reader` angle (4 sub-lenses) + [check_title_slug_drift.py](../scripts/check_title_slug_drift.py) deterministic checker. **Slice C**: `release_backend` adapter (`{tag}`/`{title}` substitution); shared `closeout-discipline.md` (Verified Ledger / Target Durability / External-Source Identity); 5 consumer skill anchor + cite. **Slice D**: `issue_tool.py close-with-comment` 헬퍼 (file-backed comment+close, partial-state error); 5개 public skill references 추가 contract (announcement digest density, gather official-URL-first, create-skill ownership-overlap + message-shape regression, debug durable follow-through, impl completion report categories); create-skill downstream materialization drift contract.
- 2026-05-09 short-follow-up sweep (commits `c4feeb3`, `f18afc3`, `7c13b73`, `7d9c575`, `cfbd791`) 닫힘. HITL SKILL.md step 12 inline cite (`agent-assessment-invariant.md`); fb87060 wording drift assertion repair; issue `_resolve_op` per-op placeholder allowlist (#134); ownership-overlap 런타임 validator + allowlist (#132, [scripts/check_skill_ownership_overlap.py](../scripts/check_skill_ownership_overlap.py)); release `backend_command` 동형 per-op allowlist 백포트 (Slice C+D 통합).

## Next Session

1. **다음 first move = #135 PR 2 (Leg 2 t-events adapter manifest + emit point + Leg 1 inventory bundle)**. spec [charness-artifacts/spec/issue-135-t-first-self-evolving-unit.md](../charness-artifacts/spec/issue-135-t-first-self-evolving-unit.md) 의 Leg 2 + Leg 1 sub-section이 owner. 다음 sequencing: PR 3 (Leg 3 Engelbart anchor), PR 4 parallel (Leg 4 operator-T doc), PR 5 after PR 2 (Leg 6 `init-repo` → `setup` rename + `seed_t_events_adapter.py`).
2. `git status --short`를 먼저 본다.
3. Cautilus rework가 끝나기 전까지 README proof ledger와 `../cautilus/bin/cautilus claim discover` 연결은 보류한다. 재개 시 Discovery output은 `cautilus.claim_proof_plan.v1` 계획이지 verdict가 아니므로, stable acceptance criteria와 direct evidence ref를 별도로 확정한다.
4. read-only blocker의 no-write seam은 landed다. 남은 작업은 (a) workspace-write workflow proof carrier 정의 (`--sandbox workspace-write` dogfood가 durable artifact + 관련 state-cache write를 깨끗이 끝내는 것을 증명) 와 (b) Cautilus adapter re-enable 후 routing eval에 `--read-only` 실제 와이어링이다. spec은 [charness-artifacts/spec/readme-proof-cautilus-eval-migration.md](../charness-artifacts/spec/readme-proof-cautilus-eval-migration.md)다.
5. README-specific Cautilus fixtures는 Cautilus adapter를 `disabled`에서 되돌린 뒤 추가한다.
6. README/help text를 다시 건드리면 먼저 `python3 scripts/render_cli_reference.py --repo-root . --output docs/cli-reference.md`로 command reference를 재생성하고, export surface가 걸리면 `python3 scripts/sync_root_plugin_manifests.py --repo-root .`를 validator보다 먼저 끝낸다.
7. public skill, handoff, validator, 또는 other prompt surface를 건드리면 [charness-artifacts/quality/latest.md](../charness-artifacts/quality/latest.md)와 [docs/public-skill-dogfood.json](./public-skill-dogfood.json)을 읽고 `impl`로 이어간다.
8. tokei follow-ups: 다음 두 SKILL.md wiring은 모두 deferred다 — `quality` SKILL.md 본문에서 [inventory_sloc.py](../skills/public/quality/scripts/inventory_sloc.py)를 참조하는 한 줄, 그리고 `init-repo` SKILL.md 본문에서 [seed_dependencies.py](../skills/public/init-repo/scripts/seed_dependencies.py)를 참조하는 한 줄. cautilus refactor가 끝난 뒤 한 슬라이스로 묶어 cautilus regression proof와 함께 commit한다.
9. 새 `inventory-sloc` quality phase는 ~96ms로 빠르지만 아직 runtime budget에 등록 안 됐다. 다음 슬로우-게이트 패스 때 다른 unbudgeted hot spot과 묶어 등록한다.
10. quality phase write-policy 슬라이스가 landing됐다. 다음 슬라이스 가능 후보는 (a) 두 번째 artifact-writing phase가 들어올 때 runner가 adapter `quality_phases`로 dispatch하도록 일반화, (b) `CHARNESS_GATE_MODE` 같은 cross-skill mode env로 generalize.
11. **남은 deferred follow-ups (2026-05-09 sweep 후):**
    - **`quality` sweep gate (#124 follow-up)** — external-capability 슬라이스가 readiness-only proof로 닫히는 패턴 자동 감지. 현재 ladder는 prose-only enforcement.
    - **Defensive runtime layer (critique이 Valid-but-Defer)** — (a) `issue` 두-콜 retry runtime fixture (#126), (b) `issue_tool.py validate-body --external-source` 런타임 린터 (#127), (c) `.charness/issue/` durable session-state 레이어 (#126의 더 강한 형태).
    - **HITL chunk_contract marker robustness (#129 follow-up)** — `check_chunk_contract`가 `?`/`Decision Needed`를 안 쓰는 결정 chunk(예: "Approve or revise.")를 놓친다. marker 셋 확장은 다음 회 같은 트랩 반복 시 짓는다.
    - **HITL applied-rewrite (step 7) inline cite (#129 follow-up)** — step 12는 inline cite landed (`c4feeb3`). step 7의 `applied_rewrite_review_status` 흐름은 "ask accept-or-revise"로 끝나며 invariant cite가 본문에 없다. 같은 트랩 재발 시 짓는다.
    - **Title-slug drift `--strict` CI/pre-push wiring (#131 follow-up)** — [check_title_slug_drift.py](../scripts/check_title_slug_drift.py)는 advisory-only로 ship됐다. Leg 5 critique rename (commit `65bb5f9`)이 첫 dogfood; pre-push wiring은 다음 슬라이스.
    - **`quality` proposal-flow 도큐 hygiene 추가 entries** — `Status`/`Changelog`/`Notes` 같은 structural-H1 entries 확장은 false positive 발생 시 짓는다.
    - **Support-skill ownership-overlap (#132 follow-up)** — 현재 [check_skill_ownership_overlap.py](../scripts/check_skill_ownership_overlap.py)는 `skills/public/`만 스캔한다. `gather-notion`/`gather-slack`이 `gather` 네임스페이스를 공유하는 parent-child 패턴은 의도지만, support-skill 범위로 validator를 확장할지는 별도 dogfood 후 결정.
    - **release file-backed close-with-comment 본격 포팅** — `cfbd791`은 per-op placeholder allowlist만 백포트했다. release 측에서 file-backed two-op flow가 실제 필요해질 때 `issue_close.close_with_comment` shape를 옮긴다.
    - **Cautilus rework 풀린 뒤** — Slice A-D에서 추가된 5개 shared reference (`agent-assessment-invariant.md`, `closeout-discipline.md`)와 7개 skill 추가 contract(announcement/gather/create-skill/debug/impl 5종 + #133 packaging 2종)에 대한 [evals/cautilus/scenarios.json](../evals/cautilus/scenarios.json) scenario coverage 검토; `agent_choice`/`provider_roundtrip` proof level 채우기.

## Discuss

- 이 [docs/handoff.md](./handoff.md)와 [charness-artifacts/quality/latest.md](../charness-artifacts/quality/latest.md)의 freshness validator를 현재 stale-claim check 너머 어디까지 확장할지는 아직 열려 있다.
- `cautilus` planner가 아직 `goal`을 사실상 `preserve`로만 둔다. named-anchor/reasoning-frame change를 언제 `improve` + compare path로 올릴지는 dogfood를 더 봐야 한다.
- README, CLI reference, operator docs 사이 overlap을 얼마나 더 줄일지는 아직 열려 있다. 지금은 top-level story vs generated command reference 분리까지 닫았고, consumer-repo dogfood에서 실제 탐색 마찰을 본 뒤 더 줄일지 결정한다.
- Cautilus claim discovery output을 checked artifact로 둘지, 아니면 Specdown report build 때 생성되는 transient proof-plan input으로 둘지는 아직 열려 있다.
- release artifact가 GitHub release creation/public verification state를 얼마나 자세히 닫아야 하는지는 helper contract 차원에서 한 번 더 볼 가치가 있다.
- `Discuss`는 unresolved decision만 남기고, metrics/history/closed proof는 owning artifact로 계속 밀어내는 방향을 유지한다.

## References

- [charness-artifacts/quality/latest.md](../charness-artifacts/quality/latest.md)
- [charness-artifacts/cautilus/latest.md](../charness-artifacts/cautilus/latest.md)
- [charness-artifacts/spec/readme-proof-cautilus-eval-migration.md](../charness-artifacts/spec/readme-proof-cautilus-eval-migration.md)
- [charness-artifacts/retro/recent-lessons.md](../charness-artifacts/retro/recent-lessons.md)
- [docs/public-skill-dogfood.json](./public-skill-dogfood.json)
