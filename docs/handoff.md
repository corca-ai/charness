# charness Handoff

## Workflow Trigger

- handoff, `구조적 가드`, 또는 `structural guard` 멘션되면 [Next Session](#next-session) item 1 (script-silence layer-3 가드) 을 시작한다. handoff 자체는 모든 task-oriented 세션이 무조건 읽는 pickup pointer다 ([AGENTS.md](../AGENTS.md) Start Here).
- repo operating contract, prompt/skill surface, export sync, 또는 checked-in artifact policy를 건드리면 [charness-artifacts/retro/recent-lessons.md](../charness-artifacts/retro/recent-lessons.md)를 같이 읽고 `impl`로 이어간다.
- 세션 시작 시 `git status` / `git log origin/main..HEAD` / `gh issue list --state open` 로 live 상태 확인.

## Current State

- [AGENTS.md](../AGENTS.md)는 craken-style compact entry, 세부 운영 규칙은 [docs/conventions/](./conventions/)가 owner. handoff 본 파일은 rolling pointer — gate 수치, dogfood evidence, 긴 history는 owning artifact로 보내고 next action을 바꾸는 사실만 남긴다.
- `critique`/`quality`/`handoff`의 fresh-eye loop는 mandatory subagent다. spawn 불가 시 same-agent fallback 금지, host signal 남기고 stop. `setup`/`AGENTS.md`가 "already delegated"로 surface한다 (second-message 대기 금지).
- prompt-affecting surface는 [.agents/cautilus-adapter.yaml](../.agents/cautilus-adapter.yaml) `run_mode`가 owner. 현재 `ask` (eval-only re-enabled per corca-ai/cautilus#32) — `cautilus eval test` / `cautilus eval evaluate` 만 지원. prompt diff 자체는 live Cautilus trigger가 아니며, 기존 route-only fixture는 deterministic sentinel/legacy 자산이다. 실제 Cautilus proof artifact는 `Behavior Source`가 있는 log-backed behavior proof일 때만 갱신한다. claim discovery, optimize, review-learning, `eval live`, broader Agent orchestration은 contract rewrite 중이라 호출 금지.
- checked-in plugin export가 걸린 source를 바꾸면 validator 전에 `python3 scripts/sync_root_plugin_manifests.py --repo-root .`로 sync. 같은 원칙으로 README/help text를 건드리면 먼저 `python3 scripts/render_cli_reference.py --repo-root . --output docs/cli-reference.md`.
- charness가 ship한 manifest는 사용자 리포에서도 자동 fallback 노출 (`tool_id` 충돌 시 user 우선). 사용자 리포는 [integrations/tools/dependencies.json](../integrations/tools/dependencies.json)에 staging.
- worktree readiness contract land — `.agents/worktree-adapter.yaml`로 prepare/doctor 등록, `impl`/`hitl` bootstrap이 `charness worktree doctor --json`을 non-fatal probe로 호출. owner는 [scripts/worktree_doctor_lib.py](../scripts/worktree_doctor_lib.py) + [docs/worktree-prepare.md](./worktree-prepare.md).
- artifact policy는 `history-default, latest optional` 재분류 중. owner는 [docs/artifact-policy.md](./artifact-policy.md).
- quality/runtime 숫자는 [charness-artifacts/quality/latest.md](../charness-artifacts/quality/latest.md), release truth는 [charness-artifacts/release/latest.md](../charness-artifacts/release/latest.md)와 `python3 skills/public/release/scripts/current_release.py --repo-root .`가 owner.
- 기 closed durable architecture (#108–#154 + v0.5.25 release, narrative widening v0.5.0, AGENTS.md craken refactor, worktree readiness, #136-#138 declared-parity trio, #139 first bug-class dogfood, #145 script-silence helper-layer fix)는 [charness-artifacts/spec/](../charness-artifacts/spec/) / [charness-artifacts/release/](../charness-artifacts/release/) / [charness-artifacts/retro/](../charness-artifacts/retro/) + close-comments가 owner (이슈/PR 번호 grep으로 reload).

## Next Session

1. **Script-silence family layer-3 structural guard (사용자 commit 2026-05-13, 다음 슬라이스).**
    - **왜 지금 짓는가**: 같은 anti-pattern (rich state → consumer collapses to headline)이 세 레이어에서 누적 관찰됐다. (a) **helper layer** — #145, closed 2026-05-11 (`08e2c3a`): 세 helper가 drifted/missing/intentional-empty를 하나의 benign payload로 collapse. closeout: [charness-artifacts/debug/2026-05-11-issue-145-script-silence-closeout.md](../charness-artifacts/debug/2026-05-11-issue-145-script-silence-closeout.md). (b) **agent-consumes-inventory layer** — 2026-05-13 (`3cf99b8`): quality 리뷰가 `## Commands Run`에 inventory를 cite하면서도 `status`/`heuristics` 헤드라인만 인용. [validate_inventory_consumption.py](../scripts/validate_inventory_consumption.py) + [inventory-consumer-fields.json](../skills/public/quality/references/inventory-consumer-fields.json) (5개 inventory 선언, `ENFORCED_FROM_DATE: 2026-05-13`) 으로 닫힘. (c) **agent-consumes-adapter layer** — 2026-05-13 미해결로 관찰: [cautilus-adapter.yaml](../.agents/cautilus-adapter.yaml)이 `run_mode: ask`, "log-backed required", "legacy route-only sentinel" 코멘트를 노출함에도, 모호한 user phrase + 바이너리 가용성 + `evaluation_input_default` 한 줄을 보고 agent가 legacy fixture로 [plan_cautilus_proof.py](../scripts/plan_cautilus_proof.py) consult 없이 `cautilus eval test` 호출 (planner는 `next_action: "none"` 반환했었음). 가드 없으면 layer-4 재발 거의 확실.
    - **읽기 우선 surface**: (i) [`.agents/cautilus-adapter.yaml`](../.agents/cautilus-adapter.yaml) 의 contract state, (ii) [scripts/plan_cautilus_proof.py](../scripts/plan_cautilus_proof.py) 의 advisory 출력 (`required`/`status`/`next_action`/`must_ask_before_running`/`notes`), (iii) [CLAUDE.md](../CLAUDE.md) `Start Here` 의 기존 cautilus 한 줄 ("deterministic gates still own closeout when no eval is requested" — 워딩 있음, 강제력 없음), (iv) [scripts/validate_inventory_consumption.py](../scripts/validate_inventory_consumption.py) + [skills/public/quality/references/inventory-consumer-fields.json](../skills/public/quality/references/inventory-consumer-fields.json) (v2 hardenings 베이스), (v) 본 handoff item 6 의 `#145 v2 + Cautilus drift` deferred bullet — rationale.
    - **권장 슬라이스 구성 — 4 묶음 (A/B/C Cautilus side + D #145 v2)** (small → large blast radius). **A (필수)** Cautilus-side agent contract anchor: SKILL.md (또는 새 reference 1쪽 cautilus-on-demand) + CLAUDE.md `Start Here` 라인 강화 — "cautilus eval test/evaluate 호출 전에 plan_cautilus_proof.py --json 을 consult; `next_action: "none"` 또는 `must_ask_before_running: true` + 명시된 failing-log 경로 없으면 호출 금지." [check_skill_contracts.py](../scripts/check_skill_contracts.py)에 신규 literal pin. **B (권장)** harness wrapper run_cautilus_eval.py: planner를 먼저 호출 → `next_action != "none"` + `--justification-log <path>` 필수 → 미충족 시 stderr refusal + exit 2. adapter `eval_test_command_templates` 라인은 이 wrapper 경유. **C (deferred, A+B 후)** validate_cautilus_call_provenance.py: [.cautilus/runs/](../.cautilus/runs/)의 최근 run마다 paired failing-log를 [cautilus/latest.md](../charness-artifacts/cautilus/latest.md) 또는 commit msg에서 찾고, 못 찾으면 fail. run-quality.sh 한 페이즈.
    - **D (같은 슬라이스, critique-derived) — #145 v2 hardenings**. A/B/C와 함께 한 묶음으로 land해야 슬라이스 완성; 분리하면 Layer 2가 stale 위험. (i) **drift check** validate_inventory_consumption_declaration.py — 각 declared inventory를 `--json`으로 실행해 선언된 field name이 실제 JSON 키인지 assert. (ii) **opt-in coverage** check_inventory_declaration_coverage.py — [skills/public/quality/scripts/](../skills/public/quality/scripts/) 의 `inventory_*.py` 15개 중 미선언 10개에 declaration 또는 `non_headline_fields: []` + `opt_out_reason` 강제. (iii) **gameability** validator를 `≥2 distinct fields per inventory`로 tighten. (iv) **SKILL.md anchor 분리** — 현재 [quality SKILL.md](../skills/public/quality/SKILL.md):117 producer-side dogfood anchor에 merged; 200줄 cap에서 별도 anchor 빼려면 다른 줄 trim 필요.
    - **Acceptance + why structural**: (1) fresh agent가 CLAUDE.md + handoff만 읽고 cautilus eval을 legacy fixture로 호출하지 못함 (새 worktree에서 동일 phrase로 거부 확인). (2) drift validator가 의도적 field rename에 fail. (3) coverage validator가 가짜 새 inventory의 declaration 누락에 fail. (4) [run-quality.sh](../scripts/run-quality.sh) 56+ phases pass. (5) `critique` bounded fresh-eye review `executed`. Layer 1·2는 prose-only일 때 fail → deterministic 가드 land 후 재발 안 함. Layer 3도 prose는 이미 있음 (adapter 코멘트, handoff `Current State`, CLAUDE.md cautilus 라인) — invoke-time consult 강제하는 호출 가드 없으면 prose 보강만으로 부족하다는 누적 증거.
2. **Open GitHub issue snapshot (refresh-on-pickup).** Live: `gh issue list --state open --limit 50 --json number,title,labels,createdAt,url`. 2026-05-13 KST 시점 2건 open: #155 (debug 스킬에 5 whys RCA 추가 — feature request + question, 작은 슬라이스), #156 (Teach create-cli intent-first command grammar from Cautilus cleanup — documentation + operations, 2026-05-13 신규). 이전 4-chunk plan (#146–#154 A/B/C/D)은 모두 closed되어 substrate 소실. item 1 가드와 독립적으로 어느 한 이슈를 닫고 싶을 때 진입.
3. **Cautilus dogfood** = read-only blocker no-write seam dogfood. workspace-write workflow proof carrier 정의 (`--sandbox workspace-write` dogfood) + Cautilus adapter re-enable 후 routing eval에 `--read-only` 와이어링. spec은 [charness-artifacts/spec/readme-proof-cautilus-eval-migration.md](../charness-artifacts/spec/readme-proof-cautilus-eval-migration.md). README-specific Cautilus fixtures + README proof ledger + `cautilus claim discover` 연결은 모두 Cautilus re-enable 후.
4. public skill, handoff, validator, prompt surface를 건드리면 [charness-artifacts/quality/latest.md](../charness-artifacts/quality/latest.md)와 [docs/public-skill-dogfood.json](./public-skill-dogfood.json)을 읽고 `impl`로 이어간다.
5. **#139 follow-up 슬라이스**. (i) 7개 dormant sibling 매니페스트 trigger 내용은 operator dogfood 후 tighten — counterweight가 `vulture "dead code"/"stale module"`, `tokei "lines of code"/"code count"`, `agent-browser "playwright"/"headed verification"`, `glow "narrative render"/"rendered review"`을 over-broad 가능성으로 flag. (ii) `support_recommendation_note` contract를 `references/support-consumption.md`에 문서화 + 같은 슬라이스에서 cautilus eval로 latest.md 갱신 (이번 commit엔 doc edit이 cautilus-proof gate를 trigger해 빠짐). (iii) advisory를 `external_binary_with_skill` fail-closed로 tighten은 (i) dogfood 후. P4 sequencing은 본 dogfood로 결정 — cite-only default 유지.
6. Active deferred follow-ups:
    - **4 missing runtime budgets + operator-acceptance Progressive Operator Path 분리** (2026-05-10 quality 발견). **부분 land 2026-05-12**: Progressive Operator Path는 [docs/operator-progressive-path.md](./operator-progressive-path.md)로 분리됨; `local-linux-x86_64-36cpu.budgets`에 `check-spec-evidence-durability: 2500`/`inventory-ci-local-gate-parity: 500` 추가됨 (`inventory-sloc`/`inventory-ubiquitous-language`는 prior 슬라이스에서 land). **잔여**: `local-linux-aarch64-4cpu` profile은 2026-05-13에 unbacked-guess 사유로 제거됨; 다시 aarch64 host에서 작업이 시작되면 `record_quality_runtime.py` 실측 후 profile 재추가. prompt-affecting adapter edit라도 deterministic gates가 기본 closeout이며, live Cautilus는 실제 failing prompt/log가 있는 behavior proof일 때만 사용한다.
    - **Compact-AGENTS.md routing discriminator 보강 — legacy route-only follow-up.** 두 cautilus run에서 `compact_with_startup_bootstrap` 변종이 매번 다른 1/5 fail; 2026-05-10 rename refresh는 우연히 5/5 (단일 clean run은 robustness 증명 X). 이 항목은 더 이상 루틴 live Cautilus closeout이 아니다. 실제 compact-routing 실패 로그가 다시 나오면 log-backed fixture로 재작성하고, 그전에는 deterministic fixture/schema validation만 유지한다.
    - **#136 spec evidence-durability cross-skill rollout.** 신규 reference + validator는 `charness-artifacts/{spec,quality,release,dogfood,debug,premortem,design-studies}/` scope. impl/release/quality SKILL.md 본문에 `evidence-durability.md` 인용 추가는 별도 슬라이스. Selected Evidence shape (Claim/Source/SHA-256/Selected fields/Captured) 을 위한 신규 scaffold 스크립트 도 첫 dogfood retro 후 정당화.
    - **#137 CI/local gate parity adapter wiring + ecosystem coverage.** adapter `coverage_floor_policy.ci_workflow_glob`/`gate_commands`를 helper가 consume해서 dual source-of-truth 해소 + pyproject/Ruby/PHP/Rust/JVM gate-command default 패턴 추가 (예: `hatch run quality`, `tox -e ci`, `nox -s verify`, `uv run pytest`, `poetry run check`, `bundle install`, `composer install`, `cargo`, `gradle`, `mvn`) + 3-state rubric (`healthy/missing/intentional`) + `--require-empty-parity-issues`/`--require-canonical-gate-match` default-on — 모두 첫 consumer report 후.
    - **#138 gather symlink cross-skill rollout.** [docs/conventions/operating-contract.md](./conventions/operating-contract.md) `Pointer-Write Discipline` 단락 land (2026-05-09) — cross-skill recurrence 가드 part 1. 잔여: `write_record`-style 안전 writer 를 shared helper 로 promote (charness 내 `latest.*` writer가 더 등장할 때) + 실제 symlinked latest.md 가진 consumer 리포에서 #138 dogfood 1회 후 본격 land. `validate_current_pointer_freshness.py:19-22` `CURRENT_POINTERS` enumeration 확장은 대응 `STALE_POINTER_PHRASES` 항목 없이는 무동작 — pointer-content invariant이 정해질 때 같이 한다.
    - **#135 PR 5 follow-up — legacy `init-repo` snippet detection의 N-release deprecation timer.** [scripts/setup_agent_docs_lib.py](../scripts/setup_agent_docs_lib.py)의 `fresh_eye_task_review_scope_uses_legacy_init_repo` advisory는 consumer migration 친화로 ship됨; fail-closed로 tighten할 시점은 사용자 인접 repo가 setup 어휘로 옮겨간 dogfood 1회 이상 관찰 후. 시점 결정 시 advisory를 `review_required`로 승격하거나 detection 자체 제거.
    - **#135 PR 3 follow-up — `skill_invoked`/`anchor_invoked` emit point.** Leg 3 anchor entry land 됨; `anchor_invoked` 한 번 emit이 자연스러운 dogfood (critique 또는 retro에서 anchor 선택 시점). 첫 dogfood retro가 Engelbart Falsifier 관찰 window도 같이 연다 — A3.3 manual smoke가 그 관찰로 충족. `skill_invoked`는 charness CLI 또는 host-injected hook 결정 후.
    - **Hard-rename orphan-dir cleanup contract (`charness update` fix 후속).** 5사이트의 `SKILL.md`-gated 필터를 `skill_iter` shared helper로 통합 (callsites: `public_skill_validation_lib.public_skill_ids`, `skill_t_inventory_lib.list_public_skill_ids`, `validate_skill_t_inventory._list_public_skill_ids`, `validate_packaging_install_surface.validate_exported_public_skills`/`validate_exported_support_assets`). 추가 valid-but-defer: (i) `charness update`에 SKILL.md 없는 `skills/public/<dir>` orphan prune (별도 safety story), (ii) rename guard 옆에 post-rename installed-copy orphan dir doctor.
    - **issue-135 spec historical Cautilus wording** — [charness-artifacts/spec/issue-135-t-first-self-evolving-unit.md](../charness-artifacts/spec/issue-135-t-first-self-evolving-unit.md) L185·L257·L401 의 "Cautilus disabled" 문구는 spec 작성 시점 historical context. preserved 상태이지만 다음 spec revision에 맞춰 갱신 가능.
    - **Upstream support-source drift deterministic gate (corca-ai/cautilus#32 root-cause prevention)** — `integrations/tools/*.json`의 `support_skill_source.path`가 upstream에 실제 존재하는지 [scripts/validate_integrations.py](../scripts/validate_integrations.py) 옆 online preflight (CI nightly 또는 `charness update doctor`)로 검사. v0.14.2 pin은 임시 완화일 뿐.
    - **Cautilus rework 풀린 뒤** — Slice A-D shared reference (`agent-assessment-invariant.md`, `closeout-discipline.md`)와 7개 skill 추가 contract에 대한 [evals/cautilus/scenarios.json](../evals/cautilus/scenarios.json) coverage 검토 + `agent_choice`/`provider_roundtrip` proof level 채우기. tokei follow-ups (`quality`/`setup` SKILL.md wiring 두 줄)도 cautilus regression proof와 같이 한 슬라이스로 묶는다.
    - **Defensive runtime layer (critique이 Valid-but-Defer)** — `issue` 두-콜 retry runtime fixture (#126), `issue_tool.py validate-body --external-source` 런타임 린터 (#127), `.charness/issue/` durable session-state (#126의 더 강한 형태). dogfood 누적 후 발화.
    - **debug ↔ issue substrate-vs-target Valid-but-Defer 잔여** — (i) `substrate_consumed:` field 를 causal-review.md Output Shape에 추가해 caller-side check (실제 dogfood에서 silent ignore 관찰될 때), (ii) `debug` standalone path를 위한 doctor/dogfood probe (scaffold_debug_artifact.py를 issue 없이 실행해 contract 검증), (iii) References-list alphabetical-ordering test, (iv) bidirectional `causal-review.md` ↔ `debug` cite — 모두 dogfood evidence 후.
    - **#139 deeper-defer**: (i) `_task_text_matches`의 단방향 substring 매처를 양방향/토큰화로 재설계하기 위한 별도 spec — false-positive 통제(stop word, 최소 토큰 특이도, 스코어링) 정책이 같이 들어와야 한다. (ii) `external_binary_with_skill` `intent_triggers` advisory를 fail-closed로 tighten — operator dogfood 1회 후 trigger 내용 검증된 다음. (iii) `skills/support/<id>/SKILL.md` 존재 vs 매칭 integration `intent_triggers` adequacy를 cross-correlate하는 validator — 한 쪽만 land될 때 silent miss를 잡는다. (iv) `STRONG_TASK_TRIGGERS_BY_SUPPORT_ID` (`list_capabilities_lib.py:10-24`) hardcoded specdown-only를 매니페스트 기반으로 migrate.
    - **#142 follow-ups (announcement source-surface preflight 후속)**. (i) `skills/public/announcement/scripts/collect_commits.py:38-57`이 여전히 `in_progress_sources`를 consume 안 함 — 현 fix는 *delivery* preflight에 invariant를 박았을 뿐, *collection* path는 그대로다. 다음 dogfood에서 step 2 record 누락이 또 보이면 collect_commits에 declared-source walker 추가. (ii) **shape-only-validator anti-pattern sibling sweep**: `scripts/quality_adapter_lib.py:37-51,150-170`, `scripts/cautilus_adapter_lib.py:27-28,96-104,188-189`의 path-list 필드 — shape만 검증하고 declared-vs-consumed gap 동일 의심. cross-skill 구조적 prevention은 7th instance가 같은 family에서 나면 그때 짓는다. **재-defer 트리거**: 다음 maintainer가 "한 번 패턴" heuristic으로 같은 family를 또 미루면 본 항목을 cite해 `Bundle Anyway`로 승격.
    - **#145 v2 hardenings + Cautilus on-demand drift 가드** — item 1 에서 한 묶음으로 land. 본 6번에는 evidence/rationale만, action plan은 item 1.
7. 새 quality 페이즈 미예산 — item 6의 4 missing budgets가 owner (`check-spec-evidence-durability`, `inventory-ci-local-gate-parity`, `inventory-sloc` + `inventory-ubiquitous-language`). quality phase write-policy 일반화 (adapter `quality_phases` dispatch / `CHARNESS_GATE_MODE`)는 두 번째 artifact-writing phase가 들어올 때.

## Discuss

- handoff/quality freshness validator를 stale-claim check 너머 어디까지 확장할지 — 열려 있다.
- `cautilus` planner의 `goal`을 `preserve`에서 `improve`+compare path로 언제 올릴지 — dogfood 더 봐야 한다.
- README/CLI reference/operator docs overlap을 더 줄일지 — consumer-repo dogfood로 탐색 마찰 본 뒤 결정.
- Cautilus claim discovery output을 checked artifact로 둘지 transient proof-plan input으로 둘지 — 열려 있다.
- release artifact가 GitHub release creation/public verification state를 얼마나 닫아야 하는지 — helper contract 차원에서 한 번 더 본다.

## References

- [charness-artifacts/quality/latest.md](../charness-artifacts/quality/latest.md)
- [charness-artifacts/cautilus/latest.md](../charness-artifacts/cautilus/latest.md)
- [charness-artifacts/spec/readme-proof-cautilus-eval-migration.md](../charness-artifacts/spec/readme-proof-cautilus-eval-migration.md)
- [charness-artifacts/retro/recent-lessons.md](../charness-artifacts/retro/recent-lessons.md)
- [docs/public-skill-dogfood.json](./public-skill-dogfood.json)
