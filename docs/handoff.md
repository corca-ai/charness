# Charness Handoff

## Workflow Trigger

- **Next pickup:** activate the [all-open-issues goal](../charness-artifacts/goals/2026-08-07-close-every-open-issue-declaration-to-verdict.md) and run **Slice 0** first. It covers all 19 open issues plus the 34 pre-existing test failures no goal owned. The [#514/#515/#518 goal](../charness-artifacts/goals/2026-08-07-repair-evidence-boundary-close-514-515.md) is SUPERSEDED — do not activate it; its committed pre-0 work is the successor's Slice 4 substrate.
- **Slice 0 is a prerequisite, not a preference.** Until it lands, every `pytest` verdict is read against a standing red baseline and a new regression is indistinguishable from the noise. Recount before acting: `python3 -m pytest tests/ -q 2>&1 | tail -1`, and `python3 scripts/publish_state_ledger.py --repo-root . --json` for the largest group's root cause. This handoff lost its publish-state claim block at `0659d5a0` (`git log --format=%h -- docs/handoff.md` then `git show <sha>:docs/handoff.md | grep -c publish-state`); the goal's Context Sources hold the measured per-group split.
- **Three ordering claims carry the plan** and are worth checking before you reorder anything: root before consumer (`#530` before `#518`/`#528`/`#526`), instrument before measurement before decision (`#532`/`#519`/`#520` before `#521` before `#523`/`#527`), and pay the refactor tax first (`#534` before the split-heavy slices).
- Read the [pre-0 retro](../charness-artifacts/retro/2026-08-07-pre0-issue-source-freeze-and-closeout-authorization.md) before adding any refusal: two were added and withdrawn there for firing where nothing could escape.

## Continuation Capability

- Keep semantic coverage, proof execution, evidence identity, execution root, final consumer, and external observation as separate claims.
- Do not let #518 consume #515's taxonomy, #515 consume #514's taxonomy, or any issue wait on another. Share fields only when one canonical projection producer per field and at least two real readers with identical semantics are proven; source facts may have separate producers.
- At irreversible boundaries, a green gate, `CLOSED` state, or local artifact is provisional; require a different observer and evidence channel.
- Any reviewed input change invalidates packet identity and the verification lock.

## Current State

- #516 and #517 are closed; use them only as typed regression fixtures, not new work.
- #515 is open and contains consumer-repo browser/sync/support-skill evidence whose fresh-eye proof is not yet closeout-grade.
- #514 is open and asks for deterministic closeout evidence assembly without weakening gates or building a monolithic orchestrator.
- #518 is open and requires a full Charness repair of adapter/preset/surface reconciliation and false-green final verdicts; its pinned consumer reproduction and sibling comparison are recorded in the [debug artifact](../charness-artifacts/debug/2026-08-07-issue-518-quality-declaration-reconciliation-debug.md).
- The current awiki run is non-clean evidence for #518: `awiki 0.5.0` was detected at `/home/hwidong/.cargo/bin/awiki`, and `awiki lint -root docs -recursive` returned exit 1 with 40 documents, 7 orphans, 0 islands, and 230 link-only lines. It must become an explicit quality integration dependency and final-artifact disposition; it is not a clean result.
- Existing Charness `check-doc-links`, `markdownlint`, `check-links-internal`, and `nose` document-duplicate review have distinct observed semantics from awiki's graph check. No deletion is authorized until a command-level overlap matrix proves a full replacement.
- The goal draft now binds all three issues in one lifecycle while preserving consumer ownership, independent carriers, and separate readbacks.
- Quality Core run `31118030353` for head `0e469e917c6fa1b07f0351da639ac4431f519acc` failed at GitHub action metadata with `Service Unavailable`; mutation was cancelled. Treat it as an external CI non-claim.

## Next Session

- Quality dependency checkpoint: add/validate `integrations/tools/awiki.json` pinned to `v0.5.0`/immutable revision `f65f8c43dbf0300609bdfdf823c09cba370222c6` and its synchronized plugin/dependency projection, run the doctor/readiness path, wire the `awiki-docs-graph` phase through `run-quality.sh` in full/read-only/explicit-label/docs-only pre-push modes, execute `awiki lint -root docs -recursive` in Charness, and make the final renderer—not the route—own the aggregate disposition. Full mode retains tracked receipt/raw evidence; read-only/pre-push uses an ephemeral receipt/raw pair without mutating tracked artifacts. Build the overlap matrix before considering deletion of any existing link, Markdown, or duplicate detector; delete only a fully subsumed gate with replacement proof.

1. Run `git status --short --branch` and reread the [awiki quality review](../charness-artifacts/quality/2026-08-07-quality-review.md). The live #514/#515/#518 bodies/comments and the adapter/owner inspection are already captured and frozen (steps 2-4 below are DONE — see the goal's Slice Log); re-validate rather than re-capture, and only re-freeze if a premise actually moved. Confirm the pinned #518 debug record and the read-only `cmanki`/`craken-agents` comparison remain the intended substrate.
2. Run the bounded pre-activation bootstrap: create/test the canonical adapter-resolved paginated source adapter through [the adapter resolver](../skills/public/issue/scripts/resolve_adapter.py) and the [issue adapter declaration](../.agents/issue-adapter.yaml), freeze validator, synchronized plugin projection, and the typed, semantics-neutral target/source authorization interface specified in the [goal](../charness-artifacts/goals/2026-08-07-repair-evidence-boundary-close-514-515.md). The receipt must preserve adapter/backend/template identity and refuse incomplete pagination or any capture omitting #514, #515, or #518. This authorization record is a narrow close-safety record; it cannot carry shared reader/applicability/verdict semantics until two equivalent non-validator readers are proven. Cover `issue_tool.py close-with-comment` with an explicit repository-qualified `manual_target_declaration`, commit-msg, draft validation, post-publication verify, and direct-carrier refusal fixtures for missing/stale crosswalk, wrong/combined/foreign target, stale source, wrong reader capture, and missing Slice-4 lock; assert the commit hook authorizes before temp-file creation and the close-with-comment path authorizes before comment/close calls. Make release preflight and closeout-message/resume paths explicitly refuse #514/#515/#518 before temp/bump/publication/issue-close until a separate release boundary. Apply authorization only to these protected targets and preserve generic behavior elsewhere. This is prerequisite operating-surface work only; it does not repair #514/#515/#518 or close an issue.
3. Capture all three issue responses completely with the planned canonical paginated source-capture adapter and its synchronized plugin projection; the three `gh issue view` commands are diagnostic cross-checks only. The adapter receipt must include command/args, every page/cursor response, bodies, every comment node, per-issue `totalCount`, raw-response digest, and a pagination-complete marker; refuse unknown/incomplete enumeration. Then normalize them into the source snapshot and stable source-clause inventory before activation. Require a nonempty per-issue clause/criterion floor; give each body/comment unit an immutable source-unit ID, normalized-clause digest, occurrence, and snapshot SHA; keep `criterion_id`, `coverage_id`, and source-clause mappings distinct, with every criterion mapped to source and executable coverage and exactly one clause disposition. Non-goal/evidence-only clauses need a bounded reason and owner.
4. Record the adapter/owner inspection as a structured artifact with locators and deterministic reviewed-input identity against that frozen snapshot; write a freeze receipt binding the snapshot identity, clause-inventory identity, normalization policy, and inspection identity, validate it, and only then activate the goal. Slice 0 must refuse a missing or stale receipt. Create the machine-readable crosswalk and validator named in the [goal](../charness-artifacts/goals/2026-08-07-repair-evidence-boundary-close-514-515.md), and use its typed `authorize_closeout()` result for every closeout ingress, including `issue_tool.py close-with-comment`; release closeout explicitly refuses these three issues before bump/publication until separately scoped. At Slice 0, reader capture may be a `baseline-refusal` fixture; a `repaired-success` capture must be produced by the reader adapter, bind its effective reader content/commit identity and verification-lock target, and gate its implementation slice closeout. If a shared projection is admitted, `projection_owner` means the serialization/version/refusal/repair owner; the validated crosswalk derives each issue's `projection_dependency` and is the sole closeout input. Each final-reader route must carry a route ID, effective reader locator, versioned field-set ID, command, parsed semantics, fixture, assertion, refusal result, capture receipt, capture phase, and result digest; the two shared routes must be distinct and contract-equivalent. Before closeout, recapture live issue source via the canonical adapter and require equality with the authorized frozen source identity or re-freeze/rebuild. The direct-carrier hook must authorize in-memory before temporary-file creation; `validate-closeout-draft`, `verify-closeout`, and `close-with-comment` must use the returned exact one-issue close-target set, reject extra targets/combined protected carriers, and reject self-declared local status. Start from [quality surface contract](../scripts/quality_surface_contract.py) → [quality surface tests](../tests/test_quality_surface_contract.py), [reviewed input identity](../scripts/reviewed_input_identity.py) → [identity binding tests](../tests/test_reviewed_input_identity_binding.py), [closeout bundle](../scripts/closeout_bundle_lib.py) → [closeout tests](../tests/quality_gates/test_closeout_bundle.py), and [final-bundle preflight tests](../tests/quality_gates/test_final_bundle_preflight.py); include root/plugin parity.
5. Decide the seam: define and validate a serialized common projection only if two actual readers share its semantics; otherwise record `no-shared-artifact` and continue all three local repairs. If a shared projection is admitted, the bundle proof in Slice 4 is mandatory before either consumer that uses it closes. If #515 or consumer-specific #518 behavior requires consumer-owned work, record `escalated-to-issue-spec` and keep that boundary open rather than claiming it from Charness.
6. Implement #518 declaration/preset/surface reconciliation, #515 routing/disclosure, and #514 deterministic assembly independently; sync root/plugin projections and replay typed #517/#516/#511/#507 fixtures plus the #518 consumer-shape fixtures. No issue waits on another issue's taxonomy.
7. If a remote carrier is required, create and validate the typed remote-carrier record plus the observation adapter's capture receipt, raw-response locator/command/digest, and structured payload from the independent channel; derive and verify repository/ref/commit/run/status agreement before issue readback.
8. Close each of the three issues separately through its full carrier, delegated resolution critique, distinct `Behavior #N:` verdict, `validate-closeout-draft`, and `verify-closeout --expect-state CLOSED`; every closeout path must consume the canonical crosswalk authorization and matching Slice 4 lock when `shared-consumer`, derive exactly one matching close target, require a current `repaired-success` capture, and reject carrier-declared local status, extra targets, or stale/mismatched source/projection identity. Unresolved external ownership keeps the goal open.

## Discuss

- No standalone CI retry, Cautilus run, browser/provider roundtrip, release, push, or consumer product claim is implied by the failed setup run or this goal activation. A remote carrier, if required at issue closeout, is a separate gated publish boundary after local proof.
- Do not create a generic evidence framework before Slice 0 proves a common representation, owner, and final reader.

## References

- [Unified goal](../charness-artifacts/goals/2026-08-07-repair-evidence-boundary-close-514-515.md)
- [Current quality posture](../charness-artifacts/quality/latest.md)
- [Session retro](../charness-artifacts/retro/2026-08-07-session-retro.md)
- [#516 debug record](../charness-artifacts/debug/2026-08-07-issue-516-mutation-regression-debug.md)
- [#514](https://github.com/corca-ai/charness/issues/514)
- [#515](https://github.com/corca-ai/charness/issues/515)
- [#518](https://github.com/corca-ai/charness/issues/518)

Refresh kept: the unified #514/#515/#518 repair goal, conditional shared-seam decision, independent issue slices, read-only sibling-repo comparison, and current CI non-claim because each changes the next action.
Refresh non-claims: #516/#517 implementation detail, consumer browser/provider behavior, and any current-head green verdict not proven by an independent run.
