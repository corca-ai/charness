# Content-fingerprint exemptions for boundary-bypass ratchet.
# Regenerated from the live inventory; each fingerprint is path-invariant.
0fb4d6af2e263393 # why: migrated from tests/quality_gates/test_critique_fresh_eye_presence.py::scripts/validate_critique_artifacts.py; the fresh-eye typed-presence floor's contract IS the validator CLI's exit code at commit/pre-push time (returncode 0 grandfathered / 1 refused), so these tests intentionally prove the subprocess exit contract per date/allowlist arm; the same-file parsing helpers stay covered in-process via test_critique_skill.py's grandfathered baseline entry for the same script. Owner: quality. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
13d4ce3f87808bff # why: migrated from tests/quality_gates/test_critique_boundary_ownership_presence.py::scripts/validate_critique_artifacts.py; same class as the fresh-eye entry above — the boundary-ownership presence floor's contract IS the validator CLI's exit code (returncode 0 grandfathered/accepted / 1 refused), so these tests intentionally prove the subprocess exit contract per date/legacy-allowlist/verdict arm; the same-file parsing helpers stay covered in-process via test_critique_skill.py's grandfathered baseline entry for the same script. Owner: quality. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
48dee51b941bdf9d # why: migrated from tests/quality_gates/test_run_slice_closeout_review_obligations.py::scripts/run_slice_closeout.py; these tests intentionally prove the final closeout status contract at the CLI boundary — blocked versus planned, JSON status, acknowledgement requirements, and forced-risk review obligations — rather than only helper return values. Owner: quality. Revisit if a shared closeout acceptance harness proves the same final-status and acknowledgement contract.
5281d7cccd2160df # why: migrated from tests/quality_gates/test_closeout_bundle.py::scripts/closeout_bundle.py; one real CLI smoke intentionally proves closeout-bundle help, no-write planning, JSON output, and ready/refused exit behavior at the operator boundary; orchestration and refusal behavior remain covered in-process in the same focused suite. Owner: quality. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
7154c13bd31dba62 # why: migrated from tests/quality_gates/test_check_git_identity.py::scripts/check_git_identity.py; two real CLI smokes intentionally prove the commit-boundary gate's operator contract — exit 1 plus the BLOCKED remediation message naming the offending `.invalid` ident, and the exit-0 clean path — which is exactly the subprocess contract .githooks/pre-commit consumes through staged_commit_gate_plan; identity resolution and detection behavior stay in-process through resolve_idents()/find_invalid_identity(). Owner: quality. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
75f6e4cd87612b6b # why: migrated from tests/quality_gates/test_release_real_host.py::skills/public/release/scripts/check_real_host_proof.py; one real CLI smoke intentionally proves the release real-host checker starts through __main__, resolves the skill/runtime bootstrap in a separate interpreter, and preserves the operator-facing summary plus exit status; detailed YAML, JSON, trigger, range, and error behavior stays in-process in the same file. Owner: release/quality. Revisit if a shared skill-entrypoint smoke harness proves this startup boundary.
7c85d3e701acc937 # why: migrated from tests/quality_gates/test_skill_contracts_validation.py::scripts/check_skill_contracts.py; one real CLI smoke intentionally proves check_skill_contracts startup, representative-contract loading, failure exit, and operator-facing stderr for a synthetic missing contract; direct contract helper behavior stays in-process in the same file. Owner: quality. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
821f68cbfd1e6a3f # why: migrated from tests/quality_gates/test_final_bundle_preflight.py::scripts/final_bundle_preflight.py; one real CLI smoke intentionally proves final-bundle preflight startup, JSON output, refusal exit status, and the operator-facing command-plan boundary; planner and evidence behavior remain covered in-process in the same focused suite. Owner: quality. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
8437cc6719a60262 # why: migrated from tests/quality_gates/test_python_length_gates.py::scripts/check_python_lengths.py, tests/quality_gates/test_python_length_gates.py::scripts/check_python_runtime_inheritance.py; these tests intentionally prove the length gate's CLI exit/stderr contract, including PATH/process-environment handling for the tokei dependency and strict git-listing failures; counting helpers stay exercised through the same module's in-process seam. Owner: quality. Revisit if a shared script-entrypoint smoke harness covers CLI, stderr, and process-environment behavior. / this test intentionally proves the runtime-inheritance gate's CLI exit/stderr contract for strict git-listing failures, including the process boundary that supplies the repository environment; parser behavior remains covered by direct gate tests. Owner: quality. Revisit if a shared script-entrypoint smoke harness covers this CLI and environment contract.
8dbe664296b5bff3 # why: migrated from tests/quality_gates/test_removed_name_consumers.py::scripts/removed_name_consumers.py; one CLI smoke proves the clean-tree contract the closeout consumes — exit 0 plus a `--json` payload on stdout whose `consumer_count` is 0 — because a tool that prints nothing on success is indistinguishable from one that never ran. Every detection behaviour (removed-name extraction, the module-mention plus attribute-access candidate rule, private names, self-exclusion, the stderr advisory) stays in-process through build_report()/advise_removed_name_consumers(). Owner: quality. Revisit if a shared script-entrypoint smoke harness proves the stdout-payload contract elsewhere.
921dfb1b7dcd33a0 # why: migrated from tests/quality_gates/test_parity_harness.py::scripts/parity_harness.py, tests/quality_gates/test_parity_harness.py::skills/shared/scripts/reviewer_boundary_fingerprint.py; the end-to-end cases genuinely need a process boundary — the harness must read a snapshot that another script wrote into a real git repo, and the `--json` payload it emits on stdout is the contract `advise_repair_parity` consumes as a subprocess. Every classification behaviour (repair-shaped detection, name qualification, generator/handle rendering, address normalisation, rename and non-ASCII path parsing) stays in-process through `repair_shaped_functions`/`compare_callables`/`changed_python_paths`. Owner: quality. Revisit if a shared script-entrypoint smoke harness proves the stdout-payload contract elsewhere. / the source-blob capture only exists as an effect of running `snapshot` against a real repo, and the defects these tests pin were both cross-process — blobs appearing as untracked drift on the NEXT verify, including through `--out`. An in-process call cannot reproduce "a second invocation observes what the first wrote". Comparison logic stays covered in-process by test_reviewer_boundary_fingerprint.py. Owner: quality. Revisit if snapshot/verify gain an in-process seam that carries the filesystem effect.
95102fe8f3ae93f1 # why: migrated from tests/test_quality_artifact_report_all.py::scripts/validate_quality_artifact.py; these tests intentionally prove the validator's default one-pass vs fail-fast stderr contract and nonzero exit behavior; the artifact-shape rules remain covered by the existing validator tests. Owner: quality. Revisit if validate_quality_artifact grows an in-process aggregate-report API that exposes the exact operator-facing message.
c665f902dfb80c4b # why: migrated from tests/quality_gates/test_plugin_dir_references.py::scripts/check_plugin_dir_references.py; one CLI smoke proves the `__main__` dispatch and both exit codes (0 with the Validated line, 1 with the unresolved-reference message on stderr) — exactly the contract run-quality.sh, staged_commit_gate_plan, and quality-core.yml consume, none of which import main(). Added because the changed-line mutation lane named those lines and was right: an in-process main() call cannot reach the entrypoint block. Every classification behaviour (package discovery, kind-flattening refusal, escape/absolute refusal, templated skip-and-count, fence skipping, the no-package notice) stays in-process via classify()/unresolved_references()/an argv-patched main(). Owner: quality. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
d540ecb4d9e97a74 # why: migrated from tests/quality_gates/test_check_plugin_doc_links.py::scripts/check_plugin_doc_links.py; ONE subprocess smoke proves the `__main__` dispatch and both exit codes (0 with the "Validated" line, 1 with the escape refusal on stderr) — exactly the contract run-quality.sh, staged_commit_gate_plan, and quality-core.yml consume, none of which import main(). Added because the changed-line mutation lane named those lines as uncovered and it was right: an in-process main() call cannot reach the entrypoint block. Every classification, fence, skip-count, and repair behaviour stays in-process via classify_link()/iter_unfollowable_links()/an argv-patched main(). Owner: quality. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
eb5f74b879091f70 # why: migrated from tests/quality_gates/test_slice_manifest.py::scripts/validate_slice_manifest.py; the plugin-layout and directory-path cases intentionally prove the mirrored CLI's subprocess exit/stdout contract at the operator boundary; validator behavior stays covered in-process through validate_manifest(). Owner: quality. Revisit if a shared script-entrypoint smoke harness proves the source-checkout-only boundary.
61f452122d9a45f0 # why: tests/test_contract_register.py intentionally proves the proposal and transition operator entrypoints in a separate interpreter, including bootstrap, JSON receipt, dry-run byte preservation, and exit behavior; replay, proposal, and application semantics stay covered in-process in the same file. Owner: quality. Revisit if a shared lifecycle CLI acceptance harness owns these entrypoints.
e6967de918a6928b # why: tests/test_session_start_lesson_context.py::scripts/render_lesson_selection_preview.py; the boundary IS the behavior under test. `session_start_lesson_context` runs inside a host SessionStart path in every session on the machine, so it is stdlib-only and renders the preview in a CHILD interpreter on purpose — a renderer that raises (this slice's own verification hit an import-time `SyntaxError` in a sibling module, which took down the whole routing directive) must come back as `not-established` text, not as a dead hook. These two tests spawn the same renderer to obtain the ground-truth bytes the hook's child produced, including the pinned `CHARNESS_REPO_ROOT`; importing it in-process would prove bytes from a different execution path and would import into the test process exactly the repo-module stack the hook path refuses to import. Owner: quality/retro. Revisit if the hook gains an in-process seam that carries the child's failure isolation and env pinning.
d7e9c10214529586 # why: tests/test_lesson_ledger.py intentionally proves the lesson lifecycle entrypoint's exact stderr/exit contract and byte-for-byte refusal for an unknown ID, the raw-KeyError escape found by fresh-eye review; lifecycle replay and state transitions stay covered in-process in the same file. Owner: quality. Revisit if a shared lifecycle CLI acceptance harness owns this refusal.
0177a7208456d3c2 # why: tests/test_lesson_lifecycle_review.py::scripts/render_lesson_lifecycle_review.py; the two remaining spawns exist to prove the EXIT CONTRACT, which is what the quality catalog's trust_model declares and what a reader uses to decide whether a nonzero exit means anything: zero over any validatable ledger including one with nothing to propose, so an unproposed graduation never reads as a failure. A fresh-eye round found that contract stated as "exits zero always" and false; pinning it in-process cannot reach the entrypoint block where the refusal becomes exit 1. The planner-routing assertion in the same file WAS convertible and was converted to build_plan/format_human in this slice rather than exempted. Every review, ordering, anchor, and non-claim behavior stays in-process through build_lifecycle_review(). Owner: quality. Revisit if a shared script-entrypoint smoke harness proves the exit contract elsewhere.
6a15852d138340b3 # why: tests/test_seed_lesson_transitions.py::scripts/seed_lesson_transitions.py; classified internal_boundary because the target spawns `git show HEAD:<path>` internally for the append-only prefix check, so no in-process call removes the boundary. The spawns prove three things an import cannot: the exit/stderr contract when the ledger is missing (naming init_lesson_ledger.py), the PROSE receipt an operator actually reads on both the dry-run and write paths -- whose freeze warning is this command's entire mitigation for an unrepairable append, and which was previously printed only after the bytes landed -- and a two-process smoke over the shared writer lock. Planning, budget arithmetic, refusals, and byte-preservation stay in-process through seed_transitions()/plan_seeds(). Owner: quality. Revisit if a shared script-entrypoint smoke harness proves the stdout-receipt contract elsewhere.
4601f4df6f492c6e # why: tests/quality_gates/test_release_fresh_checkout_probes.py::skills/public/release/scripts/check_fresh_checkout_probes.py; ONE spawn, and it is not the exit code as such -- `tests/script_main.py` already obtains a returncode in-process by calling `main()` and catching SystemExit, and the sibling tests in this file use exactly that to pin exit 3 and the omitted `probe_results` key. The spawn proves the thing that in-process path cannot: that `raise SystemExit(main())` and a FRESH interpreter agree on the byte, because the release gate packet is a shell command and the shell reads `$?` -- so a bootstrap-resolution failure under a different interpreter would otherwise surface as a release gate that silently passes. Everything else in the file -- the four statuses, the key omission, declaration parsing, run/no-run selection -- stays in-process. Owner: release/quality. Revisit if a shared skill-entrypoint smoke harness proves interpreter agreement elsewhere.
