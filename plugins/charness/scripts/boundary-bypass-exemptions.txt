# Boundary-bypass ratchet exemptions.
#
# Format:
# tests/path/test_file.py::scripts/target.py # why: short rationale with owner/revisit condition
#
# Add exemptions only for intentional boundary tests that should not count
# against the no-increase policy.

tests/test_quality_scaffold.py::scripts/export_plugin.py # why: end-to-end export portability proof — runs the real export_plugin.py as a subprocess to build a fresh export, then runs the exported scaffold+validator from a clean consumer repo, exercising the build->consume path a downstream repo uses. export_plugin is import-safe (could be called in-process), but the subprocess keeps this an honest E2E check; the debug-scaffold export test exercises the same path and is grandfathered into boundary-bypass-baseline.json rather than exempted. Owner: quality. Revisit if a lighter in-process export-and-consume proof replaces the E2E fidelity.
tests/test_skill_anchor_guard_hook.py::scripts/post_edit_skill_anchor_guard.py # why: the PostToolUse edit-time guard's contract IS the process boundary — the host pipes the hook payload to stdin and branches on exit 0 (silent) vs 2 (surface findings) — so one subprocess proof pins the stdin+exit-code contract; every logic path stays tested in-process in the same module. Owner: quality. Revisit if the host-hook machinery gains an in-process invocation seam.
