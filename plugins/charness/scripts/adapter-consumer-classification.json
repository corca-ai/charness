{
  "schemaVersion": 1,
  "consumers": {
    "scripts/adapter_key_registry.py": {
      "verdict": "safe-not-consequential",
      "reason": "Key REGISTRY, not a gate: L488 wraps `load_yaml_file` so the registry can report what an adapter literally declares. Reading raw is the point -- it inventories declared keys, including ones a refused version would hide."
    },
    "scripts/artifact_validator.py": {
      "verdict": "guarded-upstream",
      "reason": "resolve_adapter_line_budget (lines 59-91) calls load_adapter(repo_root) at line 82 and reads data.get(field) to return a numeric line ceiling without ever checking payload['errors']/['valid'] or calling adapter_version_verdict; its own docstring says this is deliberate. CALLER-COVERAGE MEASURED 2026-08-19. This row was `accepted-risk-unguarded` at the time, before a level existed for it, because the property belongs to its CALLERS and not to this file. `resolve_adapter_line_budget`'s production callers are `validate_handoff_artifact`, `validate_quality_artifact` and `scaffold_artifact_lib.size_budget` (itself reached only from the two guarded scaffolds) -- all guarded. A guard here is not merely redundant, it is wrong or impossible: its own docstring argues, correctly, that a ceiling must NOT be dropped because an unrelated field was typo'd; refusing here would key on `valid`, the polarity `adapter_version_verdict` forbids. The census's unit is the DIRECT read site, so a row whose callers are all guarded and a row whose callers are not look identical here -- and the gate's own stated blind class ('a file that reads an adapter through a HELPER in another module is invisible') means 'every caller' is a claim about the callers enumerated, not a completeness proof. MIGRATED to `guarded-upstream` (#675 round 1). This row met the level's stated criterion -- \"cannot guard itself, everything upstream is guarded\" -- and its own reason already enumerated the covering rows, but it sat in `accepted-risk-unguarded` because that was the only token available. The gate's headline `ACCEPTED RISK: N consumer(s) would use a charness default` was therefore FALSE for it by the manifest's own text, which is exactly the measurement distortion #675 reports. The enumerated rows are now in `covering_rows`, where the gate checks each one carries a guarded verdict. COVERING ROWS CORRECTED after a round-2 review: the first list was derived from this row's own PROSE and was wrong in both directions -- it dropped `scaffold_artifact_lib` (which the prose names) and never saw `validate_debug_artifact` or `check_doc_authoring_preflight` (which reaches `resolved_max_content_words` -- `resolved_max_content_lines` until the handoff budget changed unit on 2026-08-19 -- directly, bypassing `validate_handoff_artifact.main()`). Worse, `scaffold_artifact_lib` COULD NOT be listed: it is itself `guarded-upstream`, and the gate only accepted a directly-guarded row, so the enumerated set was being shaped by what the checker accepts rather than by the call graph. The gate follows a chain now, with cycle detection. Derived from grep over every call site, not from this paragraph.",
      "covering_rows": [
        "scripts/validate_handoff_artifact.py",
        "scripts/validate_quality_artifact.py",
        "scripts/validate_debug_artifact.py",
        "scripts/scaffold_artifact_lib.py",
        "scripts/check_doc_authoring_preflight.py"
      ]
    },
    "scripts/boundary_probe_lib.py": {
      "verdict": "safe-checks-errors",
      "reason": "resolve_probe_state (lines 218-289) loads the critique adapter at line 232, then at line 270 calls _adapter_lib.unreadable_reasons(adapter), which reads adapter.get('errors') (adapter_lib.py:420) \u2014 this list includes the 'version must be N' refusal string \u2014 and at lines 271-288 downgrades the probe state to PROBE_NOT_ESTABLISHED with an explanatory undetermined_reason whenever the adapter is unreadable and no hit was already found."
    },
    "scripts/build_debug_seam_risk_index.py": {
      "verdicts": [
        {
          "verdict": "accepted-risk-unguarded",
          "reason": "_load_debug_output_dir (lines 38-55) calls module.load_adapter(repo_root) at line 42 and reads adapter['data']['output_dir'] at line 52 with no reference to 'errors' or 'valid' anywhere in the file (grep confirms zero occurrences of either key). SCOPED after measurement: the parenthetical above covers the RESOLVER path only. See this row's second verdict."
        },
        {
          "verdict": "no-version-validation",
          "reason": "SECOND CLASS, added 2026-08-19 after slice 5 measured this row and did NOT pay it down. `_load_debug_output_dir` (lines 43-49) falls back to a RAW LINE SCAN of `.agents/debug-adapter.yaml` -- `if raw_line.startswith(\"output_dir:\")` -- whenever neither `skills/public/debug/scripts/resolve_adapter.py` nor `skills/debug/scripts/resolve_adapter.py` exists, which is the ordinary plugin-install layout in a consumer repo rather than an edge. That path never reads `version`, so there is no `errors` for any guard to check. MEASURED, not argued: against a temp repo with no skill tree, `build_debug_seam_risk_index.py --repo-root $D` reported the DECLARED `docs/mine-debug` identically at `version: 9` and `version: 1` -- the declaration honored by a reader that never checked whether it could speak it. This is the THIRD file found carrying this fallback shape (`risk_interrupt_lib.py` and `build_retro_lesson_selection_index.py` are the others, both already multi-class); a sweep for `startswith(\"output_dir:\")` over `scripts/` and `skills/` finds exactly these. It stays UNPAID rather than classified on an argument -- reproducing the resolver arm needs a repo that ships the skill tree."
        }
      ]
    },
    "scripts/build_retro_lesson_selection_index.py": {
      "verdicts": [
        {
          "verdict": "accepted-risk-unguarded",
          "reason": "_load_retro_paths (lines 49-57) calls module.load_adapter(repo_root) at line 55 and reads adapter['data']['output_dir']/['summary_path'] at line 57 with no check of 'errors' or 'valid' anywhere in the file (grep confirms zero occurrences)."
        },
        {
          "verdict": "no-version-validation",
          "reason": "_load_yamlish_retro_paths (lines 32-51) reads `.agents/retro-adapter.yaml` DIRECTLY at line 33 -- not through the resolver -- and never reconciles a version, so there is no `errors` for anyone to check on that path. This is a SECOND defect class in the same file, needing a different repair from the unguarded `load_adapter` call at line 55: wire it onto the shared resolver rather than adding a check it has nothing to check. Filed as a second verdict because under one-verdict-per-file it was recorded as `accepted-risk-unguarded` alone, and paying that down would have left this half live under a row reading `done`."
        }
      ]
    },
    "scripts/capability_catalog_sources.py": {
      "verdict": "safe-checks-errors",
      "reason": "Its own load_adapter (lines 208-233) calls validate_adapter_version(raw, {}, errors) at line 220 and only applies any declared field (repo/language/trusted_skill_roots/prefer_local_first/allow_external_registry) inside the `if not errors:` block at lines 221-232 \u2014 this reimplements the declared_fields_after_version_check pattern inline, so on a refused version `defaults` keeps the file's own built-in defaults dict (line 209) rather than any repo-declared value, and build_inventory (line 238+) consumes that already-correct payload."
    },
    "scripts/capture_issue_source.py": {
      "verdict": "safe-checks-errors",
      "reason": "run_capture (lines 73-93) checks `if not adapter['valid']:` at line 78 and raises CaptureRefusal before reading capability/backend fields; the issue skill's resolver (skills/public/issue/scripts/resolve_adapter.py:314-321) calls validate_adapter_version and returns early with valid:False, errors:[...] on any version error, before applying any declared field, so 'valid' correctly reflects a refused version here."
    },
    "scripts/cautilus_scenarios_lib.py": {
      "verdict": "safe-checks-errors",
      "reason": "Line 273-274: `if not adapter_payload['valid']: raise ValidationError(...)`. scripts/cautilus_adapter_lib.py calls declared_fields_after_version_check at line 165 and sets 'valid': not errors at line 214, so a refused version correctly yields valid=False before this file acts on the payload."
    },
    "scripts/check_cli_skill_surface.py": {
      "verdict": "safe-checks-errors",
      "reason": "_load_adapter (lines 49-66) calls validate_adapter_version(raw, {}, errors) at line 65 and returns ({}, errors) on any error before any field is read; build_payload (lines 291-320) checks `if adapter_version_errors:` at line 299 and returns a 'blocked' status with the version errors quoted at lines 302-313, before selecting skill paths or probe commands, so nothing downstream reads charness defaults in place of a declared value."
    },
    "scripts/check_doc_authoring_preflight.py": {
      "verdict": "guarded-errors-only",
      "reason": "adapter_load_failed (lines 110-131) calls _version_verdict.version_refused(payload.get('errors')) at line 131 (using scripts.adapter_version_verdict, imported at line 47), and its docstring explicitly names the exact 'surface_cap forecasts the shipped ceiling' bug this closes; per the task's already-guarded list. LEVEL MIGRATION (#675): this row was `guarded` and is measured `guarded-errors-only` -- it asks `declarations_unhonored`, a predicate over `errors`, so a line the parser silently DROPS walks past it with `errors: []` and `valid: True`. Nothing was upgraded or downgraded by hand; the level is what the file's own calls establish, and the gate now refuses a row that claims a different one. Raising it to `guarded-all-doors` means routing through `unspeakable_version_message` / `refuse_unspeakable_version` or asking `declarations_dropped` explicitly."
    },
    "scripts/check_doc_links.py": {
      "verdict": "safe-checks-errors",
      "reason": "load_canonical_markdown_surfaces (lines 197-203) calls load_quality_adapter(root) then checks `if payload.get('errors'):` at line 199 and raises ValidationError at line 201 before reading canonical_markdown_surfaces from data at line 202."
    },
    "scripts/check_js_mutation_score.py": {
      "verdict": "safe-checks-errors",
      "reason": "mutation_config (lines 37-46) checks `if payload.get('errors'):` at line 39, writes the errors to stderr, and returns None at line 42 before reading the mutation_testing block; main() (line 164) checks `if config is None: return 2` before proceeding."
    },
    "scripts/check_mutation_score.py": {
      "verdict": "safe-checks-errors",
      "reason": "load_mutation_config (lines 136-147) checks `if payload.get('errors'):` at line 138, prints the errors, and returns None at line 142 before reading score_break/report_paths; both call sites in main() (lines 295-296 and 325-326) check `if config is None: return 2` before acting on the config."
    },
    "scripts/check_runtime_budget_universe.py": {
      "verdict": "no-version-validation",
      "reason": "Reads `.agents/*-adapter.yaml` with a raw YAML load and never reconciles `version`, so there is no `errors` for any consumer to check. A DIFFERENT defect from the unguarded rows: the fix is wiring this onto the shared resolver, not adding a check."
    },
    "scripts/control_plane_lifecycle_lib.py": {
      "verdict": "safe-checks-errors",
      "reason": "disabled_by_cautilus_adapter() (lines 12-26) calls load_cautilus_adapter() and returns None (treated as 'not disabled') whenever `not adapter['valid']` (line 16), before ever reading `data.get('run_mode')`; a version-refused adapter (errors non-empty -> valid=False) is therefore never read for run_mode, and the caller falls back to running checks normally rather than acting on inferred defaults for the disable directive."
    },
    "scripts/critique_enforcement_scope.py": {
      "verdict": "safe-checks-errors",
      "reason": "resolve_cross_surface_scope() (lines 341-411) loads the adapter at line 362 and, at lines 364-365, calls `_adapter_lib_module.unreadable_reasons(adapter)` (which reads `adapter['errors']`/dropped-field warnings) and returns CROSS_SURFACE_NOT_ESTABLISHED before ever reading `probe['globs']`/`probe['surfaces']` from the possibly-defaulted `adapter['data']`; the docstring (lines 355-361) explicitly documents this as the fix for exactly the refused-version discard scenario."
    },
    "scripts/dup_ratchet_edit_advisory.py": {
      "verdict": "no-version-validation",
      "reason": "Reads `.agents/*-adapter.yaml` with a raw YAML load and never reconciles `version`, so there is no `errors` for any consumer to check. A DIFFERENT defect from the unguarded rows: the fix is wiring this onto the shared resolver, not adding a check."
    },
    "scripts/inventory_quality_handoff.py": {
      "verdict": "guarded-all-doors",
      "reason": "main() now refuses before `adapter[\"artifact_path\"]` is read, INSIDE the `args.artifact is None` branch so the `--artifact` arm (which asks the adapter nothing) still works -- pinned by a control. Measured flip at 1465689ac: a repo declaring `output_dir: docs/mine-q` under `version: 9` inventoried `artifact: charness-artifacts/quality/latest.md` with `status: advisory` and `findings: []`, exit 0 -- a clean bill of health on a file the repo does not keep its review in, while the one it does keep went uninventoried. Control at version 1 reports the declared path. WAS a residual: quality's resolver called `adapter_lib.load_yaml_file` bare, so the parse-refusal and silently-dropped-line doors were both invisible here. CLOSED by #673: that resolver now routes through `adapter_lib.read_declared_adapter`, so the parse-refusal and dropped-line doors are both live here. Probe record: charness-artifacts/probe/2026-08-19-inventory-gather-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "scripts/manage_mutation_reports.py": {
      "verdict": "safe-checks-errors",
      "reason": "managed_paths() (lines 48-66) calls `load_quality_adapter_strict(repo_root)` and at lines 52-54 does `if adapter.get('valid') is not True: raise SystemExit(...)` before reading `mutation_testing`/`changed_line_mutation_gate` fields from `adapter['data']`."
    },
    "scripts/plan_cautilus_proof.py": {
      "verdict": "safe-checks-errors",
      "reason": "plan_cautilus_proof() (lines 133-227) calls `load_cautilus_adapter(repo_root)` and at lines 135-136 does `if not adapter_payload['valid']: raise SurfaceError(...)` before reading `data['prompt_affecting_patterns']`, `data['run_mode']`, etc."
    },
    "scripts/proof_mismatch.py": {
      "verdict": "safe-checks-errors",
      "reason": "proof_mismatch_report() (lines 172-221) calls `load_adapter(Path(repo_root))` (proof_semantics_adapter_lib) and, at line 192, explicitly checks `if adapter['found'] and not adapter['valid']:` and returns a blocking `invalid-adapter` problem (fails closed) before ever scoring rows against `adapter['data']`; `acceptance_map_available(adapter)` (line 181) also degrades every row to 'no domain map' rather than silently trusting inferred defaults."
    },
    "scripts/public_skill_dogfood_lib.py": {
      "verdict": "accepted-risk-unguarded",
      "reason": "_resolve_artifact() (lines 139-158) calls each changed skill's own `load_adapter(repo_root)` (or subprocess+yaml.safe_load fallback) and `_artifact_path_from_payload()` reads `payload['artifact_path']`/`data['summary_path'|'output_dir'|'state_dir']` directly with no check of `payload['valid']`/`payload['errors']`; this feeds `build_matrix()`'s `expected_artifact` field used by `suggest_public_skill_dogfood.py`."
    },
    "scripts/quality_bootstrap_lib.py": {
      "verdict": "safe-checks-errors",
      "reason": "L203 `load_yaml_file_report` then `validate_adapter_version(raw, {}, version_errors)`; raises BootstrapValidationError rather than rewriting the adapter. Driven by tests/quality_gates/test_quality_bootstrap.py."
    },
    "scripts/quality_label_universe.py": {
      "verdict": "no-version-validation",
      "reason": "Reads `.agents/*-adapter.yaml` with a raw YAML load and never reconciles `version`, so there is no `errors` for any consumer to check. A DIFFERENT defect from the unguarded rows: the fix is wiring this onto the shared resolver, not adding a check."
    },
    "scripts/reconcile_usage_episodes_host_hooks.py": {
      "verdict": "safe-not-consequential",
      "reason": "_load_adapter() (lines 30-39) does a raw `yaml.safe_load` of `.agents/usage-episodes-adapter.yaml` with no `version` field / `declared_fields_after_version_check` involvement at all; there is no `usage_episodes_adapter_lib.py`, and no other script in the repo runs this adapter through `scripts.adapter_lib`."
    },
    "scripts/record_quality_runtime.py": {
      "verdict": "safe-not-consequential",
      "reason": "main() (line 340) calls `load_adapter(repo_root)` (quality resolve_adapter.py) but discards the return value entirely -- it is not assigned to a variable, and no other line in the file references `adapter`/`valid`/`errors`/`data`. `load_quality_adapter` is a pure function with no side effects (confirmed by reading `scripts/quality_adapter_lib.py:476-514`), so the call is a no-op."
    },
    "scripts/record_usage_feedback.py": {
      "verdict": "safe-checks-errors",
      "reason": "L217 `load_validated_adapter(...)` returns `(adapter, adapter_error)` and the caller branches on `adapter_error`."
    },
    "scripts/refresh_current_pointer.py": {
      "verdict": "guarded-upstream",
      "reason": "THE FIRST ROW THIS VOCABULARY EXISTS FOR (#675), and the one that renamed it. The coverage here comes from this file's CALLEE, not its caller: it imports `resolve_artifact_path.load_adapter`, which refuses before returning. A field named `covering_callers` would have published a call-graph claim in the wrong direction on a proof surface; a round-1 review caught it and the token is `guarded-upstream`, which is true of both shapes. The surface refuses -- `main()` calls `resolve_artifact_path.load_adapter`, which since #673's fold refuses on all three doors before returning, pinned by this surface's own case in tests/quality_gates/test_adapter_version_refusal_is_loud.py -- but it does not guard ITSELF. It used to call `refuse_unspeakable_version` with that loader; the loader now raises SystemExit, a BaseException that `unspeakable_version_message`'s `except Exception` does not catch, so `refused` was unreachable and deleting the block left all 39 assertions in that test file green. Under the single `guarded` token this was recorded `accepted-risk-unguarded`, which inflated the accepted-risk count by one and said the opposite of what is true. `covering_rows` is checked: the named caller must carry its own guarded row.",
      "covering_rows": [
        "scripts/resolve_artifact_path.py"
      ]
    },
    "scripts/report_usage_episodes.py": {
      "verdict": "safe-not-consequential",
      "reason": "_load_adapter() (lines 63-67) does a raw `yaml.safe_load` and main() (lines 326-332) validates the result against `manifest.schema.json` via `jsonschema.validate`; there is no `version` field / `declared_fields_after_version_check` mechanism used anywhere in this file."
    },
    "scripts/report_usage_product_review.py": {
      "verdict": "safe-not-consequential",
      "reason": "main() (lines 128-134) imports and reuses `_load_adapter`/jsonschema validation straight from `report_usage_episodes.py`; same raw-yaml + jsonschema-manifest mechanism, no `version`/`declared_fields_after_version_check` involvement."
    },
    "scripts/resolve_artifact_path.py": {
      "verdict": "guarded-all-doors",
      "reason": "GUARDED as of #673's fold. `load_adapter` calls `_refuse_unhonored_adapter` (scripts/resolve_artifact_path.py) before returning the payload, and that guard asks the CONDITION -- `adapter_version_verdict.declarations_unhonored(errors)` or `declarations_dropped(adapter)` -- so all three doors refuse here. Its message is phrased through `unhonored_cause` / `unhonored_remedy` for the unhonored arm and carries a separate, narrower sentence for the dropped-line arm, where the rest of the document IS still honored. WHY THE ROW EXISTED: this file's only protection used to be the resolver subprocess's return code, and #673 removed it -- making five resolvers render a verdict at exit 0 instead of tracebacking meant a parse-refused adapter resolved `charness-artifacts/quality/latest.md` over a repo that declared `docs/mine-q`. Measured at a776bd37d, found by a bounded review rather than declared by the batch that caused it, and pinned per door by tests/quality_gates/test_artifact_path_refuses_an_unhonored_adapter.py. RESIDUAL: `payload_for(adapter=...)` bypasses `load_adapter` and therefore this guard; the only production caller (scaffold_debug_artifact) is guarded upstream, and no test pins that parameter's contract. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "scripts/retro_output_dir_lib.py": {
      "verdict": "accepted-risk-unguarded",
      "reason": "REVERTED from `guarded-upstream` by a round-2 review, and the reversal is the finding. `guarded-upstream` means the file CANNOT guard itself -- an injected loader, an already-resolved payload, or a refusing loader. `retro_artifact_prefix` is none of those: it resolves the retro resolver itself (lines 104-113), loads the adapter itself, knows which adapter it is, and gets back a payload carrying `errors`. Nothing stops it asking `declarations_unhonored`; it simply does not. That is an accepted risk, not upstream coverage, and migrating it was the measurement distortion #675 exists to stop -- committed by the slice that built the vocabulary, from a list derived out of prose rather than the call graph. TWO RESIDUALS the reversal does not close, both recorded rather than absorbed: `scripts/retro_floor_scope_lib.py` is a real caller with NO census row (it calls `retro_artifact_prefix`, not a `load_*adapter*` name, so the AST enumerator cannot see it); and the older prose about `plan_retro_run`'s call path is STALE at HEAD -- `retro_floor_scope_lib:174` short-circuits on `output_dir is not None` and `plan_retro_run:249` passes no `output_dir`, so that path no longer reaches this function. Nobody re-measured it; this row inherited a sentence that refuted its own verdict."
    },
    "scripts/risk_interrupt_lib.py": {
      "verdicts": [
        {
          "verdict": "accepted-risk-unguarded",
          "reason": "_load_debug_adapter (lines 32-50) calls the debug skill's resolve_adapter.py load_adapter() (which uses adapter_lib.declared_fields_after_version_check) and returns the raw payload with no check of ['errors']/['valid']; current_debug_artifact_path (53-61) and plan_risk_interrupt (237-333) act directly on adapter['data']['output_dir'] with no version guard anywhere in the file (confirmed: no 'errors'/'valid'/adapter_version_verdict reference in the whole module). SCOPED, after this row gained its second verdict: the parenthetical above covers the RESOLVER path only. It reads as completeness and is not -- see this row's `no-version-validation` verdict for the raw-line-scan fallback, which is why that class was invisible here."
        },
        {
          "verdict": "no-version-validation",
          "reason": "SECOND CLASS, added 2026-08-19 after slice 5 measured this row and DROPPED it without paying it down. `_load_debug_adapter` (lines 41-49) falls back to a RAW LINE SCAN of `.agents/debug-adapter.yaml` -- `if raw_line.startswith(\"output_dir:\")` -- whenever neither `skills/public/debug/scripts/resolve_adapter.py` nor `skills/debug/scripts/resolve_adapter.py` exists, which is the ordinary plugin-install layout in a consumer repo rather than an edge. That path never reads `version` at all, so there is no `errors` for any guard to check. MEASURED, not argued: against a temp repo with no skill tree, `plan_risk_interrupt.py` returned the DECLARED `docs/mine-debug/latest.md` identically at `version: 9` and `version: 1` -- the declaration honored by a reader that never checked whether it could speak it. The row's own single-verdict entry ended with a parenthetical that reads as completeness (\"no 'errors'/'valid'/adapter_version_verdict reference in the whole module\") and is exactly why this class was invisible there. Paying the first class alone would leave the row reading `done` with half of it live -- the failure `scripts/build_retro_lesson_selection_index.py`'s row already records."
        }
      ]
    },
    "scripts/run_evals.py": {
      "verdict": "safe-not-consequential",
      "reason": "Eval harness: every `safe_load` here parses the STDOUT of a resolver CLI it just ran (L83/L120/L156/L171), not an adapter file. It asserts on what the resolver reported."
    },
    "scripts/scaffold_artifact_lib.py": {
      "verdict": "guarded-upstream",
      "reason": "size_budget() (lines 396-427) takes an already-resolved `adapter: dict` from its caller and calls `validator.resolve_adapter_line_budget(lambda _repo_root: adapter, ..., field=validator.LINE_BUDGET_FIELD, default=default)` with no check of adapter['errors']/['valid'] before using it; only an `Exception` (a cross-tree import failure) is caught to fall back to `source: default`. Consumers include skills/public/quality/scripts/scaffold_quality_artifact.py and skills/public/debug/scripts/scaffold_debug_artifact.py. CALLER-COVERAGE MEASURED 2026-08-19. This row was `accepted-risk-unguarded` at the time, before a level existed for it, because the property belongs to its CALLERS and not to this file. `size_budget` takes an ALREADY-RESOLVED adapter dict, and its only callers are `scaffold_quality_artifact` and `scaffold_debug_artifact`, both guarded in rows 6-10. A guard here is not merely redundant, it is wrong or impossible: the payload arrives resolved, so there is no load to guard and no adapter name to render. The census's unit is the DIRECT read site, so a row whose callers are all guarded and a row whose callers are not look identical here -- and the gate's own stated blind class ('a file that reads an adapter through a HELPER in another module is invisible') means 'every caller' is a claim about the callers enumerated, not a completeness proof. MIGRATED to `guarded-upstream` (#675 round 1). This row met the level's stated criterion -- \"cannot guard itself, everything upstream is guarded\" -- and its own reason already enumerated the covering rows, but it sat in `accepted-risk-unguarded` because that was the only token available. The gate's headline `ACCEPTED RISK: N consumer(s) would use a charness default` was therefore FALSE for it by the manifest's own text, which is exactly the measurement distortion #675 reports. The enumerated rows are now in `covering_rows`, where the gate checks each one carries a guarded verdict.",
      "covering_rows": [
        "skills/public/quality/scripts/scaffold_quality_artifact.py",
        "skills/public/debug/scripts/scaffold_debug_artifact.py"
      ]
    },
    "scripts/session_start_routing.py": {
      "verdict": "safe-not-consequential",
      "reason": "L164 parses the stdout of a routing command, not an adapter file."
    },
    "scripts/setup_critique_adapter_inspection.py": {
      "verdict": "safe-not-consequential",
      "reason": "_detect_critique_adapter_normalization (lines 70-198) calls scripts.critique_adapter_lib.load_adapter and reads only `found`, `path`, and `data.reviewer_tiers` (via `.get('reviewer_tiers', {})`, line 80) without checking `errors`/`valid`, but forwards `valid: bool(adapter.get('valid'))` in its own returned payload (line 186) for the caller to act on."
    },
    "scripts/setup_inspect_lib.py": {
      "verdict": "safe-checks-errors",
      "reason": "L141 `load_setup_adapter` returns `(data, path, warnings)`; setup's resolver refuses an unsupported version before surface overrides, driven by tests/quality_gates/test_setup_inspect_adapters.py."
    },
    "scripts/skill_t_inventory_lib.py": {
      "verdict": "no-version-validation",
      "reason": "Reads `.agents/*-adapter.yaml` with a raw YAML load and never reconciles `version`, so there is no `errors` for any consumer to check. A DIFFERENT defect from the unguarded rows: the fix is wiring this onto the shared resolver, not adding a check."
    },
    "scripts/slice_closeout_usage_episode.py": {
      "verdict": "no-version-validation",
      "reason": "Reads `.agents/*-adapter.yaml` with a raw YAML load and never reconciles `version`, so there is no `errors` for any consumer to check. A DIFFERENT defect from the unguarded rows: the fix is wiring this onto the shared resolver, not adding a check."
    },
    "scripts/t_events_emit_lib.py": {
      "verdict": "no-version-validation",
      "reason": "Reads `.agents/*-adapter.yaml` with a raw YAML load and never reconciles `version`, so there is no `errors` for any consumer to check. A DIFFERENT defect from the unguarded rows: the fix is wiring this onto the shared resolver, not adding a check."
    },
    "scripts/usage_episode_session_start.py": {
      "verdict": "no-version-validation",
      "reason": "Reads `.agents/*-adapter.yaml` with a raw YAML load and never reconciles `version`, so there is no `errors` for any consumer to check. A DIFFERENT defect from the unguarded rows: the fix is wiring this onto the shared resolver, not adding a check."
    },
    "scripts/validate_adapters.py": {
      "verdict": "safe-checks-errors",
      "reason": "_require_declared_version (lines 278-297) explicitly calls `validate_adapter_version(data, {}, errors, required=True)` and raises ValidationError on any error, run BEFORE the per-adapter early returns in validate_adapter_yaml (line 300); validate_adapter_yaml also checks `payload['valid']` for cautilus/critique/retro/quality adapters (lines 303-318) and raises on invalidity; validate_adapter_integration_schema (lines 244-275) runs jsonschema.validate against each adapter's integration schema (catching e.g. t-events/usage-episodes version mismatches) and raises ValidationError on rejection."
    },
    "scripts/validate_debug_artifact.py": {
      "verdict": "guarded-all-doors",
      "reason": "Already on the pre-classified guarded list; confirmed by reading: line 405 calls `_adapter_version_verdict.unspeakable_version_message(...)` as a preflight before `resolve_adapter_line_budget` (line 502) is used to enforce the line ceiling. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "scripts/validate_handoff_artifact.py": {
      "verdict": "guarded-all-doors",
      "reason": "Already on the pre-classified guarded list; confirmed by reading: line 471 calls `_adapter_version_verdict.refuse_unspeakable_version(...)`. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "scripts/validate_quality_artifact.py": {
      "verdict": "guarded-all-doors",
      "reason": "Already on the pre-classified guarded list; confirmed by reading: line 624 calls `_adapter_version_verdict.refuse_unspeakable_version(...)` before `resolve_adapter_line_budget` is used at line 589. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "scripts/validate_retro_artifact.py": {
      "verdict": "guarded-all-doors",
      "reason": "`_unspeakable_adapter_version` is passed as `preflight=` to run_changed_artifact_validator, so a refused version refuses before any scoping. Enumerated only once the census matched loader REFERENCES: this file never calls its loader, it hands `_output_dir.load_retro_adapter` to the verdict helper. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "scripts/validate_usage_episodes.py": {
      "verdict": "safe-checks-errors",
      "reason": "main() loads the adapter (`_load_adapter`, line 130) and immediately runs `jsonschema.validate(adapter, manifest_schema)` (line 131) against integrations/usage-episodes/manifest.schema.json, which declares `version: {const: 1}` and `additionalProperties: false`; a version mismatch raises `jsonschema.ValidationError`, caught at line 132, producing `status: 'invalid_adapter', valid: False` and exit code 1 (lines 133-142) before any records_path resolution or record validation runs."
    },
    "skills/public/achieve/scripts/achieve_adapter_policy.py": {
      "verdict": "accepted-risk-unguarded",
      "reason": "This module IS the achieve adapter's resolver (load_adapter/validate_adapter_data at lines 235-306) and correctly sets `valid: not errors` via `declared_fields_after_version_check` (line 239) -- that part is a proper resolver. But its own downstream helpers `resolve_discussion_deploy_vocab` (309-315) and `resolve_release_surface_tokens` (318-332) call `load_adapter(repo_root).get('data')` and read a field directly with no check of `errors`/`valid`, and their known callers (skills/public/achieve/scripts/goal_artifact_coordination_floors.py:160, skills/public/achieve/scripts/check_goal_artifact.py:165) also call them with no valid/errors check."
    },
    "skills/public/achieve/scripts/goal_artifact_closeout_evidence.py": {
      "verdict": "safe-not-consequential",
      "reason": "Calls `achieve_adapter_policy.closeout_policy_report` (L289); the adapter read and its risk belong to that module's row, not this one."
    },
    "skills/public/achieve/scripts/goal_artifact_lib.py": {
      "verdict": "safe-checks-errors",
      "reason": "upsert_goal() (lines 302-305) calls `_policy.load_adapter(repo_root)` and explicitly checks `if adapter['found'] and not adapter['valid']:` and returns a `refused` result (naming `adapter['errors']`) BEFORE any consequential action (mkdir, template render with adapter['data']['scaffold']..., or path.write_text at lines 306-326) is taken."
    },
    "skills/public/announcement/scripts/preflight_sources.py": {
      "verdict": "guarded-all-doors",
      "reason": "main() now refuses via `unspeakable_version_message` before `adapter[\"data\"]` reaches `preflight_sources`. THIS IS A PUBLISH-BOUNDARY GATE and the flip is an INVERSION, not a degradation: measured at 254fa5c44, a repo declaring one `in_progress_sources` entry got `delivery_blocked: false`, `ok: true`, `surfaces: []`, exit 0 -- clear to announce -- while the same repo at a speakable version gets `delivery_blocked: true`, `ok: false`, exit 2. The mechanism is the row's own: `announcement_preflight_lib` short-circuits to ok/unblocked the moment `in_progress_sources` is empty, so an unhonored declaration is indistinguishable from a repo that declared none. Controls: version 1 blocks and lists the declared source; no adapter clears at exit 0; an ordinarily-invalid adapter still blocks. Probe record: charness-artifacts/probe/2026-08-19-announcement-version-refusal.md. ROUND-1 REVIEW FOUND TWO LIVE EXIT-0 BYPASSES OF THIS GATE, both closed and neither needing a version to be touched: (1) `announcement_adapter_lib` called `adapter_lib.load_yaml_file` bare, so an over-indented `in_progress_sources:` block left `errors: []`, `valid: true` and cleared the delivery -- two of the guard's three doors were structurally dead for this adapter; repaired by arming the uninterpreted sink ahead of #673, because this is a publish boundary. (2) `_validate_in_progress_sources` drops a rejected entry, so `kind: Path` (one capital) emptied the list and took the short-circuit; repaired by checking `field_state` against the validated result. ROUND-2 REVIEW FOUND THE SECOND FIX INCOMPLETE: the emptiness test closes the bypass only when EVERY entry dies, and a partial loss (one `kind: Path` beside one valid entry) left a non-empty list, so the gate cleared at exit 0. Now keyed on any `in_progress_sources`-prefixed error, which is a complete witness for that validator. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/announcement/scripts/record_announcement.py": {
      "verdict": "guarded-all-doors",
      "reason": "_resolve_delivery_kind_check refuses via `unspeakable_version_message` before its own `except Exception` fallback. AN EARLIER VERSION OF THIS REASON CLAIMED THE TWO DOORS SHOULD LAND DIFFERENTLY AND THAT THE ASYMMETRY WAS CORRECT; a round-1 bounded review refuted it. The harm the claim cited (`requires_delivery_kind_agreement` comparing against a charness default) fires only for `delivery_kind: human-backend`, which the stimulus did not declare -- so the control could not exercise it. With the harm in play (`human-backend`, `--delivery-kind none`): `version: 1` raised `fail_delivery_kind_mismatch`, `version: !!int 9` exited 0 and appended a DURABLE RECORD. `adapter_resolved: false` was also not enforcement -- no production surface reads it. Closed by the resolver repair: the parse failure is now recorded in `errors`, the guard sees it, and both doors refuse. Probe record: charness-artifacts/probe/2026-08-19-announcement-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/critique/scripts/prepare_packet.py": {
      "verdict": "safe-checks-errors",
      "reason": "Line 78-81: `adapter = load_adapter(repo_root); if not adapter['valid']: yaml_output.emit_yaml({'ok': False, 'error': 'critique adapter invalid', 'adapter': adapter}); return 1` \u2014 refuses before using adapter data further; critique_adapter_lib.load_adapter sets valid = not errors (scripts/critique_adapter_lib.py:182,273) so a version refusal is included."
    },
    "skills/public/critique/scripts/scaffold_critique_artifact.py": {
      "verdict": "guarded-all-doors",
      "reason": "payload_for() now calls `_adapter_version_verdict.unspeakable_version_message(load_adapter, repo_root, adapter_name=\"critique-adapter.yaml\")` and raises SystemExit before the write target is read from the payload. Guarded at the READ SITE, not `main()`, so the refusal is a property of this function. A round-1 bounded review over rows 6-13 REFUTED the reason first written here (\"`payload_for` is the imported symbol in this skill\"): this skill's `payload_for` has NO production importer -- only tests. That is true only for retro (`plan_retro_run`) and debug (`plan_debug_run`). Read-site placement buys positional independence, which is the smaller and true claim. Measured flip on the real CLI at 0bcb6b227: a repo declaring `output_dir: docs/mine-critique` under `version: 9` got back `artifact_path: charness-artifacts/critique/2026-08-19-probe.md`, exit 0 -- an unhonored declaration RELOCATES this family's artifact rather than degrading its answer. Both polarity controls run: version 1 returns the declared path, no adapter returns the default. Probe record: charness-artifacts/probe/2026-08-19-scaffold-family-version-refusal.md. WAS a residual: this skill's resolver let a PARSER refusal's ValueError out, so `version: !!int 9` refuses with a raw traceback instead of a rendered verdict. It stops and does not relocate; the operator experience is the defect. CLOSED by #673: all five bare-loader resolvers now route through `adapter_lib.read_declared_adapter`, so this door renders a verdict and both predicates are live here. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/debug/scripts/plan_debug_run.py": {
      "verdict": "safe-checks-errors",
      "reason": "build_plan() sets `ok=bool(adapter.get('valid'))` (line 352) and main() returns `0 if payload['ok'] else 1` (line 381); gate_packets also echoes adapter['errors']/['warnings'] (lines 164-167) and ENVELOPE.adapter_echo surfaces errors (run_plan_envelope.py:206-221). resolve_adapter.load_adapter (skills/public/debug/scripts/resolve_adapter.py) runs declared_fields_after_version_check and reports errors, so a version refusal correctly drives ok=False/exit 1 even though the plan payload internally still computed some fields from defaults (true before slice 5 rows 6-13; see the update below). UPDATED 2026-08-19 after slice 5 rows 6-13, which changed this file's behavior as COLLATERAL and did not declare it until a bounded review found it: `scaffold_debug_artifact.payload_for` is now guarded, and this planner calls it BEFORE building its envelope, so an unhonored declaration now produces the guard's one-line refusal at exit 1 instead of a full diagnostic plan carrying `ok: false`, the `adapter-readiness` fail packet, and the `references/adapter-contract.md` required read. The exit code is unchanged and the plan no longer leaks a `write_artifact_path` computed from charness defaults -- which this reason previously described as acceptable and which was the weaker half. Whether the diagnostic plan should be restored for this one input class is an operator design call, staged in the goal's decision queue rather than decided here."
    },
    "skills/public/debug/scripts/scaffold_debug_artifact.py": {
      "verdict": "guarded-all-doors",
      "reason": "payload_for() now calls `_adapter_version_verdict.unspeakable_version_message(load_adapter, repo_root, adapter_name=\"debug-adapter.yaml\")` and raises SystemExit before the write target is read from the payload. Guarded at the READ SITE, not `main()`, and here the importer claim is MEASURED: `plan_debug_run` calls this `payload_for` directly, so an entrypoint-only refusal would have left that caller on charness defaults. Measured flip on the real CLI at 0bcb6b227: a repo declaring `output_dir: docs/mine-debug` under `version: 9` got back `artifact_path: charness-artifacts/debug/latest.md`, exit 0 -- an unhonored declaration RELOCATES this family's artifact rather than degrading its answer. Both polarity controls run: version 1 returns the declared path, no adapter returns the default. Probe record: charness-artifacts/probe/2026-08-19-scaffold-family-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/debug/scripts/persist_debug_artifact.py": {
      "verdict": "guarded-errors-only",
      "reason": "main() checks adapter_version_verdict.declarations_unhonored(adapter.get('errors')) before reading the adapter output directory or invoking the scaffold-selected persistence boundary; an unsupported version therefore refuses instead of selecting a default path."
    },
    "skills/public/gather/scripts/advise_google_workspace_path.py": {
      "verdict": "guarded-all-doors",
      "reason": "payload_for() now refuses before `gather_provider` is read. This advisor answers a CAPABILITY question, so an unhonored declaration does not degrade the advice -- it DENIES a capability the repo enabled. Measured flip at 1465689ac: a repo declaring `gather_provider.google_workspace.mode: host-mediated` under `version: 9` reported `provider_mode: none` and told the operator to stop with a missing-capability explanation, quoting a `mode=none` the adapter does not contain, exit 0. Control at version 1 reports `host-mediated`. gather routes through `simple_skill_adapter_lib`, so all three doors (version, parse, dropped line) are covered here. Probe record: charness-artifacts/probe/2026-08-19-inventory-gather-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/gather/scripts/gather_plan.py": {
      "verdict": "safe-checks-errors",
      "reason": "build_plan() sets `ok=bool(adapter.get('valid'))` (line 206) and main() returns `0 if payload['ok'] else 1` (line 249); the adapter-readiness gate_packet also reports status pass/fail off adapter['valid'] (lines 194-198). gather's resolve_adapter.load_adapter runs declared_fields_after_version_check (skills/public/gather/scripts/resolve_adapter.py:105), so version refusal correctly drives ok=False/exit 1."
    },
    "skills/public/gather/scripts/write_record.py": {
      "verdict": "guarded-errors-only",
      "reason": "Already in the given already-guarded list; confirmed via `_version_verdict.version_refused(payload.get('errors'))` at line 59, imported from scripts.adapter_version_verdict at line 47. LEVEL MIGRATION (#675): this row was `guarded` and is measured `guarded-errors-only` -- it asks `declarations_unhonored`, a predicate over `errors`, so a line the parser silently DROPS walks past it with `errors: []` and `valid: True`. Nothing was upgraded or downgraded by hand; the level is what the file's own calls establish, and the gate now refuses a row that claims a different one. Raising it to `guarded-all-doors` means routing through `unspeakable_version_message` / `refuse_unspeakable_version` or asking `declarations_dropped` explicitly."
    },
    "skills/public/handoff/scripts/chunked_routing_agentic_policy.py": {
      "verdict": "safe-checks-errors",
      "reason": "load_chunk_policy_config() at lines 84-92: `adapter = resolve.load_adapter(repo_root); if not adapter.get('valid', False): config['_adapter_report'] = {...}; return config` \u2014 degrades to defaults but surfaces the invalid report rather than silently applying declared-looking values; handoff resolve_adapter.load_adapter -> load_simple_adapter runs declared_fields_after_version_check (scripts/simple_skill_adapter_lib.py:169) so version refusal sets valid=False here."
    },
    "skills/public/handoff/scripts/chunked_routing_issue_config.py": {
      "verdict": "safe-checks-errors",
      "reason": "load_issue_source_config() at lines 72-75: `adapter = _load_sibling('resolve_adapter').load_adapter(repo_root); if adapter.get('valid') is False: config['enabled'] = False; return config, adapter_report(adapter)` \u2014 disables the issue source entirely and returns a report rather than silently using a defaulted config as if declared."
    },
    "skills/public/handoff/scripts/chunked_routing_issue_source.py": {
      "verdict": "safe-checks-errors",
      "reason": "build_issue_entries() at lines 229-239 loads the issue skill's adapter and checks `if adapter.get('valid') is False:` before using adapter_data, returning [] with a typed InvalidAdapter diagnostic instead of proceeding with defaulted backend/target data; also delegates load_issue_source_config() to the already-checked handoff adapter helper (chunked_routing_issue_config.py)."
    },
    "skills/public/handoff/scripts/chunked_routing_staleness.py": {
      "verdict": "safe-checks-errors",
      "reason": "resolve_states_for_repo() at lines 210-218 checks `if adapter.get('valid') is False:` immediately after loading the issue adapter and returns an empty states map plus a diagnostic instead of resolving issue states against defaulted backend/target data; callers treat the diagnostic as 'check not run' rather than 'nothing is closed'."
    },
    "skills/public/handoff/scripts/parse_handoff_entries.py": {
      "verdict": "guarded-all-doors",
      "reason": "_resolve_handoff_path now refuses via `_adapter_version_verdict.unspeakable_version_message(...)` before `adapter[\"artifact_path\"]` is read. The guard sits AFTER the explicit-path arm on purpose: `parse_handoff_entries.py <path>` asks the adapter nothing FOR THE PATH IT PARSES. A round-2 bounded review narrowed that: `<path> --with-issues` DOES reach this adapter via `chunked_routing_issue_source.build_issue_entries`, and is safe only because THAT helper checks `adapter.get(\"valid\") is False` and returns `enabled: False` -- the helper's property, not this exemption's. Now held by `test_the_explicit_path_arm_with_issues_does_not_act_on_a_charness_default`. Measured flip at 97dfc881a: a repo declaring `output_dir: docs/mine` under `version: 9` returned `handoff_path: <repo>/docs/handoff.md` with `ok: true` and a real `entry_count` -- a confident, well-formed answer about a file the repo does not use. Controls: version 1 returns `docs/mine/handoff.md`; no adapter returns the default; the explicit-path arm still works under version 9. Probe record: charness-artifacts/probe/2026-08-19-handoff-hitl-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/handoff/scripts/plan_handoff_run.py": {
      "verdict": "guarded-all-doors",
      "reason": "build_plan's adapter read now refuses before `artifact_path` and the `max_content_lines` ceiling are resolved. THIS FILE ALREADY READ `errors` AND THAT WAS NOT ENOUGH, which is the row's point: the read added an advisory and echoed errors into the envelope while never gating the `artifact_path` or `next_action` built from the defaulted data -- an echoed error beside an acted-on default. Measured flip at 97dfc881a: a repo declaring `output_dir: docs/mine` under `version: 9` planned against `artifact_path: docs/handoff.md`, exit 0, so every emitted command and gate packet named the wrong file. Control at version 1 returns `docs/mine/handoff.md`. Probe record: charness-artifacts/probe/2026-08-19-handoff-hitl-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/handoff/scripts/scaffold_handoff_artifact.py": {
      "verdict": "guarded-all-doors",
      "reason": "payload_for() now calls `_adapter_version_verdict.unspeakable_version_message(load_adapter, repo_root, adapter_name=\"handoff-adapter.yaml\")` and raises SystemExit before the write target is read from the payload. Guarded at the READ SITE, not `main()`, so the refusal is a property of this function. A round-1 bounded review over rows 6-13 REFUTED the reason first written here (\"`payload_for` is the imported symbol in this skill\"): this skill's `payload_for` has NO production importer -- only tests. That is true only for retro (`plan_retro_run`) and debug (`plan_debug_run`). Read-site placement buys positional independence, which is the smaller and true claim. Measured flip on the real CLI at 0bcb6b227: a repo declaring `output_dir: docs/mine` under `version: 9` got back `artifact_path: docs/handoff.md`, exit 0 -- an unhonored declaration RELOCATES this family's artifact rather than degrading its answer. Both polarity controls run: version 1 returns the declared path, no adapter returns the default. Probe record: charness-artifacts/probe/2026-08-19-scaffold-family-version-refusal.md. The first cut of this probe declared `artifact_path`, which this resolver ignores (it derives the path from `output_dir` plus a fixed filename), so the speakable-version control could not distinguish honored from fell-back. Re-measured on `output_dir`. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/hitl/scripts/bootstrap_review.py": {
      "verdict": "guarded-all-doors",
      "reason": "bootstrap_review() now calls `_adapter_version_verdict.unspeakable_version_message(load_adapter, repo_root, adapter_name=\"hitl-adapter.yaml\")` and raises SystemExit ABOVE the `mkdir`, so no write in the function is reachable under an unspeakable version -- earlier than the release-gate rows needed, because everything below that line writes and a refusal after `mkdir` leaves a half-bootstrapped session. Measured flip: a repo declaring `require_explicit_apply: false` got `require_explicit_apply: true` / `apply_mode: explicit-after-all-chunks` WRITTEN into state.yaml at exit 0. HARM DIRECTION RECORDED HONESTLY: the read is `.get(..., True)`, so an unspeakable version always lands on the STRICTER apply policy and cannot weaken this control; the defect is a policy the repo never declared persisted into a durable artifact read as the repo's own contract. Probe record: charness-artifacts/probe/2026-08-19-hitl-bootstrap-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/hitl/scripts/check_review_state.py": {
      "verdict": "safe-not-consequential",
      "reason": "load_session (scripts/hitl_review_artifact_lib.py line 25-26) uses adapter[\"runtime_dir\"], which resolve_adapter.py's `_runtime_dir(output_dir)` (skills/public/hitl/scripts/resolve_adapter.py) hard-codes to `.charness/hitl/runtime` and ignores its `output_dir` argument entirely."
    },
    "skills/public/hitl/scripts/render_report.py": {
      "verdict": "safe-not-consequential",
      "reason": "main() (line 37-45) passes `adapter[\"runtime_dir\"]` into render_report(); same `_runtime_dir()` hard-coded constant as check_review_state.py, unaffected by output_dir or by version-refusal reverting data to defaults."
    },
    "skills/public/hitl/scripts/sync_review_artifact.py": {
      "verdict": "guarded-all-doors",
      "reason": "The adapter read now refuses before `adapter[\"artifact_path\"]` is resolved, and this surface WRITES: without `--check` it calls `write_current_pointer_text` at that path. Measured flip at 97dfc881a: a repo declaring `output_dir: docs/mine-hitl` under `version: 9` reported `artifact_path: charness-artifacts/hitl/latest.md`, `status: synced`, exit 0 -- a review pointer written where the repo does not keep them while the one it does keep goes stale. Controls: version 1 returns `docs/mine-hitl/latest.md`; no adapter returns the default; the refusal leaves no file behind. Probe record: charness-artifacts/probe/2026-08-19-handoff-hitl-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/impl/scripts/init_adapter.py": {
      "verdict": "safe-checks-errors",
      "reason": "existing_adapter_is_valid() calls the impl resolver and checks `payload.get(\"valid\") is True` before the shared initializer writes or reuses the adapter path, so a refused adapter is not treated as an existing valid payload."
    },
    "skills/public/impl/scripts/survey_verification.py": {
      "verdict": "guarded-all-doors",
      "reason": "main() now refuses before `_group_specs(adapter[\"data\"])`. This is the row's own 'a read is not a check' shape at runtime: `adapter[\"valid\"]` was ALREADY in the emitted payload and never branched on. Measured at 724fe8a55: a repo declaring `verification_tools: [mytool]` under `version: 9` emitted `adapter_valid: false` BESIDE `tool_checks: []`, exit 0 -- the survey reporting the repo configured no verification tools in the same breath as reporting it could not read the file that says otherwise. At version 1: `spec: mytool` and `adapter_valid: true`. `impl` routes through `simple_skill_adapter_lib`, so both doors render the same refusal here. Probe record: charness-artifacts/probe/2026-08-19-narrative-impl-version-refusal.md. ROUND-1 REVIEW FOUND A REGRESSION THIS ROW INTRODUCED AND IT IS CLOSED, not filed: `adapter_lib._parse_block` drops an over-indented line into WARNINGS rather than errors, so the guard's `errors`-only predicate answered False and this CLI printed `adapter_valid: true` beside `tool_checks: []` at exit 0 -- worse than the pre-guard base, which printed `false`. `adapter_version_verdict.declarations_dropped` now reads that warning channel and renders its own message; the same input exits 1. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/issue/scripts/issue_create.py": {
      "verdict": "safe-checks-errors",
      "reason": "_resolve_backend (line 257-262): `if not adapter[\"valid\"]: return {..., \"adapter_ok\": False}`; command_create/command_verify_create (line 265-270, 299-304) check `resolved[\"adapter_ok\"]` and emit `{\"ok\": False, \"adapter\": resolved[\"adapter\"]}` with exit code 1 before touching `adapter[\"data\"]`."
    },
    "skills/public/issue/scripts/issue_plan.py": {
      "verdict": "safe-checks-errors",
      "reason": "command_plan (line 233-236): `adapter = adapter_module.load_adapter(repo_root); if not adapter[\"valid\"]: emit({...}); return 1` runs before any use of `adapter[\"data\"]` for target resolution or plan building."
    },
    "skills/public/issue/scripts/issue_tool.py": {
      "verdict": "safe-checks-errors",
      "reason": "`_resolve_backend` (line 39-44) and its callers `_run_backend_command` (line 47-51) / `_run_adapter_payload` (line 62-66) all check `adapter[\"valid\"]`/`adapter_ok` and refuse (exit 1) before using `adapter[\"data\"]`; `command_preflight` (line 76-81) relies on `build_preflight_payload`'s `ok = resolved[\"adapter_ok\"] and backend_ok(selected)` (skills/public/issue/scripts/issue_backend.py line 334) so an invalid adapter still surfaces `ok: false` and exit 1 even though the backend used for the probe silently falls back to the default."
    },
    "skills/public/narrative/scripts/map_sources.py": {
      "verdict": "guarded-all-doors",
      "reason": "main() now refuses before `adapter[\"data\"]` is read for source_documents/mutable_documents/remote_name. This command's whole output is a MAP of the repo's own narrative sources, so an unhonored declaration substitutes a different document set rather than degrading the answer -- and the emitted payload carries no adapter validity field, so no consumer could tell. Measured at 724fe8a55: a repo declaring `source_documents: [docs/mine-narrative.md]` under `version: 9` reported `path: README.md` (the inferred default), exit 0; at version 1 it reports the declared path. WAS a residual: `narrative` was one of five resolvers that let a PARSER refusal's ValueError out, so that door refused with a raw traceback; filed as #673. Probe record: charness-artifacts/probe/2026-08-19-narrative-impl-version-refusal.md. WAS a residual: narrative was one of five resolvers that called `adapter_lib.load_yaml_file` bare and discard the uninterpreted-line sink, so a silently DROPPED declaration leaves `errors: []` and `warnings` empty and neither `declarations_unhonored` nor `declarations_dropped` can see it. #673. CLOSED by #673: all five bare-loader resolvers now route through `adapter_lib.read_declared_adapter`, so this door renders a verdict and both predicates are live here. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/quality/references/find_inline_prompt_bulk.py": {
      "verdict": "guarded-all-doors",
      "reason": "_adapter_prompt_policy() now refuses via `unspeakable_version_message` before `prompt_asset_policy` is read, and only on the `--from-adapter` path -- the explicit `--source-glob` / `--min-multiline-chars` flags exist so a caller can scan without asking the adapter anything, and a control pins that arm under a refused version. Measured flip at abbcd9bff: a repo declaring `source_globs: [\"src/**/*.py\"]` and `min_multiline_chars: 40` under `version: 9` scanned `source_globs: []` at `min_multiline_chars: 400` (both charness defaults) and reported `findings: []`; the same repo at version 1 reports findings. A scanner told to use the repo's declared policy scanned NOTHING and reported clean. This file lives under `references/` with no skill-runtime bootstrap, so it reaches the verdict module by an ancestor walk and answers None when absent -- the availability gap is stated rather than assumed away. WAS a residual: quality's resolver called `adapter_lib.load_yaml_file` bare, so only the version door was live here. CLOSED by #673: that resolver now routes through `adapter_lib.read_declared_adapter`, so the parse-refusal and dropped-line doors are both live here. Probe record: charness-artifacts/probe/2026-08-19-prompt-bulk-version-refusal.md. ROUND-2 REVIEW FOUND A CHEAPER INPUT TO THE SAME HARM, needing no version change: `source_globs` written as a STRING makes `merge_prompt_asset_policy` return the charness default `[]` while `valid: false` goes unread, so the scan covered nothing and reported clean. Now also refuses on any `prompt_asset_policy`-prefixed error -- consumer-local, resting on this command's own contract that `--from-adapter` means the repo's declared policy, and NOT a widening of `adapter_version_verdict`, which rightly forbids refusing on ordinary invalidity in general. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/quality/scripts/check_changed_line_coverage.py": {
      "verdict": "safe-checks-errors",
      "reason": "run() (line 111-114): `adapter_errors = list(adapter.get(\"errors\") or []); if adapter_errors: return {\"ok\": False, \"adapter_errors\": adapter_errors, \"inert\": False, \"blocking\": []}` runs before `config = adapter[\"data\"].get(\"changed_line_mutation_gate\")` is read; main() then returns exit 1 when `report.get(\"adapter_errors\")` (line 210-211), and verdict() renders `verdict: adapter-invalid` (line 164-169)."
    },
    "skills/public/quality/scripts/check_dup_ratchet.py": {
      "verdict": "safe-checks-errors",
      "reason": "run() at line 231-235 calls load_quality_adapter_strict then checks adapter.get('errors') BEFORE touching adapter['data'], returning status 'adapter-invalid' with the errors surfaced; main() at line 324 returns 1 because report.get('ok') is falsy in that branch, so it fails closed rather than falling back to defaults."
    },
    "skills/public/quality/scripts/check_regenerable_facts.py": {
      "verdict": "safe-checks-errors",
      "reason": "main() at line 162-166 checks `not adapter.get('valid', True)` and discards the adapter (sets refusal, adapter=None) rather than scanning with its data; line 186-193 marks the run failed (`report.get('adapter_refusal')`) and main() returns 1, so it never substitutes defaults for a declared-but-unspeakable version."
    },
    "skills/public/quality/scripts/check_runtime_budget.py": {
      "verdict": "guarded-all-doors",
      "reason": "`_refuse_unhonored_adapter` runs before BOTH points where `load_adapter` is handed to a lib (the sizing path and the evaluate path). Base: two advisory-shaped findings asserting the OPPOSITE of the declaration -- `quality adapter has no effective runtime budget for the selected profile` and `quality adapter has no startup_probes` -- exit 0. Control at version 1: `WARN pytest: no sample yet (budget 70000ms)`. Guarded at the READ SITE and measured at 00c50ed3f against a repo declaring `output_dir: docs/mine-q`, a `startup_probes` entry and `runtime_budgets: {pytest: 70000}` in the shapes the contract actually reads. NOTE ON THE MEASUREMENT, corrected TWICE: the initial stimulus used field shapes this contract ignores, so the speakable-version control returned the same output as the refused one and the flip was UNPROVEN for two of the five. A bounded review then found the CORRECTION still wrong in the published probe record -- `command` was left as a flow sequence, which `adapter_lib` reads as a plain string -- so replaying the record's own stimulus reproduced the defect. Both arms re-run again on the block form the test fixture always used. Probe record: charness-artifacts/probe/2026-08-19-quality-readers-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/quality/scripts/check_standing_doc_provenance.py": {
      "verdict": "safe-checks-errors",
      "reason": "run() at line 55-65 checks adapter.get('errors') and returns early (ok=False, findings=[]) before reading config['standing_doc_provenance']; main() at line 116-118 exits 1 with 'standing-doc provenance check skipped: repair the quality adapter.'"
    },
    "skills/public/quality/scripts/inventory_ci_recoverable_gates.py": {
      "verdict": "guarded-all-doors",
      "reason": "build_report() now refuses before handing `load_adapter` to `runtime_budget_lib`. Base: `0 cost-ranked gate(s)` plus `no cost-ranked standing gates: configure runtime_budgets` -- instructing the operator to configure what the operator configured -- exit 0. Control at version 1: `1 cost-ranked gate(s) ... keep-local pytest`. Guarded at the READ SITE and measured at 00c50ed3f against a repo declaring `output_dir: docs/mine-q`, a `startup_probes` entry and `runtime_budgets: {pytest: 70000}` in the shapes the contract actually reads. NOTE ON THE MEASUREMENT, corrected TWICE: the initial stimulus used field shapes this contract ignores, so the speakable-version control returned the same output as the refused one and the flip was UNPROVEN for two of the five. A bounded review then found the CORRECTION still wrong in the published probe record -- `command` was left as a flow sequence, which `adapter_lib` reads as a plain string -- so replaying the record's own stimulus reproduced the defect. Both arms re-run again on the block form the test fixture always used. Probe record: charness-artifacts/probe/2026-08-19-quality-readers-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/quality/scripts/inventory_cli_ergonomics.py": {
      "verdict": "safe-checks-errors",
      "reason": "main() at line 91-114 uses load_quality_adapter_permissive and, although it proceeds to scan with whatever vendored_paths the (possibly-defaulted) data yields, it surfaces adapter_valid/adapter_errors/adapter_warnings in the emitted structured payload and explicitly prints 'adapter=invalid: advisory inventory is best-effort until adapter errors are repaired.' at line 125-126 -- an advisory-only inventory that discloses degradation rather than silently claiming a clean/authoritative scan."
    },
    "skills/public/quality/scripts/inventory_cli_side_effect_probes.py": {
      "verdict": "safe-checks-errors",
      "reason": "Same disclosure pattern as inventory_cli_ergonomics.py: adapter_valid/adapter_errors are placed in the payload at line 81-85 and printed at line 89-90. Note: contract_paths for --execute-probes come from explicit --contract-file CLI args (line 71), not from adapter data, and each probe entry additionally requires its own declared safe_to_execute:true (cli_side_effect_probe_lib.py) before anything runs, so an unspeakable adapter version only widens auto-discovery of candidate contract files (vendored_filter becomes None when vendored==[]), not which commands actually execute."
    },
    "skills/public/quality/scripts/inventory_command_dominance.py": {
      "verdict": "no-version-validation",
      "reason": "Reads `.agents/*-adapter.yaml` with a raw YAML load and never reconciles `version`, so there is no `errors` for any consumer to check. A DIFFERENT defect from the unguarded rows: the fix is wiring this onto the shared resolver, not adding a check."
    },
    "skills/public/quality/scripts/inventory_lint_ignores.py": {
      "verdict": "safe-checks-errors",
      "reason": "main() at line 58-71 reads adapter.get('valid')/errors and folds them into the payload; line 84-85 prints 'adapter=invalid: advisory inventory is best-effort until adapter errors are repaired.' when adapter_valid is False -- disclosed degradation, not a silent default swap, and this script never gates (always returns 0)."
    },
    "skills/public/quality/scripts/inventory_skill_ergonomics.py": {
      "verdict": "safe-checks-errors",
      "reason": "main() at line 226-230 puts adapter_valid/adapter_errors/adapter_warnings into payload, and line 246-247 prints the same 'advisory inventory is best-effort' disclosure when adapter_valid is False before continuing with defaulted skill_ergonomics_skill_paths/vendored_paths."
    },
    "skills/public/quality/scripts/inventory_standing_test_economics.py": {
      "verdict": "safe-checks-errors",
      "reason": "main() at line 122-126 attaches adapter_valid/adapter_errors/adapter_warnings to the payload, and line 155-156 prints 'adapter=invalid: advisory inventory is best-effort until adapter errors are repaired.' when adapter_valid is False -- the degraded test_file_discovery default is disclosed, not silent, and this advisory script always returns 0."
    },
    "skills/public/quality/scripts/measure_startup_probes.py": {
      "verdict": "guarded-all-doors",
      "reason": "evaluate() now refuses before `adapter[\"data\"].get(\"startup_probes\")` is read. Base: `No startup probes matched the selected class.` over a repo that declared one, exit 0. Control at version 1: `OK             probe-one: latest 16ms, median 16ms (standing, warm, direct)`. Guarded at the READ SITE and measured at 00c50ed3f against a repo declaring `output_dir: docs/mine-q`, a `startup_probes` entry and `runtime_budgets: {pytest: 70000}` in the shapes the contract actually reads. NOTE ON THE MEASUREMENT, corrected TWICE: the initial stimulus used field shapes this contract ignores, so the speakable-version control returned the same output as the refused one and the flip was UNPROVEN for two of the five. A bounded review then found the CORRECTION still wrong in the published probe record -- `command` was left as a flow sequence, which `adapter_lib` reads as a plain string -- so replaying the record's own stimulus reproduced the defect. Both arms re-run again on the block form the test fixture always used. Probe record: charness-artifacts/probe/2026-08-19-quality-readers-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/quality/scripts/nose_inventory_scope_lib.py": {
      "verdict": "safe-checks-errors",
      "reason": "_adapter_inventory_paths() at line 24-30 collects payload.get('errors'); payload_for_args() at line 106-114 checks `if adapter_errors:` and returns status='error', exit_code=1, notes=['quality adapter scope is invalid; inventory not run.'] BEFORE resolving nose_bin or scanning any roots -- refuses rather than falling back to DEFAULT_PATHS."
    },
    "skills/public/quality/scripts/propose_mutation_testing.py": {
      "verdict": "safe-checks-errors",
      "reason": "classify() at line 79-81 returns 'blocked' when adapter_payload.get('errors') is truthy, before reading the mutation_testing block; main() at line 193 only runs _execute_install when `status == 'missing'`, so a 'blocked' status (adapter errors) never triggers --execute, and _recommendation() at line 121 reports 'adapter validation failed with N errors; resolve them before proposing.'"
    },
    "skills/public/quality/scripts/public_spec_adapter_policy.py": {
      "verdict": "safe-checks-errors",
      "reason": "load_quality_adapter_data() at line 45-49 checks `not payload.get('valid', True)` and raises ValueError('Invalid quality adapter: ...') instead of returning payload['data'] (the inferred-defaults dict) to the caller."
    },
    "skills/public/quality/scripts/public_spec_inventory_lib.py": {
      "verdict": "safe-checks-errors",
      "reason": "inventory() at line 55 calls load_quality_adapter_data(repo_root) from public_spec_adapter_policy, which raises on an invalid adapter (see that file's line 46-49); the sole caller, skills/public/quality/scripts/inventory_public_spec_quality.py line 55-57, wraps that call in try/except ValueError, so the refusal propagates rather than being swallowed into a default scan."
    },
    "skills/public/quality/scripts/quality_declaration_lifecycle.py": {
      "verdict": "safe-checks-errors",
      "reason": "build_declaration_lifecycle (line 295-326) loads the quality adapter, then at line 321 checks `if adapter.get(\"valid\") is not True:` and returns a `status: invalid` report with a gap entry *before* line 328 reads the raw YAML (`raw = yaml_lib.load_yaml_file(...)`) or computes any preset/command/surface routing from it. Since `valid = not errors` and a version refusal always appends to `errors`, this check catches the refused-version case correctly and short-circuits before acting on inferred defaults."
    },
    "skills/public/quality/scripts/render_runtime_summary.py": {
      "verdict": "guarded-all-doors",
      "reason": "build_report() now refuses before handing `load_adapter` to `runtime_budget_lib`. Base: `runtime visibility: weak due to runtime_visibility_missing_budgets, runtime_visibility_missing_startup_probes`, exit 0. Control at version 1: `runtime visibility: configured.` Guarded at the READ SITE and measured at 00c50ed3f against a repo declaring `output_dir: docs/mine-q`, a `startup_probes` entry and `runtime_budgets: {pytest: 70000}` in the shapes the contract actually reads. NOTE ON THE MEASUREMENT, corrected TWICE: the initial stimulus used field shapes this contract ignores, so the speakable-version control returned the same output as the refused one and the flip was UNPROVEN for two of the five. A bounded review then found the CORRECTION still wrong in the published probe record -- `command` was left as a flow sequence, which `adapter_lib` reads as a plain string -- so replaying the record's own stimulus reproduced the defect. Both arms re-run again on the block form the test fixture always used. Probe record: charness-artifacts/probe/2026-08-19-quality-readers-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/quality/scripts/resolve_quality_artifact.py": {
      "verdict": "guarded-all-doors",
      "reason": "payload_for() now refuses before `output_dir` is read; every ARTIFACT path it returns derives from that value (the `refresh_current_pointer_argv` entry does not; a bounded review corrected the universal), so an unhonored declaration RELOCATES the quality artifact. Base: `artifact_path: charness-artifacts/quality/latest.md`, exit 0. Control at version 1: `docs/mine-q/latest.md`. Guarded at the READ SITE and measured at 00c50ed3f against a repo declaring `output_dir: docs/mine-q`, a `startup_probes` entry and `runtime_budgets: {pytest: 70000}` in the shapes the contract actually reads. NOTE ON THE MEASUREMENT, corrected TWICE: the initial stimulus used field shapes this contract ignores, so the speakable-version control returned the same output as the refused one and the flip was UNPROVEN for two of the five. A bounded review then found the CORRECTION still wrong in the published probe record -- `command` was left as a flow sequence, which `adapter_lib` reads as a plain string -- so replaying the record's own stimulus reproduced the defect. Both arms re-run again on the block form the test fixture always used. Probe record: charness-artifacts/probe/2026-08-19-quality-readers-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/quality/scripts/runtime_budget_lib.py": {
      "verdict": "guarded-upstream",
      "reason": "evaluate() (line 228-236) does `adapter = load_adapter(repo_root); adapter_data = adapter[\"data\"]` and immediately feeds `adapter_data` into `selected_runtime_profile` and `profile_budgets` (runtime ceilings) with no check of `adapter[\"errors\"]`/`adapter[\"valid\"]` anywhere in the module; `check_runtime_budget.py`'s main() ALSO never checked adapter validity, only `report[\"profile_config_errors\"]` -- TRUE BEFORE slice 5 rows 14-18 and corrected here after a bounded review found the clause standing beside its own fix: at HEAD that CLI calls `_refuse_unhonored_adapter` before BOTH loader hand-offs, and `profile_config_errors` is read at line 145-148, not 111-133 (a different, profile-shape-only error list) and `report[\"violations\"]`. UNGUARDABLE BY CONSTRUCTION, and credited only through its enumerated covering rows: this module takes its `load_adapter` INJECTED, so it cannot know which adapter it is reading or render a refusal naming that file. Slice 5 rows 14-18 guarded every production CALLER at the point the loader is handed over (`check_runtime_budget`, `render_runtime_summary`, `inventory_ci_recoverable_gates`), which closes the measured harm -- but the property is the callers', not this module's, and a future caller that forgets is unguarded again with nothing structural to stop it. CALLER-COVERAGE MEASURED 2026-08-19. This row was `accepted-risk-unguarded` at the time, before a level existed for it, because the property belongs to its CALLERS and not to this file. Every production caller of `evaluate` is guarded: `check_runtime_budget`, `render_runtime_summary` and `inventory_ci_recoverable_gates`, all in slice 5 rows 14-18. A guard here is not merely redundant, it is wrong or impossible: this module takes its `load_adapter` INJECTED, so it cannot know which adapter it is reading and cannot name a file in a refusal. The census's unit is the DIRECT read site, so a row whose callers are all guarded and a row whose callers are not look identical here -- and the gate's own stated blind class ('a file that reads an adapter through a HELPER in another module is invisible') means 'every caller' is a claim about the callers enumerated, not a completeness proof. MIGRATED to `guarded-upstream` (#675 round 1). This row met the level's stated criterion -- \"cannot guard itself, everything upstream is guarded\" -- and its own reason already enumerated the covering rows, but it sat in `accepted-risk-unguarded` because that was the only token available. The gate's headline `ACCEPTED RISK: N consumer(s) would use a charness default` was therefore FALSE for it by the manifest's own text, which is exactly the measurement distortion #675 reports. The enumerated rows are now in `covering_rows`, where the gate checks each one carries a guarded verdict.",
      "covering_rows": [
        "skills/public/quality/scripts/check_runtime_budget.py",
        "skills/public/quality/scripts/render_runtime_summary.py",
        "skills/public/quality/scripts/inventory_ci_recoverable_gates.py"
      ]
    },
    "skills/public/quality/scripts/runtime_budget_sizing_lib.py": {
      "verdict": "guarded-upstream",
      "reason": "suggest_budgets() (line 134-162) does `adapter_data = load_adapter(repo_root)[\"data\"]` at line 153 with no error/valid check anywhere in the file, then uses `adapter_data` to select the runtime profile (`selected_runtime_profile`) and to derive/round proposed budget-ms ceilings via `suggest_profile_budgets`. UNGUARDABLE BY CONSTRUCTION, and credited only through its enumerated covering rows: this module takes its `load_adapter` INJECTED, so it cannot know which adapter it is reading or render a refusal naming that file. Slice 5 rows 14-18 guarded every production CALLER at the point the loader is handed over (`check_runtime_budget`, `render_runtime_summary`, `inventory_ci_recoverable_gates`), which closes the measured harm -- but the property is the callers', not this module's, and a future caller that forgets is unguarded again with nothing structural to stop it. CALLER-COVERAGE MEASURED 2026-08-19. This row was `accepted-risk-unguarded` at the time, before a level existed for it, because the property belongs to its CALLERS and not to this file. Its only production caller is `check_runtime_budget --suggest-budgets`, guarded in row 16. A guard here is not merely redundant, it is wrong or impossible: same injected-loader seam as `runtime_budget_lib`. The census's unit is the DIRECT read site, so a row whose callers are all guarded and a row whose callers are not look identical here -- and the gate's own stated blind class ('a file that reads an adapter through a HELPER in another module is invisible') means 'every caller' is a claim about the callers enumerated, not a completeness proof. MIGRATED to `guarded-upstream` (#675 round 1). This row met the level's stated criterion -- \"cannot guard itself, everything upstream is guarded\" -- and its own reason already enumerated the covering rows, but it sat in `accepted-risk-unguarded` because that was the only token available. The gate's headline `ACCEPTED RISK: N consumer(s) would use a charness default` was therefore FALSE for it by the manifest's own text, which is exactly the measurement distortion #675 reports. The enumerated rows are now in `covering_rows`, where the gate checks each one carries a guarded verdict.",
      "covering_rows": [
        "skills/public/quality/scripts/check_runtime_budget.py"
      ]
    },
    "skills/public/quality/scripts/scaffold_quality_artifact.py": {
      "verdict": "guarded-all-doors",
      "reason": "payload_for() now calls `_adapter_version_verdict.unspeakable_version_message(load_adapter, repo_root, adapter_name=\"quality-adapter.yaml\")` and raises SystemExit before the write target is read from the payload. Guarded at the READ SITE, not `main()`, so the refusal is a property of this function. A round-1 bounded review over rows 6-13 REFUTED the reason first written here (\"`payload_for` is the imported symbol in this skill\"): this skill's `payload_for` has NO production importer -- only tests. That is true only for retro (`plan_retro_run`) and debug (`plan_debug_run`). Read-site placement buys positional independence, which is the smaller and true claim. Measured flip on the real CLI at 0bcb6b227: a repo declaring `output_dir: docs/mine-quality` under `version: 9` got back `artifact_path: charness-artifacts/quality/latest.md`, exit 0 -- an unhonored declaration RELOCATES this family's artifact rather than degrading its answer. Both polarity controls run: version 1 returns the declared path, no adapter returns the default. Probe record: charness-artifacts/probe/2026-08-19-scaffold-family-version-refusal.md. WAS a residual: this skill's resolver let a PARSER refusal's ValueError out, so `version: !!int 9` refuses with a raw traceback instead of a rendered verdict. It stops and does not relocate; the operator experience is the defect. CLOSED by #673: all five bare-loader resolvers now route through `adapter_lib.read_declared_adapter`, so this door renders a verdict and both predicates are live here. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/quality/scripts/validate_skill_ergonomics.py": {
      "verdict": "safe-checks-errors",
      "reason": "evaluate() (line 253-272) uses `load_quality_adapter_strict`, computes `adapter_errors = adapter.get(\"errors\", []) if adapter.get(\"valid\") is not True else []` at line 255, then at line 262 `if adapter_errors:` returns a `discovery_skipped_reason: \"adapter_invalid\"` result with `checked_skills: []`, before any rules/paths are actually applied to discover or check skills."
    },
    "skills/public/release/scripts/audit_public_release_narrative.py": {
      "verdict": "safe-checks-errors",
      "reason": "build_payload() (line 318-332) calls `adapter = load_adapter(repo_root)` then at line 326 `if not adapter[\"valid\"]:` returns a `status: \"blocked\"` payload listing `adapter[\"errors\"]`, before line 332 reads `adapter[\"data\"][\"output_dir\"]` to resolve the release-artifact path."
    },
    "skills/public/release/scripts/bump_version.py": {
      "verdict": "safe-checks-errors",
      "reason": "main() (line 95-98) does `adapter = load_adapter(repo_root)` then `if not adapter[\"valid\"]: raise SystemExit(...)` at line 97-98, before line 99-100 reads `data[\"packaging_manifest_path\"]` or later `data[\"sync_command\"]` to write the manifest and run the sync command."
    },
    "skills/public/release/scripts/check_fresh_checkout_probes.py": {
      "verdict": "safe-checks-errors",
      "reason": "build_payload() (line 121-131) does `adapter = load_adapter(repo_root)` then `if not adapter[\"valid\"]:` at line 123 returns `status: \"blocked\"` with the adapter's errors, before line 131 reads `adapter[\"data\"].get(\"fresh_checkout_probes\", [])`."
    },
    "skills/public/release/scripts/check_real_host_proof.py": {
      "verdict": "guarded-all-doors",
      "reason": "build_payload() now calls `_adapter_version_verdict.unspeakable_version_message(load_adapter, repo_root, adapter_name=\"release-adapter.yaml\")` and raises SystemExit before `real_host_required_surfaces`/`real_host_required_path_globs`/`real_host_checklist` are read from `adapter[\"data\"]`. Guarded at the READ SITE. All three of `publish_release_cli`, `publish_release_plan` and `plan_release_run` re-export this `build_payload` as `build_real_host_payload`, but a round-1 bounded review refuted the harm claim that a `main()`-only refusal would have left them reading defaults: under an unhonored declaration all three stop earlier (`_valid_adapter_data`; `if adapter.get(\"valid\")`, which additionally wraps the call in `except SystemExit`). Measured count of importers reaching a charness default here: ZERO. Read-site placement buys positional independence. Measured flip, not argued: a repo declaring `real_host_required_path_globs: [\"src/**\"]` handed `src/a.py` printed `real_host=not-required: This repo declares no release-time real-host proof triggers` at exit 0, and `real_host=required` at a speakable version -- an INVERTED verdict on the permissive side, not a degraded one. Probe record: charness-artifacts/probe/2026-08-19-real-host-proof-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/release/scripts/check_requested_review_gate.py": {
      "verdict": "guarded-all-doors",
      "reason": "build_payload() refuses via `_adapter_version_verdict.unspeakable_version_message(...)` before `requested_review_commands` is read from `adapter[\"data\"]` and executed. Measured flip on the real CLI: a repo declaring `requested_review_commands` under a refused version got `configuration status: not_configured` and the advisory-only warning, exit 0 -- the OPPOSITE of what the repo declared. CORRECTED AFTER A ROUND-1 BOUNDED REVIEW, twice: (1) the earlier reason claimed a refusal in `main()` \"would have left two importers reading charness defaults\" -- refuted, both stop earlier under an unhonored declaration (`publish_release_cli._valid_adapter_data`, and `plan_release_run` behind `if adapter.get(\"valid\")`), so the measured count is ZERO and read-site placement buys positional independence rather than a removed live harm; (2) the earlier reason also cited a `block-if-unconfigured` policy, which `resolve_adapter` does not accept (`{warn-if-unconfigured, advisory-only}`), so that clause described a configuration no repo can hold. Probe record: charness-artifacts/probe/2026-08-19-requested-review-gate-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/release/scripts/current_release.py": {
      "verdict": "guarded-all-doors",
      "reason": "build_payload() calls `adapter_version_verdict.unspeakable_version_message` before reading the payload and raises SystemExit on a refused version. Guarded at the READ SITE, so the three modules importing `build_payload` directly (`publish_release_cli`, `publish_release_plan`, `plan_release_run`) inherit it. Paid down by a measured behavioral flip, not a verdict-string edit: probe record charness-artifacts/probe/2026-08-19-current-release-version-refusal.md -- base bda87440c answered a repo declaring `acme-harness` and `vendor/mypkg` with a package_id inferred from its own directory name and two paths that do not exist, at exit 0 with `valid: false` echoed and unused; head refuses at exit 1. Both polarity controls run; pinned by tests/quality_gates/test_current_release_version_refusal.py. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/release/scripts/plan_release_run.py": {
      "verdict": "guarded-all-doors",
      "reason": "build_plan() now refuses an unspeakable version at its own read site before `update_instructions`, `release_record_path` and `drafted_notes_candidates` read `data`. WEAKER row than the gates, recorded as such: at base the planner did NOT go silent -- it printed `next_action=repair_adapter: Release adapter is invalid.` at exit 0, with the two `valid`-gated evidence packets null -- but it still emitted a plan asserting `package_id: <the repo directory's own name>` and two nonexistent paths, with `blockers: []`. The row for `current_release` ALONE already made this exit 1 by inheritance (`build_release_payload` runs first and its SystemExit escapes the surrounding `except Exception`); a round-1 bounded review caught this reason also crediting `check_real_host_proof`, which cannot contribute -- that call is behind `if adapter.get(\"valid\")` and inside `except SystemExit`, so its refusal becomes a payload field, never an exit; that inheritance is POSITIONAL, and `tests/quality_gates/test_release_planner_version_refusal.py::test_the_refusal_is_this_file_s_own_not_inherited_from_a_callee` isolates the guard by stubbing the callee. Probe record: charness-artifacts/probe/2026-08-19-release-planner-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/release/scripts/publish_release_cli.py": {
      "verdict": "safe-checks-errors",
      "reason": "`_valid_adapter_data` (lines 276-281) calls `load_adapter(repo_root)` then does `if not adapter[\"valid\"]: raise SystemExit(f\"release adapter is invalid: {adapter['errors']}\")` before any caller (`_load_adapter_and_gate`, `run_prep_update_instructions`) reads adapter data, so a version-refused adapter (valid=False) stops the run instead of silently using inferred defaults."
    },
    "skills/public/release/scripts/publish_release_narrative_gate.py": {
      "verdict": "safe-checks-errors",
      "reason": "Every load_adapter call gates on validity before using `adapter[\"data\"]`: `_drafted_notes_for` (line 60) returns [] when `not adapter[\"valid\"]`, `derived_claims_required` (line 73) defaults to the fail-safe True, and `_known_versions` (line 98) only reads `packaging_manifest_path` when `adapter[\"valid\"]` is true."
    },
    "skills/public/release/scripts/publish_release_preflight.py": {
      "verdict": "safe-not-consequential",
      "reason": "FALSE POSITIVE of the call-site rule: `_load_adapter_preflight_helper()` (L25) imports a sibling PYTHON MODULE whose name contains `adapter`. It loads no adapter payload. Recorded rather than special-cased, because a name-shaped matcher that over-matches fails safe and a narrowed one hides consumers."
    },
    "skills/public/release/scripts/publish_release_retro.py": {
      "verdict": "safe-checks-errors",
      "reason": "L169 loads the retro adapter and fails closed on `adapter[\"valid\"]` before deriving `output_dir`."
    },
    "skills/public/retro/scripts/check_auto_trigger.py": {
      "verdict": "safe-checks-errors",
      "reason": "`no_config_payload` (line 112) and `build_payload` (line 318) both call `adapter_unreadable_reasons(adapter)`, which is `scripts.adapter_lib.unreadable_reasons` and starts from `adapter.get(\"errors\")` (adapter_lib.py:420) \u2014 so a version-refusal error forces `unreadable` truthy, `intentional_empty=False`, `configuration_status=\"adapter-unreadable\"` (line 164-165), `state=not-established`, and exit code `UNDETERMINED_EXIT=3` instead of a silent `triggered:false`."
    },
    "skills/public/retro/scripts/persist_retro_artifact.py": {
      "verdict": "guarded-errors-only",
      "reason": "Line 73: `if _version_verdict.version_refused(adapter.get(\"errors\")):` prints a refusal naming the unspeakable version and `return 1` before `output_dir`/`summary_path` are read from `adapter[\"data\"]` (already on the ALREADY-GUARDED list; re-confirmed by reading). LEVEL MIGRATION (#675): this row was `guarded` and is measured `guarded-errors-only` -- it asks `declarations_unhonored`, a predicate over `errors`, so a line the parser silently DROPS walks past it with `errors: []` and `valid: True`. Nothing was upgraded or downgraded by hand; the level is what the file's own calls establish, and the gate now refuses a row that claims a different one. Raising it to `guarded-all-doors` means routing through `unspeakable_version_message` / `refuse_unspeakable_version` or asking `declarations_dropped` explicitly."
    },
    "skills/public/retro/scripts/plan_retro_run.py": {
      "verdict": "safe-checks-errors",
      "reason": "`build_plan` sets `ok=bool(adapter.get(\"valid\"))` (line 386) so `main()` exits 1 on an invalid adapter (line 438); the `adapter-readiness` gate_packet reports `status=\"fail\"` with `errors=adapter.get(\"errors\", [])` (lines 283-289); and `_required_reads` (lines 200-201) adds an explicit `references/adapter-contract.md` read with reason \"adapter is missing or invalid; repair before relying on adapter paths\" whenever `not adapter.get(\"valid\")`. The plan used to compute `scaffold[\"write_artifact_path\"]` via the then-unguarded scaffold module regardless, but that value is presented alongside an unambiguous fail signal (ok=False, exit 1, fail status, errors, explicit repair-first advisory) rather than silently as success. UPDATED 2026-08-19 after slice 5 rows 6-13, which changed this file's behavior as COLLATERAL and did not declare it until a bounded review found it: `scaffold_retro_artifact.payload_for` is now guarded, and this planner calls it BEFORE building its envelope, so an unhonored declaration now produces the guard's one-line refusal at exit 1 instead of a full diagnostic plan carrying `ok: false`, the `adapter-readiness` fail packet, and the `references/adapter-contract.md` required read. The exit code is unchanged and the plan no longer leaks a `write_artifact_path` computed from charness defaults -- which this reason previously described as acceptable and which was the weaker half. Whether the diagnostic plan should be restored for this one input class is an operator design call, staged in the goal's decision queue rather than decided here."
    },
    "skills/public/retro/scripts/prepare_packet.py": {
      "verdict": "safe-checks-errors",
      "reason": "Line 85: `if not adapter[\"valid\"]: yaml_output.emit_yaml({...\"error\": \"retro adapter invalid\"...}); return 1` \u2014 this precedes the line-99 `output_dir = repo_root / adapter[\"data\"].get(\"output_dir\", ...)` used to write the packet, so a version-refused adapter never reaches the write."
    },
    "skills/public/retro/scripts/refresh_recent_lessons.py": {
      "verdict": "guarded-all-doors",
      "reason": "Lines 58-62: `refused = _version_verdict.refuse_unspeakable_version(load_adapter, repo_root, adapter_name=\"retro-adapter.yaml\"); if refused is not None: return refused` runs before `summary_path`/`output_dir` are read from `adapter[\"data\"]` (line 63-64) (already on the ALREADY-GUARDED list; re-confirmed by reading, including its own comment citing the exact shadow-digest harm this guard prevents). LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/retro/scripts/scaffold_retro_artifact.py": {
      "verdict": "guarded-all-doors",
      "reason": "payload_for() now calls `_adapter_version_verdict.unspeakable_version_message(load_adapter, repo_root, adapter_name=\"retro-adapter.yaml\")` and raises SystemExit before the write target is read from the payload. Guarded at the READ SITE, not `main()`, and here the importer claim is MEASURED: `plan_retro_run` calls this `payload_for` directly, so an entrypoint-only refusal would have left that caller on charness defaults. Measured flip on the real CLI at 0bcb6b227: a repo declaring `output_dir: docs/mine-retro` under `version: 9` got back `artifact_path: charness-artifacts/retro/2026-08-19-probe.md`, exit 0 -- an unhonored declaration RELOCATES this family's artifact rather than degrading its answer. Both polarity controls run: version 1 returns the declared path, no adapter returns the default. Probe record: charness-artifacts/probe/2026-08-19-scaffold-family-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    },
    "skills/public/setup/scripts/inspect_repo.py": {
      "verdict": "safe-checks-errors",
      "reason": "L31 delegates to `setup_inspect_lib` with setup's own `load_setup_adapter`; that resolver refuses an unsupported version before surface overrides, driven by tests/quality_gates/test_setup_inspect_adapters.py."
    },
    "skills/public/setup/scripts/setup_adapter.py": {
      "verdict": "safe-checks-errors",
      "reason": "L25 `load_yaml_file` then the shared version check; refuses an unsupported version before applying surface overrides."
    },
    "skills/support/web-fetch/scripts/route_public_fetch_routes.py": {
      "verdict": "guarded-all-doors",
      "reason": "resolve_github_mode() now refuses via `adapter_version_verdict.unspeakable_version_message` before `gather_provider` is read. THE FLIP RUNS IN THE PERMISSIVE DIRECTION on an external-fetch boundary, which is why this row is guarded rather than accepted: measured at e1c93ba17, a repo declaring `gather_provider.github.mode: none` -- 'this repo has no GitHub path' -- resolved to `direct-cli` under `version: 9` and routed `github.com` to `github-grant-or-cli` instead of `github-missing-capability`. The repo said it had no path; the router offered to take one. Controls at version 1 return `none`/`github-missing-capability` and `host-mediated`/`github-host-mediated`. THE `except Exception -> direct-cli` FALLBACK IS DELIBERATELY LEFT: a missing gather resolver or an unloadable module is not 'the repo declared something this reader ignored', and degrading there keeps web fetch working in a checkout that ships no gather skill. This module has no skill-runtime bootstrap, so it reaches `scripts/adapter_version_verdict.py` by the same ancestor walk it already uses for the gather resolver. Probe record: charness-artifacts/probe/2026-08-19-web-fetch-route-version-refusal.md. LEVEL MIGRATION (#675): `guarded` -> `guarded-all-doors`, measured from this file's own calls rather than asserted."
    }
  }
}
