# Workflow Trigger

The trigger is the most important line in the handoff.

## Rule

If the next session should continue with a named workflow or skill, name it
explicitly.

Examples:

- continue with `impl`
- continue with `retro`
- read the listed files, then continue with `impl`

The next operator should not have to infer the intended workflow from summary
text.
