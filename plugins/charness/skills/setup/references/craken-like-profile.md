# Craken-like profile

The Craken-like shape is now the default `flat-wiki` setup profile. The
canonical contract lives in [`default-surfaces.md`](./default-surfaces.md);
this file remains only as a compatibility pointer for older references.
