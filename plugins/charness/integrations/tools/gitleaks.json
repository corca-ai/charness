{
  "schema_version": "1",
  "tool_id": "gitleaks",
  "kind": "external_binary",
  "display_name": "gitleaks",
  "summary": "Fast Go-based secret scanner. Preferred over the secretlint fallback by `scripts/check-secrets.sh`; sub-second on the charness tree vs ~5s for the npm secretlint path.",
  "upstream_repo": "gitleaks/gitleaks",
  "homepage": "https://github.com/gitleaks/gitleaks",
  "platforms": [
    "linux",
    "macos"
  ],
  "status": "active",
  "lifecycle": {
    "install": {
      "mode": "script",
      "commands": [
        "if command -v go >/dev/null 2>&1; then go install github.com/zricethezav/gitleaks/v8@latest && mkdir -p \"$HOME/.local/bin\" && ln -sf \"$(go env GOPATH)/bin/gitleaks\" \"$HOME/.local/bin/gitleaks\"; elif command -v brew >/dev/null 2>&1; then brew install gitleaks; else echo 'Install gitleaks from https://github.com/gitleaks/gitleaks#installing'; exit 1; fi"
      ],
      "docs_url": "https://github.com/gitleaks/gitleaks#installing",
      "notes": [
        "Preferred install paths stay host-controlled: download a tagged release from GitHub or use another host-approved package path.",
        "When `gitleaks` is missing, `scripts/check-secrets.sh` falls back to the secretlint npm path with a stderr warning. The fallback is correct but ~5s slower per run."
      ]
    },
    "update": {
      "mode": "manual",
      "docs_url": "https://github.com/gitleaks/gitleaks/releases",
      "notes": [
        "Charness updates `gitleaks` only when its install provenance is a recognized package-manager path; otherwise update through the same release or package-manager path that installed it."
      ]
    }
  },
  "checks": {
    "detect": {
      "commands": [
        "gitleaks version"
      ],
      "success_criteria": [
        "exit_code:0"
      ],
      "failure_hint": "Install `gitleaks` to take the fast secret-scan branch in `scripts/check-secrets.sh`; the secretlint fallback still works but is much slower."
    },
    "healthcheck": {
      "commands": [
        "gitleaks help"
      ],
      "success_criteria": [
        "exit_code:0",
        "stdout_contains:gitleaks"
      ],
      "failure_hint": "The binary exists but its help surface is broken. Reinstall `gitleaks` from a clean upstream release."
    }
  },
  "access_modes": [
    "binary",
    "degraded"
  ],
  "version_expectation": {
    "policy": "advisory",
    "constraint": "Prefer a recent upstream-supported gitleaks release.",
    "detected_by": "stdout"
  },
  "package_managers": {
    "go": {
      "package_name": "github.com/zricethezav/gitleaks/v8",
      "notes": [
        "The upstream Go module keeps the historical `github.com/zricethezav/gitleaks/v8` module path."
      ]
    }
  },
  "supports_public_skills": [
    "quality"
  ],
  "intent_triggers": [
    "secret scan",
    "secret detection",
    "credential scan",
    "secret leak detection",
    "gitleaks",
    "git secret scan"
  ],
  "recommendation_role": "validation",
  "degradation": {
    "when_missing": [
      "`scripts/check-secrets.sh` falls back to the secretlint npm path. The fallback is correct but ~5s slower; it also requires `npm` and the local secretlint package to be present.",
      "If neither `gitleaks` nor secretlint is available, the secret-scan gate fails closed."
    ],
    "when_version_mismatch": [
      "Treat new rule classes or config syntax that older releases reject as a manual review point before suppressing findings."
    ]
  },
  "host_notes": [
    "The repo's `.gitleaks.toml` controls scan rules and exemptions. Keep that config aligned with `.secretlintignore` so the two paths report consistently."
  ]
}
