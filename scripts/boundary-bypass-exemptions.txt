# Boundary-bypass ratchet exemptions.
#
# Format:
# tests/path/test_file.py::scripts/target.py # why: short rationale with owner/revisit condition
#
# Add exemptions only for intentional boundary tests that should not count
# against the no-increase policy.

tests/test_quality_scaffold.py::scripts/export_plugin.py # why: end-to-end export portability proof — runs the real export_plugin.py as a subprocess to build a fresh export, then runs the exported scaffold+validator from a clean consumer repo, exercising the build->consume path a downstream repo uses. export_plugin is import-safe (could be called in-process), but the subprocess keeps this an honest E2E check; the debug-scaffold export test exercises the same path and is grandfathered into boundary-bypass-baseline.json rather than exempted. Owner: quality. Revisit if a lighter in-process export-and-consume proof replaces the E2E fidelity.
tests/test_skill_anchor_guard_hook.py::scripts/post_edit_skill_anchor_guard.py # why: the PostToolUse edit-time guard's contract IS the process boundary — the host pipes the hook payload to stdin and branches on exit 0 (silent) vs 2 (surface findings) — so one subprocess proof pins the stdin+exit-code contract; every logic path stays tested in-process in the same module. Owner: quality. Revisit if the host-hook machinery gains an in-process invocation seam.
tests/test_skill_output_schemas.py::scripts/validate_skill_output_schemas.py # why: one real CLI smoke intentionally proves script startup, runtime_bootstrap import-path behavior, __main__ dispatch, exit 0, and stdout JSON for the advisory output-schema survey; the classifier/validator behavior cases stay in-process via survey(). Owner: quality. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
tests/test_gather_google_workspace.py::skills/public/gather/scripts/advise_google_workspace_path.py # why: one real CLI smoke intentionally proves the operator-facing gather Google Workspace advice command parses --repo-root, starts through __main__, and emits JSON on stdout; direct and none-mode payload behavior stays in-process through payload_for(). Owner: gather. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
tests/test_gather_google_workspace.py::skills/public/gather/scripts/advise_slack_path.py # why: one real CLI smoke intentionally proves the operator-facing gather Slack advice command parses --repo-root, starts through __main__, and emits JSON on stdout; direct and host-mediated payload behavior stays in-process through payload_for(). Owner: gather. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
tests/test_public_skill_validation.py::scripts/validate_public_skill_validation.py # why: one real CLI smoke intentionally proves the public-skill validation command starts through __main__, reports failure guidance on stderr, and preserves the operator-facing suggestion hint; partition and adapter behavior stays in-process through validate_policy() and validate_adapter_requirement(). Owner: quality. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
tests/test_public_skill_validation.py::scripts/suggest_public_skill_validation.py # why: one real CLI smoke intentionally proves the public-skill validation suggestion command parses --repo-root/--json, starts through __main__, emits JSON on stdout, and preserves nonzero status for policy gaps; report construction and human formatting stay in-process through build_report() and _format_human(). Owner: quality. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
tests/test_public_skill_dogfood.py::scripts/validate_public_skill_dogfood.py # why: one real CLI smoke intentionally proves the public-skill dogfood registry validator starts through __main__, reads the checked-in registry, and emits the operator-facing success summary; registry error behavior stays in-process through validate_registry(). Owner: quality. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
tests/test_public_skill_dogfood.py::scripts/suggest_public_skill_dogfood.py # why: one real CLI smoke intentionally proves the public-skill dogfood suggestion command parses --repo-root/--skill-id/--json, starts through __main__, and emits the requested matrix JSON; scaffold construction stays in-process through build_matrix(). Owner: quality. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
tests/test_quality_artifact_report_all.py::scripts/validate_quality_artifact.py # why: these tests intentionally prove the validator's CLI fail-fast vs --report-all stderr contract and nonzero exit behavior; the artifact-shape rules remain covered by the existing validator tests. Owner: quality. Revisit if validate_quality_artifact grows an in-process report-all API that exposes the exact operator-facing aggregate message.
tests/test_cautilus_diagnostic_artifact.py::scripts/validate_cautilus_diagnostics.py # why: these tests intentionally prove the Cautilus diagnostic validator's CLI path grouping, --all scan mode, stderr failures, and machine-evidence boundary; lower-level parsing remains in the validator implementation. Owner: quality. Revisit if validate_cautilus_diagnostics exposes a stable in-process report API with identical aggregate behavior.
tests/quality_gates/test_skill_contracts_validation.py::scripts/check_skill_contracts.py # why: one real CLI smoke intentionally proves check_skill_contracts startup, representative-contract loading, failure exit, and operator-facing stderr for a synthetic missing contract; direct contract helper behavior stays in-process in the same file. Owner: quality. Revisit if a shared script-entrypoint smoke harness proves this boundary elsewhere.
