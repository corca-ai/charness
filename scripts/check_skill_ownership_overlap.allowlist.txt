# Format: <skill-id>:<kind>:<owner-id>:<reason>
# kind = artifact | adapter
# Each entry declares an intentional cross-namespace mention so the boundary
# is explicit. Silent overlap creates drift the next operator hits.
# A waiver that is no longer needed is surfaced as a stale-allowlist advisory,
# never silently dropped -- the same posture the scenario-conditional-reads
# allowlist states, so entries here get retired instead of only accumulating.
# The advisory is a prompt to re-check the reason, not proof the mention is gone:
# the scan reads only top-level .py/.md under each skill.

handoff:artifact:release:read-only spill target -- handoff cites release/latest.md as where release detail belongs
handoff:artifact:quality:read-only spill target -- handoff cites quality/latest.md as where quality numbers belong
handoff:artifact:retro:read-only spill target -- handoff cites retro/recent-lessons.md as where repeat-trap memory belongs
handoff:artifact:debug:read-only spill target -- handoff cites debug/ artifacts as where debug detail belongs
setup:artifact:retro:seed-only -- setup seeds initial recent-lessons.md via seed_retro_memory.py; ongoing writes own to retro
setup:adapter:retro:seed-only -- setup seeds .agents/retro-adapter.yaml via seed_retro_memory.py; runtime owns to retro
setup:adapter:worktree:seed-only -- setup seeds .agents/worktree-adapter.yaml; runtime owns to worktree integration
setup:adapter:t-events:seed-only -- setup seeds .agents/t-events-adapter.yaml via seed_t_events_adapter.py; runtime owns to t-events integration
setup:adapter:usage-episodes:seed-only -- setup seeds .agents/usage-episodes-adapter.yaml via seed_usage_episodes_adapter.py; runtime owns to usage-episodes integration
issue:artifact:gather:read-only cite -- issue resolve flow references gather artifacts as evidence sources
release:artifact:critique:read-only proof -- release helper validates a tracked critique artifact path before rendering review proof in release/latest.md
release:artifact:release-review:read-write claims boundary -- the release workflow owns the narrowly scoped, committed claims-review evidence that gates a prepared local release record before any publication side effect
critique:artifact:retro:read-only cite -- code-critique reference cross-links charness-artifacts/retro/recent-lessons.md as the defect-class memory home
critique:artifact:spec:read-only scan -- autonomous trigger scans spec artifacts only to identify the current pending critique target
critique:artifact:skill-t-mechanism:read-only cite -- angle-selection lineup cites skill-t-mechanism/inventory as the Tier C anchor_invoked evidence sink
critique:adapter:retro:boundary cite -- adapter-contract.md names .agents/retro-adapter.yaml as the seam where a future retro-prepare slot would live; ownership stays retro-side
quality:artifact:cautilus:read-only cite -- cautilus-on-demand.md cites charness-artifacts/cautilus/latest.md as the behavior-proof artifact home so the planner-consult contract can name it
achieve:artifact:goals:owns -- achieve owns charness-artifacts/goals/<date-slug>.md as the goal lifecycle artifact namespace; no other skill writes there
achieve:artifact:retro:read-only proof -- After-phase closeout binds a retro artifact as the prescribed-skill evidence; lifecycle.md cites retro/ as the evidence namespace, ownership stays retro
achieve:artifact:probe:read-only proof -- After-phase closeout binds a host-log probe artifact as portability evidence; lifecycle.md cites probe/ as the evidence namespace, ownership stays probe
achieve:artifact:critique:read-only proof -- After-phase fresh-eye disposition review (rung 2, #253) is bound by the Disposition review: line; lifecycle.md cites critique/ as the review-artifact namespace, ownership stays critique
achieve:artifact:release:read-only proof -- the release coordination floor detects release work via the charness-artifacts/release/ token and binds an optional Release: reference to release proof; lifecycle.md cites release/ as the release-proof namespace, ownership stays release
handoff:artifact:goals:auto-draft seed -- handoff chunked-routing drafts a goal artifact into charness-artifacts/goals/<date>-<slug>.md for /achieve to consume; achieve still owns the lifecycle after activation
issue:artifact:critique:read-only proof -- issue closeout-discipline requires a critique artifact path or blocked-host signal as resolution-critique evidence; ownership stays critique
release:artifact:probe:read-write convention -- publication-boundary.md directs independent-observer release evidence into a durable JSON probe artifact; ownership stays probe
prove:adapter:impl:read-only consumption -- prove reads verification preferences and truth_surfaces from the impl adapter through impl's resolver; impl keeps ownership of the adapter file
setup:adapter:quality:pointer, not ownership -- default-surfaces.md names the regenerable-facts stance where a human authoring README/AGENTS reads it, and must say WHICH adapter carries the field so a consumer does not write the key into setup-adapter.yaml where it would be silently inert; the field contract, defaults, and wiring stay quality's
release:artifact:goals:classification only -- the claims-review scope splitter names charness-artifacts/goals/ so it can classify a goal artifact appearing in the release delta as advisory (session narrative) rather than tag-gating. It reads no goal, writes no goal, and knows nothing of the goal lifecycle; achieve keeps ownership
release:artifact:retro:classification only -- same splitter, same reason: a retro in the release delta is session narrative and must not gate a tag. Read-free, write-free classification by path prefix; retro keeps ownership
release:artifact:issues:classification only -- the claims-review scope splitter names charness-artifacts/issues/ to classify an issue artifact in the release delta; markdown there is session narrative, JSON there is machine state and stays blocking. No read, no write; issue keeps ownership
release:artifact:quality:classification only -- the claims-review scope splitter names charness-artifacts/quality/ to classify artifacts appearing in the release delta. Its JSON is machine state and its rolling pointer latest.md is a CURRENT_POINTERS gate input; BOTH classify blocking, so the only thing this grants is the ability to mark a DATED quality narrative advisory. An earlier version of this entry claimed it 'grants the release skill nothing quality does not already gate' while reasoning only about the JSON, which was false while latest.md was advisory. No read, no write; quality keeps ownership
