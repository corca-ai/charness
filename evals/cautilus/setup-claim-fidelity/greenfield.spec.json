{
  "_comment": "SLICE 9c RESOLUTION (2026-07-09, #410) — CAPTURE-PROVEN KEEP; the census MOVE verdicts are REFUTED for this arm by per-condition live evidence. First-ever greenfield captures ran on a fresh non-charness sandbox via capture-skill-run.sh --run-cwd (git init only; the harness gained --run-cwd for exactly this). Capture 1 (old prompt, bundle setup-greenfield-slice9-2026-07-09): 'from minimal ideation' PROVIDED no ideation, so a faithful headless run read greenfield-flow.md for the minimum-questions set, asked, and STOPPED — honest behavior that can never reach scaffolding, i.e. the old prompt was an unfixturable dead end (and evidence that greenfield-flow.md's question set is genuine on-demand DEPTH the census-DUP verdict under-counted). Capture 2 (THIS prompt, bundle setup-greenfield-slice9b-2026-07-09): outcome=passed vs this RCF — the run GENUINELY opened greenfield-flow.md + agent-docs-policy.md + default-surfaces.md (4/9 coverage), scaffolded README/AGENTS/roadmap/operator-acceptance + the CLAUDE.md symlink, committed a root commit, and emitted the observed closeout tokens now pinned as RSF (Repo mode: / Normalization non-claims: — OBSERVED, not assumed; per-surface 'scaffolded' accounting present). So the doc-open RCF floor is load-bearing/DEPTH for the greenfield condition (the normalization run's 0/9 said nothing about THIS branch — the slice7 census flattening), and it STAYS; the sibling outcome-assertions.json remains normalization-scoped (do NOT grade a greenfield capture against it without a scenario guard). thresholds.max_duration_ms=800000 from the 378840ms baseline. --- PRIOR: SLICE-7 CENSUS RECONCILIATION (2026-07-01) — SUPERSEDES THE CAPTURE-ONLY FRAMING BELOW WHERE IT CONTRADICTS census.json. Reconciled verdicts: greenfield-flow.md=MOVE (census DUP), agent-docs-policy.md=MOVE, default-surfaces.md=MOVE (both census INLINE). Same class as #413/#411: setup is SKILL/surface-driven and the normalization run opened ZERO docs, CONFIRMING census INLINE/DUP — the honest replacement is an artifact/substance judge (outcome-assertions.json), not a doc-open. NOTE: greenfield.spec.json itself is UNCAPTURED (needs a fresh-sandbox capture, deferred by #410); the verdicts here follow the census + the sibling normalization capture. Full adversarial (analyze→verify) resolution + capture-gated queue: charness-artifacts/reference-compaction/slice7-census-reconciliation.md. classTag/RCF are intentionally NOT flipped here: claim_fidelity_lib forbids DUP/INLINE-tagging a live RCF floor, so the tag/RCF flip rides with the capture-gated MOVE (no substance judge + no retained stream ⇒ each MOVE needs a fresh ask-before-run capture). --- ORIGINAL SPEC COMMENT: Claim-fidelity spec for the setup skill, GREENFIELD scenario: does a real `/charness:setup` run on a brand-new repo honor its own routing? setup branches on detected repo mode (SKILL.md steps 1, 78-83) and greenfield-flow vs normalization-flow are mutually exclusive, so this is the greenfield-branch fixture. CAPTURE CONTEXT (non-claim): this scenario is NOT capturable inside the mature charness repo — running `/charness:setup` here detects PARTIAL/NORMALIZE and never reaches greenfield-flow.md. A live capture requires running the slash command in a fresh/empty repo sandbox; until then this fixture is a static classification, not a captured run. requiredCommandFragments rationale: a greenfield run must consult greenfield-flow.md (minimum questions + expected surfaces, step 1 greenfield path), agent-docs-policy.md (host instruction surface created from scratch, step 2), and default-surfaces.md (the surface set scaffolded in step 4). thresholds omitted until a first baseline capture sets a runtime budget.",
  "skillId": "setup",
  "skillDisplayName": "setup",
  "scenarioId": "greenfield",
  "evaluationId": "execution-setup-greenfield-claim-fidelity",
  "targetKind": "public_skill",
  "targetId": "setup",
  "prompt": "/charness:setup  bootstrap the initial operating surface for this brand-new empty repository from this minimal ideation: the repo will hold 'logtrim', a small CLI utility that truncates and rotates plain-text log files for solo developers; the primary operator is the repo owner working alone; the next concrete work items are (1) implement the trim command and (2) add a retention-policy flag; a successor taking over tomorrow should read the README first and then the roadmap",
  "requiredCommandFragments": [
    "greenfield-flow.md",
    "agent-docs-policy.md",
    "default-surfaces.md"
  ],
  "requiredSummaryFragments": [
    "Repo mode:",
    "Normalization non-claims:"
  ],
  "declaredReferences": [
    "agent-docs-policy.md",
    "bootstrap-seams.md",
    "default-surfaces.md",
    "github-actions-defaults.md",
    "greenfield-flow.md",
    "normalization-flow.md",
    "operator-acceptance-synthesis.md",
    "probe-surface.md",
    "retro-memory-seam.md"
  ],
  "referenceEngagement": {
    "agent-docs-policy.md": {
      "engagement": "engage-always",
      "rationale": "This reference owns the explicit repo-level host policy (AGENTS.md and CLAUDE.md normalization) that step 2 requires for stabilizing the host-facing instruction surface first, including the both-missing create-and-symlink case a greenfield repo hits.",
      "claimRole": "host-policy"
    },
    "bootstrap-seams.md": {
      "engagement": "on-demand",
      "rationale": "This reference covers optional adapter, policy, or runtime seams that setup may seed only when the repo explicitly uses those Charness features.",
      "trigger": "repo uses optional Charness seams beyond default surfaces (worktree, T-events, usage episodes, bounded review delegation, skill routing, artifact policy, announcement workflows)."
    },
    "default-surfaces.md": {
      "engagement": "engage-always",
      "rationale": "This reference defines all default operating surfaces (README, AGENTS, Roadmap, Operator Acceptance) that step 4 uses to scaffold the basic durable surfaces for a greenfield repo.",
      "claimRole": "surface-set"
    },
    "github-actions-defaults.md": {
      "engagement": "on-demand",
      "rationale": "This reference provides specific GitHub Actions version guidance only when the representative run detects a repo using GitHub-hosted workflows; repos without Actions do not consult it.",
      "trigger": "repo scaffolds or normalizes GitHub-hosted workflows and needs current documented majors for first-party JavaScript actions."
    },
    "greenfield-flow.md": {
      "engagement": "engage-always",
      "rationale": "In a greenfield representative run, this reference defines the minimum questions and expected surfaces that shape repo mode detection and initial surface scaffolding in step 1.",
      "claimRole": "greenfield-path"
    },
    "normalization-flow.md": {
      "engagement": "on-demand",
      "rationale": "In the greenfield scenario no prior durable surface exists to realign, so the normalization path is not taken; it is consulted only if mode detection reclassifies the repo as partially initialized.",
      "trigger": "repo mode detection finds some existing surface (PARTIAL/NORMALIZE) rather than a greenfield repo."
    },
    "operator-acceptance-synthesis.md": {
      "engagement": "on-demand",
      "rationale": "This reference describes a synthesis approach for operator-acceptance.md when the repo already has functional checks to derive from, not for the default template scaffolding a greenfield repo needs.",
      "trigger": "mature repo already has real functional checks and wants to derive operator-acceptance.md from existing command and takeover surface instead of inventing a parallel ritual."
    },
    "probe-surface.md": {
      "engagement": "on-demand",
      "rationale": "This narrow reference answers how to bootstrap a probe surface when the repo exposes an installable surface, which does not appear in every representative run.",
      "trigger": "repo ships an installable CLI, plugin, package, or agent-facing integration surface that needs a small explicit probe surface for install and health semantics."
    },
    "retro-memory-seam.md": {
      "engagement": "on-demand",
      "rationale": "This reference provides opt-in seeding of retro adapter and lessons file only when the repo explicitly requests durable retro memory, not a default surface.",
      "trigger": "repo wants durable retrospective pickup between sessions instead of chat-only retros."
    }
  },
  "thresholds": {
    "max_duration_ms": 800000
  }
}
