{
  "_comment": "Claim-fidelity spec for the announcement skill: does a real `/charness:announcement` run honor its own central claims, above all that it routes to its references at the point of need? The prompt pins a draft-only chat update so the run reaches the drafting/public-body phase without branching into delivery (which would otherwise stall on the step-1 draft-vs-deliver question and pull in delivery-seams.md). declaredReferences is the full reference set (coverage report). referenceEngagement classifies each reference on the engage-always/on-demand/gate-sufficient axis from the locked methodology contract. thresholds are intentionally omitted until a first baseline capture sets a per-skill runtime budget. requiredCommandFragments rationale: draft-shape.md is the one hard matcher — step 4 says 'steer wording per references/draft-shape.md' and step 5 runs the public-body pass through it, an explicit point-of-need route a drafting run cannot skip. adapter-contract.md stays engage-always in the classification but is DEMOTED from RCF: the runtime resolves the adapter via resolve_adapter.py (reading the resolved YAML) and applies the step-4 policy enumeration inline, so a run with a present adapter need not open the human-facing contract explainer .md. This is the recurring 'script-resolved contract doc' shape (cf. achieve coordination.md): engage-always classification, not an unconditional always-open. Confirm/tighten at first live capture.",
  "skillId": "announcement",
  "skillDisplayName": "announcement",
  "evaluationId": "execution-announcement-claim-fidelity",
  "targetKind": "public_skill",
  "targetId": "announcement",
  "prompt": "/charness:announcement  최근 변경사항을 팀 채팅용 업데이트로 draft만 해줘 (배포 X)",
  "requiredCommandFragments": [
    "draft-shape.md"
  ],
  "requiredSummaryFragments": [],
  "declaredReferences": [
    "adapter-contract.md",
    "audience-policy.md",
    "delivery-seams.md",
    "draft-shape.md",
    "large-window-fanout.md"
  ],
  "referenceEngagement": {
    "adapter-contract.md": {
      "engagement": "engage-always",
      "rationale": "The Workflow step 2 explicitly consults the adapter to resolve drafting context; step 4 applies adapter policy (sections, audience_tags, omission_lenses, in_progress_sources, outputs); adapter shape governs the entire representative run from first bootstrap call through final recording.",
      "claimRole": "bootstrap & policy foundation",
      "classTag": "INLINE"
    },
    "audience-policy.md": {
      "engagement": "on-demand",
      "rationale": "Workflow step 4 applies 'audience tags when configured' and step 3 recovers 'audience value'; this doc answers how to handle unset vs. explicit-empty vs. configured tags, which is a narrower condition than the representative run's ordinary audience reframing in draft-shape.md.",
      "trigger": "adapter declares audience_tags in audience-policy decision",
      "classTag": "DEPTH"
    },
    "delivery-seams.md": {
      "engagement": "on-demand",
      "rationale": "Workflow step 7 consults this only when delivery is requested ('if the user wants delivery, resolve the backend seam only then'); covers format conversion, message_size_limit splitting, and chaining outputs via delivery_handle — not needed for draft-only runs.",
      "trigger": "user explicitly requests delivery (not draft-only)",
      "classTag": "DEPTH"
    },
    "draft-shape.md": {
      "engagement": "engage-always",
      "rationale": "Workflow step 4 says 'steer wording per references/draft-shape.md'; step 5 runs 'a public-body pass before writing' using this doc to distinguish chat_update vs. release_notes and enforce audience-tagged reframing; step 6 uses Dual-Output Drafts to label outputs by delivery_role.",
      "claimRole": "audience-tone & public-body lens",
      "classTag": "DEPTH"
    },
    "large-window-fanout.md": {
      "engagement": "on-demand",
      "rationale": "Workflow step 2 says 'consider fanning out triage per references/large-window-fanout.md' only when 'the commit window is large or spans more than one source' — a conditional optimization for specific commit-collection shapes, not the ordinary representative run.",
      "trigger": "commit window is large or spans multiple sources/outputs with non-overlapping audiences",
      "classTag": "DEPTH"
    }
  }
}
