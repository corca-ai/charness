# Impl conditional references

- Artifact: public skill `skills/public/impl/SKILL.md`. Generated host copies remain placements.
- Audience/trigger: agents implementing an agreed consumer change; trigger unchanged.
- Current center: Start/Change/Verify in the body, plus a bulk References list.
- Next center: ordinary implementation completes from `SKILL.md`. The four deepening files are named at their body triggers, not in the default References list.
- Capability failure: Goal 798 candidate traces re-read four impl references whose triggers were not live. The 2026-09-08 trial kept those four in the list with conditions and still saw a sweep; this slice removes them from the list.
- Contract: `preserve` routing and focused verification; `improve` unconditional reference sweeps.
- Proof: `test_impl_reference_triggers_are_exclusive` forbids those four from the References list and requires `(only ...)` on every remaining listed reference.
