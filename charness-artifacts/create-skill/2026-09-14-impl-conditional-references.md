# Impl conditional references

- Artifact: public skill `skills/public/impl/SKILL.md`. Generated host copies remain placements.
- Audience/trigger: agents implementing an agreed consumer change; trigger unchanged.
- Current center: Start/Change/Verify in the body, plus a bulk References list.
- Next center: ordinary implementation completes from `SKILL.md`. Body triggers name when to open each deepening file; Finish says not to sweep the list.
- Capability failure: Goal 798 candidate traces re-read four impl references whose triggers were not live. The 2026-09-08 trial kept those four in the list with conditions and still saw a sweep. This slice cannot unlist them: `tools/validate_skills.py` refuses unlisted files under `references/`.
- Contract: `preserve` routing and focused verification; `improve` is limited to exclusive `(only …)` triggers plus a do-not-sweep sentence. A new consumer trace is not claimed.
- Proof: `test_impl_reference_triggers_are_exclusive` requires those four to remain listed with `(only …)`, matching the validate-skills inventory rule.
