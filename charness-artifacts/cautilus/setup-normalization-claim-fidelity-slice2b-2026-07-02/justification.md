# Operator-authorized ask-before-run capture — setup normalization (Slice 2b)

Authorization: operator selected "Authorize the capture" (AskUserQuestion, 2026-07-02)
in the achieve goal run 2026-07-02-issue-410-411-412-413-...slice-7 (Slice 2).

Purpose: OBSERVE which canonical `## Closeout Vocabulary` token a faithful
/charness:setup NORMALIZE run stably emits (Repo mode / per-surface status /
Normalization non-claims:), added at commit 74c639cf, so the claim-fidelity floor
can move off the refuted RCF doc-opens to an emitted-token RSF (#413). Token
OBSERVED here, never assumed (impl Slice-5 lesson).

Ref under test: 74c639cf (HEAD). Prompt mirrors normalization.spec.json.
