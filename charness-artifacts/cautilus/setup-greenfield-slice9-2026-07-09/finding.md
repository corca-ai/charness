# setup greenfield capture 1 — honest ask-and-stop under the ideation-less prompt (#410 Slice 9c)

## What ran

2026-07-09, ask-before-run fresh-sandbox capture (`--run-cwd`, git-init-only
sandbox) with the OLD greenfield spec prompt; operator-authorized per
`justification.md`.

## Verdict

Evidence bundle for the FIRST greenfield fresh-sandbox capture (old spec prompt,
no inline ideation): the faithful run opened `greenfield-flow.md` for the
minimum-questions set, asked four ideation questions, and stopped without
scaffolding — grade vs RCF `failed` because an honest run can never reach the
scaffolding phase under that prompt. This is the evidence that (a) the old
prompt was an unfixturable dead end, corrected to carry inline ideation, and
(b) `greenfield-flow.md`'s question set is genuine on-demand DEPTH.

Full analysis, capture-2 PASS, and the KEEP verdict:
`../setup-greenfield-slice9b-2026-07-09/finding.md`.

## Non-Claims

- This capture proves the old prompt dead-ends honestly; it does NOT grade
  scaffolding behavior (capture 2 owns that) and pins no floor by itself.
