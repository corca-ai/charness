Closes #695

JTBD: make every new critique artifact and prepare packet state the reviewer execution mode required by the typed closeout consumer, so an incomplete typed-subagent record cannot pass through a prose-only fallback.

Root Cause: the typed closeout observer gained `Execution mode` while the scaffold and packet producers retained an older duplicated tier shape; the first clean proof also found that the standalone observer loader did not prepare the repository-root import path for the canonical shape module.

Debug Artifact: charness-artifacts/debug/2026-08-27-issue-695-critique-shape.md

Siblings: decision: keep `scripts/critique_reviewer_evidence.py` as the single shape owner, preserve legacy artifacts, and repair the standalone loader; proof: focused producer/consumer tests, standalone preflight regression tests, and export parity pass with no additional sibling fix bundled.

Resolution brief: require `file-backed-worker` or `typed-subagent` in new producer output, derive the typed consumer field list from the shared owner, and retain fail-closed refusal for missing or malformed typed evidence.

Implementation: commits `01bc266e0`, `974c654b9`, and `5941475b4` update the canonical shape, packet/scaffold producers, typed issue consumer, standalone import path, checked-in plugin mirrors, references, and focused tests. The implementation contract is `charness-artifacts/impl/2026-08-27-issue-695-critique-shape.md`.

Prevention: producers import the canonical reviewer-tier execution vocabulary; tests cover valid emission, malformed runner fallback, unknown-mode rejection, typed closeout refusal, standalone loader failure, and source/plugin/export parity. Historical critique artifacts are not mass-edited.

Boundary #695: owned-correctly — Charness owns the local shape producers, validator, typed closeout observer, standalone loader, and plugin export parity; installed-host behavior, consumer-repository adoption, and provider behavior are not claimed.

Verification: base `7bfab87ee7348ce9662dcc01016791cfa0dc7d4a`, code target `5941475b4c724257b4aa5b6c9115fd9e7485931e`, proof branch `proof/issue-695-critique-shape-coverage-20260827`, proof path `/tmp/charness-695-proof3-20260827`; proof worktree was a clean named branch before and after, with isolation doctor pass.

Verification results: focused standing tests `137 passed`; issue-mandated `tests/quality_gates/test_describe_goal_closeout_shape.py` `19 passed`; mapped changed-line producer `status: clean`, four files analyzed, all changed lines covered, `blocking_targets: {}`, exit 0; export parity, source/plugin parity, ruff, compile, length, debug validation, and diff check passed.

Behavior #695: local-only-by-contract — the distinct local behavior channel is the producer/consumer test and changed-line readback; no installed-host, hosted, provider, or remote-CI behavior is claimed.

Probe record #695: local-only-by-contract

Critique #695: blocked explicit operator direction omits forced fresh-eye review and this host exposes no Agent/subagent capability

AI-provenance: authored by an agent session.

Manual close reason: operator-directed-manual-close.

Manual fallback reason: operator-directed-manual-close.

Explicit non-claims: no fresh-eye approval, universal changed-line requirement, handoff update, micro-slice record, installed-host behavior, hosted/provider roundtrip, consumer-repository adoption, remote CI, push, release, or tag is claimed.
