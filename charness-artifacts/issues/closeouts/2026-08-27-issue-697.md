Closes #697

JTBD: make mutation sampling and changed-line coverage use distinct producer
paths and freshness identities, so a sampler report cannot satisfy the
changed-line consumer's authoritative-coverage check.

Root Cause: the sampler and changed-line producer shared
`reports/mutation/test-coverage.json` and a content-only `.fingerprint`, so the
consumer had no producer identity and accepted a sampler-shaped report as
fresh.

Debug Artifact: `charness-artifacts/debug/2026-08-27-issue-697-distinct-mutation-coverage.md`

Siblings: decision: bundle the shared sampler path, producer-qualified
changed-line marker, runtime retention paths, direct-script import seam, and
source/plugin export parity; diagnostic-only: hosted enforcement, consumer
repository adoption, and release behavior remain outside this local Charness
contract. Proof levels are separate: local regression and changed-line
consumer proof passed; no hosted/provider roundtrip is claimed.

Boundary #697: owned-correctly — Charness owns the local sampler and
changed-line producers, marker reader/writer, retention paths, and checked-in
plugin export; hosted enforcement and downstream consumer repositories do not
belong to this slice.

Implementation: commit
`711203fed259af008e4626c49d00ad2f7027ac13` separates the sampler report at
`reports/mutation/sample-coverage.json`, makes the changed-line producer own
`test-coverage.json.changed-line.fingerprint`, rejects legacy/foreign markers,
invalidates a stale changed-line marker on an explicit sampler override, and
updates the checked-in mirror and focused tests.

Prevention: producer-qualified marker schema and direct malformed-marker tests
make attribution fail closed; sampler/runtime defaults and retention tests keep
the two report paths separate; source/plugin parity remains checked.

Critique #697: blocked explicit operator direction omitted forced fresh-eye
review and this host exposes no Agent/subagent tool; no fresh-eye approval is
claimed.

Behavior #697: local-only-by-contract — the distinct behavior was confirmed
through the focused producer/consumer regression and the independent
changed-line proof output (`11/11` changed pool files analyzed,
`unmapped=[]`, `blocking=[]`, consumer exit `0`), not by the GitHub CLOSED
state or this carrier body.

Verification: clean named proof worktree
`/tmp/charness-697-proof3-20260827`, base
`6b42f7d9b8ec106bcfe575cc149198624753c477`, target
`711203fed259af008e4626c49d00ad2f7027ac13`; changed-line proof passed with
`status: clean`, `11` analyzed, `0` unmapped, `0` blocking, and exit `0`.
Targeted pre-commit, Ruff, compile, artifact/doc checks, source/plugin parity,
and diff check passed. Python bytecode was routed outside the proof worktree;
pytest's ignored `.pytest_cache` was moved to a recoverable `/tmp` cache after
the run.

AI-provenance: authored and published by this agent session from the verified
local implementation and proof receipts.

Manual close reason: operator-directed-manual-close; no push, release, tag,
installed-host mutation, hosted evaluator, or consumer-repository migration is
claimed.
