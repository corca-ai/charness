# Release Critique — charness v0.10.0 (short, scoped hygiene)

Scope: routine release hygiene for 0.9.0 -> 0.10.0. The only shipped change
beyond v0.9.0 is the #226 portable fresh-eye reviewer-tier policy, which already
received two bounded fresh-eye subagent reviews at its closeout this session
(no blockers). This critique covers the release action itself.

- **Version drift:** none — packaging, claude/codex plugin, and marketplace
  surfaces are all at 0.9.0 and bump together via the helper.
- **Bump level:** minor is honest — `reviewer_tiers` is a new additive,
  optional operator-facing adapter field and the reviewer-tier policy is new
  adoptable behavior; no breaking change, fast path unchanged.
- **Generated surfaces:** the plugin mirror was synced and `validate_packaging`
  passed this session; the helper re-syncs and the `--release` gate revalidates.
- **Operator risk:** low — the new field is optional; repos that omit it see no
  behavior change; hosts without subagent model overrides ignore the tier.
- **Publish boundary:** tag + GitHub release via the helper; treat workflow
  publication and public-surface verification as separate states in closeout.

Critique: short. No blockers; proceed to publish.
