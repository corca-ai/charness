# v0.55.0 real-host proof

- Release: v0.55.0
- Date: 2026-06-25
- Proof mode: maintainer-machine real-host checks before publish, with
  post-publish `charness update` delegated to the release helper's
  `post_publish_install_refresh` step.

## Trigger

`python3 skills/public/release/scripts/check_real_host_proof.py --repo-root . --paths $(git diff --name-only origin/main..HEAD)`
reported:

- `required: true`
- `surface_hits: integrations-and-control-plane`
- configured checklist included maintainer `charness update`, `nose` doctor,
  `nose` installer dry-run/install, support sync, and clone inventory.

## Nose Initial State

`charness tool doctor nose --json --no-write-locks` was run on this maintainer
machine before install. `nose` was already present, so the missing-binary branch
could not be re-created on this host without destructively changing the existing
tool install.

Observed state:

- doctor status: `ok`
- doctor disposition: `ready`
- observed version: `0.15.0`
- resolved binary: `/home/hwidong/.cargo/bin/nose`
- latest upstream release: `v0.15.0`

## Nose Install Route

`charness tool install nose --dry-run --json` reported the supported upstream
release installer:

```bash
curl --proto '=https' --tlsv1.2 -LsSf https://github.com/corca-ai/nose/releases/latest/download/nose-cli-installer.sh | sh
```

The dry-run release metadata pointed at `https://github.com/corca-ai/nose/releases/tag/v0.15.0`
with `nose-cli-installer.sh` present in the release assets.

## Nose Install And Detected State

`charness tool install nose --json` was executed through the manifest-supported
installer path and completed successfully.

- install status: `installed`
- installer stderr: `downloading nose-cli 0.15.0 x86_64-unknown-linux-gnu`
- installer stdout included: `everything's installed!`
- detected command: `nose --version`
- detected version: `nose 0.15.0`

After installation, `charness tool doctor nose --json --no-write-locks` reported:

- doctor status: `ok`
- doctor disposition: `ready`
- observed version: `0.15.0`
- latest upstream release: `v0.15.0`

## Support Sync Result

`charness tool sync-support nose --json` reported:

- support status: `skipped`
- reason: `integration has no support_skill_source`

This is the expected result for `nose`: it is an integration-only validation
binary consumed by `quality`, not a materialized support skill.

## Quality Inventory Result

`python3 skills/public/quality/scripts/inventory_nose_clones.py --repo-root . --json`
reported:

- status: `clean`
- tool version: `0.15.0`
- total duplicate lines: `0`
- advisory findings remain refactoring candidates, not standing quality failures.

## Maintainer Install Refresh

The release adapter declares:

```text
post_publish_install_refresh: charness update
```

That command is executed by the release helper after the GitHub release is
created and verified. Its status and output tail are recorded in
`charness-artifacts/release/latest.md` by the post-publish artifact commit.

## Result

The pre-publish real-host checks that can be executed on this maintainer machine
passed. The only checklist item intentionally left to the publish helper is the
post-publish `charness update`, because it requires the new release to exist.

