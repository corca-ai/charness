# v0.13.0 real-host proof — maintainer attestation

- **Release:** v0.13.0 (`https://github.com/corca-ai/charness/releases/tag/v0.13.0`)
- **Date:** 2026-05-30
- **Provenance:** completed by the maintainer (bae.hwidong@corca.ai / `spilist`)
  on their own clean machine and **attested to the agent in-session**. The raw
  command outputs were **not captured in this session** — this is a maintainer
  attestation, not an agent-captured machine record. Attach the `--json` outputs
  here to upgrade it to captured evidence.

## Checklist attested complete

The `real_host_required` checklist the publish helper carried for this release
(from `.agents/release-adapter.yaml` `real_host_checklist`):

1. On a second machine / clean temp-home, refreshed `charness` through the
   published operator path before claiming the release surface ready.
2. `charness tool doctor cautilus --json` before install — missing-binary state
   surfaces an install-document URL, not a guessed package-manager command.
3. Followed the official Cautilus install script
   (`https://github.com/corca-ai/cautilus/blob/main/install.sh`), then verified
   `cautilus --version` and `cautilus version --verbose`.
4. Re-ran `charness tool doctor cautilus --json` — binary detected on PATH.
5. `charness tool sync-support cautilus --json` — generated support surface
   exists and the doctor payload reports support as materialized.

## Effect

This closes the one open item from the v0.13.0 publish (`real_host_required:
true`) and clears the long-carried "v0.12.0 real-host proof pending" handoff
note: the published operator path is now maintainer-verified on a clean machine
for the current release line. `latest.md` (generated) still shows the
"required" template text for this slice; this attestation is the durable record
that satisfies it.
