# Charness 3.2.0 Public Readback

Date: 2026-08-04

This evidence is recorded after the release boundaries. It does not treat a
local terminal green as proof of remote CI, public visibility, or installed
refresh.

## Release Commit and CI

- Release-content commit: `2a652b18de280fa50d0f1e46f9caebe41c70755a`.
- GitHub branch API readback: `refs/heads/main` returned that exact SHA.
- GitHub commit API readback: `/commits/2a652b18de280fa50d0f1e46f9caebe41c70755a`
  returned that exact SHA.
- Workflow: `Quality Core`, run `30874005717`, head SHA exact, completed
  `success`.
- Jobs: `Core deterministic gates` success; `Changed-line mutation coverage
  (push/PR mirror)` success, completed `2026-08-04T03:22:52Z`.

## Tag and Public Release

- Tag `v3.2.0` resolves through `git ls-remote` to the exact release-content
  SHA.
- Release helper created and verified the public release URL:
  `https://github.com/corca-ai/charness/releases/tag/v3.2.0`.
- Independent unauthenticated HTTPS page readback returned HTTP 200 and
  contained both `Charness 3.2.0` and `v3.2.0`.
- Independent unauthenticated REST API readback returned HTTP 403 because of
  rate limiting; it is recorded as unavailable, not as a success claim.

## Installed Readback

- `charness update`: return 0; checkout, Codex source/cache, and installed
  surfaces refreshed to `3.2.0`; `codex_source_cache_drift: false`.
- `charness version`: return 0; `version: 3.2.0`.
- `charness doctor`: return 0; checkout/source/cache and target repo read back
  at `3.2.0` with no cache drift.
- Baton reconcile: `n/a`; `docs/handoff.md` has no release-version claim in
  its routing sections.

## Issue Boundary

Issues #496 and #503 remain open. No issue-close operation was run.
