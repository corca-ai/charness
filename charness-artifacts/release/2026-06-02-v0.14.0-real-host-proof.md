# v0.14.0 real-host proof

- Release: v0.14.0 (`https://github.com/corca-ai/charness/releases/tag/v0.14.0`)
- Date: 2026-06-02
- Proof mode: agent-captured clean temp-home smoke on the maintainer machine.
- Temp home: `/tmp/charness-v014-proof-home-JnDy6H`

## Captured Evidence

The proof used a clean `--home-root` and cloned the published repository through
the managed install path.

- `charness init --home-root <temp> --repo-url https://github.com/corca-ai/charness --json --skip-claude-wrapper`
  - checkout version: `0.14.0`
  - managed checkout: cloned to `<temp>/.agents/src/charness`
- `<temp>/.local/bin/charness update --home-root <temp> --json --skip-claude-wrapper --skip-codex-cache-refresh`
  - checkout version: `0.14.0`
  - managed checkout: pulled successfully

## Tokei Missing State

With `PATH=/usr/bin:/bin`, `charness tool doctor tokei --json --no-write-locks`
reported:

- doctor status: `missing`
- doctor disposition: `blocking-install-needed`
- install docs URL: `https://github.com/XAMPPRocky/tokei#installation`
- next step named the install docs and latest upstream release
  `https://github.com/XAMPPRocky/tokei/releases/tag/v14.0.0`

This proves the missing-binary state surfaces upstream install guidance instead
of guessing a package-manager command.

## Tokei Install And Detected State

The proof installed `tokei` inside the clean temp home with the
manifest-supported cargo path:

```bash
cargo install tokei --root /tmp/charness-v014-proof-home-JnDy6H/.cargo --force
```

The install completed successfully:

- installed package: `tokei v14.0.0`
- installed binary: `/tmp/charness-v014-proof-home-JnDy6H/.cargo/bin/tokei`
- `tokei --version`: `tokei 14.0.0 compiled with serialization support: json`

With `PATH=/tmp/charness-v014-proof-home-JnDy6H/.cargo/bin:/usr/bin:/bin`,
`charness tool doctor tokei --json --no-write-locks` reported:

- doctor status: `ok`
- observed version: `14.0.0`
- resolved binary: `/tmp/charness-v014-proof-home-JnDy6H/.cargo/bin/tokei`

`tokei --version` is therefore available to validation gates through the clean
temp-home install path.

## Support Sync Result

`charness tool sync-support tokei --json` reported:

- support status: `skipped`
- reason: `integration has no support_skill_source`

This is the correct result for `tokei`: it is an integration-only validation
binary, not a materialized support skill. The original v0.14.0 checklist wording
expected a generated support surface; that checklist has been corrected for
future releases.

## Result

This closes the `real_host_required: true` item recorded for v0.14.0. The
release remains public and verified; this artifact supplies the captured
real-host proof that `charness-artifacts/release/latest.md` required at publish
time.
