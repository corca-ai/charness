# Claims Review — Charness v8.0.1

Prepared commit: `b4753f3b29b7e976be2b5f05e6e366eef4fb80dd`
Target version/tag: `8.0.1` / `v8.0.1`
Previous release base: `refs/tags/v8.0.0`
Observer: `separate-agent-context` (`/root/luna_v801_claims_review`)
Verdict: **PASS**

## Scope

The prepared release is clean at the named commit and remains in the expected
`prepared-awaiting-claims-review` state. `v8.0.0` is the reachable previous release
tag; `v8.0.1` is correctly still unpublished. Packaging, Claude plugin, Codex
plugin, and Claude marketplace version surfaces all report `8.0.1`; the Codex
marketplace source path is present. `current_release.py` reports no drift across
four versioned surfaces and one presence-only surface.

Exact release delta:

- Changed-path count: `21`
- Changed-path digest: `1286e84c20056099918e3a800dfa5042e1724a145b25bbdc5d733066c83f3617`
- Base: `refs/tags/v8.0.0`

Blocking shipped/product paths:

- `.agents/release-adapter.yaml`
- `.claude-plugin/marketplace.json`
- `.githooks/pre-push`
- `charness-artifacts/debug/2026-08-30-subprocess-git-fixture-duplication.md`
- `charness-artifacts/probe/2026-08-30-v8.0.0-release-observer.json`
- `charness-artifacts/release-review/2026-08-31-v8.0.0-claims-review.json`
- `charness-artifacts/release/2026-08-31-v8.0.1-notes.md`
- `charness-artifacts/release/latest.md`
- `packaging/charness.json`
- `scripts/prepush_quality_receipt.py`
- `scripts/proof_receipt.py`
- `scripts/run-quality.sh`
- `skills/public/release/references/adapter-contract.md`
- `skills/public/release/scripts/publish_release_common.py`
- `skills/public/release/scripts/publish_release_resume_publish.py`
- `tests/quality_gates/test_prepush_runtime_regime.py`
- `tests/quality_gates/test_proof_receipt.py`
- `tests/quality_gates/test_quality_runner_coverage_selection.py`
- `tests/quality_gates/test_release_quality_status_binding.py`

Advisory session-narrative paths:

- `charness-artifacts/critique/2026-08-31-v8-0-1-release.md`
- `charness-artifacts/release-review/2026-08-31-v8.0.0-claims-review.md`

## Blocking shipped/product findings

No blocking claims defects found.

The release RECORD correctly asserts:

- target `8.0.1` and tag `v8.0.1`;
- previous version `8.0.0`;
- clean local release mutation with branch/tag push, GitHub publication, public
  verification, and install refresh still pending;
- quality command exit `0`, measured by the helper at `post-bump, pre-commit`;
- version-drift result from `current_release.py`;
- adapter preflight passed;
- no completed claims review yet.

The quality claim is correctly bounded to:

`./scripts/run-quality.sh --release --read-only --non-claim=release-changed-line-coverage`

The notes explicitly preserve the only omitted verdict: changed-line mutation
coverage. They claim no mutation verdict or score. No broader omission is asserted.

The receipt-reuse claim is accurately conditional. The shipped implementation binds
reuse to the exact clean `HEAD`, Git tree, repo-owned release quality command, and
materialized plugin bytes; validates pushed local-ref ancestry; runs the close-keyword
guard before receipt reuse; consumes a valid one-shot receipt; and falls back to the
ordinary pre-push gate for dirty, stale, malformed, export-drift, changed-commit, or
uncovered-ref conditions. The notes do not claim unconditional speedups or that the
canonical quality run itself is faster.

Update and rollback guidance is consistent with the shipped adapter and notes:

- update via `charness update`;
- verify `charness version` and `charness doctor`;
- restart the host session;
- installed consumers can reinstall/pin `v8.0.0`;
- source checkouts must also revert/pin the checkout to restore repo-local hooks and
  release helpers.

The derived claim block is clean under the release-note generator/checker:

- `json-declaring-scripts`: `0`
- `charness-subcommands`: `12`
- `public-skills`: `19`
- `repo-shell-gates`: `8`

These counts are generated from the declared shipped-tree scopes and include explicit
scanned/unscanned boundaries. No unsupported authored quantity was found.

The runtime values in `latest.md` are helper-emitted measurements
(`requested_review_gate`, `cli_skill_surface_gate`, `quality_command`, and
`fresh_checkout_probes_initial`). The quality line names both the command and helper
measurement; the notes avoid turning these timings into a consumer-performance claim.

## Advisory findings

No advisory findings require a release-blocking disposition. The two advisory paths
are session narratives and remain outside the tag-gating verdict under
`charness.release.claims-review.v4`.

## Explicit nonclaims

This review does not claim:

- code safety or complete behavioral correctness;
- public tag push, GitHub release creation, or public release visibility;
- installed-host version/doctor readback;
- changed-line mutation coverage or a mutation score;
- that release quality, plugin import checks, or subsequent pushes are generally fast;
- a consumer-development speedup or acceptable absolute process-spawn floor;
- small residual Git/Python process populations;
- receipt integrity as a security boundary against checkout rewriting or Git-hook bypass;
- independent verification of the advisory session narratives.

## Verdict

PASS for the prepared release claims record. The version, target, release record,
notes, and derived structural facts are aligned with the evidence available before
publication. The pass is scoped to the complete machine-derived release delta and the
claims present in the prepared record. It does not assert mutation score, consumer
speed, public readback, or install refresh; those remain explicit nonclaims and must be
verified or recorded by the publication boundary after this review.

Fresh-Eye Satisfaction: parent-delegated
