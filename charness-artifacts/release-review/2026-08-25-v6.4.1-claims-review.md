# v6.4.1 claims review

- Prepared commit: `924c7f5d619f7cb062ad676601f0653bf4dba3b0`
- Target version/tag: `6.4.1` / `v6.4.1`
- Release record: `charness-artifacts/release/latest.md`
- Release record SHA256: `da33db3a25a9d107bc4174ac546dc16a06bed1f31575bbe9e3fa67057679ef6a`
- Verdict: **pass**, with one advisory narrative finding and no confirmed blocking claim defect.

## Independent checks

A separate read-only claims observer reviewed the exact prepared record and immutable `v6.4.0..924c7f5d619f` delta. It verified:

- `current_release.py --repo-root .`: all five version surfaces are `6.4.1`, with `drift: []`.
- `.charness/release-quality-receipt.json`: typed `status: pass`, `effective_exit_code: 0`, 99 passed, 0 failed, 0 unproven/adverse.
- Fresh-checkout probes independently rerun in a temporary clone: all five returned 0.
- `validate_packaging.py` and `validate_packaging_committed.py`: both passed.
- `validate_current_pointer_freshness.py`: passed.
- Immutable real-host trigger evaluation: `required: false`, `evaluation_scope: evaluated`, 218 paths evaluated, no trigger matches.
- The critique artifact is tracked, binds `6.4.1`/`v6.4.1`, and records pre-mutation provenance.

## Scope

The claims scope is mechanically partitioned from `git diff --name-only v6.4.0..924c7f5d619f` using `skills/public/release/scripts/claims_review_scope.py`: 171 blocking paths and 47 advisory dated-narrative paths (218 total). The JSON record carries the complete partition.

## Advisory finding

`charness-artifacts/critique/2026-08-25-release-6-4-1.md` is dated narrative scope. Re-running its validator against the final delta reports stale declared inputs because the packet was prepared before the release mutation. This does not establish a false claim in the prepared release record; it is carried as advisory rather than silently omitted.

## Observer boundary

This verdict is from an independent read-only agent context, not a same-agent reread. The observer did not mutate the worktree, index, or external services. The publication lane must still re-run its own resume quality and public readback gates; this claims review does not claim tag, push, GitHub release, or install refresh completion.

