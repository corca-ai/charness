# Charness v8.4.4 claims review

Date: 2026-09-06
Prepared commit: `00400d1cf4e3c3e7af529f9de44611e9a5756235` (`Release charness 8.4.4`)
Target version: `8.4.4`, tag `v8.4.4`
Record under review: `charness-artifacts/release/latest.md` at the prepared commit

## Observer and review boundary

Observer identity: `/root/preview_design`, `separate-agent-context`. This is a
parent-delegated claims-only review performed outside the preparing agent's
context. It audits what the prepared release record and public notes assert; it
does not substitute another code review or infer correctness from the prior
critique.

The committed source was read through the exact prepared SHA. The review also
used the actual `v8.4.3..00400d1c` path delta, the clean checkout at that exact
HEAD, the repo-owned release-note derivation and narrative checks, the local
release quality receipt, and the parent's captured release-helper readbacks.

## Judged source scope

The assertion-bearing scope comprised:

- `charness-artifacts/release/latest.md` and
  `charness-artifacts/release/2026-09-06-v8.4.4-notes.md`;
- `packaging/charness.json`, `.claude-plugin/marketplace.json`,
  `.agents/plugins/marketplace.json`, and `.agents/release-adapter.yaml`;
- `charness-artifacts/critique/2026-09-06-goal798-release-8-4-4.md` and its
  bound final-consumer worker report and packet;
- the consumer comparison, output errata, frozen protocol and acceptance
  records under `charness-artifacts/probe/2026-09-06-consumer-journeys/`, plus
  the preview-recovery and advisory-render comparisons;
- the relevant implementation, documentation, and regression-test changes for
  evidence reuse, advisory wording, preview continuation and directory
  refusal, and staged Python-owner selection; and
- the complete release delta against `v8.4.3`, used to test the stated patch
  boundary rather than treating the four-note summary as a complete file list.

Excluded from this claims round were a broad test rerun, a new code-correctness
verdict, Claude-host behavior, generalized consumer speed or reliability,
remote tag/branch state, GitHub release visibility, post-publish installation,
and issue closure. Raw consumer traces and release-helper streams that are not
committed were treated only as parent-held corroboration, not durable evidence
inside the prepared commit.

## Re-derived claims

- The target is consistently `8.4.4`: the packaging version, embedded Codex
  and Claude manifest versions, and both Claude marketplace version slots agree.
  The Codex marketplace remains the declared presence-only path
  `./plugins/charness`. A fresh read-only release plan at the exact clean HEAD
  likewise reported four aligned versioned surfaces, one presence-only surface,
  and no drift.
- The actual `v8.4.3..00400d1c` delta contains 196 paths, 17,279 additions and
  6,778 deletions. The public-skill set remains the same 19 names, no schema
  path changed, and the `charness` entrypoint change is task-scope help text,
  not a new command. The source diff directly contains the four release-note
  themes. These facts support a patch repair; the conclusion that patch is the
  right semantic level remains human judgment, not a measured property.
- The repo-owned derived-claims checker re-derived all four note surfaces with
  no finding: zero repo-owned Python sources declaring their own `--json`
  option, 12 top-level `charness` subcommands, 19 public skills, and eight
  `check-*.sh` shell gates. The release narrative linter also returned clean.
- The consumer arithmetic is internally exact. Journey A totals move from
  3 duplicates, 217,928 tokens and 853.845 seconds to 5, 188,753 and 643.879;
  Journey B moves from 11, 126,035 and 610.259 to 9, 124,385 and 451.080.
  Aggregate duplicates therefore remain 14. Both alias arms have committed
  four-check acceptance passes and both CLI arms have committed eight-check
  acceptance passes. The notes correctly call the repeated-work result mixed
  and decline a generalized speed claim; the timeout deviation from frozen
  1,800 seconds to actual 3,600 seconds is disclosed.
- The preview comparison demonstrates both generated- and supplied-packet
  continuation: the baseline needs one renamed recovery invocation, while the
  candidate starts the live run with the same attempt and retains duplicate-live
  refusal. The source and focused tests implement the collision-proof preview
  artifact key, exact supplied-packet path matching, and typed directory
  refusal. The advisory comparison likewise shows the release renderer changed
  from declaring every advisory a known inaccuracy to neutral finding wording.
- The cited release critique exists, binds `8.4.4`, and records an eligible
  delivered final-consumer review. Its committed worker-report and packet
  SHA-256 values re-derived as
  `e10c776d5bd78d3171012bc65e24ad1e1250dca9ae39dd735ff55b3a546aeb80`
  and `8fe925ea438785dc2ff91219e5c51cf2a3dbb6f33714d1329e2ffd7569122cfc`,
  matching the critique.
- The recorded quality command exactly matches the unchanged release adapter.
  The local receipt for this run reports status `pass`, effective exit 0,
  88 passed, 0 failed, 410.2 seconds of quality-engine time, four explicit
  opt-in omissions, no adverse or unproven subjects, and
  `release-changed-line-coverage` in measured scope. The helper's wall time is
  410.655 seconds, which correctly renders as 410.7 seconds in the release
  record. The receipt's index tree is the pre-bump parent source tree; the
  record honestly labels the quality observation `post-bump, pre-commit`
  rather than claiming it was a rerun over the final prepared commit.
- The three fresh-checkout commands match the adapter. Parent-held helper output
  records pass in 4.580 seconds before the release-record amendment and pass in
  4.521 seconds after it. A separate fresh changed-line consumer readback over
  the final six changed pool files reported an empty blocking set. These are
  actual executions, but their raw streams are not committed in this strict
  claims-evidence slice.
- The adapter-unchanged explanation is supported by an empty adapter diff
  against `v8.4.3`; requested-review policy is `advisory-only` with zero
  configured commands; and the update text matches the adapter. No local
  `v8.4.4` tag exists. The record consistently leaves tag push, GitHub release,
  public verification, install refresh, and issue closeout pending.

## Proof versus reasoning

Directly re-derived proof covers the prepared commit identity, version and
marketplace values, complete changed-path inventory, unchanged public-skill and
schema surfaces, derived note figures, acceptance outputs, critique bindings,
adapter configuration, and the local quality receipt. Parent-held execution
output corroborates the helper wall time, both fresh-checkout runs, and final
changed-line readback.

Reasoning remains in the semantic patch-bump choice, the manual strict-duplicate
adjudication, and the claim that these bounded consumer observations are useful
evidence of preserved capability. The public notes mark the observations as
bounded and mixed, so none of that reasoning is presented as statistical or
causal proof.

## Advisory findings

- The release quality result is corroborated by local receipt `e83944f5c683da3eb28792df482a07a46dbd27c1ddd1d6f756467dc63ddb4c65`, but that receipt is uncommitted and its index-tree binding is the pre-bump parent source tree rather than the prepared release commit.
- Fresh-checkout passed is corroborated by parent-held initial and post-amend helper output, but the prepared commit contains no raw per-probe receipt.
- Consumer acceptance outputs are committed, while strict duplicate counts and some token/runtime provenance depend on normalized judgment records plus ephemeral or parent-held producer traces rather than a complete committed set of raw task receipts.
- The notes' phrase `Installed-package` refers to canonical exported plugin packages used outside the consumer repos, not a post-publish installed-host roundtrip; install refresh remains pending.

No reviewed assertion was demonstrated false, and each evidence limitation is
compatible with the record's explicit pending states and bounded non-claims.

## Verdict

pass
