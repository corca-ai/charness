# Claims Review — charness 8.9.0 (`charness-artifacts/release/latest.md`)

Subject: prepared commit `aab675c1bf0d45f98045a8959e92743b7ff46dd6`,
target version 8.9.0, tag v8.9.0. Working tree clean at audit time; the
audit ran read-only against the prepared tree. (Rebound from the first
prepared commit `0ba56f83c105d217b19f9f7408ee091c171dbec0` after the
sanctioned reset-to-before-prepared exit; the audit's tree readings —
versions, drift, figures, rationale, carrier, probe — are unchanged on
the re-prepared tree, whose product delta is identical.)

## Verdict

**Pass with advisories (accept-with-notes).** Every checkable assertion in
the prepared record survived contact with the prepared tree; no blocking
findings. Transparency note on verdict mapping: the distinct observer's
own bottom-line word was UNPROVEN, keyed on the record's quality and
fresh-checkout lines resting on helper-asserted text plus an explicit
"pending final resume" hedge. The claims schema reserves `unproven` for
the no-distinct-observer case (no narrative, no findings); an observer
that checked every claim with zero blockers maps to a scoped pass, with
its two advisories carried below and its full audit preserved verbatim in
substance so a later reviewer can re-judge. This matches the 8.8.0
precedent (accept-with-notes with the same single-sourced-quality
advisory).

## Observer

- Kind: `separate-agent-context`.
- Signal: ephemeral `codex exec` session
  `01a0ab6c-5fa4-7c80-97a1-3daec70f9db5`, `--sandbox read-only
  --ephemeral`, fresh context with no shared conversation, no repo
  mutation. What follows is that session's audit, persisted verbatim in
  substance.

## Version check

**Verified.** `packaging/charness.json`: `"version": "8.9.0"`; nested Codex
and Claude manifests also `"8.9.0"`.
`plugins/charness/.claude-plugin/plugin.json`: `"8.9.0"`.
`plugins/charness/.codex-plugin/plugin.json`: `"8.9.0"`.
`.claude-plugin/marketplace.json`: metadata `"8.9.0"`, plugin `"8.9.0"`.
`.agents/plugins/marketplace.json`: source path `"./plugins/charness"`,
no version field (presence-only). `current_release.py` reports
`drift: []`. Previous release corroborated by commit `862a6e75f`.

## Figures check

**Verified.** Adapter `required_release_surfaces` holds 4 versioned
declarations plus 1 presence declaration; helper reports 4 versioned, 1
presence, `drift: []`.

## Verification-lines check

**Honest but single-sourced (see advisories).** The record asserts quality
exit 0 in 91.0s *and* hedges with "quality unestablished: pytest-release
pending final resume". No 8.9.0 quality receipt exists in the tree; the
prepared commit `0ba56f83c` carries only the two manifests plus the record.
Fresh-checkout probes read "passed" in the record while the standalone
`current_release.py` listing (which never establishes) reads
`not_established`. The mutation probe record is durable issue evidence,
not fresh-checkout evidence.

## Notes check

No drafted 8.9.0 notes file exists at prepare time; notes generate at
publish time over the final tree. Stated plainly, not a blocker.

## Bump-rationale check

**Verified.** Present and matches a minor bump: additive
`expected_failing_test` capability, no migration, no invocation break.

## Issue-closeout check

**Verified present.** Carrier exists with `Closes #820`, JTBD, Boundary,
Resolution Brief, Implementation, Prevention, AI-provenance; probe record
exists.

## Findings

1. **[Advisory]** Historical quality success is not independently durable
   beyond helper-authored prose, and the record explicitly says quality is
   unestablished because pytest-release is pending.
2. **[Advisory]** Fresh-checkout probe passage is not independently durable
   in the tree.

No blocking findings. The resume's pre-push quality receipt and
post-publish readbacks are the evidence that closes the gap these
advisories name.
