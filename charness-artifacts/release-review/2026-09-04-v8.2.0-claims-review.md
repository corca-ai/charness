# v8.2.0 claims review

Date: 2026-09-04
Prepared commit: `f4b85e906365f021fb2dae9d9815043fc942c2b8` (Release charness 8.2.0)
Target version: `8.2.0`, tag `v8.2.0`
Record under review: `charness-artifacts/release/latest.md` (prepared, awaiting claims review)

## Observer

A file-backed Codex reviewer run through `skills/public/critique/scripts/run_review.py`
(backend `codex_exec`, boundary `read-only-worker`), attempt
`release-8-2-0-claims-1`, packet
`charness-artifacts/critique/release-8-2-0-claims-1-packet.json`
(SHA256 `518a44081a40dfa2f98bc9e6f0b15bdfe0bc7a1413a594cf788bf0300fca5071`,
reviewed-input identity
`30804a66cd777ea10710fb8f6d734e99712351b43f5843a2378d310fe984db53`).
The worker ran in its own process and context with the packet as its only
input; the preparer (this session's Claude Code agent) wrote neither its
prompt answers nor its result. Delivery state `findings-received`, verdict
`pass`.

## What the round audited

The reviewer was asked to audit what the record asserts: re-derive every
figure it can, check each recorded reason against the text it cites, and
separate a record defect from an evidence-boundary gap. The packet bound the
prepared record, the v8.1.0 notes, the packaging manifest, the Claude
marketplace file, the release adapter, the critique record and the debug
record the release cites, and the shipped surfaces the bump rationale
describes (`README.md`, `skills/public/impl/SKILL.md`,
`skills/public/debug/references/detection-gap.md`, `docs/agent-task-runs.md`).
The plugin manifests under `plugins/` are a generated, git-ignored mirror and
were not bound.

## Findings of this round

Re-derived and correct: the version figures across the bound manifests
(`8.2.0` in `packaging/charness.json` and `.claude-plugin/marketplace.json`,
previous `8.1.0`); the record's release-state sentences, which claim no push,
tag, or hosted completion; the bump rationale's description of each shipped
surface against its bytes. No reviewed sentence was demonstrated false. Eight
findings are typed as evidence-boundary gaps and recorded as advisory
findings in the claims record, not repaired:

- CLAIM-001 (medium): the quality lane, timings, and fresh-checkout probes
  are asserted by the helper's own record without raw receipts. The helper
  measured them in this prepare (`publish_release.py --execute` in this
  checkout, lane 327.6 s); the same lane also ran standalone before the
  helper (88 passed, 0 failed). This record does not claim the reviewer did.
- CLAIM-002 (low): the version bytes agree, but the exact `current_release.py`
  result is not bound.
- CLAIM-003 (medium): "release adapter did not change in the release delta"
  cannot be re-derived from the packet. The measurement:
  `git diff v8.1.0..f4b85e906 -- .agents/release-adapter.yaml` is empty.
- CLAIM-004 (medium): "no public skill, CLI subcommand, adapter key, or
  install surface gained or lost a member" is not provable from the bound
  surfaces. The measurement: `git ls-tree` of `skills/public/` and `.agents/`
  is identical at `v8.1.0` and `f4b85e906`; the set of `add_parser(...)`
  names in `charness` is identical; the 19 non-artifact changed paths are
  `README.md`, `charness`, two docs pages, one gate script, five task_run
  modules, three skill files, four test files, and the two version files.
- CLAIM-005 (medium): the task-run rationale is supported by the bound docs;
  runtime behaviour and "in every record" are proven by the tests in
  `tests/charness_cli/test_task_run_result.py` and `test_task_run_scope.py`
  (48 passed), not by the packet.
- CLAIM-006 (medium): the critique record is a narrative over reviewer
  decisions the packet does not carry; the six reviewer packets it names are
  tracked under `charness-artifacts/critique/` with their SHA256s.
- CLAIM-007 (low): the minor-bump reading and the prepared Git state are
  authored judgment; the policy text is
  `skills/public/release/references/version-policy.md` (Minor: "meaningful
  new behavior that existing users can adopt without migration").
- CLAIM-008 (low): "corrected against `init.sh` and the CLI" exceeds the
  packet; the two README reviewers' reports are in this session's transcript
  and the corrections are commit `b528a0add`.

## Verdict recorded

`pass`, with the eight advisory findings above. The distinct-observer property
is established by the worker's separate process and context, not by the
reviewer's agreement; what the review proves is that the record's sentences
survive contact with the bound bytes, and what it does not prove is listed
above.
