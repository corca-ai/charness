# v8.1.0 claims review

Date: 2026-09-04
Prepared commit: `b66a55f8950e1d77f2d1bcd5e9165ab1f5efd3e4` (Release charness 8.1.0)
Target version: `8.1.0`, tag `v8.1.0`
Record under review: `charness-artifacts/release/latest.md` (prepared, awaiting claims review) and `charness-artifacts/release/v8.1.0-notes.md`

## Observer

A file-backed Codex reviewer run through `skills/public/critique/scripts/run_review.py`
(backend `codex_exec`, boundary `read-only-worker`), attempt
`release-8-1-0-claims-1`, packet
`charness-artifacts/critique/release-8-1-0-claims-1-packet.json`
(SHA256 `a57f98fea547645c1354424a7e6136ef6462b3a5a5b9cdc4f8a034baaa6fbe2f`,
reviewed-input identity
`2af9db5682fee195b460e8815bd41d913e4a5e835dc6aed0779459932b40ede1`).
The worker ran in its own process and context with the packet as its only
input; the preparer (this session's Claude Code agent) wrote neither its
prompt answers nor its result. Delivery state `findings-received`, verdict
`pass`.

## What the round audited

The reviewer was asked to audit what the record asserts: re-derive every
figure it can, check each recorded reason against the text it cites, name
anything claimed as proven that was only reasoned about, and separate a record
defect from evidence the packet cannot carry. The packet bound the prepared
record, the v8.1.0 notes and the v8.0.3 notes (so the derived-block comparison
could be re-derived, the gap the v8.0.3 round named), the packaging manifest,
both plugin manifests, both marketplace files, the release adapter, the
critique record, and the install rehearsal receipt.

## Findings of this round

Re-derived and correct: the version figures across every bound manifest; the
four derived counts against their item lists; the four item sets and the
whole derived block against the v8.0.3 notes; the required surfaces, the
review-gate posture, the update instructions, the probe list; the release
state sentences, which claim no hosted completion. No reviewed sentence was
demonstrated false. Five findings are typed as evidence-boundary gaps and
recorded as advisory findings in the claims record, not repaired:

- CLAIM-003: the sentence that `required_release_surfaces` is byte-identical
  to v8.0.3 binds only the current adapter. The measurement:
  `git show v8.0.3:.agents/release-adapter.yaml` has the same
  `required_release_surfaces` block.
- CLAIM-004: the record's "adapter did not change in the release delta" has no
  bound prior adapter; consistent with CLAIM-003's measurement.
- CLAIM-006: the release lane, `current_release.py`, and the fresh-checkout
  probes are asserted by the helper's own record without raw receipts; the
  helper measured them in this prepare (`publish_release.py --execute`, run in
  this checkout), and this record does not claim the reviewer did.
- CLAIM-007: the critique record says the helper runs a derived-notes check on
  the candidate and the release record does not name that step; the reviewer's
  own comparison of the two bound derived blocks agrees, and the publish
  preflight's notes check is not itemized in the record.
- CLAIM-009: the operator naming 8.1.0, the bump-policy reading, and the
  migration sentence are authored rationale the packet cannot prove; the
  operator's instruction ("8.1.0 으로 릴리즈") was given in the session on
  2026-09-04 and is recorded in the Goal Run #784 session record.

## Verdict recorded

`pass`, with the five advisory findings above. The distinct-observer property
is established by the worker's separate process and context, not by the
reviewer's agreement; what the review proves is that the record's sentences
survive contact with the bound bytes, and what it does not prove is listed
above.
