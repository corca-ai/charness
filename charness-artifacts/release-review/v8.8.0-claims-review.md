# Claims Review — charness 8.8.0 (`charness-artifacts/release/latest.md`)

## Verdict

**Accept-with-notes.** Every checkable assertion in the record survived contact with the prepared tree. No blocker found. One note (N1) records the limit of what this audit could independently corroborate on the quality line.

Scope notes: read-only audit as instructed; no files modified. HEAD is exactly the prepared commit (`862a6e75f45796e4ffc8d089fd50ac01aeef702d`), working tree clean (`git status --short` empty), so working-tree probes reflect the prepared tree.

## Version check

**Pass.** All version strings at `862a6e75` read `8.8.0`; no stale `8.7.2` remains in any versioned file:

- `git show 862a6e75:packaging/charness.json` → top-level `version: 8.8.0`, plus embedded codex-manifest and claude-manifest versions both `8.8.0` (verified via the shown diff `8.7.2 → 8.8.0` in all three hunks).
- `git show 862a6e75:.claude-plugin/marketplace.json` → `metadata.version: 8.8.0` and `plugins[0].version: 8.8.0`.
- `git grep -n "8\.7\.2" 862a6e75 -- packaging/charness.json .claude-plugin/marketplace.json .agents/plugins/marketplace.json` → no matches (exit 1), so nothing stale.
- Presence-only surface: `git show 862a6e75:.agents/plugins/marketplace.json` carries **no** `version` field (name/interface/plugins only) — consistent with the record's "4 versioned surface(s), with 1 presence-only surface(s)". There is no `plugins/charness` materialized root in the tree (`git ls-tree -r --name-only 862a6e75 -- plugins` empty), so there is no additional versioned root the record omits.
- Record's previous/target pair (`8.7.2` → `8.8.0`) matches the observed diff direction and the tag list (latest tag `v8.7.2`, no `v8.8.0` yet).

## Verification check

**Pass, with noted limit (N1).**

- Prepared commit exists and is minimal: `git show --stat 862a6e75` touches exactly 3 files — `.claude-plugin/marketplace.json` (4 lines), `packaging/charness.json` (6 lines), `charness-artifacts/release/latest.md` (record rewrite). **No code changes smuggled into the release commit.**
- Critique proof cited by the record exists and is tracked: `git ls-files --error-unmatch` succeeds for both `charness-artifacts/critique/review-20260916T115208Z-1966439-packet.md` and `charness-artifacts/critique/workers/review-20260916T115208Z-1966439/result.json`.
- Worker result verified by direct read of `charness-artifacts/critique/workers/review-20260916T115208Z-1966439/result.json`: `verdict: "pass"`, `findings: []` — matches the expected verdict.
- Bindings match on all three legs (all read from the `862a6e75` tree):
  - `result.json: packet_sha256 = 6e8d2d1a…03224` == raw sha256 of `review-20260916T115208Z-1966439-packet.json` at `862a6e75` (computed `6e8d2d1a…03224`) == sha in the packet.md verify command line.
  - `result.json: reviewed_input_identity_sha256 = ce08896a…4cc4` == packet JSON `reviewed_input_identity.identity_sha256 = ce08896a…4cc4` == packet.md line 9 `Reviewed input identity`.
  - Packet JSON `changed_ref = 4df26881…` matches the follow-up fix commit in the delta log.
- Quality line (`run-quality.sh --release --read-only` exit 0 in 267.6s post-bump/pre-commit, "quality unestablished: pytest-release pending final resume") is **honestly hedged** — it asserts a partial result and explicitly disclaims full quality establishment. See N1 for the corroboration limit.

## Rationale check

**Pass.** The delta `v8.7.2..862a6e75` plausibly contains all three claimed capabilities; one supporting file per claim inspected via `git show 862a6e75:<path>`:

1. **#818 body-evolution chain for Goal Run closeout** — `skills/public/issue/scripts/issue_goal_run_body_chain.py` opens with "Managed-body identity and revision descent for Goal Run closeout… accepts an evolved body only through the parent-owned revision chain"; `issue_goal_run_close.py` calls `CONTRACT.BINDING.require_body_descent(binding, issues, metadata)`. Present.
2. **#819 opt-in reviewer coverage floor with durable join** — `skills/shared/scripts/reviewer_result_contract.py` defines `ReviewerCoverageError`, `_normalize_expected_targets`, and documents "`expected_targets` is the opt-in declarative coverage floor". Present.
3. **Staged-tree changed-line analysis as new operator surface** — `scripts/gates_support/changed_line_staged_head.py` opens "Staged-tree analysis target for changed-line gates… `:staged:` names the staged index tree (`git write-tree`) as the analyzed head". Present, with accompanying `tests/quality_gates/test_changed_line_staged_head.py` (+272 lines in `99bb29a66`).
- **No breaking-change signal**: `git diff v8.7.2..862a6e75 --name-status | grep -E "^[RD]"` → empty (no renames/deletes); skills-scoped deletions → none. The "all additive with no breaking changes" clause is consistent with the observed delta shape.

## Non-claims check

**Pass.** The record claims nothing it should not. All public-visibility items read pending:

- `latest.md` lines 24–26: "branch/tag push: pending", "GitHub release record: pending… before creation", "public release surface verification: pending".
- Line 31: "GitHub release publication: expected after branch/tag push; not verified yet."
- Line 59: "Post-publish install refresh: pending final publish verification."
- Line 77: "Issue closeout verification: pending or not requested."
- Line 48: "Claims review: not yet performed — THIS record is the subject of the pending independent review."
- Fresh-checkout probes are stated as "passed" with the exact three commands enumerated — I re-ran all three read-only probes against the prepared tree and each exited 0 (`./charness --help`, `./charness goal run --help`, `python3 scripts/doctor.py --repo-root . --skip-release-probe`).
- Requested-review gate stated as `ok` / `advisory_only` / `0` commands — a null gate, not an inflated pass; no execution claimed beyond the 0.007s timing.
- Adapter preflight explicitly recorded as `not_required` / `not_run` with "This is a recorded absence, not a passing preflight" — honest non-claim.

## Findings

1. **[Note / low] Quality exit-0 line is honest but single-sourced.** Evidence: `charness-artifacts/release/latest.md:18` claims `./scripts/run-quality.sh --release --read-only` "exited 0 in 267.6s" but cites no receipt artifact (contrast the v8.7.2 record, which cited `charness-artifacts/release/8.7.2-prepush-quality.json` with sha256). The same line hedges with "quality unestablished: pytest-release pending final resume", and `Release Runtime` line 65 records `quality_command: 267.580s`, consistent with 267.6s. This audit did not re-run the 267s quality lane. Not a blocker: the record does not overclaim — it withholds any "quality established" verdict — but a future release-prepare should attach the quality receipt hash as v8.7.2 did.
2. **[Info] "4 versioned + 1 presence-only" figure is consistent but counting-method opaque.** Evidence: direct `git show`/`git grep` at `862a6e75` confirms every observed version string reads `8.8.0` (Finding: version check above); the checker source (`skills/public/release/scripts/current_release.py`) delegates surface verdicts to the adapter module, so the "4+1" split was corroborated by file-level observation rather than by re-deriving the helper's count. No inflation detected.

No blocking findings. The record is publishable to the next gate (branch/tag push) once this review is committed.
