# Charness 8.6.1 prepared-record claims review

**Verdict: PASS, with no blocking or advisory finding.**

I reviewed amended prepared commit
`983bf1da2f530af609d3f1b895d87cb203e91b21` (`983bf1da2`) for target
version `8.6.1` and tag `v8.6.1`, using the prepared release record, its release
critique, and the complete repository delta from prior release tag `v8.6.0`.
This is a distinct review of the claims made by the prepared release record. It
does not approve implementation correctness or establish any push, tag, GitHub
release, public readback, install refresh, or issue closeout.

## Findings

No blocking claim defect remains. No advisory finding was identified.

The earlier two-repair summary has been corrected. Both
`charness-artifacts/release/latest.md` and
`charness-artifacts/critique/2026-09-08-release-8-6-1.md` now identify all six
substantive change families visible in `v8.6.0..983bf1da2`:

- task worktrees survive generic cleanup;
- review lifecycle results retain structured metadata while complete worker
  input remains available to the worker path;
- implementation guidance tolerates a repository without `docs/index.md`;
- the implementation consumer fixture preserves literal relative references;
- resolved debug risks route through their recorded implementation handoff;
- task completion persists useful candidate bytes before later proof gates and
  re-observes the carrier before granting eligibility.

The corresponding changed owners are
`scripts/worktree/worktree_lifetime.py`,
`skills/public/critique/scripts/run_review.py`, `skills/public/impl/SKILL.md`,
`evals/fixtures/consumer-journeys/spec-impl-alias/prompts/impl.md`,
`skills/public/debug/scripts/debug_artifact_state.py`,
`skills/public/debug/scripts/plan_debug_run.py`, and
`scripts/task_run/task_run_completion.py`, with their focused tests and owning
documentation in the same delta. The amended scope and bump rationale no longer
reduce this set to only retention and lifecycle transport.

The patch classification is supportable as a claims statement. The listed
changes repair existing boundary behavior or guidance while the record says
existing invocation and metadata contracts remain intact. The delta does not
present a renamed public skill, removed command, new required migration, or
incompatible install surface that would contradict the stated patch rationale.

## Prepared-state facts confirmed

- **Version identity:** the prepared record consistently names previous version
  `8.6.0`, target `8.6.1`, and tag `v8.6.1`. At `983bf1da2`,
  `packaging/charness.json` reports `8.6.1` for the package and its embedded
  Codex and Claude manifests. `.claude-plugin/marketplace.json` also reports
  `8.6.1`. `.agents/plugins/marketplace.json` retains the declared local Codex
  marketplace source path. This agrees with the record's four versioned
  surfaces, one presence-only surface, and no-drift statement.
- **Preparation state:** `charness-artifacts/release/latest.md` retains the
  `charness-release-state:prepared-awaiting-claims-review` marker. It states
  that local release mutation is complete while branch/tag push, GitHub release
  creation, public verification, and install refresh are pending. The record
  does not describe the release as publicly available.
- **Preparation quality:** the record says
  `./scripts/run-quality.sh --release --read-only` exited 0 in 328.4 seconds at
  the post-bump, pre-commit stage. Its runtime table reports 328.441 seconds,
  consistent with that rounding. The same sentence explicitly says overall
  quality is unestablished because `pytest-release` remains pending final
  resume. A preparation command green is not presented as final release
  quality.
- **Fresh-checkout probes:** the prepared record says the initial probes passed
  and names the three commands: `./charness --help`,
  `./charness goal run --help`, and `scripts/doctor.py` with the release probe
  skipped. It records 5.389 seconds for `fresh_checkout_probes_initial`. This
  review did not rerun the probes and therefore confirms only the committed
  prepared-stage claim, not a post-review or published-tree result.
- **Critique:** the named release critique exists and preserves its separate
  pre-bump worker verdict. Its version-sync action is reflected in the prepared
  version files; its narrative action is reflected in the expanded scope and
  bump rationale. Its remaining public and installed-host readback actions are
  still future work rather than silently represented as complete.
- **Operator instructions:** the record directs existing users to `charness
  update`, `charness version`, and the GitHub release notes. These are update
  instructions, not claims that a maintainer or user installation has already
  refreshed.

## Evidence that remains pending

- Final `pytest-release` proof remains pending the release resume. The recorded
  328.4-second preparation lane does not establish it.
- Branch push and tag `v8.6.1` are pending. This review establishes no local or
  remote tag state.
- GitHub release creation and public release visibility are pending. The record's
  “expected after branch/tag push; not verified yet” wording is an explicit
  future-state nonclaim.
- Post-publish install refresh plus `charness version` and doctor readbacks are
  pending. No installed-host result is claimed here.
- Issue closeout is pending or was not requested. No issue closure is proven.
- Provider-side application of the critique worker's requested model and effort
  remains unexposed, as the critique states. Delivery of the critique result
  does not establish provider-applied tier metadata.

## Scope and nonclaims

The comparison basis is the complete repository delta
`v8.6.0..983bf1da2`, which contains 100 changed paths including evidence and
release records. The claims judgment above follows the shipped behavior owners
and the amended release narrative; historical artifacts in the delta are not
treated as proof of this release's future publication state.

This PASS establishes that the amended prepared record accurately describes the
version, prepared-stage verification, change scope, and remaining boundaries of
commit `983bf1da2`. It does not establish final release quality, security-clean
dependencies, performance or token savings, remote publication, public
visibility, installation refresh, or issue closure. Those claims require the
separate final, public, and installed-host evidence paths already named by the
release record.
