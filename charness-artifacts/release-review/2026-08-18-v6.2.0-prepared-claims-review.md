# v6.2.0 prepared claims review

Date: 2026-08-18
Prepared commit under review: `f3ae540f3611bff1c5f3d58112883c9fc2a2e41f`
(record repaired and the prepared commit amended to `e6d695e425d73ebf89eb7373f5dfa33ad19e02e7`)

## What this round asked

Not whether the code is correct — five earlier rounds asked that, and the range's
own critique artifacts hold their findings. This round asked the different
question the release contract reserves for it: **does the record of this release
survive contact with what shipped?**

## Observer

One bounded read-only reviewer subagent context (Read/Grep/Glob only; Bash, Edit,
Write and Agent absent, which it reported itself), spawned fresh against the
prepared record and deliberately not told the preparer's verdict. It could not run
`git show`, so it named four items it could not fetch rather than asserting them.

## Findings

It returned `blockers-found`, with three blockers — all of them about the record,
none about the code, and all three real.

1. **A green gate asserted without disclosing that its bar moved in the same
   delta.** The record read `./scripts/run-quality.sh --release passed before
   publish.` while `.agents/quality-adapter.yaml` — the first entry in the record's
   own changed-path list — raised the `pytest` bar from 120000 to 155000 inside
   this release. A verified sentence whose evidence changed under it.
2. **The record's own Review Proof contradicted the tree it vouched for.** The
   release critique artifact still read "not a third number" about the runtime
   budget, written before the relevel and left standing after it.
3. **No non-claim for the `--no-verify` push in this range.** The bypass was
   recorded in the handoff and in the adapter comment, and nowhere in the record
   an outside reader gets.

Two minors, both stale-state in `docs/handoff.md`: it still said "do NOT relevel"
after the relevel had landed, and "main and the released tag are pushed" when the
release commit was by the record's own account unpushed.

One minor it raised and this narrative accepts as fair but does not repair: the
record cites itself as its own audit narrative, which establishes nothing external.

## Disposition

All three blockers and both stale-state minors were repaired **before**
publication, and the prepared commit was amended rather than followed by a fixup,
so the record an outside reader gets is the repaired one:

- The record's `## Verification` section now carries two explicit non-claims: the
  relevel inside this delta, with the measured refutation it rests on, and the
  `--no-verify` push with the note that this repo's contract treats it as revoking
  the push grant.
- The release critique's runtime-budget bullet now describes what actually
  happened, including the figures that superseded it.
- Both `docs/handoff.md` sentences now match the tree.

## What this round did NOT establish

- Four evidence items the reviewer could not fetch without Bash: the `git show`
  behind the bump rationale's "verified, not assumed" (corroborated indirectly by
  the #640 resolution brief's pre-change table, which names the same three
  defaults, but not proven by that round); whether `c34155a48` is the v6.1.0
  release commit; the release-adapter no-change claim; and the helper's own
  timing log.
- Two derived note surfaces it did not fully verify: the `charness-subcommands`
  and `json-declaring-scripts` counts, which need the gate's own re-derivation.
- That a distinct observer existed is asserted by the recorded signal below and by
  nothing runnable on this machine. The release contract states that residual at
  its real size and this narrative does not enlarge the claim.
