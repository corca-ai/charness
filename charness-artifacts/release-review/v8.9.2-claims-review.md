# Claims Review — charness 8.9.2 (`charness-artifacts/release/latest.md`)

Subject: prepared commit `4b4f7fa2185c4680caeae81f4689a25b293f62e7`,
target version 8.9.2, tag v8.9.2. Working tree clean at audit time; the
audit ran read-only against the prepared tree.

## Verdict

**Pass with notes (accept-with-notes).** Every checkable assertion in the
prepared record survived contact with the prepared tree; no blocking
findings. The distinct observer's own bottom-line word was
PROVEN-WITH-NOTES, with one evidence-durability advisory carried below.

## Observer

- Kind: `separate-agent-context`.
- Signal: ephemeral `codex exec` session, `--sandbox read-only
  --ephemeral`, fresh context with no shared conversation, no repo
  mutation. What follows is that session's audit, persisted verbatim in
  substance.

## Version check

**Verified.** `packaging/charness.json`, both plugin manifests, and the
marketplace manifest all contain `8.9.2`; no drift.

## Figures check

**Verified.** Recorded surface counts match the adapter's
`required_release_surfaces`.

## Verification-lines check

**Honest but single-sourced (see advisories).** The `136.9s` exit-0
claim agrees with the recorded `136.905s` and is explicitly hedged with
pytest-release pending final resume. Fresh-checkout commands match the
adapter and read passed, with no separate hash-bound receipt durable in
the tree.

## Notes check

**Verified.** No drafted 8.9.2 notes file exists; notes generate at
publish time. Stated plainly, not a blocker.

## Bump-rationale check

**Verified.** Present and matches a patch bump: two refusal-behavior
repairs with no command, argument, plugin, or invocation-shape change.

## Issue-closeout check

**Verified.** "Pending or not requested" makes no closeout claim, and
none exists in the prepared tree. No issue is linked to this release.

## Findings

1. **[Advisory]** Preparation quality and fresh-checkout results lack
   separate committed receipts; both are durable only through the
   committed record. The resume's pre-push quality receipt closes this
   gap.

No blocking findings.
