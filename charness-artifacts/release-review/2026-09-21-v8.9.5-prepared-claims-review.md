# Prepared-claims review — charness 8.9.5

- Prepared commit: `8b78a8f12378c7d39170e7276ecacd9e85a61931` (range `v8.9.4..8b78a8f12`, verified fresh; tag `v8.9.5` not yet created)
- Verdict: **pass** — no failing claims.

## Claims

- Version bump 8.9.4 → 8.9.5, patch-only, no stray production changes: **pass**. Re-ran `git show --stat` on the prepared commit independently: exactly 3 files (`.claude-plugin/marketplace.json`, `packaging/charness.json`, `charness-artifacts/release/latest.md`), 27 insertions / 110 deletions. The production delta since `v8.9.4` (task-run persistence scoping, staged-owner hooks, reviewer contract, critique/release scripts, regression tests) sits in earlier commits in the range, not in the bump commit itself. The `latest.md` diff shrinks the record to prepared-state shape (push/publish/install-refresh marked pending) rather than adding production code.
- No version drift: **pass**. Independent grep over all release surfaces shows `8.9.5` in `.claude-plugin/marketplace.json` (2 spots), `packaging/charness.json` (3 spots), `plugins/charness/.codex-plugin/plugin.json`, and `plugins/charness/.claude-plugin/plugin.json`; zero `8.9.4` hits outside `charness-artifacts/` history. `current_release.py` corroborates with `drift: []`, no unpublished or undeclared-absent surfaces.
- Critique record + validator: **pass**. `charness-artifacts/critique/v895-release-critique.md` exists and carries a fresh-eye line (four file-backed workers, two rounds, no same-agent substitution) plus a tier-evidence section (requested tier n/a, host-defaulted, no provider-confirmed model/effort claim). Re-ran `scripts/review/validate_critique_artifacts.py --repo-root . --all`: exit 0, 993 artifacts validated; the single date-channel note concerns an unrelated historical file.
- Fresh-checkout probes: **pass**. Re-ran `python3 skills/public/release/scripts/check_fresh_checkout_probes.py --repo-root . --run-probes` independently: `fresh checkout probes: passed`, exit 0.
- Bump rationale matches the delta: **pass**. The prepared commit message is the bare `Release charness 8.9.5` marker (no rationale inline), but the range delta matches the critique's stated patch story: task-run completion/persistence scoping (`task_run_completion.py`, `task_run_git.py`, `task_run_lane_runner.py`), staged eviction owners (`check_staged_cheap_owners.py`, `check_module_eviction_form.py`), reviewer worker contract and coverage-floor opt-in (`reviewer_result_contract.py`, `run_reviewer_worker.py`, `run_review*.py`, `plan_release_run_packets.py`), plus regression tests. Additive flags only; no new user-facing surface, so patch (not minor) is the correct call. Adapter preflight `not_required` is consistent with no release-adapter change in the delta.
- Focused regression suites: **pass**. Ran via the standing runner with `--pytest-target` per file (bare paths are rejected by the runner's argparse, so three `--pytest-target` flags): `tests/charness_cli/test_task_run_completion.py`, `tests/quality_gates/test_staged_cheap_owners.py`, `tests/quality_gates/test_review_target_coverage_floor.py` — 50 passed, exit 0.

## Notes

- The standing-runner invocation in the task text (bare positional paths) does not parse; used the runner's `--pytest-target` flag per file instead, same suites, same gate.
- `latest.md` records quality as post-bump/pre-commit 258.4s exit 0 with pytest-release pending final resume; that residual is disclosed in the record itself, not hidden, and does not fail any claim above.
- Open risks: branch/tag push pending, GitHub release record pending, public-surface verification pending, install refresh pending; installed surfaces remain at the prior release until refresh, as expected for a prepared-but-unpublished state.

## Files

- `charness-artifacts/release-review/2026-09-21-v8.9.5-prepared-claims-review.md` (this narrative)
