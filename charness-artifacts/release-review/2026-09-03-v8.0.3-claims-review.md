# v8.0.3 claims review

Date: 2026-09-03
Prepared commit: `516da0a55c4de3713406f0bf246a866a422957c2` (Release charness 8.0.3)
Target version: `8.0.3`, tag `v8.0.3`
Record under review: `charness-artifacts/release/latest.md` (prepared, awaiting claims review) and `charness-artifacts/release/v8.0.3-notes.md`

## Observer

A file-backed Codex reviewer run through `skills/public/critique/scripts/run_review.py`
(backend `codex_exec`, boundary `read-only-worker`), attempt
`release-8-0-3-claims-4`, packet
`charness-artifacts/critique/release-8-0-3-claims-4-packet.json`
(SHA256 `e42a8be82a4b104ba99e593b549179fb57aa694b5fb7931edbae8ff668746f0b`,
reviewed-input identity
`0eefdd72ed06a22a28da0fce22458fc04bf3ec47e1687ea54768871273d875df`).
The worker ran in its own process and context with the packet as its only
input; the preparer (this session's Claude Code agent) wrote neither its
prompt answers nor its result. Delivery state `findings-received`, verdict
`defer`.

## What the round audited

The reviewer was asked to audit what the record asserts: re-derive every
figure it can, check each recorded reason against the text it cites, name
anything claimed as proven that was only reasoned about, and separate a record
defect (a sentence false or unverifiable as written) from evidence the packet
cannot carry.

## Rounds before this one

Three earlier rounds on earlier prepared records found and drove repairs:
round 1 (`release-8-0-3-claims-1`) found the notes said the release planner
refuses a drifted copy when it only reports, and asked for the measurement
behind the no-membership-change claim; round 2 (`release-8-0-3-claims-2`)
found the file-level add/delete measurement did not prove set membership and
that "nothing needs migration" was unqualified; round 3
(`release-8-0-3-claims-3`) reviewed a stale tree because that prepare had run
in a push clone rather than this checkout, and its findings were about the
wrong record. Each repair moved the tree, the record was re-prepared, and
this round is the one bound to the prepared commit above.

## Findings of this round

No reviewed sentence was demonstrated false from the bound bytes (the
reviewer's own non-claim). Three findings remain, each typed by the reviewer
as an evidence-boundary gap rather than a record defect; they are recorded as
advisory findings in the claims record and are not repaired in the record:

- CA-1: the set-identity comparison of the derived inventories against the
  v8.0.2 notes' block, and the byte-identity of the adapter's required
  surfaces, are preparer measurements the packet cannot re-derive (the v8.0.2
  bytes are not bound). The measurement: the four derived item lists in
  `charness-artifacts/release/v8.0.2-notes.md` compare equal as sets to the
  same lists in the v8.0.3 notes, and
  `git show v8.0.2:.agents/release-adapter.yaml` has the same
  `required_release_surfaces` block.
- CA-2: the release quality run (exit 0 in 164.7 s at post-bump, pre-commit),
  the focused adapter preflight, and the fresh-checkout probes are asserted by
  the helper's own record without a bound receipt. The helper measured them in
  this prepare (`publish_release.py --execute`, run in this checkout); the
  reviewer did not, and this record does not claim the reviewer did.
- CA-3: the Codex plugin manifest and Codex marketplace file were not in the
  packet, so the four-versioned-plus-one-presence-only result is only
  partially inspectable from the bound surfaces; the surfaces that were bound
  all read 8.0.3.

The reviewer cleared the remaining concerns it raised against itself: the
derived counts are arithmetically consistent with their item lists; the
record does not imply publication already occurred; the migration sentence,
the rehearsal label, and the pickup/planner/publisher wording were binned as
over-worry after the earlier repairs.

## Verdict recorded

`pass`, with the three advisory findings above. The distinct-observer
property is established by the worker's separate process and context, not by
the reviewer's agreement; what the review proves is that the record's
sentences survive contact with the bound bytes, and what it does not prove is
listed above.
