# Claims Review — charness 8.9.8 (v8.9.8)

Subject: `charness-artifacts/release/latest.md` at prepared commit 5d5994d1a2603a08a4a05e80566a8bcb0bb60ccc for target version 8.9.8 (hash bound by the prepared commit, not by this text).
Observer: independent claims-review observer (record audit only; no code read for correctness, no mutation, no commits).

Verdict: no BLOCKER. Publication stays stopped until the pending resume steps complete, exactly as the record already states.

## Version claim

- Record claims target `8.9.8` from previous `8.9.6`. Verified against the prepared commit: `packaging/charness.json` reads `"version": "8.9.8"` at top level and in both embedded manifests (codex `8.9.8`, claude `8.9.8`); `.claude-plugin/marketplace.json` reads `8.9.8` in metadata and plugin entry. The prepared-commit diff is `8.9.6` → `8.9.8` on exactly those two files, plus the record itself.
- The record's `4 versioned surface(s), 1 presence-only surface(s)` line matches the tree: `.agents/plugins/marketplace.json` carries no version field (local-source entry, `AVAILABLE`/`ON_INSTALL` policy), so it is presence-only by construction, not a gap. The per-executor plugin manifests regenerate at publish sync; their absence from the prepared commit matches the helper's stated order, not a gap.
- Advisory: tag `v8.9.7` already exists (commit `50f68ab4`, ancestor of the prepared commit) for earlier work, but its tree still reads `8.9.6` in all versioned surfaces — it was tagged without a packaging bump. The record's `previous 8.9.6 → target 8.9.8` therefore matches the actual file lineage; nobody should expect a `8.9.7`-reading manifest anywhere. No record change required.
- Bump rationale rendered as passed (patch: task-run lane resilience, goal contracts, persistence-loss lens, #826–#830).

## Verification lines

- Record says the read-only quality command exited 0 (96.8 s) and in the same line states `quality unestablished: pytest-release pending final resume` — i.e. quality is explicitly unestablished pending resume. No premature green is claimed. Advisory: the 96.8 s figure is helper-measured at `post-bump, pre-commit`; accept it as stated, it is not independently re-timed here.
- Fresh-checkout probes reported passed (3/3: `./charness --help`, `./charness goal run --help`, `doctor.py --skip-release-probe`). Commands are enumerated, not just counted. No concern.
- Full-lane evidence for the shipped code exists independently of this record: `/tmp/release-lane14.log` closes with `89 passed, 0 failed, 4 not run` (all four not-run are opt-in unmet: browser-runtime-baseline, dead-code-advisory, supply-chain-online, browser-runtime-hygiene). This supports the code; it does not substitute for the pending `pytest-release` resume the record already demands.
- No release-notes figures are asserted by the record, so there is nothing to bind. The record's `not_required`/`not_run` adapter preflight is a recorded absence, explicitly not a pass — correctly framed.

## Release-notes linkage

- No `v8.9.8` notes file exists under `charness-artifacts/release/` (checked `*8.9.8*`, `*v898*`: absent), and no GitHub release exists yet (record: pending review). The notes are therefore a resume/publish-step obligation, not a missing artifact at this gate.
- Binding obligation carried forward: per `charness-artifacts/critique/v898-critique.md` Act Before Ship item 1, the GitHub release notes MUST lead with the persistence-lens green-to-red paragraph (DROP/TRUNCATE/unscoped-delete now block as needs-review; previously-green lanes turn red; `MAX_ATTEMPTS` x `--timeout-seconds` wall-time budget). The docs half of item 1 is verified done — `docs/agent-task-runs.md` names the blocked shapes, the green-to-red change, and the budget formula (committed in `d9696d85d`). The notes half is unverifiable until the resume writes them; the publisher must bind the lead paragraph to the docs wording, not compose a variant. Advisory, not a blocker now — but a notes-first-paragraph check belongs in the post-publish readback.

## Closeout linkage

- The record says `Issue closeout verification: pending or not requested`, which conflates two states (same wording weakness as the v8.9.6 review). Exact state: closeout REQUESTED for #826–#830 via the bundled bug carrier `/tmp/v898-closeout-carrier.md`; verification PENDING the resume run, which performs it and rewrites this record. No silent not-applicable is claimed. Advisory only; no carrier change required.
- Carrier read directly: `Closes #826`–`Closes #830` lines present, critique pointer present, all required sections present (JTBD, Root Cause, Debug Artifact, Siblings, Prevention, per-issue Behavior #826–#830, AI-provenance) with no missing fields on inspection. Draft-verified status is parent-asserted; the resume's `verify-closeout` is the independent confirmation, still pending.

## Review-proof linkage

- Verified directly: `charness-artifacts/critique/v898-critique.md` exists, is committed (`d9696d85d`, ancestor of the prepared commit), cites #826–#830 with scope (`v8.9.7..f1b790c8f`), records the two angle reviewers plus synthesis, and carries the packet/identity pair (`2026-09-22-215724-packet.json`, packet `634dcbd2…`, identity `f3f05340…`). Packet file exists and its sha256 re-hashes to the cited `634dcbd2…` value. The release record already names the critique under Review Proof. Advisory only; nothing to add.
- Critique verdict (`Ship v8.9.8 (patch) once item 1 is in the tree and the notes lead with it`) is consistent with this review: item 1's docs half is in the tree, the notes half rides the resume.

## Non-claims

- Branch/tag push, GitHub release creation, public readback, and install refresh are all pending review — the record claims none of them (working tree confirms: `main` ahead 1 with the unpushed prepared commit). GitHub CLOSED states for #826–#830 are unverified until the resume's `verify-closeout` re-reads them as CLOSED.
- Requested-review gate (`ok`, `advisory-only`, 0 commands) claims no enforcement; correctly framed as advisory.
