# v8.3.0 claims review

Date: 2026-09-04
Prepared commit: `c2283c0dfa81b9a019ca37553863fb6bc62d2aaa` (Release charness 8.3.0)
Target version: `8.3.0`, tag `v8.3.0`
Record under review: `charness-artifacts/release/latest.md` (prepared, awaiting claims review)

## Observer

A file-backed Codex reviewer run through `skills/public/critique/scripts/run_review.py`
(backend `codex_exec`, boundary `read-only-worker`), attempt
`release-8-3-0-claims-2`, packet
`charness-artifacts/critique/release-8-3-0-claims-2-packet.json`
(SHA256 `79a9ab617c2061ca848883e1d04f6366b06ad246ec45adf07c35aab68999d717`, reviewed-input identity `fc6a39ae88f340fcaa04fe623fe95a4e27fb0520f0da6e36df99eb39e32da3c6`).
The worker ran in its own process and context with the packet as its only
input; the preparer (this session's Claude Code agent) wrote neither its
prompt answers nor its result. Delivery state `findings-received`, verdict
`pass`.

## What preceded this round

A first claims round (attempt `release-8-3-0-claims-1`, packet
`charness-artifacts/critique/release-8-3-0-claims-1-packet.json`) read the
first prepared record at commit `31d231b68` and delivered `block` on one
record defect, CR-1: the cited critique record said the `dependency_reuse`
field "is absent from every existing manifest" while this repository's own
`.agents/worktree-adapter.yaml` declares it. That prepared commit was reset
(unpushed, untagged), the sentence was corrected in commit `c6bcda492`, and
the release was re-prepared; this round reads the corrected bytes. Its four
advisory findings (CR-2 to CR-5) are re-examined below.

## What the round audited

The reviewer was asked to audit what the record asserts: re-derive every
figure it can, check each recorded reason against the text it cites, and
separate a record defect from an evidence-boundary gap. The packet bound the
prepared record, the packaging manifest, the Claude marketplace file, the
release adapter, the version policy, the #792 critique record the release
cites, and the shipped surfaces the bump rationale describes
(`docs/worktree-prepare.md`, `docs/agent-task-runs.md`,
`integrations/worktree/manifest.schema.json`, `.agents/worktree-adapter.yaml`).
The plugin manifests under `plugins/` are a generated, git-ignored mirror and
were not bound.

## Findings of this round

Re-derived and correct: the version figures across the bound manifests
(`8.3.0` at every version slot of `packaging/charness.json` and
`.claude-plugin/marketplace.json`); the record's release-state sentences,
which claim no push, tag, or hosted completion; the corrected critique
sentence that CR-1 blocked (the field is optional, and this repository's
manifest declares it). No reviewed sentence was demonstrated false. Five
findings are typed as evidence-boundary gaps and recorded as advisory
findings in the claims record, not repaired:

- CR2-A1 (advisory): the quality lane, gate, and probe outcomes and the four
  runtime figures are asserted by the helper's own record without raw
  receipts. The helper measured them in this prepare (`publish_release.py
  --execute` in this checkout, lane 342.0 s); the same full lane also ran
  standalone before the helper (88 passed, 0 failed, 335.4 s). This record
  does not claim the reviewer did.
- CR2-A2 (advisory): the census of four versioned surfaces plus one
  presence-only surface comes from `current_release.py`'s derivation, which
  the packet did not bind; the five visible version literals agree.
- CR2-A3 (advisory): the minor rationale's change-relative claims have no
  8.2.0 baseline in the packet. The measurement: `git diff --name-only
  v8.2.0..c2283c0df` outside `charness-artifacts/` is 29 paths; the schema
  gains one optional object, the CLI one flag, the docs two sections, and no
  `add_parser(...)` name, adapter key, or public skill was removed.
- CR2-A4 (advisory): the #792 critique record is a narrative over nine
  reviewer rounds whose packets are tracked under `charness-artifacts/critique/`
  with their SHA256s; the worker reports under `.charness/` are run state, not
  tracked. The thirty-seven tests are `tests/charness_cli/test_worktree_dependency_reuse.py`.
- CR2-A5 (advisory): previous version, branch, remote, and "release adapter did
  not change in the release delta" are not in the packet. The measurement:
  `git diff v8.2.0..c2283c0df -- .agents/release-adapter.yaml` is empty;
  `packaging/charness.json` at `v8.2.0` reads `8.2.0`.

## Verdict recorded

`pass`, with the advisory findings above. The distinct-observer
property is established by the worker's separate process and context, not by
the reviewer's agreement; what the review proves is that the record's
sentences survive contact with the bound bytes, and what it does not prove is
listed above.
