# Claims Review — charness 8.9.1 (`charness-artifacts/release/latest.md`)

Subject: prepared commit `b4058b80fef5748b307b8fa07ea58819ce9c7582`,
target version 8.9.1, tag v8.9.1. Working tree clean at audit time; the
audit ran read-only against the prepared tree.

## Verdict

**Pass with notes (accept-with-notes).** Every checkable assertion in the
prepared record survived contact with the prepared tree; no blocking
findings. The distinct observer's own bottom-line word was
PROVEN-WITH-NOTES, with notes on single-sourced quality/fresh-checkout
prose and unverified issue closeout carried below. This matches the 8.9.0
precedent (accept-with-notes with the same single-sourced-quality
advisory).

## Observer

- Kind: `separate-agent-context`.
- Signal: ephemeral `codex exec` session, `--sandbox read-only
  --ephemeral`, fresh context with no shared conversation, no repo
  mutation. What follows is that session's audit, persisted verbatim in
  substance.

## Version check

**Verified.** `packaging/charness.json`, `plugins/charness/.claude-plugin/plugin.json`,
`plugins/charness/.codex-plugin/plugin.json`, and
`.claude-plugin/marketplace.json` all contain `8.9.1`; no drift. The
Codex marketplace presence surface points at `./plugins/charness`.

## Figures check

**Verified.** The record's four versioned surfaces plus one presence-only
surface match the adapter contract (`required_release_surfaces`).

## Verification-lines check

**Honest but single-sourced (see advisories).** The record asserts quality
exit 0 in 138.7s *and* hedges with "quality unestablished: pytest-release
pending final resume". Fresh-checkout probes read "passed" in the record
with no independent `v8.9.1` probe receipt durable in the tree.

## Notes check

**Verified.** No drafted 8.9.1 notes file exists; notes generate at
publish time over the final tree. Stated plainly, not a blocker.

## Bump-rationale check

**Verified.** Present and matches a patch bump: Goal Run establishment
repair (freeze CLI, bootstrap/attempt-id/cursor documentation,
observation hint), restoring promised behavior with no invocation break.

## Issue-closeout check

**Verified with note.** The record's "pending or not requested" wording is
accurate for the tree: no 8.9.1 closeout artifact exists. Closeout of
`corca-ai/charness#821` is verified separately after publication, not
claimed here.

## Findings

1. **[Advisory]** Final `pytest-release` proof is absent at prepare time;
   the record hedges honestly and the resume's pre-push quality receipt
   closes this gap.
2. **[Advisory]** Fresh-checkout probe passage is durable only as record
   prose, not as an independent receipt.
3. **[Note]** Issue closeout remains unverified at prepare time by design.

No blocking findings. The resume's pre-push quality receipt and
post-publish readbacks are the evidence that closes the gap these notes
name.
