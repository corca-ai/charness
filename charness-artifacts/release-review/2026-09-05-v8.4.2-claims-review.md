# v8.4.2 claims review

Date: 2026-09-05
Prepared commit: `132c28f4647315a0fc8184dbb2b31dd847a0c09f` (Release charness 8.4.2)
Target version: `8.4.2`, tag `v8.4.2`
Record under review: `charness-artifacts/release/latest.md` (prepared, awaiting claims review)

## Observer

A typed host `bounded-reviewer` spawned from the preparing Grok session with
a claims-only brief (audit the record, not the code). Delivery state
`findings-received`, verdict `pass`. The preparer wrote neither the
reviewer's answers nor its result.

## What the round audited

Re-derive every figure the prepared record can be checked against: packaging
and marketplace versions, bump rationale vs `version-policy.md`, the quality
command string vs the adapter, tag/push/GitHub claims vs local git state, and
the cited 8.4.2 critique path.

## Findings of this round

Re-derived and correct: `8.4.2` at `packaging/charness.json` and both version
slots of `.claude-plugin/marketplace.json`; the record does not claim tag,
push, or a hosted GitHub release; the bump rationale matches patch in
`version-policy.md`; the cited critique file exists and binds `8.4.2`. No
reviewed sentence was demonstrated false. `#797` and `#764` are not claimed
closed in the prepared record.

Advisory evidence-boundary gaps (not repaired):

- Quality 320.0s / exit 0 is helper-measured; a gitignored local receipt
  corroborates pass but is not in the prepared commit.
- Runtime figures exist only in the helper record.
- `current_release.py` was not re-executed by the reviewer; versions were
  re-derived from files.
- Fresh-checkout "passed" has no raw receipt; the three listed commands match
  the adapter.
- Adapter-unchanged was not `git diff`'d against `v8.4.1`.
- The "two bug-class repairs" half of the bump rationale is operator prose
  citing the critique, not a helper measurement.
