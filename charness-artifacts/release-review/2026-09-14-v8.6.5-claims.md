# v8.6.5 claims review (parent narrative)

Date: 2026-09-14. Reviewer: parent agent (prepared the release lane).
Observer: distinct read-only subagent (`v865-claims-review`), verdict PASS.

## What was audited

The prepared record `charness-artifacts/release/latest.md` at prepared
commit `463d7e9b733d9f5072c78fbea24aeccfabf4f315` — its assertions only,
not code correctness (covered by the release critique rounds and the
standing suite).

## Observer findings (all hold)

1. Version claimed (8.6.5, previous 8.6.3) matches every versioned
   surface; no drift; HEAD message is `Release charness 8.6.5`.
2. Quality `exited 0 in 298.0s` matches the prepare log verbatim,
   including the pytest-release qualifier.
3. No-drift report (4 versioned + 1 presence surface) matches the log
   and an independent re-run.
4. Fresh-checkout probes `passed` matches the three declared commands.
5. Review-proof path exists and is tracked.
6. Runtime figures match the log exactly.
7. Bump rationale holds: `v8.6.4` tag is local-only, remote tags stop at
   `v8.6.3`, and no GitHub release exists for 8.6.4 or 8.6.5
   (independently verified via `ls-remote` and `gh release view`).

## Minor observation (not a failure)

The record's Release Runtime omits a fifth log timing
(`fresh_checkout_probes_after_amend: 5.459s`). Nothing asserted is
false; carry it in future records for completeness.

## Out of scope (not counted against)

The observer did not re-run the 298s quality gate; the
"unestablished" qualifier is the helper's own, faithfully quoted.

## Disposition

No unsupported numbers, figures, or verification sentences found.
Proceed to resume publication.
