# Claims review narrative — charness v8.11.1 (target version 8.11.1, prepared commit 0f81d826a443)

Reviewer: distinct subagent context (claims-review-v8111), read-only.
Preparer context: parent release session (fix + critique + closeout author).

Re-prepare note (parent re-verification): a first prepare (9a4a14226) was
abandoned unpublished after an out-of-order ledger commit broke the
prepare→scaffold→resume topology; nothing was pushed. This record was
re-prepared as 0f81d826a443 on ancestry that already contains the ledger
correction (b599d5d91). Parent re-verified for the new hash: `git show`
touches exactly the 3 release files, `git log f90c6863a..0f81d826a` is the
ledger correction plus the release-only commit, and all 5 version slots read
8.11.1. The reviewer's substance findings below transfer unchanged.

## What was checked (each claim with its source)

- Release commit scope: the prepared commit touches exactly 3 files
  (marketplace.json, latest.md, packaging/charness.json); it is a single
  release-only commit, so the "patch for lane repairs" rationale correctly
  points at the earlier real fix `755342cda`, not the release commit itself.
- Version exactness: the prepared diff moves every tracked slot 8.11.0→8.11.1
  (3 slots in packaging/charness.json, 2 in .claude-plugin/marketplace.json);
  current grep confirms all 5 read 8.11.1. `plugins/` manifests also read
  8.11.1 but are gitignored generated exports, so their absence from the
  commit is correct, not drift.
- latest.md verification honesty: it claims read-only lane exit 0 AND
  "quality unestablished: pytest-release pending" — matches
  `.charness/quality/last-release-receipt.json` <!-- reproduction-source -->
  (reproduction locator only; durable evidence is the helper-captured quality
  block). Push/publish/claims-review are all marked pending — nothing asserted
  as done that isn't.
- Issues: `gh issue view` shows 834/835/836 all OPEN with titles matching the
  lane defects; the closeout ledger's `Closes` lines are closeout intent, not
  a false "already closed" claim.
- Closeout Behavior lines: all 6 named tests exist via grep (2 in
  test_task_run_scope_mismatch_834.py, 2 in
  test_task_run_no_progress_835.py, 2 in test_task_run_scope.py).

## Advisory findings

- Tag `v8.11.1` is named in latest.md but does not exist yet — expected (push
  pending, record says so); no consumer should cite the tag as existing yet.
- Publication must still wait on the pytest-release resume — the receipt's
  `unestablished` status means the read-only pass is necessary but not
  sufficient evidence.
- Closeout Implementation line credited `test_task_run_implementation_lane.py`
  for flag override/validation although the new pins live in
  `test_task_run_no_progress_835.py` — corrected in the ledger (b599d5d91)
  before re-prepare.

## Verdict

pass — the record survives contact with what shipped; advisories recorded above.
