# Claims Review — charness 8.11.0 (`charness-artifacts/release/latest.md`)

Subject: prepared commit `91e89d4241935eb7604f10a4eb1c46b334e19327`
(short `91e89d424`), target version 8.11.0, tag v8.11.0. Working tree
clean at audit time; the audit ran read-only against the prepared tree.

## Verdict

**Pass with notes (accept-with-notes).** Every checkable assertion in
the prepared record survived contact with the prepared tree; no
blocking findings. The distinct observer's own bottom-line word was
pass-with-notes, with three evidence-durability advisories carried
below.

## Observer

- Kind: `separate-agent-context`.
- Signal: ephemeral native subagent session with a fresh context, shared
  checkout, read-only objective, no repo mutation. What follows is that
  session's audit, persisted verbatim in substance.

## Version check

**Verified.** `packaging/charness.json`, both plugin manifests, and the
marketplace manifest all contain `8.11.0`; no drift
(`current_release.py` reports `drift: []`).

## Figures check

**Verified.** Recorded surface counts match the adapter's
`required_release_surfaces` (4 versioned surfaces, 1 presence-only
surface).

## Reviewer audit (verbatim in substance)

Release claims-review audit — charness v8.11.0 (target tag `v8.11.0`),
read-only, no files modified.

### Check 1 — Release delta (pass)

`git show 91e89d424 --stat`: release commit touches exactly 3 files
(`.claude-plugin/marketplace.json`, `packaging/charness.json`,
`charness-artifacts/release/latest.md`; 27 insertions, 99 deletions).
Full range `5e7f293a..91e89d424` (base is the v8.10.0 closeout record,
child of `9014aa444` Release charness 8.10.0) contains 9 commits across
25 files: `f2259c365` reclaim + divergence wording (#833),
`dffd95ac4` empty-lane clean-tree gate (#833), `0eda44235` critique
record, `53a7a91be` issue-833 closeout, plus four migrated
managed-checkout commits (claim-boundary lessons, two task-run fixes,
one retro). This matches the stated scope.

### Check 2 — Version surfaces, no drift (pass)

`current_release.py` on the audit tree reports `drift: []`,
`git_status: []`, all surfaces at `8.11.0`:
`packaging/charness.json:5` (package version), `:26` (codex manifest
mirror), `:69` (claude manifest mirror); `.claude-plugin/marketplace.json:8`
(metadata), `:14` (plugin entry);
`plugins/charness/.claude-plugin/plugin.json:3`,
`plugins/charness/.codex-plugin/plugin.json:3`.
`codex_marketplace_source_path` is presence-only, correctly not
version-checked.

### Check 3 — Notes/prose match the delta (pass)

No `*8.11*`/`*8110*` notes draft exists under
`charness-artifacts/release/` (publish will use generated notes; the
derived-claims gate applies at publish time). The authored prose that
does exist matches the delta: `latest.md:85-87` bump rationale (minor;
divergence-refusal repair plus prune-reclaim/empty-lane/patch-equivalence
behavior, no migration) and
`charness-artifacts/critique/v8110-critique.md:5-9` scope
(`v8.10.0..HEAD`, #833 reclaim + divergence wording + four migrated
commits with gate fallout repaired) both describe exactly the commits in
the range. The `53a7a91be` closeout body
(JTBD/boundary/resolution/implementation/prevention/behavior) names the
same files that `git diff 5e7f293a..91e89d424 --stat` shows changed. No
ungrounded quantity found in the record.

### Check 4 — Record figures vs `required_release_surfaces` (pass)

`.agents/release-adapter.yaml:15-19` requires 4 surfaces
(`claude_plugin`, `codex_plugin`, `claude_marketplace_version`,
`codex_marketplace_source_path`). `latest.md:19` claims "no version drift
across 4 versioned surface(s), with 1 presence-only surface(s) not
version-checked" — exactly what `current_release.py` reports.
Figures agree.

## Advisory notes (non-blocking)

1. `latest.md:18` — quality is honestly framed as unestablished
   (`--release --read-only` exit 0; pytest-release pending final
   resume). Resume must run the full release lane before publication.
2. `latest.md:77` — "Issue closeout verification: pending or not
   requested" reads stale next to `53a7a91be`, which records the #833
   closeout inside this delta; consider letting the record reflect that
   the closeout carrier is present (behavior evidence cited therein is
   the reclaim test file).
3. Pre-publish state is as expected, not a finding: tag `v8.11.0` does
   not exist yet (only `v8.10.0`; `git describe` reads
   `v8.10.0-13-g91e89d424`), and branch/tag push plus GitHub release
   creation are recorded pending this review (`latest.md:24-25,48`).
