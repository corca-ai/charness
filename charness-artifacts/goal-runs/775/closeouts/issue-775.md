Goal Run #775 closed through `issue_tool.py goal-run-close` after exact readback.

Classification: feature
Jtbd: every channel the repository reads its own health through now reports the same truth in the same shape: the release lane runs on every push, one resolver answers script locations, no test claim depends on wall-clock time, the docs-graph gate's console matches its verdict, and a proven lesson leaves the ledger into the page that owns it.
Boundary: seven Work Items (776, 777, 778, 779, 780, 781, 782), each closed by a `Closes #N` commit on `origin/main`, each `verify-closeout` = `verified`, each with an issue-owned closeout comment bound in `charness-artifacts/goal-runs/775/final-close-proof.json`. #764 is not closed by this run: the most recent scheduled mutation run failed its baseline on one test that #782 rewrote, and only the next scheduled run may report recovery (#358). No release, tag, publish, or installed-host mutation is claimed.
Resolution Brief: `charness-artifacts/goals/2026-09-03-verification-shape-alignment.md` (frozen draft sha256 6f2f63ecd8264a4502f7feeb9884a2e2cbb5e3f288be22536fd93a2eed010898; binding sha256 ea389dd26293eb3deff01bf502073c6d242dd84bb3a1df42b245fcac729d46b0).
Implementation: `charness-artifacts/goal-runs/775/final-proof.md` names the carrier commit and comment for every child and the whole-system evidence: three lanes green in a clean clone (`782-clean-clone-lanes.md`), the hosted run read and #764's state made consistent with it, the pickup preview showing the three re-admitted classes, and the joint lesson review (`781-lesson-dispositions.md`).
Prevention: the pre-push release lane, the script-lookup, wall-clock and module-eviction form checks with empty records, the mirror preamble in both runners, the quality summary's not-run clause, retro persistence seeding tagged classes, and `lesson-graduation.md` carry the run's lessons forward; `verification-shape-mismatch` itself graduated into `docs/development.md`.
Behavior: verified — `goal-run-read` returned seven linked children all `CLOSED` before this close; the close proof binds the expected graph, the parent terminal obligation, and the role-labelled evidence artifacts by byte hash.
AI-provenance: the run was executed by an AI agent (Claude Code) with Codex lanes and Claude subagents, under operator approval for the graph, every push, every lesson disposition, and the decision to close with #764 open.
