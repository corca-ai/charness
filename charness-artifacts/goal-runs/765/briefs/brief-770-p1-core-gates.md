# Lane brief P1: `core`, `gates`, `gates_support` (#770)

Follow `charness-artifacts/goal-runs/765/briefs/brief-770-p-common.md`.
Packages in this lane, one commit each, in this order: `core` (map 2.3
"_lib core", 27 minus the four pinned), `gates_support` (map 2.4, 43),
`gates` (map 2.3, 55). `run_standing_pytest` and the `standing_pytest_*`
trio go to `gates_support`; `check_coverage_lib` goes to `gates`;
`public_skill_*_lib` go to `gates_support`.
