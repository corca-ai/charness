# Lane brief P3: `review`, `lessons`, `adapters`, `artifacts` (#770)

Follow `charness-artifacts/goal-runs/765/briefs/brief-770-p-common.md`.
Packages, one commit each: `review` (map 2.3 review incl. `hitl_*`,
`closeout_*`, `adversarial_evidence`, `render_critique_section_changed_surfaces`),
`lessons` (14, plus `build_retro_lesson_selection_index`), `adapters` (map
2.4, 31, minus the pinned `adapter_lib`; includes `quality_universes_lib`
and the `quality_bootstrap_*` family; re-point every skill script's
`import_repo_module(__file__, "scripts.quality_adapter_lib")` and the
`nose_inventory_scope_lib` loader), `artifacts` (map 2.4, 13).
