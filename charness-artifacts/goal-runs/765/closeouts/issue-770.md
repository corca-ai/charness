Closeout carrier: commit `ff71dc9a97452dcb77f30111f4d38b54f94e5cc4` on `origin/main` (`Closes #770`), Goal Run corca-ai/charness#765; `issue_tool.py verify-closeout` = `verified` against that commit.

Classification: feature
Jtbd: a maintainer can find a helper by the concept it serves instead of by grepping a flat directory, a package can be added or split without any gate silently losing its universe, and an exported script keeps its published path while still importing packaged siblings.
Boundary: scripts/ (322 renames into packages, the canonical _load_repo_runtime_bootstrap shim in every repo script), tools/ (check_bootstrap_shim_consistency, rewrite_script_preambles, snapshot_gate_universes), the gate universes in .agents/quality-gates.yaml and the quality adapter, the length gate's shim exclusion, the artifact referents gate's moved-path-referent rule, skills whose fallbacks reach into scripts/, the plugin mirror, and the tests and seeded fixtures that named flat paths. No exported path under plugins/ or skills/ changed; no gate was deleted.
Resolution Brief: charness-artifacts/goal-runs/765/briefs/map-770.md and brief-770-p0-foundation.md, brief-770-p{1,2,3,4}-*.md; the #770 Work Item body.
Implementation: map-770 section 0 recorded four stale premises before any lane ran: scripts/__init__.py already existed, import_repo_module already resolved dotted names, the real blocker was the bare sibling import on line one of 185 files plus repo_root_from_script computing parent.parent, and scripts/ already had two non-Python subdirectories. A P0 foundation lane at xhigh effort landed the shim, the bootstrap fixes, the shim-consistency and preamble-rewrite tools, and the gate-universe snapshot; four parallel package lanes (P1 to P4) moved the modules and were integrated by cherry-pick with a rename-aware conflict resolver and a cumulative rename sweep. The parent then reconciled the whole packaged tree: the length gate excludes the shim (10 lines) from the measured count, check_artifact_referents reports a path that existed at the record's last commit as moved-path-referent instead of blocking, check_bootstrap_shim_consistency takes --files for the pre-commit shape, seeded gate tests gained seed_script_closure so fixtures copy a packaged script with its import closure, the quality adapter loader walks its own tree for the quality skill's validators, and the plugin mirror was regenerated.
Prevention: tools/check_bootstrap_shim_consistency.py runs in the standing lane and fails on any repo script whose shim differs from the canonical text; tools/snapshot_gate_universes.py compares every gate's matched file set against charness-artifacts/quality/2026-09-02-gate-universes-before-770.yaml so a move that drops a file from a gate is red; the referents gate's pre-move test seeds a git history where a path existed and asserts moved-path-referent is reported, not blocking.
Behavior: verified — full standing pytest green; ./scripts/run-quality.sh --full --read-only green after regenerating the mirror with sync_root_plugin_manifests; python3 <file> --help from the repo root for the touched CLIs; tools/snapshot_gate_universes.py parity against the pre-770 snapshot (every file set identical by basename, plus the one test module split in this session); /goal #765 pickup still resolves after the moves.
Review disposition: critique not required for the closeout; the design was critiqued before the lanes ran (map-770 section 0 corrections drove the brief), and the mechanical moves are proven by gate-universe parity and the full lane.
AI-provenance: implemented by five Codex lanes and integrated, reconciled, and verified by an AI agent (Claude Code) in the Goal Run #765 session.
Goal lineage: Goal Run corca-ai/charness#765; draft sha256 129f065a28ce2c6a6a7fd7dc5f6ff2b63349b75ddf1ab62411ea4879ff8d2501; binding sha256 20fdaf7e9a3e1489a308b11041555a9249b2e826d93d951f294a565b24d161cf; Work Item scripts-packaging (#770).
