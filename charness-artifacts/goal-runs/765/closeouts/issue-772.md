Closeout carrier: commit `9b7b0115b47d827a1c7ef0bb7f43f7ec9868a710` on `origin/main` (`Closes #772`), Goal Run corca-ai/charness#765; `issue_tool.py verify-closeout` = `verified` against that commit.

Classification: feature
Jtbd: an operator can trust that what the Goal Run integrated is what a consumer installs, because the proof ran from the installed export against a repository that is not this one.
Boundary: the live install proof (`charness-artifacts/probe/2026-09-03-772-installed-consumer-proof.md` and its transcripts), the release lane run on the committed tree, and the Goal Run's closeout records under `charness-artifacts/goal-runs/765/` (child closeout bodies, the expected final graph, the parent terminal obligation). The release-only fixes landed one commit earlier (`1da602d3f`): the four `scripts/*_unavailable.py` stubs, `publish_release_runtime.print_failure_payload`, the partial-publish fixture's argv, and two test locators. No product behaviour changes for a consumer.
Resolution Brief: `charness-artifacts/goal-runs/765/briefs/map-772.md` (the executed recipe in section 3) and the #772 Work Item body.
Implementation: the recipe from map-772 section 3 was rerun verbatim against `55a1f235e` with HOME, CHARNESS_STATE_HOME, CHARNESS_CONFIG_HOME, and PATH isolated; the installed checkout resolved to the same commit; the transcripts and the two `tools/` greps are kept beside the proof. F10 (the exported-copy test seeds a narrower shape than a real install) is answered by this live proof rather than by widening the seeded test. The release lane's nine reds were all verification-shape drift: the sync fixture parses argv at import and the in-process loader gave it none; the failure payload printed to the interpreter's original stderr; the standalone probe imported four stubs that exit at import; the scaffold coverage locator and the standalone cycle test named pre-#770 lines and module paths.
Prevention: the release lane now runs green on the integrated tree, so the release-only tests are back in the evidence the parent close cites; the install proof's `exits.txt` and `tools-grep*.out` transcripts are the check a later run reruns.
Behavior: verified — `charness-artifacts/probe/2026-09-03-772-installed-consumer-proof-transcripts/exits.txt` shows every consumer command exit 0 and the exported runner refusing by name; `./scripts/run-quality.sh --release` 84 passed, 0 failed (`docs-graph-awiki` prints FAIL as an uncounted advisory), run at `3fd042d4c`; standing pytest unaffected outside the release-only marker.
Review disposition: the distinct-observer review of the export boundary and gate classification is `charness-artifacts/critique/2026-09-02-769-export-boundary.md`; this closeout adds live proof, not a new boundary, so no second review is run.
AI-provenance: proof executed, reconciled, and verified by an AI agent (Claude Code) in the Goal Run #765 session.
Goal lineage: Goal Run corca-ai/charness#765; draft sha256 129f065a28ce2c6a6a7fd7dc5f6ff2b63349b75ddf1ab62411ea4879ff8d2501; binding sha256 20fdaf7e9a3e1489a308b11041555a9249b2e826d93d951f294a565b24d161cf; Work Item integrated-closeout (#772).
