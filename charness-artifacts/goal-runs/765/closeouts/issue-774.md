Closeout carrier: commit `2681dba4efb35396238516a0223cf1d6898dd36d` on `origin/main` (`Closes #774`), Goal Run corca-ai/charness#765; `issue_tool.py verify-closeout` = `verified` against that commit.

Classification: feature
Jtbd: an operator who chose the ledger as the one lesson surface sees no second surface rebuilt on every retro, and a session-start pointer no longer needs to survive a byte comparison.
Boundary: .agents/retro-adapter.yaml, the deleted charness-artifacts/retro/recent-lessons.md, goal_run_pickup.py's lesson projection, build_retro_lesson_selection_index.py --check, the retro persistence and ledger libraries' absent-digest handling, the quality attention-state registry entry for the ledger projection, and the charness-only prose that named the digest. No change to the ledger schema, scoring, or lifecycle; no change to what a consumer may declare.
Resolution Brief: the #774 Work Item body added to Goal Run #765 by operator-approved amendment (b8ee002aa).
Implementation: one Codex lane produced every change except the two .agents files its sandbox held read-only; the parent applied those and committed in the lane worktree (0ae7121a6), then landed three follow-ups: stale retro ownership allowlist entries and the preview seam in the pickup test, the attention-state declaration rebound to recent_lessons_lib, and the selection-index payload naming the absent digest as not_configured. charness task run now grants the worktree's .agents directory to the sandbox so the next such lane can commit alone.
Prevention: a seeded consumer adapter that declares a digest keeps the byte comparison and the digest read (existing tests); the pickup test patches the preview seam and refuses a digest read when none is declared; the attention-state registry declares the not_configured state so validate-attention-state-visibility refuses a silent absence.
Behavior: verified — charness-artifacts/retro/recent-lessons.md absent; python3 skills/public/achieve/scripts/goal_run_pickup.py --repo-root . --objective "/goal #765" returns ok true with lessons.selection bounded-ledger-preview; python3 scripts/build_retro_lesson_selection_index.py --repo-root . --check exits 0 with summary_projection not_configured; 243 focused ledger, lesson, and pickup tests pass; ./scripts/run-quality.sh --full --read-only 80 passed, 0 failed on the tree before this record edit; ./scripts/check-docs.sh PASS.
Review disposition: critique not required; a reversible adapter declaration plus a projection fallback proven by the seeded-consumer test and the live pickup.
AI-provenance: implemented by a Codex lane and integrated, probed, and verified by an AI agent (Claude Code) across two Goal Run #765 sessions.
Goal lineage: Goal Run corca-ai/charness#765; draft sha256 129f065a28ce2c6a6a7fd7dc5f6ff2b63349b75ddf1ab62411ea4879ff8d2501; binding sha256 20fdaf7e9a3e1489a308b11041555a9249b2e826d93d951f294a565b24d161cf; Work Item ledger-only-lessons (#774).
