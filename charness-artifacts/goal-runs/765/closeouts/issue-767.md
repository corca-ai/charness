Closeout carrier: commit `7f4bcf835a64147624063fa5000392c732191e3a` on `origin/main` (`Closes #767`), Goal Run corca-ai/charness#765; `issue_tool.py verify-closeout` = `verified` against that commit.

Classification: feature
Jtbd: a maintainer can add a subdirectory under scripts/ without any gate silently dropping it, a shell file cannot grow unmeasured, and a script nobody references is refused instead of shipped.
Boundary: the glob-driven gates (check_code_lengths, check_python_runtime_inheritance, sample_mutation_files, helper_provenance_lib, suggest_mutation_coverage_command, inventory_gitignore_scan_hygiene, inventory_adapter_gate_design, run-quality.sh py-compile), .agents/quality-adapter.yaml review globs, the new unreferenced-script check and its lane wiring, docs/validator-timing-layers.md, and the gate tests. No file moved; no gate deleted. Also carried here: issue_goal_run_binding.py and issue_goal_run_contract.py from #773 exceeded the 360-line skill cap, which the lane's length gate surfaced; they are split cohesively into issue_goal_run_parent_body.py and issue_goal_run_capabilities.py with the Achieve loader moved into issue_local_import.py.
Resolution Brief: charness-artifacts/goals/2026-09-02-north-star-realignment.md slice 4 and the #767 Work Item body.
Implementation: a Codex lane at xhigh effort produced the WIP candidate at its time bound; the parent finished it: universe diff recorded in charness-artifacts/quality/2026-09-02-gate-universe-diff.md (every .py set identical before and after; the shell family adds 12 files), two refusal-message mismatches reconciled, three unreferenced libraries and one tests-only library deleted, and the #773 length debt cleared. Deleted scripts/atomic_write_lib.py and scripts/setup_markdown_section_lib.py (referenced only by historical artifacts; no live caller, so they guarded nothing), scripts/readme_proof_ledger_lib.py (its only reader was the README proof ledger test retired in #766), and scripts/portable_artifact_lib.py with tests/test_portable_artifact_lib.py (tests-only: the test exercised a helper no production path called, so it proved nothing a consumer runs). Tests-only disposition table: portable_artifact_lib.py, deleted. No other tests-only script remains under the reference graph.
Prevention: a seeded oversize .sh turns the length gate red and a seeded unreferenced .py turns the new check red (tests/quality_gates/test_code_length_gates.py, test_unreferenced_scripts.py); every touched gate has an out-of-glob-root refusal test in test_empty_scope_refusals.py, so the S40 zero-file pass cannot recur there.
Behavior: verified — python3 scripts/check_code_lengths.py --repo-root . passes with warnings only; python3 scripts/check_unreferenced_scripts.py --repo-root . --strict reports unreferenced 0 and tests_only 0; 162 focused gate and goal-run tests pass; ./scripts/run-quality.sh 5 passed; ./scripts/check-docs.sh PASS; /goal #765 pickup still returns verified-read after the module split.
Review disposition: critique not required; reversible gate and test change proven by seeded refusals and the recorded universe diff.
AI-provenance: implemented by a Codex lane and finished, probed, and verified by an AI agent (Claude Code) in the Goal Run #765 session.
Goal lineage: Goal Run corca-ai/charness#765; draft sha256 129f065a28ce2c6a6a7fd7dc5f6ff2b63349b75ddf1ab62411ea4879ff8d2501; binding sha256 20fdaf7e9a3e1489a308b11041555a9249b2e826d93d951f294a565b24d161cf; Work Item gate-scope-repair (#767).
