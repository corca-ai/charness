# Context reuse code disposition

Date: 2026-09-06

The parent consumed both initial blocking reviews, reproduced the dropped
evidence-mode flag through the emitted continuation command, and repaired the
four concrete findings. Their findings and counterweight dispositions remain
in the adjacent context-code observation record and typed JSON results.

The single bounded follow-up passed in 53.8s with no findings. Parent read the
full typed result and verified its packet as current before integration.
Fresh-Eye Satisfaction: worker-delivered, code review only.

- Report: `charness-artifacts/critique/workers/goal805-code-context-repair/worker-report.yaml`
- Packet SHA256: `03840bc84a1ec58d699b12e704ea3c4f619b61a5e6b2ac47a5aea16aa4bd6796`
- Input SHA256: `3f2a4f28365f89ed2d7e46f2427f92c5bd87e0725b2fe4295d03de65ecfd0383`
- Findings identity: `197ed75f5b735ae2b095b56a333882886acf947ebcd917955bbebeb3a8c25f64`

The repaired focused selection passed 111 tests in 4.13s; the preceding broader
focused selection passed 197 in 3.99s. Python lint, targeted Markdown and skill
contract checks passed. No broad release pass or publication occurred.

The subsequent changed-line gate against `e752cf5e` passed at integrated commit
`23a9eaa6`: all seven changed production pool files were mapped and covered,
with no unmapped files or targeted-mutant requirement. Its coverage producers
passed in 123.1s and 118.1s. This establishes local coverage, not publication.

## Forward consumer observations

Fresh downstream contexts used the actual exported `plugins/charness` skills,
not copied prose or instructions about the expected answer. They used bounded
local fixtures; outputs are retained in
`charness-artifacts/probe/2026-09-06-context-reuse/`.

- Retro: the explicit no-write request produced a concrete counterfactual and
  no writes despite an invalid output path and existing summary. A subsequent
  explicit persistence request repaired the adapter to the authorized local
  home, invoked planner/scaffold/persistence, and produced a validated artifact
  and summary. These are separate requests, not a first-pass durable claim.
- Debug: before choosing between server latency and client timeout, the
  consumer actually ran baseline/current controlled combinations. It recorded
  that the lower timeout sufficiently explains this simulator, while leaving
  its provenance and real-server behavior unestablished. Parent reran the
  emitted validator successfully. On a same-context equality-boundary follow-up,
  it observed `request(60, 60) -> ok`, updated only the record and left it open.
  It retained a planner read but omitted adapter resolution and scaffolding;
  this is not a claim that every bootstrap call disappeared.
- Spec: after a separate read-only context-preparation step, the consumer added
  same-target and different-target duplicate examples to the known contract,
  with no broad rediscovery or implementation edit. Its first continuation did
  not execute the skill's current-state checks. Parent explicitly clarified
  that the initial no-planner restriction did not govern the subsequent edit
  and requested the omitted checks. This is a counted intervention, not a
  complete first-pass preservation result. After clarification it ran inventory,
  relevant-signal scans and the risk planner, which returned `not-applicable`
  because no current debug artifact exists. No blocker resulted. This additional
  discovery is part of the observation's cost, not removed work.

The debug cold start still performed substantial reference/source reading,
including a mistaken shared-reference path before correcting it. The observations
are evidence of bounded behavior, not general efficiency or reliability gains.
Changed/stale/unknown cases, provider-backed state, the frozen four-arm
comparison, integrated changed-line/release proof and installed observation
remain separate acceptance obligations. No child or Goal #805 closes on this
record alone.

## Changed-context fallback observation

A further debug request changed the thin adapter's output home from
`notes/debug` to `notes/current`, changed subject to `retry-boundary`, and selected
evidence-led mode. The actual consumer resolved the changed adapter, ran the
planner with subject/mode, scaffolded in the new home, executed two identical
calls, and recorded both timeouts. Parent independently reran the emitted
evidence-led validator successfully; the prior `notes/debug/latest.md` remained
byte-identical (`5fb02e4aec1663acfcde3c2ff52ca115ddd93bbe568374120e7e0b9a2a8738a7`).
Raw new artifact and receipt are archived alongside the earlier consumer output.
This establishes the bounded routing/mode fallback, not complete evidence-led
provenance: its Report Source hashes the simulator, although the reported claim
came from the request. That hash does not bind the original report. The
validator's pass does not establish that missing semantic provenance, and no
repair of the fixture is counted as first-pass success. The fallback took
14 tool calls and roughly 2.5 minutes; it is not claimed to be cheap.
