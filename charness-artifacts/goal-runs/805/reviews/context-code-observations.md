# Context reuse implementation observations

Date: 2026-09-06

The first isolated candidate `ed9f35a38a52ef035660533d1f6fa2a11ea37c36`
timed out as WIP, with no integration-eligible receipt. The parent did not
promote that result: it retained selected edits, excluded the tautological
debug contract-summary comparisons and invented marker validator, then repaired
the actual warm bootstrap and contradictory reference instructions. New parent
verification owns the result. The original lane status remains unchanged.

The resulting scope is 17 existing files, without a new helper, mode, cache,
eligibility record, or spec reference duplication. Debug's existing planner
behavior and subject-identity tests are unchanged. The direct validator replaces
only the candidate quota; one candidate without a disconfirmer still fails.
The prior contract-check phrase follows the renamed short reflection and is not
claimed as semantic proof.

Focused validation: 197 tests passed in 3.99s (wrapper 4.8s), covering debug
artifact/planner/scaffold/persistence/subject identity, retro planner/scaffold/
auto-trigger/persistence/installed plan, and skill contract guards. An initial
fail-fast fixture hit the earlier empty-section refusal; giving the candidate
section non-bullet text retained the intended two independent violations and
the original fail-fast assertions. Python lint passed before the final small
argument cleanup; final lint/export checks follow.

Removed work is conditional: warm debug no longer unconditionally resolves,
plans and scaffolds; warm spec avoids broad discovery; short chat retro avoids
the durable planner/scaffold and catalog. Replacement cost is the core's
identity/delta and persistence selection. These are implementation changes,
not yet measured savings or successful consumer behavior. First-reader action
traces, independent code review, integrated changed-line proof, the frozen
four-arm comparison, publication and installed observation remain pending.

Separately, #811 changed-line proof passed at
`e752cf5eedffc169e85b8db3371334ac0d8d5ac4` against
`0f25bcefa0ef4fa7bec2dcbd0ea81cde2444dcae`: all six mapped production files'
changed lines covered, with no unmapped files. The producer took 24.6s; this is
not a broad release pass.

## Review findings and repairs

Both first code reviews blocked. The parent read their full typed results and
verified both input packets as current before editing. Their delivered JSON is
retained beside this record; IDs below are qualified by lens.

- Integrity F1: reproduced through the actual emitted refusal-continuation
  command and planner CLI. An evidence-led invocation resumed with
  `evidence_mode: false`. Preserve its existing flag in the refusal-command
  owner; the regression now consumes that command, the emitted scaffold and
  validator, including the Evidence Disposition section. This is a direct
  fallback consumer, not a warm-eligibility engine.
- Integrity F2: accepted the counterexample where raising a timeout supports
  both latency and configuration explanations. Core and five-steps now require
  executing the discriminating check and recording the result before choosing;
  unresolved alternatives remain unresolved. No new marker or certificate.
- Simplicity F1: accepted the remaining two-lens/catalog instructions in
  ordinary/docs planner briefs. Both now start with one concrete counterfactual
  and make the second conditional. Removed a test's obsolete `Default Pattern`
  phrase assertion, not its work-class assertions.
- Simplicity F2: accepted the metric sequencing contradiction in the edited
  planner's direct consumer. Generic claim-supporting metrics belong before
  the claim; a command whose own contract needs the persisted artifact runs
  afterward and its result must be reconciled before closeout. This is emitted
  guidance, not a new command classifier or metrics schema.

All four are Act Before Ship within the changed capability/direct-consumer
seam. No extra warm certificate or literal discriminator marker is justified
(Over-Worry). Additional performance/general-reliability claims remain Valid
but Defer. No separate Bundle Anyway work was identified.

Repaired focused selection: 111 tests passed in 4.13s (wrapper 5.9s). During
test authoring, a new test was inserted before the previous function's final
assertions; restored those assertions to their original test. This authoring
mistake and the obsolete phrase expectation were not production failures.
Python lint passed. The earlier 13-file targeted Markdown check passed; an
accidental invocation included repository-configured globs and was stopped
without a verdict after about 2.5 minutes. The corrected command uses
`--no-globs`; no broad Markdown pass is claimed.

## Forward consumer: explicit no-write retro

A fresh downstream context consumed the real exported retro SKILL, then the
consumer README describing the formatter miss. The consumer had a preexisting
summary and invalid escaping output directory. Its trace contains four read-only
command calls: SKILL plus local inventory; README; section/trigger/sibling
references; final inventory/status. It invoked no adapter, planner, scaffold,
catalog, persistence, lesson, or RCA writer. The response gave a concrete
boundary-case counterfactual and explicitly reported non-persistence; durable
memory was proposed for when writes are permitted. The files remained the three
seeded inputs. This proves this bounded choice, not every retro branch or timing
improvement. Debug's competing-cause and spec's warm continuation consumers are
being observed separately.
