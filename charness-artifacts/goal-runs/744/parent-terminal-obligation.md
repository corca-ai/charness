# Goal Run #744 Parent Terminal Obligation

The parent is not an approved Work Item and receives no child cursor. It remains the provider-owned terminal obligation for the activation-open issue #744.

Before parent close, reconcile the original typed-Rust-core umbrella honestly: preserve #743/#745/#746/#747 history, record why migration/test evidence changed the original direction, and do not claim that every repository-analysis responsibility should move to Rust. Charness owns composable capabilities; consumer repositories own their own composition and policy.

The parent closes only after all seventeen direct children have issue-owned
evidence identities and are `CLOSED`, the exact expected graph is green, the
current provider-main SHA has required CI evidence, and docs and skipped checks
are reconciled. The guarded ingress published at
`ab4b2d8b72d9450dbab32da89e4934acdf6724e8` enforces binding-bound inputs,
terminal observation metadata, idempotent recovery, and a second still-`CLOSED`
readback. The terminal recipe must use that ingress; prose or a manual fallback
cannot substitute for it.
