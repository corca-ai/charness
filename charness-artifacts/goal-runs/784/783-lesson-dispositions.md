# #783 joint lesson review, 2026-09-03

Settled lesson by lesson between the operator and the agent in conversation,
under the three questions of
`skills/shared/references/lesson-graduation.md`. Nothing below was decided by
a rule, a classifier, or commit inspection. The reason is written before the
event is applied. Same shape as `../775/781-lesson-dispositions.md`.

Drift at session start, against the #783 body's 2026-09-03 read: the ledger
still has 34 scored active lessons (47 active, budget 50), but
`durable-lesson-ledger-first` totals +1 (two legacy scalars, one
`read-but-not-applied`), not a negative; the negative group is six, not seven,
and the middle group is eighteen (fifteen at +1 with one event,
`durable-lesson-ledger-first` at +1 with three, and two at 0:
`cause-named-from-one-observation`, `executed-vs-read-field`). The order of
conversation is unchanged.

Operator rule, settled after lesson 2 ("will it really work buried between
docs?" and "docs will grow without bound"): a docs sentence carries the rule and
the mechanism's name, never the why, the counts, or the dates (those stay in
this record, the ledger, and the retros); a gate that holds the whole class
earns one table row, and a paragraph survives only for a stated gap; `docs/`
pages have a 1,500-word budget (`check_docs_length.py`, record shrinks only);
and a graduation's readback is the graduated-recurrence advisory in
`check_lesson_ledger.py` (lifecycle events record `reviewed_retros`). Applied
retroactively to lessons 1 and 2 in `c20352d45`.

Third operator rule, settled at lesson 4 ("keep with a sentence is a
contradiction"): the principle is `docs/design-north-star.md` P2/P3 (unread
prose is not a contract; principle plus worked example over rulebook), the
worked examples are the mechanisms table in `docs/development.md`, and a
graduation adds a row there or a stated gap, never a new sentence per lesson.
Applied retroactively to today's and #781's pages in the same commit: the
Disjoint Writers paragraphs lose their dates and timings, documentation-
principles loses its dated instances.

| # | lesson | disposition | settled reason | docs edit |
| --- | --- | --- | --- | --- |
| 1 | changed-line-proof-before-broad-quality | graduate | Helped: 14 of 20 encounters changed an action (slice commit → changed-line proof over `base..HEAD` → broad lane, saving a full broad rerun per uncovered line); the 4 read-but-not-applied and 1 not-consulted all predate 2026-08-18 with no recurrence since; the 1 pushed-a-wrong-action (2026-08-16) corrected the wording to "after the slice commit", because the gate reads committed lines. Operator: 동의. Before this lesson the operator asked how a docs-only exposure can be checked at all; answered by the graduated-recurrence advisory shipped one commit earlier (`check_lesson_ledger.py` names a graduated lesson that a retro outside its event's `reviewed_retros` tags again). | docs: `docs/parallel-execution.md` "Disjoint Writers" owns the order beside the lane receipt's `changed_line_gate`, names the two mechanisms (receipt gate, pre-push release lane) and the stated gap (no preamble orders proof before the broad lane, because that lane runs in consumer repos without a mutation pool); `docs/implementation-discipline.md` step 4 loses its reversed order and links. code: none new; the readback for this graduation is the pre-push hook's changed-line refusal count. |
| 2 | green-test-is-not-covered-line | graduate | Helped: all 11 encounters changed an action (2026-08-17 to 08-22), the same shape each time: a green batch left named lines unreached until the coverage read said so. The two 2026-09-03 re-tags are the lane half (subagents trusting a focused green), which the 8.0.3 lane receipt closes. Operator: 추천대로. | docs: one row in `docs/development.md`'s mechanisms table (rule, mechanism, record, cannot see); the first four-sentence version with ledger history was cut the same day under the operator's docs rule below; `parallel-execution.md` keeps the lane half; `.agents/claude-host.md` bullet stays one line plus link. code: none new; `release_changed_line_coverage.py` measures line reach and runs in the lane receipt's `changed_line_gate` and the pre-push hook, so no gap is stated. |
| 3 | detector-blind-class-unstated | keep | Helped (10 of 15 changed an action, 2026-08-16 to 08-20) but the five misses are "wrote it but wrong" (one negation instance named, an unchecked scaffold line called live), which no presence check catches; only 11 of 41 `scripts/gates/check_*.py` docstrings state a blind class today and no gate holds that. No mechanism, and a docs-only graduation would lower exposure. The mechanisms table in `docs/development.md` now asks the question structurally (a "cannot see" column, empty cell visible); whether that changes the next gate author's action is scored, not assumed. Operator: 킵 동의. | none |
| 4 | bar-recorded-as-prose | keep | Helped early (7 of 12 changed an action, 2026-08-15 to 08-21) but the three most recent encounters are misses (a structural field filled from prose, an enumerated negation list, a checked pin moved into a docstring). No mechanism can tell a bar from a description in prose, and the 8.0.3 surfaces named at session start (retention constants, `script_origin`, shrink-only records) are instances of the rule, not enforcement of it. Under the third rule a keep carries no sentence. Operator: 킵. | none |
| 5 | goal-closeout-evidence-binding | graduate | Helped: 6 of 7 changed an action (2026-08-19 to 08-21), none failed; the class stopped appearing once the dedicated close operation carried the order (index validated before provider selection, close, distinct readback, terminal metadata through the binding-aware update; failed readback is `unverified`). Operator: ok. | none: `docs/goal-lifecycle.md` "Execution And Closeout" already states the mechanism as current fact; decision_ref points there |
| 6 | proof-surface-message-drift | graduate | Helped: all 5 encounters changed an action (2026-08-21 to 08-23), two of them surfacing real defects by reading `--help` before composing a command; none since. Operator: ok. | docs: none for the command half (`documentation-principles.md` "Current state only" already states it; `check_documented_command_flags.py`, `check_documented_subcommands.py`, `check_doc_links.py` hold it); one row in `development.md`'s mechanisms table states the refusal-text half as a gap |
| 7 | agent-authored-score-role | graduate | Helped: all 4 encounters changed an action (2026-08-16 to 08-22), each authoring scores from cited actions instead of asking the operator; the rule was written nowhere. No mechanism (behavior, not form); the continuity gate the class once tripped no longer exists. Operator: ok. | `skills/public/retro/SKILL.md` ledger bullet gains one sentence (the surface read at the scoring moment); docs: none; decision_ref `docs/development.md` whose Optional records paragraph links the retro skill for the ledger |
| 9 | graduation-is-proposal | graduate-with-gap | Helped: The rule is stated as current fact on the page read at exactly the moment it applies (lesson-graduation.md names this lesson by id, and the retro skill routes there), so preview-exposure loss is nil; keeping it active only re-selects a rule the process page already carries. The mapper's owner page is not a legal decision_ref (must be docs/), and the refuter is right that docs/development.md's paragraph names no mecha Operator: 묶음 동의 (principle-fold pass). | docs/development.md mechanisms table, one row: `/ A lesson leaves the working set only by a person-settled lifecycle event whose decision_ref is a docs/ page / [record_lesson_lifecycle.py](../scripts/lessons/record_lesson_lifecycle.py) and the ledger validator |
| 10 | closeout-diagnostic-visibility | graduate-with-row | Helped: A repo-owned mechanism exists for precisely this class and shipped the day the lesson last scored; the loss it records (a suite's result dying with the foreground wrapper) cannot silently recur because the record is written to disk on every standing run regardless of how it was invoked. What is missing is that no docs/ sentence names it, so a fresh session would not know to read it back; one row in the mechanisms tab Operator: 묶음 동의 (principle-fold pass). | docs/development.md mechanisms table, one row: `/ A standing run's result outlives the caller that started it / [standing_pytest_run_record.py](../scripts/gates_support/standing_pytest_run_record.py) writes `last-run.json`; read back with `run_standing_pytest. |
| 11 | proof-surface-review-binding | graduate-with-gap | Helped: Score +2 over four events, the mechanism caught the one miss and held on the next encounter, and the page CLAUDE.md routes to before spawning any reviewer already states the rule and the mechanism as current fact, so the exposure loss is acceptable for the review-window half. The refuter is right that the two positive packet-identity anchors (08-06, 08-13) name a shape the fingerprint cannot see, but that half has no Operator: 묶음 동의 (principle-fold pass). | docs/development.md mechanisms table, one row: `/ A shared-tree review's findings apply only to the tree they were read against; a packet is minted only from frozen inputs / [reviewer_boundary_fingerprint.py](../skills/shared/scripts/reviewer_boundary_fingerpr |
| 12 | line-anchored-ledger-fields | graduate-with-row | Helped: Both halves exist: the grammar makes a wrapped value a continuation by construction, so the author no longer has to remember anything and the preview-exposure loss is acceptable (mechanism makes reading unnecessary). The refuter is wrong on call sites: check_probe_record.py is unwired, but the grammar in probe_record_lib is what the issue close floor and release closeout floors read, so it is live at the irreversible Operator: 묶음 동의 (principle-fold pass). | docs/development.md mechanisms table, one row: `/ In a probe record a field name is recognized only at column 0; an indented line continues the previous value / [probe_record_parse.py](../scripts/evidence/probe_record_parse.py), read by the issue close floor a |
| 13 | isolated-agent-base-mismatch | graduate-with-gap | Helped: The seed premise is gone for the lane channel (task run branches from HEAD and records base_sha) and the packet channel binds base_head with a real refusal, so the mapper's instinct to graduate is right, but its owner page is not a docs/ page and P4 is not the owning principle; the refuter left no argument to weigh, and the strongest one — the only encounter was on the unheld ad hoc channel — is met by stating that c Operator: 묶음 동의 (principle-fold pass). | One row in docs/development.md mechanisms table: `/ A subagent reads a named base, never whatever tree is on disk / task run receipt `base_sha` ([agent task runs](./agent-task-runs.md)); packet `reviewed_input_identity` `base_head`/`substrate_mode` ([prepare p |
| 14 | keyword-in-prose-is-a-directive | graduate-no-sentence | Helped: Both the refuter and mapper hold up under source: the owning page states the mechanism as current fact and the hook makes reading unnecessary, so preview-exposure loss is nil. The one gap in the docs sentence under the operator's rule (a sentence carries the mechanism's name) is that "the irreversible close-keyword scan" is unlinked prose; linking the phrase to the guard script adds no sentence and completes the name Operator: 묶음 동의 (principle-fold pass). | In docs/development.md line 122-123 replace "After the\nirreversible close-keyword scan," with "After the irreversible [close-keyword scan](../scripts/prepush_close_keyword_guard.py)," (link only, no new sentence); decision_ref docs/development.md. |
| 15 | parallel-coverage-runtime-collision | graduate-with-row | Never consulted: The general rule already lives verbatim at the page CLAUDE.md routes to for fan-out choice, and the specific collision is held structurally by code that every coverage producer must pass through, so dropping the preview line loses nothing at the batch-forming moment. Both mapper and refuter checked out; the mapper's only gap is that the mechanism is unnamed in development.md's cache table, which is what a row fixes.  Operator: 묶음 동의 (principle-fold pass). | docs/development.md, "Cache subtree / Writer" table, one row: `<key>/coverage/<repo-key>-<report-stem>` / [`coverage_runtime_paths()`](../scripts/mutation/mutation_sampling_lib.py), one directory per coverage report so parallel producers never share a data fil |
| 16 | branch-hidden-by-earlier-guard | graduate-no-sentence | Never consulted: The lesson's stated cost premise ("the changed-line gate names only one uncovered line per pass and N hidden branches cost N round-trips") no longer holds: the gate lists every uncovered changed line at once, so a hidden arm costs the same single round-trip as any other uncovered line, which is exactly the class already graduated as #2 and owned by the existing row. The mapper's keep leaves a never-consulted lesson i Operator: 묶음 동의 (principle-fold pass). | none; decision_ref docs/development.md (the existing "A passing test is not a covered line" row) |
| 17 | foreign-helper-command-in-hook | graduate-with-gap | Did not help: Both Sonnets overstate: the mapper claims the mechanism already holds the whole class and no edit is needed, but bootstrap-resolution.md scopes enforcement to an enumerated write/irreversible set and the seeding retro says the guard did not stop the trigger; the refuter concludes keep, but the operator's rules allow graduation with a stated gap, and the unguarded remainder (a read-only helper from a stale copy) is re Operator: 묶음 동의 (principle-fold pass). | docs/development.md mechanisms table, existing row: change `/ Inside this repo a skill script runs from the checkout / `script_origin` ([bootstrap resolution](../skills/shared/references/bootstrap-resolution.md)) /` to `/ Inside this repo a skill script runs f |
| 18 | lane-scoped-proof | graduate-no-sentence | Never consulted: The refuter examined this one and did not refute; my verification agrees. The false-stop direction is the same stated gap as the already-graduated green-test-is-not-covered-line (#783 row 2), written in the same mechanism's docstring, and the decision moment is the BLOCK verdict where the subprocess advisory already prints, so the mechanism makes a page read unnecessary and the preview-exposure loss is nil. Zero enco Operator: 묶음 동의 (principle-fold pass). | none; decision_ref docs/development.md (the existing "A passing test is not a covered line" row) |
| 19 | guard-adjacent-to-action | archive | Did not help: The vehicle every scored encounter rode on (a free-form handoff document plus its validator) is gone from the tree and operating-contract.md states there is no handoff document at all, so the recorded premise cannot recur. The lesson never changed an action even while presented at session open five times in a row, so keeping it buys no exposure; the general half ('a gate's green is a claim') is already the north star Operator: 묶음 동의 (principle-fold pass). | none |
| 20 | durable-lesson-ledger-first | archive | Helped: The premise is gone by construction: the durable ledger was built, it is the sole lifecycle state, the entry point mandates reading it, and the session-snapshot/handoff path that could have authorized a lifecycle step without it was deleted. The lesson's one positive score (legacy +2, "used the ledger-first sequence") records the transition being made, not a rule that still needs remembering. The mapper is right; not Operator: 묶음 동의 (principle-fold pass). | none |
| 21 | closeout-authoring-rework | archive | Never consulted: Every concrete instance was a rework cycle on a surface that no longer exists (handoff packet, handoff validator, unclaimed-emission gate), and the lesson never once changed an action, so there is nothing proven to promote. The general author-time idea it gestures at is already owned and mechanism-held in docs/authoring-preflight.md independent of this lesson, so archiving loses no coverage; graduating a lesson that  Operator: 묶음 동의 (principle-fold pass). | none |
| 22 | artifact-contract-late-feedback | archive | Did not help: The lesson's rule is a capability ask ("have the scaffold report its budget against a draft rather than only after a complete artifact"), and that has shipped: the scaffold states the size budget before any draft exists and the validator states the exact overage in one number, so the late-feedback premise no longer holds; neither the mapper nor the refuter opened scaffold_artifact_lib.py. The only recorded miss was h Operator: 묶음 동의 (principle-fold pass). | none |
| 23 | command-plan-premise-before-fanout | archive | Never consulted: The refuter is right and the mapper misread the page: commit 489df2c37 (2026-08-27, "Simplify execution ownership and delegation") deliberately replaced the universal "before starting a manual or parallel command fan-out, write a plan and run preflight" sentence with the narrower rule quoted above, which says the opposite of the lesson ("only for a manually assembled fan-out whose ... resolution is otherwise ambiguou Operator: 묶음 동의 (principle-fold pass). | none |
| 24 | fallback-aimed-at-production | archive | Never consulted: The mapper's archive case verifies from source: the concrete premise (a leak guard exempting one live-repo subtree) was removed with the usage-episodes surface four days after seeding, the lesson has zero encounters in 17 days, and its origin was a counterfactual rather than a scored failure. The residual general habit is ordinary test-authoring judgment on reversible work (north-star P1), which needs no ledger slot. Operator: 묶음 동의 (principle-fold pass). | none |
| 25 | unclaimed-session-disposition | archive | Never consulted: The mapper's archive case verifies: the failure this lesson names requires a receipted session and a pre-push continuity gate, and both were removed with the score-event model (the ledger is now optional and selection needs no receipt). Its tracking issue was closed as requalified the day the lesson was seeded. A class whose triggering architecture is gone cannot recur, so this is "premise no longer holds", not keep; Operator: 묶음 동의 (principle-fold pass). | none |
| 26 | wrong-path-and-target-binding | archive | Never consulted: The lesson's binding premise (nothing binds a release artifact to its target) was false when written: the dry-run refusal it cites is reviewed_input_identity.py doing its job, and the fan-out half is held by command_plan_preflight.py and owned by parallel-execution.md as current fact. The path half is a loud, session-local, immediately failing mistake squarely inside P1, with no docs owner, no recurrence in 13 days,  Operator: 묶음 동의 (principle-fold pass). | none |
| 27 | premise-not-checked-against-source | keep | Helped: Still the widest-reaching lesson in the slice and still transferring in its latest evidence, with no docs/ owner that a person reads at the moment a design, spec, or release premise is written and no mechanism that could hold it. disconfirmer-first.md covers only the debug-claim subset and is not a legal decision_ref (lesson_ledger_lib.py:358 refuses non-docs/ refs). Mapper and reasoning are right; a keep carries no  Operator: 묶음 동의 (principle-fold pass). | none |
| 28 | release-proof-identity-churn | keep | Helped: The refuter is right on both legs: the principle sentence is invented and the named mechanism binds the claims-review record, not the changed-line receipts the lone anchor is about, which were re-earned by a human reviewer's call. A graduate-with-row would cite a non-covering principle and a non-covering mechanism at an irreversible boundary, exactly the north star's failure signature. A stated-gap row is possible la Operator: 묶음 동의 (principle-fold pass). | none |
| 29 | runtime-budget-contention | keep | Helped: The mapper's graduate-no-sentence cannot be recorded: lesson_ledger_lib.py:358 refuses a graduate event whose decision_ref is not under docs/, and disconfirmer-first.md is under skills/. The refuter is also right that the page neither describes this falsifier nor was read in the one instance that produced the score. No owner page, no mechanism, one helpful encounter: keep, without a sentence. Operator: 묶음 동의 (principle-fold pass). | none |
| 30 | removal-consumer-grep-incomplete | keep | Helped: One encounter, it changed the action, and the gap it names is real and unowned: the closest tool disclaims the identifier-only blind spot in its own docstring and nothing else stands at a deletion sweep. There is no docs/ page a person opens at that moment, so a docs-only graduation would only lower exposure. Mapper and reasoning hold; keep with no sentence. Operator: 묶음 동의 (principle-fold pass). | none |
| 31 | mutation-producer-selection | keep | Helped: The premise still holds (the suggester can miss producers and D44's residual is open), nothing in docs/ holds the rule, and the one page a person reads at the moment of producer selection says nothing about it, so a graduation now would move the memory to a surface nobody opens at the decision moment. The evidence is a single positive encounter, too thin to call the class recurring or dead; the mapper's keep is right Operator: 묶음 동의 (principle-fold pass). | none |
| 32 | rule-exists-but-does-not-bind | keep | Helped: The habit (open the listed reference before declaring an outcome unrecoverable) is judgment, not form, so no gate can hold it, and no docs/ page owns it, so both halves of graduation are missing; the premise still holds, so archive is wrong. The mapper's live finding checks out: the fresh-eye reference asserts an AGENTS.md rule that the post-simplification AGENTS.md does not contain, which is a stale cross-reference  Operator: 묶음 동의 (principle-fold pass). | none |
| 33 | counted-limit-retry-loop | keep | Did not help: The refuter is right on the decisive fact: the headroom command and its authoring-preflight sentence both predate the 2026-08-16 miss, and the miss happened anyway with the lesson in view, so nothing about the current mechanisms supports the claim that they now close the class. A graduation would also remove the session-open exposure while adding nothing (the advisory never blocks), and the lesson-graduation referenc Operator: 묶음 동의 (principle-fold pass). | none |
| 34 | cause-named-from-one-observation | keep | Did not help: The mapper's principle and mechanism both exist verbatim, but there is no docs/ owner, the ledger validator refuses a graduate event pointing at a skill reference, and the marker is form-only and artifact-scoped, so it does not hold the miss half of the score history; the refuter is right on every point. Net 0 over two events is not a proven lesson, and a row in development.md would be a page nobody reads at the mome Operator: 묶음 동의 (principle-fold pass). | none |
| 35 | conservative-static-verdicts | keep | Helped: Mapper is right and there was no refutation to weigh. No docs page owns the rule as current fact, no mechanism can hold how a recommendation is phrased, and the decision moment (proposing a new static rule) has no dedicated page a person opens, so a docs-only graduation would lower exposure per lesson-graduation.md Q2. The premise (new detectors keep being proposed in this repo) still holds, so archive is not support Operator: 묶음 동의 (principle-fold pass). | none |
| 36 | evidence-channel-identity | keep | Contradicted: The only encounter is the class recurring as a miss while the lesson was in the working set, which is the strongest available evidence that nothing holds it. No owner page, no mechanism, and the decision moment (any ad hoc yaml/json read used as a fact) has no page. Archive is unsupported because the premise (repo-owned readers that differ from generic parsers) is still true. Keep. Operator: 묶음 동의 (principle-fold pass). | none |
| 37 | harness-differential-not-test | keep | Helped: Mapper correct. The debug skill is the surface read at the decision moment, but it does not carry this ordering, and the operator's third rule forbids adding a sentence for a keep; a mechanism (an environment diff in the runner) does not exist and nobody has shown judgment alone fails, so P1 says do not build one. One helped encounter, premise intact (isolation vs full-gate differentials still occur). Keep. Operator: 묶음 동의 (principle-fold pass). | none |
| 38 | premises-not-debt | keep | Helped: The refuter is right that deferred-decisions.md is a neighbor, not the owner: its contract fires when reopening a recorded deferred decision, whereas both the seeding bullet and the score anchor fire at release-goal shaping over open issues, and the achieve/release skills (the pages actually read at that moment) say nothing about it. Graduating with decision_ref to a page nobody opens when shaping a goal would lower  Operator: 묶음 동의 (principle-fold pass). | none |
| 39 | release-candidate-preflight-order | keep | Helped: The mapper is right that no page owns the release-critique consumer and nothing holds it: the release skill and critique-boundary order critique before mutation, not coverage before critique, and the planner in the 08-22 retro scoped its real-host verdict to three dirty worktree files, so the premise still holds. A graduate-with-gap would restate parallel-execution's existing gap for a different consumer on one encou Operator: 묶음 동의 (principle-fold pass). | none |
| 40 | bar-recorded-as-prose | keep | Helped: Ledger matches the map (7/1/4) and the operator settled keep in conversation today under the same three questions; nothing has changed since that settlement. The refutation-relevant fact the mapper missed is that P3 already states the rule's principle verbatim, so an owner page exists — but without a mechanism the exposure loss is real and the misses are recent, which is exactly the keep case. Keep. Operator: 묶음 동의 (principle-fold pass). | none |
| 41 | coverage-claim-exceeds-the-pin | keep | Did not help: Premise still holds (a docstring can claim more fields than a gate compares and nothing red follows), so archive is not available, and there is no owner page or mechanism, so graduation is not available either. The evidence is thin — one miss, no recurrence in 18 days of dense retros — and in practice the class rides along with detector-blind-class-unstated's audits; if it stays silent through the next review it is a Operator: 묶음 동의 (principle-fold pass). | none |
| 42 | detector-blind-class-unstated | keep | Helped: Ledger matches the map (10/1/4) and the operator settled keep today (row 3) after the same verification; the misses are presence-with-wrong-content, which no form check catches, so no mechanism and a docs-only graduation would lower exposure. Nothing changed since the settlement. Keep. Operator: 묶음 동의 (principle-fold pass). | none |
| 43 | executed-vs-read-field | keep | Did not help: The mapper's owner page and mechanism are real but cover only the release adapter's two fields; the refuter is right that the miss's actual preventer (export_self_sufficiency_lib's structural exclusion) is named on no docs page and no table row, and neither surface is read when someone reaches for a regex over config values. The general class (rewrite without naming the consumer) recurred on 2026-09-03 with the lesso Operator: 묶음 동의 (principle-fold pass). | none |
| 44 | one-engine-per-pattern | keep | Helped: The mapper's principle is generic and its owner page does not carry the rule at all, so the first graduation question (one owning page) fails; the only mechanisms hold two narrow sub-instances that predate or bypass the lesson. The refuter is right that the class is not absorbed: it recurred on 2026-09-03 under a new id, which is a sign the lesson did not transfer rather than that it graduated. Keep with no sentence; Operator: 묶음 동의 (principle-fold pass). | none |
| 45 | prose-claim-without-a-reader | keep | Helped: The refuter is right on both counts: the principle is P2/P3, and the only mechanism holds the release-record instance while the seeding instance (handoff/goal-body counts) has no reader and recurred today. A no-sentence graduation into critique-boundary.md would retire the general class into a page read only at release closeout, lowering exposure at the moment a handoff or goal body is written; that is the same reaso Operator: 묶음 동의 (principle-fold pass). | none |
| 46 | sweep-overreach | keep | Never consulted: Seeded today with no encounter, no owner page, and no mechanism, so neither graduation half exists and there is nothing to archive yet; the refuter had nothing to refute. Two cautions for the next review: the premise depends on another path migration happening, and the durable fix is the resolver the sibling `layout-oracle-duplication` class names, so this lesson may archive into that one rather than graduate on its  Operator: 묶음 동의 (principle-fold pass). | none |
| 47 | probe-stimulus-from-model-not-source | keep | Never consulted: The refuter is right and the mapper overstated: the mechanism evaluates and reports but by explicit operator ruling does not veto, so this is the stated-gap branch, not the shipped-mechanism branch. Graduating today would write a gap the operator has already chosen to hold open on a different timetable (the severity flip after slice 5), into a development.md row no one reads at close time, while the only page read at Operator: 묶음 동의 (principle-fold pass). | none |
| 48 | wrong-path-is-premise-failure | keep | Never consulted: The refuter is right: the mechanism holds a narrower class (planned fan-outs) than the lesson (any gate invocation), and the page that documents it is read when forming a fan-out, not when typing one command, so a docs-only graduation lowers exposure for the case that actually happened. Zero score events over two weeks is indistinguishable from never-consulted, not evidence a mechanism retired the class. The mapper's Operator: 묶음 동의 (principle-fold pass). | none |
| 49 | boundary-identity-unbound | keep | Never consulted: The mapper's keep is right and no refuter argued otherwise. The class is real and still surfacing (#773) on surfaces the reviewer envelope does not cover, no docs/ page owns the rule, and the one mechanism is scoped to one producer, so neither graduation half exists. Premise holds, so archive is wrong too. Operator: 묶음 동의 (principle-fold pass). | none |
| 50 | uncounted-count | keep | Never consulted: Zero score events in 17 days, but the mapper's starvation cause checks out from source: 14 active lessons sit at score_count 0 (verified from the ledger), the uncertainty bucket sorts ties by lesson_id ascending (lesson_selection_preview_lib.py line 180), and `lesson_id: uncounted-count` appears in none of seeds 1, 2, 3, 42, 100, 784, so absence of encounters is a selection artifact, not evidence the class resolved.  Operator: 묶음 동의 (principle-fold pass). | none |
| 51 | live-corpus-invariant-unnamed | keep | Never consulted: A same-day class with one concrete incident, no owning sentence, and no mechanism has nothing to graduate into and no basis to archive. Unlike the other two lessons it is actually exposed: `lesson_id: live-corpus-invariant-unnamed` appears in all six sampled preview seeds (recent bucket), so keeping it active is a real exposure, and the next session that gives a baseline or record its first writer is the encounter th Operator: 묶음 동의 (principle-fold pass). | none |

## Tally

Rows 1-7 one at a time; rows 9-51 by group after the principle-fold pass
(`783-principle-fold-map.md`), which re-judged every active lesson including
rows 3 and 4 (both keep again). Graduate 15 (1, 2, 5, 6, 7, and ten in the
fold pass), archive 8, keep 27 (25 active after events, since two keeps were
re-judged). Active after events: 25 of 50; 62 lessons, 37 lifecycle events.

The fold pass also cut `docs/`: 35 pages, 43,078 -> 31,366 gate-measured
words, four narratives moved to dated artifacts, one generator and one gate
adapter edited for the same reason (dates and duplicate contract text), one
test re-pointed at the rule's owner. `development.md` 1,929 -> under 1,000.
