# #783 principle-fold map, 2026-09-03

Two workflow passes over every active lesson (43) and every `docs/` page (35), settling nothing.
Pass 1 (Sonnet): map each lesson to the higher principle already in docs (north-star P1-P5, documentation-principles), the mechanism, and a move; map each page for passages a principle, an artifact, a gate, a docstring, or another page already owns; refute every proposed graduation. Pass 2 (session model, adversarial): re-judge every lesson from source and every proposed cut. The operator settles each lesson in the disposition record; this file is the evidence.

## Lessons

| lesson | pass 1 | pass 2 | disposition | principle / mechanism (pass 2, verified) | reason (pass 2) |
| --- | --- | --- | --- | --- | --- |
| guard-adjacent-to-action | archive | archive | did not help | docs/design-north-star.md P4: "A passing gate, a `CLOSED` state, a readback, an advisor verdict — each is a *claim*, not a conclusion." The mapper's quote also exists verbatim at docs/operating-contract.md:17-18: "There  / mech: none for the recorded shape: scripts/validate_handoff_artifact.py and skills/public/handoff/SKILL.md were deleted in commit 6bce02c58 ("Simplify goal execution  | The vehicle every scored encounter rode on (a free-form handoff document plus its validator) is gone from the tree and operating-contract.md states there is no handoff document at all, so the recorded premise cannot recur. The lesson never changed an action even while presented at session open five times in a row, so keeping it buys no exposure; the general half ('a gate's green is a claim') is al |
| premise-not-checked-against-source | keep | keep | helped | skills/public/debug/references/disconfirmer-first.md (not a docs/ page): "Before stating one of these as confirmed, name and run the single cheapest check that would falsify it:" — verbatim, but scoped to absence/attribu / mech: none. No form check can tell whether the source was read before a sentence was written; the 9 score anchors show the failure landing on prose (a reviewer's grep | Still the widest-reaching lesson in the slice and still transferring in its latest evidence, with no docs/ owner that a person reads at the moment a design, spec, or release premise is written and no mechanism that could hold it. disconfirmer-first.md covers only the debug-claim subset and is not a legal decision_ref (lesson_ledger_lib.py:358 refuses non-docs/ refs). Mapper and reasoning are right |
| release-proof-identity-churn | graduate-with-row | keep | helped | none verbatim. The mapper's quote "it reads the exact prepared commit and derives record identity" exists at skills/public/release/references/critique-boundary.md:104-105 (a skills page, not docs/); its paraphrase of P4  / mech: split, and the mapper named the wrong half. The claims-review half of the class is held: skills/public/release/scripts/claims_review_evidence.py:179 ("review_ar | The refuter is right on both legs: the principle sentence is invented and the named mechanism binds the claims-review record, not the changed-line receipts the lone anchor is about, which were re-earned by a human reviewer's call. A graduate-with-row would cite a non-covering principle and a non-covering mechanism at an irreversible boundary, exactly the north star's failure signature. A stated-ga |
| runtime-budget-contention | graduate-no-sentence | keep | helped | skills/public/debug/references/disconfirmer-first.md, verbatim: "attribution: \"caused by X\", \"this came from Y\", \"scheduled/manual did it\"" and "Before stating one of these as confirmed, name and run the single che / mech: none. skills/public/quality/scripts/runtime_budget_lib.py knows stale hotspots (STALE_HOTSPOT_SAMPLE_DAYS) but nothing about machine contention; the stop-concur | The mapper's graduate-no-sentence cannot be recorded: lesson_ledger_lib.py:358 refuses a graduate event whose decision_ref is not under docs/, and disconfirmer-first.md is under skills/. The refuter is also right that the page neither describes this falsifier nor was read in the one instance that produced the score. No owner page, no mechanism, one helpful encounter: keep, without a sentence. |
| removal-consumer-grep-incomplete | keep | keep | helped | none in docs/. The mapper's quote is verbatim (joined by an ellipsis) from the scripts/gates_support/removed_name_consumers.py docstring: "Non-claim: a reader matched here is a TEXTUAL `.<name>` in a file that also menti / mech: none. removed_name_consumers.py is advisory by its own docstring ("the answer is information, not teeth"), finds identifier-shaped `.<name>` readers only, and i | One encounter, it changed the action, and the gap it names is real and unowned: the closest tool disclaims the identifier-only blind spot in its own docstring and nothing else stands at a deletion sweep. There is no docs/ page a person opens at that moment, so a docs-only graduation would only lower exposure. Mapper and reasoning hold; keep with no sentence. |
| graduation-is-proposal | graduate-no-sentence | graduate-with-gap | helped | skills/shared/references/lesson-graduation.md (read at the decision moment): "Graduation is a **proposal settled by a person and the agent together, lesson by lesson**. No rule, score threshold, classifier, or commit ins / mech: scripts/lessons/lesson_ledger_lib.py:358-361 refuses a `graduate` event whose decision_ref is not under `docs/`; scripts/lessons/record_lesson_lifecycle.py:131- | The rule is stated as current fact on the page read at exactly the moment it applies (lesson-graduation.md names this lesson by id, and the retro skill routes there), so preview-exposure loss is nil; keeping it active only re-selects a rule the process page already carries. The mapper's owner page is not a legal decision_ref (must be docs/), and the refuter is right that docs/development.md's para |
| durable-lesson-ledger-first | archive | archive | helped | AGENTS.md:9 (CLAUDE.md is its symlink), verified verbatim: "Read the lesson ledger before planning, not after failing:" followed by the render_lesson_selection_preview.py command. docs/development.md "Optional records" s / mech: charness-artifacts/retro/lesson-ledger.json is the only state scripts/render_lesson_selection_preview.py and scripts/lessons/check_lesson_ledger.py read or writ | The premise is gone by construction: the durable ledger was built, it is the sole lifecycle state, the entry point mandates reading it, and the session-snapshot/handoff path that could have authorized a lifecycle step without it was deleted. The lesson's one positive score (legacy +2, "used the ledger-first sequence") records the transition being made, not a rule that still needs remembering. The  |
| closeout-diagnostic-visibility | keep | graduate-with-row | helped | docs/development.md "Verification and export": "[The standing pytest runner](../scripts/gates_support/run_standing_pytest.py) is the pytest owner (xdist workers, chunk sizing, an external basetemp); a bare `python3 -m py / mech: scripts/gates_support/standing_pytest_run_record.py (split from the runner in S6, 2026-08-15, commit 54654b032): "How a standing pytest run OUTLIVES the caller  | A repo-owned mechanism exists for precisely this class and shipped the day the lesson last scored; the loss it records (a suite's result dying with the foreground wrapper) cannot silently recur because the record is written to disk on every standing run regardless of how it was invoked. What is missing is that no docs/ sentence names it, so a fresh session would not know to read it back; one row i |
| closeout-authoring-rework | archive | archive | never consulted | docs/authoring-preflight.md "Artifact-shape preflight", verified verbatim: "It generalizes the same author-time idea to the recurring "authoring-preflight skip" class (issues 284 → 308 → 325 → 329 → 332 → 334): an author / mech: scripts/gates/check_artifact_surface_preflight.py (--type/--emit-stub/--path gives the shape before drafting; blocking `check-artifact-shape (staged)` in the co | Every concrete instance was a rework cycle on a surface that no longer exists (handoff packet, handoff validator, unclaimed-emission gate), and the lesson never once changed an action, so there is nothing proven to promote. The general author-time idea it gestures at is already owned and mechanism-held in docs/authoring-preflight.md independent of this lesson, so archiving loses no coverage; gradu |
| mutation-producer-selection | keep | keep | helped | none. skills/public/quality/references/mutation-testing.md (the page read during mutation work) never mentions suggest_mutation_coverage_command.py or per-refusal producer selection; docs/deferred-decisions.md D44 ("Resi / mech: Partial: scripts/mutation/suggest_mutation_coverage_command.py's HELP_EPILOG (statuses `partial`/`missing`/`blocked`, and step 3 "For partial, inspect unmapped_ | The premise still holds (the suggester can miss producers and D44's residual is open), nothing in docs/ holds the rule, and the one page a person reads at the moment of producer selection says nothing about it, so a graduation now would move the memory to a surface nobody opens at the decision moment. The evidence is a single positive encounter, too thin to call the class recurring or dead; the ma |
| proof-surface-review-binding | graduate-no-sentence | graduate-with-gap | helped | docs/design-north-star.md P4, verified verbatim at lines 65-67: "Confirm it by re-examination that uses a **different evidence channel and a different observer** than the claim under review; never by re-reading the same  / mech: skills/shared/scripts/reviewer_boundary_fingerprint.py: snapshot/verify bound by --window-id, exit 1 on undeclared drift; opened, and it refuses exactly the -2  | Score +2 over four events, the mechanism caught the one miss and held on the next encounter, and the page CLAUDE.md routes to before spawning any reviewer already states the rule and the mechanism as current fact, so the exposure loss is acceptable for the review-window half. The refuter is right that the two positive packet-identity anchors (08-06, 08-13) name a shape the fingerprint cannot see,  |
| rule-exists-but-does-not-bind | keep | keep | helped | docs/design-north-star.md P2, verified verbatim at line 52-53: "Because displaced overflow goes unread, and unread prose is not a contract." This is the principle behind the class (a listed-but-unopened reference does no / mech: none for the general class. The one scored instance (named spawn stranding a packet) has skills/shared/scripts/reviewer_result.py and the unnamed-spawn rule in  | The habit (open the listed reference before declaring an outcome unrecoverable) is judgment, not form, so no gate can hold it, and no docs/ page owns it, so both halves of graduation are missing; the premise still holds, so archive is wrong. The mapper's live finding checks out: the fresh-eye reference asserts an AGENTS.md rule that the post-simplification AGENTS.md does not contain, which is a st |
| counted-limit-retry-loop | graduate-no-sentence | keep | did not help | docs/authoring-preflight.md "Length headroom", verified verbatim: "Before a large addition to a `scripts/*.py` or skill-helper file, check how much room is left" and "It prints `limit − current` (tokei Python code lines) / mech: scripts/gates/check_code_lengths.py --headroom prints the number but never refuses anything (git -S shows it shipped 2026-05-30, 5aacd0d5f, before the only scor | The refuter is right on the decisive fact: the headroom command and its authoring-preflight sentence both predate the 2026-08-16 miss, and the miss happened anyway with the lesson in view, so nothing about the current mechanisms supports the claim that they now close the class. A graduation would also remove the session-open exposure while adding nothing (the advisory never blocks), and the lesson |
| artifact-contract-late-feedback | graduate-with-gap | archive | did not help | docs/deferred-decisions.md D28, verified verbatim: "should `emit_payload_main` in [scaffold_artifact_lib.py] grow a `--write` mode so scaffold-first becomes the path of least resistance?" and "Trigger checked 2026-08-01: / mech: scripts/core/scaffold_artifact_lib.py:122-128, opened: the scaffold payload now carries `size_budget` "so a run writes-to-fit up front instead of writing long a | The lesson's rule is a capability ask ("have the scaffold report its budget against a draft rather than only after a complete artifact"), and that has shipped: the scaffold states the size budget before any draft exists and the validator states the exact overage in one number, so the late-feedback premise no longer holds; neither the mapper nor the refuter opened scaffold_artifact_lib.py. The only |
| cause-named-from-one-observation | graduate-no-sentence | keep | did not help | skills/public/debug/references/disconfirmer-first.md, verified verbatim: "Before stating one of these as confirmed, name and run the single cheapest check that would falsify it" ... "attribution: \"caused by X\"" ... "Fo / mech: scripts/gates/validate_debug_artifact.py validate_falsifiable_hypothesis_marker (lines 228-260), opened: requires a non-empty `disconfirmer:` line in a debug ar | The mapper's principle and mechanism both exist verbatim, but there is no docs/ owner, the ledger validator refuses a graduate event pointing at a skill reference, and the marker is form-only and artifact-scoped, so it does not hold the miss half of the score history; the refuter is right on every point. Net 0 over two events is not a proven lesson, and a row in development.md would be a page nobo |
| conservative-static-verdicts | keep | keep | helped | docs/design-north-star.md P1, verbatim: "Where a mistake stays inside this session's editable state, the default is a short principle plus the agent's judgment; a gate/validator/rule here bears the burden of showing why  / mech: none; opened docs/development.md mechanisms table and grepped docs/ for no-increase/baseline/reporting-only: nothing refuses a detector proposal that frames a b | Mapper is right and there was no refutation to weigh. No docs page owns the rule as current fact, no mechanism can hold how a recommendation is phrased, and the decision moment (proposing a new static rule) has no dedicated page a person opens, so a docs-only graduation would lower exposure per lesson-graduation.md Q2. The premise (new detectors keep being proposed in this repo) still holds, so ar |
| evidence-channel-identity | keep | keep | contradicted | none. docs/design-north-star.md P4 ("Confirm it by re-examination that uses a different evidence channel and a different observer than the claim under review") is about a second channel at an irreversible boundary, not a / mech: none; scripts/adapter_lib.py defines the repo's load_yaml reader but nothing checks that a claim about a file was produced through that reader | The only encounter is the class recurring as a miss while the lesson was in the working set, which is the strongest available evidence that nothing holds it. No owner page, no mechanism, and the decision moment (any ad hoc yaml/json read used as a fact) has no page. Archive is unsupported because the premise (repo-owned readers that differ from generic parsers) is still true. Keep. |
| harness-differential-not-test | keep | keep | helped | none. skills/public/debug/SKILL.md step 4 reads verbatim "isolate the smallest input, path, or environment that still fails" and step 5 "include environment, dependency, state, and control-flow causes alongside obvious l / mech: none; the debug skill's sibling-search/collection-bisection section addresses xdist pollution, a different cause, and no runner diffs GIT_CONFIG_PARAMETERS/HOME | Mapper correct. The debug skill is the surface read at the decision moment, but it does not carry this ordering, and the operator's third rule forbids adding a sentence for a keep; a mechanism (an environment diff in the runner) does not exist and nobody has shown judgment alone fails, so P1 says do not build one. One helped encounter, premise intact (isolation vs full-gate differentials still occ |
| line-anchored-ledger-fields | graduate-with-row | graduate-with-row | helped | none stated in docs/; the owning surface under the operator's third rule is the mechanisms table in docs/development.md ("a rule is held by a mechanism or it is prose", design-north-star P3), which today has no row for r / mech: scripts/evidence/probe_record_parse.py _FIELD_RE (comment verbatim: "at column 0 it is a field, indented it is a continuation, and nothing has to arbitrate"), c | Both halves exist: the grammar makes a wrapped value a continuation by construction, so the author no longer has to remember anything and the preview-exposure loss is acceptable (mechanism makes reading unnecessary). The refuter is wrong on call sites: check_probe_record.py is unwired, but the grammar in probe_record_lib is what the issue close floor and release closeout floors read, so it is live |
| premises-not-debt | graduate-no-sentence | keep | helped | docs/deferred-decisions.md Named Remedy Premise Contract, verbatim: "A remedy recorded in a deferred decision is a hypothesis, not an implementation plan. Before shaping work around a named remedy, the next resolver must / mech: none, and the same page states it (verbatim: "This is a review convention, not a new mechanical blocking floor"); nothing in scripts/gates, hooks, or the achiev | The refuter is right that deferred-decisions.md is a neighbor, not the owner: its contract fires when reopening a recorded deferred decision, whereas both the seeding bullet and the score anchor fire at release-goal shaping over open issues, and the achieve/release skills (the pages actually read at that moment) say nothing about it. Graduating with decision_ref to a page nobody opens when shaping |
| release-candidate-preflight-order | keep | keep | helped | docs/design-north-star.md P4: "A passing gate, a `CLOSED` state, a readback, an advisor verdict — each is a *claim*, not a conclusion." and docs/parallel-execution.md#disjoint-writers: "the order has no gate, because `ru / mech: none — opened skills/public/release/SKILL.md (workflow steps 2-4 say plan, resolve blockers, run critique; no ordering of frozen-diff coverage before critique), | The mapper is right that no page owns the release-critique consumer and nothing holds it: the release skill and critique-boundary order critique before mutation, not coverage before critique, and the planner in the 08-22 retro scoped its real-host verdict to three dirty worktree files, so the premise still holds. A graduate-with-gap would restate parallel-execution's existing gap for a different c |
| bar-recorded-as-prose | keep | keep | helped | docs/design-north-star.md P3: "This repo's worked examples are the mechanisms table in [development](./development.md#verification-and-export): a rule is held by a mechanism or it is prose." This sentence owns the rule's / mech: none — no gate can tell a bar recorded as a structural value from prose describing the same fact; the 8.0.3 surfaces (retention constants, script_origin, shrink | Ledger matches the map (7/1/4) and the operator settled keep in conversation today under the same three questions; nothing has changed since that settlement. The refutation-relevant fact the mapper missed is that P3 already states the rule's principle verbatim, so an owner page exists — but without a mechanism the exposure loss is real and the misses are recent, which is exactly the keep case. Kee |
| coverage-claim-exceeds-the-pin | keep | keep | did not help | docs/development.md#verification-and-export: "The mechanism's docstring owns the exact form and what it cannot see; this table does not repeat it." This puts the coverage claim in the docstring but does not say the claim / mech: none — grepped scripts/gates for baseline_check / residual figure / labels_key: nothing current; check_documented_command_flags.py and check_documented_subcomma | Premise still holds (a docstring can claim more fields than a gate compares and nothing red follows), so archive is not available, and there is no owner page or mechanism, so graduation is not available either. The evidence is thin — one miss, no recurrence in 18 days of dense retros — and in practice the class rides along with detector-blind-class-unstated's audits; if it stays silent through the |
| detector-blind-class-unstated | keep | keep | helped | docs/development.md#verification-and-export: "Each rule below names the mechanism that holds it. The mechanism's docstring owns the exact form and what it cannot see; this table does not repeat it." The page asks the que / mech: none — no gate in scripts/gates requires a blind-class docstring or checks its accuracy; the development.md sentence is a prompt read when a row is added, not w | Ledger matches the map (10/1/4) and the operator settled keep today (row 3) after the same verification; the misses are presence-with-wrong-content, which no form check catches, so no mechanism and a docs-only graduation would lower exposure. Nothing changed since the settlement. Keep. |
| isolated-agent-base-mismatch | graduate-no-sentence | graduate-with-gap | helped | docs/agent-task-runs.md "Run": "The command creates the `task/feature-lane` branch from `HEAD`, derives the task id and linked worktree under the external task runtime, routes cache and logs outside the repo, and records / mech: two, each holding one channel: `charness task run` branches the lane from HEAD and binds `base_sha` into the receipt's `content_digest` (docs/agent-task-runs.md | The seed premise is gone for the lane channel (task run branches from HEAD and records base_sha) and the packet channel binds base_head with a real refusal, so the mapper's instinct to graduate is right, but its owner page is not a docs/ page and P4 is not the owning principle; the refuter left no argument to weigh, and the strongest one — the only encounter was on the unheld ad hoc channel — is m |
| executed-vs-read-field | graduate-no-sentence | keep | did not help | skills/public/release/references/adapter-contract.md "Executed fields": "`sync_command` and `quality_command` are RUN by a subprocess; every other field in this contract is READ." — verified verbatim, but it owns only th / mech: skills/public/release/scripts/resolve_adapter.py `EXECUTED_COMMAND_FIELDS` warning and skills/public/release/scripts/bump_version.py refusal (missing script pat | The mapper's owner page and mechanism are real but cover only the release adapter's two fields; the refuter is right that the miss's actual preventer (export_self_sufficiency_lib's structural exclusion) is named on no docs page and no table row, and neither surface is read when someone reaches for a regex over config values. The general class (rewrite without naming the consumer) recurred on 2026- |
| one-engine-per-pattern | graduate-no-sentence | keep | helped | docs/design-north-star.md P1 quote exists verbatim ("Where a mistake stays inside this session's editable state, the default is a short principle plus the agent's judgment; a gate/validator/rule here bears the burden of  / mech: scripts/adapters/surfaces_lib.py `_check_surface_idiom` raises `SurfaceError` for a `<dir>/**/*.X` pattern without its `<dir>/*.X` sibling — it refuses the seed | The mapper's principle is generic and its owner page does not carry the rule at all, so the first graduation question (one owning page) fails; the only mechanisms hold two narrow sub-instances that predate or bypass the lesson. The refuter is right that the class is not absorbed: it recurred on 2026-09-03 under a new id, which is a sign the lesson did not transfer rather than that it graduated. Ke |
| keyword-in-prose-is-a-directive | graduate-no-sentence | graduate-no-sentence | helped | docs/development.md "Pushing": "After the irreversible close-keyword scan, a push touching anything beyond docs and artifacts runs `./scripts/run-quality.sh --full --read-only --release`" — verified verbatim (lines 122-1 / mech: .githooks/pre-push runs `python3 scripts/prepush_close_keyword_guard.py --repo-root ... --remote ...` first, unconditionally, before the receipt shortcut and th | Both the refuter and mapper hold up under source: the owning page states the mechanism as current fact and the hook makes reading unnecessary, so preview-exposure loss is nil. The one gap in the docs sentence under the operator's rule (a sentence carries the mechanism's name) is that "the irreversible close-keyword scan" is unlinked prose; linking the phrase to the guard script adds no sentence an |
| prose-claim-without-a-reader | graduate-no-sentence | keep | helped | docs/design-north-star.md P2 ("*Because displaced overflow goes unread, and unread prose is not a contract.*") and P3 ("a rule is held by a mechanism or it is prose") — verified; this is the principle the operator fixed  / mech: skills/public/release/scripts/claims_review_schema.py (`charness.release.claims-review.v4`) plus claims_review_evidence.py refusals at publish (record sentinel, | The refuter is right on both counts: the principle is P2/P3, and the only mechanism holds the release-record instance while the seeding instance (handoff/goal-body counts) has no reader and recurred today. A no-sentence graduation into critique-boundary.md would retire the general class into a page read only at release closeout, lowering exposure at the moment a handoff or goal body is written; th |
| sweep-overreach | keep | keep | never consulted | none — no docs/ page owns a rename-sweep exclusion rule; the nearest is the development.md row "A repo script's location is answered only by `repo_layout.py`" (scripts/gates/check_script_lookup_form.py), which constrains / mech: none — grep for a sweep exclusion set in scripts/ finds only scripts/review/reviewed_input_identity.py's unrelated auto-sweep exclusions; the five 2026-09-03 re | Seeded today with no encounter, no owner page, and no mechanism, so neither graduation half exists and there is nothing to archive yet; the refuter had nothing to refute. Two cautions for the next review: the premise depends on another path migration happening, and the durable fix is the resolver the sibling `layout-oracle-duplication` class names, so this lesson may archive into that one rather t |
| parallel-coverage-runtime-collision | graduate-with-row | graduate-with-row | never consulted | docs/parallel-execution.md "Disjoint Writers": "Concurrent writers must not share a file or a single-writer surface. Partition the fan-out into disjoint path sets when the work allows it, and take isolated checkouts when / mech: scripts/mutation/mutation_sampling_lib.py `coverage_runtime_paths()` — namespaces the coverage db, rcfile, and sitecustomize dir under `<runtime-root>/coverage/ | The general rule already lives verbatim at the page CLAUDE.md routes to for fan-out choice, and the specific collision is held structurally by code that every coverage producer must pass through, so dropping the preview line loses nothing at the batch-forming moment. Both mapper and refuter checked out; the mapper's only gap is that the mechanism is unnamed in development.md's cache table, which i |
| probe-stimulus-from-model-not-source | graduate-with-row | keep | never consulted | skills/public/issue/references/closeout-discipline.md (a skill reference, not a docs/ page): "a `status: verified` / `CLOSED` state and a passing carrier are *claims* — the tracker flipped, the close keyword carried — ** / mech: skills/public/issue/scripts/issue_probe_record_floor.py — `PROBE_RECORD_SEVERITY = "review"` by dated operator ruling (2026-08-18, pending slice-5 cost data), s | The refuter is right and the mapper overstated: the mechanism evaluates and reports but by explicit operator ruling does not veto, so this is the stated-gap branch, not the shipped-mechanism branch. Graduating today would write a gap the operator has already chosen to hold open on a different timetable (the severity flip after slice 5), into a development.md row no one reads at close time, while t |
| wrong-path-is-premise-failure | graduate-no-sentence | keep | never consulted | docs/design-north-star.md P3 exists as quoted with elision ("Prefer one sharp worked example plus the principle behind it, so the reader derives the next case." and "a rule is held by a mechanism or it is prose."), but i / mech: scripts/gates_support/command_plan_inputs.py resolves plan targets from `rg --files` and refuses `target-mismatch`, `rg-unavailable`, `rg-files-failed` — but on | The refuter is right: the mechanism holds a narrower class (planned fan-outs) than the lesson (any gate invocation), and the page that documents it is read when forming a fan-out, not when typing one command, so a docs-only graduation lowers exposure for the case that actually happened. Zero score events over two weeks is indistinguishable from never-consulted, not evidence a mechanism retired the |
| boundary-identity-unbound | keep | keep | never consulted | docs/design-north-star.md P4: "A passing gate, a `CLOSED` state, a readback, an advisor verdict — each is a *claim*, not a conclusion. Confirm it by re-examination that uses a **different evidence channel and a different / mech: skills/shared/references/fresh-eye-subagent-review.md runner boundary: "The receipt must carry the attempt, scope, packet/input, mode/backend, prompt/schema, an | The mapper's keep is right and no refuter argued otherwise. The class is real and still surfacing (#773) on surfaces the reviewer envelope does not cover, no docs/ page owns the rule, and the one mechanism is scoped to one producer, so neither graduation half exists. Premise holds, so archive is wrong too. |
| branch-hidden-by-earlier-guard | keep | graduate-no-sentence | never consulted | docs/design-north-star.md P1, verbatim as the mapper quoted: "Where a mistake stays inside this session's editable state, the default is a short principle plus the agent's judgment; a gate/validator/rule here bears the b / mech: scripts/mutation/release_changed_line_coverage.py — "a mapped changed pool file has uncovered changed lines (the blocker)"; it refuses every uncovered changed l | The lesson's stated cost premise ("the changed-line gate names only one uncovered line per pass and N hidden branches cost N round-trips") no longer holds: the gate lists every uncovered changed line at once, so a hidden arm costs the same single round-trip as any other uncovered line, which is exactly the class already graduated as #2 and owned by the existing row. The mapper's keep leaves a neve |
| command-plan-premise-before-fanout | graduate-no-sentence | archive | never consulted | docs/parallel-execution.md "Command Fan-Out Preflight": "`charness task run` already owns its lane's base, branch, path, scope, and worktree preflight. Do not create a second plan for that path. Use `command_plan_preflig / mech: scripts/gates_support/command_plan_preflight.py refuses a missing or ambiguous target when it is run; nothing forces it before a manual fan-out, and the lane pa | The refuter is right and the mapper misread the page: commit 489df2c37 (2026-08-27, "Simplify execution ownership and delegation") deliberately replaced the universal "before starting a manual or parallel command fan-out, write a plan and run preflight" sentence with the narrower rule quoted above, which says the opposite of the lesson ("only for a manually assembled fan-out whose ... resolution i |
| fallback-aimed-at-production | archive | archive | never consulted | none — no docs/ page or north-star facet owns "trace where the remaining fallback lands when a test removes an input"; I searched docs/, tests/conftest.py, scripts/mutation and scripts/core for a leak guard with an exemp / mech: none. The subtree the seeding retro names (`.charness/usage-episodes/sessions/`) belonged to the usage-episodes feature deleted whole in 7f482ded7 (2026-08-25:  | The mapper's archive case verifies from source: the concrete premise (a leak guard exempting one live-repo subtree) was removed with the usage-episodes surface four days after seeding, the lesson has zero encounters in 17 days, and its origin was a counterfactual rather than a scored failure. The residual general habit is ordinary test-authoring judgment on reversible work (north-star P1), which n |
| foreign-helper-command-in-hook | graduate-no-sentence | graduate-with-gap | did not help | skills/shared/references/bootstrap-resolution.md (owner, linked from the docs/development.md mechanisms-table row "Inside this repo a skill script runs from the checkout / `script_origin`"): "The installed copy is still  / mech: scripts/core/helper_provenance_lib.py `require_repo_local_helper` (callers: record_lesson_score.py, record_lesson_lifecycle.py, seed_lesson_transitions.py, rece | Both Sonnets overstate: the mapper claims the mechanism already holds the whole class and no edit is needed, but bootstrap-resolution.md scopes enforcement to an enumerated write/irreversible set and the seeding retro says the guard did not stop the trigger; the refuter concludes keep, but the operator's rules allow graduation with a stated gap, and the unguarded remainder (a read-only helper from |
| lane-scoped-proof | graduate-no-sentence | graduate-no-sentence | never consulted | docs/development.md lines 75-76, verbatim: "Each rule below names the mechanism that holds it. The mechanism's docstring owns the exact form and what it cannot see; this table does not repeat it." governing the existing  / mech: scripts/mutation/release_changed_line_coverage.py — module docstring states this exact blind class: "Coverage collected from a SUBSET of the suite is a subset o | The refuter examined this one and did not refute; my verification agrees. The false-stop direction is the same stated gap as the already-graduated green-test-is-not-covered-line (#783 row 2), written in the same mechanism's docstring, and the decision moment is the BLOCK verdict where the subprocess advisory already prints, so the mechanism makes a page read unnecessary and the preview-exposure lo |
| unclaimed-session-disposition | archive | archive | never consulted | docs/development.md "Optional records", verbatim: "Use [artifact policy](./artifact-policy.md) for durable evidence and [retro](../skills/public/retro/SKILL.md) for the optional lesson ledger; neither is a session-start  / mech: none, by design: scripts/lessons/ (16 files listed) has no session-declare, receipt, or continuity-gate code; check_lesson_ledger.py prints only the unseeded-cl | The mapper's archive case verifies: the failure this lesson names requires a receipted session and a pre-push continuity gate, and both were removed with the score-event model (the ledger is now optional and selection needs no receipt). Its tracking issue was closed as requalified the day the lesson was seeded. A class whose triggering architecture is gone cannot recur, so this is "premise no long |
| uncounted-count | keep | keep | never consulted | none. The mapper's P1 quote is verbatim in docs/design-north-star.md ("Where a mistake stays inside this session's editable state, the default is a short principle plus the agent's judgment; a gate/validator/rule here be / mech: none. docs/development.md's mechanisms table (rows 80-92) has no row asking whether a count was enumerated; nothing under scripts/gates or scripts/hooks reads a | Zero score events in 17 days, but the mapper's starvation cause checks out from source: 14 active lessons sit at score_count 0 (verified from the ledger), the uncertainty bucket sorts ties by lesson_id ascending (lesson_selection_preview_lib.py line 180), and `lesson_id: uncounted-count` appears in none of seeds 1, 2, 3, 42, 100, 784, so absence of encounters is a selection artifact, not evidence  |
| wrong-path-and-target-binding | archive | archive | never consulted | docs/design-north-star.md P1, verbatim: "Where a mistake stays inside this session's editable state, the default is a short principle plus the agent's judgment; a gate/validator/rule here bears the burden of showing why  / mech: scripts/gates_support/command_plan_preflight.py, docstring lines 13-14 verbatim: "Missing or ambiguous targets are refusals, not permission to guess a sibling p | The lesson's binding premise (nothing binds a release artifact to its target) was false when written: the dry-run refusal it cites is reviewed_input_identity.py doing its job, and the fan-out half is held by command_plan_preflight.py and owned by parallel-execution.md as current fact. The path half is a loud, session-local, immediately failing mistake squarely inside P1, with no docs owner, no rec |
| live-corpus-invariant-unnamed | keep | keep | never consulted | none. The mapper's principle_quote is empty and correctly so: docs/development.md's testing paragraph (lines 94-102) covers in-process loading, FIFO waits, and bisection, and says nothing about deriving a live-artifact e / mech: none. scripts/lessons/check_lesson_ledger.py validates the ledger and prints the graduated-recurrence advisory; nothing in scripts/gates, scripts/hooks, or the  | A same-day class with one concrete incident, no owning sentence, and no mechanism has nothing to graduate into and no basis to archive. Unlike the other two lessons it is actually exposed: `lesson_id: live-corpus-invariant-unnamed` appears in all six sampled preview seeds (recent bucket), so keeping it active is a real exposure, and the next session that gives a baseline or record its first writer |

## Docs pages

| page | words | approved cut | missed by pass 1 | rejected | structure |
| --- | --- | --- | --- | --- | --- |
| docs/agent-task-runs.md | 892 | 0 | 0 | 0 | none — agreed. Every paragraph is a current fact about `task run`/`task status` output fields (carrier_kind, content_digest, changed_line_gate, retention) that no docstring or other page carries; 892 words is under budget; no dated evidence, issue citations, or how-to for a refused move found. |
| docs/artifact-policy.md | 1869 | 482 | 60 | 0 | Agree with the split: 'Goal Draft And Execution Lineage' is a second copy of goal-lifecycle.md's owned contract (lines 85-137 opened) and should leave this page as one link plus the `goal_lineage.py` disposition mention above; goal-lifecycle.md needs no new prose because it already states the frozen |
| docs/authoring-preflight.md | 2781 | 382 | 144 | 0 | none for this pass — agreed that it is one checklist per gated surface. But the mapper's budget claim is wrong: the dated cuts total roughly 400 words, leaving the page near 2,300 against a 1,000 budget (`check_docs_length.py` WORD_BUDGET = 1000, opened). The remaining excess is because the page cov |
| docs/capability-resolution.md | 815 | 335 | 87 | 0 | Agree: keep Current Slice, Fixed Decisions, Command Surface, File Shape. Disagree on 'move to a charness-artifacts/spec artifact': the Problem/Success Criteria/Acceptance Checks are superseded by the shipped CLI and its tests, and documentation-principles says superseded material with no durable val |
| docs/cli-reference.md | 291 | 70 | 12 | 2 | none — agreed; the generated per-command sections are the page, and the only hand-authored prose is the intro template in render_cli_reference.py, which is where every cut above lands. |
| docs/control-plane.md | 1466 | 45 | 35 | 0 | none — agreed; one question (the `charness tool` manifest/lock/support contract), distinct from capability-resolution.md. |
| docs/deferred-decisions.md | 10949 | 5623 | 400 | 4 | Agree with the direction, with two corrections. (1) A settled entry collapses to Question + outcome + the mechanism's name + reopen trigger + one artifact link — not to a bare one-liner; every mapper replacement for D34/D35/D36/D40/D42/D43/D44/D48 dropped the reopen trigger or a stated gap, and the  |
| docs/design-north-star.md | 1450 | 41 | 55 | 0 | none — agreed. Sole owner of the one idea and P1-P5; Taste is already marked separate-in-kind in-page (118-126), so no split. The page remains over the 1,000-word budget (docs-length-baseline.json, 1450) even after both cuts; that is a shrink-only record, so the cuts are still worth taking. |
| docs/development.md | 996 | 113 | 0 | 0 | none — agreed. The mapper's note that Mutation phase barriers should merge into implementation-discipline.md is half right: the concurrency half already lives in parallel-execution.md Disjoint Writers, which is where the pointer belongs; implementation-discipline gains nothing. Line 137-138 (#778 cl |
| docs/docs-graph-checks.md | 1839 | 1075 | 110 | 0 | none — agreed, one page one question. With the four approved cuts plus the two missed ones the page loses roughly 1,100 of 1,839 words and should drop under the 1,000-word budget, which is a prompt to remove it from docs-length-baseline.json (check_docs_length.py: "a recorded page now under budget i |
| docs/documentation-principles.md | 480 | 33 | 0 | 0 | none — agreed. Note for the operator record, not the page: line 61's 1,000-word budget matches check_docs_length.py (`WORD_BUDGET = 1000`) and docs-length-baseline.json (`"budget": 1000`); the 1,500 figure in charness-artifacts/goal-runs/784/783-lesson-dispositions.md line 23 is the wrong one. |
| docs/export-boundary.md | 168 | 0 | 0 | 1 | none — agreed; the page is 168 words and the tools/-exclusion probe is owned nowhere else. |
| docs/external-integrations.md | 1283 | 142 | 30 | 0 | merge — agreed. The section collapses to one line plus the owner link; gather-provider-ownership.md is the single owner. The missed cut at 9-12 is required by the same merge: as written it names Slack as a gather provider path, which the owner page (line 14-15, "is **not** a gather source") and the  |
| docs/gather-provider-ownership.md | 926 | 313 | 70 | 0 | none — agreed. This page becomes the sole owner once external-integrations.md 49-69 collapses to a link. Reference Implementation Rule (62-82) stays here until a second consumer needs it. The 141-143 fix is not a cut but the operator rule (a docs sentence names its mechanism): the gate exists as tes |
| docs/goal-lifecycle.md | 2646 | 381 | 4 | 0 | Agree: no split. Both stale sections are planning-status history that goes to charness-artifacts (goal-runs/724 already holds it); the lifecycle question stays one page. After these cuts the page is still ~2,250 words against the 1,000-word budget; the next candidates are the Parent Metadata field l |
| docs/harness-composition.md | 1093 | 33 | 34 | 0 | Agree: none. Ownership map plus precedence/assembly order is one boundary-map question. |
| docs/host-packaging.md | 1619 | 215 | 70 | 0 | Reject the split. README.md is the minimal user guide by documentation-principles ('Keep the entry surfaces small: README.md is the minimal user guide') and README line 23 already routes to this page's Repo-Root Install Surface section for the detail, so moving the operator install narrative into RE |
| docs/implementation-discipline.md | 687 | 236 | 222 | 0 | Agree with the substance, not the label: this is a trim, not a page merge. operating-contract.md stays the sole owner of worktree, verification, external-change, and capability-failure rules; this page keeps the six-step default loop plus the authoring-affordance paragraph and links out. After the c |
| docs/index.md | 347 | 10 | 0 | 0 | Agree: none. A flat routing map is the page's one question. |
| docs/narrative-announcement-boundary.md | 280 | 163 | 0 | 0 | Agree with the trim, not with a split: nothing moves to a second docs page. What remains (orientation sentence, Fixed Decisions, Non-Goals, ~110 words) is the current boundary and stays here; the spec-phase content is deleted or written once to charness-artifacts/spec as noted in the 7-20 cut. |
| docs/operating-contract.md | 745 | 37 | 0 | 0 | Agree: none. This page is the sole owner of the rules implementation-discipline.md restates; keep it and cut the restatements there. |
| docs/operator-acceptance.md | 989 | 288 | 140 | 0 | Agree with the mapper's direction and go one step further: the page is framed on a Goal Run that is already CLOSED (#765), so 'Remaining Items' is not current state. Items 1 and 4 are done (owners opened above); item 2's suggested runs are on-demand regression per retro.md; item 3 is either done or  |
| docs/operator-progressive-path.md | 231 | 42 | 10 | 0 | none — agree: one owning question (operating path by horizon), distinct from workflow-routes and development. |
| docs/parallel-execution.md | 1166 | 145 | 16 | 0 | Reject the split and the fold. Cost-Aware Model Selection is the portable principle; .agents/claude-host.md ('Delegation and model policy') and .agents/codex-host.md ('Model and effort presets') hold only the host-specific mappings, so folding the principle into both adapters would create two copies |
| docs/prescribed-skill-closeout-contract.md | 234 | 44 | 12 | 0 | none — agree: single narrow question. |
| docs/proof-semantics-adapter.md | 774 | 191 | 39 | 0 | none — agree. The schema and query-function sections are owned here: scripts/adapters/proof_semantics_adapter_lib.py's docstring says 'Schema, resolution, degradation, and the three closeout-blocking conditions are documented in docs/proof-semantics-adapter.md'. Page is under budget (984), so any ad |
| docs/provenance-placement.md | 510 | 176 | 59 | 0 | none — agree. The page is in `.agents/quality-adapter.yaml` `standing_docs` (line 791), so it is gate-scanned; the portable reference declares itself the shipped copy of this policy, so the pair is one owner plus a declared export, not drift. |
| docs/public-skill-dogfood.md | 198 | 20 | 18 | 0 | none — agree. |
| docs/public-skill-validation.md | 1470 | 615 | 95 | 0 | Reject the split as stated; approve the collapse. After the approved cuts the page is ~600 words and owns two related questions that share one reader (the person adding or reclassifying a public skill): bucket membership (thin narrative over the JSON) and what proof a prompt-affecting change needs.  |
| docs/runtime-capability-contract.md | 824 | 435 | 45 | 0 | Approve the merge direction with a correction: do not merge this page into another. After the approved cuts it still owns one question nothing else does — what a public skill, adapter, or manifest may say and store about capability access (Access Modes 41-57, Secrets Rule 59-93, Skill Boundary 95-11 |
| docs/support-skill-policy.md | 732 | 155 | 45 | 0 | None — agree. One question (ship / consume upstream / fork / integration-only), and after the cuts nothing on it restates another page. |
| docs/surface-driven-adapter-triggers.md | 617 | 205 | 10 | 0 | None — agree, including keeping 'Required fixture shape' as a stated gap. |
| docs/validator-timing-layers.md | 763 | 220 | 12 | 0 | None — agree. The generated block is owned by .agents/quality-gates.yaml plus the renderer and is held by `check-timing-layer-completeness` (run-quality.sh, opened); manual cuts do not apply to it. |
| docs/workflow-routes.md | 595 | 125 | 0 | 0 | None — agree; after the cuts the page is pure routing. |
| docs/worktree-prepare.md | 1349 | 215 | 37 | 0 | None — agree. Quickstart (a block of commands to type), the 'When to run' sections, the manifest contract notes, and the canonical checks table are all decision-moment content and stay. |

## Approved cuts, per page

### docs/artifact-policy.md

- L11-24 (111w) "Long-running work has one planning artifact and one provider execution identity." -> ## Goal lineage

Goal Draft/Binding identity, the `goal_lineage` record, and the parent cursor are owned by [goal-lifecycle.md](./goal-lifecycle.md); evidence dispositions (`goal-bound`, `planning-onl [docs/goal-lifecycle.md lines 85-137 (opened) own frozen draft + SHA-256, immutable V1 binding, approved work items, and the mutable progress cursor; the disposi]
- L216-251 (169w) "The repo currently intends these families to be history-default:" -> Delete the three hand lists; the `inventory_current_pointer_layouts.py` command already shown at line 202 is the live per-family class. [I ran `python3 scripts/artifacts/inventory_current_pointer_layouts.py --repo-root .`: it reports `capability-catalog` and `ideation` as `artifact_class: null` (]
- L252-268 (108w) "These are intentional exceptions to the simple defaults:" -> delete [Each bullet restates a line already on this page: `spec` outside the latest.md flow is line 246; `capability-catalog` current-pointer-first is lines 234-235; `h]
- L315-328 (94w) "Avoid these mistakes: storing secrets or copied credentials in any artifact" -> delete [Every bullet is the negation of a rule stated once already: secrets — capability-resolution.md Fixed Decisions lines 56-57 (opened); `latest.md` as default — li]
- L209-214 (60w) "Current repo examples:" -> delete; the inventory command at line 202 reports every family's live pointer layout (symlink, regular file, rolling, missing, adapter-unmanaged), so a hand-picked dated example (`debug-2026-04-20-pro [missed by pass 1]

### docs/authoring-preflight.md

- L9-12 (6w) "attention-state vocabulary, single-file length headroom, and string-matching edg" -> drop "(#308)" [issue provenance is dated history; the 2026-06 goal and retro records under charness-artifacts/goals/ and charness-artifacts/retro/ carry the issue chain, and t]
- L24-28 (20w) "That split is what ended the #302 `"silently-skipped"` docstring detour and its " -> "The token-shaped reading also catches two real states a substring scan cannot see, spelled with a different separator (`advisory_only_no_cli_surface`, `not-configured`)." [the #302 detour is dated history (retros/ledger); the surviving sentence keeps the current capability fact (what the validator can see that a substring scan can]
- L60-67 (13w) "appending — that avoids the unplanned mid-slice extraction" -> "If a file is near its limit, start a new module instead of appending. The advisory never blocks; the length gate is the hard floor." [the `acquire_public_url.py` extraction during #302 is a dated instance; the rule and the mechanism (`check_code_lengths.py --headroom`, the length gate) stand w]
- L76-81 (12w) "but fails the headroom buffer — the trap that bit `achieve/SKILL.md` during the " -> end the sentence at "fails the headroom buffer." [dated instance; the two numbers (160 ceiling, 4-line buffer) and the gate that holds them are stated in the same paragraph and at lines 95-100.]
- L116-121 (33w) "skip" class (issues 284 → 308 → 325 → 329 → 332 → 334): an author should learn a" -> "It generalizes the same author-time idea: an author should learn an artifact's required shape at author time, not by failing the broad gate." [the issue chain and the 334 instance are counts and history; the lesson ledger and the 2026-06 retros hold them.]
- L236-242 (66w) "Why the split exists: 13 shipped commands accumulated undetected because a" -> delete the paragraph [the docstring of tools/inventory_skill_script_references.py (opened, lines 1-25) owns this exactly: the thirteen accumulated references, the three overlapping s]
- L311-329 (192w) "Multi-checkout posture (#343, decided 2026-06-10): charness installs **one" -> "One logical hook per machine, deduped by script basename across checkouts; the commit-time sweep covers edits in a checkout the surviving hook does not. A moved or deleted checkout's leftover is flag [decision history lives in charness-artifacts/critique/2026-06-10-343-host-hook-lifecycle-critique.md and 2026-06-10-settings-scan-slice-critique.md plus the 202]
- L393-399 (40w) "broad scanning regexes accept inputs you did not intend (the #305" -> "When a check matches a version, identifier, or other token by string content, broad scanning regexes accept inputs you did not intend. Before shipping a string/regex check, walk this list:" [the #305 rewrite is a dated instance; the bullets that follow (402-410) are the rule, including the exact date/`v`-prefix edges the parenthetical cites.]
- L51 (4w) "entirely (the #302 over-correction). Prefer the wording" -> drop "(the #302 over-correction)" [missed by pass 1]
- L132-141 (60w) "This is not merely a doc: the dispatcher's `--changed-artifacts` mode is wired" -> "`--changed-artifacts` runs as the blocking `check-artifact-shape (staged)` member of [staged_commit_gate_plan.py](../scripts/staged_commit_gate_plan.py) for critique/ideation/retro; the adapter-scope [missed by pass 1]
- L180-187 (45w) "The rules mode is what makes this surface match its sibling" -> "The rules are rendered, never restated: each line is the owning validator's own constant, or the verdict that validator returns when the preflight probes it with a sample." — the 'without it an autho [missed by pass 1]
- L384-386 (35w) "This stays REVIEW, not BLOCK, on purpose: a hard "every removed line must reappe" -> drop the sentence; tools/check_skill_cut_safety.py's docstring (opened, lines 9-27) already states that reference-home gaps are REVIEW-only, why blocking them would forbid the prune cure, and the `--s [missed by pass 1]

### docs/capability-resolution.md

- L7-24 (112w) "`charness` already models provider capability metadata for external tools and" -> "Capability resolution maps a skill-facing logical capability id to a repo-local provider profile, and that to a provider `charness` already models, without storing secrets in committed files." [the Problem section is spec rationale whose premise is stale ("does not yet give each repo one local place" — the commands and files below exist). No charness-a]
- L85-102 (138w) "- `charness capability resolve <logical-id>` reads" -> delete [each 'should' criterion is now stated as current fact by the generated `charness capability` --help text in docs/cli-reference.md (opened): init scaffolds both ]
- L104-116 (85w) "- run `charness capability init --target-repo-root <repo>` against a fresh" -> delete the section; move the one worked output example into File Shape: "With that profile, `charness capability env github.default` prints `export GH_TOKEN=\"${GH_TOKEN_ACME_DEV}\"` and never the val [construction-time QA steps held by tests/charness_cli/test_capability_resolution.py; the last bullet ('run repo validators and standing tests after syncing') is]
- L68-74 (30w) "## Non-Goals" -> delete; lines 42-49 ('This slice does not add') already list the non-goals as current fact. [missed by pass 1]
- L76-83 (45w) "## Constraints" -> delete; the runtime-dependency constraint is Fixed Decision line 64, env-alias consumption is the `env` command, and 'durable docs must explain the separation' is met by the File Shape section itself. [missed by pass 1]
- L118-121 (12w) "## Canonical Artifact" -> delete; the header line 4 `Source of truth` already says this. [missed by pass 1]

### docs/cli-reference.md

- L10 (55w) "Default operational responses are compact summaries: aggregate tool operations r" -> in scripts/gates_support/render_cli_reference.py line 128 (the real edit target), keep the first sentence and replace the rest with: "Default responses are compact; `--detail` returns the full per-ite [docs/control-plane.md lines 64-71 (opened) state the compact-summary, attention-ids, and `--detail` rule; 'This replaces the former aggregate `results` payload']
- L11 (15w) "Task runs persist one atomic typed result in the external runtime; task status r" -> in render_cli_reference.py line 129, keep only: "Human-readable summaries print the affordance line with the `NEXT:` prefix." [docs/agent-task-runs.md lines 48-49 and 115-116 (opened) own the single persisted result and that status reads exactly that store.]
- L12 (12w) "Regenerate it with `python3 scripts/gates_support/render_cli_reference.py --repo" -> delete from the template (render_cli_reference.py ~line 130); line 1's GENERATED comment already carries the command, and `--output` defaults to `docs/cli-reference.md` (render_cli_reference.py:153). [missed by pass 1]
- REJECTED L10 "Operational command payloads, including structured command f": grep over README.md, docs/*.md, and .agents/*.md finds no other statement of the stdout-YAML / stderr split; control-plane.md line 64 says only 'stdout should stay structured'. It is a current fact an
- REJECTED L11 "Human-readable summaries print the affordance line with the ": the `NEXT:` prefix is stated nowhere else in docs (grep), and a consumer parsing human output needs it; the mapper's 'delete' would drop the only copy.

### docs/control-plane.md

- L57-58 (3w) "## Command Responsibilities" -> delete the empty heading [it has no body and no `###` children — the next heading is a sibling `## Agent-Readable State` — so removing it removes nothing.]
- L242-251 (42w) "The first manifest wave should cover:" -> delete the section; add one bullet to Non-Goals: "- evaluator-specific products and consumer-owned behavior evaluators" ['first wave' is history: integrations/tools/ now holds 16 manifests (agent-browser, awiki, defuddle, github-gh, gitleaks, glow, lychee, nose, repograph, ruff, s]
- L34-35 (10w) "Support skills are now always materialized. `reference` is no longer a sync stra" -> "Support skills are always materialized." — 'now' and 'no longer' describe a removed option; integrations/tools/manifest.schema.json (line 22's named schema) carries no strategy enum, and the two `sup [missed by pass 1]
- L70-71 (25w) "this replaces the former aggregate `results` payload; automation that needs indi" -> delete the bullet; 'former' is history and the preceding bullets (65-69) already say default output omits per-tool records and `--detail` returns them. [missed by pass 1]

### docs/deferred-decisions.md

- L205-214 (300w) "Template-First Fill Guards And Report-All For Sibling Artifact Validators —" -> Keep Question (207). Replace 208-213 with: "- Current choice: one-pass is resolved — `--fail-fast` is the family's only control, declared once in `add_one_pass_args`, and the critique/debug/retro/idea [the ~8-validator-round measurement and the operator reversal of `a930cc5f` are held by charness-artifacts/retro/2026-07-26-lesson-recurrence-mechanism.md (cited]
- L223-270 (440w) "dup-ratchet id-rotation affordance (gate auto-downgrade) — RESOLVED (2026-06-27," -> "### D30. dup-ratchet id-rotation — RESOLVED\n\nNewness is keyed on a content fingerprint (`nose_fingerprint_lib`, algo v2; baseline schema v3), so a pure line-shift produces no 'new' family; a member [charness-artifacts/spec/boy-scout-dup-ratchet.md (exists; cited twice on the page) owns Slice 4 and the S4-Defer entries; skills/public/quality/references/dup-r]
- L271-277 (130w) "Announcement delivery `confirmed` accepts a same-observer self-attestation" -> "### D34. Same-observer `confirmed` on announcement delivery — DECLINED\n\n`record_announcement.py` enforces the presence of a typed verification record, not its independence (operator, 2026-07-04; [p [charness-artifacts/critique/2026-07-04-pr419-adversarial-fix-closeout.md (exists) holds the adversarial-verification finding and the independent verifier's read]
- L278-284 (110w) "Release distinct-channel probe shape-match is loose against same-proxy commands" -> "### D35. Release distinct-channel probe shape-match — DECLINED\n\n`_probe_matches_release_view_shape` in `publish_release_post_create.py` matches leading tokens only; the default HTTPS-fetch channel  [same artifact owner as D34; reopen trigger retained.]
- L285-315 (290w) "Surface the question/decision-needed exemption on the commit-msg close carrier" -> "### D36. Exemption advisory on the commit-msg close carrier — RESOLVED\n\n`review_advisory_for_classification` has one carrier-neutral owner, [issue_closeout_rung1_floors.py](../skills/public/issue/s [charness-artifacts/critique/2026-07-04-d36-close-exemption-advisory-single-source.md (exists) owns the refactor, the dup-ratchet collateral, and the fresh-eye v]
- L344-359 (120w) "Reopened by its own trigger and resolved, 2026-09-03 (#781). Three" -> "- Resolved 2026-09-03: the promotion contract is [lesson-graduation.md](../skills/shared/references/lesson-graduation.md); the first joint review is [781-lesson-dispositions.md](../charness-artifacts [lesson-graduation.md (opened) owns the contract; 781-lesson-dispositions.md holds the counts (fifteen settled, nine/four/two); the stated residual and its mecha]
- L369-393 (700w) "Changed-line proof has one release-final owner" -> "### D40. Changed-line proof has one release-final owner\n\n- Question: (keep 378)\n- Current choice: the release-final lane owns the blocking proof — `run-quality.sh --release` runs [release_changed_ [development.md mechanisms table row 83 and the Pushing section (opened) state the current owner; the eight-instance chain, CI run ids, #469 misattribution, and ]
- L398 (90w) "[test_degradation_branch_coverage.py](../tests/test_degradation_branch_coverage." -> "- Why now: `suggest_mutation_coverage_command.py` maps tests by dotted module name, so a bare `import <stem>` (which works at runtime because a conftest puts `scripts/` on `sys.path`) matches none of [the specific instance (which test, which two lines, dogfooding on its own slice) is dated evidence; the retros of that period (charness-artifacts/retro/2026-08-]
- L403-411 (260w) "Should a claim-time proof gate that established nothing exit 3" -> Keep Question (405). Replace 406-408 with: "- Current choice: exit 0; the verdict carries `range_established` and the hedge prints to stderr on both range and conclusion. Operator-confirmed 2026-07-30 [charness-artifacts/goals/2026-07-29-close-the-armed-changed-line-pre-push-lane-s-known-holes-pin.md (opened by grep, lines 330 and 377) records the exit-3 quest]
- L412-420 (320w) "Should the loose-bar advisory be per-label rather than one global" -> Keep Question (414). Replace 415-417 with: "- Current choice: `BUDGET_SLACK_FACTOR` stays global; the advisory divides by `max_recent_elapsed_ms`, so a bar sized from a documented range cost rather th [the count correction (three → one), the #503 pytest retune numbers, and the rejected aarch64 bar are dated counts held by charness-artifacts/goals/2026-07-29-cl]
- L421-433 (700w) "`blocking_targets` should name a blocked line's subprocess-only coverage path —" -> "### D44. Per-line subprocess-only path in `blocking_targets` — DECLINED (2026-08-01)\n\n- Question: (keep 423 without the retro chain)\n- Current choice: declined; the premise ('reached only via subp [subprocess_only_coverage_advisory.py's docstring (opened) carries the coverage.process_startup mechanism, the purpose-built control, and the confounded first at]
- L442-456 (200w) "Named remedy's premise, ANSWERED 2026-08-01 (by reading, before any S31" -> delete; the five-field Named Remedy Premise block at 457-463 already records remedy, premise, evidence channel, observation, delta, and `falsified`. [the block is this page's own contract shape (lines 40-48) and carries the same finding; the prose above it restates the block with the reading narrative. Histor]
- L492-504 (190w) "Withdrawn, do not retry — the named better repair cannot be" -> delete; the Named Remedy Premise block at 505-511 (status: withdrawn) already carries the remedy, why it cannot both impose a marker rule and spare the cited reviews, and the delta. [duplicate of the five-field block immediately below; the 2026-08-01 corpus figures are in charness-artifacts/probe/2026-08-01-inventory-marker-rule.json.]
- L512-522 (60w) "Immutable executed measurement (captured 2026-08-12). The headline figures are" -> "- Measurement: the immutable [2026-08-12 snapshot](../charness-artifacts/probe/2026-08-12-inventory-marker-rule-snapshot.json), hash-pinned by [test_inventory_marker_rule_measurement.py](../tests/tes [the four headline counts and the SHA-256 are inside the snapshot the sentence links; the test pins the hash.]
- L523-528 (52w) "Historical live-pin maintenance (superseded 2026-08-12). The 2026-08-07 and" -> delete [self-declared superseded; the old probe's `_provenance` and #596 hold the diagnosis.]
- L529-544 (221w) "The first executed number was WRONG, and how it was wrong is the point" -> delete [the entry itself says both runs are recorded in the probe's `_provenance` (charness-artifacts/probe/2026-08-01-inventory-marker-rule.json); the unit-swap lesson]
- L583-611 (330w) "as posed ("should absence be drift by default?") is answered no. But the defect " -> "- Current choice: declared-only, default empty (operator, 2026-08-01). `current_release.py` reports `absence_corroboration` and `undeclared_absent_surfaces`; `publish_release_preflight.release_surfac [the first-cut mistakes, round-1/round-2 findings, and the marketplace-file reasoning are review history held by charness-artifacts/goals/2026-08-01-get-the-oper]
- L624-632 (140w) "Withdrawn, do not retry: the "derive the expected set from the repo's own sync" -> delete; the Named Remedy Premise block at 633-639 (status: withdrawn) carries the same three observations. [duplicate of the five-field block immediately below.]
- L640-651 (170w) "Why now: found while closing sweep row S35 on 2026-08-01. The repair is an insta" -> delete Why-now and Why-deferral; keep Non-claims (652-654), Impact surfaces (655-658), and Reopen trigger (659-661). [the S35 sweep-row origin and the D45 cross-reference are history in charness-artifacts/audit/2026-07-28-evidence-surface-triage-sweep.md; the 'better repair' na]
- L665-737 (650w) "Should `<plugin-dir>/` get a real user, or a bootstrap variable," -> "### D50. `<plugin-dir>/` — RESOLVED (2026-08-04)\n\nAdopted for the trigger the deferral named (#479's `<repo-root>/skills/public/...` family needs the installed layout). `<plugin-dir>/` is the only  [the decision is closed and the mechanism (`plugin-refs` in scripts/native_gate_lib.py:70, opened) is what a reader needs; the shim-count argument, the `$SKILL_D]
- L763-793 (150w) "Should a per-gate runtime budget measure the gate's own work rather than contend" -> Keep the heading. "- Question: the budget grades recorded wall-clock (`elapsed_ms`, verdict on `median_recent_elapsed_ms`) of a concurrently queued command, so it measures contention, not gate work. S [the `check-seed-fixture-budget` 19x example and the #668 numbers (12% CPU → 0.6% wall) are held by charness-artifacts/impl/2026-08-27-issue-668-runtime-budget-a]
- L316-343 (300w) "### D38. Promotion gate for decaying retro lessons" -> Since the entry is now resolved (344-359), keep only the Question (318-319) and one line: "The single instance was fixed first: the reviewer result-delivery rule lives in fresh-eye-subagent-review.md  [missed by pass 1]
- L663-664 (30w) "## Next Action Contract" -> delete this heading and lines 738-740 ("After these closures, the next major workstream is reducing the remaining consumer friction..."); it is a stale session next-step, not a decision, and D50/D51/D [missed by pass 1]
- L200 (40w) "(slice 6 critique, agentId `a28af53807ad5aef1`, F1+F3 confirmed Over-Worry)" -> drop the parenthetical, the `~50KB`/`~350x` figures, and the '#230 Waste 2' citation from D27's Current choice and Why now; the filter's existence and its reopen trigger are the current facts, the cou [missed by pass 1]
- L748-752 (30w) "The local release quality gate measured 168.19 seconds and the pre-push gate mea" -> "The v3.2.0 closeout was run as a manually split branch-push → CI readback → tag → independent readback sequence; its measured gate runtimes are in the v3.2.0 release record under charness-artifacts/r [missed by pass 1]
- REJECTED L457-464 "- Named remedy premise:
  - Remedy: move the exemption decla": the mapper's range 442-464 swallows the five-field Named Remedy Premise block and D45's Reopen trigger; the block is the page's own record shape (lines 40-48) and the trigger is the decision-moment fa
- REJECTED L618-623 "Known gap, not closed: [`publish_release_resume.py`] reaches": the mapper's 574-662 replacement drops a stated gap on an irreversible boundary (resume reaches publish with no surface check). A stated gap is exactly what documentation-principles and lesson-graduat
- REJECTED L612-617 "Not warned earlier, and it cannot be: `plan_release_run` run": where the gate sits (immediately after sync, so the refusal lands after the version bump has rewritten the worktree) is a current fact about the release flow that a release operator meets at the momen
- REJECTED L394-402 "The coverage mapper cannot see a bare top-level import of a ": as a whole-entry cut: D41 is still deferred, and its Current choice (dotted-import convention), the blind-spot statement (which no docstring carries — suggest_mutation_coverage_command.py's docstring

### docs/design-north-star.md

- L128-132 (41w) "Adopted from Tasteful software (gathered copy), where taste is explicitly not in" -> Adopted from [Tasteful software](https://wiki.g15e.com/pages/Tasteful%20software.md) ([gathered copy](../charness-artifacts/gather/2026-08-10-wiki-g15e-com-pages-tasteful-software-md-536ebc23.md)). [(b)/(d): the paraphrase of the source's own framing lives in the gathered copy at charness-artifacts/gather/2026-08-10-wiki-g15e-com-pages-tasteful-software-md-]
- L107-110 (55w) "What this does **not** license is a gate that checks gates. That is the anti-pat" -> This does not license a gate that checks gates (the Diagnosis anti-pattern applied to itself); the teeth here are a distinct observer and a scope a verdict must name. [missed by pass 1]

### docs/development.md

- L68-73 (70w) "Verify in the shape production uses: after an integration step the only green th" -> After an integration step the only green that counts is the standing runner followed by the full read-only lane; any other green is a proxy ([P4](./design-north-star.md)). The slice order (commit, cha [(a): the enumerated proxy list restates design-north-star.md P4, opened and quoted: "A passing gate, a CLOSED state, a readback, an advisor verdict — each is a ]
- L153-163 (43w) "## Mutation phase barriers — Keep state-changing work and verification in separa" -> Delete the section. Fold one line into "Verification and export" after line 117: "`mutate -> sync -> verify -> publish` stays serial in the parent; the single-writer surfaces and who runs them are own [(d): docs/parallel-execution.md lines 127-130, opened, own the serial rule verbatim ("So `mutate -> sync -> verify -> publish` (implementation discipline) stays]

### docs/docs-graph-checks.md

- L51-66 (181w) "This reverses a decision this page used to record and no longer contains — that " -> 2. **`link_only_lines` is gated too, at a ratchet above zero.** The gate judges a named metric against a bar, not awiki's exit code, which bundles every rule and cannot be selected down; the bar is th [(b)/(e): the reversal history is held by the retraction comment in tests/test_docs_graph_gate.py lines 185-197 (opened: "RETRACTED: `test_link_only_lines_alone_]
- L67-77 (134w) "Measured 2026-08-09, roughly three-fifths of the findings were this repo's own 8" -> 3. **The residual under that bar is hard-wrapped prose.** `awiki lint -root docs -recursive` prints the current count and it moves with every docs edit; recount rather than trust a number here. A refl [(b): dated shares (2026-08-09 three-fifths, 2026-08-15 two-thirds) are measurements. The retros hold the 2026-08-15 session (charness-artifacts/retro/2026-08-15]
- L86-127 (500w) "That sentence lived only in prose, which made the cheapest repair for a red lane" -> The bar may only ever decrease. [check_docs_graph.py](../scripts/gates/check_docs_graph.py) reads the last row of the table under this heading through `ratchet_rows`; an absent, unreadable, or rising  [(e)/(b): the ratchet_rows docstring in scripts/gates/check_docs_graph.py lines 182-208 (opened) carries the whole story — one parser, monotonicity enforced in t]
- L128-156 (260w) "### How this repo's founding bar was measured — Moved here from a comment in the" -> Delete the subsection; keep the table (157-159) directly under the `## The `link_only_lines` ratchet record` H2, whose founding-row `why it moved` cell already states the split in one sentence. Move t [(b): counts and a dated measurement. No charness-artifacts file holds the 255/88/167 derivation today (grep across charness-artifacts for `link_only_lines=255`,]
- L22-24 (40w) "Neither is a superset of the other, which is why the docs-graph question stayed " -> Neither is a superset of the other. [missed by pass 1]
- L45-50 (70w) "Before the docs index hub existed, seven pages were unreachable while check_doc_" -> 1. **Nothing else answers the connectivity question.** `orphans` and `islands` are barred at zero; [check_doc_links.py](../scripts/gates/check_doc_links.py) stayed correctly green while pages were unr [missed by pass 1]

### docs/documentation-principles.md

- L46-49 (33w) "scripts/check-docs.sh is the canonical document lint. It composes Markdown synta" -> [`scripts/check-docs.sh`](../scripts/check-docs.sh) is the canonical document lint; its header names the checks it composes, and quality and pre-push read its one receipt. [(e): the scripts/check-docs.sh header (opened, lines 4-10) states the same composition and the same diagnosis-vs-receipt rule: "One operator-facing docs gate. T]

### docs/external-integrations.md

- L49-69 (142w) "### `gather` is public-source only — `gather` is public-source only. Credentiale" -> ### `gather` is public-source only

Credentialed organizational data is reached through the consuming runtime's own capability/connector, never a `charness` gather provider; [gather-provider-ownership [(d): docs/gather-provider-ownership.md is the declared owner and was opened; its Default Boundary (11-21) and Consumer Contract (86-99) state every clause here ]
- L9-12 (30w) "For example, `gather` stays one public skill even when it uses separate provider" -> For example, `gather` stays one public skill even when it uses separate public routes such as `gh` or published-document fetching. [missed by pass 1]

### docs/gather-provider-ownership.md

- L23-31 (86w) "This is a deliberate narrowing. An earlier design shipped charness-owned credent" -> The earlier charness-owned `gather-slack`/`gather-notion` runtimes and credentialed `direct-cli` routes were removed under #418 ([v0.60.0 notes](../charness-artifacts/release/v0.60.0-notes.md)); [test [(b): removal history. charness-artifacts/release/v0.60.0-notes.md (opened, lines 13 and 54: "gather public-source only (#418). Deleted the `gather-slack`/`gathe]
- L108-133 (227w) "X/Twitter status posts (exact-source identity): the `twitter-syndication` route'" -> - X/Twitter status posts: the `twitter-syndication` route's `domain-specific-route` stage fetches the exact post and never substitutes a similar source; [twitter_exact_source.py](../skills/support/web [(e): the twitter_exact_source.py module docstring (opened, lines 1-15) carries the identity-keyed endpoints, the status-id match rule, `invalid-proof`, the hone]
- L86-91 (40w) "When a consumer needs credentialed org data: - Slack / Notion / private Google W" -> When a consumer needs credentialed org data, the Default Boundary above applies: the consuming runtime's own capability/connector, never gather. [missed by pass 1]
- L141-143 (30w) "a Charness-side provider-boundary gate keeps model-facing skill surfaces (and th" -> [test_provider_boundary.py](../tests/quality_gates/test_provider_boundary.py) keeps model-facing skill surfaces and their `plugins/` mirror free of raw-token and removed-runtime wording [missed by pass 1]

### docs/goal-lifecycle.md

- L13-15 (30w) "guarded parent close have each run live in this repo (the #724 clean-process pic" -> "...guarded parent close have each run live in this repo; the recorded runs live under `charness-artifacts/goal-runs/`." [Dated evidence. charness-artifacts/goal-runs/724/ (target-roundtrip-readback.md, final-graph-readback.md, observations/) holds the #724 roundtrip; the general p]
- L201-219 (144w) "The provider implementation keeps those concerns on explicit seams: - `issue_ide" -> "Provider mechanics split across `issue_identity`, `issue_backend`, `issue_json_pages`, the `issue_tracker_*` family, `issue_tracker_cli` (+`_preflight`/`_parser`), and `issue_tool` (+`_parser`); each [Mechanism detail carried by docstrings I opened: issue_identity ('Exact repository and issue identity parsing for provider answers'), issue_backend, issue_json_]
- L338-356 (127w) "## Cutover And Removal The existing mutable goal artifact currently owns plannin" -> Delete the section. Carry one sentence into Ownership And Portability: "Legacy goal artifacts under `charness-artifacts/goals/` are planning provenance only; they are not migrated, activated, or accep [History: the checklist is done. skills/public/achieve/SKILL.md line 81 states 'no local status or progress mutation is authorized', upsert_goal.py's docstring i]
- L358-368 (80w) "## Proof And Non-Claims Implementation is not complete until all canonical and g" -> Delete the section. Every non-claim in 366-368 is already stated as current fact elsewhere on the page: not transactional (line 229), planning-only fallback / no offline execution (251-254), single-ag [Pre-implementation status: charness-artifacts/goal-runs/724/final-graph-readback.md and bootstrap-final-graph.json record the #724 reconciliation this paragraph]
- L6 (4w) "> Last reviewed: 2026-09-03" -> delete the line; documentation-principles.md 'Every docs/*.md page carries exactly one header line matching `> Last verified: YYYY-MM-DD`' — no other page carries a second dated header and nothing rea [missed by pass 1]

### docs/harness-composition.md

- L307-311 (33w) "## Maintenance Rule If this document starts repeating detailed policy that alrea" -> delete the section [Restates documentation-principles.md 'Docs-as-code principles': 'Reduce duplication so one fact does not drift across competing copies' and 'Let one page own on]
- L191 (12w) "- [goal-lifecycle.md](./goal-lifecycle.md) — the active goal, current state, and" -> delete the bullet from Visible Artifacts (goal-lifecycle.md is a docs page, not a visible artifact surface); operating-contract.md line 16-17: 'The provider-backed Goal Run parent and cursor are live  [missed by pass 1]
- L230 (13w) "- [goal-lifecycle.md](./goal-lifecycle.md) when the next maintainer needs the ac" -> delete; the active goal state lives in the provider-backed parent/cursor (operating-contract.md Ownership), not in the docs page. Superseded sentence, delete per documentation-principles 'Current stat [missed by pass 1]
- L263 (9w) "- [goal-lifecycle.md](./goal-lifecycle.md) — the active goal and current repo st" -> "- [goal-lifecycle.md](./goal-lifecycle.md) — how to enter the active Goal Run from its parent cursor" (same fix at line 106 wording is already correct) [missed by pass 1]

### docs/host-packaging.md

- L54-65 (107w) "A prebuilt-artifact distribution layer used to live here: a `native_core` declar" -> Delete the paragraph; lines 38-52 and 66-67 already state the current fact (no native artifact, built from `native/repograph`, checkout determines the binary). Keep the spec link as a trailing clause  [Dated history (2026-08-30 retirement, twelve phase statuses, rationale). charness-artifacts/spec/repograph-tool-control-plane.md exists and is already the linke]
- L230-231 (17w) "- the official operator install path is a thin `charness` CLI rooted at the mana" -> delete the bullet [Duplicate: README.md lines 19-20 own it ('creates or reuses the managed checkout at `~/.agents/src/charness`, bootstraps its Python runtime, and runs `charness ]
- L232-237 (58w) "- refreshing the maintainer/authoring machine's own managed checkout is a requir" -> "- after publish the maintainer's managed checkout is refreshed as a release-closeout step (`charness update` here); the `release` skill owns it, see [install refresh](../skills/public/release/referen [The why is owned by skills/public/release/references/install-refresh.md 'Why It Exists' ('The maintainer's installed plugin can otherwise lag the repo and cause]
- L238-240 (33w) "- operators do not need to clone `charness` manually before first install when t" -> delete the bullet [Duplicate of README.md 19-20 (init.sh creates or reuses the managed checkout, then runs `charness init`). The clause that `charness init` itself clones from a c]
- L217-227 (70w) "The repo-root install surface exists for a different reason than temporary expor" -> "Temporary export trees prove a host layout can be materialized under any root; the repo-root `plugins/charness/` tree is the one stable install path, regenerated rather than stored (see Shared Bundle [missed by pass 1]

### docs/implementation-discipline.md

- L35-36 (21w) "6. Commit after verification. Do not push, release, tag, install, or mutate an e" -> "6. Commit after verification; the external phases that need an explicit request are listed in [operating contract](./operating-contract.md#external-changes)." [Duplicate: operating-contract.md 59-61 owns it ('Push, pull request creation, reopening, tagging, version changes, release publication, installation, and evalua]
- L38-43 (46w) "[check_symbol_residue.py] remains advisory by design (#259): use it to find cons" -> drop "(#259)" only: "...remains advisory by design: use it to find consumers, then let the owning focused test decide." [Dated reference. scripts/gates/check_symbol_residue.py's own docstring is 'Advisory scan for stale docs after deleting a public symbol (#259).' so the issue num]
- L45-50 (52w) "## Worktree and runtime hygiene The parent worktree is user state. Never reset, " -> Replace the paragraph with: "Worktree rules are owned by [operating contract](./operating-contract.md#git-and-worktrees) and the commands by [worktree prepare](./worktree-prepare.md)." [Duplicate: operating-contract.md 24-31 states it near-verbatim ('The parent worktree is user state. Preserve its tracked, untracked, and ignored files; never re]
- L67-82 (117w) "## Proof proportionality Normal local implementation needs focused tests and, wh" -> "Proof proportionality is owned by [operating contract](./operating-contract.md#verification); the `prove` skill is an evidence formatter for the cases it lists, not a universal stop ceremony." [Duplicate: operating-contract.md Verification 35-48 ('Changed-line mutation proof, full-suite proof, and artifact ledgers are not universal implementation requi]
- L52-65 (120w) "Use the repository runtime wrapper or equivalent external locations for `PYTHONP" -> delete with the 45-50 cut (the whole section becomes the one-line link). development.md 48-51 owns the runtime-path rule ('keep Python bytecode, pytest cache, coverage, and temporary output outside th [missed by pass 1]
- L84-102 (75w) "## Shared commands ```bash ./scripts/run-quality.sh ... ./scripts/run-quality.sh" -> "Commands: the quality lanes and standing runner are in [development](./development.md#verification-and-export), the docs receipt at [development](./development.md#pushing) (`./scripts/check-docs.sh`) [missed by pass 1]
- L104-106 (27w) "If a command fails because a capability is unavailable, report that concrete fai" -> delete; operating-contract.md 102-104 owns it ('When a command cannot run because a host capability is missing, report the exact failure and leave the affected proof unclaimed. Do not add a prose work [missed by pass 1]

### docs/index.md

- L8-9 (10w) "dated evidence, proposals, and retrospectives belong in [`charness-artifacts/`](" -> "Start with the smallest page that answers the current question. [`check-docs.sh`](../scripts/check-docs.sh) is the composite docs receipt." [Restates documentation-principles.md 'Current state only' ('the why, the counts, and the dates go to `charness-artifacts/`. Move superseded decisions, experimen]

### docs/narrative-announcement-boundary.md

- L7-20 (75w) "## Problem `narrative` and `announcement` were drifting into overlapping territo" -> Replace the Problem section with one orientation sentence under the header: "`narrative` aligns durable truth and compresses it into one audience-neutral brief skeleton; `announcement` adapts that sto [History (the drift and the motivation). No existing charness-artifacts record holds this rationale (grep for 'audience-neutral' and 'announcement' in charness-a]
- L40-48 (57w) "## Success Criteria - `narrative` public docs describe the brief as audience-neu" -> delete the section [Satisfied spec checklist. .agents/narrative-adapter.yaml line 17 declares `brief_template`; tests/quality_gates/test_narrative_adapter.py line 48 asserts the ke]
- L50-54 (31w) "## Acceptance Checks - narrative resolver accepts `brief_template` - narrative a" -> delete the section; optionally end Fixed Decisions with "held by [test_narrative_adapter.py](../tests/quality_gates/test_narrative_adapter.py)" [Satisfied checklist held by passing tests I opened (test_narrative_adapter.py line 48 loops over `brief_template` among required keys; test_narrative_scenario_b]

### docs/operating-contract.md

- L7-9 (37w) "Charness exists to help an agent reach a correct result with less rediscovery. E" -> "This page states the operating floor under [design north star](./design-north-star.md); each contract below earns its place against that standard." [Restates design-north-star.md Purpose ('Charness exists to reduce rework in the repositories that consume it ... Machinery earns its place by preventing work th]

### docs/operator-acceptance.md

- L7-8 (22w) "The current plan of record is the active [Goal Run parent issue #765](https://gi" -> "The plan of record is the active Goal Run parent ([goal lifecycle](./goal-lifecycle.md))." — no issue number. [Dated evidence, already stale: `gh issue view 765 --json state` returns CLOSED. Its record lives in charness-artifacts/goal-runs/765/ (final-proof.md, final-clo]
- L14-21 (16w) "## Shared Start

Run these first at the repo root:

```bash
git status --short
." -> "Start from a clean tree and the small quality lane ([verification and export](./development.md#verification-and-export))." [Duplicate: docs/development.md#verification-and-export (lines 53-66) owns `./scripts/run-quality.sh` and when to run it; `git status --short` is generic git, no]
- L22-28 (19w) "If the work touches integrations or packaging, also read:

```bash
sed -n '1,220" -> "If the work touches integrations or packaging, read [control plane](./control-plane.md) and [public skill validation](./public-skill-validation.md)." [A how-to for reading a doc; nothing must be typed. Every other cross-reference on the page is a link, and both target pages are opened as the owners of that con]
- L47-51 (35w) "Acceptance:

- [`docs/deferred-decisions.md`](./deferred-decisions.md) stays in " -> "Acceptance: the contracts in [deferred decisions](./deferred-decisions.md) (record shape on reopen) and [goal lifecycle](./goal-lifecycle.md) (cursor is navigation state, reconciled by sync/closeout) [Duplicate: deferred-decisions.md lines 7-25 state it is 'the canonical closure surface' and carry the reopen Record Shape (Current choice, Why now, ...); goal-l]
- L52, 138, 165 (13w) "- [`./scripts/run-quality.sh`](../scripts/run-quality.sh) passes after the doc u" -> Delete all three bullets. [Duplicate of this page's own Closeout Rule step 2 (line 172: 'run the strongest honest local validation'), which applies to every accepted item.]
- L81 (11w) "- treat initial install/enable as pre-proven unless the host reports otherwise" -> Delete. [An unlinked history claim ('pre-proven'); no charness-artifacts record cites it (grep of docs and charness-artifacts finds the phrase only here). The next bulle]
- L89-90 (22w) "- Codex update propagation is already operator-proven; keep any future rerun as
" -> Delete; lines 91-93 already state the on-demand rerun (`pytest -q tests/charness_cli/test_update_propagation.py` plus a host spot-check). [Dated evidence: charness-artifacts/retro/retro.md lines 68-70 hold the decision ('narrow the old update-propagation proof from a standing concern to an on-deman]
- L140-166 (150w) "### 4. Decide Adapter Requirements Per Public Skill

Focus: classify which publi" -> Delete the item. If a pointer is wanted: "Missing-adapter policy per public skill: [public skill validation](./public-skill-validation.md#fallback-policy) and docs/public-skill-validation.json." [The item is done. docs/public-skill-validation.md lines 183-266 carry `adapter-required`/`adapter-free` and `allow`/`visible`/`block` with a current per-skill a]
- L64-66 (20w) "Useful local commands. The export line is for a hand run of this experiment; the" -> "Useful local commands (the runners refresh the generated mirror themselves: [generated surfaces](./operating-contract.md#generated-surfaces)):" [missed by pass 1]
- L120-138 (120w) "### 3. Raise `create-skill` / `spec` Workflow Gates" -> Delete, or reduce to one line naming where the open work lives. This is a roadmap item of Goal Run #765, which is CLOSED; goal-lifecycle.md line 25 says 'execution truth is the GitHub parent/sub-issue [missed by pass 1]

### docs/operator-progressive-path.md

- L23-24 (14w) "- Keep current procedures in their owner docs and move dated evidence to
  `char" -> Delete. [Restates docs/documentation-principles.md 'Current state only': 'the why, the counts, and the dates go to `charness-artifacts/`' — no operator-specific fact add]
- L25 (9w) "- Run the canonical exporter once when `skills/public/` changes." -> Delete. [A gate already holds it: docs/development.md mechanisms table row 'A stale `plugins/` mirror is regenerated or refused, never read as a failure | the standing r]
- L38-39 (19w) "- Let [achieve](../skills/public/achieve/SKILL.md) own provider-backed goal
  pr" -> "- Let [achieve](../skills/public/achieve/SKILL.md) own goal progress ([goal and issue work](./development.md#goal-and-issue-work))." [Duplicate: docs/development.md lines 42-44 'Goal lifecycle owns parent/cursor state and issue membership... Do not recreate either state in a local handoff or s]
- L14-15 (10w) "- Run [`./scripts/run-quality.sh`](../scripts/run-quality.sh) when the core
  co" -> "- Run the small quality lane when the core contract is relevant ([verification and export](./development.md#verification-and-export))." — development.md line 55-56 already says 'the small quality lan [missed by pass 1]

### docs/parallel-execution.md

- L80-88 (96w) "Each command must declare `owner_target` and use exactly
`{target:<owner_target>" -> "Each command declares `owner_target` and the standalone `{target:<owner_target>}` token in `argv` and `help_argv`; the checker refuses a mismatch or an embedded form such as `--input={target:<id>}`,  [Mechanism the gate already refuses: scripts/gates_support/command_plan_preflight.py docstring states the `{target:<id>}` token rule, that missing/ambiguous targ]
- L111-117 (49w) "A lane is done only when its receipt's `changed_line_gate` is `clean` or
`noop`:" -> "A lane is done only when its receipt's `changed_line_gate` is `clean` or `noop` ([agent task runs](./agent-task-runs.md)); the parent reads the receipt before integrating." [Duplicate: docs/agent-task-runs.md lines 73-84 own `changed_line_gate` in full (run of release_changed_line_coverage.py over `base_sha..HEAD` with `--refuse-une]
- L77-78 (16w) "It proves resolution and parser
ownership only, not runtime, installed state, ho" -> "It is a plan checker, not a runner." — the docstring's first paragraph ('deliberately a plan checker, not a command runner... runs only `--help` probes') carries the blind class. [missed by pass 1]

### docs/prescribed-skill-closeout-contract.md

- L31-35 (44w) "Achieve owns planning until Goal Binding, then reads the provider-backed Goal
Ru" -> "Achieve owns planning through Goal Binding and then only the provider-backed cursor ([goal lifecycle](./goal-lifecycle.md#authority-by-phase)); it holds no other closeout state." [The ownership half is a duplicate of goal-lifecycle.md 'Authority By Phase' (lines 47-57: binding → active → complete, each with its authority). The enumerated ]
- L9 (12w) "and it no longer participates in Achieve Goal Draft lifecycle or completion." -> Delete the clause ('no longer' is history); the Removed ownership sentence above already states current ownership. [missed by pass 1]

### docs/proof-semantics-adapter.md

- L1 (1w) "# Proof-Semantics Adapter (#339)" -> "# Proof-Semantics Adapter" [Dated provenance in an H1; charness-artifacts/goals/2026-06-08-339-portable-disposition-ledger-adapter-proof-semantics.md holds the goal record. provenance-plac]
- L14-17 (32w) "This keeps the gate-and-intelligence split (#253/#329/#337): Charness does only
" -> "This keeps the gate-and-intelligence split: Charness does only generic lookups and a rank/incomparability comparison over the tokens the adapter declares, never a domain proof level or acceptance cla [Restates provenance-placement.md 'The Rule': 'At most one ref per rule line' and 'never stacks dates or incident-names in the rule body'. The three issues are i]
- L19-28 (79w) "## Why it exists

Filed from `corca-ai/acme` closeout misses where honest prose " -> "Closes a portable gap: a closeout can carry honest non-claims and still publish unless the workflow requires a machine-checkable acceptance/proof/disposition ledger (background: charness-artifacts/go [Incident history; the acme cases and the four slices are in the 339 goal record (opened: Status complete, S1-S4 with commit hashes). documentation-principles 'C]
- L30-39 (79w) "The portable contract: require the adapter to supply the acceptance class and th" -> Delete; add a forward link 'the blocking conditions are in Closeout proof ledger below', and rewrite line 120-121 ('conditions (1)–(3) above') to point at that section. Keep line 62's note that the re [Duplicate within the page: the 'Closeout proof ledger' section (lines 150-158) states the same conditions against the actual `## Proof Ledger` table and names t]
- L120-122 (22w) "Charness (Slice 3) wires these into achieve/issue closeout to enforce conditions" -> "Issue closeout (`issue_verify_closeout.py`) and achieve closeout call these through `proof_mismatch.py` (see Closeout proof ledger)." — 'Slice 3' is history from the 339 record; the second sentence r [missed by pass 1]
- L127 (3w) "Slice 3 must not conflate them:" -> "Do not conflate them:" [missed by pass 1]
- L159-160 (14w) "The floor fires only when a `## Proof Ledger` is present (no over-fire; no exist" -> "The floor fires only when a `## Proof Ledger` is present." — the parenthetical is a count claim about the artifact tree at one date. [missed by pass 1]

### docs/provenance-placement.md

- L48-60 (108w) "- **Exported reusable guidance** — surfaces that ship to consumers as current
  " -> "- **Exported reusable guidance** (`skills/public/**`, `skills/support/**`, `skills/shared/references/**`, generated [cli-reference](./cli-reference.md)) — no issue anchors and no charness self-versio [Partly duplicate of docs/authoring-preflight.md#portable-skill-packages (lines 263-286: validate_skill_ergonomics, bare issue anchors, dated incidents, host-sur]
- L76-86 (68w) "The portable check is config-driven through the **quality adapter**
(`<repo-root" -> "Config-driven through the quality adapter's `standing_doc_provenance` block (`standing_docs`, `tracking_allowlist`, `inline_allow_marker`); [adapter-contract.md](../skills/public/quality/references/a [Duplicate: skills/public/quality/references/adapter-contract.md lines 690-705 own the field list with defaults and semantics; the shipped reference skills/publi]
- L27-29 (34w) "Dates age and issue numbers couple a standing rule to mutable tracker state
(iss" -> Delete; the rule above stands without its why (documentation-principles: 'the why, the counts, and the dates go to charness-artifacts/'), and the shipped copy skills/public/quality/references/standing [missed by pass 1]
- L93-95 (25w) "It generalizes the skill-package anchor gate
([`skill_text_quality_lib.py`](../s" -> "Portable description: [standing-doc-provenance.md](../skills/public/quality/references/standing-doc-provenance.md)." — that reference (lines 15-19) and adapter-contract.md line 694 already state the  [missed by pass 1]

### docs/public-skill-dogfood.md

- L17-18 (20w) "- keep the registry small and current; historical review detail belongs in
  dat" -> Delete. [Restates documentation-principles.md 'Current state only' ('the why, the counts, and the dates go to `charness-artifacts/`') with no registry-specific fact; art]
- L33-34 (18w) "The next leverage is stronger proof for the weakest reviewed cases, not more
reg" -> Delete; a priority statement, not a current fact — goal-lifecycle.md line 25 puts open work in the Goal Run graph. Keep lines 31-32 (the rule for when a case is added). [missed by pass 1]

### docs/public-skill-validation.md

- L125-126,135-144,155-161 (60w) "Current assignment: - none / - announcement - create-cli ... - spec" -> Under each tier heading keep only the 'Use this when' paragraph plus one line: 'Current membership: `tiers.<name>` in [public-skill-validation.json](./public-skill-validation.json).' [Duplicate of docs/public-skill-validation.json `tiers`, which line 10 already names as canonical. The copies have drifted: the JSON's `hitl-recommended` include]
- L198-211,220-222 (40w) "Current assignment: - announcement - create-skill - critique ... / - create-cli " -> 'Current membership: `adapter_requirements.required` / `adapter-free` in public-skill-validation.json.' [Duplicate of the JSON `adapter_requirements` block (opened). Drifted: JSON `required` has `achieve`, `adapter-free` has `prove`; neither appears in the md lists]
- L233-240,250-254,264-266 (40w) "Current assignment: - create-cli - debug - gather ... / - announcement ... / - h" -> 'Current membership: `fallback_policy.allow` / `visible` / `block` in public-skill-validation.json.' [Duplicate of JSON `fallback_policy` (opened). Drifted: JSON `visible` includes `hotl` and `allow` includes `achieve`/`prove`; the md lists omit all three while ]
- L163-181 (170w) "announcement, create-cli, ideation, narrative, critique, quality, release, and r" -> Delete the section. Only `critique` is pinned by tests/quality_gates/test_skill_docs_contracts.py:288 (`"`critique`" in validation_doc`); it already appears at 174 elsewhere only in this section, so k [Per-skill why. Operator rule in charness-artifacts/goal-runs/784/783-lesson-dispositions.md: a docs sentence carries the rule and the mechanism's name, never th]
- L268-279 (90w) "hitl, narrative, and release mutate high-leverage review, truth, or publication " -> Delete; the `block`/`visible`/`allow` 'Use this when' paragraphs (228-229, 244-246, 258-260) already carry the generic reason. [Restates the reasoning of the three fallback definitions on the same page, per skill. Also internally inconsistent with the md list it rationalizes (names `hotl]
- L76-82,103-113 (150w) "Deterministic local gates own ordinary closeout. Prompt-affecting diffs do not t" -> Drop the `Execution Policy` heading and paragraph (76-82) and the five bullets (103-113); fold the one bullet not already stated — 'closeout states when requested proof is missing' — as a final senten [76-82 restates 48-51 and 72-74 (same page: deterministic first, no live evaluator or proof artifact merely because a prompt changed). Bullets 103-108 restate 65]
- L281-292 (65w) "The next integration session should: 1. revisit any HITL recommended skill that " -> Delete. If items 1-2 are still open, one D-entry in docs/deferred-decisions.md (opened; no entry for this exists today). Item 3 is already the rule at 40-44 plus the JSON. [Forward-looking session plan, not current state; documentation-principles 'Current state only' sends superseded decisions and session history to charness-artifa]
- L84-95 (80w) "Intent is part of the proof contract, not only a chat-side interpretation. - pro" -> 'Intent is part of the proof contract. No mechanism reads an intent label today; the labels below would have to be read by a gate before they are a contract.' then delete the six-item enum, or keep it [missed by pass 1]
- L10-17 (15w) "Canonical machine-readable policy lives in ... This markdown file stays as the h" -> After the list cuts land, drop 'and rationale layer' — the page then narrates the three buckets and owns the prompt-affecting-change policy; keep the `tools.suggest_public_skill_validation` command (a [missed by pass 1]

### docs/runtime-capability-contract.md

- L11-31 (160w) "charness should assume that it may run inside an isolated agent runtime where th" -> '## Core Assumption\n\nAssume an isolated agent runtime with no arbitrary local-secret-file read; the grant > authenticated binary > env preference is owned by [external-integrations.md](./external-in [external-integrations.md 'Runtime Access Principle' (opened, lines 73-85 of that file) states the isolated-runtime assumption near-verbatim and the same ordered]
- L115-136 (115w) "When one repo wants to reuse the same private provider across multiple charness " -> '## Local Resolution Layer\n\nRepo-local capability config models `logical capability id -> repo-local profile -> provider id` and records env-name aliases only, never secret values or machine-global  [docs/capability-resolution.md (opened) owns the path (line 35, 54), the `logical capability -> profile -> provider` model (59), the no-secret-values rule (57), ]
- L158-168 (70w) "When a public skill depends on an external capability, onboarding should follow " -> '## Onboarding Rule\n\nOrder: grant, authenticated binary, env fallback ([external-integrations.md](./external-integrations.md#runtime-access-principle)); then offer install only when it is safe and d [Items 1-3 are the external-integrations ordered list (opened) and this page's own `config_layers` order at 79-83. Items 4-5 (install-if-safe, stop cleanly) are ]
- L138-156 (90w) "gather is the clearest example because one user-facing concept may consume many " -> '## Gather As Exemplar\n\n`gather` is one public concept (durable knowledge acquisition) over many providers; provider, access mode, and onboarding stay below the skill surface. [gather-provider-owner [docs/gather-provider-ownership.md (opened: 'gather is public-source only', credentialed `gather-slack`/`gather-notion` removed) and external-integrations.md 60-]
- L170-177 (45w) "Profiles may still group workflow emphasis, but they are not the authority for s" -> One sentence: 'Profiles never carry secret transport or access control; capability availability is modeled by manifests, adapters, and host grants.' The profile-inheritance decision is docs/deferred-d [missed by pass 1]

### docs/support-skill-policy.md

- L125-129 (40w) "When the capability depends on private access: - prefer a runtime grant or authe" -> 'Private-access capabilities follow the onboarding order and secrets rule in [runtime-capability-contract.md](./runtime-capability-contract.md).' [Duplicate of runtime-capability-contract.md Onboarding Rule (158-168) and Secrets Rule (59-93), and of external-integrations.md Runtime Access Principle, all op]
- L131-141 (40w) "## Session 12 Hook\n\nThis policy is the bridge into control-plane work. Session" -> Delete the heading and the session plan. Retitle the surviving 142-150 paragraph '## Consumable Locally' and open it with: 'Support skills are always materialized into the installed plugin under `supp [Session numbering is history. The four bullets already exist: docs/control-plane.md (opened, lines 27, 34-40, 75, 103-104) owns sync/materialization, `support/<]
- L142-150 (75w) "For the current seam, support skills should always be consumable locally: - if t" -> Covered by the one-sentence replacement above. [Duplicate of docs/control-plane.md 34-40 (`upstream_repo` fetch into user cache then copy under `support/<tool-id>/`; `local_wrapper` render into the same cache]
- L94-98 (45w) "These labels matter for later sync/update/doctor behavior. They should also stay" -> 'The state is recorded in the manifest lock state ([control-plane.md](./control-plane.md)).' — 'later' and 'should also' are forward-looking; the lock state exists now. [missed by pass 1]

### docs/surface-driven-adapter-triggers.md

- L23-37 (95w) "## Why surface ids, not raw globs\n\nA surface id is a named claim about what th" -> Delete the section; amend lines 10-12 to: 'Raw globs (`auto_session_trigger_path_globs`) remain for a narrow repo-local exception or a genuinely cross-cutting path that no declared surface should clai [23-31 is the why (design-north-star P3 / operator rule: rule plus mechanism, never the why). 32-37 restates lines 10-12 of the same page, adding only the 'cross]
- L56-59 (35w) "Without this fail-loud step, a typo in auto_session_trigger_surfaces is indistin" -> Delete. [Rationale for a rule whose observable behavior is fully stated at 44-50 and whose mechanism (`resolve_trigger_surfaces` in scripts/adapters/surfaces_lib.py) is ]
- L87-98 (75w) "## Call-site checklist\n\nWhen you add or change a trigger consumer: - subscribe" -> Delete the section. [Every item restates a section above on the same page: bullet 1 = lines 7-12, bullet 2 = 52-55, bullet 3 = 79-85, bullet 4 = 61-77.]
- L61-64 (10w) "Every trigger consumer ships at least the following four fixtures, in its own te" -> Append the gap: 'No gate checks that a new consumer ships these; the retro fixtures in tests/quality_gates/test_retro_auto_trigger.py are the worked example.' — the section stays (correctly, as the ma [missed by pass 1]

### docs/validator-timing-layers.md

- L47-60 (190w) "Budget rule: ... The 2026-06-10 audit's five pulled guards add about 0.5s combin" -> 'Budget rule: the pulled commit-time subset stays small enough that the hook never tempts `--no-verify`; roughly one second of added typical-path latency is the line for removing something before addi [Dated timings and counts. charness-artifacts/critique/2026-06-15-issue-368-leak-scan-shift-left.md F4 (opened, line 65) records the ~0.9s-in-isolation and hones]
- L85-87 (30w) "so the table cannot silently fall behind a newly added validator (the #368 recur" -> '...fails if a declared `run-quality.sh` gate label has no verdict row here; list the exact label, not a glob shorthand.' [The issue reference is history held by the same #368 critique artifact (line 21 of it: 'corrected + expanded to exact labels'). The rule 'exact label' stays.]
- L68,70,75 (12w) "**pulled → commit-time** (2026-06-23) / **pulled → commit-time** (#368, 2026-06-" -> Strip the dates and issue number from the `timing_layer` values in .agents/quality-gates.yaml (the table is rendered from there by scripts/render_validator_timing_layers.py; edit the yaml, then `--wri [missed by pass 1]

### docs/workflow-routes.md

- L31-33 (35w) "For prompt- or behavior-affecting changes, use quality's smallest sufficient det" -> 'Prompt-affecting changes follow [public-skill-validation.md](./public-skill-validation.md#prompt-affecting-changes).' or delete outright. [Duplicate of docs/public-skill-validation.md 48-51 and 72-74 (opened). Under lesson-graduation Q1 a cheat-sheet line survives only as one line plus the owner li]
- L60-65 (90w) "achieve first shapes prose into one complete Goal Draft under charness-artifacts" -> '`achieve` shapes and freezes a Goal Draft, binds it to one Goal Run, and resumes with `/goal #N`; [goal-lifecycle.md](./goal-lifecycle.md) owns the binding, cursor, and reconciliation model.' [docs/goal-lifecycle.md Design Center (opened, lines 29-32: cursor is navigation state, explicit sync/closeout owns full reconciliation) and Activation And Picku]

### docs/worktree-prepare.md

- L20-27 (150w) "## Why this exists\n\nTwo well-known failure modes share the same symptom ... 1." -> '## Why this exists\n\nTwo silent-hook-skip failure modes: lefthook's `.git/hooks/` shim hardcodes the install-time worktree path, and husky's `.husky/_/` is generated per worktree. The `lefthook_shim [charness-artifacts/spec/worktree-prepare-doctor.md (opened, line 7) carries the full lefthook #1398 / husky #580 analysis with both upstream links. The mechanis]
- L121 (65w) "Prepare skipping is licensed by the exact intersection of doctor.checks[].covers" -> '- A skip is licensed only by the manifest's `doctor.checks[].covers` -> `prepare.commands[].id` relation, established when every prepare command has a unique id covered by a check that passed (`_prep [The `_prepare_coverage` docstring (opened, worktree_doctor_lib.py:311-318) states the relation, the no-argv-interpretation rule, and the unique-id-plus-passing-]
- L142 (12w) "Symlinking the whole `node_modules` is not — Vite/Vitest will break on module re" -> Delete this sentence; the same fact is at the (new) line 22-ish 'Why this exists' and at 153. [missed by pass 1]
- L148-153 (25w) "## Limitations - Manifest argv lists cannot use inline YAML arrays today. Block " -> Drop bullet 150 (restates note 118 verbatim) and bullet 153's first clause (restated at 27/142); keep the heuristic-shim and monorepo bullets. [missed by pass 1]
