Goal Run #784 closed through `issue_tool.py goal-run-close` after exact readback.

Classification: feature
Jtbd: the lesson ledger serves only what the repo does not yet hold by mechanism or page, and the four escape paths #775 left open are closed: a lane cannot report done past an unproven changed line, a test cannot ride a timeout knob, the runtime tree reclaims itself, and skill scripts inside this repo run from the checkout, shipped as 8.0.3.
Boundary: six Work Items (783, 785, 786, 787, 788, 789), each closed by a `Closes #N` commit on `origin/main`, each `verify-closeout` = `verified`, each with an issue-owned closeout comment bound in `charness-artifacts/goal-runs/784/final-close-proof.json`. #764 is not closed by this run: the most recent scheduled mutation run passed its baseline and lost `Run mutation` to the job budget; the budget is recorded there as the next work. No release beyond 8.0.3 and no installed-host mutation beyond #788's recorded refresh is claimed.
Resolution Brief: `charness-artifacts/goals/2026-09-03-lesson-review-and-775-followups.md` (frozen draft sha256 878f74409e4c271d07a87c4bb34108376d10878cfdc476249311ec3671a8a151; binding sha256 9e4650f0f731add79de7a0b68cc3b918ed087e71d7e28f43cb74c0973ddafd82).
Implementation: `charness-artifacts/goal-runs/784/final-proof.md` names the carrier commit and comment for every child and the whole-system evidence: three lanes green in a clean clone (`789-clean-clone-lanes.md`), the hosted run read and #764's state made consistent with it, the pickup from the checkout's own script, and the joint lesson review (`783-lesson-dispositions.md`).
Prevention: the lane done-gate, the timeout-bound form check, the docs length gate, the graduated-recurrence readback, the runtime-root retention sweep, and the checkout-first provenance guard carry the run's lessons forward as mechanisms; the session retro seeds the one class this closeout found.
Behavior: verified — `goal-run-read` returned six linked children all `CLOSED` before this close; the close proof binds the expected graph, the parent terminal obligation, and the role-labelled evidence artifacts by byte hash.
AI-provenance: the run was executed by an AI agent (Claude Code) with Codex workers and Claude subagents, under operator approval for the graph, every push, every lesson disposition, the 8.0.3 release, and the decision to close with #764 open.
