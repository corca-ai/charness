Recovery-observer reading, 2026-09-03 (Goal Run #784, #789 integrated-closeout).

The most recent scheduled `mutation-tests.yml` run is https://github.com/corca-ai/charness/actions/runs/33756376766 (`bce861e15`, 2026-09-03T12:38Z, cron), on a tree after `bdddbe93a` (#782) and after every #784 child except the #783 docs series.

Step outcomes read from the run: `Select mutation sample` **success** (the coverage baseline that failed the previous two scheduled runs passed on this tree; the #782 rewrite held), `Run mutation` **cancelled**, `Summarize mutation report` **failure**, `Comment mutation recovery candidate` **skipped**.

Why `Run mutation` was cancelled, from the job log: the job started 12:38:37Z; `Select mutation sample` ran 12:40:41Z to 13:09:53Z (29 min); `cosmic-ray exec` started 13:11:08Z on 118 executable mutants (146 inspected, 28 trivial entry guards filtered) with its internal timeout of 9000 s; the log shows heartbeats to `elapsed=8880.1s` and then `##[error]The operation was canceled.` at 15:39:30Z, which is the job's `timeout-minutes: 180` firing before the exec's own 9000 s deadline. No per-mutant result reached the log. The sample was 5 files (core-python 3/13, unknown 2/151) against a per-mutant test command that runs the non-release pytest suite (8,863 tests, about 90 s a run on a hosted runner), so 118 mutants cannot finish inside the roughly 147 minutes the job has left after setup and sampling.

This issue stays OPEN. Under the rule its recovery-observer path carries (#358), it closes only when a scheduled run's sampler ran and the run was green; this run's sampler ran and the run was not green. The failing set recorded as the next work is not a test: it is the budget. The `Run mutation` step's mutant population (`max_executable_mutants` 120, per-file 80) times the per-mutant suite runtime exceeds `timeout-minutes: 180` minus sampling, and the exec's internal 9000 s timeout is longer than the job's remaining budget, so the job cancel arrives first and the run reports UNEXPLAINED instead of a timeout marker. A next slice owns one of: a smaller executable-mutant ceiling sized to the measured per-mutant runtime, a per-mutant test command narrower than the whole suite, or a job budget above the exec timeout so the exec's own marker is what the summary reads.

Recorded by an AI agent (Claude Code) in the #784 session; not closed by hand (#789 non-claim).
