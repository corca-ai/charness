# WI-8a closeout: completed

## Evidence

- Generic train core: pure decision core (`task_run_train_core.py`), runner plus verify-profile schema/loader/default (`task_run_train.py`), top-level `train` subcommand, command-docs plus registry entries, repo verify profile declared in-slice.
- `tests/charness_cli/test_task_run_train.py` (new): green on the integrated tree with the command-docs gate.
- Changed-line gate clean on the final candidate; cli-reference regenerated by parent post-merge; integration worktrees provisioned via the lane path (install plus drift check), never bare worktree add.
- Lane path: premise rulings added the profile declaration and the root-CLI owner to the brief instead of stopping; registry family completed file by file.

Classification: feature

Jtbd: as the integrator I need one verify run to land N non-conflicting branches, name the first red branch, and re-queue when main moves, without re-implementing the loop per repo.

Boundary: generic core plus repo verify profile; DAG queue is WI-8b; no daemon, watch loop, or background poll.

Resolution Brief: pure decision core with repo-supplied profile and charness-provisioned integration worktrees.

Implementation: train core, runner, profile, CLI, registries, and train tests as listed above.

Prevention: profile declared in-slice when the repo had none; registry family enumerated once instead of per-stop discovery.

Behavior: train suite plus command-docs gate green on the integrated tree — a channel distinct from the CLOSED state and the carrier body.

AI-provenance: agent-authored from verified lane receipts wi-8a-train-4/5/7/8 under Goal Run #844.

Closes #852.
