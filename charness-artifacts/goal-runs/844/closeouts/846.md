# WI-2 closeout: completed

## Evidence

- Executor probe on main: `charness task executors` resolves executable availability without starting a lane; usage-limit failures yield typed `executor_unavailable` with `retry_after` (exit 4 via the WI-1 map); ordered `--executor codex,muse` lists fall through only on that typed failure (lane candidate `d483fdc7c`, merged `12a67bea1`).
- `tests/charness_cli/test_task_run_executor_probe.py` (new): 6 passed on the integrated tree; lane ran 177 affected tests with a clean changed-line gate. Command-docs gate findings empty; parent regenerated `docs/cli-reference.md` for the new command (`0d7a432b2`).
- Integration repair: plan/task_run conflict resolved keeping both WI-4 prelaunch and WI-2 executor blocks; prelaunch/payload splits hold the 480-line caps; run_task holds its statement cap. No new lint errors from this integration.

Classification: feature

Jtbd: as the integrator I need lanes to report quota exhaustion as a typed, retryable verdict with fallback instead of a generic failure, and to probe availability without paying a lane start.

Boundary: probe plus typed failure plus ordered fallback; no steer, premise, lesson, or review work. No executor CLI changes. No release or version changes.

Resolution Brief: availability probe without lane start; usage-limit transcripts become `executor_unavailable` with `retry_after`; fallback walks the ordered list.

Implementation: execution-path typed failure, state kind plus exit 4, attempt fallback, `task executors` command with registry entries, probe tests, and regenerated CLI reference as listed above.

Prevention: quota failures typed at the execution boundary with ordered fallback, so the next usage-limit stops falling through to a generic failure.

Behavior: probe suite (6) plus core (47) green on the integrated tree, and `task executors` reports availability with lane_started false — channels distinct from CLOSED and the carrier.

AI-provenance: agent-authored from verified lane receipts wi-2-executor-probe-2 under Goal Run #844.

Closes #846.
