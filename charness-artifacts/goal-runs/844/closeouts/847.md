# WI-3 closeout: completed

## Evidence

- Stop-and-resume steer in the same worktree (session resume where supported, relaunch-in-place fallback): `task_run_progress.py` (queue poll), `task_run_lane_runner.py` (turn-boundary injection), `task_run_execution.py` (resume invocation), `task_run_scope.py` (same-candidate amend), `task steer` subcommand in root `charness`, command-docs plus registry entries.
- `tests/charness_cli/test_task_run_steer.py` (new): green on the integrated tree with the command-docs gate.
- Changed-line gate clean on the final candidate; cli-reference regenerated by parent post-merge.
- No transport surgery in shared `subprocess_guard.py` (refused by design; both executors are one-shot with session resume).
- Lane path: two pre-edit premise stops (execution boundary, then transport) resolved by rulings with evidence; registry entries approved file by file.

Classification: feature

Jtbd: as the integrator I need to steer a running lane so a changed plan, landed sibling, or premise answer reaches it before its next edit instead of costing a relaunch.

Boundary: file queue, turn-boundary delivery, resume-in-place, timestamped record, scope amend. No live-transcript injection, no shared-transport changes.

Resolution Brief: steer as stop-and-resume in the same worktree with session resume where supported.

Implementation: steer channel across progress, lane runner, execution, scope, and CLI with registry entries and steer tests.

Prevention: typed extension requests approved explicitly; transport-scale claims checked against executor capabilities before widening scope.

Behavior: steer suite plus command-docs gate green on the integrated tree — a channel distinct from the CLOSED state and the carrier body.

AI-provenance: agent-authored from verified lane receipts wi-3-steer-3/4 under Goal Run #844.

Closes #847.
