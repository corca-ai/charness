# WI-6 closeout: completed

## Evidence

- Declared-class lesson injection on main: `scripts/task_run/task_run_lesson_injection.py`
  (new; ledger lookup, recurrence-class matching, budgeted prompt rendering,
  result normalization), thin wiring in `scripts/task_run/task_run_lane_runner.py`
  (`prepare_lane_execution`, `lesson_injection_block` prompt param) and
  `scripts/task_run/task_run_completion.py` (`lesson_injection` result block in
  the WI-1 envelope shape) (lane candidate `416f8f17`, merged `b84816d0a`,
  zero-conflict merge; parent extraction for the length gate).
- `tests/charness_cli/test_task_run_lesson_inject.py`: 11 passed
  (declared-class injects through a full `run_task`, unknown-slug unmatched,
  byte-budget exclusion, ids-in-result, plus parent-added no-root
  unavailable, untagged-wording error, invalid-ledger error, header-budget,
  error-passthrough, plain-import cases); adjacent `steer`, `completion`,
  `result`, `implementation_lane`, `exit_codes`, `self_review` suites green.
  Release changed-line gate clean, exit 0; length gate passes with the new
  module at 190 code lines.
- Attempt 1 premise-BLOCKED with no change (validated ledger has no
  code-scope tags; verified against the 70-lesson ledger and the replay
  code); operator amended the matching rule to declared recurrence-class
  equality. Scope-path matching stays an explicit non-claim pending ledger
  scope tags.

Classification: feature

Jtbd: as a lane author I need ledger lessons relevant to my brief injected
into my prompt automatically, with a record that tells a failed lesson from
a failed injection.

Boundary: declared-class match only (slug equality against validated
lessons); byte budget plus full-ledger pointer; no ledger migration, no
ranking, no prompt changes beyond the injected block. No release or version
changes.

Resolution Brief: briefs declare `recurrence-class: <slug>` tags; the lane
prompt gains the validated lesson block under a 16 KiB cap; the result
lists injected ids, unmatched slugs, budget exclusions, and the
scope-path-deferred non-claim.

Implementation: injection module with lookup/match/render/normalize,
runner and completion wiring, injection tests with match/unmatched/budget/
ids/error cases as listed above.

Prevention: unmatched slugs and ledger failures are reported as such, never
as injected; the non-claim blocks scope-path coverage claims, so a silent
miss cannot masquerade as relevance.

Behavior: injection suite (11) plus steer/completion/result/lane/exit/self-review
suites green on the integrated tree — channels distinct from CLOSED and the
carrier.

AI-provenance: agent-authored from verified lane receipt wi-6-lesson-inject-2
under Goal Run #844.

Closes #850.
