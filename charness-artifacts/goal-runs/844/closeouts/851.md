# WI-7 closeout: completed

## Evidence

- Self-review phase on main: `scripts/task_run/task_run_execution.py`
  (`run_self_review`: read-only Codex pass, unavailable-skip paths),
  `scripts/task_run/task_run_state.py` (`self_review_block` with
  fixed/deferred/disputed findings, `self_review_requested` boundary policy,
  structured-review extraction), `self_review` block wired in
  `scripts/task_run/task_run_completion.py`, `--self-review` opt-in flag
  (lane candidate `4d31633d`, merged `d6f9211f3`, zero-conflict merge).
- Landing review trigger on main: pure routing derivation plus packet/record
  planning in `scripts/task_run/task_run_train_core.py`
  (`derive_landing_review_routing`, `landing_review_plan`), stdlib-only
  fire-and-record launch in `scripts/task_run/task_run_train.py`
  (`_landing_review_trigger` with launch record, non-claims,
  unavailable-skip) on the fast-forward path via the canonical critique
  lifecycle (lane candidate `2c1045a2`, merged `51b567928`, zero-conflict
  merge). Two premise findings on the way (ledger scope tags, review-lifecycle
  ownership) were verified against source and resolved by operator amendment
  and rescope; the third attempt's subprocess-guard excursion was rejected
  with a binding in-scope directive.
- `tests/charness_cli/test_task_run_self_review.py` (16 passed) and
  `tests/charness_cli/test_task_run_landing_review.py` (6 passed: routing,
  packet identity, non-blocking launch, launch-failure non-claim, plus
  parent-added shape-rejection, prep-success, record-write-failure cases);
  adjacent `exit_codes`, `completion`, `train` suites green. Parent completed
  both lanes' flagged changed-line coverage (persist-seam fix, routing error
  paths, packet-prep success, record-write failure); release changed-line
  gate clean, exit 0, for both merges.
- Live P1 delivery to a next unit stays an explicit non-claim of the stub
  fixtures, recorded in the trigger record and the tests.

Classification: feature

Jtbd: as the integrator I need lanes touching irreversible boundaries to get
a fresh-eye pass by default, and every train landing to start a review of
the landed range, without either ever reading as independent proof.

Boundary: self-review default-on for irreversible-boundary lanes only;
landing trigger fires and records without holding the landing; skips are
non-claims, never review-passed. No reviewer CLI changes, no metrics. No
release or version changes.

Resolution Brief: read-only self-review with fixed/deferred/disputed
findings in the result envelope; fast-forward builds the critique packet
and launches the canonical review detached, routing P1 to the next unit and
P2/P3 to the batch.

Implementation: self-review orchestration plus boundary policy plus block
helpers; train-core routing/plan derivation plus train fire-and-record
trigger; self-review and landing-review suites with disposition, skip,
routing, launch-record, and failure cases as listed above.

Prevention: skips and stub limits are recorded as non-claims in both the
result envelope and the trigger record, so an unreviewed lane cannot
masquerade as reviewed.

Behavior: self-review suite (16) plus landing-review suite (6) plus
train/completion/exit_codes suites green on the integrated tree — channels
distinct from CLOSED and the carrier.

AI-provenance: agent-authored from verified lane receipts wi-7-review-4 and
wi-7-landing-review-3 under Goal Run #844.

Carrier refs: phase-1 merge `d6f9211f3` (lane `4d31633d`), trigger merge
`51b567928` (lane `2c1045a2`), parent coverage `f6e1981f7`; changed-line
gate clean (exit 0) for both merges; `check-docs.sh` and length gate pass.

Closes #851.
