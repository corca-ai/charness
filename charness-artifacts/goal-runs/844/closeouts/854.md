# WI-9 closeout: completed

## Evidence

- Report economy plus decision ledger on main: `scripts/task_run/task_run_evidence.py` (full-report file write plus three-field short summary), `scripts/task_run/task_run_ledger.py` (new, repo-local decision ledger with append/read), summary/ledger-link/delivery-blocker wiring in `scripts/task_run/task_run_completion.py` (lane candidate `e291e307`, merged `ac84d2d28`, zero-conflict merge).
- `tests/charness_cli/test_task_run_report_ledger.py`: 8 passed (summary-brevity, file-completeness, ledger-link, append-uniqueness, plus parent-added clipping-error, unsaved-report blocker, foreign-source, invalid/foreign-line cases); adjacent `report_only`, `completion`, `exit_codes`, `self_review` suites green. Parent completed the lane's flagged changed-line coverage (evidence.py OSError path, ledger validation paths); release changed-line gate clean, exit 0.
- Report-only delivery checks moved intact with report-economy semantics (a complete persisted full report excuses inline truncation); blockers observe the original delivery text before summary attach.

Classification: feature

Jtbd: as the integrator I need long lane reports out of my context and mid-goal decisions in one linked ledger, without any local log ever reading as progress.

Boundary: three-field summary plus file write only; decision ledger is repo-local indexed content, no provider writes; no DAG or friction-log stores. No release or version changes.

Resolution Brief: full report lands in `full-report.log` beside the result, the result carries the short summary; decisions append as WI-1 v1 events to `charness-artifacts/task-run/decision-ledger.jsonl` and link from every result.

Implementation: evidence report/summary helpers, ledger append/read/link module, completion wiring, ledger tests with brevity/completeness/link/uniqueness/rejection cases as listed above.

Prevention: the provider graph owns open/blocked/closed truth; the ledger link records `exists` without asserting progress, so local content cannot masquerade as closeout proof.

Behavior: ledger suite (8) plus report_only/completion/exit_codes/self_review suites green on the integrated tree — channels distinct from CLOSED and the carrier.

AI-provenance: agent-authored from verified lane receipt wi-9-report-ledger under Goal Run #844.

Closes #854.
