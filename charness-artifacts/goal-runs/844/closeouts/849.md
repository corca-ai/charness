# WI-5 closeout: completed

## Evidence

- Launch hygiene on main: base freshness refuse/warn in `task_run_git.py`, skeleton-first ordering plus size calibration with split suggestion in `task_run_plan.py`, primitive-rule directive plus candidate reporting in `task_run_lane_runner.py`, stacked-base support in `task_run_dag.py` (per-lane base/stacked-on schema, `base=` forwarding in `_launch_lane`, predecessor SHA in `_lane_result`) — lane candidate `ed86805f0`, merged `39e7d72b9`, zero-conflict merge, 5 files +279/-7.
- `tests/charness_cli/test_task_run_launch_hygiene.py` (new, 111 code lines): lane-reported 207 focused tests passed, ruff/length/diff clean.
- Parent verification on the integrated tree: full `tests/charness_cli` 722 passed; file caps hold (dag 478, git 480, plan 319, runner 412, all under/at 480).
- Integration repair `b1ca600ab`: the WI-2/WI-4 module splits broke 4 legacy tests (`exit_codes` receipt persistence, `scope_gaps` persist-completion names, execution flat-layout binding); restored the execution fallback verbatim and retargeted persist/patch sites to `task_run_payload`; 36/36 green after.
- First attempt BLOCKED on DAG stacking scope; parent amended scope with inspected evidence (`run_task` already honors `base`, so DAG-only change, no derive_ready restructure).

Classification: feature

Jtbd: as the integrator I need stale-base lanes refused or warned, shared seams landing before sibling fan-out, grep-before-create primitive discipline, and oversize lanes split with a suggestion, so launches stop drifting onto stale bases with duplicated primitives.

Boundary: launch-time warnings and refusals only, verified with a standalone preflight fixture; no DAG scheduler ownership, no premise verdicts, no metric persistence. No release or version changes.

Resolution Brief: freshness against the dependency tip with refuse-or-warn and never auto-rebase; dependents stack on the predecessor tip; seam-first ordering via hard plan dependencies; primitive candidates reported; calibrated size limit with split suggestion.

Implementation: git freshness check, plan ordering plus calibration, runner directive plus reporting, DAG stacked-base support, hygiene fixture suite as listed above.

Prevention: freshness is refuse-or-warn at launch with no auto-rebase path, so a stale base can never silently become a stacked one.

Behavior: full charness_cli suite (722) green on the integrated tree including the new hygiene suite — a channel distinct from CLOSED and the carrier.

AI-provenance: agent-authored from verified lane receipts wi-5-launch-hygiene-2 under Goal Run #844.

Closes #849.
