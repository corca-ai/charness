# WI-4 closeout: completed

## Evidence

- Prelaunch gates on main: `--critical-lane`, `--acceptance-skeleton`, `--premise-check` (lane candidate `a91719605`, merged `b0f790bb7`, clean merge).
- New `scripts/task_run/task_run_prelaunch.py` (brief critique, premise checks, acceptance skeleton, prelaunch runner) split from `task_run_plan.py` at integration to hold the 480-line cap; companion `task_run_payload.py` holds payload phase/persistence helpers. Both splits verbatim moves, behavior preserved.
- `tests/charness_cli/test_task_run_premise_gates.py` (new): 8 passed on the integrated tree. Core `test_task_run` 47 passed, completion 31 passed, full `tests/charness_cli` 640 passed, `check-docs.sh` PASS.
- Parent regenerated `docs/cli-reference.md` for the new flags (the lane's declared BLOCKED, parent-owned surface).
- Integration repair: acceptance-skeleton spawns now go through the subprocess guard (subprocess-form gate 7 passed); touched import blocks sorted.

Classification: feature

Jtbd: as the integrator I need a cheap fresh-eye brief critique, per-item premise verdicts, and a committed failing acceptance skeleton before a lane starts, so false-premise lanes stop before they burn executor budget.

Boundary: prelaunch gates only; no steer, probe, lesson, review, or ledger work. No release or version changes.

Resolution Brief: bounded prelaunch phase (critique plus typed premise results plus skeleton baseline) with per-item BLOCKED verdicts in WI-1's kind vocabulary.

Implementation: prelaunch contract/plan/completion wiring, brief-critique executor call, acceptance-skeleton runner with committed-and-failing enforcement, premise tests, and regenerated CLI reference as listed above.

Prevention: premise vocabulary frozen in WI-1 before gates consume it; the skeleton must be committed and failing before launch so the gate cannot be gamed.

Behavior: premise suite (8) plus core (47) plus completion (31) green on the integrated tree, and the subprocess-form gate green after the guard repair — channels distinct from CLOSED and the carrier.

AI-provenance: agent-authored from verified lane receipts wi-4-premise-gates-5 under Goal Run #844.

Closes #848.
