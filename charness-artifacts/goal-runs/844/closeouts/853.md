# WI-8b closeout: completed

## Evidence

- Declarative DAG module `scripts/task_run/task_run_dag.py` (new, 444 code lines) plus `tests/charness_cli/test_task_run_dag.py` (new, 384 lines) on main (lane candidate `4fa8247b1`, merged `031528248`, zero-conflict merge).
- 49 DAG tests passed on the integrated tree; lane-reported changed-line `clean` plus a killed launcher-exception mutant.
- Pull is one explicit foreground command: `python3 scripts/task_run/task_run_dag.py pull --plan-file <path>`. No watch loop, background poll, daemon, or persisted DAG status. No `charness task` dispatcher route (out of the declared two-file scope; recorded here, not smuggled in).

Classification: feature

Jtbd: as the integrator I need one static file holding lanes, dependencies, carrier, and brief path, with ready-derivation from provider child state and a single-shot pull, so queue truth stops living in private session scripts.

Boundary: static plan plus pure ready-derivation plus single-shot pull; provider child state owns open/blocked/closed truth and the DAG never asserts completion. No train core, progress cursor, or scheduler behavior.

Resolution Brief: one JSON plan file as the queue's source of truth; readiness derived from provider state plus hard dependencies; freed slots pull the next ready lane.

Implementation: DAG schema, ready-derivation, foreground pull, and fixture suite with mocked provider child state as listed above.

Prevention: provider graph owns open/blocked/closed truth and the DAG persists no status, so no second progress channel can drift.

Behavior: DAG suite (49) green on the integrated tree — a channel distinct from CLOSED and the carrier.

AI-provenance: agent-authored from verified lane receipts wi-8b-dag under Goal Run #844.

Closes #853.
