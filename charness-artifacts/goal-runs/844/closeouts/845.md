# WI-1 closeout: completed

## Evidence

- Kind enum plus exit-code map in `scripts/task_run/task_run_state.py`; kind plus stable blocker emission in `task_run_completion.py`; `_terminal()` hunk in `scripts/task_run/task_run.py` so interrupted/failed receipts carry the envelope; exit codes 0-5 in `task run --help` plus regenerated `docs/cli-reference.md`; event-schema v1 in `scripts/task_run/task_run_events.py`.
- `tests/charness_cli/test_task_run_exit_codes.py` (new): 22 passed on the integrated tree; related suites `test_task_run_result.py` + `test_task_run_completion.py`: 50 passed.
- Changed-line gate clean on the final candidate; docs length gate held (page condensed to stay within budget).
- Commits: lane branches `task/wi-1-result-kinds-2` (rebased) and `task/wi-1-result-kinds-4` fast-forwarded, plus parent regen commit.
- Lane path: no-progress stop on contract-heavy reading (relaunched with 1200s budget), typed scope-extension for `task_run.py` (approved), stale-base cherry-pick refusal (parent rebased instead), generated-surface stop at `cli-reference.md` (parent regenerated). Each was the designed behavior, not a failure.

Classification: feature

Jtbd: as the goal integrator I need one typed verdict vocabulary with one exit code per result kind so scripting around lanes stops parsing YAML to tell broken from rebase-it.

Boundary: envelope, exit map, `_terminal` coverage, help text, event-schema v1 freeze. No executor probe, steer, premise gates, review, train, DAG, ledger, metrics, cache, or friction work.

Resolution Brief: land kinds plus codes 0-5 with prose rendered from receipt facts and event-schema v1 frozen for producers.

Implementation: envelope and exit mapping across state, completion, CLI, and `_terminal`, with exit-code tests and regenerated CLI reference; completes WI-1 with commits above.

Prevention: kind/exit vocabulary frozen first so every later slice reports through it; extension requests approved explicitly with same-candidate re-validation instead of relaunch.

Behavior: repo `./charness task run --help` prints exit codes 0-5; 22 exit-code tests plus 50 related tests green on the integrated tree — a channel distinct from the CLOSED state and the carrier body.

AI-provenance: agent-authored from verified lane receipts wi-1-result-kinds-2/3/4 and the integrated parent-tree test runs under Goal Run #844.

Closes #845.
