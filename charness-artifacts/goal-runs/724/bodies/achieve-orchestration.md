<!-- charness-work-item-key: achieve-orchestration -->
# Orchestrate Research Through `/goal #N` Pickup

## Purpose

Make `achieve` research and draft before approval, ask only consequential
questions under an adapter-controlled ceiling, then recover an approved Goal
Run through its frozen binding and real provider graph.

## Bounded contract

- Preserve the full approved local Goal Draft and never use it as a mutable
  progress mirror.
- Require structured interview decisions with options, tradeoffs,
  recommendation, reason, answer, and rejected alternatives.
- Resolve `/goal #N` against the current repository, read the parent and binding,
  refuse stale or incomplete identity chains, and select only an executable open
  child from provider state.
- Keep provider-unavailable fallback planning-only unless the adapter explicitly
  permits it; the bootstrap marker is not target-runtime readiness.

## Acceptance and verification

Run the interview-cap, approval-boundary, warm-start, stale-binding, incomplete
child, clean-process pickup, and compatibility tests through the repo-owned
standing runner. Prove that `/goal #725` is refused because a child is not a
Goal Run.

## Evidence boundary

This child does not infer live establishment from local artifacts, close #724,
or mutate installed host state. The target `/goal #724` pickup roundtrip is
verified in a clean Python process; that proves the local/provider read path,
not child completion, parent completion, installed-host behavior, or issue
closure.

## 2026-08-27 efficiency-first synchronization

The existing interview, compatibility, binding, and pickup lifecycle passed its
focused proof: 18 tests covering interview cap/approval, stale and incomplete
identity refusal, clean pickup, and pre-activation behavior. The current live
`/goal #724` readback selects `backlog-708` at cursor revision 2 with zero child
issue reads, so the routine path consumes the parent cursor rather than
rescanning the graph.

This is a local/provider read-path claim only. Hosted, installed, release, and
consumer-repository behavior remain explicit non-claims; this synchronization
does not itself close #727.
