<!-- charness-work-item-key: dogfood-724-establishment -->
# Bootstrap, Then Prove #724 As The First Goal Run

## Purpose

Reconcile the approved #724 parent and complete 31-child graph after approval,
while preserving the premature bootstrap history and recording the verified
one-time `bootstrap_verification: verified-target-roundtrip` boundary.

## Bounded contract

- Freeze and bind the approved draft only after exact parent/provider readback.
- Rewrite/reuse #725–#727 with executable managed bodies, create the two missing
  system children, preserve the three closed issue bodies/evidence, and prepare
  all 23 open backlog addenda.
- Add only missing approved relationships and remove only manifest-excluded #3.
- Read every managed body, issue identity, state, URL, and parent relationship
  back; persist a typed bootstrap observation.
- Keep the parent OPEN. The final `/goal #724` pickup is proven by a clean
  process after all four system capabilities were exercised; this does not
  claim child completion, parent completion, or installed-host adoption.

## Acceptance and verification

The exact 31-child graph, managed body digests, closed-evidence dispositions,
binding hash, and parent metadata must agree through the provider boundary.
Unverified or partial results stop and remain retryable.

## Evidence boundary

This bootstrap child does not close any child or parent, push/release/tag, mutate
the installed host, or substitute local/fake proof for live readback.

## 2026-08-27 efficiency-first synchronization

The establishment evidence is now aligned with the current parent rather than
the stale bootstrap counts. A separate live readback confirmed #724 OPEN with
31 direct children, 14 CLOSED, 17 OPEN, and cursor revision 2 selecting
`backlog-708` (`#708`). The parent-only pickup returned `child_reads: 0`; its
provider and binding identities matched the frozen run.

This update records establishment/readback only. It does not claim that #725 is
closed, that the parent is complete, or that installed, hosted, release, or
consumer-repository behavior has been verified.
