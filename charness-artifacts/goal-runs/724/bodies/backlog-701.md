<!-- charness-work-item-key: backlog-701 -->
# Existing Work Item #701 — Fixed-point claims review

## Purpose and premise

Define a claims-review bundle whose input excludes artifacts describing that same
review, or converges on a bound second identity. Re-read current bundle assembly
and preserve the prior timeout as unproven evidence.

## Owned change and acceptance

Candidate identity and convergence are deterministic; a self-describing bundle
refuses or binds a second review identity rather than accepting its own verdict.

## Verification and evidence boundary

Run `python3 scripts/run_standing_pytest.py --repo-root . --mode read-only --pytest-target tests/quality_gates/test_claims_review_scope.py`, then changed-line proof. No release planning claim is made here.
