# Goal Run `goal-run-provider` provider cutover

## Scope

- Work item: `goal-run-provider` / issue `#726`
- Parent: `corca-ai/charness#724`
- Implementation target: `35240a200ea77e82a64d9e719d9ae14f2f2e5518`
- Previous implementation base: `71778264d30c7b0ec502e8a554c7debb2f66632f`
- Ownership: adapter-resolved Goal Run provider operations and guarded close

## Local verification

- Standing provider target through `run_standing_pytest.py`: `8 passed`.
- Combined provider/runner/pickup/binding/lineage target: `65 passed`.
- Provider py_compile, Ruff, skill-contract/evaluation, bootstrap-shim,
  standalone-import, mirror-drift, and boundary-bypass checks passed before
  the implementation commit.
- Narrow proof worktree: `/tmp/charness-726-narrow-proof-20260827` on named
  branch `proof/issue-726-provider-narrow-20260827`, created at the target SHA
  with `charness worktree create --prepare`; doctor reported isolation pass and
  the worktree had no tracked or untracked changes before and after proof.
- Narrow proof path scope: the guarded-close open-child refusal and generic-close
  Goal Run carrier refusal in `tests/quality_gates/test_issue_goal_run.py` —
  `2 passed`; provider py_compile and Ruff passed; canonical source and checked-in
  plugin mirror were byte-identical. `PYTHONPYCACHEPREFIX`, `COVERAGE_FILE`, and
  pytest basetemp were outside the proof worktree.

## Live readback

- `goal-run-preflight` against #724: `status: ready`, `outcome: verified-read`,
  `mutation_invoked: false`; all nine Goal Run operations and all ten backend
  operations were available with no template errors.
- `goal-run-read` against #724: parent OPEN, exact graph `31` children, `3`
  completed, `28` open.
- The body update operation for #726 is recorded separately by the immutable
  started/terminal observations generated by the provider.

## Changed-line policy disposition

The repo-wide changed-line mutation wrapper was intentionally not used as a
blocking gate for the whole provider implementation. That would contradict the
current friction-reset direction, which reserves changed-line proof for narrow
verdict-logic/proof-surface checks. Its earlier clean-worktree attempt returned
`status: blocked` because it demanded coverage for the entire seven-file provider
pool and found both uncovered lines and mapper-unmapped CLI/guard files. That is
retained as an explicit non-claim, not converted into a provider defect or a
false clean result.

## Boundary

This receipt proves the local provider contract, the selected guarded-close
refusal surface, and the live read/preflight boundary. It does not prove issue
closure, scheduler changes, hosted or installed-host enforcement, consumer
repository adoption, remote CI, push, release, tag, or a clean `/goal` pickup.
The user-directed implementation path omits forced fresh-eye, handoff, and
micro-slice rituals. #724 and #726 remain OPEN.
