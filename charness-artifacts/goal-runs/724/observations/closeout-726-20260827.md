Closes #726

JTBD: route issue operations through the selected provider and manage real
Goal Run tracker state without duplicate provider selection or silent fallback.

Boundary: Charness-owned provider selection, Goal Run body operations, and typed
readback are in scope; push, release, installed-host, scheduler, and consumer
repository enforcement are not.

Resolution brief: keep one adapter-resolved backend boundary with file-backed
Goal Run operations and explicit verified outcomes.

Implementation: provider selection, Goal Run operations, fake-provider coverage,
and focused task/pickup proof are recorded in
`charness-artifacts/goal-runs/724/bodies/goal-run-provider.md` and the target
implementation commit.

Prevention: routine pickup reads the parent cursor once; explicit sync owns graph
reconciliation; uncertain writes remain typed rather than being retried blindly.

Behavior #726: local-only-by-contract — focused provider and Goal Run tests
exercise selection, update, readback, and refusal paths; no installed or hosted
adoption is claimed.

Critique #726: blocked explicit operator direction omits forced fresh-eye review and this host exposes no Agent/subagent capability
AI-provenance: authored by an agent session.
Manual fallback reason: operator-directed-manual-close.
