Closes #710

JTBD: normalize advisory scope paths without silently widening or silencing the
advisory.

Boundary: Charness-owned local advisory behavior is in scope; ratchet policy,
hosted behavior, installed-host behavior, and release behavior are not.

Resolution brief: keep the advisory-only policy and route membership through the
canonical normalized scope resolver.

Implementation: the source/plugin implementation and focused regression evidence
are recorded in `charness-artifacts/goal-runs/724/bodies/backlog-710.md` and the
target implementation commit.

Prevention: retain one canonical scope resolver and regress whole-tree, nested,
mixed literal/glob, missing-metadata, and import-failure paths.

Behavior #710: local-only-by-contract — the focused advisory suite exercises the
normalized membership boundary; no hosted or installed-host behavior is claimed.

Critique #710: blocked explicit operator direction omits forced fresh-eye review and this host exposes no Agent/subagent capability
AI-provenance: authored by an agent session.
Manual fallback reason: operator-directed-manual-close.
