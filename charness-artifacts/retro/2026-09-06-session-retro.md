# Goal 798 consumer improvement session retro

Date: 2026-09-06

## Context

Autonomous North Star improvement for Goal #798: contract ownership, honest
release advisory semantics, two installed-package consumer journeys, and
observed workflow friction. Publication is still pending at this checkpoint.

## Window

Planning at `be60aba4b62bfbb9a8bc385227b9db9fa0581c23` through consumer
completion at `44cd33060026fe4fe603632df1ef24677913fcda` and the final
quality wording/packet-selection repairs. This is a bounded narrative retro;
no parent-session token, turn, or tool-call efficiency metric is claimed.

## Evidence Summary

- `goal798-session-packet.md`: committed changed-surface packet and provider
  rework read; the two observed rework issues are closed #774 (retro, achieve)
  and #773 (achieve, issue), not newly caused by this session.
- Frozen protocol and actual baseline/candidate records under
  `../probe/2026-09-06-consumer-journeys/`; both fixtures pass their frozen
  acceptance checks. CLI repeated work declines, alias spec→impl repeats
  increase. This is not general speedup proof.
- Three debug records dated 2026-09-06 own advisory meaning, preview/live
  collision, and staged-owner universe mismatch; their causal ledgers already
  exist and are not duplicated here.
- Initial integrated standing suite passed 9,032 tests. The later full gate
  reported 80 pass, 3 fail, 5 not-run: two failure causes were a required
  quality phrase removed during wording cleanup and a function-statement
  limit. Focused repaired packet/path tests passed 31; core skill contracts
  passed. Final integrated reruns and release proof are still required.
- `integration-filename-refusal.json` preserves the failed coverage producer:
  evidence-copy filenames violated the existing snake_case convention.

## Waste

Artifact-only staging exposed a mismatch between the cheap hook's broad Python
selection and the length owner's narrower universe. The owner now selects its
own targets. Late broad verification also found two cheap static failures that
could have been caught before the expensive run. Earlier truncated tool output
required bounded recovery; later raw returns were captured at sufficient size.
These are observed avoidable operations, not an inference from total duration.

## Critical Decisions

- Freeze fixtures, oracle and intervention policy before either arm; reject
  seed false positives instead of tuning the oracle after seeing performance.
- Export candidate bytes from a named commit, keep fresh consumer tasks and
  distinguish package version from exact source identity.
- Keep repeated-read regression visible; guidance does not guarantee agent
  compliance, and lower elapsed time is descriptive, not causal evidence.
- Reuse the length owner's universe and packet owner instead of adding gates.
  Preserve distinct review and publication readback at irreversible boundaries.

## Trends vs Last Retro

The 2026-09-05 retro also identified gates repaired serially. This session again
paid that cost, so the next action is earlier use of existing cheap owners,
not another validator. Provider rework attribution remains achieve/issue/retro
for the two closed issues above; this packet establishes no new rework trend.

## North Star Alignment

The CLI consumer completes the same requested behavior with fewer repeated
executable checks, while alias behavior remains correct but repeated work does
not improve. Preview→live review no longer requires an artificial new attempt
label after a preview. Contract routing clarifies when existing evidence is
applicable without weakening fresh final-consumer proof. Capability preservation
and honest non-claims matter more than a universally positive headline.

## Expert Counterfactuals

Engelbart's system-improving-itself lens would co-design the method and tool:
name the canonical path universe before building a wrapper; bind a preview to
an executable continuation before presenting it; check cheap filename, lint and
contract owners immediately after edits. This changes the next command and
prevents unnecessary broad reruns. It does not eliminate release-boundary proof.

## Next Improvements

- workflow: run the existing cheap owners on each changed source or evidence
  family before the broad integration gate; retain failed receipts honestly.
- capability: applied owner-universe reuse, non-consuming preview identity,
  and applicable-evidence guidance; no new evaluator platform.
- memory: preserve this comparison, including the alias regression, so a
  later improvement starts from measured behavior rather than a speed slogan.

## Sibling Search

- same layer: scripts/hooks/check_staged_cheap_owners.py | decision: same waste, fix now | proof: artifact-only, mixed source/artifact, and custom owner-universe tests pass; owner selector reused
- abstraction up: docs/operating-contract.md Verification | decision: same waste, fix now | proof: integrated contract routes applicability and distinct-boundary exceptions to one owner
- specialization down: skills/public/impl/SKILL.md | decision: same waste, fix now | proof: commit-bound installed consumer comparison shows preserved capability and bounded reuse guidance; no universal speed claim
- mental-model siblings: release publication and claims review | decision: intentional boundary | proof: release owner requires distinct observation of prepared claims and public readback; these are not duplicate local checks

## Portable Candidate

Abstract pattern: reuse unchanged evidence only while its inputs and applicability
remain unchanged. Trigger: frozen installed consumer traces repeated unchanged
reads/checks. Consumer shape: ordinary repository implementation. Destination:
create-skill; applied through `../create-skill/2026-09-06-impl-evidence-reuse.md`.
First-prompt acceptance: the requested behavior passes the independent frozen
fixture without an operator rescue; both candidate fixtures do. Reduced repeated
work is a separate, mixed result and remains so.

## Packet Consumed

`charness-artifacts/retro/goal798-session-packet.md` (JSON sibling); both configured
sections read. The packet stops at 44cd330; later focused results above come from
the current task's actual command receipts, not from that earlier snapshot.

## Persisted

Persisted: yes: charness-artifacts/retro/2026-09-06-session-retro.md
Seeding: none pending
