# Issue 294-298 Closeout Retro

## Context

Resolved Charness issues #294, #295, #297, and #298 through direct-to-default
carrier commits, with #296 kept as already-closed context. The final push
verified all four close-intended issues as `CLOSED`.

## Window

- Goal: `charness-artifacts/goals/2026-06-04-issue-294-298.md`
- Carrier commits: #294 `32f4d360`, #295 `856bfdc9`, #297 `ab5f3c63`, #298
  `6442011a`
- Lifecycle repair commit: `4d2c2579`

## Evidence Summary

- Broad pytest passed: `2151 passed, 4 skipped, 6 deselected`.
- Pre-push full quality passed after repair: `70 passed, 0 failed`.
- GitHub state verifier returned `status: verified` for #294, #295, #297, and
  #298 with `--expect-state CLOSED`.

## Waste

- Pre-push found debug seam-risk enum drift after the issue carriers were
  already locally verified.
- The boundary-bypass ratchet caught subprocess-style quality tests late in the
  publish phase.
- Repeated carrier verification and pre-push retries added overhead that would
  have been cheaper before the first push attempt.

## Critical Decisions

- Kept scripts as facts producers and agent judgment as the semantic decision
  layer for #298.
- Used direct commits instead of PRs, with close keywords only on the natural
  carrier commits.
- Treated post-carrier goal/retro updates as lifecycle/audit artifacts, not
  issue-resolution carriers.

## Trends vs Last Retro

The broad-test and VCS overhead pattern persisted, but #295 improved the
pre-lock vs final-lock proof language. The new waste was mostly validator
surface mismatch: one validator accepted debug artifact prose that another
quality index rejected.

## Expert Counterfactuals

- W. Edwards Deming: run the same publish-blocking system gate earlier when a
  slice creates current debug artifacts or subprocess-style tests.
- Barbara Minto: put issue carrier publication and lifecycle/audit publication
  in separate buckets before writing closeout prose.

## Next Improvements

- Applied: run `build_debug_seam_risk_index.py --check` for slices that create
  or edit current debug seam-risk artifacts.
- Applied: prefer in-process test APIs for import-safe repo scripts unless the
  subprocess boundary is the behavior under test.
- Applied: keep final lifecycle/audit commits free of close keywords after the
  issue-resolution carriers have already been verified.

## Persisted

This artifact is cited by the goal closeout. No new external issue was filed;
the improvements above were applied in the lifecycle repair and final goal
closeout.
