# Goal 805: capability improved, delegation value not established

Date: 2026-09-06

## Context

Goal #805 asked whether Charness makes long-task delegation better, not whether
more workflow machinery can be shipped. Strong local evidence supports the
bounded carrier/scheduling changes. The actual pilot does not establish an
efficiency benefit. This required retro preserves that distinction and the
operator's less-but-better steering; it opens no new implementation scope.

## Window

Approved Goal activation through the 8.5.0 release, installed observation and
implementation-carrier pre-publication repairs.
Issue and guarded parent closeout are subsequent operations, not claimed done
by this record. The parent session's complete time/token cost was not separately
instrumented here; pilot process costs and release gate costs are measured.

## Evidence Summary

- Frozen original four-cell results, manifests and trace/evaluator observations:
  `../probe/2026-09-06-delegation-net-value/results.md`.
- Local rework and sequencing: `../goal-runs/805/reviews/integration-observations.md`
  and the code/forward-consumer dispositions beside it.
- Actual prepare/final invocation records:
  `../probe/2026-09-06-release-scheduling/observations.md`.
- Published 8.5.0, claims review and machine install readbacks:
  `../release/latest.md` and its observer record. These are distinct from tracker
  state; no new-session host loading is claimed.
- The retro packet reports #810 attributed to issue/critique, plus earlier
  #773 (achieve/issue) and #774 (retro/achieve). Its attribution counts are not
  a count of all session rework, and those older issues are not newly caused here.

## Waste

Verification was the intended phase after the implementation lock. The operator
had to steer the run back to observing the frozen candidate because preparation
and evidence handling delayed the comparison. That is a real intervention,
separate from the pilot's zero unscheduled semantic rescues.

- Executing-agent judgment: cheap length and command-expression failures were
  discovered after broad execution. Two direct Markdown invocations expanded
  through the configured global glob; both were stopped, not recorded as pass.
  The existing `--no-globs` option was the correct narrow next command. This is
  repeated selection error, not evidence that a new linter is needed.
- Repository development rules: standalone standing tests followed by a full
  lane containing pytest overlap in protected behavior. The rule required both;
  complete input/verdict equivalence was not established. Their count alone does
  not justify deleting a boundary. The late cheap failures were still avoidable.
- Distributed skill behavior: the pilot candidates read additional skill
  instructions and produced additional records. Some HOTL/Prove/quality/achieve
  selections were inapplicable to the local task. The traces establish that
  overhead, not whether skill text or the executing agent alone caused each
  selection. General plugin attribution remains contested.
- Gate-baseline runtime: actual changed-line producers cost 145.050s in prepare
  and 151.820s in final resume. These are substantial repo-owned producer costs,
  not automatically necessary merely because they passed. Their distinct
  stage inputs and required boundary remain; no reuse proof exists for them.
- Observation overhead: the rejected broad syscall probe produced 5.7 GB and
  was stopped after about eight minutes. The low-candidate isolation leaked an
  unfrozen installed helper. This is experimental launcher/observer work, not a
  producer failure. The invalid original cell is retained without a tuned rerun.
- Closeout execution rework: the parent launched a 134.7s five-target review
  without the consumer-required `issue-resolution` prefix. Actual draft refusal
  caught it. After an operator pause and explicit permission, corrected delivery
  cost another 131.9s and passed draft plus commit-msg. Required pre-push then
  rejected a runtime-only source citation; selected original rows were preserved
  durably, without rerunning the measured subject. This is late cheap validation
  and evidence packaging overhead, not a claimed live A1 first-pass success.
- The first durability comparator incorrectly compared plain content SHA256 to
  domain-separated file/mode identities and reported all sixteen inputs changed.
  Parent stopped that worker after 28.9s before verdict. Calling the existing
  hash owner and asserting the expected change set established fifteen unchanged
  inputs and only the two-line citation change. The corrected narrow review uses
  that result. This error belongs to parent judgment; no new hashing abstraction
  or rewritten experiment was needed.

## Critical Decisions

- Rejected cross-commit test-result reuse after observing live Git and excluded
  evidence reads. Scheduled the standing subject suite once at final state
  instead; no cache or universal equality claim was introduced. Preparation
  remains explicitly partial, and final current proof still runs.
- Kept exact bundle identity and classification ownership with the issue owner.
  Composed controls exposed false greens that isolated parsers missed. The
  current mixed carrier must directly consume its delivered review, with each
  issue's existing floor retained; no formatting-only semantic rerun is owed.
- Froze candidate/protocol before the four cells and preserved unfavorable
  results. The valid high pair passed the same acceptance but candidate time
  was 33.4% higher; the low pure-candidate comparison is invalid. Choosing not
  to tune the protocol constrains the release headline to capability, not value.
- Retained prepared-claims, public and installed observation. These answer
  different questions from code review. The mutable notes link was repaired only
  after the actual cheap publication refusal, with one narrow binding follow-up.

## Trends vs Last Retro

The earlier same-day Goal #798 retro already named late cheap gate failures and
mixed consumer reuse results. Both patterns recurred. More recorded lessons did
not prevent the executing agent from selecting an over-broad Markdown command.
That recurrence is not counted as successful conversion merely because it is
documented again. The #798 historical record is left unchanged.

## North Star Alignment

Actual behavior, distinct observers and unfavorable evidence preserved the
governing standard's proof honesty. Efficiency was mis-applied when evidence
handling became a prerequisite-shaped backlog ahead of the actual user-value
comparison. Tool/record count is not a product outcome. The valid comparison
shows preserved completion, not improved delegation cost; total lifecycle net
benefit remains unestablished even though one initial suite invocation is gone.

## Expert Counterfactuals

Engelbart's method-and-tool lens changes the next action: at the locked seam,
state the exact consumer question, use its existing cheapest owner, then execute
the frozen comparison as soon as readiness holds. In this run that means scoped
length/CLI checks before broad tests, `--no-globs` before Markdown, and a bounded
input-read sample before an amplified syscall trace. It does not mean deleting
current publication or installed proof to improve a timing number.

## Sibling Search

- same layer: scripts/run_quality_engine.py and release common orchestration | decision: same waste, fix now | proof: exact pytest-release deferral in preparation and one actual final suite; incomplete receipt refusal controls retained
- abstraction up: docs/development.md and the authoring integration rule | decision: diagnostic-only | proof: standing/full overlap observed, but complete verdict equivalence not established; no rule rewrite authorized
- specialization down: scripts/mutation/release_changed_line_coverage.py | decision: valid follow-up outside the slice | proof: two measured producer costs with distinct stage inputs | follow-up: deferred charness-artifacts/retro/2026-09-06-session-retro-2.md#next-improvements
- mental-model siblings: prepared claims, public readback and installed consumer probes | decision: intentional boundary | proof: they ask record-honesty, public-state and installed-behavior questions that code reading cannot answer

## Next Improvements

- workflow: use the existing scoped owner before broad execution; do not restart
  completed review when only formatting changed. Structural-follow-up
  destination: applied: exact multi-target carrier consumption and final-only
  subject-suite scheduling, with the existing final-consumer refusal tests.
- capability candidate, not implementation: first investigate the largest
  remaining measured release producer cost. Structural pattern: instrumented
  coverage producers may repeat equivalent work across lifecycle stages.
  Triggering instance(s): the 145.050s and 151.820s producers above.
  Destination: repo-local. Structural-follow-up destination: none — equality is
  unproved; this is a bounded next-task proposal, not authority to remove a gate,
  add a cache or file a new tracker in this Goal.
- capability candidate, not implementation: before another pilot, mask installed
  Charness helpers and clarify whether added tests must be in new files. This
  repairs the experimental observation boundary, not the original result.
  Structural-follow-up destination: none — the frozen pilot is complete and a
  new experiment requires a new explicit scope decision.
- memory: retain original results, the counted spec-consumer intervention,
  runtime observations and this detection-gap/sibling record. Do not promote
  repeated Markdown selection failure to a successful structural conversion.
  Structural-follow-up destination: none — the existing narrow option already
  works; an added guard has not been justified.
- capability candidate, not implementation: reject an issue-closeout scope
  mismatch before executing the reviewer, within the existing entrypoint.
  Structural pattern: producer-accepted input is refused only by its downstream
  consumer after expensive work. Triggering instance(s): the wrong scope prefix
  above. Destination: portable. Structural-follow-up destination: none — this
  requires a separately scoped producer/consumer contract change; the current
  release is not reopened to repair parent launch mistakes.

## Portable Candidate

Pattern: use known applicable context and verify only changed assumptions while
retaining current irreversible-boundary proof. Existing spec/debug/retro edits
already carry that pattern; destination `create-skill` needs no additional
invocation here. First-prompt acceptance was mixed: the spec forward consumer
needed an intervention and the high pilot gained no acceptance or efficiency.
No general portability or pure low-tier benefit is established.

## Packet Consumed

`charness-artifacts/retro/goal805-session-packet.md`; both changed-surface and
provider rework sections were consumed. The committed range ends at
`bd3e677dd4cdbea0253d462e7ea42ae18d83c946`; later closeout state is not inferred.

## Persisted

Persisted: yes: charness-artifacts/retro/2026-09-06-session-retro-2.md
Seeding: none pending
