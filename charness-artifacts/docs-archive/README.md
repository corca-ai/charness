# Retired documentation

Pages moved here from `docs/` are dated records, not current contracts. Each
file starts with a `> Status: retired <date>` line and names its current owner
page when one exists. See [artifact policy](../../docs/artifact-policy.md) for
why history lives under `charness-artifacts/` instead of `docs/`.
