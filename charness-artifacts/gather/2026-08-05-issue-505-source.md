# Gathered Source: Issue #505

## Source

- Canonical source: https://github.com/corca-ai/charness/issues/505
- Source identity: `github:corca-ai/charness#505`
- Captured: 2026-08-05
- Access mode: authenticated `gh issue view` direct CLI

## Knowledge Capability

Carry the problem-first contract for the next Charness goal: identify and
reduce structural runtime waste on the real closeout critical path while
preserving changed-line coverage, failure visibility, boundary contracts, and
the existing proof floor.

## Route / Selected Attempt

- The repository-owned public URL route was attempted through
  `gather_public_url.py`; it stopped at a CAPTCHA with `final_status: captcha`,
  `final_confidence: none`, and no durable record from that route.
- The declared GitHub-specific `gh` route was not implemented by that helper.
- The source was then read through authenticated `gh issue view`, which
  returned the issue's structured title, state, and body. This is the captured
  source used below; the public-fetch failure remains an explicit gap.

## Captured Source Facts

- Title: Make mutation and quality-gate runtime actionable without weakening
  proof floors.
- State at capture: OPEN.
- Three matched local quality runs measured a 123.51s median total runtime and
  a 120.2s median changed-line mutation phase.
- The CLI family measured only 3.62s and is not a justified migration target.
- The intended outcome is a reproducible, proof-preserving way to identify and
  reduce structural runtime waste on the real closeout critical path.
- Explicit non-goals are mutation/coverage proof removal or weakening, file
  pattern test pruning, a repo-wide in-process CLI harness, and treating a
  passing terminal command as remote/public proof.
- The proposed starting direction is the current mutation mapper plus a
  phase-level cost/consumer map and one owned structural family.

## Open Gaps

- This record does not claim a public unauthenticated fetch; that route was
  CAPTCHA-blocked.
- Issue state and body should be re-read through the GitHub adapter before any
  issue-close or remote-state claim.
