# Critique: 291 292 284 final bundle

## Execution

Fresh-eye bounded critique ran through three parent-delegated subagents:

- Franklin: implementation correctness and regression risk.
- Banach: closeout, artifact, generated-surface, and validation risk.
- Raman: counterweight pass against over-costly concerns.

## Fresh-Eye Satisfaction

parent-delegated

## Target

Code and closeout critique for the bundle resolving #291, #292, and #284.

## Change

- #291: `achieve --pursue-ready` now blocks unresolved consequential
  activation discussion instead of returning warning-only success.
- #292: standing quality tests no longer exercise `git write-tree` against the
  shared parent repo index; the staged mirror gate itself remains intact.
- #284: skill-surface edits have a cheap pre-edit helper that reports SKILL.md
  total/core headroom and known coupling hazards.

## Act Before Ship

- Update durable goal state before claiming completion: status, final
  verification, auto-retro, and #284 slice commit fields were stale during
  review.
- Produce a closeout carrier with `Close #291. Close #292. Close #284.` plus
  the issue ledger and verify GitHub state after push.
- Rerun broad read-only quality after the bundle is locked.

## Bundle Anyway

- #291's readiness split is the right boundary: shape-ready and activation-ready
  are different consumer contracts, and tests cover surfaced-unresolved versus
  explicitly resolved discussion.
- #292 preserves production staged-index behavior while isolating test access to
  a private repo copy.
- #284 is correctly scoped as an authoring preflight, not a replacement for
  broad quality. Optional targeted checks are enough for the current issue.
- Plugin mirrors were synced for the changed public skill and new helper.

## Over-Worry

- Do not require release, installed-machine, scheduled CI, or live host proof
  for this bundle; the goal records those as non-claims.
- Do not make activation discussion resolution semantic or ML-classified. The
  explicit `RESOLVED` / `CONFIRMED` / `APPROVED` marker is a deterministic floor.
- Broad pytest cost is a cadence concern, not a reason to skip final broad proof.

## Valid But Defer

- A broader static inventory for all future index-sensitive tests would be
  useful, but #292 fixed the known flake and added an exact recurrence guard.
- Installed plugin/cache dogfood can be proven in a later release/update slice.
- `achieve/SKILL.md` is at core 160/160, so future additions should move to
  references or trim first.

## Follow-Up Disposition

- applied: `3b7ed973` marked the copy-heavy isolated repo-copy test
  `release_only` and converted #284 tests to the in-process preflight API after
  the first broad gate exposed those closeout issues.
- applied: final broad quality rerun passed after the test-structure repair.
- no new issue filed: broader index-sensitive inventory and installed-host
  proof are valid future improvements, but they are outside this bounded issue
  bundle and already recorded as residual non-claims.
