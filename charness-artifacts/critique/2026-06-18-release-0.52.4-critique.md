# Release Critique — charness 0.52.4 (patch)

- Date: 2026-06-18
- Target reference: `release-critique`
- Release: 0.52.3 → 0.52.4 (patch)
- Contents: issue #386 fix (non-terminal issue-bundle closeout — achieve/hotl
  reference prose + the design north star `docs/design-north-star.md`, commit
  6e795f61) + RCA ledger event (commit 8bdb847f). #386 already verified CLOSED.

## Execution

- Fresh-Eye Satisfaction: parent-delegated
- Reviewers: 1 release-surface angle subagent (surface-lock + bump correctness +
  publish boundary + repo-internal-doc risk) + 1 separate counterweight, each a
  bounded fresh-eye subagent. The achieve/hotl DESIGN was deliberately
  out-of-scope (covered by the separate resolution critique
  `2026-06-18-issue-386-north-star-pilot.md`); this short pass reviewed the
  release surface only, proportionate to a prose patch shipping already-critiqued
  work.

## Reviewer Tier Evidence

- **Requested tier**: `high-leverage`
- **Requested spawn fields**: none sent — this Claude Code run spawned each
  bounded reviewer via the Agent tool with `subagent_type: general-purpose` and
  no model/effort override; the adapter `reviewer_tiers` mapping was not
  forwarded through the Agent spawn interface.
- **Host exposure state**: `host-defaulted`
- **Application state**: `unverified-by-host` (the host does not confirm applied
  spawn fields back to the parent; reviewers ran with the host default reviewer).

## Surface-Lock Inventory

- `packaging/charness.json`, `plugins/charness/.claude-plugin/plugin.json`,
  `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`: all at
  `0.52.3`, `current_release.py` reports `drift: []`, tree clean at HEAD
  8bdb847f. The publish helper bumps + syncs all four atomically to 0.52.4
  (aborts on any post-bump drift). Latest tag v0.52.3, so 0.52.4 is the next
  free tag.
- Repo-internal doc: `docs/design-north-star.md` is source-repo-only (confirmed
  absent from `plugins/` export). Every vendored reference to it in the achieve
  `lifecycle.md` and hotl `ledger-and-dispositions.md` mirror carries the
  explicit `authoring-repo-internal` marker, so vendored consumers are not
  pointed at a missing export path. Source↔export prose is byte-aligned.

## Bump Verdict

**PATCH (0.52.4) is correct** — unanimous across the angle + counterweight.
Issue #386 is a bug fix (behavior repair): the issue-bundle closeout reviewer
gained a per-issue distinct-channel mandate so deployment readback + `CLOSED` can
no longer stand in for behavior proof. The change ships **no new command, gate,
skill, flag, always-rendered section, or export surface** — it is reference prose
plus a repo-internal governing doc. This matches the #385/#383 prose-fix patch
precedent and is narrower than #381 (an always-rendered new section shipped as a
minor). The public invocation shape is unchanged.

## Counterweight Triage

- **Act Before Ship** → none. The publish helper's atomic bump+sync+gate+
  fresh-checkout+`gh release view` chain owns every real release-surface risk.
- **Bundle Anyway** → done. The RCA ledger event and both critique artifacts are
  already on `main`; the release ships them as-is.
- **Over-Worry** → rejected: "needs minor" (no consumer-facing capability);
  "north star not shipped to consumers" (repo-internal by design — do NOT vendor
  a governance doc into the export); "fresh-checkout/real-host proof" (prose
  patch; the helper runs fresh-checkout probes anyway); "re-review the design"
  (the separate premortem owns it).
- **Valid but Defer** → Direction-3 (verify-closeout HOTL refusal) and the
  generalized per-unit-disposition abstraction — already deferred in the commit
  body and the resolution critique.

## Verification To Run

- The publish helper's canonical quality gate + generated-surface agreement.
- Fresh-checkout probes are declared in the adapter; run them before tag publish
  and report status.
- Public verification via `gh release view <tag>`; do not claim "published"
  until it verifies.

## Deliberately Not Doing

- Not re-reviewing the achieve/hotl design (separate resolution critique owns it).
- Not bumping minor (verdict: patch is honest).
- Not vendoring the north star into the plugin export (it is repo-internal).

## Next Move

Run `publish_release.py --part patch --execute` with this critique artifact;
report tag/version/URL and the public-verification + fresh-checkout-probe state
honestly in the closeout.
