# Slice B #503 metric report code critique
Date: 2026-08-04

## Execution

Executed as a parent-delegated fresh-eye code critique with three bounded
angles (Jackson problem framing, Weinberg boundary diagnosis, Gawande
operations) and one separate counterweight. A second bounded repair-read
review then read the repaired verdict surface. The reviewed change is the
opt-in detail report in the retro miner, its generated plugin mirror, focused
tests, and the adjacent reference correction.

## Decision Under Review

Whether an opt-in --detail mode for the existing closeout-telemetry miner is
the smallest reversible intervention that turns the recurring #503 signal into
a rerunnable owner decision without changing the emitter schema, gate
blocking posture, or proof scope.

## Diff Scope

The source miner audits the currently readable schema-1 stream, reports
retained population/window/status counts and per-cohort elapsed summaries, and
preserves explicit unknowns. The plugin mirror is regenerated byte-identically;
focused tests cover default compatibility, mixed input, missing input, finite
numeric handling, recurrence-threshold schema filtering, parent-record status
counting, elapsed/budget pairing, and mirror parity.

## Failure Angles

- Jackson: counts without a required owner action are prettier telemetry, not
  an actionable #503 surface.
- Weinberg: the miner is the correct read-only derivation layer, but it cannot
  infer historical runner/profile or assign a gate implementation owner.
- Gawande: a missing, malformed, foreign, unsupported-schema, or non-finite
  input must not render as a clean zero; the first-use detail receipt must
  label its retention and provenance boundaries.

## Counterweight Pass

- Act Before Ship: bind detail to the #503 decision record and a retained-window
  recurrence reopen trigger; audit input classes before deriving denominators;
  reject non-finite elapsed values; preserve status/provenance/rotation
  non-claims in the output itself.
- Bundle Anyway: lock default output compatibility, run the CLI against a mixed
  fixture, and verify the generated mirror rather than duplicating behavior
  suites.
- Over-Worry: do not add a new telemetry schema, global ranking, runner/profile
  inference, CI relocation, gate, or backup recovery consumer.
- Valid but Defer: a proof-preserving optimization experiment still needs a
  named lower-layer seam and separate correctness channel; this report does
  not claim to supply either.

## Structured Findings

- F1 | bin: act-before-ship | evidence: strong | ref: skills/public/retro/scripts/mine_closeout_telemetry.py:_read_retained_records | action: fix | note: detail must audit raw input classes before claiming a retained denominator
- F2 | bin: act-before-ship | evidence: strong | ref: skills/public/retro/scripts/mine_closeout_telemetry.py:_finite_elapsed | action: fix | note: NaN and infinity must be excluded from numeric summaries
- F3 | bin: act-before-ship | evidence: strong | ref: charness-artifacts/issue/2026-08-04-issue-503-slice-b-decision.md | action: fix | note: the receipt must carry the owner action and retained-window reopen trigger
- F4 | bin: bundle-anyway | evidence: strong | ref: tests/quality_gates/test_retro_closeout_telemetry_mining.py | action: fix | note: preserve default output and exercise the real detail CLI with mixed statuses and malformed input
- F5 | bin: over-worry | evidence: weak | ref: charness-artifacts/goals/2026-08-04-make-recurring-closeout-cost-actionable.md#Deferred-Decisions | action: defer | note: global ranking, new run identity, and CI relocation have no named consumer in this slice
- F6 | bin: valid-but-defer | evidence: moderate | ref: charness-artifacts/goals/2026-08-04-make-recurring-closeout-cost-actionable.md#Slice-Plan | action: defer | note: optimization experiment waits for a named seam and separate correctness proof

### Round-two repair-read findings

- R2-1 | bin: act-before-ship | evidence: strong | ref: skills/public/retro/scripts/mine_closeout_telemetry.py:mine_detailed | action: fix | note: detail `--recur-min` must aggregate the already-audited schema-1 records, not raw lines that can re-admit schema-2 records.
- R2-2 | bin: act-before-ship | evidence: strong | ref: skills/public/retro/scripts/mine_closeout_telemetry.py:_aggregate | action: fix | note: non-finite elapsed values must not reach the aggregate peak or JSON output.
- R2-3 | bin: act-before-ship | evidence: strong | ref: skills/public/retro/scripts/mine_closeout_telemetry.py:record_status_counts | action: fix | note: parent status counts must count one status per matched retained record even when that record contains multiple matching entries.
- R2-4 | bin: act-before-ship | evidence: strong | ref: skills/public/retro/scripts/mine_closeout_telemetry.py:_elapsed_summary | action: fix | note: elapsed/budget excess must use preserved per-entry pairs and become unavailable when any matched entry lacks a finite pair.

All four repairs were applied and covered by focused regression tests. The
repository's two-round cap makes these round-two repairs accepted-unreviewed;
the final broad proof remains the next independent verification channel.

## Reviewer Tier Evidence

- Requested tier: high-leverage
- Requested spawn fields: model=gpt-5.6-terra, reasoning_effort=medium, service_tier=priority, fork_turns=none
- Host exposure state: requested_fields_sent
- Application state: unverified — the host returned findings but exposed no provider-application confirmation
- Delivery state: findings-received

## Fresh-Eye Satisfaction

parent-delegated — three angle reviewers and one separate counterweight
returned the first findings; the second bounded reviewer read the repaired
surface. All reviewer boundary fingerprints verified clean before parent
writes.

## Reviewed Input Identity

- Packet consumed: charness-artifacts/critique/2026-08-04-slice-b-metric-report-final-packet.md
- Packet path: charness-artifacts/critique/2026-08-04-slice-b-metric-report-final-packet.json
- Packet SHA256: 08d1fa7506ec2edac298be105ce28a14c816050dbac16001f7f6010ac603cf45
- Identity SHA256: 892fe255daa6246d277e0628fd339d12bc49ab7772316f7bcf2309e05bc9b70b

## Boundary Ownership

- Producer: the detail miner derives the read-only population audit and exact
  cohort summaries from the local stream; the emitter/verdict producer remains
  unchanged.
- Consumer: the Charness quality/achieve maintainer operating #503 reads the
  receipt and records retain, bounded experiment, or no-safe-change.
- Owning surface: the miner owns derivation; the goal's #503 decision record
  owns the action and reopen trigger.
- Verdict: moved-to-owner

## Capability Gap

No missing runtime capability. Installed quality, critique, and impl metadata
routed the work to the existing miner/report and generated-surface paths. The
remaining gap is judgment about an optimization seam, deliberately left for
the next decision slice.

## Pre-Merge Action

The implementation must keep the opt-in report explicitly advisory and bind
its use to `charness-artifacts/issue/2026-08-04-issue-503-slice-b-decision.md`.
The focused tests and generated mirror are required before the slice closeout;
no gate weakening is authorized. The second-round repairs are accepted-
unreviewed under the two-round cap and must receive final broad proof.

## Deliberately Not Doing

No new per-run telemetry schema, cross-machine normalization, global ranking,
CI/local proof relocation, automatic gate scheduling, or runner/profile
inference is introduced. Those would exceed the measured local decision
capability and have no named consumer here.
