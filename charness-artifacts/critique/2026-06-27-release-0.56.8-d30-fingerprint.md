# Critique Review

Date: 2026-06-27

Execution: completed via bounded fresh-eye subagents (3 angles + 1 counterweight)
through the host Agent tool.
Packet Consumed: charness-artifacts/critique/2026-06-27-112007-packet.md
Target: references/release-critique.md

## Decision Under Review

Publish charness `0.56.8` (patch bump from `0.56.7`, tag `v0.56.8`) shipping the
`origin/main(b460dc3a)..HEAD` delta = three commits resolving deferred decision D30:
the boy-scout dup-ratchet quality gate and its clone advisory re-key code-clone
newness off nose's offset/path-folding `family_id` onto a gate-computed,
offset/path-independent content fingerprint (`nose_fingerprint_lib`). The gate and
advisory baselines plus the `dup-review.json` reviewed-overlay migrate in lockstep
to the new identity (`code_family_fingerprints`, schema `dup_ratchet_baseline.v2` /
`nose_baseline.v3`, 541 fingerprints), and the `nose` tool floor is reconciled
`>=0.14.0` -> `>=0.15.0`.

## Release Scope

Patch release (behavior repair). The public skill surface, command surface, and the
`dup_ratchet` adapter block shape are unchanged (`evaluate`'s param/verdict names are
deliberately preserved). A pure member-file line-shift no longer false-blocks the
hard arm; a genuine span edit still rotates the fingerprint (no false-negative). The
only consumer-facing migration is a one-time, advisory-safe re-baseline for repos
that opted into the `dup_ratchet` gate with a committed baseline.

## Surface-Lock Inventory

- Version manifests (`packaging/charness.json`, `plugins/charness/.claude-plugin/plugin.json`,
  `.claude-plugin/marketplace.json`): at 0.56.7; the `--part patch` bump moves them to
  0.56.8 as step 1 (helper-owned).
- Plugin export mirror (7 quality scripts + `references/dup-ratchet.md` + `nose.json`):
  byte-identical to `skills/public/...`, committed in lockstep with the impl.
- Quality baselines + overlay: `dup-ratchet-baseline.json` / `nose-baseline.json`
  re-seeded to the lockstep-identical 541-fingerprint set (schema v2/v3, tool_version
  0.15.0, algo 1); `dup-review.json` overlay member-preservingly re-keyed.
- Documentation: `charness-artifacts/release/notes-v0.56.8.md`, `docs/handoff.md`,
  `docs/deferred-decisions.md` (D30 RESOLVED), `references/dup-ratchet.md`.
- Pre-publish: `notes-v0.56.8.md` + this critique artifact + the prepare packet must
  be committed before the helper's clean-worktree gate.

## Failure Angles

- Gawande (operational): surface-lock complete, mirror byte-identical, baselines
  lockstep and committed; a genuine fresh checkout at the release commit runs the
  dup-ratchet phase CLEAN (block=false, never inert) on nose 0.15.0; the consumer
  legacy-baseline graceful-degrade is real in shipped code (legacy key -> None ->
  ok=true/status=degraded, no crash, no false-block). Only gap: the untracked release
  notes + critique packet trip the clean-worktree precondition before the bump.
- Minto (communication): numbers honest (3797 collected; baseline is v2 / 0.15.0 /
  algo 1 / 541 fps; legacy key absent; re-baseline command pair both exist). One
  overclaim: "preserving every operator note" — 6 reviewed orphan entries were
  dropped from the live overlay. And the blanket "never false-blocks" held only for
  the legacy-key path, omitting the nose-version-skew regroup path that can hard-block.
- Raskin/Jackson (interface + bump-level): PATCH is correct per `version-policy.md`
  (bug fix / metadata fix / runtime correction preserving public shape); the schema
  bumps are internal artifact schemas with a no-dual-read advisory degrade, the
  nose-floor bump is metadata reconciliation of stale drift (the v0.56.7 baseline was
  already `tool_version: 0.15.0`), and the consumer re-baseline is non-forced and
  self-healing-to-safe — none meet the minor/major bars.

## Counterweight Pass

A separate skeptical pass triaged every concern. The two Act/Bundle accuracy items
(overlay-note overclaim, nose-skew/floor disclosure) are cheap and real and were
fixed before publish; the clean-worktree gap is the standard commit-the-artifacts
step. Bump-to-minor was steelmanned (the nose-floor bump is the one real break-vector
for a consumer pinned to nose 0.14.x) and self-defeated (the floor reflects already-
shipped 0.15.0 provenance; the gate is opt-in and advisory-safe). The mutation-gate
skip was rejected as a blocker (direct 89-100% line coverage plus a dedicated
guard-coverage commit is a stronger evidence channel than the skipped proxy). A
dedicated release for one internal-quality fix is sound: it removes a recurring
false hard-block that hit the v0.56.7 release itself.

## Structured Findings

- F1 | bin: act-before-ship | evidence: strong | ref: charness-artifacts/release/notes-v0.56.8.md | action: fix | note: "preserving every operator note" overclaimed; 6 already-stale orphan entries (5 with notes) were dropped from the live overlay. Reworded to disclose the drop (notes in git history; still-live notes preserved).
- F2 | bin: act-before-ship | evidence: strong | ref: skills/public/release/scripts/publish_release_cli.py:333 | action: fix | note: Untracked release notes + critique packet trip the publish clean-worktree gate before the bump; commit the three durable artifacts before --execute.
- F3 | bin: bundle-anyway | evidence: moderate | ref: charness-artifacts/release/notes-v0.56.8.md | action: fix | note: "never false-blocks" was true only for the legacy-key path; the nose-version-skew regroup path does not auto-degrade and can hard-block. Added that path + the nose-floor effect to Consumer Impact.
- F4 | bin: bundle-anyway | evidence: weak | ref: charness-artifacts/release/notes-v0.56.8.md | action: fix | note: The nose-floor bullet did not say `>=0.14.0` was stale drift against an already-0.15.0 baseline; clarified as metadata reconciliation, not a new requirement.
- F5 | bin: valid-but-defer | evidence: moderate | ref: charness-artifacts/release/notes-v0.56.8.md | action: document | note: v1 rstrip-only in-place-comment-edit and membership-shrink re-baseline triggers were omitted; added a one-line pointer to references/dup-ratchet.md (folded early).
- F6 | bin: valid-but-defer | evidence: moderate | ref: skills/public/quality/scripts/check_dup_ratchet.py:289 | action: defer | note: The legacy-degrade operator message reads "gate baseline missing/unreadable" — accurate but not legacy-key-specific; the notes' Consumer Impact carries the action. Cosmetic.
- F7 | bin: over-worry | evidence: strong | ref: integrations/tools/nose.json | action: defer | note: nose-floor bump reads like a consumer-breaking requirement but reconciles stale drift against an already-0.15.0 baseline. Patch level holds.
- F8 | bin: over-worry | evidence: strong | ref: tests/quality_gates/test_dup_ratchet.py | action: defer | note: The changed-line mutation gate skips these files (dynamic module load); direct line coverage 89-100% (new module 100%) is the stronger evidence channel. Not a hole.

## Reviewer Tier Evidence

<!-- allowed Host exposure state enums only -->
- Requested tier: high-leverage
- Requested spawn fields: model=gpt-5.5, reasoning_effort=medium, service_tier=priority
- Host exposure state: metadata-hidden
- Application state: parent spawned three angle reviewers (Gawande operational, Minto communication, Jackson bump-framing) and one separate counterweight reviewer through the host Agent tool; the host accepted the agents but did not confirm tier metadata application, so reviewers ran on the parent's available high-leverage model.

## Fresh-Eye Satisfaction

Fresh-Eye Satisfaction: parent-delegated. Three angle reviewers and one separate
counterweight reviewer ran through the host Agent tool and returned distinct lens
verdicts (Gawande FIX-BEFORE-PUBLISH on the clean-worktree precondition, Minto
NOTES-NEED-FIXES, Jackson PATCH-IS-RIGHT, counterweight PUBLISH). All Act/Bundle
items were applied before the publish.

## Deliberately Not Doing

Not escalating to minor (F7 — schema/floor changes are internal/metadata,
advisory-safe), not blocking on the mutation-gate skip (F8 — direct coverage is the
stronger channel), not adding a migration script or louder migration ceremony for a
two-command, never-blocking, self-announcing re-baseline, and not re-wording the
legacy-degrade operator message this release (F6 — cosmetic, deferred).

## Next Move

Commit the release notes, this critique artifact, and the prepare packet, then run
`publish_release.py --execute --part patch --critique-artifact <this file>
--notes-file charness-artifacts/release/notes-v0.56.8.md`, then verify the public
release through a distinct channel and the maintainer install refresh before treating
it as done.
