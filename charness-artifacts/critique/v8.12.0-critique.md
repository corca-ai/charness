# Release Critique — charness v8.12.0 (minor)

- **Kind**: `charness.release-critique` (v1)
- **Prepared for**: v8.12.0-release
- **Scope**: `origin/main..HEAD` — 58 commits, 193 files, +10558/-571; Goal
  #844 lane lifecycle and orchestration ownership (WI-1..WI-12) plus
  closeout records
- **Reviewers**: two fresh-eye parallel reviewers (operations angle,
  record/consumer angle) + parent synthesis; read-only, no repo mutation
- **Bump rationale (minor)**: new additive operator surface (task-run lane
  lifecycle: envelope/exit codes, steer, premise gates, launch hygiene,
  lesson injection, self-review, merge train, DAG, report ledger, metrics,
  test cache, friction learning loop). No invocation breaks, no renames.

## Verdicts

- Operations: **SHIP-WITH-NOTES** — delta coherent, no integrity red flags
  (no fixup/WIP markers, no secrets, proof files sane); the single red test
  is real and contained but will redden the release preflight.
- Record/consumer: **NEEDS-NOTES-EDITS** — underlying record truthful on
  every live-checked point, but the release notes must carry the red tree,
  the open umbrella, and the warn bands; headline counts need in-record
  sources.

## Findings (triaged)

Blocker-candidates (none — no HOLD from either reviewer):

1. [noted-in-notes] Full suite is 10288 passed / 1 failed:
   `test_public_skill_yaml_output_contract.py::test_no_repo_owned_command_writes_json_to_stdout`
   offender `scripts/task_run/task_run_dag.py:540` (introduced by
   goal-session lane persist 4fa8247b1 creating the module; gate predates
   from 2026-07-18). Path unwired from any CLI surface (no command wiring,
   no CLI-reference entry) — operationally contained, contractually red.
2. [noted-in-notes] Umbrella #843 stays OPEN with recorded reason (items
   8-9 functional but module carries finding 1); #837-#842 closed with
   verified ledgers; children #845-#858 all read back CLOSED; parent #844
   closed via goal-run-close with bound proof.
3. [should-note] Length warn bands near hard limit after this release:
   `task_run_completion.py` 475/480, `task_run_execution.py` 473/480,
   `task_run_lane_runner.py` 458/480 (tokei code lines; gate exit 0,
   advisory only). Next slice touching these modules should split, not
   grow.
4. [acknowledged] Changed-line proof no-verdict for WI-11 (focused producer
   fails only on finding 1); substituted with parent coverage (friction
   module 100% under 159 topical+sibling tests). Recorded, not hidden.

## Must-include release-notes lines

- "Known issue: one quality-gate test fails on this tree
  (`test_no_repo_owned_command_writes_json_to_stdout`,
  `scripts/task_run/task_run_dag.py:540` writes JSON to stdout); the path
  is not wired to any CLI surface. Follow-up: migrate or exempt."
- "Umbrella #843 remains open pending that follow-up; #837-#842 closed."
- "Length watch: task_run completion/execution/lane_runner modules sit in
  the advisory warn band under the 480 hard limit."

## Safety for push

- Push ships 58 local commits on main to origin/main; no tags moved, no
  history rewritten (fast-forward push only).
- Proof files under `charness-artifacts/goal-runs/844/` committed with the
  release per operator choice; no other untracked files ship.

Fresh-eye satisfaction: two angle reports plus synthesis delivered with
concerns and triage.

AI provenance: agent-authored critique synthesis by the Goal #844 parent
operator (Muse Code) from two read-only reviewer reports.
