# Issue 618-628 Closeout Critique
Date: 2026-08-14

## Decision Under Review

Run the `issue` closeout floor for two landed cohorts — #618-#624 and
#625/#626/#627 — post closeout evidence to each issue, add retro-family evidence
to #628, and decide whether any of the ten may be closed while their fix exists
only in unpushed local commits.

## Failure Angles

- Closeout honesty: `manual-fallback` reached for because auto-close is
  unavailable, when the unavailability is self-inflicted by not pushing.
- Cohort A: a repaired gate that refuses instead of answering may not serve the
  reporter's job at all, and "the class is closed" may outrun the scanned surfaces.
- Cohort B: issue TITLES name consequences (resurrection slot fillable, lessons
  rewritten) that the shipped code does not deliver, so a close would assert them.
- Repair recursion: a fix for "instructs an action the reader cannot perform" and
  a fix for "errored probes fail toward silence" are both proof-surface verdict
  logic, and this repo's measured pattern is that such a fix ships the class it fixes.

## Counterweight Pass

- Closing on a green gate plus a `CLOSED` readback would have passed every
  automated check; both are claims, not conclusions, and the floor is the
  authorization rather than the gate's colour.
- #618's refusal genuinely serves the reporter's stated discrimination ("is my
  change broken, or is the gate misconfigured"); the reviewer's stronger reading
  (consumer self-linting must work) is declined by the code in writing, so the
  honest closeout narrows the claim rather than withdrawing it.
- #623 came back clean on all four items — the review corrected the parent's own
  assumption that item 2 was met only indirectly, in the fix's favour.
- The `parsed_mapping` signal added mid-repair was reverted rather than shipped:
  measurement showed it could never be `False`, and an inert guard presented as
  protection is worse than an honest comment naming the gap.
- Round 2 was not ceremony: it found a live blocker in each repair, and one of
  them was the fixed class recurring inside its own fix.

## Structured Findings

- F1 | bin: act-before-ship | evidence: strong | ref: skills/public/issue/SKILL.md:102 | action: fix | note: closing any of the ten before push is refused by the floor; deliverable narrowed to comments, issues stay open.
- F2 | bin: act-before-ship | evidence: strong | ref: scripts/session_start_lesson_context.py:108 | action: fix | note: seed next-step hardcoded `scripts/...` a consuming repo lacks; now resolved per reading tree.
- F3 | bin: act-before-ship | evidence: strong | ref: scripts/boundary_probe_lib.py:247 | action: fix | note: probe discarded loader `errors`, so a refused adapter rendered `not-configured` + exit 0.
- F4 | bin: act-before-ship | evidence: strong | ref: scripts/boundary_probe_lib.py:248 | action: fix | note: round-2 blocker — guard keyed on state exempted the evaluated MISS, re-shipping F3's class; now keyed on `hit`.
- F5 | bin: act-before-ship | evidence: strong | ref: scripts/init_lesson_ledger.py:82 | action: fix | note: round-2 blocker — F2's class recurred verbatim in the same file's twice-init refusal.
- F6 | bin: act-before-ship | evidence: strong | ref: tests/test_session_start_lesson_context.py:296 | action: fix | note: round-2 blocker — the recurrence guard's path check was cwd-relative and would have passed on the original defect.
- F7 | bin: act-before-ship | evidence: strong | ref: scripts/critique_adapter_lib.py:222 | action: fix | note: an explicitly empty probe key was recorded as an error; surfaced only once the discard was fixed, and would have turned every such opt-out into a refusal.
- F8 | bin: bundle-anyway | evidence: strong | ref: skills/public/retro/scripts/check_auto_trigger.py:92 | action: fix | note: dup ratchet hard-blocked the duplicated helper; de-duplicated rather than classified, and `git show HEAD` confirmed behavior identical.
- F9 | bin: bundle-anyway | evidence: strong | ref: charness-artifacts/quality/dup-review.json | note: ledger-transaction family rotated by a content edit; recorded as rotation, and the note corrected after review showed the added import cannot rotate a fingerprint. | action: document
- F10 | bin: valid-but-defer | evidence: strong | ref: scripts/command_carrier_discovery.py:96 | action: defer | note: shell scripts and `.github/workflows` remain unscanned flag carriers; #619 closeout claims five carrier kinds, not the class.
- F11 | bin: valid-but-defer | evidence: strong | ref: scripts/validate_debug_artifact.py | action: defer | note: date-coherence exists only for the quality family while debug shares the scaffold; #620 shipped detection, not prevention.
- F12 | bin: valid-but-defer | evidence: strong | ref: scripts/render_lesson_lifecycle_review.py:187 | action: defer | note: review sort key is sign-blind, so a working lesson outranks the one whose prose is the problem; unobservable today at zero negative scores.
- F13 | bin: valid-but-defer | evidence: strong | ref: skills/public/setup/references/default-surfaces.md:136 | action: defer | note: still points consumers at a gate script that now refuses inside a consumer repo.
- F14 | bin: over-worry | evidence: strong | ref: skills/public/retro/scripts/check_auto_trigger.py:92 | action: document | note: the new repo-module import is the root-resolution marker itself, so it cannot be missing where the module already resolved its root.
- F15 | bin: over-worry | evidence: moderate | ref: scripts/adapter_lib.py:424 | action: document | note: substring matching on adapter-influenced warning text is contrived to exploit; structured signal would be cleaner but is not a live risk.

## Reviewer Tier Evidence

- Requested tier: high-leverage
- Requested spawn fields: charness:bounded-reviewer read-only one-shot, inherited model, no host addressing name
- Host exposure state: requested_fields_sent
- Application state: five bounded reviewers completed — round 1 (cohort A scope, cohort B scope, closeout-honesty scope) and round 2 (probe repair, command-resolution repair); every round-1 blocker was repaired or carried into the closeout text, and both round-2 blockers were repaired in-tree before publication
- Delivery state: findings-received

## Fresh-Eye Satisfaction

parent-delegated

## Reviewed Input Identity

<!-- No prepare packet was consumed; reviewers were briefed against the live worktree at 91be132a9 plus the GitHub issue bodies read through the issue backend. -->

## Boundary Ownership

- Producer: the repaired proof surfaces — `boundary_probe_lib`/`adapter_lib` (probe verdicts) and `session_start_lesson_context`/`seed_lesson_transitions`/`init_lesson_ledger` (operator-facing next-step commands).
- Consumer: the impl stop-gate and critique validator for the first; a freshly-opted-in consuming repo for the second.
- Owning surface: quality/retro for the probe verdict logic; retro for the bootstrap instruction text.
- Verdict: owned-correctly

## Non-Claims

No push, tag, version bump, release, hosted CI, installed-consumer behavior, or
issue closure. All ten issues remain OPEN by decision, recorded in the comments
posted to each. Every verdict here is local to this worktree at the commit that
carries it.
