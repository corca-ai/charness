# v8.6.5 release critique (parent counterweight)

Date: 2026-09-14

## Decision Under Review

Ship v8.6.5 (patch): ceiling-aware repository discovery centralized in
`scripts/core/git_checkout.py`, ceiling-independent ledger lock identity,
and `hold_out` staged destinations. No new public capability. This
release supersedes the unshipped v8.6.4 prepare (blocked, reset;
nothing of v8.6.4 ever left this machine: no remote tag, no GitHub
release).

## Verification Scope Decision

- Claim under test: the patch release locks no broken operator surface and its record states the behavior delta honestly.
- Changed surfaces: 8 reviewed paths (packet `charness-artifacts/critique/2026-09-14-v865-release-packet.json`, sha `e131e86af2b75378e2869de68aad85f777b3f5d27e3cf71ca04822e34765cd9f`, input `329366b830f1adc1a7c8f9daa5d5bbd3223b866e50efc7ee7cdc490d12b094c3`). The broader mutation/sampler delta was covered by the v8.6.4 critique rounds (operational block, communication defer); every blocker from those rounds is fixed in-tree, so this round covers only the new ceiling repair surface. That carry-forward is stated, not proven here.
- Minimum sufficient proof: release operational worker + one narrow follow-up worker + parent code-against-claim verification + full standing suite (9695 passed) + packet-declared gates (export sync, committed packaging, scan hygiene, code lengths).
- Deliberately omitted checks: second communication-lens pass (no operator-facing surface changed; notes derive at publish and the claims round audits the record); re-review of the unchanged v8.6.4-covered delta.
- Verifier contract: critique file-backed workers via codex_exec; release gates via the standing pytest runner and the publish helper's own probes at publish time.
- Failure classification: subject-defect (every blocking round found a real defect; no verifier-defect or scope-too-broad retry was needed).
- Subject identity: sha256:e131e86af2b75378e2869de68aad85f777b3f5d27e3cf71ca04822e34765cd9f
- Input identity: sha256:329366b830f1adc1a7c8f9daa5d5bbd3223b866e50efc7ee7cdc490d12b094c3

## Failure Angles

- Two writers with different ceiling environments meeting at different lock files (silent lost update).
- A ceiling-placed repository capturing discovery above the ceiling.
- Tagging over an unsynced manifest or unproven install surface.
- Release notes unreadable to operators who did not follow development.

## Angle Pass (worker results)

- Release operational (`workers/v865-release-operational/result.json`), verdict **block**: lock-namespace split across ceiling environments (high). Real: `_lock_path` chose its directory through ceiling-sensitive discovery.
- Release operational follow-up (`workers/v865-release-operational-followup/result.json`), verdict **defer**: caller/rollout compatibility unproven; no executed cross-ceiling contention regression (both medium).

## Counterweight Pass (parent verification against code)

- Lock split **confirmed real and fixed**: the writer lookup now ignores ceilings (lock placement only; lock identity is a pure function of the ledger path), bare `.git` names still excluded. Pinned by `test_lock_path_agrees_across_ceiling_environments` (fails pre-fix, passes post-fix).
- Caller/rollout **dispositioned, no migration needed**: the only `_repository_root` consumer is `_lock_path`, reached by all production writers through `ledger_lock` (`init_lesson_ledger`, `record_lesson_score`, `record_lesson_lifecycle`) — no call-site change. Writers without an intersecting ceiling (all production; ceilings are a test-isolation mechanism) compute byte-identical lock paths before and after, because an empty ceiling makes the blind walk equal the aware walk. Writers with an intersecting ceiling converge toward the no-ceiling namespace. Mixed old/new writers can only diverge in a ceiling-hidden configuration with no known production instance.
- Contention regression **added and executed**: `test_cross_ceiling_writers_serialize_on_one_lock` forks a ceiling-hidden holder and a plain contender on one ledger; the contender must meet the holder at the same lock file and must not enter before release (3s hold; deterministic). Fails on the old placement (verified by swapping in the prior module), passes on the new one. Full standing suite: 9695 passed after the final test edit.
- Export/packaging **executed**: `sync_root_plugin_manifests.py` refreshed the materialized mirror after every source edit; `validate_packaging.py`, `validate_packaging_committed`, scan-hygiene inventory (empty), and code-length gate all pass. Fresh-checkout probes run inside the publish helper before tag push; not claimed here.

## Structured Findings

- F1 | bin: act-before-ship | evidence: strong | ref: scripts/lessons/lesson_ledger_writer_lib.py:60 | action: fix | note: lock identity now ceiling-independent + agreement regression
- F2 | bin: act-before-ship | evidence: strong | ref: tests/test_lesson_ledger_concurrency.py | action: test | note: cross-ceiling contention regression added and executed
- F3 | bin: bundle-anyway | evidence: moderate | ref: scripts/core/git_checkout.py | action: document | note: no lock migration; ceiling-hidden writers converge to the shared namespace

## Reviewer Tier Evidence

- Requested tier: high-leverage
- Requested spawn fields: file-backed workers via `run_review.py`, backend `codex_exec`, attempts `v865-release-operational` and `v865-release-operational-followup`, read-only boundary, packet-bound scope
- Host exposure state: requested_fields_sent
- Application state: both workers accepted; read-only boundary held; no separate runtime metadata proves effective model parameters
- Delivery state: findings-received
- Execution mode: file-backed-worker

## Fresh-Eye Satisfaction

parent-delegated — two file-backed workers delivered typed results (release block, follow-up defer); parent verified each finding against code, fixed the real defect with pinned regression tests (identity and executed contention), and triaged the rest above. No same-context substitute was used.

## Boundary Ownership

- Producer: lesson-ledger locking (writer lib) and checkout discovery (git_checkout); consumer: every ledger writer via `ledger_lock`.
- Render vs. produce: the lock-path change renders the same digest into one stable directory per ledger regardless of writer environment; no new producer state is created.
- Move vs. encode: no consumer-specific knowledge encoded in shared code; the ceiling rule stays with the checkout owner, the lock-identity rule with the writer.
- Verdict: owned-correctly — every touched surface sits on its existing owner; nothing crosses a producer/consumer boundary.

## Next Move

Commit this artifact with its packets and worker evidence, then run the v8.6.5 prepare lane, the claims round against the record, then publish.
