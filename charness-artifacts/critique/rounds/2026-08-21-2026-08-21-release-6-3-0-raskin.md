# Critique Round Findings

- Round: 1
- Recorded date: 2026-08-21
- Boundary window id: `2026-08-21-release-6-3-0-raskin`
- Boundary snapshot: `charness-artifacts/critique/snapshots/2026-08-21-release-6-3-0-raskin.json`
- Boundary snapshot SHA-256: `fca3f6dc89aedcd12f89923799c733a17f165d213eddc32f24a5a94921d47170`
- Findings SHA-256: `1550fb3a12d47e833bcbb2a4793face0dc903b130567355e5587ae86f322e573`

## Findings Returned

Verdict: **BLOCK**

Fresh-eye satisfaction: parent-delegated, read-only. No agent spawn, file writes, git mutation, or external publication.

Packet was read first: [release packet](</home/hwidong/codes/charness/charness-artifacts/critique/2026-08-21-release-6.3.0-packet.md:1>). Verified:

```text
sha256sum charness-artifacts/critique/2026-08-21-release-6.3.0-packet.json
da91b2c1cf73fabda39a4c706449f08060b95db95116df8b9d3219b967cf2c36
```

Reviewed-input identity matches `cfecfe36279b82aeab4c86dc10da31019e6f92f049210e5606e1aaa7e50aeab0`.

## Release Scope

Target `6.3.0` / `v6.3.0`, from current `6.2.0`; no version, tag, push, publication, install, or public readback has occurred.

## Surface-Lock Inventory

Reviewed the goal, issue ledger/read, source and plugin catalog/checker mirrors, root CLI/help, capability inventory, release adapter, install-refresh guidance, and prior semantic-candidate critique.

## Act Before Ship

1. **#670’s decided consumer contract is not actually exposed.** `[strong]`

The goal requires stable id, packaged path, purpose/artifact type, invocation, adoption policy, documented doctor/inventory exposure, and consumer declarations using exactly one of `wired` or non-empty `opt_out_reason` ([goal](</home/hwidong/codes/charness/charness-artifacts/goals/2026-08-20-repairs-that-carry-their-class.md:356>)).

The catalog currently provides path, `consumer_facing`, decision, reason, purpose, and invocation, but no stable id, artifact type, wired/opt-out declaration, or consumer-declaration reader ([catalog](</home/hwidong/codes/charness/skills/public/quality/references/consumer-validator-catalog.yaml:13>); [checker](</home/hwidong/codes/charness/scripts/check_consumer_validator_catalog.py:116>)).

The checker passes:

```text
status: pass
packaged_validator_count: 133
decision_count: 133
consumer_facing_count: 14
```

That proves source-side enumeration only. `charness --help`, `charness catalog --help`, `charness catalog list --help`, `doctor`, and the quality skill expose no validator-catalog discovery path; the only documentation is an index link ([quality index](</home/hwidong/codes/charness/skills/public/quality/references/index.md:68>)).

Issue #670 is still `open`, classified `qualified-repair`, with `close_disposition: leave-open` ([ledger](</home/hwidong/codes/charness/charness-artifacts/issues/2026-08-20-next-release-ledger.json:427>)). I found no matching deferred decision authorizing this narrower public contract. Before bumping, either complete the promised consumer-facing surface and proof, or explicitly amend the goal/release rationale and state the narrower non-claim.

2. **Wrong-owner `--repo-root` silently returns a green answer.** `[strong]`

```text
./charness catalog list --repo-root /definitely/not-a-repo --summary
# exit 0; returns inventory with paths under the actual Charness plugin checkout
```

By contrast:

```text
python3 scripts/check_consumer_validator_catalog.py \
  --repo-root /definitely/not-a-repo
# exit 1; status: fail; catalog is missing
```

`list_catalog()` performs no repository-root validation, while validation exists only on the mutating refresh path ([capability catalog](</home/hwidong/codes/charness/scripts/capability_catalog.py:20>), [CLI](</home/hwidong/codes/charness/charness:5089>)). This is precisely the axis where a similar-looking invocation targets the wrong owner/path yet emits success. It directly conflicts with the CLI’s promise that humans can inspect state “instead of guessing.”

This behavior is pre-existing (`git diff --name-only origin/main..HEAD -- charness scripts/capability_catalog.py` is empty), but it remains a dangerous supported-surface defect and undermines the intended inventory discovery story.

## Bundle Anyway

- Add a documented direct invocation and owner/path explanation for the validator catalog once its contract is settled.
- Reconcile the goal’s recorded `132` decisions / `13` consumer-facing validators with the live checker’s `133` / `14`.
- Improve nested argparse failures to show subcommand-local help and a next action. Current wrong-order, extra-argument, and unsupported-flag cases exit nonzero and fail closed, so wording alone is not a blocker.
- Source/export parity is clean: catalog and checker SHA-256 values match between `skills/public` and `plugins/charness`.

## Valid but Defer

Upgrade and rollback instructions belong to the post-bump release artifact. The adapter correctly specifies `charness update`, `charness version`, and `charness doctor`, while release-specific migration and rollback notes belong in release notes ([release adapter](</home/hwidong/codes/charness/.agents/release-adapter.yaml:12>); [install-refresh](</home/hwidong/codes/charness/skills/public/release/references/install-refresh.md:6>)).

The prior semantic critique explicitly issued no upgrade/rollback instruction and deferred those operations ([critique](</home/hwidong/codes/charness/charness-artifacts/critique/2026-08-21-semantic-candidate-release-critique.md:260>). This is not a current semantic-stage blocker, but v6.3.0 notes must eventually explain updating from v6.2.0, restart/readback expectations, and the supported rollback path.

## Over-Worry

Do not block on every generic `unrecognized arguments` string or require every command to accept `--detail`; those cases exit `2` before execution, and the doctor conflict wording is clear. The material refusal defect is the nonexistent-root command returning success.

## Explicit Non-Claims

- No claim that `6.3.0` is version-bumped, tagged, pushed, published, installed, or publicly read back.
- No claim that #670 is closed.
- No claim that the green catalog checker proves consumer adoption or wiring.
- No hosted, real-machine, Cautilus, or rollback execution was performed.
