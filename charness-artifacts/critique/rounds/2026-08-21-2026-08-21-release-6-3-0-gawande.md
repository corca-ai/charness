# Critique Round Findings

- Round: 1
- Recorded date: 2026-08-21
- Boundary window id: `2026-08-21-release-6-3-0-gawande`
- Boundary snapshot: `charness-artifacts/critique/snapshots/2026-08-21-release-6-3-0-gawande.json`
- Boundary snapshot SHA-256: `cba86f012493c8ced6391a5e940ae6262e385ea7a703070112c1de764fbd0035`
- Findings SHA-256: `08358b3ec0c4bc57632607dd6e2dd5f6d610b4891021fa73840f9681cec9308a`

## Findings Returned

## Verdict: BLOCK

### Release scope and packet identity

- Reviewed the assigned Atul Gawande operational-checklist lens for `6.3.0` / `v6.3.0`.
- Read the [release packet](/home/hwidong/codes/charness/charness-artifacts/critique/2026-08-21-release-6.3.0-packet.md:5) first.
- Packet JSON SHA verified:

  ```text
  da91b2c1cf73fabda39a4c706449f08060b95db95116df8b9d3219b967cf2c36
  ```

- Reviewed-input identity matches `cfecfe36279b82aeab4c86dc10da31019e6f92f049210e5606e1aaa7e50aeab0`.
- Current HEAD is `67946f19b8478cea40ba76d8209bfe82dfd03603`.

### Surface-Lock Inventory

- Pre-bump surfaces are internally coherent at `6.2.0`; `current_release.py` reports `drift: []` and no absent surfaces.
- The adapter’s sync path is correct: `python3 scripts/sync_root_plugin_manifests.py --repo-root .` in [.agents/release-adapter.yaml](/home/hwidong/codes/charness/.agents/release-adapter.yaml:10).
- The current planner receipt is stale and targetless: it was captured against `b9e894809...`, with `target_version: null` and `tag_name: null` in the [planner receipt](/home/hwidong/codes/charness/charness-artifacts/release/2026-08-20-slice-0-release-planner.raw.yaml:117).
- The packet lists packaging validators, but no `6.3.0` post-bump generated/plugin/marketplace result has been executed.
- `./charness doctor --help` and `./charness update --help` both exit successfully. The flags are valid, but operational behavior is not proven.
- The declared fresh-checkout probes are `not_established`, and test source confirms that this is not a pass: [probe checker](/home/hwidong/codes/charness/skills/public/release/scripts/check_fresh_checkout_probes.py:21).

### Act Before Ship concerns

1. **The exact release target is not currently bound through the release plan.**  
   The packet is prepared for `6.3.0`, but the saved planner has no target or tag and predates the current HEAD. Re-run a current target-bound plan, preferably with:

   ```bash
   python3 skills/public/release/scripts/plan_release_run.py \
     --repo-root . --set-version 6.3.0 --detail
   ```

   Then require the release record to carry `current=6.2.0`, `target=6.3.0`, `tag=v6.3.0`, the prepared commit, packet SHA, and reviewed-input identity.

2. **Resume publication bypasses the dedicated release-surface check.**  
   [`publish_release_resume.py`](/home/hwidong/codes/charness/skills/public/release/scripts/publish_release_resume.py:234) explicitly calls this a known gap. The resume publisher has “NO release-surface gate” and “NO focused adapter preflight” [here](/home/hwidong/codes/charness/skills/public/release/scripts/publish_release_resume_publish.py:129). It also derives the tag from the current manifest rather than an immutable prepared target. A resumed publication can therefore proceed without the same explicit `6.3.0` surface-parity proof as the initial path.

3. **Fresh-checkout coverage does not test the required operator path.**  
   The adapter probes source help and `python3 scripts/doctor.py`, which is the external-tool doctor. They do not install the packaged plugin or run managed `charness update`, `charness version`, and `charness doctor`. The goal explicitly requires those behaviors in a fresh checkout: [goal acceptance](/home/hwidong/codes/charness/charness-artifacts/goals/2026-08-20-repairs-that-carry-their-class.md:427). The adapter itself warns that `python3 scripts/doctor.py` does not measure managed-checkout/plugin skew [here](/home/hwidong/codes/charness/.agents/release-adapter.yaml:66).

   The read-only external doctor command also exited `1` in this environment because the markdown-preview backend could not create a usable temporary directory. This is not proof of a product defect, but it is not a clean probe pass.

4. **The 6.3.0 rollback path is not yet bound.**  
   The adapter correctly points operators to `charness update`, `charness version`, and `charness doctor`, and internal pre-commit rollback exists. However, the current release evidence contains no 6.3.0-specific notes naming the prior `6.2.0` recovery path. The canonical guidance requires release-specific rollback advice in release notes or the generated release artifact: [install-refresh reference](/home/hwidong/codes/charness/skills/public/release/references/install-refresh.md:11). `charness update --help` exposes no version/tag rollback option, so the release notes must document the supported recovery procedure.

### Bundle Anyway concerns

- Add `charness version`, `charness doctor --help`, and `charness update --help` to the isolated operator/fresh-checkout proof set.
- Use explicit `--set-version 6.3.0`, then run sync plus both packaging validators and confirm every versioned surface reads `6.3.0`.
- Emit the exact target/tag and prepared commit in the resume next action and release artifact.

### Valid but Defer concerns

- A dedicated public `charness rollback` or version-pinning command can be a follow-up if 6.3.0 release notes document the supported recovery route.
- A full multi-host installation matrix is deeper than this checklist, provided the required isolated managed-install proof is added now.

### Over-Worry concerns

- Seeing `6.2.0` before the version bump is expected; current pre-bump `drift: []` is healthy.
- The Codex marketplace file has no version because it is intentionally a source-path presence surface.
- The external doctor’s temporary-directory failure is environment evidence, not by itself a `6.3.0` code failure.
- The current untracked packet/snapshot files are reviewer artifacts, not generated release-surface drift; they still must be reconciled before the clean release preflight.

### Explicit non-claims

I did not bump versions, mutate files, mutate git, run fresh-checkout probes with `--run-probes`, run `charness update`, run managed `charness doctor`, tag, push, publish, install, or perform public/readback verification. I am not claiming the release exists, that target artifacts are synchronized, or that installed hosts match the candidate.
