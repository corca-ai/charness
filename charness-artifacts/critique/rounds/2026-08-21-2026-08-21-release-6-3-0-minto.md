# Critique Round Findings

- Round: 1
- Recorded date: 2026-08-21
- Boundary window id: `2026-08-21-release-6-3-0-minto`
- Boundary snapshot: `charness-artifacts/critique/snapshots/2026-08-21-release-6-3-0-minto.json`
- Boundary snapshot SHA-256: `bb2aad12ebb762cbeba757cd19494c7b7b9bc251824bdd90958d440fd817b51d`
- Findings SHA-256: `78b693c975cee23076c93b94c72c6f04fa193dbc5b3a452d2d51412ebf91241f`

## Findings Returned

Verdict: BLOCK

Fresh-Eye Satisfaction: parent-delegated. Read-only review; no files, git state, or external systems were changed.

The coherent story currently supported is:

> 6.3.0 adds a packaged, path-indexed catalog classifying validator-like scripts. Local evidence reports 133 candidates, 133 decisions, 14 consumer-facing validators, and 119 exclusions. It does not change validator behavior or prove installed/hosted behavior.

The current goal and documentation imply a broader contract than the implementation proves.

### Evidence

- `python3 scripts/check_consumer_validator_catalog.py --repo-root .` passes with `133 / 133 / 14 / 119`.
- `tests/test_consumer_validator_catalog.py:78` independently asserts `133` candidates and `14` consumer-facing validators.
- Root/plugin catalog, checker, and index copies are parity-identical via `cmp -s`.
- The goal at `charness-artifacts/goals/2026-08-20-repairs-that-carry-their-class.md:356` promises stable ID, artifact type, adoption policy, a documented doctor/inventory surface, and `wired`/`opt_out_reason` declarations.
- The actual catalog and checker require only path, classification, decision, reason, purpose, and invocation: [catalog](</home/hwidong/codes/charness/skills/public/quality/references/consumer-validator-catalog.yaml:1), [checker](</home/hwidong/codes/charness/scripts/check_consumer_validator_catalog.py:116>).
- The same goal’s verification prose at `:632` still says `132` decisions and `13` consumer-facing validators.
- `release_claim_surfaces.py:16` explicitly says unregistered claim surfaces are invisible to release-note derivation; the validator catalog is not registered there.
- `rg` found no consumer-validator catalog guidance in README, operator acceptance, host packaging, CLI reference, or release notes. The only public index wording describes it as a maintainer quality-detection catalog.
- The current release artifact is still explicitly 6.2.0: `charness-artifacts/release/latest.md:6`.
- Packet envelope status is `Overall ok: True`, but it contains changed surfaces, non-goals, and a generic semantic question—not a concrete 6.3.0 release scope/operator-action/upgrade narrative.

### Act Before Ship

- Packet identity is unresolved. Delegation supplied SHA `da91b2c1...`; `sha256sum charness-artifacts/critique/2026-08-21-release-6.3.0-packet.md` returned `88d954e6...`. The reviewed-input identity `cfecfe...` matches, but that does not establish the requested packet SHA. Reissue or rebind the packet.
- Narrow the release claim to the implemented path-indexed catalog, or complete and verify the promised ID/artifact-type/adoption/declaration contract. Calling the current slice the full #670 public capability overstates it.
- Correct or explicitly historical-mark the goal’s `132/13` count. If release notes mention quantities, register a mechanically derived claim surface or omit the numbers.
- Before calling the capability consumer-facing and discoverable, add an operator-facing path explaining where the installed catalog lives, how to use its invocation entries, what the 14/119 distinction means, and that no validator behavior changed.

### Bundle Anyway

- Generate the final 6.3.0 release artifact after the final tree, with the exact catalog path and bounded scope.
- Retain the evergreen update instruction `charness update`; put release-specific migration/rollback guidance in the 6.3.0 notes as required by `skills/public/release/references/install-refresh.md`.
- State explicitly that local catalog validation proves packaging/classification completeness only.

### Valid but Defer

- Fresh-checkout, installed-update, hosted/public readback, and post-publish observer evidence belong to the later release boundary. Existing quality receipts correctly call themselves local-only.
- Deeper CLI integration of the validator catalog can be a later enhancement if a documented packaged path is accepted as the bounded 6.3.0 surface.

### Over-Worry

- Requiring all 133 validator-like scripts to be consumer-facing would contradict the intended exclusion model.
- Requiring Cautilus or live consumer behavior to prove this static catalog’s schema is unnecessary.
- The absence of a finalized 6.3.0 release artifact before version mutation is not itself a defect; it must simply exist and be checked before publication.

### Explicit non-claims

- No claim of version bump, tag, push, publication, installation, hosted readback, or public readback.
- The local checker does not prove validators work in a consumer repository or installed cache.
- The semantic-candidate PASS is not a 6.3.0 release PASS; the retro explicitly records that the wrong-target critique was rejected.
- The packet’s `Overall ok: True` proves packet shape/status only, not release-story correctness.
- `charness-artifacts/release/latest.md` is historical 6.2.0 evidence, not v6.3.0 evidence.
