# v0.7.12 Release Risk Check
Date: 2026-05-24

## Execution

- Target: release critique.
- Fresh-Eye Satisfaction: parent-delegated.
- Packet Consumed: n/a (bounded release summary supplied to reviewer).

## Release Scope

- Current version: `0.7.11`.
- Target version: `0.7.12`.
- Target tag: `v0.7.12`.
- Consumer change: publish the completed quality/critique sweep fixes and
  release proof updates that are currently local on `main`.

## Surface-Lock Inventory

- Generated/install surfaces: `packaging/charness.json`,
  `.claude-plugin/plugin.json`, `.agents/plugins/codex-plugin.json`, and
  marketplace plugin metadata.
- Consumer-visible behavior: RCA ledger timestamp validation, mutation-line
  coverage eligibility, current-pointer write scanning, `sync-support` exit
  status, and standing-test economics temp-path tolerance.
- Documentation/artifacts: `docs/handoff.md`,
  `charness-artifacts/release/latest.md`, `charness-artifacts/quality/latest.md`,
  and `charness-artifacts/debug/latest.md`.
- Release backend: GitHub release `v0.7.12` through `gh`.

## Findings

- Act Before Ship: pass a tracked critique artifact to the release helper so
  the release record carries review proof.
- Act Before Ship: do not pass `--close-issue 211`; GitHub already reports
  #211 closed by `github-actions[bot]` at `2026-05-24T10:08:06Z`, and this
  release should not create a second closeout claim.
- Bundle Anyway: refresh stale handoff wording before the release so the
  shipped operator pickup contract says the quality sweep is completed, not in
  progress.
- Expected Pre-Publish State: `charness-artifacts/release/latest.md` still
  names `v0.7.11`; the publish helper must rewrite, audit, commit, and push the
  `v0.7.12` release artifact.

## Counterweight Triage

- Over-Worry: fresh-checkout probes are not configured, so their absence is not
  a release blocker.
- Over-Worry: real-host proof is not required for this slice because the
  adapter reported no matching trigger.
- Valid but Defer: requested-review enforcement is advisory-only until the
  release adapter declares concrete review commands.

## Operator Action Required

- Run the repo release helper with `--part patch --execute` and
  `--critique-artifact charness-artifacts/critique/2026-05-24-v0.7.12-release-risk-check.md`.
- Verify `gh release view v0.7.12` after publish.
- Verify `git status --short --branch` is clean after the post-publish artifact
  commit and push.

## Upgrade Path

- Operators run `charness update`.
- Restart Claude Code or Codex if the host cache still shows the previous
  version.

## Next Move

Commit this critique and the handoff correction, then publish `v0.7.12` through
the checked-in release helper.
