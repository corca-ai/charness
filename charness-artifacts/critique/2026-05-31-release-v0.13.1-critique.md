# Release Critique - v0.13.1

Fresh-Eye Satisfaction: parent-delegated
Packet Consumed: `charness-artifacts/critique/2026-05-31-104554-packet.md`
Target: release critique

## Release Scope

Target release: `v0.13.1` patch for `charness`.

Consumer-facing frame: maintenance patch for mutation-gate closeout hardening,
staged commit gate prediction, Codex find-skills hook cleanup, and release /
handoff evidence refresh. This is reliability hardening, not a new public API.

## Surface-Lock Inventory

- Packaging and plugin versions: `packaging/charness.json`,
  `plugins/charness/.claude-plugin/plugin.json`,
  `plugins/charness/.codex-plugin/plugin.json`, `.claude-plugin/marketplace.json`.
- Release adapter and generated release record: `.agents/release-adapter.yaml`,
  `charness-artifacts/release/latest.md`.
- Operator update text copied into the release record.
- Host-hook behavior in `scripts/host_hook_find_skills.py` and packaged mirror.
- Handoff source included in the tag: `docs/handoff.md`.
- Publish helper order: critique gate, clean tree, version bump/sync, release
  quality gate, tag/branch push, GitHub release creation, post-publish artifact.

## Angle Findings

### Operational Checklist

Act Before Ship:

- The publish helper requires a tracked standalone critique artifact and a clean
  tree before it starts.
- `.agents/release-adapter.yaml` still said operators should pull `0.13.0`;
  the helper copies that text into the release record.
- If this release is expected to verify issue closure, pass
  `--close-issue 262 --close-issue 219 --close-issue 267`.

Bundle Anyway:

- Patch version is appropriate for maintenance/reliability hardening.
- Fresh checkout probes are not configured and are not a blocker.

### Operator Communication

Act Before Ship:

- Update instructions must say `0.13.1`, not `0.13.0`.
- `docs/handoff.md` should not be tagged with a stale "release line still
  v0.13.0" statement.

Bundle Anyway:

- Use a short public notes file. Generated commit-subject notes are mechanically
  valid but too internal for this patch.
- Mention Codex find-skills hook cleanup explicitly.

### Problem Framing

Act Before Ship:

- Host-hook behavior changed, so the release frame needs to name hook cleanup or
  proof explicitly.

Over-Worry:

- A minor bump is not required; no new public CLI or package API is being
  promised.
- A broad new clean-machine Cautilus run is too broad for this patch; carry the
  configured real-host checklist honestly if it triggers.

## Counterweight Triage

Act Before Ship:

- Commit this critique artifact so the helper's critique gate can pass.
- Fix `.agents/release-adapter.yaml` update instructions before publish.

Bundle Anyway:

- Refresh `docs/handoff.md` release-line wording before tag.
- Pass close-issue flags for #262, #219, and #267 so the release artifact
  records verification.
- Use concise self-contained release notes.

Over-Worry:

- Do not upgrade to minor solely because helper scripts changed.
- Do not block on fresh-checkout probes when the adapter declares none.

Valid But Defer:

- Add a future freshness check for static release adapter instructions.
- Add requested-review commands to the release adapter later; the standalone
  critique is the effective review gate for this release.
- Improve release prepare packets so they summarize `v<previous>..HEAD` instead
  of only current worktree state.

## Operator Action Required

- Publish with the tracked critique artifact.
- Publish with concise release notes and close-issue flags for #262, #219, and
  #267.
- Verify the helper records public GitHub release visibility.

## Upgrade Path

Operators run `charness update` to pull `0.13.1`, then restart Claude Code or
Codex if a host cache still shows the previous version. No new manual migration
is required beyond the existing update flow.
