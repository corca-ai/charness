# Release Critique: v0.43.0
Created: 2026-06-12
Target: `release-critique`
Fresh-Eye Satisfaction: parent-delegated
Packet Consumed: `charness-artifacts/critique/2026-06-12-143609-packet.md`

## Release Scope

- version: `0.43.0`
- tag: `v0.43.0`
- consumer change: minor release; the overnight autonomous structural-quality
  bundle — clone advisories consumed as structural signals
  (inventory-dispatch taxonomy plus nose payload notes), the quality-signal
  scorecard (issue 356 resolution), the shared meaningful-slice cadence
  reference wired from critique/impl/quality/achieve (issue 357 resolution),
  two new authoring-repo standing gates (bootstrap-shim consistency with
  `--fix`; advisory public-doc coupling) with the exported-reusable-guidance
  provenance class, the log-backed contract-effectiveness cautilus fixture
  (deterministic validation only), and the operator-approved retirement of
  the second-machine release-proof arm. Eight content commits on v0.42.0
  (`b99748b1`..`535b9435`), each slice already covered by committed bundle,
  resolution, and disposition critiques.

## Surface-Lock Inventory

- `.agents/release-adapter.yaml` update instructions for `0.43.0`.
- Packaging and generated plugin manifests, bumped and synced by
  `publish_release.py`.
- `charness-artifacts/release/latest.md`, rewritten by `publish_release.py`;
  the regenerated real-host checklist renders from the adapter and correctly
  omits the retired second-machine arm.
- Fresh-checkout probes: passed this session pre-critique; the helper re-runs
  them during `--execute`.
- Installed-plugin operator update path: `charness update`
  (adapter-declared `post_publish_install_refresh` auto-runs after verified
  publish).

## Angles

- Operational checklist: minor bump verified against the full diff (skill
  references, repo-local gates, tests, fixture only; `packaging/`, the
  `charness` CLI, and command surfaces untouched; no renames, no migration).
  Version surfaces uniformly `0.42.0` pre-helper. No `--close-issue` needed:
  #356/#357 already CLOSED via carrier `f9271594` (verified live). Packet
  verifiers run green by the reviewer (`validate_adapters.py` 17/18;
  cautilus proof deterministic path; both new gates green live).
- Cold reader: every numbered claim in the 0.43.0 update-instructions entry
  was verified against the actual diffs with no overstatement; the
  consumer-vs-authoring scoping was probed on a consumer-shaped repo — the
  shipped runner inherits no blocking behavior (shim gate trivially clean,
  coupling gate advisory exit 0), so the OPERATOR NOTE stays honest.
- Counterweight: the only act-before-ship items are procedural and
  helper-enforced — the clean-tree/tracked-critique carrier commit before
  `--execute`, and honest reviewer-tier evidence in this artifact. The
  update-instructions staleness gate passes (entry names 0.43.0); older
  entries cannot block (whole-text containment).

## Reviewer Tier Evidence

- Requested tier: high-leverage
- Requested spawn fields: model=gpt-5.5, reasoning_effort=medium, service_tier=priority
- Host exposure state: host-defaulted
- Application state: requested fields not applied — the host spawned its
  default reviewer (`claude-fable-5`, agent ID `a5e637c05037d728f`); no
  provider-side application confirmation was exposed.

## Findings

### Act Before Ship

- F1 | bin: act-before-ship | evidence: strong | ref: skills/public/release/scripts/publish_release_preflight.py | action: fix | note: commit the 0.43.0 adapter entry, both packet files, and this critique artifact as one tracked carrier commit before `publish_release.py --execute --critique-artifact <this file>` (clean-tree and tracked-critique preconditions are helper-enforced). Resolution: committed as the release-critique carrier commit.
- F2 | bin: act-before-ship | evidence: strong | ref: charness-artifacts/critique/2026-06-12-143609-packet.json | action: fix | note: record reviewer-tier evidence as host-defaulted / requested-fields-not-applied rather than `applied`. Resolution: recorded above.

### Over-Worry

- F3 | bin: over-worry | evidence: strong | ref: plugins/charness/scripts/run-quality.sh | action: document | note: the mirror ships the new gate wiring but operates on its own repo root; consumer-shaped probe confirmed no blocking behavior leaks.
- F4 | bin: over-worry | evidence: strong | ref: skills/public/release/scripts/publish_release_artifact.py | action: document | note: the regenerated release record renders the real-host checklist from the adapter, so the retired second-machine arm stays retired; the disposition survives in git history and in the new entry's operator note.

### Bundle Anyway

- F5 | bin: bundle-anyway | evidence: strong | ref: .agents/release-adapter.yaml | action: document | note: `requested_review_commands` is empty (advisory-only review gate) and the helper re-runs fresh-checkout probes during `--execute`; the maintainer-machine `charness update` proof remains the one required post-publish real-host arm.

## Verdict

SHIP: minor bump justified, entry accurate against diffs, both helper
preconditions handled by the carrier commit, no issue-close flags needed,
release-record carry-forward verified safe.
