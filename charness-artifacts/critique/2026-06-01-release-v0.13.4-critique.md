# Release Critique: v0.13.4

Target: patch release `v0.13.4` for the handoff chunker candidate-filter fix.

Reviewer: Laplace subagent (`019e82b9-650d-7b31-aa0f-0cf43a9e39db`)

## Verdict

Patch release is appropriate after refreshing release update instructions.

## Act Before Ship

Fixed before publish:

- `.agents/release-adapter.yaml` update instructions still named `0.13.3` and
  the prior release scope. They now name `0.13.4` and the handoff pickup
  behavior repair.
- Do not publish the current `0.13.3` version. `v0.13.3` already exists and the
  current HEAD changes bundled handoff behavior, so this must be a patch bump.

## Bundle Anyway

- Keep the plugin mirror in the release. Source and checked-in plugin copies
  for the changed handoff files are synced.
- Carry the release note as a behavior repair: bare handoff pickup no longer
  offers local-state preflight, completed goal activation, or cadence/invariant
  constraints as selectable chunks.
- Do not close #184 or #261 from this release; they remain live product/policy
  work and were not requested as release closeouts.

## Over-Worry

- No real-host proof blocker for this slice; the changed paths do not hit the
  configured real-host proof trigger.
- No generated mirror drift blocker remains after sync.

## Valid But Defer

- Fresh-checkout probes are not configured for this repo.
- Requested-review enforcement remains advisory-only because
  `requested_review_commands` is empty.
