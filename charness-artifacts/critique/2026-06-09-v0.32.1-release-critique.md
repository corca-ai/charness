# Release critique — charness v0.32.1 (patch; behavior-preserving closeout module split)

Date: 2026-06-09

Fresh-eye satisfaction: satisfied — the v0.32.1 release decision was reviewed by a
fresh-eye reviewer in a separate agent context, distinct from the agent that produced
the refactor. The reviewer judged the RELEASE decision (bump level, generated-surface
sync, operator-note accuracy, draft-goal disclosure, publish boundary), not the
already-reviewed refactor code, and independently re-ran the load-bearing parity proof.

## Scope

The patch bump 0.32.0 → 0.32.1 publishing the 5 local commits ahead of `origin/main`:
the two-slice behavior-preserving split of the two at-cap `achieve` closeout modules
(`goal_artifact_disposition.py` → `goal_artifact_disposition_grammar.py` leaf;
`goal_artifact_closeout_evidence.py` → `goal_artifact_closeout_loaders.py` leaf), the
slice-3 bundle closeout (coverage test + retro/critique artifacts; goal `Status:
complete`), an inert `Status: draft` deferred-queue goal artifact, and the v0.32.1
`update_instructions` release-prep entry. The refactor code already carried 2 fresh-eye
slice critiques (SHIP), a disposition review (PASS), the full broad verification-lock
gate (PASS), and an end-to-end byte-identical closeout-gate verdict-parity proof (sha
`7a5d99ae`), so this review does not re-review the refactor code — it reviews the
RELEASE decision.

## Verdict

SHIP. PATCH is the honest bump, the generated surfaces are sync-ready, the
`update_instructions` note is accurate and does not overclaim, the inert draft goal is
disclosed, and the tree is clean with no publish-dirtying residue. One should-fix (a
stale corpus-count number in the note) and one over-worry are recorded below; neither
blocks publish.

### Confirmed correct (verified against code / command output)

- **PATCH is the right bump.** The change is a behavior-preserving internal refactor
  with no new operator-facing surface. `git diff origin/main..HEAD --stat` touches only
  the 4 split module files (+ their byte-synced plugin mirrors), one coverage test
  (`tests/quality_gates/test_goal_disposition_gate.py`, +72), the release adapter
  (+1 line), and `charness-artifacts/` (goals/retro/critique). No new CLI command, no
  new skill, no new gate, no `update_instructions`-declared behavior change → not minor.
  No compat break, no verdict change → not major. `bump_part("0.32.0", "patch")` →
  `0.32.1` (`bump_version.py:39-46`).
- **Behavior-preserving proof independently reproduced (load-bearing).** Built a
  faithful pre-split baseline by hardlink-cloning the repo to a temp mirror, overlaying
  the `origin/main` versions of `goal_artifact_disposition.py`,
  `goal_artifact_closeout_evidence.py`, and `check_goal_artifact.py`, and removing the
  two leaf modules (which did not exist pre-split). Ran the baseline gate and the HEAD
  gate over the full live goal corpus: **69/69 byte-identical closeout-gate verdicts, 0
  mismatches** (after stripping only the volatile `remaining_minutes`/`head_sha`
  wall-clock fields the goal already flags as the lone non-parity axis). The live tree
  was never mutated (`git status --porcelain` empty throughout). This corroborates the
  cited sha-`7a5d99ae` proof on a fresh harness.
- **Public + monkeypatch import surface preserved.** `disposition_gate_applies`,
  `auto_retro_is_blank`, `find_disposition_optout`, `retro_lists_improvements`,
  `_mask_fences`, `apply_disposition_rungs`, and `check_complete_evidence` all still
  resolve on their original modules (imported and asserted present). `python3
  scripts/check_export_safe_imports.py` validated 448 files; `check_plugin_import_smoke.py`
  imported every `.py` under `plugins/charness` successfully.
- **Generated-surface sync ready.** `packaging/charness.json` is the single version
  source; `sync_root_plugin_manifests.py` regenerates the root marketplace + plugin
  manifests (`expected_root_artifacts` / `export_plugin_tree`). All 5 surfaces currently
  read `0.32.0` consistently (`packaging/charness.json`, `.claude-plugin/marketplace.json`,
  `plugins/charness/.claude-plugin/plugin.json`, `plugins/charness/.codex-plugin/plugin.json`)
  with no drift, so the publish bump-and-regen starts from a clean, consistent base.
- **Plugin mirror byte-synced.** `git diff --quiet plugins/` is clean (exit 0). The two
  new leaf modules and the two trimmed monoliths are byte-identical between source
  (`skills/public/achieve/scripts/`) and the mirror (`plugins/charness/skills/achieve/scripts/`),
  confirmed by `diff -q` on all four files. (Note the mirror flattens `skills/public/...`
  → `skills/...`; the `git diff --stat` `plugins/charness/.../scripts/...` paths are the
  flattened mirror, not a separate tree.)
- **Length gate goal achieved.** `check_python_lengths.py` exits 0 (732 files
  validated). The two split targets are no longer in the advisory warn band — the split
  restored the single-file headroom that motivated the work. Remaining warn-band files
  (`issue_tool.py`, two `publish_release_*` files, one test) are pre-existing and
  unrelated.
- **`update_instructions` accurate and does not overclaim.** The new top entry
  (`.agents/release-adapter.yaml:34`) correctly states: patch / behavior-preserving
  internal refactor; both leaf names; the byte-identical closeout-gate verdict claim;
  the public+monkeypatch surface preservation; the byte-synced mirror; and the inert
  draft-goal disclosure with its `Status: draft` / "changes no behavior until a
  maintainer activates it with `/goal`" framing. The OPERATOR NOTE ("existing
  goal/closeout verdicts are unchanged and installed-plugin consumers inherit no new
  blocking behavior") holds against the reproduced parity proof.
- **Staleness guard will pass.** `update_instructions_version_blocker`
  (`publish_release_preflight.py:69-95`) blocks publish only when the note still
  describes the previous version (`0.32.0`) but not the target (`0.32.1`). The new entry
  contains `0.32.1`, so the guard passes.
- **Inert draft goal disclosed and well-formed.** Commit `2372d01f` adds only the single
  artifact (`2026-06-09-deferred-queue-341-340-activation-preflight.md`, 270 insertions,
  no code). It is `Status: draft`, the closeout gate returns `ok: true, status: draft`
  (it does not require the complete-only sections), and its inclusion is named in the
  v0.32.1 note — matching the v0.32.0 precedent of shipping an inert draft goal under
  explicit disclosure.
- **Publish boundary / git state clean.** `git status --porcelain` is empty; `HEAD` is
  `e26726ee` on `main`, 5 commits ahead of `origin/main`. The release-prep commit
  (`e26726ee`, the adapter note) is already committed, so the publish will not refuse a
  dirty worktree. No uncommitted file would dirty the publish.

### Blockers

- None.

### Should-fix (non-blocking; can ship as-is or trim next touch)

- **[SHOULD-FIX] Stale corpus count "68 goals" in the note and goal artifact.** The
  v0.32.1 `update_instructions` and the closeout goal say the parity proof covered "68
  goals". The live corpus now holds 69 goals (my reproduced run shows 69/69
  byte-identical) because the inert draft goal authored in this same bundle was added to
  `charness-artifacts/goals/` after the proof was first run at 68. The number is a
  point-in-time corpus-size snapshot, not a verdict claim, and it understates (not
  overstates) coverage, so it is honest-but-stale rather than an overclaim. Worth
  reconciling to "69" (or "the full live goal corpus") on the next adapter touch; not a
  publish blocker.

### Over-worries (raised, not blocking)

- **Generated-surface drift after bump.** The 5 surfaces are currently consistent at
  `0.32.0` and regenerated from the single `packaging/charness.json` source by
  `sync_root_plugin_manifests.py`; the publish path bumps and regenerates them together,
  so a half-bumped surface set is not a realistic risk here.
- **Draft goal tripping a commit-boundary gate at publish.** The draft goal validates
  clean as `status: draft` and is already committed, so it neither dirties the tree nor
  fails a closeout gate at the publish boundary.
