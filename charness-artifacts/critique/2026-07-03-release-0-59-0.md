# Critique Review — Release 0.59.0
Date: 2026-07-03

Target: release-critique (`skills/public/critique/references/release-critique.md`).

## Decision Under Review

Push `main` (46 commits since `v0.58.0`, clean fast-forward) and publish charness
**0.58.0 → 0.59.0 (MINOR)**. Headline consumer-visible change: gather
credentialless-by-default (commit `065e3fad`, refs #417) — `slack`/`notion`
default provider mode flips `direct-cli → none`; credentialed access becomes an
explicit adapter opt-in; support runtimes kept (reversible). Bundled: reference-
compaction slices 1–6, a test-value audit, scaffold/eval-floor internals.

## Release Scope

- Version: 0.58.0 → 0.59.0, tag `v0.59.0`, MINOR bump.
- For consumers: one behavior change — plain (no-adapter) Slack/Notion `gather` is
  now credentialless (stops clean) instead of pointing at the charness-owned
  provider wrapper. Everything else is internal.

## Surface-Lock Inventory

- Generated: `packaging/charness.json`, both plugin manifests, `plugins/charness/`
  mirror, marketplace files (all at 0.58.0 pre-bump; move together on bump).
- Consumer-visible behavior: gather `slack`/`notion` default provider mode
  (`direct-cli → none`); scaffold `--json` flag removed (internal skill machinery).
- Docs: `docs/gather-provider-ownership.md`, `skills/public/gather/SKILL.md`,
  `references/gather-provider.md`, release notes.
- Adapter/integration: gather adapter schema unchanged (only default VALUES moved).

## Failure Angles

Four bounded fresh-eye angle subagents + one separate counterweight subagent
(all read-only, shared parent worktree).

- **Gawande (operational/checklist):** manifest sync runs before tag; checked-in
  mirror in sync at HEAD; CLI reference byte-identical; fresh-checkout probes pass;
  version surfaces uniformly 0.58.0; no broken links; real-host proof not required;
  push is fast-forward. **Nothing Act-Before-Ship.**
- **Minto (communication):** the release had no operator-facing migration artifact
  for the gather default flip; drafted the migration line + 3-bullet summary; flagged
  the bundled `--json` removal and a `gather-provider-ownership.md` self-inconsistency.
- **Raskin (humane interface):** Slack `none` message is a net honesty win, BUT the
  `SKILL.md` body falsely said Google Workspace/Drive are "off until opt-in" (Google
  stays `direct-cli` → host/export/browser); `none` payload still emitted `wrapper_path`.
- **Weinberg (bump/layer):** MINOR is honest, not MAJOR (public shape 100% intact:
  skill name, adapter field, mode enum, source names unchanged; no consumer-facing
  renames/deletes; the feared DEAD/DUP reference deletes never shipped — 0/17
  delete-safe). Right layer (default value + guidance, schema untouched). Above PATCH
  because it is user-visible runtime behavior for a whole source class.

## Counterweight Pass

Separate skeptical subagent. Verdict: **SHIP after two prose fixes; no
release-stopping blocker.** Independently verified: eval changes pass (99 targeted
tests; the new `gather-claim-fidelity/outcome-assertions.json` is advisory, does not
contradict the fix); `git add -A` sweep safe (only the two critique packets untracked,
and `charness-artifacts/` is repo state that should ship); no half-landed reference-
compaction (`quality-claim-fidelity` demotions carry their enum inline in
`references/catalog.yaml`; demoted refs still on disk); deletions delete-safe (only an
orphan `render_issue_57_design_study.py`, zero refs). Sharpened Act #2: the Google
overclaim was mirrored in SKILL.md **and** the ownership doc and required a re-sync.

## Structured Findings

<!-- allowed enums (substitute only these) — bin: act-before-ship | bundle-anyway | over-worry | valid-but-defer; evidence: strong | moderate | weak | contested; action: fix | file-issue | document | defer. action: file-issue also needs a follow-up: (issue URL or 'deferred ' plus a handoff anchor). -->
- F1 | bin: act-before-ship | evidence: strong | ref: charness-artifacts/release/v0.59.0-notes.md | action: fix | note: no operator-facing migration artifact existed for the gather default flip; wrote release notes with the opt-in migration line, 3-bullet operator summary, internal --json note, and honest Notion framing (no Slack-style stop claim). RESOLVED.
- F2 | bin: act-before-ship | evidence: strong | ref: skills/public/gather/SKILL.md:30 | action: fix | note: SKILL.md body + gather-provider-ownership.md Default Boundary falsely grouped Google Workspace/Drive as off-until-opt-in; scoped credentialless-none to slack/notion and described Google's host/export/browser route accurately; re-synced mirror. RESOLVED.
- F3 | bin: bundle-anyway | evidence: moderate | ref: skills/public/gather/scripts/advise_slack_path.py:71 | action: fix | note: none payload still emitted wrapper_path (export-thread.sh) — the exact route the flip meant to stop advertising; stripped it from the none branch (kept in the opt-in branches). RESOLVED.
- F4 | bin: bundle-anyway | evidence: moderate | ref: charness-artifacts/release/v0.59.0-notes.md | action: document | note: commit 7d2bb959 removed --json from scaffold_*_artifact.py (self-labeled breaking, but internal skill machinery, zero stale callers); documented in release notes as internal, public behavior unchanged. RESOLVED.
- F5 | bin: valid-but-defer | evidence: moderate | ref: skills/public/gather/references/gather-provider.md:50 | action: defer | note: Notion flipped to none but has no advise_notion_path.py and no notion.so redirect in gather_plan.py, so the default is declarative-only (a private Notion URL falls through to generic public-URL fetch, not a Slack-style stop). No regression (pre-existing). Track as follow-up; release notes avoid over-claiming a Notion stop.
- F6 | bin: over-worry | evidence: weak | ref: n/a | action: defer | note: git add -A publish sweep — only the intended critique packets are untracked; charness-artifacts is repo state that should ship. Standing sharp edge for future releases, not a 0.59.0 issue.

## Reviewer Tier Evidence

<!-- allowed Host exposure state: pending-parent-spawn | requested_fields_sent | metadata-hidden | host-defaulted | unsupported | applied. Use applied only with Application state: host-confirmed: plus a concrete signal. -->
- Requested tier: high-leverage bounded fresh-eye subagents (adapter `reviewer_tiers.high-leverage`).
- Requested spawn fields: 4 distinct-lens angle reviewers (Gawande/Minto/Raskin/Weinberg) + 1 separate counterweight, each single-lens, read-only, shared parent worktree.
- Host exposure state: applied
- Application state: host-confirmed: all 5 bounded subagents spawned via the Agent tool and returned verdicts (angle agents + counterweight).

## Fresh-Eye Satisfaction

nested-delegated — the parent spawned 5 fresh bounded subagents; every reviewer
was a distinct fresh context, not a same-agent pass. Consensus: SHIP 0.59.0 as
MINOR after the two Act-Before-Ship fixes (both now applied) and the bundled nits.

## Operator Action Required

- Land `charness-artifacts/release/v0.59.0-notes.md` (done) and use it as the
  GitHub release body so the gather migration line reaches consumers.
- Gather fix + wrapper_path strip + Google-scope correction committed and mirror
  re-synced before the bump (done).

## Upgrade Path

Consumers who relied on no-adapter Slack/Notion `gather` re-enable it by adding to
`.agents/gather-adapter.yaml`: `gather_provider.slack.mode: direct-cli` (maintainer
owns the grant) or `host-mediated` (route via the runtime's Slack capability); same
under `notion`. `github` and `google_workspace` defaults are unchanged. No rollback
needed for public/local sources.

## Deliberately Not Doing

- Building `advise_notion_path.py` / a `notion.so` redirect this release (F5) — no
  regression; deferred as a follow-up.
- Stripping `support_skill_path`/`runtime_contract_path` from the `none` payload —
  only `wrapper_path` was the runnable route; the doc paths are non-runnable references.

## Next Move

Ship 0.59.0 (MINOR): critique gate satisfied by this artifact; run
`publish_release.py --part minor --execute --critique-artifact <this file>`.
#417 issue closeout remains separate (post-publish).
