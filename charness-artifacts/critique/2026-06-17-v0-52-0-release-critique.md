# v0.52.0 Release Critique

Date: 2026-06-17
Target: v0.52.0 release
Packet consumed: `charness-artifacts/critique/2026-06-16-225543-packet.md`
Fresh-Eye Satisfaction: parent-delegated

## Decision Under Review

Release Charness v0.52.0 as a minor release for the Operator Decision Queue
surface added after issue #381.

## Release Scope

Target version: 0.52.0
Target tag: v0.52.0
Consumer change: fresh achieve/HOTL/handoff work gets an explicit Operator
Decision Queue, and complete-state evidence for fresh or undated/corrupt-date
goals must disposition that queue.

## Surface-Lock Inventory

- `.agents/release-adapter.yaml` update instructions for 0.52.0.
- Versioned packaging manifest and generated plugin/marketplace manifests
  produced by the release helper.
- GitHub release notes generated by the release helper.
- Installed plugin refresh through adapter-declared `charness update`.
- Fresh-checkout probes declared in the release adapter.

## Failure Angles

- Gawande/checklist: publish must happen through the release helper so the tree
  is clean at the publish boundary and declared fresh-checkout probes run before
  tag/publication closeout.
- Minto/communication: the initial update instruction overstated the final-report
  shape as a `##` section and understated the date-gated floor semantics.
- Raskin/operator experience: the release note must make the new closeout form
  and fail-closed behavior discoverable without waiting for a validator failure.

## Counterweight Pass

Act before ship:
- Done - update instructions now say goal artifacts carry `## Operator Decision
  Queue`, final reports render an `Operator Decision Queue:` line, and valid
  pre-rule `Created` dates are grandfathered while missing/corrupt dates fail
  closed.
- Required at publish - use `publish_release.py --execute` with this critique
  artifact so the helper owns bump, sync, release gates, fresh-checkout probes,
  tag/public release verification, and maintainer install refresh.

Bundle anyway:
- Keep the minor bump. The release adds meaningful operator-facing behavior and
  a fresh-goal completion floor without migration for valid pre-rule artifacts.
- Keep the #371 non-claim. This release does not prove invocation-bound
  `agent-browser` process/profile teardown.

Over-worry:
- Do not hold v0.52.0 for a richer fresh-host probe that creates a goal and
  exercises the queue. Focused deterministic tests cover the queue floor and
  producer shape; the release helper's declared probes cover the configured
  release boundary.

Valid but defer:
- A future CLI/doc affordance could list complete-state goal floors, but the
  template, references, validator message, and release note are enough for this
  release.

## Operator Action Required

- None before publish beyond using the release helper.
- Operators update with `charness update`; valid artifacts created before
  2026-06-17 need no migration.

## Upgrade Path

Run `charness update` to pull v0.52.0. Restart Claude Code or Codex if host
visibility still depends on marketplace rediscovery. Roll back by reinstalling
or pinning v0.51.1; no data migration needs undoing.

## Structured Findings

- F1 | bin: act-before-ship | evidence: strong | ref: .agents/release-adapter.yaml:34 | action: fix | note: update instruction needed to distinguish artifact section from final-report line and name missing/corrupt Created fail-closed behavior; fixed before publish.
- F2 | bin: act-before-ship | evidence: strong | ref: skills/public/release/scripts/publish_release_cli.py:319 | action: fix | note: publish requires clean worktree boundary; commit adapter and critique artifacts before publish helper execute.
- F3 | bin: over-worry | evidence: moderate | ref: tests/quality_gates/test_goal_artifact_operator_queue.py | action: defer | note: richer fresh-host queue exercise is useful later but deterministic tests cover this release boundary.

## Reviewer Tier Evidence

- Requested tier: high-leverage
- Requested spawn fields: model=gpt-5.5, reasoning_effort=medium, service_tier=priority
- Host exposure state: requested_fields_sent
- Application state: fields accepted by spawn call; provider application not independently confirmed

## Fresh-Eye Satisfaction

parent-delegated: Boole reviewed the checklist/operational angle, James reviewed
the release-note structure angle, Pascal reviewed operator experience, and
Turing performed the separate counterweight pass.
