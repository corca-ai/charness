# Release Critique — v0.25.0 (changed-line mutation-coverage producer + bundle)

Date: 2026-06-07
Target reference: `release-critique`
Execution: subagents (3 angle + 1 counterweight, bounded fresh-eye)
Fresh-Eye Satisfaction: parent-delegated
Packet Consumed: `charness-artifacts/critique/2026-06-06-232037-packet.md`

## Change

Cut **v0.25.0 (MINOR from 0.24.1)** and publish (tag + GitHub release) via
`skills/public/release/scripts/publish_release.py --part minor`. Bundles all
commits since `v0.24.1` (`git log v0.24.1..HEAD`). Headline: the changed-line
mutation-coverage **pre-push gate is now ACTIVE** (consumer slice 1 +
producer slice 2, content-fingerprint freshness). Also bundles a
quality-scan/closeout-discipline goal (advisory-interpretation contract, nose
pilot, #2b validator, rca advisory). The work commits already pushed to
`origin/main` with the full pre-push suite green (72 passed, 0 failed),
including `check-changed-line-mutation-coverage`.

Success: correct bump level; all version surfaces synced to 0.25.0; canonical
quality passes; publish boundary honest; operator record truthful.
Out of scope: re-reviewing the producer code (already fresh-eye reviewed, REVISE
folded); internals of the other bundled slices.

## Angles

- Gawande (checklist/operational) — release-readiness gates.
- Minto (structure/communication) — operator legibility of the record.
- Raskin (humane interface) — blocked-push UX + flag discoverability.
- Counterweight — skeptical triage of the above.

## Surface-Lock Inventory

- Generated: `packaging/charness.json` version + Claude/Codex plugin manifests +
  `.claude-plugin/marketplace.json` (Codex marketplace is a path pointer, no
  version). All bump atomically via `bump_version.py` + `sync_root_plugin_manifests.py`.
- Consumer-visible behavior: NONE for plugin consumers — the gate wiring
  (`run-quality.sh` / `.githooks`) is charness-repo-local and not exported; the
  gate is non-blocking by construction absent fresh coverage. New operator
  surface is the charness-repo-local `run_slice_closeout.py --produce-mutation-coverage`.
- Docs: `charness-artifacts/release/latest.md` (regenerated from adapter
  `update_instructions`); `docs/conventions/implementation-discipline.md`.

## Counterweight Triage (final)

### Act Before Ship

- **B — `update_instructions` must name 0.25.0 AND be truthful.** The helper's
  `update_instructions_version_blocker` hard-stops publish until the text
  mentions `0.25.0` (and not only `0.24.1`); `latest.md` is regenerated wholesale
  from `update_instructions`. The stale text said "0.24.1 … no behavior change",
  which would ship a false v0.25.0 record. Fix = one honest bullet naming the
  now-active gate (block-only-on-uncovered-changed-lines, legible skip), **not** a
  full rewrite. DONE in `.agents/release-adapter.yaml`.
- **C — commit the critique artifact + clean the worktree.** Publish refuses a
  dirty tree and requires `--critique-artifact` to be git-tracked. This file +
  the packet are committed in the release-prep commit.
- **A — use the correct helper path** `skills/public/release/scripts/publish_release.py`
  (the repo-root `scripts/publish_release.py` does not exist).

### Bundle Anyway

- **F — document `--produce-mutation-coverage`** next to the sibling closeout
  flags in `docs/conventions/implementation-discipline.md` (the one omission is
  the flag this release ships). DONE in the release-prep commit.
- **E — stale `--write-head-marker` in `docs/handoff.md`** → `--write-fresh-marker`.
  Folded into the closeout handoff refresh (handoff writes are closeout-only per
  the operating contract).

### Over-Worry (skipped)

- **D — hand-written `--notes-file`.** Gold-plating for a maintainer-internal
  harness plugin: `--notes-file` is optional, no gate enforces operator prose,
  and `latest.md` (fixed via B) is the durable operator record. Omit it; let the
  release backend generate notes from commits.
- **G — MINOR-vs-breaking consumer breakage.** Verified non-issue: gate wiring is
  repo-local, not plugin-exported, and non-blocking by construction. MINOR is
  correct.

### Valid but Defer

- Blocked-push message could also point to `mutation-testing.md` (it already
  prints actionable `path:line` targets) — fast follow-up, not a blocker.
- Skill-portability promotion of the gate doctrine to `quality` —
  already tracked as the handoff "Skill portability" follow-up.

## Deliberately Not Doing

- No hand-written release notes file (D): the auto-generated commit notes plus the
  truthful `latest.md` headline are sufficient for this maintainer-internal repo.

## Next Move

Apply B/C/F edits, commit (clean tree + tracked critique artifact), then
`publish_release.py --part minor --execute --critique-artifact <this file>`
(omit `--notes-file`). MINOR/0.25.0 is correct; the publish boundary is
synchronous (no tag-triggered workflow), so tag + GitHub release are created and
verified in-process. No `--close-issue` (#320/#321 already closed).
