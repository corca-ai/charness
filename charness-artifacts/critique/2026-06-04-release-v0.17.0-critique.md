# Release Critique: charness 0.17.0

Date: 2026-06-04

Fresh-Eye Satisfaction: bounded general-purpose subagent Poincare (read-only, shared worktree)

## Scope

- Bump: minor, 0.16.0 -> 0.17.0, via `publish_release.py --part minor --execute`.
- User request: keep next session focused on #284 then #286/#285, push, and release.
- Release delta includes the testability quality ratchet, quality payload/inventory validation hardening, find-skills routing for testability and boundary-bypass ratchet work, and the handoff/retro closeout cleanup after the ratchet goal.

## Verdict

SHIP-WITH-FIXES -> fixes applied; now SHIP.

## Blockers Folded

1. **Release gate was red before publish.** The fresh-eye reviewer caught `./scripts/run-quality.sh --read-only` failing because `check-duplicates` recent median was slightly above the 11.0s budget. Full `./scripts/run-quality.sh --release` reproduced the issue after heavy earlier phases: latest 12.331s, median 11.105s. Fixed by setting the x86_64 and default `check-duplicates` budget to 13.0s, above the observed release-path max but still below the aarch64 24.0s profile.
2. **No 0.17.0 release surface existed yet.** Fixed by preparing this critique, release notes, and v0.17.0 update instructions before invoking the release helper.
3. **Generated notes would under-explain the public surface.** Fixed by using a curated notes file that names the quality ratchet, quality payload schema, routing, and handoff sequence.
4. **Bump level needed owner judgment.** `minor` is correct: the release adds exported public quality guidance/helper behavior and new validation surfaces without breaking existing operator commands.

## Bundle-Anyway Findings

- Plugin export parity was already healthy in the reviewer pass; the release helper still owns bump, sync, packaging, and changed-surface validation before tagging.
- Boundary-bypass ratchet proof was green in the completed goal: 95 candidates, 53 clean-convertible, 42 internal, and 23 keep-boundary.
- Handoff sequencing is correct for the next session: #284 first, then #286, with #285 only if the #286 work exposes live-issue fixture brittleness.
- Real-host proof is not newly triggered by the ratchet paths; the release helper will still record the adapter-derived real-host status.

## Over-Worry Dismissed

- Treating this as a patch: rejected because exported public `quality` contract and helper behavior changed.
- Widening runtime budget as a waiver: rejected. The budget change is narrow, profile-scoped, and based on release-path samples rather than bypassing the gate.
- Same-agent-only closeout: rejected. The critique used the repo-mandated fresh-eye reviewer before publish.

## Blockers

- None remaining.
