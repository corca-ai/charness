# Critique: #261 Mutation-Standard Policy Decision

Date: 2026-06-02
Goal:
`charness-artifacts/goals/2026-06-02-261-mutation-standard-policy-decision.md`
Carrier:
`charness-artifacts/issue/2026-06-02-261-mutation-standard-policy-decision.md`

## Execution

Fresh-Eye Satisfaction: parent-delegated
Packet Consumed: n/a (no critique adapter packet sections)
Target: decision premortem / issue closeout review

Reviewers:

- Bacon: policy-decision angle.
- James: issue-closeout angle.
- Euclid: counterweight pass.

All reviewers were bounded read-only fresh-eye subagents in the shared parent
worktree. They were instructed not to mutate files or git state.

## Change

Stage #261 to close as a no-code mutation-standard policy decision. The selected
policy is to keep the current mutation gate unchanged, accept the residual
coordination-cues survivors as equivalent/low-value target-local residue, and
carry `Close #261` in the issue carrier for default-branch auto-close after
publication.

## Findings

### Act Before Ship

- Fill the goal closeout sections before flipping the goal to complete. The
  active goal still carried stale pending/future text after slices 1-2 executed.
- Ensure the final direct-to-default commit body carries `Close #261` and the
  closeout ledger. The carrier has the keyword, but the commit body is the
  auto-close surface.

### Bundle Anyway

- Clarify in the carrier that accepted low-value survivors remain report-visible
  and countable residue, not a new hidden exclusion class.
- Keep the no-code policy closure. The policy reviewer found no contradiction
  requiring code, another full mutation campaign, or a leave-open disposition.
- Keep `decision-needed` classification. The closeout reviewer confirmed the
  carrier fields and draft verifier match this classification.

### Over-Worry

- Re-running the full mutation campaign before closure is over-worry. Existing
  proof records 514/514 executed, 467 killed, 47 survived, and 90.9% reachable
  score; the carrier does not claim fresh CI or live mutation proof.
- Adding a global equivalent-mutant rule for regex flag arithmetic, string
  identity, release-span blanking, or current-contract ordering is over-worry.
  These residuals are too local and could hide real future mutants.
- Adding a `Critique:` header to the issue carrier is over-worry for
  `decision-needed`; the closeout contract and validator skip that gate for this
  classification.
- Manually closing GitHub now is over-worry. The carrier correctly states that
  #261 remains open until publication.

### Valid But Defer

- A future registry for accepted target-local mutation residue may help if the
  pattern repeats across issues. It is not needed for #261 because the carrier
  names the survivor classes, scope, non-claims, and no-change boundary.
- Post-publication GitHub verification is still required after the maintainer
  pushes the closeout commit. Draft verification is not final issue closeout.

## Disposition

- Applied: carrier now says accepted low-value survivors remain
  report-visible/countable residue and not a hidden exclusion class.
- Applied: stale active-goal slice-log text removed.
- Applied/planned: final commit body will carry `Close #261` and the
  decision-needed ledger by using the carrier text as the commit body.

## Next Move

Run targeted artifact gates, write retro/host-probe/disposition evidence, flip
the goal to complete after `check_goal_artifact.py` passes, then commit with a
body carrying `Close #261`.
