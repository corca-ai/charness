# Critique Review
Date: 2026-06-23
Packet Consumed: charness-artifacts/critique/2026-06-23-022641-packet.md

## Decision Under Review

Publish Charness 0.54.0 as a minor release from current `main`, advancing the
version from 0.53.0 and pushing the branch, tag, GitHub release, generated plugin
surfaces, and operator update instructions through the repo release helper.

Success means the release notes, bump level, manual-tool update advisory, version
surfaces, fresh-checkout probes, and public release verification are not stale or
misleading. Out of scope: closing #397 or claiming planner-backed runtime
reference consultation is proven.

## Release Scope

- Version/tag: 0.54.0 / `v0.54.0`.
- Consumer story: maintained authoring/quality surfaces, clearer local push-safety
  signals, and behavior-preserving prompt-surface cleanup.
- Bump rationale: minor is the lightest honest bump because this release adds a
  maintained `quality` run planner, reusable claim-fidelity eval harness, and a
  local pre-commit blocking guard; these are additive public/operator behaviors
  without migration.

## Surface-Lock Inventory

- Generated/versioned surfaces: `packaging/charness.json`, plugin manifests,
  `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`,
  `plugins/charness/**`, and `charness-artifacts/release/latest.md`.
- Consumer-visible behavior: `quality` run planning, claim-fidelity eval harness,
  staged-worktree pre-commit guard, manual/advisory tool behind-latest messages.
- Documentation/artifacts: release adapter `update_instructions`, quality/cautilus
  artifacts, handoff and recent lesson records.
- Prompt/skill semantics: exported `quality`, `debug`, and `impl` skill surfaces.

## Failure Angles

- Gawande operational: publish cannot proceed without a tracked final critique
  artifact, post-helper version drift check, fresh-checkout probes, and a 0.54.0
  release narrative.
- Minto communication: the release note must include shipped `debug`/`impl`
  prompt-surface cleanup and should not bury operator action in internal terms.
- Raskin interface: manual-tool behind-latest advisory text must not tell users to
  use an internal "manifest install/update route" when a docs URL is available.
- Jackson framing: patch would be dishonest for the new maintained planner,
  harness, and pre-commit behavior; release notes must explicitly state #397 is
  not closed.

## Counterweight Pass

- Act before ship: create and track this final critique artifact before invoking
  `publish_release.py --execute`.
- Bundle anyway: keep the advisory wording fix and tests; keep the expanded
  0.54.0 release note with `debug`/`impl` cleanup and the #397 non-claim.
- Over-worry: current 0.53.0 version surfaces and stale `release/latest.md` are
  expected pre-helper state; the release helper owns bump, sync, narrative rewrite,
  and public verification.
- Valid but defer to helper flow: fresh-checkout probes and `release/latest.md`
  verification are post-mutation release-helper proof, not manual pre-helper edits.

## Operator Action Required

- Before publish: ensure this artifact is tracked and pass it as
  `--critique-artifact charness-artifacts/critique/2026-06-23-release-0-54-0.md`.
- During publish: run the repo helper with `--part minor`; require its normal
  mutate, sync, release-quality, fresh-checkout probe, public-release, and
  install-refresh proof.

## Upgrade Path

Operators run `charness update` to pull 0.54.0. No migration or consumer-side
manual reproof is required. Rollback is reinstalling or pinning 0.53.0.

## Deliberately Not Doing

- Not claiming #397 is closed.
- Not claiming the report-first planner has already produced a passing real
  runtime consultation capture.
- Not manually editing `charness-artifacts/release/latest.md` before the helper;
  the helper must regenerate and audit that release record.

## Structured Findings

- F1 | bin: act-before-ship | evidence: strong | ref: git status; charness-artifacts/critique/2026-06-23-022641-packet.md | action: fix | note: tracked final release critique artifact for 0.54.0 is required before publish
- F2 | bin: valid-but-defer | evidence: strong | ref: current_release.py fresh_checkout_probes | action: defer | note: configured fresh-checkout probes belong to helper/post-helper release verification
- F3 | bin: over-worry | evidence: strong | ref: current_release.py surface_versions; charness-artifacts/release/latest.md | action: document | note: 0.53.0 version surfaces are expected pre-publish helper state
- F4 | bin: bundle-anyway | evidence: strong | ref: scripts/control_plane_lifecycle_lib.py:151; tests/charness_cli/test_tool_lifecycle.py:42 | action: fix | note: manual tool advisory wording repair is ready to keep in release
- F5 | bin: bundle-anyway | evidence: strong | ref: .agents/release-adapter.yaml:34 | action: document | note: release note now includes debug/impl cleanup and #397 non-claim
- F6 | bin: over-worry | evidence: strong | ref: .agents/release-adapter.yaml:34; publish_release.py --part minor --prep-update-instructions | action: document | note: patch-vs-minor mismatch is resolved as 0.54.0 minor

## Reviewer Tier Evidence

- Requested tier: high-leverage bounded fresh-eye release critique.
- Requested spawn fields: model gpt-5.5, reasoning_effort medium, service_tier priority.
- Host exposure state: requested_fields_sent
- Application state: host signal: multi_agent_v1 spawned four angle reviewers and one counterweight reviewer; completed agents 019ef24d-50e8-7692-ae2a-34cfee4bdcee, 019ef24d-d114-7d51-a4d3-6f948f1e20cf, 019ef24e-18f8-7703-871a-34319e68417d, 019ef24e-3a09-7fe1-b03f-fa42cc1c88dd, and 019ef252-4005-7570-9e4a-788129919436 returned read-only findings.

## Fresh-Eye Satisfaction

Fresh-eye reviewer verdict: ship after the final critique artifact is tracked,
with the 0.54.0 minor bump and release-note/advisory fixes kept. The packet label
was prepared before the target changed and still says `0.53.1`; reviewers were
given the current 0.54.0 scope in their task prompts, and this artifact records
the corrected target.
