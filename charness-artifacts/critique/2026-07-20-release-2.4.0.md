# Critique Review
Date: 2026-07-20

## Decision Under Review

Releasing charness 2.4.0 (minor bump from 2.3.1): push main plus tag, GitHub
release via the repo publish helper. Scope since v2.3.1: the sibling-scan
Tier-1 concurrency/tie-direction fixes and Tier-2 test de-flake (bug/test
repairs), the `verify-closeout` additive `confirmation` object with its
scope-tracking verb, and observer-identity recording on post-publication
distinct-channel verdicts with the rendered release-artifact line (additive
operator surfaces). Each slice already carries its own bounded critique
artifact and locked closeout with broad standing pytest; this critique reviews
the release decision itself.

## Failure Angles

- Bump level: could any consumer treat the new `confirmation`/`observer` keys
  or the mtime-tie flip as a compatibility break demanding major, or is the
  scope patch-only?
- Release readiness: version-surface consistency and mirror fidelity for the
  exported 2.4.0 package.
- Release-note honesty: commit-derived notes overclaiming terminal success.
- Publication boundary: missed real-host or host-hook proof triggers.
- Issue linkage: the release must not close the tracked follow-up issue for
  the machine-distinct CI-side observer.

## Counterweight Pass (four-bin triage)

- K2 | over-worry (confirmed, no change): minor is correctly leveled — both
  new surfaces are strictly additive (status tokens unchanged; observer
  rendered behind a presence check), no consumer strictly iterates the payload
  keys, and the channel-label rename happened within this unreleased slice so
  nothing shipped is renamed. The `>=`→`>` flip is a fail-safe behavior repair
  in a private single-caller function, patch-class, riding along.
- K2 | over-worry (confirmed, no change): every version surface sits
  consistently at 2.3.1 pre-bump with zero premature 2.4.0 references, and the
  changed-file mirrors byte-match canonical; the helper performs the bump and
  re-validates consistency at release time.
- K2 | over-worry (confirmed, no change): no real-host trigger matched — the
  changed paths touch none of the external-tool-control-plane surface or the
  adapter's real-host path globs; the matched `cli_skill_surface_change_globs`
  route to the helper's own fresh-checkout probes, which is the expected local
  gate.
- K1 | act-before-ship (fixed): the read-only reviewer could not read commit
  bodies, so two log-backed spot checks were named as pre-push obligations.
  Both executed clean by the parent through the direct git channel: zero
  close-keyword-plus-number matches in the release range (the follow-up issue
  stays open), and the only "verified" mentions are titles describing the
  migration that removes that vocabulary, not terminal-success claims.
- K4 | valid-but-defer (tracked): post-publication proof from a
  machine-distinct observer remains impossible from the publisher's
  environment by definition; the release records its observer identities
  honestly and the CI-side observer stays a tracked open follow-up.

## Recurrence Verdict

The release ships the very mechanisms that reduce this critique class's
future risk: closeout claims now carry observer/channel/scope, and
publication verdicts name their observer identity, so the next release's
evidence reads its own distinctness limits instead of implying them.

## Boundary Ownership

- Verdict: owned-correctly

The publish helper owns bump/sync/tag/publish sequencing and its refusal
gates; the release critique judges the decision; per-slice correctness stayed
with the per-slice critiques; issue closeout linkage stays with the issue
skill (unused this release by design).

## Reviewer Tier Evidence

<!-- allowed Host exposure state enums only -->
- Requested tier: high-leverage
- Requested spawn fields: none — Claude Code host, typed `bounded-reviewer`
  (Read/Grep/Glob) with session-model inheritance per the repo per-host
  subagent contract; no Codex model requested on this host, so the omission is
  contract-conformant, not a degradation.
- Host exposure state: host-defaulted
- Application state: the host spawned the typed `bounded-reviewer` agent by
  name; the read-only envelope bound and the rail-1 reviewer-boundary
  fingerprint verified clean (no index/worktree drift) after the reviewer
  returned, so approvals are valid and the reviewer ran on the parent's
  session-inherited model.

## Fresh-Eye Satisfaction

parent-delegated — one high-leverage bounded release reviewer (five angles,
in-report counterweight); verdict approve with two log-backed spot checks,
both executed clean by the parent before publish; rail-1 reviewer-boundary
fingerprint verified clean.
