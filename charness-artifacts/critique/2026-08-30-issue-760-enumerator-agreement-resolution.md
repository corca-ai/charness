# Issue #760 Changed-Path Owner Resolution

Date: 2026-08-30
Classification: resolution verification
Fresh-eye satisfaction: parent-delegated — a distinct Luna reviewer inspected the integrated owner delegation and ran the agreement, identity, and deletion discriminator matrix read-only.
Verdict: PASS for one canonical changed-path enumeration owner.

## Decision Under Review

Close #760 if critique identity binding and surface narration select paths
through one canonical capability, and fixtures make known drift axes and a
deliberate disagreement observable.

## Verification Scope

- Integrated commit: `d9cea8a55`.
- Parent focused run: 75 agreement, reviewed-input identity, and packet tests
  passed in 6.82s.
- Independent Luna slice: four agreement tests, four identity discriminators,
  and three surface-deletion discriminators passed.
- Ruff and official tokei checks passed for the changed files.

## Failure Angles

- Duplicate enumeration: identity could reintroduce its own Git flag set.
  `_auto_paths()` now delegates to `surfaces_lib`, and the ownership fixture
  asserts the exact module object.
- Quoted names: default `core.quotepath=true` could turn non-ASCII paths into a
  different spelling. The matrix pins the real NUL-delimited name.
- Merge omission: a merge commit could produce no paths without `-m`. The
  fixture requires both parent-relative paths.
- Staged/worktree gap: a staged path removed from disk could vanish between
  `diff HEAD` and `--cached`. Staged, unstaged, deleted, staged-then-removed, and
  untracked forms agree.
- Rename and non-blob shapes: rename and submodule changes remain selected by
  the shared capability without declaring a consumer topology policy.
- False confidence: a matrix where both consumers accidentally call the same
  broken stub could always pass. The deliberate divergent-enumerator control
  must fail agreement.
- Scope expansion: this is one Charness capability boundary, not a universal
  Git abstraction or a rule for consumer repository topology.

## Counterweight

An agreement gate alone would preserve two implementations. Delegation removes
the duplicate owner, while the matrix remains valuable as a consumer contract
and negative control. The implementation does not need a new generic Git layer;
the existing surface enumerator already owns the required repository facts.

## Findings

No blocking or material advisory finding remains in the bounded #760 claim.

## Reviewer Tier Evidence

- Requested tier: high-leverage bounded fresh-eye.
- Requested spawn fields: Luna model lane under the operator's all-Luna rule.
- Host exposure state: requested_fields_sent
- Application state: metadata-hidden
- Delivery state: findings-received
- Execution mode: typed-subagent

## Boundary Ownership

- Producer: `scripts/surfaces_lib.py` changed-path enumeration.
- Consumers: surface narration and reviewed-input identity binding.
- Owning surface: `collect_changed_paths*` in `surfaces_lib`.
- Verdict: owned-correctly

AI-provenance: Agent-authored resolution critique from integrated source,
focused tests, official tokei evidence, and an independent Luna fresh-eye. No
provider state, release, universal Git abstraction, or consumer topology claim
is made.
