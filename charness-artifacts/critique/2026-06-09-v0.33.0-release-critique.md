# Release critique — charness v0.33.0 (minor; #340 find-skills support routing + #341 mutation-gate reclassification)

Date: 2026-06-09

Fresh-eye satisfaction: satisfied — the v0.33.0 release decision was reviewed by a
fresh-eye reviewer in a separate agent context, distinct from the agent that produced
the slice work. The reviewer judged the RELEASE decision (bump level, generated-surface
readiness, `update_instructions` accuracy, issue-closeout carrier, publish boundary,
operator risk), not the already-reviewed slice code (each slice carried its own fresh-eye
SHIP critique). The reviewer independently re-ran the bump-level, surface-consistency,
mirror byte-sync, staleness-guard, and issue-state checks.

## Scope

The minor bump 0.32.1 → 0.33.0 publishing the 6 local commits ahead of `origin/main`:
slice 1 (#341, `84dde097`) reclassifying per-file-budget mutation drops as advisory and
killing 5 survived `main()` mutants; slice 2 (#340, `c6c0ee9a`) surfacing shipped
charness-support skills (specdown/cautilus/agent-browser) via find-skills support routing;
slice 3 (`e30643cc`) pinning the goal `Activation:`-preamble enforcement test; the
bundle-boundary coverage fixup (`6e751831`); the goal closeout + session retro
(`6de578d6`); an inert `Status: draft` next-queue goal artifact (`8d5188c5`); and the
v0.33.0 `update_instructions` release-prep entry. Each slice carried a fresh-eye SHIP
critique, the bundle broad gate passed (73/0), the changed-line producer is `ok: true`
over the final range, and the full pytest suite is green (2658).

## Verdict

SHIP. MINOR is the honest bump (#340 adds new adoptable operator-facing behavior on the
find-skills recommendation surface); the generated surfaces are sync-ready and consistent
at 0.32.1; the plugin mirror is byte-synced; the `update_instructions` entry is accurate,
contains the literal `0.33.0` (staleness guard passes), and does not overclaim; the issue
carriers are correct; and the tree is clean. One should-fix (a `.gitignore` gap) and the
honest #341 status note below; neither blocks publish.

### Confirmed correct (verified against code / command output)

- **MINOR is the right bump.** #340 (`c6c0ee9a`) adds `shipped_support_recommendations_for_task`
  (`skills/public/find-skills/scripts/list_capabilities_lib.py`) wired through
  `_task_support_recommendations` (`list_capabilities.py`). An operator running
  `find-skills --recommend-for-task` now receives a NEW recommendation route —
  shipped-but-unmaterialized support skills surfaced in `support_skill_recommendations`
  carrying the integration `summary` and authoring pointer — genuinely new adoptable
  operator-facing output, not a shape-preserving repair (which would be patch). The
  inventory arrays and existing recommendations are behavior-preserved (additive). #341
  is maintainer-internal and rides along. Per `version-policy.md` Minor ("meaningful new
  behavior existing users can adopt without migration").
- **Generated surfaces consistent at 0.32.1.** `packaging/charness.json`,
  `.claude-plugin/marketplace.json`, both `plugins/charness/.*-plugin/plugin.json` all
  read `0.32.1`; `.agents/plugins/marketplace.json` carries no version field (source-path
  manifest). The bump helper writes the single manifest source and regenerates the derived
  surfaces together — no half-bumped risk.
- **Plugin mirror byte-synced.** `git status --porcelain plugins/` empty; the find-skills
  scripts and mutation libs are `diff -q` SYNCED source↔mirror (the mirror collapses
  `skills/public/...` → `skills/...`).
- **`update_instructions` accurate, contains `0.33.0`.** The new entry
  (`.agents/release-adapter.yaml`) describes #340 (additive support routing, "not
  materialized locally", `summary` field) and #341 (per-file-budget drop → advisory
  `changed_files_excluded_by_per_file_budget`, changed-line arm still blocks, budget
  unchanged) accurately against the diff, discloses the inert draft goal, and the staleness
  guard passes (the entry names the target version).
- **Issue carriers correct.** #340 is OPEN; `c6c0ee9a` carries `Closes #340.` → closes on
  push. #341 must NOT be commit-closed by this release; it was **already CLOSED**
  (`COMPLETED`, 2026-06-09T07:59:35Z) by the scheduled mutation marker (the intended
  auto-closing carrier, `commit_id: null`) — so re-closing on push is a harmless no-op and
  the "do NOT manual-close #341" boundary is honored.
- **Publish boundary clean.** HEAD on `main`, 6 commits ahead of `origin/main`, index
  clean; the only uncommitted change is the v0.33.0 adapter entry (release-prep), staged
  with the bump.

### Blockers

- None.

### Should-fix (non-blocking)

- **[SHOULD-FIX] `reports/` is not fully gitignored.** `.gitignore` ignores only
  `reports/mutation/`, not `reports/`, so session gate logs (`reports/broad-gate.log`,
  `reports/bundle-*.log`) are untracked-but-not-ignored and a blanket `git add -A` could
  pollute a commit. Cleaned the stray logs before publish; the publish helper stages named
  surfaces (not `-A`), so no realistic risk for THIS release. Worth adding `reports/` to
  `.gitignore` on the next hygiene touch.

### Honest status note (not a blocker)

- **#341 closed on a range-sensitive scheduled run, not on this fix.** #341 auto-closed at
  07:59:35Z (before this fix lands) — a scheduled run over the pre-fix `main` happened to
  go green because the per-file-cap selection drop is range-sensitive (the goal anticipated
  this). The slice-1 reclassification is therefore the STRUCTURAL robustness fix that
  prevents the per-file-cap class from re-blocking when a module split produces an
  oversized-but-well-covered changed file — forward-looking, not the cause of this
  instance's close. The release narrative reflects this honestly.

### Over-worries (raised, not blocking)

- **Generated-surface drift after bump.** The 5 surfaces are consistent at 0.32.1 and
  regenerated from the single `packaging/charness.json` by `sync_root_plugin_manifests.py`;
  the publish path bumps and regenerates together.
- **Adapter YAML parse error.** A PyYAML strict scan chokes on the historical colon-backtick
  prose, but the release path uses the repo's own lenient `adapter_lib` loader (which all
  prior releases shipped through); a false alarm.
