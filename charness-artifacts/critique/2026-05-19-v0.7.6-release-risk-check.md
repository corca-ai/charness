# v0.7.6 Release Risk Check

## Scope

Prepare a patch release for the merged `create-skill` adapter work and the
worktree workflow inventory update currently ahead of `origin/main`.

## Findings

- Act before ship: publish must bump from `0.7.5` to `0.7.6`, not reuse the
  existing `v0.7.5` tag.
- Act before ship: release helper should close Charness issue `#178`, because
  the release contains the `create-skill` adapter contract and hardening work.
- Bundle anyway: use generated GitHub notes; the release artifact will carry
  the detailed verification ledger.
- Over-worry: no extra real-host proof is triggered by the changed paths in
  this slice; release adapter checks reported no matching real-host surface.

## Decision

Proceed with the repo-owned release helper using a patch bump and issue
closeout.
