# Release Critique — v6.1.0 minor cut
Date: 2026-08-18

## Decision Under Review

Cut charness v6.1.0 (minor from 6.0.1) over `da6913245..242de7c6c` — the
bump-rationale operator surface plus its five review-round repairs, the #636
one-pass debug-validator reporting, the critique `blocked` vocabulary fix, and the
session's records — pushed to origin with the pre-push lane green (95 passed, 0
failed) before this cut.

## Failure Angles

Raised by the bounded fresh-eye reviewer, not the author:

- **Bump-level honesty.** MINOR is earned by exactly one additive operator surface:
  `--bump-rationale` and the record's Bump Rationale section, which `git show` proves
  absent at the base commit. Everything else in range is acceptance-equivalent repair;
  nothing renames, removes, or changes invocation expectations.
- **Record-parser safety.** The new last-emitted section cannot enter the Release
  State ledger span (`audit_public_release_narrative` terminates at the next heading)
  and blockquoted rationale cannot inject a target-version claim
  (`TARGET_VERSION_RE` is anchored); both arguments are recorded at
  `publish_release_premutation_sections.py`.
- **Export parity.** All four changed scripts exist in `plugins/charness/` with the
  load-bearing semantics identical; `packaging/charness.json` is correctly pre-bump
  at 6.0.1 on all three fields.

## Counterweight Pass

- The one LIVE caution is procedural, not structural: every `--execute` stops at
  `prepared-awaiting-claims-review`, so publish always resumes — and the resume
  command must REPEAT `--bump-rationale` or the published record honestly records an
  absence instead of the rationale. This is the operator's step to hold, named in the
  publish plan below, and it is the handoff's open item about the non-claims lane.
- The debug-validator acceptance-change worry and the record-shape-breakage worry are
  both over-worry: the acceptance-equivalence walk is recorded in the #636 critique,
  and the parser-safety arguments are written at their surfaces.
- The `blocked` vocabulary fix makes the critique-scope gate stricter, not looser —
  patch-class content that rides the minor.

## Structured Findings

- F1 | bin: act-before-ship | evidence: strong | ref: skills/public/release/scripts/publish_release_resume_publish.py | action: document | note: the resume invocation for this publish must repeat `--bump-rationale`; only the planner's repeat_original_arguments names it and nothing refuses the omission. Carried into the publish runbook for this cut.
- F2 | bin: over-worry | evidence: strong | ref: charness-artifacts/critique/2026-08-18-issue-636-resolution.md | action: defer | note: debug-validator acceptance equivalence already reviewer-walked and recorded; no major-bump trigger.
- F3 | bin: over-worry | evidence: moderate | ref: scripts/audit_public_release_narrative.py | action: defer | note: last-position emission keeps the new section out of every existing parser span; arguments recorded at the emitting surface.

## Reviewer Tier Evidence

- Requested tier: high-leverage (release critique).
- Requested spawn fields: subagent_type=bounded-reviewer, unnamed one-shot spawn, no
  model/effort override (session-inherited).
- Host exposure state: applied
- Application state: host-confirmed: typed `bounded-reviewer` spawn accepted and ran
  with the read-only toolset (Read/Grep/Glob only, per its own envelope report).
- Delivery state: findings-received

## Reviewed Input Identity

<!-- No packet was consumed: this critique reviews the release decision over the
pushed range da6913245..242de7c6c. -->

## Boundary Ownership

- Producer: the release helper chain (`publish_release_args/execute/artifact`) mints
  the version, record, and stop marker.
- Consumer: record parsers (`audit_public_release_narrative`,
  `validate_current_pointer_freshness`) and operators reading the published record.
- Owning surface: release publish lane (skills/public/release).
- Verdict: owned-correctly

## Fresh-Eye Satisfaction

`parent-delegated`. One bounded reviewer, spawned unnamed as `bounded-reviewer`, over
the four named angles; verdict RELEASE-SAFE at MINOR. Boundary fingerprint verify
around the round: `ok: true`, `verdict: clean`, `drift: []`
(window w-20260817T235247Z-1315235). The parent afterwards ran the one git probe the
reviewer's toolset could not (`git show da6913245:.../publish_release_args.py` → no
`--bump-rationale`), which converts the reviewer's documentary new-in-range evidence
to git proof.

## Non-Claims

- No claim that the claims-review round has run; it runs against the prepared record
  after the version claim exists, per the critique-boundary reference.
- No claim about install-refresh or public-surface verification; those are
  post-publish evidence recorded by the helper.
