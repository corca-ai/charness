# Release Critique: v0.42.0
Created: 2026-06-12
Target: `release-critique`
Fresh-Eye Satisfaction: parent-delegated
Packet Consumed: `charness-artifacts/critique/2026-06-12-112934-packet.md`

## Release Scope

- version: `0.42.0`
- tag: `v0.42.0`
- consumer change: minor release; template-first artifact-gate hardening
  (quality scaffold fill-time guards, shared `run_validation_checks` with
  `--report-all` on the quality/critique/debug validators wired into the
  charness-local standing gates, Template-First Artifact Gates doctrine in
  the public `quality` skill) plus the nose 0.7.0 quality pass and real-host
  nose proof closure. Three content commits on v0.41.1
  (79e09a1a, 5773718f, a930cc5f), each with its own committed slice
  critique.

## Surface-Lock Inventory

- `.agents/release-adapter.yaml` update instructions for `0.42.0`.
- Packaging and generated plugin manifests, bumped and synced by
  `publish_release.py`.
- `charness-artifacts/release/latest.md`, written by `publish_release.py`.
- Fresh-checkout probes declared in `.agents/release-adapter.yaml` (passed
  this session pre-critique).
- Installed-plugin operator update path: `charness update`.

## Angles

- Atul Gawande / operational checklist: bump-level justification, entry
  accuracy vs actual diffs, clean-tree precondition, version-surface
  uniformity at 0.41.1 pre-helper, no `--close-issue` needed (no commit
  claims a GitHub issue close), packet verifiers run green
  (`validate_adapters.py`: 17 resolvers / 18 YAML; cautilus proof:
  deterministic path).
- Barbara Minto / cold reader: the 0.42.0 entry implies no new command
  surface or migration; rollback line present; "no new blocking behavior"
  verified — `--report-all` changes error aggregation, never verdicts, and
  the gate wiring is charness-repo-local.
- Counterweight: separated the one real bundle item (stale handoff
  narration) from expected pre-helper state and over-worry.

## Reviewer Tier Evidence

- Requested tier: high-leverage
- Requested spawn fields: model=gpt-5.5, reasoning_effort=medium, service_tier=priority
- Host exposure state: host-defaulted
- Application state: host returned agent ID `a49cd97694d4284c1`; parent used
  host-default model selection and no provider-side application confirmation
  was exposed.

## Findings

### Act Before Ship

- Commit the 0.42.0 adapter entry, this critique artifact, and the packet
  files before `publish_release.py --execute` (clean-tree precondition).
  Resolution: committed as the release-critique carrier commit.

### Bundle Anyway

- F1 | bin: bundle-anyway | evidence: strong | ref: docs/handoff.md | action: fix | note: handoff Current State still narrates v0.41.0 as shipped (two releases stale); refresh to the verified v0.42.0 state immediately after publish so the pickup surface stops misleading.

### Over-Worry

- F2 | bin: over-worry | evidence: strong | ref: 3a8ba3a1 | action: document | note: the fourth commit on v0.41.1 is that release's own verification record; the three-content-commit release story stands.
- F3 | bin: over-worry | evidence: moderate | ref: plugins/charness/scripts/run-quality.sh | action: document | note: the mirror ships --report-all wiring but operates on its own repo root; aggregation-only, verdicts unchanged, no consumer blocking-behavior leak.

### Valid But Defer

- F4 | bin: valid-but-defer | evidence: weak | ref: skills/public/quality/scripts/scaffold_quality_artifact.py | action: defer | note: MAX_ARTIFACT_LINES mirrored rather than imported; drift is test-guarded and D28-adjacent.

## Verdict

SHIP-with-named-fixes: F1 only (handoff Current State refresh bundled
immediately after publish). Entry accurate against diffs, both packet
verifiers green, minor bump justified, no issue-close flags needed.
