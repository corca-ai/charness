# Critique Review
Date: 2026-07-20

## Decision Under Review

Release v2.3.0 → v2.3.1 (patch). Scope: two commits since v2.3.0, all bug
fixes / behavior repairs / test hardening for the flaky-suite OPEN SMELL:

- `1834090b` — retention eviction ordered by embedded `time.time_ns()` stamp
  instead of coarse-granularity `st_mtime_ns` (fixes an arbitrary-record-dropped
  flake); `run_standing_pytest` basetemp renamed `pytest-<ns>` → `charness-run-<ns>`
  to dodge pytest's numbered-dir cleanup (fixes a mid-run temp-tree deletion race
  causing mass `FileNotFoundError` under `--release`); economics session regex
  broadened to still count the renamed leaf.
- `bd055dc2` — deterministic `(st_mtime, name)` tiebreaks on four recency selectors;
  `missing_ok=True` on the retention unlink for concurrent-eviction safety.

One bounded high-leverage fresh-eye release reviewer ran the publish-safety /
backward-compat / bump-correctness / mirror-sync lens; rail-1 reviewer-boundary
fingerprint verified clean around the pass.

## Failure Angles

- Bump correctness: patch vs minor vs major.
- Backward compatibility: does any exported-runtime behavior change break a consumer?
- Generated-surface sync: are all 7 changed runtime files byte-identical to mirrors?
- Publish safety: half-wired change, missing test, or premature version bump.

## Counterweight Pass

- Publish verdict: **safe-to-publish**. All 7 changed runtime files are byte-identical
  canonical vs `plugins/charness/**` mirror; every fix carries a deterministic
  regression test; no version surface is prematurely bumped (all read 2.3.0; the
  helper owns the bump).
- Bump verdict: **patch (2.3.1)**. All changes are internal behavior repairs — no new
  maintained capability, no renamed/removed surface, no changed invocation or exported
  signature. Minor/major not warranted.
- Backward-compat: the `charness-run-*` basetemp leaf is an ephemeral temp dir with no
  exported name contract; its sole in-repo name-keyed consumer (the economics regex)
  ships fixed in the same release. The broadened economics regex corrects prior
  under-counting on an advisory inventory with unchanged schema — a fix, not a break.
- Over-worry (no action): the four recency-selector tiebreaks have no dedicated unit
  tests, but they are single-token additions that only make previously-arbitrary ties
  deterministic and cannot regress the non-tie path — nice-to-have, not publish-gating.

## Publish Verdict

Safe to publish as patch 2.3.1. The public verification (fresh-checkout probe,
distinct-channel confirmation, install refresh) is owned by the publish helper and
judged at the publication boundary, not by this pre-publish critique.

## Boundary Ownership

- Verdict: owned-correctly

The release advances only internal quality/release/test-infra behavior repairs; the
portable body owns the fixed mechanisms and the consuming repo owns its declared
surfaces. The publication-boundary proofs stay with the publish helper and a distinct
observer channel — ownership is not inverted into the reviewer.

## Reviewer Tier Evidence

<!-- allowed Host exposure state enums only -->
- Requested tier: high-leverage
- Requested spawn fields: none — Claude Code host, typed `bounded-reviewer` (Read/Grep/Glob) with session-model inheritance per the repo per-host subagent contract; no Codex model requested on this host, so the omission is contract-conformant, not a degradation.
- Host exposure state: host-defaulted
- Application state: the host spawned the typed `bounded-reviewer` agent by name for the release review; the read-only envelope bound and the rail-1 reviewer-boundary fingerprint verified clean (no index/worktree drift) after it returned, so approvals are valid and the reviewer ran on the parent's session-inherited model.

## Fresh-Eye Satisfaction

parent-delegated — one high-leverage release reviewer (publish-safety / backward-compat
/ bump / mirror-sync lens) with an in-lens counterweight; rail-1 reviewer-boundary
fingerprint verified clean.
