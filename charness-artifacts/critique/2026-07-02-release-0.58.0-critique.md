# Release Critique — charness 0.58.0

Execution: fresh bounded subagents (3 angles + 1 counterweight), parent-delegated.
Fresh-Eye Satisfaction: parent-delegated.
Packet Consumed: charness-artifacts/critique/2026-07-02-025020-packet.md
Target: references/release-critique.md

## Change

Cut **0.58.0** (minor from 0.57.0), push 39 commits to origin/main, tag + GitHub
release. Content = the reference-compaction claim-fidelity floor overhaul (Slices
1–7 + the #409 eval-runtime fix + this session's setup #413 + handoff #412
per-condition floor redesigns).

## Capability at Stake

Ship an honest, legible release of the claim-fidelity floor model without a stale
mirror, a dangling reference, a mislabeled bump, or a falsely-claimed issue close.

## Surface-Lock Inventory

- Generated/mirror (consumers install): `plugins/charness/skills/{impl,setup,spec,create-skill,handoff}/SKILL.md` + references — `## Closeout Vocabulary` ADDITIONS + DUP reference DELETIONS (24 files); `plan_handoff_run.py`; `scripts/agent-runtime/build-skill-execution-observation.mjs`. Version surfaces bumped at release (`packaging/charness.json`, both marketplaces, plugin manifests).
- Consumer-visible behavior: handoff pickup planner CONDITIONALLY reads `continuation-sequence.md`; setup emits a normalization `## Closeout Vocabulary`. Skill INVOCATION contracts unchanged.
- Docs: `docs/handoff.md` refreshed; README unchanged; no release note yet.
- Internal (not consumer-installed): `evals/cautilus/**`, `claim-fidelity-registry.json`.

## Angles

- Gawande (operational checklist), Minto (structure/communication), Raskin
  (humane interface). Verified live: sync clean (drift=[]), fresh-checkout probes
  pass (doctor 15/15), `check_doc_links` OK (no dangling refs from the 24
  deletions), mirror byte-identical to source.

## Counterweight Triage (four bins)

- **Act Before Ship**
  - Commit the untracked critique packet (`charness-artifacts/critique/2026-07-02-025020-packet.{json,md}`) — repo phase rule; trivial.
- **Bundle Anyway (into the pre-release commit)**
  - Changed-line mutation coverage is a silent-skip (`coverage_not_verified:true`) for 4 changed pool files (`claim_fidelity_lib.py`, `plan_handoff_run.py`, `check_skill_surface_preflight.py`, `run_skill_efficiency_ab.py`). NOT the #404 cause (#404 is a JS/StrykerJS infra failure — different class), and the gate is non-blocking by design, but refreshing coverage is cheap and pre-empts a #335-class next-cron flag. Run the producer + commit the refreshed fingerprint.
  - Write a synthesized GitHub release note (commit subjects are operator-illegible: RCF/RSF/"census"). Inherent to the publish step, not a separable gate.
- **Valid but Defer**
  - Verify #412/#413 auto-closed AFTER push (closing keywords in the commit subjects fire on merge to the default branch; push IS that merge — verify, don't hold).
  - #404 (full/JS mutation regression) is a pre-existing OPEN precondition the release gate does not block on; record in the ledger.
- **Over-Worry**
  - "Every release has a hand-written note" — false (0.53.0/0.54.0/0.57.0 are bare auto-generated). Auto notes are an accepted fallback.
  - "Stale coverage caused #404" — false, different class.
  - Operator surface (closeout vocab, deletions, CLI, README) — improved/unchanged, self-consistent; no action.

## Deliberately Not Doing

- Not holding the tag on the mutation-coverage skip (non-blocking by design; refreshed as hygiene, not a gate).
- Not re-litigating minor→patch: the additive `## Closeout Vocabulary` surfaces + floor model make minor honest; the note states "no invocation contract change, no migration."
- Not fixing #404 in this release (pre-existing, out of scope for this minor).

## Reviewer Tier Evidence

<!-- allowed Host exposure state: pending-parent-spawn | requested_fields_sent | metadata-hidden | host-defaulted | unsupported | applied -->
- Requested tier: high-leverage
- Requested spawn fields: model=default, reasoning_effort=default (Claude Code host does not expose reviewer-tier spawn fields to this agent)
- Host exposure state: host-defaulted
- Application state: requested fields not host-confirmed; Claude Code spawned its default bounded reviewers (3 angle subagents + 1 counterweight, general-purpose)

## Next Move

1. Run the mutation-coverage producer; commit it + the critique packet + this artifact (pre-release hygiene).
2. Publish via the helper: bump minor → sync → verify → tag → push → GitHub release with a synthesized note.
3. After push: verify #412/#413 CLOSED (different-channel), then `charness update` install refresh + doctor.
