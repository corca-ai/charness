# Release Critique — charness 0.52.3 (patch)

- Date: 2026-06-18
- Target reference: `release-critique`
- Release: 0.52.2 → 0.52.3 (patch)
- Contents: issue #385 fix (achieve pre-block remaining-boundary-matrix floor,
  commit 672ff64b) + RCA ledger event (commit 6c5f1aed)

## Execution

- Fresh-Eye Satisfaction: parent-delegated
- Reviewers: 2 angle subagents (release surface-lock + publish boundary;
  version-bump correctness + operator legibility) + 1 separate counterweight,
  each a bounded fresh-eye subagent. The achieve code was deliberately
  out-of-scope (covered by the separate resolution critique
  `2026-06-18-issue-385-achieve-blocked-boundary-matrix.md`); this pass reviewed
  the release surface only.

## Reviewer Tier Evidence

- **Requested tier**: `high-leverage`
- **Requested spawn fields**: `model=gpt-5.5, reasoning_effort=medium, service_tier=priority` (adapter `.agents/critique-adapter.yaml` Codex-host mapping; this Claude Code run spawned each bounded reviewer with `model=opus` via the Agent tool; reasoning_effort/service_tier are not exposed through the Agent spawn interface)
- **Host exposure state**: `requested_fields_sent`
- **Application state**: `unverified-by-host` (the host does not confirm applied spawn fields back to the parent; sent fields are not proof of application)
- **Instruction**: Review artifacts record requested_fields_sent, metadata-hidden, host-defaulted, unsupported, or applied only when host-confirmed.

## Surface-Lock Inventory

- `packaging/charness.json`, `plugins/charness/.claude-plugin/plugin.json`,
  `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`: all at
  `0.52.2`, `current_release.py` reports `drift: []`. The publish helper bumps +
  syncs all four atomically (`ensure_release_surface` raises `SystemExit` on any
  post-bump drift), so no surface can be left at 0.52.2 or publish aborts.
- Publish boundary: the helper sets `public_release_verification` to `verified`
  only after a real `gh release view <tag>` poll with backoff; tag-pushed vs
  release-visible are distinct states, so an unverified release is not labeled
  "published".
- Adapter `update_instructions` 0.52.3 entry added at
  `.agents/release-adapter.yaml:34`, staleness guard cleared
  (`update_instructions_stale: False`).

## Bump Verdict

**PATCH (0.52.3) is correct** — unanimous across both angles + counterweight.
Issue #385 is fundamentally a bug fix (a goal wrongly flips `blocked` while a
runnable lane remains). The new `## Remaining Boundary Matrix` section is
*conditional* (authored only when blocking) and *presence-only*; there is no new
always-required section, no migration, and the floor is grandfathered to
`Created >= 2026-06-18`. This matches the #383/#382 patch precedent and is
narrower than #381 (Operator Decision Queue), which was an always-rendered new
section adopted by every fresh goal and shipped as 0.52.0 minor. The public
`achieve` invocation shape (routing, draft creation, activation,
inert-until-activation) is unchanged.

## Counterweight Triage

### Act Before Ship → handled by the publish helper (normal flow)

- Version surfaces must move 0.52.2 → 0.52.3 in lockstep — this IS the publish
  helper's bump+sync step; a partial bump aborts.
- Publish state recorded honestly (tagged vs published) — the helper's
  `gh release view` verification owns this.

### Bundle Anyway → done

- The RCA ledger event and the resolution critique artifact are already landed
  on `main` (commits 672ff64b / 6c5f1aed); the release ships them as-is.

### Over-Worry → rejected (not acted on)

- "Should be MINOR not patch." Rejected: invocation contract preserved; the
  matrix is inert unless blocking; bug→gate conversion, not adoptable feature.
- "New blocking floor breaks installed consumers' in-flight blocked goals."
  Rejected: Created-date grandfathering + fail-closed-on-missing + new-module
  isolation scope the blast radius; pre-rule goals return `applies: False`.
- "Blocked-state taxonomy mirror now." Rejected: adjacent refactor, no evidence;
  already deferred by the resolution critique.

### Valid but Defer

- Formal blocked-state taxonomy mirroring the closeout-state taxonomy — deferred
  (consistent with the resolution critique's deferral).

## Verification To Run

- Fresh-checkout probes are declared in the adapter; run them before tag publish
  and report status (counterweight rated them over-worry for a patch, but the
  adapter declares them, so report honestly).
- Canonical quality gate + generated-surface agreement via the publish helper.

## Deliberately Not Doing

- Not re-reviewing the achieve code (separate resolution critique owns it).
- Not bumping minor (verdict: patch is honest).
- Not building the blocked-state taxonomy in this release.

## Next Move

Run the publish helper `--part patch --execute` with this critique artifact;
report tag/version/URL and the public-verification + fresh-checkout-probe state
honestly in the closeout. Do not claim "published" until `gh release view`
verifies.
