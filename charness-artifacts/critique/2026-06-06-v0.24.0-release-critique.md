# v0.24.0 Release Critique (bounded fresh-eye, RELEASE decision)

Date: 2026-06-06
Reviewer scope: RELEASE decision only (the two `feat` commits were already code-reviewed CLEAR; this is not a line-by-line re-review).
Bundle: `git log --oneline origin/main..HEAD` = `3be63424` handoff refresh, `c427e327` + `479345a4` goal closeout/cleanup, `9bc40f2a` feat(achieve) #318, `b92dd9f9` feat(quality) #319, `fad569a6` goal draft, `404abfe8` critique.
Plan: `publish_release.py --part minor --execute --close-issue 318 --close-issue 319` → 0.24.0, sync, `./scripts/run-quality.sh --release`, commit, push, tag `v0.24.0`, GitHub release, verify #318/#319 CLOSED.

Verdict: **CLEAR to ship at minor (0.24.0).** No Act-Before-Ship blockers found. One pre-existing operator nose real-host proof carries forward as a tracked, non-blocking open risk.

---

## Four-bin triage

### Act Before Ship
(none)

No removed/renamed public surface, no missing mirror sync, no stale update_instructions lead, no wrong-issue-close risk, no new required dependency. Nothing must change before invoking `publish_release.py`.

### Bundle Anyway (ship as-is; minor/cosmetic, not worth a re-cut)
- **`.agents/release-adapter.yaml` is an uncommitted working-tree edit** (`git status` shows ` M .agents/release-adapter.yaml`). This is expected — `publish_release.py` stages and commits adapter/version surfaces as part of the release commit. Ship as-is; do not pre-commit it separately.
- **`--path` argparse on `check_skill_surface_preflight.py` changed `required=True` → optional**, with a new sibling `--changed-skill-md` (nargs="*"). This is purely additive at the operator CLI level (the flag still works; it just is no longer mandatory and a new mode was added). Not a breaking change. Mirror is byte-identical.

### Over-Worry (raised by the brief / plausible, but verified non-issues)
- *"Could this be breaking → forcing major?"* No. `git diff origin/main..HEAD --diff-filter=DR` returns **empty** (zero deletions/renames). No SKILL.md frontmatter `name:`/`description:` rename (no SKILL.md files changed at all). The goal-artifact contract change is `## Closeout Delegation (optional, orchestrated mode)` — explicitly additive; the commit body confirms "absent section or `Closeout mode: standalone` is untouched (the strict standalone default stays the hard default)."
- *"New required binary/dependency on update?"* No. Diff `subprocess` hits are all `git` (already required) and test-fixture `git init`. The only flagged validation dependency (`tokei`) is **carried-forward from 0.14.0** per `.agents/release-adapter.yaml:61`, not new this release.
- *"Mirror drift — source edited without its plugin mirror?"* No. Every edited source surface is byte-identical to its `plugins/charness/...` mirror (corrected mapping `skills/public/X` → `plugins/charness/skills/X`): `check_skill_surface_preflight.py`, `staged_commit_gate_plan.py`, `achieve/scripts/goal_artifact_closeout_delegation.py`, `goal_artifact_closeout_evidence.py`, `check_goal_artifact.py`, `achieve/references/{lifecycle,goal-artifact}.md`, `quality/references/attention-state-visibility.json` — all SYNCED.
- *"Could the bump leave version surfaces inconsistent?"* No. `bump_version.py` writes a single source-of-truth `packaging/charness.json` (`version` + `claude.manifest.version` + `codex.manifest.version`), then runs `sync_command` = `python3 scripts/sync_root_plugin_manifests.py --repo-root .`, which regenerates `.claude-plugin/plugin.json`, `.codex-plugin/plugin.json`, and the root marketplace from that manifest. All 4 version surfaces fold to one source. Current state is a clean `0.23.0` across all three manifest fields (no patch suffix), so minor → `0.24.0` is the correct, unambiguous arithmetic.

### Valid but Defer (track, do not block this cut)
- **v0.23.0 operator nose real-host proof** (second-machine `charness update` + `tool doctor/install nose`) is still pending per `docs/handoff.md:49-50`. This release does **not** introduce a new real-host requirement: `check_real_host_proof.py --repo-root .` reports `"required": false` with reason "No configured release-time real-host proof trigger matched the current slice." Carry the v0.23.0 nose checklist forward as an open risk, not a v0.24.0 blocker.

---

## Bump-level verdict

**MINOR (0.24.0) is correct.**
- Two additive `feat` commits: `#319` new commit-boundary SKILL.md core-headroom ratchet (grandfathers existing under-buffer skills, blocks new erosion); `#318` new opt-in orchestrated achieve closeout-delegation mode.
- No removed/renamed public surface (`--diff-filter=DR` empty), no removed CLI flag (the one argparse change relaxes a requirement + adds a sibling), no renamed skill, no deleted operator-depended file.
- Not a mere patch: both ship new operator-visible capability (a new blocking gate path; a new goal-artifact mode), which is exactly the additive-feature signature of a minor.

## Compatibility / install-update surface

Safe for existing operators on update:
- **#319 ratchet is opt-in-by-change**: only fires on a *changed* public/support SKILL.md and grandfathers skills already under buffer — no existing skill breaks on update.
- **#318 mode is opt-in by a new `## Closeout Delegation` section**: absent section = strict standalone default (unchanged). No existing goal artifact breaks on update; commit `9bc40f2a` body confirms the standalone default stays hard.
- **No new binary/dependency**; `tokei` is the only validation dep and is carried-forward, already documented in `update_instructions`.

## update_instructions honesty (`.agents/release-adapter.yaml`)

Accurate and non-overclaiming, and it satisfies the staleness guard:
- Lead line names the **TARGET 0.24.0** ("pull 0.24.0"). The guard in `publish_release_preflight.py:88` returns no blocker when `target_version in text` — confirmed present. (It also still mentions 0.23.0 in a clearly-labeled "Carried-forward (0.23.0)" line, which is fine — the guard only blocks if target is *absent*.)
- #318 description faithfully states OPT-IN, NAMED orchestrator, absent-section-unchanged, and the resolve-every-item checklist requirement — matches the shipped contract and commit body.
- #319 description faithfully states the >=4 headroom buffer, CHANGED-file commit-boundary enforcement, grandfathering ratchet, and that the broad-gate test is unchanged — matches the shipped gate.

## Issue-close linkage

- `gh issue view 318 --json state` → `"state":"OPEN"` ("Support orchestrator-owned external proof for achieve sub-goal closeout").
- `gh issue view 319 --json state` → `"state":"OPEN"` ("SKILL.md core-nonempty headroom-buffer test runs only in the broad gate, not the commit boundary").
- Both feature commit bodies carry the correct linkage: `b92dd9f9` → `Close #319`, `9bc40f2a` → `Close #318`. Numbers match the `--close-issue 318/319` plan. No wrong-issue-close risk.

## Standing proof gaps

- `check_real_host_proof.py --repo-root .` → `required: false`; this release adds **no** new real-host requirement.
- v0.23.0 nose operator real-host checklist remains pending (handoff.md:49-50): tracked open risk, not a blocker.

---

## Open Risks

1. **(Carried-forward, non-blocking)** v0.23.0 operator nose real-host proof still pending — second-machine `charness update` + `tool doctor/install nose`. Not re-triggered by this slice; keep on the handoff after the cut.
2. **(Operational, expected)** `publish_release.py` must perform the adapter+version commit itself (the `.agents/release-adapter.yaml` edit is intentionally uncommitted now). If a maintainer pre-commits it manually, re-confirm the release commit still contains the version-surface bump before push/tag.
3. **(Verify-at-runtime, standard)** Post-publish, the plan's own `--close-issue` verification must show #318/#319 flip to CLOSED and the 4 version surfaces all read `0.24.0`; if `run-quality.sh --release` or the GitHub release/tag step fails mid-flow, halt and reconcile before re-running (do not double-tag).
