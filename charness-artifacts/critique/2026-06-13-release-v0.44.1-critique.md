# Release Critique: v0.44.1
Created: 2026-06-13
Target: `release-critique`
Fresh-Eye Satisfaction: parent-delegated
Packet Consumed: `charness-artifacts/critique/2026-06-13-000213-packet.md`

## Decision Under Review

Cut and publish `charness` patch release `0.44.1` after the issue #359
goal-closeout placeholder hardening carrier (`ebff7d50`) and the release
adapter update-instructions refresh for `0.44.1`.

Success means the patch tag, package manifests, plugin exports, release
record, GitHub release, and maintainer install refresh all describe the same
version and the operator-facing update note does not misstate the new
diagnostics. Out of scope: new CLI help text for the draft/active/complete
distinction and any new requested-review adapter command.

## Release Scope

- version: `0.44.1`
- tag: `v0.44.1`
- consumer change: patch release; complete-state goal artifacts now refuse
  untouched template placeholder content instead of allowing a goal to close
  with scaffold text still present. `## Final Verification` evidence
  placeholders are reported as missing evidence, and H2 section placeholders
  such as `## Auto-Retro` are reported under `section_placeholders`.
- operator action: run `charness update`; no migration or renamed command
  surface. Roll back by reinstalling or pinning `0.44.0`.

## Surface-Lock Inventory

- Committed carrier `ebff7d50`:
  `skills/public/achieve/scripts/goal_artifact_section_placeholders.py`,
  `skills/public/achieve/scripts/goal_artifact_closeout_evidence.py`,
  `skills/public/achieve/scripts/goal_artifact_lib.py`,
  `skills/public/achieve/scripts/check_goal_artifact.py`,
  `skills/public/achieve/references/goal-artifact.md`, matching
  `plugins/charness/skills/achieve/**` mirror files, tests under
  `tests/quality_gates/`, and `docs/public-skill-dogfood.json`.
- Release-prep working tree: `.agents/release-adapter.yaml` top
  `update_instructions` entry for `0.44.1`.
- Generated/package surfaces to be locked by `publish_release.py`:
  `packaging/charness.json`, `.claude-plugin/marketplace.json`,
  checked-in plugin manifests under `plugins/charness/`, Codex marketplace
  source path, `charness-artifacts/release/latest.md`, tag `v0.44.1`, and the
  GitHub release record.
- Declared release probes: `./charness --help >/dev/null`,
  `./charness goal check --help >/dev/null`, and
  `python3 scripts/doctor.py --repo-root . --json --skip-release-probe
  >/dev/null`.
- Post-publish install refresh: adapter-declared `charness update`.

## Failure Angles

- Operational checklist: the prepare packet only saw the current dirty file,
  so relying on it alone would under-scope the release. The final inventory
  above includes the committed #359 carrier and the versioned/generated
  surfaces the release helper will lock.
- Operational checklist: the prepare packet must not be passed as
  `--critique-artifact`; it is input evidence, while this tracked markdown file
  is the release critique artifact.
- Operational checklist: fresh-checkout probes are configured but were not run
  when the angle review started. They must pass before publish/tag.
- Structure/communication: the first `0.44.1` note over-named
  `section_placeholders` for every placeholder failure. The adapter entry was
  rewritten before publish to distinguish missing Final Verification evidence
  from H2 `section_placeholders`.
- Humane interface: saying the `charness goal check` surface is unchanged could
  read as behavior unchanged. The note now says command/options are unchanged
  while complete-state checks may newly fail on untouched placeholders.

## Counterweight Pass

- Act before ship: include the broader carrier and generated-surface inventory
  in the final critique artifact. Folded in this artifact.
- Act before ship: use this tracked release critique artifact, not the prepare
  packet, as `publish_release.py --critique-artifact`. Folded by using this
  path.
- Act before ship: run the declared fresh-checkout probes before publish/tag.
  This remains the next gate.
- Bundle anyway: the release-note wording fix is real and cheap. Folded into
  `.agents/release-adapter.yaml`.
- Bundle anyway: no special pre-publish real-host checklist is required for
  this patch; `check_real_host_proof.py` reported `required: false`, while the
  normal post-publish `charness update` refresh remains declared.
- Over-worry: `requested_review_commands` is empty and advisory-only for this
  release; parent-delegated angle reviewers plus the separate counterweight
  reviewer satisfy the release critique path.
- Valid but defer: `charness goal check --help` remains terse about
  draft/active/complete behavior, but that is pre-existing help text and not a
  blocker for a patch release that keeps command/options unchanged.

## Operator Action Required

- Before publish/tag, run the declared fresh-checkout probes and record the
  passing result.
- Publish with
  `--critique-artifact charness-artifacts/critique/2026-06-13-release-v0.44.1-critique.md`.

## Upgrade Path

Run `charness update` to pull `0.44.1`. No migration or renamed command
surface is required. Roll back by reinstalling or pinning `0.44.0`; no data
migration needs undoing.

## Structured Findings

- F1 | bin: act-before-ship | evidence: strong | ref: charness-artifacts/critique/2026-06-13-000213-packet.md | action: fix | note: final release critique must include the committed #359 carrier and generated/package surfaces, not only the current dirty adapter file
- F2 | bin: act-before-ship | evidence: strong | ref: skills/public/release/scripts/publish_release.py --help | action: fix | note: use a tracked release critique artifact as publish input, not the prepare packet
- F3 | bin: act-before-ship | evidence: strong | ref: .agents/release-adapter.yaml | action: fix | note: declared fresh-checkout probes must pass before publish/tag
- F4 | bin: bundle-anyway | evidence: strong | ref: .agents/release-adapter.yaml:34 | action: fix | note: update instruction wording now distinguishes missing evidence placeholders from H2 section_placeholders
- F5 | bin: over-worry | evidence: moderate | ref: .agents/release-adapter.yaml | action: document | note: requested_review_commands is advisory-only for this release because parent-delegated critique ran
- F6 | bin: valid-but-defer | evidence: moderate | ref: ./charness goal check --help | action: defer | note: terse help for draft_active_complete behavior predates this patch and does not block v0.44.1

## Reviewer Tier Evidence

- Requested tier: high-leverage
- Requested spawn fields: model=gpt-5.5, reasoning_effort=medium, service_tier=priority
- Host exposure state: requested_fields_sent
- Application state: counterweight spawn received the requested fields; angle reviewer spawns ran parent-delegated with host default fields.

## Fresh-Eye Satisfaction

parent-delegated. Angle reviewers ran for operational checklist, structure and
communication, and humane interface; a separate counterweight reviewer triaged
the concerns and left only the final critique artifact plus fresh-checkout
probe as publish blockers. This artifact folds the critique-artifact blocker;
the fresh-checkout probe remains the next gate before publish.

## Next Move

Validate this artifact, run sync for generated release/plugin surfaces as
needed, run the declared fresh-checkout probes, and then publish with the
release helper using this critique artifact.
