# Release v0.40.0 critique (overnight quality goal terminal lane)
Date: 2026-06-11

## Decision Under Review

Cut release `v0.40.0` (minor bump from `0.39.0`) at origin/main HEAD
`a7185616` via the repo publish helper (bump + sync + tag + push + GitHub
release + adapter-declared `charness update` install refresh). Delta
768ded84..a7185616: hitl reciprocal boundary (#349 CLOSED), quality posture
refresh, create-skill at-cap guards + near-cap preflight warning (#350
CLOSED), bootstrap adapter unknown-field round-trip fix, commit-time
handoff validator pull, scheduled-mutation capacity-advisory
reclassification. Bump rationale: additive operator-visible behavior plus
a bug fix; no renames, no invocation breaks, no install-surface changes.

## Failure Angles

- Minor could underestimate the scheduled-lane blocking-semantics change
  (consumer repos share these scripts/references).
- Generated version surfaces could be out of sync pre-bump.
- The delta could ship a silently misread gate relaxation to installed
  users; generated notes could mislead.
- A still-running scheduled mutation run on the old head could argue for
  waiting.

## Counterweight Pass

- Bounded fresh-eye release reviewer (read-only): **SHIP**. Verified
  itself: `current_release.py` all surfaces 0.39.0, drift `[]`, clean
  tree, HEAD == origin/main; notes range v0.39.0..HEAD adds only the
  v0.39.0 verification-record commit beyond the reviewed delta. Read the
  da6b9a8e diff directly: manifest keys unchanged, blocking arm computed
  over ALL eligible changed files before selection — the removed blocker
  was a false-red generator (run 27279937136 red at score PASS 88.2%
  purely on a 10-vs-5 budget; 47 auto-issue lifecycles), so fix-class,
  not MAJOR. Zero deletes/renames over the range. Scheduled run
  27308933212 COMPLETED success on old head 768ded84 — waiting is moot.
  Post-push quality-core 27312178167 green on a7185616.
- Nits (advisory, not folded into the gate): (a) a post-release
  `announcement` explaining "capacity drops now advisory; uncovered
  changed lines still block" would help consumer operators tracking
  scheduled-run red rates — optional per the narrative-announcement
  boundary; (b) `requested_review_commands` remains advisory-empty
  (pre-existing).

## Structured Findings

<!-- allowed enums (substitute only these) — bin: act-before-ship | bundle-anyway | over-worry | valid-but-defer; evidence: strong | moderate | weak | contested; action: fix | file-issue | document | defer. action: file-issue also needs a follow-up: (issue URL or 'deferred ' plus a handoff anchor). -->
- F1 | bin: act-before-ship | evidence: strong | ref: scripts/publish_release.py | action: fix | note: publish executed only after this critique artifact existed (helper enforces --critique-artifact); bump level minor justified in the artifact, not silently defaulted.
- F2 | bin: valid-but-defer | evidence: moderate | ref: skills/public/announcement/SKILL.md | action: defer | note: optional post-release announcement of the scheduled-lane semantics change; deferred to the goal Auto-Retro disposition per the narrative-announcement boundary.
- F3 | bin: over-worry | evidence: strong | ref: scripts/mutation_sample_manifest_score_lib.py | action: document | note: MAJOR-bump worry rejected — reviewer-read diff shows manifest keys unchanged and the blocking uncovered arm intact over the full changed set; removal of a false-red arm is fix-class.

## Reviewer Tier Evidence

<!-- allowed Host exposure state: pending-parent-spawn | requested_fields_sent | metadata-hidden | host-defaulted | unsupported | applied. Use applied only with Application state: host-confirmed: plus a concrete signal. -->
- Requested tier: one bounded fresh-eye subagent reviewer (repo-contract
  pre-approved scope), read-only.
- Requested spawn fields: subagent_type=general-purpose; prompt carried
  the release decision, delta, bump rationale, prior verification state,
  and five release-specific failure angles.
- Host exposure state: applied
- Application state: host-confirmed: Agent tool returned a structured
  SHIP verdict (7 findings, 17 tool uses, 140s) with independently
  re-run release-state probes (current_release, host-proof, review-gate,
  run conclusions).

## Fresh-Eye Satisfaction

Release v0.40.0: the bounded review returned no blockers (verdict SHIP;
two advisory nits recorded as F2 and the pre-existing review-gate
warning). Binding artifact for the v0.40.0 publish: this file.
