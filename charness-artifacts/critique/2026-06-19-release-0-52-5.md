# Release critique — charness 0.52.5 (patch)
Date: 2026-06-19

## Decision Under Review

Publish charness **0.52.5 (patch)** — push 8 local commits (north-star overhaul
Track 1a S2/S3 + Track 2 S4/S5, the #390 code-layer quality pass, a retro, the
update_instructions refresh) and cut a GitHub release.

## Failure Angles

- Bump level too low: an additive operator surface or compat break hiding in the
  bundle would make patch dishonest.
- Public-visibility dishonesty: the operator note over/understating what
  installed-plugin consumers actually receive.
- Surface skew: packaging / plugin / marketplace versions disagreeing after bump.
- A shipped liability: the committed 350KB baseline + its re-baseline trap.
- Stale steering: handoff / quality artifacts not reflecting the shipped bundle.

## Counterweight Pass

- Patch is the real-blocker-free call: every verdict change is framing/prose with
  no new gate/script/token (verified in commits + diffs + goal closeout), and the
  #390 flags are on a repo-local quality tool, not the `charness` CLI. The
  #386 precedent (same class) shipped as patch 0.52.4. Not over-worry to keep
  patch; would be over-worry to inflate to minor.
- Surface consistency is a non-issue: one packaging manifest feeds the sync helper
  that regenerates both plugin manifests + marketplace (`current_release` shows
  drift: []).

## Structured Findings

- F1 | bin: act-before-ship | evidence: strong | ref: .agents/release-adapter.yaml (0.52.5 update_instructions) | action: fix | note: CONSUMER NOTE understated — the changed issue/release/critique/retro skill BODIES are byte-identical mirrors in plugins/charness/ and DO ship; consumers receive the new (non-blocking) per-unit verdict mandate prose. Reworded to state this honestly. FIXED this session.
- F2 | bin: act-before-ship | evidence: moderate | ref: docs/handoff.md | action: fix | note: handoff stale (says overhaul "pushed nothing", no #390/0.52.5/release mention). Refreshed at this session's closeout. FIXED this session.
- F3 | bin: valid-but-defer | evidence: moderate | ref: charness-artifacts/quality/nose-baseline.json | action: file-issue | follow-up: deferred charness-artifacts/issue-drafts/2026-06-19-code-layer-dup-followups.md | note: written baseline records no scanner tool_version, so a future nose upgrade silently reads a stale baseline — the re-baseline-per-version trap is unenforced (Baseline robustness gap section).
- F4 | bin: over-worry | evidence: strong | ref: bump level (patch vs minor) | action: document | note: patch is honest — a framing-only generalization of #386 (patch 0.52.4 precedent); #390 flags are repo-local tooling, not a new consumer capability.

## Reviewer Tier Evidence

- Requested tier: bounded fresh-eye release critic (distinct agent context, read-only, shared parent worktree).
- Requested spawn fields: full release context (bump, commit range, update_instructions, baseline) with A–E adversarial assessment instructions.
- Host exposure state: applied
- Application state: host-confirmed: subagent a020d27c9272ba472 ran 27 tool calls (read-only git, current_release.py, gh release view, plugin-export mirror diff) and returned the A–E verdict with "no blockers — patch 0.52.5 is honest".

## Fresh-Eye Satisfaction

Fresh-eye reviewer verdict: **no hard publish blockers; patch 0.52.5 is honest.**
Two should-fixes (F1 note honesty, F2 handoff) were fixed this session; one
robustness gap (F3 baseline tool_version) filed as a follow-up. Bump level,
surface consistency, and the publish boundary were independently confirmed sound
(distinct evidence channel: `gh release view` shows v0.52.4 is still the latest
public release, so no premature 0.52.5 surface exists).
