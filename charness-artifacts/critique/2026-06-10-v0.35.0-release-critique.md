# Critique Review
Date: 2026-06-10

Release: v0.34.0 -> v0.35.0 (minor)

## Decision Under Review

Whether to publish v0.35.0 (minor bump) shipping the already-merged+pushed
closeout-preflight work (range `0734179d..313384cc`): the new `--type
closeout-draft` + enriched `--type goal-closeout` author-time surfaces (shape
rendered live from the owning validators' constants), the goal-template
disposition-form drift-pin guard, the verify-only scaffold-citation finding, and
an inert next-queue draft goal.

## Failure Angles

- Wrong bump level (a hidden compatibility/invocation break forcing MAJOR, or
  nothing-new making it PATCH).
- `update_instructions` overclaiming "no new blocking behavior for consumers".
- Version drift across the 4 generated surfaces after the bump.
- A tag-triggered async publication falsely reported "done".
- An unintended issue close.

## Counterweight Pass

- MINOR is the honest level: the range is purely additive (a new `--type`
  capability + two new scripts + enriched text + one repo-local test), removes/
  renames no Surface entry or invocation, and the validators are untouched
  (verdict-preserving) — not PATCH (new author-facing capability), not MAJOR (no
  contract break).
- The consumer-safety claim is doubly verified: both new surfaces register
  `validator=None` + `commit_boundary=False`, so the commit-boundary blocking
  sweep excludes them on both filters; they are author-time `--type`/`--emit-stub`
  only. Real, not over-worry-driven.

## Structured Findings

<!-- allowed enums (substitute only these) — bin: act-before-ship | bundle-anyway | over-worry | valid-but-defer; evidence: strong | moderate | weak | contested; action: fix | file-issue | document | defer. action: file-issue also needs a follow-up: (issue URL or 'deferred ' plus a handoff anchor). -->
- F1 | bin: bundle-anyway | evidence: strong | ref: packaging/charness.json:5 | action: defer | note: the bump+sync has not run yet (all surfaces at 0.34.0); this is the publish helper's job (bump manifest -> `sync_root_plugin_manifests.py`). The version is single-sourced (`packaging_lib.manifest_with_version_override`) so the one bump cascades to all 7 occurrences across the 4 surfaces — no surface missed.
- F2 | bin: over-worry | evidence: strong | ref: scripts/check_artifact_surface_preflight.py | action: defer | note: "new `--type` surfaces add a blocking gate for consumers" is FALSE — both surfaces are `validator=None` + `commit_boundary=False`, excluded by both the staged-gate filter and `changed_artifacts`; author-time-only. The "no new blocking behavior" claim holds.

## Reviewer Tier Evidence

<!-- allowed Host exposure state: pending-parent-spawn | requested_fields_sent | metadata-hidden | host-defaulted | unsupported | applied. Use applied only with Application state: host-confirmed: plus a concrete signal. -->
- Requested tier: bounded fresh-eye release reviewer (separate subagent context, read-only, release-hygiene packet).
- Requested spawn fields: the release scope (range + commits), the proposed 0.35.0 `update_instructions` line, and the five release-hygiene checks (bump level, consumer safety, version drift, publish boundary, issue close).
- Host exposure state: applied
- Application state: host-confirmed: tool signal: a bounded fresh-eye subagent ran via the Agent tool and returned a SHIP verdict with cited evidence (Surface dataclass field order at `check_artifact_surface_preflight.py:41-52`; the single-sourced version cascade in `packaging_lib.py:58-64,251-252,289`; the only workflow `.github/workflows/mutation-tests.yml` has no push/tag/release trigger; #335 already closed; no close-keyword in the range).

## Fresh-Eye Satisfaction

Reviewer verdict: SHIP — zero real blockers; MINOR is the honest bump; the
`update_instructions` consumer-safety claim is verified true; the version cascade
misses no surface; no tag-triggered async publish boundary; no unintended issue
close. Concrete signal: the subagent independently confirmed both new surfaces are
`validator=None`/`commit_boundary=False` (doubly excluded from the blocking sweep)
and that the shape is rendered live from the validators' constants (not hardcoded),
with the drift-pin test covering the one prose seed copy.
