# Release Critique - v0.13.2

Fresh-Eye Satisfaction: parent-delegated
Reviewer: Curie (`019e7fdd-0b98-7620-8f04-f4050e813a66`)
Target: release critique

## Release Scope

Target release: `v0.13.2` patch for `charness`.

Consumer-facing frame: maintenance patch for autonomous goal closeout
hardening, survivor-test coordination guards, issue/achieve/gather validation
floors, and release evidence refresh. This is reliability hardening, not a new
public API.

## Fresh-Eye Verdict

Curie returned `Blocked As-Is` for a bare
`publish_release.py --part patch --execute` run.

Blockers recorded by the reviewer:

- `.agents/release-adapter.yaml` still had update instructions for `0.13.1`,
  which would render stale operator update text into
  `charness-artifacts/release/latest.md`.
- The publish helper requires the release critique gate to receive a tracked
  critique artifact path.

## Resolution Before Publish

- Updated `.agents/release-adapter.yaml` so `update_instructions` tells
  operators to pull `0.13.2` and describes this maintenance patch.
- Recorded this standalone critique artifact for the helper's
  `--critique-artifact` gate.
- Re-ran release quality after the update-instruction and skill-ergonomics
  fixes: `./scripts/run-quality.sh --release` passed with `69 passed, 0 failed`.

## Go Conditions

- Use `--part patch`. `v0.13.1` already exists, so `--publish-current` would
  target an existing tag/release.
- Let the publish helper own version bump, plugin/marketplace sync, release
  quality, commit, tag, branch push, GitHub release creation, and post-publish
  release artifact update.
- Preserve open-issue non-claims. This release does not close issues
  `#268`, `#269`, `#264`, `#270`, `#265`, or `#261` unless explicit
  `--close-issue` flags are provided.
- Real-host proof remains a release closeout non-claim unless actually run.

## Operator Action Required

Publish with:

```bash
python3 skills/public/release/scripts/publish_release.py --repo-root . --part patch --critique-artifact charness-artifacts/critique/2026-06-01-release-v0.13.2-critique.md --execute
```

After publish, verify `v0.13.2` is visible on GitHub and that any generated
post-publish release artifact commit has been pushed.
