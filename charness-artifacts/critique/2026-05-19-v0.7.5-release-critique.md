# v0.7.5 Release Critique

Date: 2026-05-19
Scope: publish the mutation-score validity repair as `charness` patch release `v0.7.5`.
Reviewer: subagent `019e3fc4-84bc-7710-90fd-64d07423b8f7`

## Act Before Ship

- Record this tracked release-critique proof before running the release helper. The existing critique artifacts cover the mutation fix itself, not the `v0.7.5` release boundary.

## Findings

- Patch bump is appropriate. The change repairs a quality-gate false-green and mutation sampling gap without adding a new maintained capability or breaking an operator contract.
- Current release surfaces are clean before the bump: packaging, Claude plugin, Codex plugin, and marketplace metadata report `0.7.4`, with no version drift.
- Branch state is expected: `main` is ahead of `origin/main` by the mutation fix commit.
- Remote/public state is clear: no `v0.7.5` tag or GitHub release exists before publish.
- GitHub auth is available for the release backend.
- Real-host proof is not triggered by this slice.
- Fresh checkout probes are not configured in the release adapter, so their absence is not a blocker.
- Existing validation is enough to start release because the mutation-fix commit recorded full closeout, and the release helper reruns `./scripts/run-quality.sh` after the version bump before tag and publish.

## Deferrable Risks

- The release helper's CLI-surface gate primarily sees worktree changed paths after the bump. The unreleased mutation-fix paths were manually reviewed by the subagent and did not produce a blocker; helper hardening can be tracked separately.
- Hosted GitHub mutation proof for the new sampler will arrive on the next scheduled workflow run after push. This is post-push evidence, not a release blocker.
