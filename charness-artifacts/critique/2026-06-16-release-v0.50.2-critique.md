# Release v0.50.2 critique
Date: 2026-06-16

## Decision Under Review

Cut release `v0.50.2` (patch bump from `0.50.1`) after the quality runtime
speed work and release-lane proof repair on `main`. Scope includes the quality
runtime optimizations already committed, issue #378 as a deferred quality
follow-up for duplicate-discovery and broad-scanner waste, and the test-only
repair that cleared the changed-line mutation blocker for this release range.

## Failure Angles

- The patch could ship while changed-line mutation coverage still blocks the
  branch.
- Release adapter `update_instructions` could still describe only `0.50.1`,
  causing stale operator instructions or a publish-helper HOLD.
- Generated plugin and package version surfaces could drift during the bump.
- Release notes could overstate consumer migration risk for authoring-repo
  quality/runtime hardening.
- Requested review enforcement remains advisory-only because
  `requested_review_commands` is empty.

## Counterweight Pass

- Bounded fresh-eye release reviewer (read-only, parent-delegated, requested
  tier `medium`) initially returned **HOLD**. Act-before-ship findings were:
  changed-line mutation coverage blocked eight files, `update_instructions`
  lacked a `0.50.2` entry, and branch-path real-host proof should be carried
  into release closeout.
- The mutation blocker was resolved before release mutation. Focused tests
  were added for the standing pytest runner, focused mutation coverage producer,
  and run-slice closeout focused-coverage path; the changed-line mutation gate
  now reports `ok: true` over the release range.
- The `0.50.2` update instruction was added before publish, so the stale-note
  guard should no longer HOLD.
- Issue #378 is correctly deferred. It asks quality to surface this class of
  duplicate-discovery/broad-scanner waste earlier; it does not block shipping
  the current runtime fixes.

## Structured Findings

<!-- allowed enums (substitute only these) - bin: act-before-ship | bundle-anyway | over-worry | valid-but-defer; evidence: strong | moderate | weak | contested; action: fix | file-issue | document | defer. action: file-issue also needs a follow-up: (issue URL or 'deferred ' plus a handoff anchor). -->
- F1 | bin: act-before-ship | evidence: strong | ref: scripts/check_changed_line_mutation_coverage.py | action: fix | note: Initial release critique correctly held on changed-line mutation coverage. The blocker is now cleared by focused tests and a fresh coverage marker; local gate reports `ok: true`.
- F2 | bin: act-before-ship | evidence: strong | ref: .agents/release-adapter.yaml | action: fix | note: Added the `0.50.2` update instruction before publish so generated release records do not carry stale `0.50.1` operator steps.
- F3 | bin: over-worry | evidence: moderate | ref: https://github.com/corca-ai/charness/issues/378 | action: document | note: #378 is a valid quality follow-up, but it is advisory detection for the next recurrence, not a blocker for the current patch.
- F4 | bin: valid-but-defer | evidence: moderate | ref: .agents/release-adapter.yaml | action: defer | note: `requested_review_commands` remains empty, so requested-review enforcement is advisory-only for this release; keep as release-process follow-up rather than patch blocker.

## Reviewer Tier Evidence

<!-- allowed Host exposure state: pending-parent-spawn | requested_fields_sent | metadata-hidden | host-defaulted | unsupported | applied. Use applied only with Application state: host-confirmed: plus a concrete signal. -->
- Requested tier: medium routine release hygiene.
- Requested spawn fields: reasoning_effort=medium; no model override.
- Host exposure state: requested_fields_sent
- Application state: host-confirmed: unavailable; host accepted the request
  fields, but no provider-side confirmation of applied effort was exposed.
- Fresh-Eye Satisfaction: parent-delegated.

## Fresh-Eye Satisfaction

Release v0.50.2 had a parent-delegated bounded fresh-eye reviewer. The reviewer
returned HOLD until mutation coverage and update-instruction blockers were
addressed. Both blockers are now resolved locally; publish may proceed through
the repo release helper and its release-quality/fresh-checkout/public-verifier
gates.
