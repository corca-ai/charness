# v0.7.13 Release Risk Check
Date: 2026-05-25

## Execution

- Target: release critique.
- Fresh-Eye Satisfaction: parent-delegated.
- Packet Consumed: bounded release summary supplied to reviewer.

## Release Scope

- Current version: `0.7.12`.
- Target version: `0.7.13`.
- Target tag: `v0.7.13`.
- Consumer change: publish post-`v0.7.12` fixes already on `main`, including
  pytest temp retention reduction, deferred issue closeouts #212/#213/#214, and
  #215 mutation coverage probe source-boundary fix.

## Surface-Lock Inventory

- Generated/install surfaces: `packaging/charness.json`,
  `.claude-plugin/plugin.json`, `.agents/plugins/codex-plugin.json`, and
  marketplace plugin metadata.
- Consumer-visible behavior: release/local quality temp retention,
  idempotent RCA ledger recording, packaging-surface import bootstrap,
  advisory CLI ergonomics inventory, and scheduled mutation sample coverage
  source scoping.
- Documentation/artifacts: `docs/handoff.md`,
  `charness-artifacts/release/latest.md`,
  `charness-artifacts/debug/latest.md`, and release notes generated by the
  release helper.
- Release backend: GitHub release `v0.7.13` through `gh`.

## Findings

- Act Before Ship: update stale `docs/handoff.md`; it still said #215 was open
  even though GitHub reports #215 closed.
- Act Before Ship: do not frame v0.7.13 as only #215. The release also carries
  pytest temp retention work and #212/#213/#214 deferred follow-ups.
- Act Before Ship: run the repo release helper for `0.7.13`; current version
  surfaces are aligned at `0.7.12`, and `v0.7.13` does not exist yet.
- Act Before Ship: release notes must be self-contained and mention the mutation
  coverage probe boundary fix plus deferred follow-up fixes.
- Bundle Anyway: keep #215 fix and closeout carrier in this patch; `main` is
  clean and #215 is closed on GitHub.

## Counterweight Triage

- Over-Worry: full real-host install smoke is not required for this release
  scope because changed paths do not hit the adapter's real-host trigger
  surfaces.
- Over-Worry: waiting for scheduled mutation to rerun before tagging is not
  required if release notes say local closeout proved the coverage/sample
  blocker.
- Valid but Defer: add actual fresh-checkout probes to the release adapter
  later; current `not_configured` status is a known release-process gap.
- Valid but Defer: make requested-review enforcement non-advisory later by
  declaring concrete review commands.

## Operator Action Required

- Run the repo release helper with `--part patch --execute` and
  `--critique-artifact charness-artifacts/critique/2026-05-25-v0.7.13-release-risk-check.md`.
- Verify `gh release view v0.7.13` after publish.
- Verify `git status --short --branch` is clean after the post-publish artifact
  commit and push.

## Upgrade Path

- Operators run `charness update`.
- Restart Claude Code or Codex if the host cache still shows the previous
  version.

## Next Move

Commit this critique and the handoff correction, then publish `v0.7.13` through
the checked-in release helper.
