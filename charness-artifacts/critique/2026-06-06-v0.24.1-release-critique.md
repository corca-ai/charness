# v0.24.1 Release Critique (bounded fresh-eye, RELEASE decision)

Date: 2026-06-06
Reviewer scope: bounded fresh-eye, RELEASE decision only (bump level, compatibility, install/update honesty, issue-close linkage, version drift, mirror sync, open risks). Read-only in the shared parent worktree; no index/worktree mutation, no publish helper, no file writes.
Bundle + plan: `v0.24.0..HEAD` = `219ec1ab` (fix #320, the only local-ahead-of-origin commit) + `d262adfa` (handoff) + `2164889a` (v0.24.0 closeout record). Plan: `publish_release.py --part patch --execute` → 0.24.0 → 0.24.1, sync manifests, `run-quality.sh --release`, commit/push/tag `v0.24.1`, create GitHub release; open question = add `--close-issue 320`.

## Verdict: CLEAR to ship at PATCH — but the plan is INCOMPLETE as stated and will HARD-BLOCK at preflight unless two flags/edits are added.

The change content is clean, honest, and correctly sized at patch. But the literal plan (`--part patch --execute`, no other flags) will be **refused by two unconditional publish gates** before any mutation:

1. **`update_instructions` staleness guard fires.** `.agents/release-adapter.yaml:33` still reads "Run `charness update` to pull **0.24.0** (minor release...)". The guard `update_instructions_version_blocker` (publish_release_preflight.py:69-94) blocks when `target_version in text` is false AND `previous_version in text` is true. Confirmed read-only: `grep '0.24.1'` → absent; `0.24.0` → present (line 33). `previous_version` resolves to `0.24.0` (manifest current + latest tag `v0.24.0`, the only v0.24.x tag). So target absent + previous present → **blocker fires**. This must be refreshed to describe 0.24.1 (and drop the misleading "minor release" framing) before publish.

2. **Release-critique gate requires an artifact.** `enforce_release_critique_gate` (publish_release_preflight.py:97-136) refuses publish unless `--critique-artifact <tracked charness-artifacts/critique/...md>` (or an honest `--critique-blocked <host-signal>`) is passed. The plan names neither. **This very document is that artifact** — it must land at `charness-artifacts/critique/2026-06-06-v0.24.1-release-critique.md` (matching the prior `…-v0.24.0-release-critique.md` pattern, confirmed in `charness-artifacts/critique/`), be `git add`-tracked, and passed via `--critique-artifact`.

Neither is a content problem — both are honesty guards working as designed. Add `--critique-artifact <this file>` and refresh `update_instructions` and the release is CLEAR.

## Four-bin triage

**Act Before Ship (must do, or preflight refuses / record is dishonest):**
- Refresh `.agents/release-adapter.yaml` `update_instructions` to describe **0.24.1** (the test-only bug-repair for #320), removing the stale "pull 0.24.0 (minor release...)" lead line. Staleness guard depends on the `0.24.1` string appearing.
- Commit this critique under `charness-artifacts/critique/` and pass `--critique-artifact`. Without it the publish gate raises.

**Bundle Anyway (correct to ship inside this release, no separate action):**
- The handoff (`d262adfa`) and v0.24.0 closeout record (`2164889a`) — already-merged repo state, ride along correctly.
- The debug artifact and spec handoff — durable memory committed with the work they support (per repo phase rules). Correct to include.

**Over-Worry (raised, dismissed):**
- "Pre-existing uncovered lines in `staged_commit_gate_plan.py`." Coverage after fix is 85%, Missing = `49, 250, 264, 273, 277-293, 297` — all pre-existing `main()`/CLI lines, none changed in this bundle, none in #320's proof targets. Not a release concern.
- "Source edited without its mirror." `diff -q scripts/staged_commit_gate_plan.py plugins/charness/scripts/staged_commit_gate_plan.py` → IDENTICAL. And the bundle touches **no** source file at all — only the test plus artifacts. No mirror obligation.
- "Cutting a release for a test-only change is noise." It is honest: it advances the repo's own quality-gate coverage and lets `--close-issue 320` ride the release commit-body auto-close path. Patch is the lightest honest bump.

**Valid but Defer (tracked, not a release blocker):**
- Systemic recurrence #219→#251→#260→#320 (changed-line mutation coverage has no pre-merge gate) — correctly escalated to `charness-artifacts/spec/mutation-changed-line-premerge-gate.md`. A spec/impl follow-up, not this release.
- `setup_agent_docs_fresh_eye_lib.py` / `setup_commit_discipline_lib.py` selection-budget drops — a separate non-coverage signal, tracked as `follow-up:mutation-selection-budget-setup-libs`. Not part of #320's resolved signal.

## Bump-level verdict: PATCH is correct.

The only code change is `tests/quality_gates/test_staged_commit_gate_plan.py` (+10 lines, one new test). Zero operator-facing / install / CLI / skill surface moved (verified: `git diff --name-only v0.24.0..HEAD` matched none of `scripts/**`, `skills/public/**`, `skills/support/**`, `plugins/**`, `.claude-plugin/**`, `.agents/plugins/**`, `packaging/**`, `charness`, `.agents/command-docs.yaml`). No additive feature, no behavior change, no API/contract change. Semver patch (bug-repair to the repo's own quality coverage) is exactly right; minor/major would over-state it. Confirmed `bump_version.py --part patch` yields 0.24.1 from a manifest uniformly at 0.24.0.

Caveat tied to the bump: the stale `update_instructions` call this a "minor release" — that framing is wrong for a patch and must be rewritten (see Act Before Ship), independent of the guard.

## Compatibility / install + update

- **Version-surface cleanliness:** `packaging/charness.json` reads `0.24.0` at all three fields — top-level `version` (line 5), `codex.manifest.version` (line 26), `claude.manifest.version` (line 69). Generated surfaces all at 0.24.0: `.claude-plugin/marketplace.json` (lines 8, 14), `plugins/charness/.claude-plugin/plugin.json:3`, `plugins/charness/.codex-plugin/plugin.json:3`. `current_release.py --repo-root .` reports `drift: []` and `git_status: []` (clean tree, confirmed via `git status --porcelain` → empty). Patch → 0.24.1 is unambiguous.
- **Mirror sync:** `tests/**` is not a mirrored surface — no plugin mirror obligation for the test change. No source file was edited, so no `plugins/charness/**` mirror is owed; the one relevant mirror (`staged_commit_gate_plan.py`) is already byte-identical.
- **Install honesty:** nothing installable changed; existing installs are unaffected by content. The helper still syncs/bumps the generated manifests to 0.24.1 as part of the version-only delta, which is correct.
- **`update_instructions` honesty:** Does NOT currently overclaim functionally, but is **stale and mislabeled** — it advertises 0.24.0's two #318/#319 features and calls it a "minor release," which is wrong for a 0.24.1 patch carrying only a test-coverage repair. The staleness guard (target string must appear) correctly refuses to publish over it. Fix: lead with a 0.24.1 line naming the #320 test-coverage repair (test-only, no operator action required), then keep the carried-forward history. After that edit the guard passes (`0.24.1` present).

## Issue-close linkage (#320): recommend CLOSE — with `--close-issue 320`, conditioned on the two Act-Before-Ship items.

- #320 is currently **OPEN** (`gh issue view 320` → `state: OPEN`), so closing at release is the live action, not a no-op.
- The specific blocking signal is provably resolved: the new test `test_fast_surface_verify_gates_degrade_on_surface_error` exercises the `except SurfaceError: return []` branch at lines 72-73 (confirmed in source). Read-only proof I ran: full file `20 passed`; `coverage report` shows 72-73 no longer in Missing (was `72-73` at 84%, now absent at 85%). The debug artifact additionally records a targeted-mutant kill on line 73 with sibling-specificity — credible.
- It **won't recur in the next base..head** for this file: the changed line is now covered in the same slice that introduced the need.
- The `--close-issue` mechanism is honest: it writes `Close #320.` into the release commit body and then verifies the issue reaches `CLOSED` via `gh`, with a logged manual-fallback close if auto-close doesn't fire (`release_issue_closeout.py:87-131`) — the same path that closed #318/#319 at v0.24.0. The closeout is recorded durably in `charness-artifacts/release/latest.md`.
- **Weighing the Non-Claim** ("cannot prove the next scheduled run is green"): this is the right scope to close on anyway. The issue's *blocking signal* was the uncovered changed line, and that is fixed and proven. The two residual non-claims are correctly carved off — the budget-drop is a separate tracked follow-up (`follow-up:mutation-selection-budget-setup-libs`), and the systemic recurrence is escalated to the spec handoff. Holding #320 open to await a CI-only scheduled verdict it cannot reproduce locally would conflate three distinct signals into one un-closeable ticket. Close #320 (its filed signal is resolved); let the spec/follow-up carry the rest.

If you would rather not depend on the GitHub auto-close round-trip during publish, LEAVE-OPEN and close manually post-release is acceptable — but CLOSE via `--close-issue 320` is the cleaner, honest linkage and is my recommendation.

## Open Risks

1. **Plan-as-written halts at preflight (HIGH, fully mitigable).** Two unconditional gates — `update_instructions` staleness and the release-critique requirement — will refuse `--part patch --execute` alone. Add `--critique-artifact charness-artifacts/critique/2026-06-06-v0.24.1-release-critique.md` (this file, tracked) and refresh `update_instructions` to mention 0.24.1 first. Not blockers on the change; blockers on the invocation.
2. **Stale "minor release" prose in `update_instructions` (MEDIUM).** If shipped unedited it would both trip the guard and, were the guard bypassed, mislead operators into expecting features. Rewrite for 0.24.1 test-only repair.
3. **Scheduled-mutation green is CI-only and unproven locally (LOW, accepted).** The debug/spec artifacts state this honestly; a `workflow_dispatch` cannot reproduce a `schedule`-computed `base_sha`. This does not block the release or the #320 close — the filed blocking signal is independently proven resolved. Track via the spec handoff.
4. **Systemic recurrence not closed by this release (LOW, by design).** The #219→#251→#260→#320 class needs the pre-merge gate spec'd in `charness-artifacts/spec/mutation-changed-line-premerge-gate.md`. Expected and correctly deferred — note it in the release record so it is not mistaken for fixed.

Commands run (all read-only): `git log/diff/show/status` over `v0.24.0..HEAD`; `python3 .../release/scripts/current_release.py --repo-root .`; `python3 -m pytest tests/quality_gates/test_staged_commit_gate_plan.py` (20 passed); `coverage run`+`coverage report` on `staged_commit_gate_plan.py` (72-73 covered; `.coverage` removed); `diff -q` source vs plugin mirror (identical); `gh issue view 320` (OPEN); `grep` for `0.24.1`/`0.24.0` in the release adapter; file reads of `packaging/charness.json`, `.agents/release-adapter.yaml`, the debug + spec artifacts, and the release-helper preflight/closeout scripts.
