# Release Critique: v0.44.0
Created: 2026-06-13
Target: `release-critique`
Fresh-Eye Satisfaction: parent-delegated
Packet Consumed: `charness-artifacts/critique/2026-06-12-164434-packet.md`

## Release Scope

- version: `0.44.0`
- tag: `v0.44.0`
- consumer change: minor release; the #358 tripwire response plus the #184
  numeric-target wiring it responds to. Three content commits on v0.43.0
  (`6f3d28e8`, `22f3542d`, `cc5c30f1`): the `check_mutation_run_proof.py`
  claim gate (refuses citing a mutation run as proof of a claim its trigger
  cannot evaluate), the loud machine-readable no-base-sha verdict in
  `check_changed_line_mutation_coverage.py`, the schedule-only mutation
  auto-close (charness workflow + shipped quality template), the
  `conversion_upgrade` RCA-ledger mechanism, quality reference and advisory
  wiring, and tests. Issue #358 already CLOSED via carrier `22f3542d`
  (verified live); no `--close-issue` needed.

## Surface-Lock Inventory

- `.agents/release-adapter.yaml` update instructions for `0.44.0`
  (act-before-ship scoping fix folded; see Findings).
- `packaging/charness.json`, generated plugin manifests, and marketplace
  versions — uniformly `0.43.0` pre-helper, bumped and synced by
  `publish_release.py`.
- `charness-artifacts/release/latest.md`, rewritten by `publish_release.py`.
- Fresh-checkout probes: passed this session pre-critique; the helper re-runs
  them during `--execute`.
- `post_publish_install_refresh`: adapter-declared, auto-run after verified
  publish.
- Git tag `v0.44.0` + GitHub release record.

## Angles

- Operational checklist: minor is the honest bump — new maintained script,
  additive optional schema field, workflow auto-close timing change; no
  command-surface break, rename, or migration (not major), and the workflow
  behavior change rules out patch. All version surfaces uniformly `0.43.0`
  pre-helper. Helper enforces clean worktree, the critique-artifact gate, and
  the update-instructions staleness guard (the 0.44.0 entry pre-empts it).
- Cold reader: every numbered claim in the 0.44.0 update-instructions entry
  was verified against the actual `22f3542d` diff. Claims (1), (2), and (4)
  accurate (the RCA-ledger consumer no-op matches the presence-gating in
  `skills/shared/references/rca-ledger-append.md`). Claim (3) as first
  drafted was operator-misleading: the shipped template reaches consumers
  only at install time (`propose_mutation_testing.py` short-circuits at
  `installed`), so existing consumer workflow instances keep dispatch
  auto-close until the operator re-copies the template.
- Counterweight: the entry is otherwise calibrated one-to-one against the
  diff with no tripwire-status inflation; the behavior-change scoping issue
  was a one-clause fix, not a redesign.

## Findings

### Act Before Ship

- Scope the BEHAVIOR CHANGE clause in the 0.44.0 entry to charness + newly
  installed consumer workflows, with an explicit re-copy step for existing
  installs. **Folded before publish** (adapter entry rewritten).

### Bundle Anyway

- Commit the untracked critique packet files with the release-prep commit
  (helper's clean-worktree refusal forces this anyway). **Folded.**

### Over-Worry

- Version-surface drift (uniform pre-helper, helper-bumped), `--close-issue`
  (already CLOSED), staleness HOLD (entry pre-empts it).

### Valid but Defer

- Template-drift advisory: detect installed-workflow vs shipped-template
  divergence in consumer repos so future template behavior changes are
  self-announcing. Matches the deferred-follow-up pattern in the #358 commit
  message; not a ship blocker.

## Reviewer Tier Evidence

- Requested tier: high-leverage
- Requested spawn fields: model=gpt-5.5, reasoning_effort=medium,
  service_tier=priority
- Host exposure state: host-defaulted
- Application state: requested fields not applied — the host spawned its
  default reviewer (agent name `release-critique-0440`); no provider-side
  application confirmation was exposed.

## Next Move

Commit the adapter entry, this artifact, and the packet files; then
`publish_release.py --repo-root . --part minor --critique-artifact
charness-artifacts/critique/2026-06-13-release-v0.44.0-critique.md --execute`.
