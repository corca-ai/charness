# Release 0.56.7 Quality Loop Critique
Date: 2026-06-27

Packet Consumed: charness-artifacts/critique/2026-06-27-031756-packet.md

## Decision Under Review

Publish Charness `0.56.7` as a patch release for the `v0.56.6..HEAD` delta.
The release locks proof-scope honesty and validation-runtime repairs without a
migration or new public command surface.

Release scope: patch bump from `0.56.6` to `0.56.7`; consumers get release-delta
real-host planning evidence, a safer closeout refusal when broad pytest proof is
narrower than the closeout payload, faster standing pytest startup probes,
clearer focused coverage producer statuses, and planner plain-output guidance
when real-host proof is required.

Surface-lock inventory:

- generated/plugin surfaces: `plugins/charness/scripts/run_slice_closeout.py`,
  `plugins/charness/scripts/run_standing_pytest.py`,
  `plugins/charness/scripts/suggest_mutation_coverage_command.py`, and
  `plugins/charness/skills/release/scripts/plan_release_run*.py`
- consumer-visible behavior: `plan_release_run.py --json` evaluates real-host
  proof from the release delta; plain planner output names required real-host
  proof scope; `run_slice_closeout.py` fails on narrow broad pytest proof scope;
  `run_standing_pytest.py` avoids startup probe subprocesses while preserving
  serial fallback; `suggest_mutation_coverage_command.py` explains
  recommended/partial/missing/noop status
- docs/release notes: `docs/handoff.md`, the focused coverage UX artifacts, and
  these `v0.56.7` notes must use the release delta, not the prepare packet's
  clean-worktree section
- release proof seams: release planner reports real-host proof required for
  `integrations-and-control-plane`; the publish helper must run the final
  bump/sync/quality/fresh-checkout/publish/install-refresh path

## Failure Angles

- Gawande/checklist: clean or commit the critique packet state before publish;
  persist this final critique artifact; treat real-host proof as required; run
  the release helper for bump/sync/verify/tag/publish/distinct-channel
  verification/install refresh.
- Minto/structure: release notes must be grouped by operator story: proof-scope
  honesty and faster validation. Do not use the prepare packet's empty changed
  path section as the release inventory.
- Raskin/interface: bundle the cheap planner plain-output/help guidance so the
  default human path does not hide required real-host proof.
- Counterweight: patch bump is correct; no migration or rollback section is
  needed beyond the normal `charness update` path; broader nested-CLI runtime
  debt and closeout auto-discovery remain later work.

## Counterweight Pass

- Act before ship: commit the release critique artifact and packet, commit the
  planner plain-output/help guidance, use release-delta inventory for notes, run
  final closeout/quality gates, and publish through the helper with real-host
  proof recorded.
- Bundle anyway: pass a concise `v0.56.7` notes file to the publish helper; keep
  the modified `v0.56.6` proof ledger out of the headline; state that update
  path is unchanged.
- Over-worry: minor bump, migration notes, and long per-commit appendix are not
  justified by this patch.
- Valid but defer: split broad real-host checklist triggers more narrowly, add a
  path-list mode for large real-host gate packets, improve closeout error
  remediation wording, and revisit closeout auto-discovery for focused coverage
  producer commands.

## Structured Findings

- F1 | bin: act-before-ship | evidence: strong | ref: `plan_release_run.py --part patch --json` | action: fix | note: final release must treat `evidence_scope=release_delta`, previous `0.56.6`, target `0.56.7`, and `real_host.required=true` as the release proof posture.
- F2 | bin: act-before-ship | evidence: strong | ref: `charness-artifacts/critique/2026-06-27-031756-packet.md` | action: fix | note: the packet is input evidence only; persist this final critique artifact and do not use the packet's clean-worktree changed-path section as release inventory.
- F3 | bin: bundle-anyway | evidence: strong | ref: `skills/public/release/scripts/plan_release_run.py` | action: fix | note: plain planner output should name required real-host proof scope; bundled before publish.
- F4 | bin: bundle-anyway | evidence: strong | ref: `scripts/run_slice_closeout.py` | action: document | note: notes should frame broad pytest proof-scope refusal as closeout honesty, not as a user migration.
- F5 | bin: over-worry | evidence: strong | ref: `packaging/charness.json` | action: document | note: patch bump is sufficient because this release repairs validation/proof/runtime behavior without breaking invocation expectations.
- F6 | bin: valid-but-defer | evidence: moderate | ref: `.agents/release-adapter.yaml` | action: defer | note: real-host checklist is broad for integrations-and-control-plane; split triggers after this release.

## Reviewer Tier Evidence

- Requested tier: high-leverage
- Requested spawn fields: model=gpt-5.5, reasoning_effort=medium, service_tier=priority
- Host exposure state: metadata-hidden
- Application state: parent spawned three angle reviewers and one counterweight
  reviewer through `multi_agent_v1`; host accepted the agents but did not
  confirm metadata application

## Fresh-Eye Satisfaction

Fresh-Eye Satisfaction: parent-delegated. Operational reviewer
`019f0715-9ec9-76a1-939f-c182ec1fd57b`, structure reviewer
`019f0715-c385-7720-ba1a-045d41ec9e86`, interface reviewer
`019f0715-e0eb-7752-9dfb-f85e6fc84417`, and counterweight reviewer
`019f0716-0451-7a83-ac71-0e4f502c7961` completed read-only release critique.
Their Act Before Ship concerns are handled by this artifact, the planner
plain-output/help fix, and the required release helper path.
