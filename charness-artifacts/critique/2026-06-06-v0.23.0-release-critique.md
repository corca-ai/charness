# Fresh-Eye Release Critique — v0.23.0 (`306-316-open-followups`)

Verdict: **ONE BLOCKER** — bump level (minor) is correct and the bundle is honest, but the release adapter `update_instructions` still describe 0.22.0/#302–#305 and not 0.23.0, which the publish preflight version-blocker will REFUSE; refresh that line, then `--part minor --execute --close-issue` is safe.

Scope: release decision for tag `v0.23.0` (0.22.0 → 0.23.0), bundling goal slug `306-316-open-followups` issues #306, #311, #314, #315, #316, #317. Different agent context from the implementers and from the rung-2 disposition reviewer (`2026-06-06-306-317-disposition-review.md`, CLEAR). This critiques the CUT DECISION, not the code (code passed two prior fresh-eye reviews).

## Angles

### 1. Bump level — minor is the lightest honest bump (no blocker)

- The bundle is additive-only operator surface change: #317 new greenfield AGENTS.md `## Commit Discipline` block (new-body path only) + new `commit_discipline_drift` inspector finding; #315 new visible closeout-evidence placeholders + a parser HARDENING (placeholders now rejected, which is stricter not looser); #314 new commit-boundary checker wiring + path reconciliation (no existing gate removed); #311 new `fresh_eye_delegation_rule_drift` inspector finding; #316 prose/template guardrail with explicitly NO new gate; #306 coverage-honesty test work (gate stays blocking).
- No BREAKING marker anywhere: `git log origin/main..HEAD --format='%B' | grep -iE 'BREAKING|major|incompat'` returns nothing.
- No invocation/compatibility break: every `setup`/`achieve` change is tell-don't-rewrite or new-body-only; existing AGENTS.md bodies are never rewritten (disposition review confirmed byte-identical-after-inspection tests). No CLI flag/contract removed.
- **Real-blocker vs over-worry:** OVER-WORRY both ways. Not a major (nothing breaks downstream). Patch would be *dishonest* — `feat(quality-gates)`, `feat(setup)`×2, `feat(achieve)` add new operator-visible surfaces (new inspector findings an operator will see after `charness update`, a new greenfield AGENTS.md block). `minor` is the lightest HONEST bump. CLEAR.

### 2. Generated-surface sync — consistent, in sync (no blocker)

- `current_release.py` reports `drift: []`, clean `git_status`, and all five version surfaces uniform at 0.22.0 (packaging manifest, claude plugin, codex plugin, claude marketplace; codex marketplace by source-path).
- Independently confirmed: `packaging/charness.json` (3 sites), `.claude-plugin/marketplace.json` (2 sites), `plugins/charness/.claude-plugin/plugin.json` all read 0.22.0.
- No `skills/` source ahead of the `plugins/` mirror: a fresh `sync_root_plugin_manifests.py` run was a no-op (`git status --porcelain` stayed empty afterward), and the disposition review diff-verified the mirror for every edited `skills/` + `scripts/` path.
- **Real-blocker vs over-worry:** OVER-WORRY. Surfaces are coherent; the bump script will move all five in lockstep. CLEAR.

### 3. Publish boundary honesty — publish IS synchronous & verified, but "green CI on released SHA" is NOT automatic (real honesty caveat, not a cut-blocker)

- `release_backend` is unset in the adapter, so it defaults to `{id: gh, binary: gh}` (`release_backend.py::default_release_backend`). Publication is therefore NOT a tag-triggered async pipeline — it is in-process and verified: `publish_release_cli.py` does git tag → `git push <remote> <branch> <tag>` → `create_release` (`gh release create`) → `verify_release_visible` (fails the publish if the release object is not visible) → `ensure_release_issues_closed` (actively re-reads each issue state and manual-closes + re-verifies as fallback). So after a successful run, "tag pushed + GitHub release CREATED and VERIFIED visible + issues VERIFIED closed" is an HONEST claim — the release object exists in the same run, not "pending."
- **Caveat that must not be over-claimed:** the goal artifact (Agent Verification Plan → External/Live Proof, User Acceptance) promises "one remote GitHub Actions CI run on the released SHA … CI is green on the released SHA." `.github/workflows/` contains ONLY `mutation-tests.yml`, whose triggers are `workflow_dispatch` + `pull_request` (two specific paths) + `schedule` (cron) — there is **no `push:` trigger and no tag/release trigger.** Pushing `main` + tag `v0.23.0` will NOT automatically start a CI run on the released SHA. Any "CI green on the released SHA" claim requires a manual `workflow_dispatch` (or waiting for the 3-hourly cron to pick up `main`), and even then `mutation-tests.yml` is a scheduled deeper-check, not a release-gating CI.
- **Real-blocker vs over-worry:** the publish-immediacy worry is OVER-WORRY (publish is synchronous and self-verifying). The CI-on-released-SHA promise is a REAL but NON-cut-blocking honesty caveat: the closeout must say "remote CI is not push-triggered; run it via workflow_dispatch or rely on the next scheduled run" rather than asserting an automatic green CI the push will not produce. Does not block the cut; the broad `--release` gate already passed locally (72/0) on the bundled state.

### 4. Issue-closeout carriers — all six present (no blocker)

- `git log origin/main..HEAD --format='%B' | grep -i 'close #'` returns exactly: Close #316, #315, #317, #311, #306, #314 — one carrier per slice commit, all six in-scope issues covered.
- Belt-and-suspenders: the release flow itself ALSO appends `Close #N.` lines to the release commit body (`release_commit_body`) for each `--close-issue`, AND `ensure_release_issues_closed` verifies/forces closure post-push. So closure is double-carried (slice-commit carriers + release-commit body + active verification).
- #319 correctly has NO carrier and is confirmed OPEN (`gh issue view 319` → state OPEN; `gh issue list --state open` lists 319, 318, 184, plus the six to-be-closed). It must STAY open — verified it will not be closed (not in any `Close #` line, not passed to `--close-issue`). Note #318 is also a pre-existing open follow-up that stays open; out of this bundle's scope.
- **Real-blocker vs over-worry:** OVER-WORRY. All six carriers present and triple-reinforced; #319/#318 stay open. CLEAR. (Maintainer must pass `--close-issue 306 311 314 315 316 317` and NOT 318/319.)

### 5. Operator-misread risk / update_instructions accuracy — flag-only is honest, but update_instructions are STALE (this is the blocker)

- Misread risk is LOW and verified: #317's AGENTS.md block lands ONLY via `render_agents_template` (new-body path); #311/#317 inspectors only APPEND findings (`commit_discipline_drift`, `fresh_eye_delegation_rule_drift`, both `review_required`) and return `(state, findings)` touching no file. Tests assert byte-identical bodies after inspection (`test_setup_commit_discipline.py:102`, `test_setup_inspect_policy.py:375`). So "setup now rewrites my AGENTS.md" is NOT a real risk — setup TELLS, never rewrites. The disposition review independently confirmed this never-rewrite invariant HOLDS.
- **But the `update_instructions` are stale and will block publish.** Adapter line 33 still reads: ``Run `charness update` to pull 0.22.0 (minor release: gather/setup/release robustness fixes for #302-#305 …)``. It names the PREVIOUS version 0.22.0 and #302–#305 (the LAST release's content), and says nothing about 0.23.0 or this bundle's #306/#311/#314/#315/#316/#317 operator-facing changes. There is no new top-line entry describing the 0.23.0 inspector findings, so an operator reading the generated release record would get the prior release's steps.
- **Real-blocker vs over-worry:** the misread risk is OVER-WORRY (flag-only, tested). The stale update_instructions is a **REAL BLOCKER** — see Angle 6 / Blockers: it both ships stale operator steps AND is mechanically caught by the publish preflight.

### 6. Release-time proof — real_host_proof honestly not required (no blocker)

- `real_host_required=false` is honest for THIS bundle. The adapter's `real_host_required_path_globs` (README.md, docs/host-packaging.md, integrations/tools/**, scripts/doctor*.py, install/support-sync scripts) are the host-PATH/install-surface paths that demand a second-machine smoke. This bundle touches none of them: changes are in `scripts/` (commit-boundary gate wiring, setup inspector libs), `skills/public/achieve/**`, `skills/setup/**`, and tests — no installer, doctor, tool-manifest, or host-packaging change.
- Fresh-checkout probes are `configured` and (per the goal's Final Verification) pass: `./charness --help`, `./charness goal check --help`, `doctor.py --json --skip-release-probe`. No install-surface change means a human host smoke would prove nothing new here.
- **Real-blocker vs over-worry:** OVER-WORRY. `required=false` + passing fresh-checkout probes is honest for an inspector/gate/prose bundle. CLEAR.

## Counterweight

Worries that are NOT blockers (do not delay the cut for these):

- **Bump to major** — nothing breaks invocation/compatibility; tell-don't-rewrite + new-body-only. Minor is correct.
- **Mirror/version drift** — `drift: []`, clean tree, idempotent sync no-op, all surfaces uniform at 0.22.0.
- **"Publish is async/pending"** — false; `gh release create` runs and is verified visible in-process; issue closure is actively verified with a manual-close fallback.
- **Issue carriers missing** — all six present and triple-reinforced; #318/#319 correctly excluded.
- **"setup will rewrite my AGENTS.md"** — false; inspectors only flag, greenfield block only on new-body path, byte-identical-after-inspection tests pass.
- **real_host smoke needed** — false; no install/host-PATH surface in the bundle.
- **Self-modifying #315 gate weakening this goal's own closeout** — already dispositioned: the parser HARDENED (placeholders rejected), tests prove placeholder-only artifacts still fail the complete gate.

## Bump Decision

**minor (0.22.0 → 0.23.0).** Justified: the bundle adds new operator-visible surfaces (two new setup inspector findings, a new greenfield AGENTS.md commit-discipline block, new commit-boundary checker wiring, new achieve closeout placeholders + prose guardrail) — that is additive feature surface, so `patch` would understate it. Nothing removes or breaks an invocation, flag, or compatibility contract — there is no BREAKING change, so `major` would overstate it. `minor` is the lightest honest bump.

## Publish Boundary

What the closeout MAY honestly claim after a successful `--part minor --execute --close-issue` run:

- "Pushed `main` + tag `v0.23.0`; GitHub release CREATED via `gh release create` and VERIFIED visible in the same run; issues #306/#311/#314/#315/#316/#317 VERIFIED CLOSED (carrier: direct release commit body, with active post-push verification + manual-close fallback)." This is a synchronous, verified boundary — NOT "tag pushed, workflow pending."

What the closeout may NOT claim without doing more work:

- "CI is green on the released SHA." No workflow is `push`/tag-triggered (`mutation-tests.yml` runs only on `workflow_dispatch` / two-path `pull_request` / cron `schedule`). The released SHA gets no automatic CI run. To honor the goal's "one remote CI run on the released SHA," the maintainer must `workflow_dispatch` `mutation-tests.yml` on the released ref (or accept the next scheduled cron) and bind that result — otherwise record it as a non-claim. The local `./scripts/run-quality.sh --release` (72/0 on `54e1b6b1`) is the real bundle proof; remote CI is supplementary and not push-automatic.

## Blockers

1. **Stale release `update_instructions` (must-fix before cutting).** Adapter `.agents/release-adapter.yaml` line 33 still describes 0.22.0 / #302–#305 and not 0.23.0. The publish preflight `update_instructions_version_blocker` (`publish_release_preflight.py:69`, wired at `publish_release_plan.py:95` → `raise SystemExit`) fires exactly when the text contains the previous version (0.22.0) but not the target (0.23.0) — which is the current state. The publish (even dry-run) will REFUSE with a `update_instructions … 0.23.0` SystemExit (proven by `test_publish_blocks_when_update_instructions_are_stale`). FIX: add/refresh a top-line 0.23.0 `update_instructions` entry describing this bundle's operator-facing changes (new setup commit-discipline block + the two new inspector findings #311/#317, the #314 commit-boundary checker wiring, the #315 placeholders, the #316 publication-approval boundary) before running the cut. This is normal release prep, but it is a HARD gate — the cut cannot proceed without it.

Non-blocking but record honestly:

- The "remote CI green on the released SHA" promise is not satisfied by the push alone (no push/tag CI trigger). Either dispatch `mutation-tests.yml` on the released SHA and bind the result, or record it as a non-claim in the release closeout. (See Publish Boundary.)

After the update_instructions refresh, the maintainer should proceed with `--part minor --execute --close-issue 306 311 314 315 316 317` (NOT 318/319). Everything else — bump level, mirror sync, carriers, flag-only operator safety, real_host non-requirement — is CLEAR.
