# v2.1.3 Release Critique
Date: 2026-07-18

## Execution

Operational and first-touch release angles ran as read-only parent-delegated reviewers, followed by a separate counterweight. The first counterweight approval was quarantined when the parent changed the worktree before fingerprint verification; a stable-tree rerun passed with no drift.

## Packet Consumed

`charness-artifacts/critique/2026-07-18-135512-packet.md`

## Target

Release critique shaped by Gawande operational and Minto/Raskin narrative/first-touch angles.

## Decision Under Review

Publish Charness v2.1.3 as a patch release, push the seven local implementation/proof commits, and close only after public and installed-state readback.

## Release Scope

`v2.1.3`: safer failed release preparation and resume, better focused coverage selection, and more precise dead-code advisory triage. Patch is the lightest honest bump because public invocation and compatibility remain unchanged.

## Capability at Stake

Operators must be able to upgrade onto safer release maintenance without confusing a focused hint or advisory classification for terminal proof.

## Surface-Lock Inventory

- Generated artifacts: packaging manifest, Claude/Codex plugin manifests, marketplaces, and checked-in plugin export.
- Consumer behavior: pre-commit restore/quarantine, post-commit resume, focused coverage suggestions, and dead-code advisory classifications.
- Documentation: self-contained v2.1.3 notes, release artifact, evergreen update instructions, and post-publish handoff baton.
- Integration/install: public GitHub release, fresh-checkout probes, `charness update`, installed version, doctor, and cache/path readback.

## Failure Angles

- Operational: distinguish the content delta, implementation proof range, tag/public state, and installed state; ensure helper preconditions and rollback/resume seams are closed.
- First touch: explain recovery and advisory behavior without internal commit-subject jargon, false speed claims, or stale v2.1.2 handoff routing.

## Findings

- Release content correctly spans `v2.1.2..v2.1.3`; implementation changed-line proof correctly spans `3c2516b9..b618c784`. The notes make the different consumers explicit.
- Generated commit subjects were insufficient operator notes. `charness-artifacts/release/2026-07-18-v2.1.3-notes.md` now explains rollback/resume, selector and dead-code non-claims, update, and no migration.
- The publish helper needs a clean worktree, so this critique, packet, and notes must be committed before dry-run/execute.
- Publication may proceed without a configured real-host exercise, but lifecycle completion requires installed version, doctor, and cache/path readbacks after update.
- The current handoff is truthful before publish but must be reconciled to v2.1.3 after public/install verification.

## Counterweight Pass

- `Act Before Ship`: commit the release-review records and bind distinct installed-state readbacks to closeout.
- `Bundle Anyway`: reconcile the handoff after publication before ending the session.
- `Over-Worry`: do not treat the tag-based content range and branch-based implementation proof range as a defect; both are honest and documented.
- `Valid but Defer`: no real-host trigger matched, so do not invent an unrelated host exercise.

## Structured Findings

- R1 | bin: act-before-ship | evidence: strong | ref: charness-artifacts/release/2026-07-18-v2.1.3-notes.md | action: fix | note: commit critique packet, critique record, and self-contained notes so publish starts clean
- R2 | bin: act-before-ship | evidence: strong | ref: skills/public/release/references/install-refresh.md | action: fix | note: require installed version, doctor, and cache/path readbacks after update before installed-status claim
- R3 | bin: bundle-anyway | evidence: strong | ref: docs/handoff.md | action: fix | note: reconcile the session baton to verified v2.1.3 state after publication
- R4 | bin: over-worry | evidence: strong | ref: v2.1.2..HEAD and 3c2516b9..b618c784 | action: document | note: content and implementation proof ranges intentionally serve different consumers
- R5 | bin: valid-but-defer | evidence: strong | ref: .agents/release-adapter.yaml | action: defer | note: no configured real-host trigger matched this release delta

## Deliberately Not Doing

No compatibility shim, new migration, wall-clock speed claim, or unconfigured real-host exercise is added. The focused selector and dead-code inventory remain advisory.

## Reviewer Tier Evidence

- Requested tier: high-leverage
- Requested spawn fields: `model=gpt-5.6-terra`, `reasoning_effort=medium`, `service_tier=priority`, `fork_turns=none`
- Host exposure state: requested_fields_sent
- Application state: host accepted the fields but did not independently expose provider-application confirmation.

## Fresh-Eye Satisfaction

parent-delegated

## Boundary Ownership

- Producer: release helper state, configured probes, public verifier, install refresh, and post-refresh readbacks.
- Consumer: maintainer deciding whether v2.1.3 is publicly and locally usable.
- Owning surface: release ledger and post-publish baton; quality artifacts retain implementation proof.
- Verdict: owned-correctly

## Operator Action Required

Commit the review/notes, run dry-run then execute, verify public release through the distinct channel, run update, read back installed version/doctor/cache, then reconcile and push the handoff.

## Upgrade Path

Run `charness update`; start a new host session when it needs the refreshed plugin. No migration is required. Use structured rollback/quarantine evidence before commit and `--resume --publish-current` after a release commit exists.

## Next Move

Commit the release critique and notes, prove a clean worktree, then use the repo-owned publish helper with the notes and this critique artifact.
