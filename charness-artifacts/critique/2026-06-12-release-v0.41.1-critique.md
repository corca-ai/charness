# Release Critique: v0.41.1
Created: 2026-06-12
Target: `release-critique`
Fresh-Eye Satisfaction: parent-delegated
Packet Consumed: `charness-artifacts/critique/2026-06-12-071941-packet.md`

## Release Scope

- version: `0.41.1`
- tag: `v0.41.1`
- consumer change: patch release for quality workflow hardening from the
  2026-06-12 quality cadence follow-up: validator-backed goal closeout
  authoring, explicit slice-vs-bundle validation cadence, and reduced
  repo-owned adapter scalar helper duplication.

## Surface-Lock Inventory

- `.agents/release-adapter.yaml` update instructions for `0.41.1`.
- Packaging and generated plugin manifests, to be bumped and synced by
  `publish_release.py`.
- `charness-artifacts/release/latest.md`, to be written by
  `publish_release.py`.
- Fresh-checkout probes declared in `.agents/release-adapter.yaml`.
- Goal closeout authoring guidance for the repo-local validator helper:
  `python3 scripts/check_artifact_surface_preflight.py --type goal-closeout --emit-stub`.
- Installed-plugin operator update path: `charness update`.

## Angles

- Atul Gawande / operational checklist: checked durable critique, clean-tree
  preconditions, helper-owned version bump/sync/tag, release record refresh,
  fresh-checkout probes, and public-vs-real-host boundary.
- Barbara Minto / communication structure: checked patch-vs-minor framing,
  cold-reader release story, duplicate-cleanup scope, and rollback wording.
- Jef Raskin / humane interface: checked that the release note names the
  usable helper path without implying a new `charness goal` subcommand.
- Counterweight: separated actual publish blockers from expected pre-helper
  state and deferred non-blocking cleanup.

## Reviewer Tier Evidence

<!-- allowed Host exposure state: pending-parent-spawn | requested_fields_sent | metadata-hidden | host-defaulted | unsupported | applied. Use applied only with Application state: host-confirmed: plus a concrete signal. -->

- Requested tier: high-leverage
- Requested spawn fields: model=gpt-5.5, reasoning_effort=medium, service_tier=priority
- Host exposure state: host-defaulted
- Application state: host returned agent IDs `019ebab3-a2b7-7990-89e6-2015ef1fb599`, `019ebab3-c719-7470-bc54-5c704219175c`, `019ebab3-e7b5-7581-a2d7-1bbb593eccb1`, and `019ebab6-7bba-70c0-8f79-7a357acdab50`; parent used host-default model selection and no provider-side application confirmation was exposed.

## Findings

### Act Before Ship

- Persist a standalone tracked release critique artifact before publish and
  pass it to the helper with `--critique-artifact`.
  Resolution: this artifact is the final critique record and will be committed
  before `publish_release.py --execute`.
- Start the publish helper from a clean tree.
  Resolution: commit the updated release adapter instruction and the critique
  packet/artifact before running the helper.
- Do not tag manually while packaging/plugin surfaces still report `0.41.0`.
  Resolution: use `publish_release.py --part patch`, which owns version bump,
  manifest sync, `latest.md`, fresh-checkout probes, tag push, GitHub release
  creation, and public verification.

### Bundle Anyway

- Keep the tightened `0.41.1` update instruction: it separates validator-backed
  closeout from inert draft shaping, names the repo-local helper path, says it
  is not a new `charness goal` subcommand, and records rollback guidance.
- Keep focused adapter proof in the closeout bundle:
  `python3 scripts/validate_adapters.py --repo-root .`.
- Record fresh-checkout probes as already passed parent-side, and let the
  release helper run/record them again at the release boundary.

### Over-Worry

- Packaging/plugin surfaces remaining at `0.41.0` before the helper is expected
  pre-helper state.
- No CLI docs update is needed for `charness goal`; the release note correctly
  frames the stub affordance as a repo-local validator helper, not a new
  maintained subcommand.
- The duplicate cleanup is scoped enough for a patch: one adapter scalar-helper
  family, with explicit non-claims about broader clone health.
- Real-host proof was not triggered by the current adapter-only release-note
  slice; the helper/release record should still report any release-boundary
  obligations honestly.

### Valid But Defer

- Broader release-note density cleanup can wait; the operator action is clear
  enough for `0.41.1`.
- Remaining portable-skill boilerplate clone families are real future cleanup
  candidates, but outside this patch release.
- Public release verification and maintainer install refresh remain
  release-boundary obligations after publish and should be recorded by the
  helper.

## Operator Action Required

- Commit this critique, the critique packet, and the updated release adapter
  note.
- Run the release helper with this critique artifact:
  `python3 /home/hwidong/.codex/plugins/cache/local/charness/0.41.0/skills/release/scripts/publish_release.py --repo-root . --part patch --critique-artifact charness-artifacts/critique/2026-06-12-release-v0.41.1-critique.md --execute`.
- Do not hand-edit generated manifests, tags, or `charness-artifacts/release/latest.md`.

## Upgrade Path

Operators run `charness update` after the release is published. No migration,
data rewrite, or command rename is required. Rollback is reinstalling or
pinning `0.41.0`.
