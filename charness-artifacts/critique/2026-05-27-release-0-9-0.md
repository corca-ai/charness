# Release Critique — charness 0.9.0

- **Execution**: executed (bounded fresh-eye subagent)
- **Fresh-Eye Satisfaction**: parent-delegated
- **Target**: `references/release-critique.md` (release-surface lock)

## Release

Minor bump 0.8.0 -> 0.9.0 publishing origin/main HEAD (d1a2c89a): first public
shipment of the `achieve` goal-lifecycle skill, the #223 quality/achieve
duplicate-test-pressure contract, test-regression fixes, and a new
`local-linux-aarch64-4cpu` runtime budget profile.

## Findings / Counterweight Triage

- **Bump level (Over-Worry to ask major)**: `achieve` is purely additive new
  public surface; #223 adds an optional `--test-pressure` slice field and flag,
  no removed/renamed invocation or manifest schema. Minor is correct.
- **Generated-surface coverage (Act Before Ship — post-bump verify)**: all five
  version-bearing surfaces are uniformly 0.8.0 with empty drift. After the bump,
  re-run `current_release.py` and confirm all read 0.9.0 with `drift: []` (two
  occurrences each in packaging manifest and the claude marketplace; codex
  marketplace is path-pinned and inherits).
- **Public-visibility readiness (Bundle Anyway — ready)**: achieve SKILL.md has
  valid frontmatter, all references resolve, bootstrap paths correct, source
  matches checked-in plugin copy, no compiled artifacts shipped.
- **Publish boundary (Over-Worry)**: publish is synchronous via the repo helper
  (no async release workflow); tag/release existence is probed separately, so
  push-only/tag-only states are distinguishable.
- **#223 closeout coupling (Over-Worry)**: #223 is already CLOSED; do NOT pass
  `--close-issue 223` (the helper would no-op anyway, but skip it).

## Deliberately Not Doing

- Not deferring the two quality defects surfaced this session (xdist parallel
  test-isolation flakiness + cautilus version-gating); filed as #225 instead of
  blocking this release.

## Next Move / Verdict

0.9.0 minor is safe to cut now. Gating action: after bump, re-run
`current_release.py` and confirm all five surfaces are 0.9.0 with empty drift
before tag/publish.
