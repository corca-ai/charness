# Critique Review
Date: 2026-06-09

## Decision Under Review

Standalone release critique for **charness v0.34.0** (minor bump 0.33.0 → 0.34.0).
The release bundles two additive capabilities + a verification + an inert draft
goal over base `81f2e1ab`: (1) #N-anchor edit-time guard (`skill_issue_anchor_scan.py`
+ `check_skill_surface_preflight.py --scan-issue-anchors`); (2) #338 gather
X/Twitter exact-source (`twitter_exact_source.py` + `acquire`/`gather` wiring,
`source_identity`, `Closes #338`); (3) slice-3 verification + stale-handoff
correction (no behavior change); (4) an inert `Status: draft` next-queue goal.

## Failure Angles

- Wrong bump level (a compatibility/invocation break hiding under "additive" →
  should be major).
- `update_instructions` overclaim or omission of an operator-visible change.
- Dishonest/double #338 closure, or acceptance not actually met.
- Generated-surface drift (packaging vs plugin mirrors disagree) or an
  uncommitted skill-script making the bump produce an inconsistent surface.
- A NEW blocking behavior inherited by installed-plugin consumers.

## Counterweight Pass

- All five angles checked and cleared by the fresh-eye reviewer: no deletions/
  renames over the range; gather CLI preserves every existing arg (only adds two
  opt-in flags + a defaulted `source_identity`); the gather change and
  `source_identity` are gated to `route_id == "twitter-syndication"` only (other
  sources byte-untouched); the #N-anchor scan runs only behind `--scan-issue-anchors`
  (commit sweep unchanged); mirrors uniformly at 0.33.0 with no staged drift.
- #338 is OPEN now (no premature close); `Closes #338` appears once (`40e492ed`);
  `--close-issue 338` is belt-and-suspenders (GitHub dedups).

## Structured Findings

<!-- allowed enums (substitute only these) — bin: act-before-ship | bundle-anyway | over-worry | valid-but-defer; evidence: strong | moderate | weak | contested; action: fix | file-issue | document | defer. action: file-issue also needs a follow-up: (issue URL or 'deferred ' plus a handoff anchor). -->
- F1 | bin: over-worry | evidence: strong | ref: .agents/release-adapter.yaml:34 | action: document | note: the new 0.34.0 update_instructions entry was cross-checked against the actual commits — accurate, no overclaim/omission; minor bump is the lightest honest level.
- F2 | bin: over-worry | evidence: strong | ref: skills/support/web-fetch/scripts/acquire_public_url.py | action: document | note: double-close (commit trailer + --close-issue 338) is a GitHub dedup, not a hazard; #338 acceptance (mocked captcha test, identity-keyed fallback, proof-before-original, visible trace, answer-path distinction) maps to what shipped.

## Reviewer Tier Evidence

<!-- allowed Host exposure state: pending-parent-spawn | requested_fields_sent | metadata-hidden | host-defaulted | unsupported | applied. Use applied only with Application state: host-confirmed: plus a concrete signal. -->
- Requested tier: bounded fresh-eye standalone `critique` subagent (general-purpose), read-only in the shared parent worktree.
- Requested spawn fields: the release facts (0.33.0→0.34.0 minor, the 4 bundled items) and six release-specific risks to verify (bump level, update_instructions accuracy, #338 closure, drift readiness, operator risk, draft inertness).
- Host exposure state: applied
- Application state: host-confirmed: subagent a6b252b7591d8edfa ran (20 tool uses), verified all six risks against the diff/commits/mirrors, and returned SHIP with no blockers (four-bin triage: 0 act-before-ship, 0 bundle-anyway).

## Fresh-Eye Satisfaction

tool signal: bounded standalone-critique subagent a6b252b7591d8edfa ran read-only
(20 tool uses) and returned SHIP for v0.34.0, confirming the minor bump is the
lightest honest level, the update_instructions are accurate, #338's closure is
honest (open now, single `Closes` carrier, acceptance met), the generated surface
is clean (mirrors at 0.33.0, no staged drift, doctor 16/16), no installed-plugin
consumer inherits new blocking behavior (both changes additive/route-gated/opt-in),
and the shipped draft goal is genuinely inert. No blockers.
