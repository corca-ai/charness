# v0.7.8 Release Risk Check

Date: 2026-05-21
Scope: publish the #183 mutation-testability repair as `charness` patch release
`v0.7.8` after hosted Mutation Tests succeeded on `5666571`.
Fresh-Eye Satisfaction: parent-delegated release critique by a separate agent.

## Act Before Ship

- Do not claim release-time real-host proof is unneeded from a clean worktree
  check alone. The publish helper must evaluate the full `v0.7.7..HEAD` delta,
  and that delta includes control-plane paths that match the configured
  real-host checklist. This release is acceptable only if the generated release
  artifact records that checklist instead of implying local CI replaced it.

## Bundle Anyway

- Patch bump is the correct version level. The delta fixes mutation sampling,
  scoring closeout semantics, public quality workflow propagation, and focused
  tests exposed by the bounded mutation run.
- Hosted Mutation Tests run `26196843109` passed on
  `5666571bce0f9544b0128e41a78380b007d29598`, and #183 is verified `CLOSED`.
- `v0.7.8` does not exist before publish, and current release surfaces agree on
  `0.7.7` with no drift.

## Valid But Defer

- The release adapter still has no executable `requested_review_commands`.
  This release carries an explicit tracked critique artifact; future releases
  should prefer an executable review gate or an explicit waiver path.
- No fresh-checkout probes are configured. The release artifact must say that
  directly.

## Over-Worry

- Do not use `--close-issue 183` as the release close carrier. The hosted
  mutation workflow already closed and verified the issue; the release only
  ships the fix to plugin users.

## Decision

Proceed with `v0.7.8` through the repo-owned publish helper. The release record
must include public release verification and must carry the real-host checklist
as required for the actual tag delta.
