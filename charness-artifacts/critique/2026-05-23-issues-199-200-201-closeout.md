# Critique Closeout: Issues #199 / #200 / #201

Date: 2026-05-23

## Execution

Three sequential bounded fresh-eye subagent passes plus a similar-pattern scan
plus an RCA pass. All passes ran as parallel subagents spawned per
`AGENTS.md` `Subagent Delegation` (parent-delegated).

- Design critique (`general-purpose` subagent) ran on the issue proposals
  before any code landed and identified that #199 and #201 mostly re-asked
  for already-shipped contracts; #200 was the only genuinely missing rule.
- Pattern scan (`Explore` subagent) surfaced sibling gaps in `retro`,
  `create-cli`, and `ideation` plus a `create-skill` meta-rule candidate.
- RCA (`general-purpose` subagent) named the unifying root cause: skills
  declare structured Output Shape fields but ship no validator, so the
  caller re-derives compliance every time.
- Post-impl critique surfaced 11 findings; 7 act-before-ship (substring
  false-fire, case sensitivity, em-dash mismatch, ruff bare-table false
  positive, missing test coverage, weak ValidationError message,
  cross-skill grammar drift) were fixed in-slice.
- Closeout critique (a third fresh-eye pass on the tightened diff) found
  one residual ship-blocker: `follow-up: deferred` with no anchor passed
  the validator. Fixed in `_is_valid_followup_tail`.

## Fresh-Eye Satisfaction

parent-delegated

## Packet Consumed

n/a (no adapter sections)

## Target

`references/code-critique.md` substrate (post-impl review of a non-trivial
public-skill + validator change).

## Change

- `skills/public/debug/`: sibling-search escalation rule for the
  `valid follow-up outside the slice` decision; validator enforces a
  `follow-up:` identifier with `deferred` requiring an anchor.
- `skills/public/impl/`: `survey_verification.py` probes for a standing
  lint gate; `verification-ladder.md` owns the `Lint Gate` closeout shape;
  `SKILL.md` adds the `Lint Gate` Output Shape field.
- `skills/public/critique/`: `counterweight-triage.md` defines an opt-in
  `## Structured Findings` per-finding schema reusing the existing four
  bins and four evidence tags; `validate_critique_artifacts.py` enforces
  enum values when the section is present.

## Findings

Three layers of critique surfaced 11 + 1 findings total. The two structural
themes were (a) substring matchers that need bullet + token gating to avoid
prose false-positives, and (b) probe sharpness for adapter-resolved
detection (bare `[tool.ruff]` vs `[tool.ruff.lint]`).

## Counterweight Triage

`act-before-ship` findings collapsed into validator hardening and detection
sharpening; both landed inside the slice. `bundle-anyway` findings were
cross-skill grammar reciprocation, which also landed in-slice. `over-worry`
findings (e.g., Deno detection) were deferred with a documented note.
`valid-but-defer` findings (sibling Pattern A retro, Pattern B create-cli,
Pattern C ideation, create-skill `Closeout Schema Rule` meta) are
out-of-slice for #199/#200/#201 closure and will be filed as separate
GitHub issues with `follow-up:` identifiers per the new sibling-search
discipline.

## Structured Findings

- F1 | bin: act-before-ship | evidence: strong | ref: scripts/validate_debug_artifact.py:140-150 | action: fix | note: bare `follow-up: deferred` token passed validator; fixed via `_is_valid_followup_tail` requiring anchor after `deferred`
- F2 | bin: act-before-ship | evidence: strong | ref: scripts/validate_debug_artifact.py:96-100 | action: fix | note: substring match false-fired on prose; fixed by gating on leading `- ` + `decision:` token
- F3 | bin: act-before-ship | evidence: strong | ref: scripts/validate_debug_artifact.py:128 | action: fix | note: case-sensitive match bypassed on title-case; fixed via lowercase compare
- F4 | bin: act-before-ship | evidence: strong | ref: skills/public/impl/scripts/survey_verification.py:104-118 | action: fix | note: bare `[tool.ruff]` table false-surfaced as lint gate; fixed by requiring `[tool.ruff.lint` subtable
- F5 | bin: act-before-ship | evidence: strong | ref: tests/test_debug_artifact.py | action: fix | note: 4 new edge-case tests (prose mention, title-case, em-dash short-circuit, mixed bullets) + 3 deferred-anchor tests landed
- F6 | bin: act-before-ship | evidence: strong | ref: tests/test_impl_survey_verification.py | action: fix | note: new file with 11 detect_lint_gate tests covering probe order, false positives, empty repo, runner detection
- F7 | bin: bundle-anyway | evidence: moderate | ref: skills/public/impl/references/verification-ladder.md:90-99 | action: fix | note: cross-skill grammar reciprocation added (sibling-search ↔ counterweight-triage ↔ verification-ladder)
- F8 | bin: valid-but-defer | evidence: moderate | ref: charness-artifacts/retro/lesson-selection-index.json | action: file-issue | follow-up: https://github.com/corca-ai/charness/issues/203 | note: retro lacks waste-pattern sibling scan (Pattern A sibling)
- F9 | bin: valid-but-defer | evidence: moderate | ref: skills/public/create-cli/SKILL.md:121-147 | action: file-issue | follow-up: https://github.com/corca-ai/charness/issues/204 | note: create-cli should adopt lint-gate-before-complete (Pattern B sibling)
- F10 | bin: valid-but-defer | evidence: weak | ref: skills/public/ideation/SKILL.md:116-134 | action: file-issue | follow-up: https://github.com/corca-ai/charness/issues/205 | note: ideation should classify Open Questions by urgency (Pattern C sibling)
- F11 | bin: valid-but-defer | evidence: strong | ref: skills/public/create-skill/references/portable-authoring.md | action: file-issue | follow-up: https://github.com/corca-ai/charness/issues/206 | note: meta-rule per RCA — skills with structured Output Shape fields should ship a closeout validator
- F12 | bin: over-worry | evidence: weak | ref: skills/public/impl/scripts/survey_verification.py:163-175 | action: document | note: Deno (deno.json/deno.lock) not probed; defer until a Deno repo signal arrives

## Sibling Search

This is a critique artifact, not a debug artifact, so a `## Sibling Search`
section is not required by the artifact validator. It is added voluntarily
to dogfood the cross-skill schema reciprocation introduced in this slice
(`debug/references/sibling-search.md` ↔
`critique/references/counterweight-triage.md` ↔
`impl/references/verification-ladder.md`) — the same four-axis taxonomy
applies to lessons emitted from a critique that crosses skill contracts.

- Mental model: skills declare structured Output Shape fields the caller
  is expected to act on, but ship no validator, so the caller re-derives
  compliance every time.
- same layer: skills/public/retro/SKILL.md emits a per-session lesson
  without sibling-scan over the named waste pattern | decision: valid
  follow-up outside the slice | proof: static scan only | follow-up: https://github.com/corca-ai/charness/issues/203
- abstraction up: skills/public/create-skill/references/portable-authoring.md
  has no "Closeout Schema Rule" enforcing validators for structured Output
  Shape fields | decision: valid follow-up outside the slice | proof:
  static scan only | follow-up: https://github.com/corca-ai/charness/issues/206
- specialization down: skills/public/create-cli/SKILL.md slice-complete
  contract does not run the repo's lint gate on the created CLI |
  decision: valid follow-up outside the slice | proof: static scan only |
  follow-up: https://github.com/corca-ai/charness/issues/204
- mental-model siblings: skills/public/ideation/SKILL.md Output Shape
  emits unclassified `Open Questions` forcing the downstream `spec`/`impl`
  skill to re-triage | decision: valid follow-up outside the slice |
  proof: static scan only | follow-up: https://github.com/corca-ai/charness/issues/205

The over-reach check is satisfied: each sibling shares the abstracted
pattern (output without enforcement, or output without classification) and
was filed via the `issue` skill against a named SKILL.md location, not a
keyword match.

## Deliberately Not Doing

- The proposed `--phase design|impl|final` flag on `critique` (#201): rejected as
  reinvention of the existing target-reference selection (`code-critique.md`,
  `spec-critique.md`, `release-critique.md`, `premortem-decision.md`,
  `rename-critique.md`). The orchestrator's three-pass cycle maps to those
  references; adding a parallel flag taxonomy would confuse callers.
- Invariant-naming as a separate ceremonial step in `debug` (#199):
  rejected as gold-plating. The existing `Mental model:` bullet in
  `sibling-search.md` already names the invariant.
- A new `validate_impl_artifact.py` to enforce `Lint Gate` field presence
  on disk (#200): deferred because no impl-artifact directory exists today;
  reconsider if `charness-artifacts/impl/` ever lands.
- Hard-blocking commit on lint-gate failure: rejected. The skill discipline
  is disclosure-via-`Lint Gate` field; the repo's pre-push hook (lefthook
  or `.githooks/pre-push`) owns hard-block.

## Next Move

- Commit landed as `a689024` (auto-closes #199 #200 #201 on push).
- 4 follow-up issues filed: #203 (retro Pattern A), #204 (create-cli
  Pattern B), #205 (ideation Pattern C), #206 (create-skill meta-rule).
  Each carries `follow-up:` back to this artifact per the new
  sibling-search discipline (dogfood).
- Push and handoff update deferred to user decision.
