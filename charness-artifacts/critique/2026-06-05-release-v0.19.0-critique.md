# Release Critique: v0.19.0

Execution: parent-delegated fresh-eye release critique, medium effort.

Fresh-Eye Satisfaction: parent-delegated.

Packet Consumed: n/a (bounded release-state packet in parent prompt).

Target: release-critique.

## Reviewer Tier Evidence

- Requested tier: medium
- Requested spawn fields: agent_type=explorer, reasoning_effort=medium
- Host exposure state: metadata-hidden
- Application state: requested-fields-sent; host did not expose provider application confirmation

Change: publish the seven commits after v0.18.0 so installed Charness users can consume the updated `achieve` timebox continuation contract and the quality test-economics cleanup.

Findings:

- Act Before Ship: release level should be minor, not patch. The delta adds public `achieve` timebox behavior and complete-status enforcement while preserving non-timeboxed goals. Target set to `0.19.0`.
- Act Before Ship: version and operator update surfaces still named `0.18.0` before release mutation. Fix by updating the release adapter instructions before running the checked-in publish helper.
- Act Before Ship: the release narrative must be regenerated for `v0.19.0`; `charness-artifacts/release/latest.md` still named `v0.18.0` before publish. The publish helper owns regenerating and auditing it.
- Bundle Anyway: fresh-checkout probes passed before the bump, but must run again after generated version surfaces are written. The publish helper reruns them after the release commit.
- Bundle Anyway: real-host proof is not configured for this delta because the changed paths are public/bundled skill and tests, not the integration/install globs.

Counterweight Triage:

- Minor version is warranted by new public behavior, but not major because existing non-timeboxed goals pass through and no invocation compatibility breaks.
- The old `nose` real-host checklist should not be carried as a blocking proof for this release; it is a prior integration surface and no configured trigger matched the current delta.
- Use the repo-owned helper at `skills/public/release/scripts/publish_release.py`, not an installed plugin-cache helper.

Deliberately Not Doing:

- No issue-closeout release carrier is attached; this release is for already-committed skill/operator improvements, not a tracked issue closure.
- No real-host install proof is claimed for `nose` in this release.

Next Move: run `publish_release.py --part minor --critique-artifact charness-artifacts/critique/2026-06-05-release-v0.19.0-critique.md --execute` after the prep commit.
