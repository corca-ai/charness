# Release Critique — v0.22.0 (#302–#305)

- **Execution**: bounded fresh-eye subagents (3 angle + 1 counterweight), parent-delegated under `AGENTS.md` Subagent Delegation
- **Fresh-Eye Satisfaction**: parent-delegated
- **Packet Consumed**: `charness-artifacts/critique/2026-06-05-111217-packet.md` (note: changed-surfaces section rendered "clean working tree" because the release content is already committed; reviewers inspected `origin/main..HEAD` read-only instead)
- **Target**: `references/release-critique.md`

## Release Scope

- **Current → Target**: 0.21.0 → 0.22.0 (**minor**)
- **Why minor**: exactly one additive `feat` (#303 `setup` generates an adapter-first subagent reviewer rule in AGENTS.md); the rest are fixes (#302, #304, #305). One additive operator-facing capability → minor per `references/version-policy.md`. Counterweight confirmed.
- **Consumer one-liner**: gather agent-browser sessions now always close and prove a clean runtime; `release` publish is resumable and stale-update-step-checked; `setup` generates an adapter-first reviewer rule and agrees its compact delegation contract across line wraps.
- Closes GitHub issues #302, #303, #304, #305.

## Surface-Lock Inventory

- **Generated artifacts**: `packaging/charness.json`, Claude plugin manifest, Codex plugin manifest, marketplace version → all bump 0.21.0 → 0.22.0 (via `sync_root_plugin_manifests.py`; derived, not hand-edited).
- **Consumer-visible behavior**:
  - `release` publish helper: new `--resume` flag, installed-cache-safe bootstrap, unconditional stale-`update_instructions` preflight blocker (#305).
  - `setup`: generates adapter-first subagent reviewer rule into greenfield AGENTS.md (#303); compact delegation block agrees across line wraps so the setup inspector stops rejecting its own output (#304).
  - `gather`/web-fetch: agent-browser session close guarantee + fail-visible clean-runtime proof; runtime guard now also counts reparented + zombie residue (#302).
- **Documentation surfaces**: `.agents/release-adapter.yaml` `update_instructions` (rewritten for 0.22.0), regenerated `charness-artifacts/release/latest.md`, retro/goal artifacts.
- **Source/plugin parity**: `skills/public/**` and `plugins/charness/**` copies verified byte-identical across release, web-fetch, and setup scripts (`diff -rq` clean).

## Angles

- **Atul Gawande (operational/checklist)** — pipeline order, manifest sync, two-copy parity, issue-close half-finish risk, newly-changed release-flow code.
- **Barbara Minto (structure/communication)** — release-note legibility, process-commit noise, durable-record accuracy, minor-bump legibility.
- **Jef Raskin (humane interface)** — operator-facing surfaces: setup-generated AGENTS.md rule, `--resume`/staleness error wording, runtime-guard remediation guidance.

## Findings → Counterweight Triage (four bins)

### Act Before Ship (cleared as part of this release prep)

- **A. Stale `update_instructions`** — `.agents/release-adapter.yaml` still described 0.21.0/nose-0.5 with zero #302–#305 content; the new #305 staleness guard hard-blocks publish at plan time. **Action taken**: rewrote `update_instructions` to the 0.22.0 story (includes the literal `0.22.0`). *The release was caught by the very guard it ships — guard working as designed.*
- **B. Untracked critique packet + tracked-`.md` rule** — the generated packet files trip the clean-worktree precondition, and `--critique-artifact` must be git-tracked. **Action taken**: commit this artifact + packet in the release-prep commit before invoking publish.
- **C. Process-commit noise in auto-notes** — without `--notes-file`, `--generate-notes` would surface 4 process commits (`goal(draft)`, `chore(goal)`×2, `test`). **Action taken**: author a curated `--notes-file` with the 4 consumer entries.
- **D. Minor-bump legibility** — folded into C: the curated notes lead with the #303 feature so the minor bump reads as obvious. No independent cost.

### Valid but Defer (ship as-is; follow-up issues)

- **E. Runtime-guard cleanup dead-end** — `cleanup_orphans` targets only `orphan_tree_pids`, but `runtime_residue_total` now also counts reparented + zombie residue and emits `next_step: CLEANUP_COMMAND`; for pure reparented/zombie residue the suggested command clears nothing (a humane no-exit loop). NEW in this delta, but non-blocking and the stricter detection is itself the #302 win; reparented/zombie residue is genuinely the container init's job to reap. → filed **#309**.
- **F. `acquire_public_url.py` error clobber** — on `cleanup_error` the last attempt's `.error` is overwritten, losing the original acquisition reason. PRE-EXISTING (identical at `origin/main`), merely relocated into the extracted browser stage. → filed **#310**.
- **G. Reviewer rule not backfilled** — the new AGENTS.md reviewer block is written only for greenfield; existing consumers re-running `setup` won't get it, and the inspector won't flag it stale. Matches the established template-injection design. → filed **#311**.

### Over-Worry (dropped)

- **H. `--resume` help omits dry-run note** — the global `--execute` help already states the no-`--execute` dry-run behavior; duplicating it is cosmetic. Dropped.

## Operator Action Required (Act Before Ship)

1. Rewrite `.agents/release-adapter.yaml` `update_instructions` to 0.22.0 content (done).
2. Commit critique artifact + packet so the worktree is clean and the artifact is tracked (done in release-prep commit).
3. Pass a curated `--notes-file` leading with the #303 feature (done).

## Upgrade Path

Operators refresh via `charness update`; restart Claude Code / Codex if the host cache still shows 0.21.0. No manual migration. Existing repos that already have an AGENTS.md will NOT auto-receive the new adapter-first reviewer rule (finding G) — re-running `setup` on an existing AGENTS.md is a no-op for that block; backfill is a deferred follow-up.

## Deliberately Not Doing

- Not fixing E/F/G in this release: E is a non-blocking remediation-guidance wart on a net-positive guard, F is pre-existing behavior merely relocated, G matches the existing greenfield-only template design. All three are filed as follow-up issues rather than expanding the release scope.
- Not dropping to a same-agent review: the canonical bounded fresh-eye path ran.

## Next Move

Commit the release-prep (adapter rewrite + this artifact + packet), then run `publish_release.py --part minor --critique-artifact <this file> --notes-file <curated> --close-issue 302..305 --execute`. File follow-ups for E/F/G at closeout.

## Release Outcome (2026-06-05)

Shipped v0.22.0 (tag `v0.22.0` at `a50cf8b3`, release live, #302–#305 closed via
push close-keywords). The publish surfaced two non-critique issues handled during
the run:

- The stricter `--release` gate caught **pre-existing committed debt** in the
  #302–#305 work (issue anchors in portable skill scripts; 3 debug artifacts in a
  non-canonical structure; stale debug seam-risk index). Fixed in commit
  `f17e3e8e`; full `--release` gate then passed 72/0.
- The `publish_release.py` happy-path run wraps quality in `run_slice_closeout`,
  which writes live usage episodes and raced `test_session_capture..._round_trip`
  (issue #194 state-bleed class); `--resume` then left an uncommitted `latest.md`
  retro-payload regression that the `.githooks/pre-push` clean-`charness-artifacts`
  check (correctly) blocked. Completed via the repo push path directly
  (`git push origin main v0.22.0` → pre-push gate 71/0 → `gh release create`).
  Worth a follow-up: the resume flow should commit its refreshed `latest.md`
  before pushing, and/or the closeout-wrapped quality run should not write live
  usage episodes during the suite.

## Reviewer-Tier Evidence

- **Requested tier**: `high-leverage` (`.agents/critique-adapter.yaml` → `model=gpt-5.5, reasoning_effort=medium, service_tier=priority`).
- **Host exposure state**: `host-defaulted` — this Claude Code session does not expose a `gpt-5.5` agent type; bounded reviewers spawned with the host default reviewer model. Requested spawn fields were NOT sent to a matching provider.
- **Application state**: not applied (host cannot honor the provider-specific tier). The tier is a request, not a hard requirement, so this is not a blocked state.
