# Release Critique — v0.27.0

Date: 2026-06-07
Scope: minor release v0.26.0 → v0.27.0 (bump + tag + push + GitHub release of
already-merged, already-pushed work).
Reviewer provenance: bounded fresh-eye subagent (independent context, read-only in
the shared parent worktree), per the repo's release fresh-eye delegation.

## Bundle (commits since tag v0.26.0)

- `feat(quality,find-skills)` advisory-interpretation contract rollout to six
  inference-layer surfaces (#322, CLOSED; already independently fresh-eye
  critiqued + pushed).
- `feat(quality)` provenance-placement policy + portable standing-doc check
  (#325) and a follow-up gitignore-aware fix to `standing_doc_provenance_lib.py`.
- `feat(quality)` portable changed-line coverage gate (handoff-3).
- supporting `chore`/`retro` commits.

## Findings (no blockers)

- **A. Bump level — minor is correct.** The range adds new maintained quality
  capabilities, all backward-compatible. Not patch (real new capability), not
  major (no break).
- **B. No major-warranting change / no must-not-ship.** No `BREAKING`/`type!:`
  markers. Both new gates are **opt-in/inert by default** (`eligible_globs: []`,
  `standing_docs: []`) — a consuming repo running `charness update` inherits no
  newly-enforced gate. `check_python_lengths.py` change is purely additive (new
  `interpretation` field + `ADVISORY:` line; exit codes, hard limits, CLI
  unchanged). The `fix` hardens the brand-new #325 scanner, not a shipped
  regression.
- **C. No generated-surface drift.** Per-file parity of every changed
  `skills/public/*` and `scripts/*` path against its `plugins/charness/*` mirror:
  zero tracked diffs (only untracked `__pycache__` + an unchanged source-only
  `.gitkeep`). Bundle is merged-and-pushed only (HEAD == origin/main); the
  release introduces no unreviewed new code.
- **D. No operator/install/compat risk.** No install/update/CLI/launcher/
  marketplace/packaging surface changed in the range — confirms
  `check_real_host_proof.py = not required` against the actual changed paths. No
  step beyond `charness update`; new gates stay dormant unless opted in.

### NIT (carried into closeout)

`charness-artifacts/release/latest.md` still documents v0.26.0 (expected pre-bump
state). The publish helper rewrites it during the cut — closeout confirms the
committed `latest.md` names v0.27.0 before tag push (audited by
`audit_public_release_narrative.py`).

## Verdict

Minor v0.27.0 is the honest bump. No blockers. Safe to bump, tag, and publish.
