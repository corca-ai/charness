# v0.30.0 release critique
Date: 2026-06-08

## Decision Under Review
Cut charness 0.29.0 → 0.30.0 (minor): ship the workflow-ergonomics bundle (8 commits `cead7949..117c8d20`), bump/push/tag, create the GitHub release, and close #336 via `--close-issue 336`.

## Failure Angles
- Bump level wrong: a hidden breaking change (removed/renamed public symbol or CLI arg) would force `major`; a pure bugfix-only set would warrant `patch`.
- update_instructions inaccuracy: the new 0.30.0 entry could overclaim behavior change on additive/inert work, omit the `0.30.0` token (tripping the staleness guard at the critique gate), or misdescribe one of the three slices.
- Wrong/premature issue closure: #336 might already be closed, or the fix might not actually be in this range, or `b1dd7e16` might not carry the `Close #336` trailer — any of which corrupts the closeout.
- Publish-boundary leakage: the #337 draft goal artifact could be active/pursue-armed and auto-execute, or a stale `latest.md` / generated-surface drift / mixed-concern artifact could ride along.
- Adapter unparseable: the long new update_instructions list item could break YAML loading and fail the release loader.
- Honesty: a provider/live proof claimed where only local ran, or a dishonest closeout-state taxonomy.

## Counterweight Pass
- Adapter-parse worry is over-worry: a naive `yaml.safe_load` choked on an unquoted mid-string colon, but the repo's own `load_adapter` returns `valid: True, errors: []` — the real release path parses it cleanly. Not a blocker.
- The uncommitted `M .agents/release-adapter.yaml` (the 0.30.0 entry) is expected pending state — the release flow stages the bump + update_instructions together; it is correctly NOT yet on HEAD. Not a blocker, but the release must commit it with the bump (it owns that).
- Breaking-change worry is over-worry: the only removed symbol is `def main()`, re-added in the same diff (body relocated to `execute_publish_plan`, `main()` kept as dispatcher); zero CLI args removed. Behavior-preserving refactor, additive `--prep-update-instructions` only. Minor is honest.
- Real, satisfied blockers: #336 OPEN now (closes on push), fix in range (`cead7949` Slice 1), `b1dd7e16` carries `Close #336`. Draft goal is `Status: draft`, "active only when the user runs `/goal`", no slices — inert.

## Structured Findings
- F1 | bin: bundle-anyway | evidence: strong | ref: git diff v0.29.0..HEAD --stat | action: defer | note: No breaking change — only `def main()` removed-then-re-added as a dispatcher; no public CLI arg removed. `minor` (0.30.0) is the lightest honest bump (additive contract rule + author-time legend + new read-only CLI mode); not patch (no bugfix-only), not major.
- F2 | bin: bundle-anyway | evidence: strong | ref: .agents/release-adapter.yaml | action: defer | note: The 0.30.0 update_instructions entry accurately describes all three slices, mentions `0.30.0`, and flags each as additive/behavior-preserving with no overclaim (e.g. "drafting already left the slot empty on Claude; the new acceptance criterion documents existing behavior"). Cross-checked against `cead7949`/`a101e716`/`138366c1`.
- F3 | bin: bundle-anyway | evidence: strong | ref: skills/public/release/scripts/publish_release_preflight.py | action: defer | note: Staleness guard verified via the real loader: `update_instructions_version_blocker(ui, target='0.30.0', previous='0.29.0')` returns `None` (no HOLD); adapter loads `valid: True, errors: []`.
- F4 | bin: bundle-anyway | evidence: strong | ref: b1dd7e16 | action: defer | note: #336 is currently OPEN (`gh issue view 336` → state OPEN), the fix is in range (Slice 1 `cead7949`), and `b1dd7e16` carries `Close #336` in its trailer; closing via release is correct.
- F5 | bin: over-worry | evidence: strong | ref: charness-artifacts/goals/2026-06-08-retro-disposition-structural-followup-classification.md | action: defer | note: The #337 next-goal artifact is `Status: draft`, "becomes active only when the user runs /goal", "No slices yet" — fully inert, no auto-execution risk in the publish.
- F6 | bin: over-worry | evidence: moderate | ref: .agents/release-adapter.yaml | action: document | note: Uncommitted `M release-adapter.yaml` is the pending 0.30.0 entry; the release flow stages it with the bump. Not drift — but the release MUST include it in the bump commit (it does by contract).
- F7 | bin: over-worry | evidence: strong | ref: plugins/charness/skills | action: defer | note: All four shipped surfaces (scaffold_critique_artifact.py, publish_release_cli.py, publish_release_preflight.py, achieve/SKILL.md) are byte-synced root-to-plugin mirror; no generated-surface drift.
- F8 | bin: over-worry | evidence: moderate | ref: charness-artifacts/quality/latest.md | action: defer | note: `quality/latest.md` Date is 2026-06-06 (pre-session) but the broad gate ran 73/73 this session per the closeout commit; the latest pointer staleness is cosmetic, not a release blocker.

## Reviewer Tier Evidence
- Requested tier: fresh-eye bounded RELEASE reviewer (read-only).
- Requested spawn fields: read-only inspection (`git log`/`show`/`diff`, `cat`, `gh`, `python3`); no index/worktree mutation.
- Host exposure state: applied
- Application state: host-confirmed: subagent a4642eeb8762ce66d (claude-opus-4-8[1m]) ran git/gh/python3 read-only probes against the repo and returned a SHIP release verdict.

## Fresh-Eye Satisfaction
SHIP. Concrete signal: minor is the lightest honest bump (no breaking change — `main()` removed-then-re-added as a refactor dispatcher, zero CLI args dropped); the 0.30.0 update_instructions entry honestly and accurately describes all three additive slices with the `0.30.0` token, and the real loader confirms `valid: True` plus a `None` staleness verdict; #336 is OPEN with the fix in range and `Close #336` on `b1dd7e16`; the #337 artifact is `Status: draft` and inert; all four shipped surfaces are mirror-synced; no live/provider proof is claimed where only local ran. The one item to confirm at execution: the release commit carries the pending release-adapter 0.30.0 entry alongside the bump (contract-owned). No release-stopping concern.
