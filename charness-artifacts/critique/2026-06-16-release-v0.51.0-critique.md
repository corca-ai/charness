# v0.51.0 release critique

Date: 2026-06-16
Fresh-Eye Satisfaction: parent-delegated
Reviewer: `019ece35-b807-76e0-aab7-194815419b39`

## Reviewer Tier Evidence

- requested tier: default explorer
- requested spawn fields: inherited parent model and reasoning settings
- host exposure state: host-defaulted
- application state: spawn tool accepted reviewer agent id
  `019ece35-b807-76e0-aab7-194815419b39`; reviewer-tier application details
  are host-hidden

## Scope

Publish the local open-issue HOTL bundle as `charness` v0.51.0. The bundle
implements and proves #375, #376, #377, and #378, records #371 as separate
upstream lifecycle proof debt, and prepares a release/issue closeout carrier.

## Act Before Ship

- Finding: the release publish gate needs a standalone critique artifact.
  `publish_release.py --execute` refuses without `--critique-artifact`.
- Resolution: this artifact records the bounded fresh-eye release critique and
  is committed before invoking the release helper.
- Finding: `.agents/release-adapter.yaml` needed a v0.51.0
  `update_instructions` entry before publish; otherwise the helper staleness
  guard would hold the release.
- Resolution: added a v0.51.0 entry that names the #375-#378 operator-facing
  changes, keeps #371 excluded, and records normal `charness update` as the
  refresh path.
- Finding: the checked-in HOTL carrier draft is a PR-body draft, but this
  release path is not opening a PR.
- Resolution: publish through `publish_release.py --close-issue 375
  --close-issue 376 --close-issue 377 --close-issue 378`, then verify issue
  state after publish with the release helper and `issue_tool.py` readback.

## Bundle Anyway

- Live GitHub readback currently shows #375, #376, #377, #378, and #371 open.
  This is expected before the release carrier lands.
- Fresh checkout probes are configured but not yet run. The release helper owns
  running them before tag/release publication.
- Requested review commands are not configured in the release adapter, so the
  requested-review gate is expected to be non-blocking.

## Do Not Ship As

- Do not close #371 in this release. Current evidence proves only downstream
  post-hoc mitigation, not invocation-end `agent-browser` process/profile
  teardown for normal completion, cancellation, provider failure, and timeout.
- Do not report the HOTL PR-body carrier draft as final issue closeout. Final
  closeout requires the release carrier to land and GitHub state to verify
  `CLOSED` for #375-#378.

## Proceed Call

Proceed after this critique artifact and the v0.51.0 release-adapter update are
tracked. Use a minor bump because the bundle adds maintained public skill/helper
capabilities and operator-facing advisory surfaces, without a breaking change.

## Proof To Run

- `python3 skills/public/release/scripts/publish_release.py --repo-root .
  --part minor --critique-artifact
  charness-artifacts/critique/2026-06-16-release-v0.51.0-critique.md
  --close-issue 375 --close-issue 376 --close-issue 377 --close-issue 378
  --execute`
- After publish, verify the release and issue states from GitHub source of
  truth.
