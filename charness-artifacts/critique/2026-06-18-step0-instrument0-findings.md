# Step 0 — Instrument 0 (real-history mining) FINDINGS

Status: EXECUTED 2026-06-18 (fresh post-compaction session). Result is durable
record; the forward re-scope below is RECOMMENDED, pending operator ratification.

## What ran

Per `step0-design-v2.md`, Instrument 0 = mine the real #386 + lifecycle-family
history for the proxy-trust signature and classify the OPERATIVE mechanism.
Method honored the facts/judgment split: I (a load-primed prior) extracted only
the OBSERVABLE FACTS, then handed them — stripped of all fix-framing — to two
OPPOSITE-PRIMED fresh judges (pro-load vs pro-structural). I synthesize.

## Evidence base (real artifacts)

- `charness-artifacts/metrics/rca-ledger.jsonl` (#359/#382/#383/#385/#386 rows)
- `charness-artifacts/issue-drafts/2026-06-18-issue-386-commit-body.md` (doer RCA)
- `charness-artifacts/critique/2026-06-18-issue-386-north-star-pilot.md` (independent 4-angle critique)
- `charness-artifacts/critique/2026-06-17-issue-381-operator-decision-queue-resolution.md`

## Observable facts (the packet given to judges)

- F1. Bundle closeout marked N issues closed; per-issue runtime behavior NOT
  exercised — closure rested on deployment readback + tracker CLOSED state.
- F2. A distinct disposition REVIEWER existed and RAN; it was FRESH (separate
  spawn, not the doer). It approved; it did not catch the gap.
- F3. It consulted the SAME aggregate bundle proxy (readback + CLOSED); no
  distinct per-unit channel exercised.
- F4. No per-issue behavior obligation reached the decision point; the audit
  duty lived in inert prose elsewhere.
- F5. The gap was caught by the HUMAN — not agent, not gate.
- F6. Across the lifecycle family (#359/#382/#385/#386), catch agent = HUMAN
  every time; only the unrelated #383 was caught_by agent.
- F7. HONEST GAP: the reviewer's accumulated-context conditions are NOT recorded.

## Judge panel result

| Judge (primed) | Dominant pick | Conf | Verdict on context-load |
|---|---|---|---|
| pro-LOAD | (c) summary-laundering | 0.78 | conceded — "near-zero weight"; salience-zero collapses into structural absence, not fatigue |
| pro-STRUCTURAL | (c)+(d) structural | 0.82 | "unevidenced, not disproven"; refused to call it a red herring |

Convergence is AGAINST the shared prior (the pro-load judge defected on the
evidence) → robust per the bracket logic. Both named the same keystone (F2:
fresh reviewer demonstrably failed) and same falsifier (F3/F7).

## CONCLUSION

**Operative mechanism = STRUCTURAL: same-channel re-read (c) + never-obligated-
per-unit (d), causally entangled. Context-LOAD is NOT supported** (unevidenced;
the structural account fully explains the outcome without it, and is corroborated
by the F6 base rate). The handoff's "load / low-salience" hypothesis is
REDIRECTED — which is precisely what Instrument 0 was built to be able to do.

Note: bounded fresh-eye reviewers are low-context BY CONSTRUCTION (spawned with a
bounded packet), which further disfavors load. The residual is only F7 (the exact
reviewer context is unrecorded) → load cannot be affirmatively DISPROVEN, only
shown unsupported and unnecessary.

## Recommended re-scope (pending ratification)

1. **CUT Instrument 1 (salience contrast) and Instrument 2 (load axis).** Both
   isolate variables the evidence does not implicate. The "salience" that mattered
   was the duty being structurally ABSENT (F4), not buried-under-load — running a
   load/salience experiment now repeats v1's "test the wrong variable" error.
2. **PROMOTE Instrument 3 / C3b (summary-laundering routing) to PRIMARY** remaining
   experiment — it directly tests the confirmed mechanism: does a fresh reviewer
   fed pre-digested doer claims (vs raw per-unit state) rubber-stamp? If catch <
   0.80, Track 1a's "PULL raw state + RUN the behavioral check per-unit" is
   load-bearing and must be enforced.
3. **Track 1a sharpened:** the lever is NOT freshness/context-reset (present and
   FAILED at #386). The load-bearing requirement is (a) an obligated per-unit
   verdict + (b) an obligated DISTINCT evidence channel (re-run behavior; never
   re-read the readback/summary). Freshness = necessary precondition, not the fix.
4. **Plan-v2 Q3 (multi-unit manifest) largely dissolves:** the worry was "N units
   → load → rubber-stamp." Load isn't the driver, so the real risk is per-unit
   distinct-channel exercise actually happening — already covered by (3). Cap-for-
   load is unmotivated; require per-unit distinct-channel verdict instead.

## Residual / honest limit

F7: the #386 reviewer's exact context size is unrecorded → load is unsupported,
not disproven. Closing it (mining the reviewer transcript) is LOW value: the
structural account is sufficient and bounded-reviewers are low-load by design.
Pursue only if the operator wants positive certainty that load contributed nothing.
