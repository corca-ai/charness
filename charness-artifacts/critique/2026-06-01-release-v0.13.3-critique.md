# Release Critique - v0.13.3

Execution: release critique before publish.
Fresh-Eye Satisfaction: parent-delegated
Packet Consumed: `charness-artifacts/critique/2026-06-01-062646-packet.md`
Target: `release-critique.md`

Counterweight note: two release angle reviewers completed. A separate
counterweight subagent (`019e81df-2406-76e2-9734-4a4e8529a900`) timed out twice
and was closed before returning findings.

Host signal: counterweight subagent stayed running across two 120s waits after
the two angle reviews completed, then returned shutdown when closed.

## Release Scope

Target release: `v0.13.3` patch for `charness`.

Consumer-facing frame: maintenance patch for long-goal efficiency work. It adds
a goal-window host metric evidence path and pins goal artifact shape across the
primary achieve generator and handoff draft generator. It is additive; existing
full-thread host-log audit behavior remains available.

## Surface-Lock Inventory

- Public skill semantics: `achieve` goal artifact references and lifecycle
  guidance now describe goal-window metric evidence.
- Public skill semantics: `retro` now consumes a goal artifact path for scoped
  Codex rollout JSONL audit when the evidence line is valid.
- Host-log helper behavior: invalid or incomplete goal-window evidence is
  reported as invalid and does not fall back to the latest unrelated Codex
  rollout.
- Checked-in plugin export: `plugins/charness/**` mirrors the changed scripts
  and skill references.
- Public dogfood registry: `docs/public-skill-dogfood.json` records the new
  achieve/retro consumer evidence.
- Tests: producer contract and goal-window positive/negative audit tests are
  part of the quality gate.

## Angles

- Operational checklist angle (`019e81dd-2a2a-7be2-bc81-d800e4863b77`): found
  no unresolved implementation blocker, but required the critique evidence to
  be tracked before publish and reminded that release artifact refresh must be
  owned by the helper before tag/push.
- Communication/operator angle (`019e81dd-48c4-7502-8d21-4635abf9aa47`): found
  no release blocker. It recommended wording the docs/release frame as the
  `retro` host-log probe with `--goal-path`, not as a new global CLI.

## Findings

- F1: The release critique evidence must be tracked before invoking
  `publish_release.py --execute`.
- F2: `charness-artifacts/release/latest.md` still names `v0.13.2` before the
  release helper runs.
- F3: Release proof must use the full surface-driven deterministic gates, not
  focused tests only.
- F4: The achieve-side wording should avoid implying `probe_host_logs.py` is a
  new global CLI.
- F5: Requested-review enforcement is advisory-only because the release adapter
  declares no requested review commands.

## Counterweight Triage

- F1 | bin: act-before-ship | evidence: strong | ref: release publish gate |
  action: fix | note: the helper refuses without a tracked critique artifact.
- F2 | bin: over-worry | evidence: moderate | ref:
  `charness-artifacts/release/latest.md` | action: document | note: stale
  `latest.md` is normal pre-publish state; verify the helper refreshes it after
  version bump/tag/release.
- F3 | bin: act-before-ship | evidence: strong | ref:
  `charness-artifacts/critique/2026-06-01-062646-packet.md` | action: fix |
  note: full post-commit gates already ran and passed; keep that proof attached
  to closeout.
- F4 | bin: bundle-anyway | evidence: moderate | ref:
  `skills/public/achieve/references/goal-artifact.md` | action: fix | note:
  cheap wording fix before release.
- F5 | bin: valid-but-defer | evidence: moderate | ref:
  `skills/public/release/scripts/check_requested_review_gate.py` | action:
  document | note: not a release blocker because parent-delegated release
  critique ran; adapter hardening can happen in a later slice.

## Operator Action Required

- Track this critique artifact before publish.
- Keep the full deterministic gate proof in closeout:
  `1937 passed, 4 skipped` for the broad Python suite, plus packaging,
  markdown/link/secrets, skill validation, Cautilus proof validator,
  public-skill dogfood/validation, integration dry-runs, ruff, py_compile,
  Python length, and attention-state gates.
- Invoke the release helper with this artifact:

```bash
python3 skills/public/release/scripts/publish_release.py --repo-root . --part patch --critique-artifact charness-artifacts/critique/2026-06-01-release-v0.13.3-critique.md --execute
```

## Upgrade Path

After publish, operators should update to `v0.13.3` by pulling the repository
or reinstalling the checked-in plugin surface as usual. There is no migration
step; the new goal-window audit path activates only when a goal artifact
contains a valid `Host metric window:` evidence line.

## Deliberately Not Doing

- Not claiming product success metrics are solved. This release only improves
  goal-window evidence and usage-cost retrospectives.
- Not claiming real-host fresh-checkout proof; the release adapter reports no
  configured fresh-checkout probe obligation for this slice.

## Next Move

Commit the critique artifact and wording fix, then run the release helper with
`--critique-artifact` so version bump, export sync, release artifact refresh,
tag, push, and GitHub release creation are handled by the release workflow.
