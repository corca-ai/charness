# Release Critique: charness 0.16.0

Date: 2026-06-03

Fresh-Eye Satisfaction: bounded general-purpose subagent (read-only, shared worktree)

## Scope

- Bump: minor, 0.15.0 → 0.16.0, via `publish_release.py --part minor --execute`.
- Delta (`origin/main..HEAD`):
  - `81c04ef1` Resolve #282 provider-safe goal closeout metrics (feature; carries `Close #282.`)
  - `3759f597` Add `validate_quality_artifact --report-all` (additive validator mode)
  - `3f87027c` Record #283 quality-session waste retro (artifact only)
  - `9fb08f6f` Kill #283 mutation survivors (tests only; no production source change)

## Verdict

SHIP-WITH-FIXES → both fixes applied; now SHIP.

## Blockers Folded

1. **Mandatory release-critique gate argument.** `publish_release.py --execute`
   hard-refuses without `--critique-artifact`/`--critique-blocked`
   (`publish_release_preflight.enforce_release_critique_gate`). Fixed by writing
   this release-scoped artifact and passing it as `--critique-artifact`.
2. **Stale 0.15.0 `update_instructions`.** `.agents/release-adapter.yaml` led with
   "pull 0.15.0 … usage product-review reporter" and a 0.15.0-specific NEW
   OPERATOR SURFACE line; the narrative audit does not scan update_instructions
   for stale versions, so it would have shipped unflagged. Fixed: lead line now
   describes 0.16.0 (#282), with #283 reliability, the `--report-all` validator
   mode, and the 0.15.0 reporter demoted to carried-forward.

## Bump-Level Judgment

`minor` is correct. The full delta has no deletes/renames and every production
diff is additive/default-preserving: `--report-all` defaults off,
`probe_host_logs.py --format` defaults `json`, the new `metric_window` closeout
field is strictly non-blocking, and `goal_artifact_lib` only adds re-exports.
New CLI (`record_metric_window.py`) + new lib are net-new operator surface with
no migration. Not `patch` (adoptable new surface), not `major` (no break).

## #283 Safety

Confirmed safe. A precise verb+`#283` scan over the delta finds no auto-close
keyword bound to 283; the only close keyword on push is `Close #282.`. This
matches `docs/handoff.md`: #283 stays OPEN until the scheduled `mutation-tests.yml`
auto-issue confirms recovery above the 80% threshold. The release lands the
already-committed fix on `main` and lets the workflow own close/reopen.

## Publish-Boundary Honesty

`--execute` synchronously bumps + syncs generated manifests + runs the release
quality gate + commits + tags + pushes, then `gh release create --verify-tag`
and `verify_release_visible` (`gh release view`, up to 3 retries), raising on
failure. So the closeout may honestly claim "public GitHub release created and
verified visible", not merely "tag pushed". Notes are GitHub `--generate-notes`
(auto from commits), not author-curated — acceptable for this release.

## Generated-Surface Drift

Wired and cannot ship stale: `bump_version` writes all packaging version slots
then runs the sync command; `ensure_release_surface` re-reads and raises on any
drift across packaging/claude-plugin/codex-plugin/claude-marketplace before
tagging. All five surfaces currently agree at 0.15.0.

## Over-Worry Dismissed

- Async-workflow conflation: not applicable; the helper creates+verifies the
  release synchronously.
- Hidden breaking change under "minor": dismissed (zero deletes/renames,
  default-preserving diffs).
- Hidden `Close #283`: dismissed by precise scan.
- Stale generated manifests: dismissed (sync + drift check wired into the bump).

## Blockers

- None remaining.
