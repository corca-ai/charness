# Pilot verifier review disposition

Before any model cell, two independent file-backed reviewers blocked the first
protocol revision. Their exact results are preserved beside this record; neither
block is approval. Parent owns this counterweight decision.

- Act Before Ship: fairness F1/F2 and observer F1–F5 all accepted. Bind retained
  control observations; check complete source/test inventories and missing files;
  preserve phase results before continuation; use normal package import semantics;
  test unknown names with aliases and deep-copy mutable inputs. Implemented before
  the first cell, with 2 correct/8 broken behavior controls and 7 launcher controls.
- Bundle Anyway: bound observer execution to 20 seconds; record and reject
  escaping symlinks; require candidate package equality against its separate
  freeze. Added. Preflight now exercises real shell/read/write, not names alone.
- Over-Worry: malicious filesystem/process escape resistance, general crash
  recovery, statistical confidence and backend model attestation remain nonclaims.
- Valid but Defer: actual candidate layout/discovery preflight and candidate
  revision freeze remain required before the first candidate cell. No pilot output
  may shape initial candidate code before that freeze.

One bounded follow-up reviews only these repaired verifier risks; it is not a
second complete design round. No actual model cell or product-benefit claim has
been made. The fake-process transport control is not a comparison cell.
