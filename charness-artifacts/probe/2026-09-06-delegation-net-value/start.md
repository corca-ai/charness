# Initial context

Own the delegated task in `.task/current.md`. Read the repository, act within the
current task's first-context boundary, and preserve what a fresh context needs.
Do not use external services or install packages. Read only this workspace and
the execution tools you need. Leave existing tests and the task channel intact.
