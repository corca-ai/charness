# Fresh continuation context

Continue the delegated Catalog task from this durable workspace to completion.
The current authoritative task is `.task/current.md`. Recover the task and prior
decisions from the workspace; do not assume access to a previous conversation.
Do not use external services or install packages. Read only this workspace and
the execution tools you need. Leave existing tests and the task channel intact.
