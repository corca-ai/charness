# Probe record — #820 named expected failing test (charness 8.9.0)

Claim: A Node/TAP sweep whose plan requires an unfulfillable expected_failing_test refuses the red run instead of killing it.
Claim Kind: change
Observable: per-mutant verdict plus sweep killed/refused counts from mutate_and_restore.py over a real node --test run.
Source Ref: scripts/mutation/mutation_expected_failure.py
Source Conditions: node v22.22.2 whose default reporter emits TAP version 13; scratch fixture with src/calc.js (return a + b) and two node:test cases; mutant replaces with return a * b so both tests fail; plan requires expected_failing_test no-such-test, a name no test reports.
Base Ref: v8.8.0 (tag, commit 862a6e75f)
Head Ref: 96b9c6560 plus v8.9.0 critique-evidence commits (product code unchanged)
Base Arm: base-observed
Call sites unproven: none — single probed entrypoint (mutate_and_restore sweep with the node-test reporter); the pytest counts-only naming refusal is covered by unit tests, not by this probe.

## Source text

```text
"""One declarative expected failing test per mutant (#820).

A downstream guard must prove not merely "the suite went red" but "the
intended named test reported failure" -- an unrelated failure must not kill
the mutant. A plan entry opts in with `"expected_failing_test": "<name>"`,
and the verdict narrows to:

- expected test among the reported failures: the kill stands;
- command failed without it: REFUSED (unestablished proof, not a kill);
- command passed: SURVIVED (the expectation only narrows the kill).
```

## Stimulus

```text
Seed the fixture above in a scratch repo (git init + commit), write plan.json {"test_command": ["node", "--test"], "reporter": "node-test", "mutants": [{"id": "add-to-mul", "path": "src/calc.js", "find": "return a + b;", "replace": "return a * b;", "expected_failing_test": "no-such-test"}]}, then run `python3 <tree>/scripts/mutation/mutate_and_restore.py --repo-root <fixture> --plan plan.json` once from the v8.8.0 tree and once from the 8.9.0 tree, and read the YAML verdict lines.
```

## Base observable

```text
verdict: killed
killed: 1
refused: 0
expected_failing_test: None
detail: (empty)
restored: True
```

## Head observable

```text
verdict: refused
killed: 0
refused: 1
expected_failing_test: no-such-test
detail: the command failed without the expected test 'no-such-test'; failing tests were: 'add sums', 'add zero'
restored: True
```
