# v8.7.2 verification receipts (final-head evidence for the approval round)

Date: 2026-09-15. Heads: fix `23dc82206`, prepare `53e61b83`,
claims `737b87426`. Tree clean at each gate run unless noted.

## 1. Full release-quality gate on the committed fix tree

- Command: `./scripts/run-quality.sh --full --read-only --release`
  at committed `23dc82206` (clean worktree).
- Observed: `PASS release-changed-line-coverage 69.6s`;
  `Quality summary: 89 passed, 0 failed` (4 opt-in not run).
- Meaning: the #815 slice's changed lines are covered, including the
  lane-completion wiring.

## 2. Focused task_run suite at the fix head

- `test_task_run_implementation_lane.py` +
  `test_task_run_muse_executor.py`: 12 passed.
- Full `-k task_run` sweep: 157 passed, 0 failed.

## 3. Resume pytest-release at the publish candidate `737b87426`

- Command (via release helper resume): full `pytest-release` lane.
- Observed: `1 failed, 9846 passed in 148.82s`.
- The single failure:
  `FAILED tests/quality_gates/test_critique_fresh_eye_presence.py::test_live_corpus_critique_artifacts_pass_whole_tree_validation`
  with assertion:
  `Invalid critique artifact charness-artifacts/critique/v872-release-critique.md: 2 critique artifact rule violation(s): reviewer tier evidence missing fields: ['requested spawn fields']; worker-delivered requires the durable worker report carrier fields: ['worker report identity', 'worker report delivery']`.
- Disposition: no code defect anywhere in the 9846 passes. The failure
  is this release's own critique artifact typed conformance — the item
  under repair in the approval round (tier line restored; carrier
  rebinding follows the passing worker round). Raw log (ephemeral
  cache):
  `/home/hwidong/.cache/tmp/charness/runtime/811b9f8f8a808bfa/quality-failure-logs/pytest-release.log`.

## 4. Claims review over the prepared record

- Narrative `charness-artifacts/release-review/2026-09-15-v8.7.2-claims.md`
  + scaffolded record `...-prepared-claims-review.json`: verdict pass,
  distinct workflow-child observer, no findings; committed as `737b87426`.

## 5. Residual and next binding

- These receipts are bound to the stated heads, not to a future tag.
  The publish resume re-validates every gate at the final publish
  candidate before tag push; that run is the terminal binding, and this
  file is its input evidence — not a substitute for it.
