# v5.1.0 Post-Publication Observables (captured 2026-08-12T23:42:03Z)

Purpose: supply the post-publication fresh-eye closeout review with hosted
observables captured on a channel distinct from the release record's own claims.
The v5.1.0 publication session recorded its claims review against the *prepared*
commit `1024e500` only; nothing audited what became true after publication.

Provenance: raw, unedited stdout of the commands named in each block, run from
this repo by the parent session (authenticated `gh`, and `git ls-remote` against
`origin`). The bounded reviewer has no exec capability, so the parent captured
them; the reviewer's job is to audit the RECORDS against this capture, not to
trust the parent's summary of it.

## What this capture establishes that the existing observer record does not

`charness-artifacts/probe/2026-08-12-v5.1.0-release-observer.json` records a
credential-free HTTPS fetch and states its own limit explicitly: it
`does_not_establish` "that a GitHub RELEASE exists for this tag — the same page
returns 200 for a pushed tag with no release".

**CORRECTION (2026-08-13, after the floor audit).** This section originally
claimed the authenticated `gh release view` block below closed that gap. It does
not, and the claim was wrong in exactly the way this repo has a mechanical check
for: `gh release view <tag>` is `release_view_shape`, the command shape
`confirm_release_via_distinct_channel` flags as `same-proxy-flagged`
(`skills/public/release/scripts/publish_release_post_create.py:338-356`;
`skills/public/release/references/publication-boundary.md:108-112`). It re-reads
the same CLI, credentials, and host that created the release, so it confirms the
publisher's claim with the publisher's own proxy — the P4 violation named at
`docs/design-north-star.md:52-55`.

The gap is closed instead by the credential-free REST readback appended below,
which uses no credentials and is distinct from tag state (this endpoint returns
404 for a pushed tag that has no release).

## Non-claims for this capture

- It does not establish that any tracker issue is closed or closable.
- It does not establish that hosted CI covered every changed line, only the
  conclusions the named jobs reported.
- The capturing process is the same host as the publishing session; the distinct
  channels here are the hosted API and remote refs, not a distinct machine.

## Raw capture

```text
=== capture_utc ===
2026-08-12T23:42:03Z

=== gh release view v5.1.0 (hosted authenticated API) ===
{"author":{"id":"MDQ6VXNlcjE1ODk0MzA=","login":"spilist"},"isDraft":false,"isPrerelease":false,"name":"v5.1.0","publishedAt":"2026-08-12T23:17:31Z","tagName":"v5.1.0","targetCommitish":"main","url":"https://github.com/corca-ai/charness/releases/tag/v5.1.0"}

=== git ls-remote --tags origin v5.1.0 ===
1024e50038adc5c027c64cf6b750a556bf679693	refs/tags/v5.1.0

=== git ls-remote origin refs/heads/main ===
4aa76a19e5c4fc725c6d971e09dda433015542ad	refs/heads/main

=== gh run list --branch main --limit 6 ===
{"conclusion":"success","createdAt":"2026-08-12T23:21:11Z","databaseId":31650565315,"event":"push","headSha":"4aa76a19e5c4fc725c6d971e09dda433015542ad","workflowName":"Quality Core"}
{"conclusion":"success","createdAt":"2026-08-12T23:17:57Z","databaseId":31650365451,"event":"push","headSha":"6bf2d54524826f8a58006446c7ea6757d6863064","workflowName":"Quality Core"}
{"conclusion":"cancelled","createdAt":"2026-08-12T23:17:33Z","databaseId":31650339669,"event":"push","headSha":"bc9f86c0c86f8ca3a5c4697155b75b66d6354588","workflowName":"Quality Core"}
{"conclusion":"success","createdAt":"2026-08-12T13:12:41Z","databaseId":31600258657,"event":"push","headSha":"ef07aca4af2339872708f4141ec7d96dec8e9f5c","workflowName":"Quality Core"}
{"conclusion":"cancelled","createdAt":"2026-08-12T13:12:16Z","databaseId":31600223616,"event":"push","headSha":"fe5c3cd98d21d5a06cc0160c800d42c08090b5ae","workflowName":"Quality Core"}
{"conclusion":"success","createdAt":"2026-08-12T12:33:41Z","databaseId":31597030355,"event":"schedule","headSha":"1c1acd90cb45f4e8fa9f7b1159caca82520c3423","workflowName":"Mutation Tests"}

=== gh run view 31650565315 ===
{"conclusion":"success","headSha":"4aa76a19e5c4fc725c6d971e09dda433015542ad","jobs":[{"conclusion":"success","name":"Changed-line mutation coverage (push/PR mirror)"},{"conclusion":"success","name":"Core deterministic gates"}],"workflowName":"Quality Core"}
```

## Credential-free REST readback (added 2026-08-13, closes the release-existence gap)

Command: `curl -sS -H "Accept: application/vnd.github+json" https://api.github.com/repos/corca-ai/charness/releases/tags/v5.1.0`
(no credentials sent; distinct transport and distinct credential context from the
publishing `gh` CLI; returns 404 for a tag with no release, so it is also
distinct from tag state).

```json
{
  "tag_name": "v5.1.0",
  "name": "v5.1.0",
  "draft": false,
  "prerelease": false,
  "published_at": "2026-08-12T23:17:31Z",
  "created_at": "2026-08-12T22:39:05Z",
  "target_commitish": "main",
  "html_url": "https://github.com/corca-ai/charness/releases/tag/v5.1.0",
  "id": 369579752
}
```

Still same host as the publisher. What it establishes: a GitHub release object
exists for this tag, is not a draft, is not a prerelease, and targets `main`.

## Reading aids (facts, not verdicts)

- The tag `v5.1.0` points at `1024e500` (lightweight tag, `git cat-file -t` =
  `commit`); the default branch head is `4aa76a19`.
- **CORRECTION (2026-08-13, after the claims audit).** This aid originally said
  main was "two commits later". It is **four**:
  `git log --oneline --first-parent 1024e500..4aa76a19` =
  `970e90ca` (claims review record), `bc9f86c0` (release artifact before resume
  push), `6bf2d545` (release verification record), `4aa76a19` (handoff reconcile).
  A tag behind the branch head is expected for this release shape.
- The `cancelled` run at `bc9f86c0` is superseded by the two later successful
  runs on descendants of that commit.
- `charness-artifacts/release/latest.md` recorded `RECONCILE REQUIRED` for
  `docs/handoff.md`; commit `4aa76a19` claims to be that reconcile.
