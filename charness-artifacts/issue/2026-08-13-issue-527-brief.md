# Issue 527 Decision Brief

Issue: [corca-ai/charness#527](https://github.com/corca-ai/charness/issues/527)
Status: OPEN — operator decision required before implementation.

## Reporter Job To Be Done

Let a human choose and safely invoke Charness skills without mistaking
agent-execution instructions for reader-oriented documentation or relying on
prose alone for destructive boundaries.

## Evidence Re-read

- The issue is an enhancement proposal, has no comments, and names four open
  product decisions: invocation-lock owner and scope, human-documentation owner
  and lifecycle, observable success signal, and maturity/retirement cost.
- The fixed backlog goal permits a tracker-visible deferral for #527; it does
  not authorize selecting product direction or adding a new invocation policy
  without the operator.
- Existing `release` and `issue` workflows retain their current explicit
  confirmation and closeout boundaries. This brief does not claim a missing
  harness field or a behavior escape has been reproduced.

## Decision Required

The operator must choose one coherent initial boundary, or explicitly defer:

1. Introduce a harness-recognized invocation lock for a named, small set of
   destructive operations (at minimum release publication and issue closure),
   with the policy owner selected before implementation.
2. Define a reader-oriented skill-documentation contract and choose whether it
   starts as a maintained template plus pilot or an all-public-skill backfill.
3. Decide whether maturity/retirement metadata is part of the same contract or
   a later, separately justified capability.

## Current Disposition

`unproven-defer`: no code, policy, documentation template, or tracker closure
is authorized from the comparison alone. The owner is the repository operator.
Revisit before #527 moves from investigation to an implementation brief, or
when the operator explicitly selects the minimum viable boundary above.

AI-provenance: agent-drafted pause state for issue-resolution discussion; not a
closeout carrier and not a behavior verdict.
