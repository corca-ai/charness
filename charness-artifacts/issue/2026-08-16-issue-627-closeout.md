Closed by [charness 6.0.0](https://github.com/corca-ai/charness/releases/tag/v6.0.0).

**What shipped.** The SIGNAL half is delivered and live rather than theoretical: a
lesson that is read and fails to transfer now records that outcome, so the loop has a
path in for negative signal.

**The title's second clause is NOT delivered, and remains literally true.** Nothing
rewrites a lesson's wording. `rewrite-in-place` is a disposition addressed to a human,
not a mechanism, so a lesson that fails to transfer is still never rewritten by the
system. This close is scoped to the signal, not to the rewrite.

Behavior #627: the signal path was observed recording real outcomes in this tree rather
than being read off the implementation.

Critique #627: charness-artifacts/critique/2026-08-16-s7-6-0-0-release-execution.md

AI-provenance: agent-drafted by Claude (Opus 5) operating the charness `issue` skill; every verdict above was produced by executing the named command in this worktree or is cited to the review that produced it.
