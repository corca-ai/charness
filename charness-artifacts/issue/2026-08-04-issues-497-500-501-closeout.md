Closes #497
Closes #500
Closes #501

Jtbd: #497 needs the exported plugin's `validate_adapters.py` entrypoint to
import and discover the resolver files that actually exist in the flattened
layout; #500 needs both goal-artifact creators to apply the same value guards
before writing; #501 needs the export-safe-import proof surface to catch the
recorded literal helper-path form.

Root Cause: #497 assumed the authoring-tree package layout at runtime and in its
resolver glob; #500 kept value predicates at one producer instead of the shared
value boundary; #501 modeled import-statement syntax instead of the supported
literal `import_repo_module` call contract.

Debug Artifact: charness-artifacts/debug/2026-08-04-debug-review-followup.md

Siblings: #497 source and flattened resolver layouts are real interface-shape
siblings — decision: fix now with isolated exported-consumer proof; proof:
source/export validator outputs and subprocess. #500 achieve and handoff are
same-class producers — decision: fix now at the shared value owner; proof:
hostile input and no-artifact tests. #501 literal helper calls are the supported
static sibling — decision: fix now, while variables, aliases, f-strings,
concatenation, qualified calls, and arbitrary dynamic imports remain diagnostic
non-claims; proof: direct gate tests and negative controls.

Prevention: `goal_artifact_lib.py` validates the exact canonical values rendered
by both producers before filesystem mutation; `check_export_safe_imports.py`
detects the supported literal helper-call forms; and `validate_adapters.py`
selects source or flattened resolver loading/discovery based on the layout.
Source and generated plugin mirrors are synced.

Boundary #497: moved-to-owner — the exported validator owns its runtime layout
resolution and discovery.
Boundary #500: moved-to-owner — the shared goal-value library owns the invariant
while callers retain transport/status policy.
Boundary #501: owned-correctly — the export proof surface owns the narrow literal
helper-call predicate and records its dynamic-form non-claims.

Critique #497 #500 #501: charness-artifacts/critique/2026-08-04-issue-497-500-501-resolution-critique.md

Behavior #497: confirmed through a freshly generated exported-plugin subprocess
with `CHARNESS_REPO_ROOT` removed; it imports and validates the flattened
resolver set. Evidence: tests/quality_gates/test_profile_and_preset_validation.py:515.
Behavior #500: confirmed through hostile handoff producer inputs that refuse
unsafe values before mkdir/write and leave no artifact, plus the sibling achieve
input-channel tests. Evidence: tests/test_handoff_chunker_auto_draft.py:569 and
tests/quality_gates/test_upsert_goal_input_channel.py:85.
Behavior #501: confirmed through direct export-gate execution for positional,
keyword, and `Path(__file__)` supported literals, with negative controls for
unsupported dynamic forms. Evidence: tests/quality_gates/test_export_safe_asset_paths.py:361.

AI-provenance: This carrier was drafted by the Codex agent from the live issue
reads, the checked-in causal/debug record, the exact critique packet, and the
delegated multi-angle resolution critique; remote issue state remains the
maintainer's separate readback responsibility after publication.
