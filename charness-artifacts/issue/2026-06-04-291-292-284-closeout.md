Close #291. Close #292. Close #284.

Critique #291 #292 #284: charness-artifacts/critique/2026-06-04-291-292-284-final-bundle.md

## #291

Classification: bug
JTBD: Ensure `/goal` activation cannot proceed when consequential activation
discussion is merely surfaced but not resolved.
Root cause: `pursue_ready` treated shaped artifacts with a warning as ready for
activation, so the consumer boundary between shape readiness and activation
readiness was collapsed.
Debug artifact: charness-artifacts/debug/2026-06-04-291-achieve-pursue-ready-activation-discussion.md
Siblings: Decision was to split `shape_ready` and `activation_ready`; proof came
from focused helper/CLI tests covering hidden, surfaced-unresolved,
issue-number-leading, and explicitly resolved discussion summaries.
Prevention: `check_goal_artifact.py --pursue-ready` now exits nonzero for
unresolved consequential activation discussion, public `achieve` guidance names
the activation boundary, and dogfood records the contract.

## #292

Classification: bug
JTBD: Keep pre-push/parallel quality tests from touching the shared parent repo
index while preserving staged mirror-drift coverage.
Root cause: the standing e2e test ran the real staged-index gate against
`ROOT`, and `git write-tree` interacts with the live index during concurrent
push/hooks.
Debug artifact: charness-artifacts/debug/2026-06-04-292-prepush-real-index-flake.md
Siblings: Decision was to keep production staged-index behavior but move the e2e
proof to an isolated seeded repo copy; proof came from focused pytest plus a
static guard against reintroducing `check_staged_mirror_drift.py` with
`str(ROOT)`.
Prevention: the copy-heavy e2e is release-only, standing pytest avoids the
shared index, and the debug artifact records the index-sensitive test hazard for
future quality work.

## #284

Classification: deferred-work
JTBD: Give authors a cheap pre-edit signal before skill-surface edits breach
known length, markdown, link-depth, mirror-sync, or index-safety couplings.
Boundary: The helper is an authoring preflight, not a replacement for broad
quality or a change to public skill runtime behavior.
Resolution brief: Implement a repo-local helper with human/JSON output,
`--preview-delta`, optional targeted checks, and explicit coupling output; no
product or external-side-effect decision remained open.
Implementation: `scripts/check_skill_surface_preflight.py` reports SKILL.md
total and core nonempty headroom, blocks preview breaches, and surfaces
validate-skills, markdown inline span, doc-link, plugin mirror, reference-depth,
and staged-index hazards. Tests cover SKILL.md ceiling blocking, reference-path
preview behavior, invalid paths, and the in-process API.
Prevention: `docs/conventions/implementation-discipline.md` now routes skill
prose edits through the preflight, and the helper caught the saturated
`achieve/SKILL.md` core during this run before broad quality.

## Verification

- Focused #291 tests passed before commit `b7c62cf1`.
- Focused #292 tests passed before commit `d2a0e9fa`.
- Focused #284 tests and slice closeout passed before commit `14339a73`.
- Closeout repair tests passed before commit `3b7ed973`.
- Final broad quality after repair: `./scripts/run-quality.sh --read-only` →
  70 passed, 0 failed, total 48.9s.

## Non-Claims

- No release, installed-machine `charness update`, scheduled CI, or live host
  proof was run for this bundle.
- GitHub issue state must be verified after the closeout carrier is pushed.
