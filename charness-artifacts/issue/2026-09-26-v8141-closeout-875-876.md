Closes #875
Closes #876

Critique #875 #876: charness-artifacts/critique/v8141-critique.md

Jtbd: task lanes must land without false out-of-scope flags after merging the
target branch, and an installed standalone charness entry must run every
subcommand including task run. Both issues blocked ordinary lane operation.

Root Cause: #875 computed lane changed paths from the task's original base
sha to the candidate, so merging an advanced main counted merged-in target
content as lane changes. #876 imported scripts.cli payloads in _load_feature
without first making the check-resolved checkout importable, so a standalone
copy with no packaging dir beside it died with ModuleNotFoundError scripts.

Debug Artifact: tests/charness_cli/test_task_run_scope.py carries the
merge-lane attribution cases, and
tests/charness_cli/test_standalone_entry_task_run_876.py carries the
standalone-copy payload case. Both run in the release lane green.

Siblings: for #875 the sibling search over changed_paths producers found the
carrier as the single attribution point and the changed-line reader as a pure
consumer, so the decision was to fix attribution in
scripts/task_run/task_run_carrier.py only, and the proof is that no other
producer exists in scripts/task_run. For #876 every entry dispatch funnels
through _load_feature in charness, so the decision was to retry there rather
than per command, and the proof is the single funnel at charness:1211.

Prevention: the PATH-independence gate in scripts/gates/check_path_independence.py
runs the task_run surface under a minimal PATH in the local gate graph and CI
mirror, and the two regression tests pin the fixed behaviors.

Behavior #875: verified via the merge-lane attribution regression tests in
tests/charness_cli/test_task_run_scope.py plus the green release lane.
Behavior #876: verified via the standalone-copy payload test in
tests/charness_cli/test_standalone_entry_task_run_876.py plus the green
release lane.

Probe record #875: local-only-by-contract — the verification ran locally
through committed regression tests and the release battery, which is the
evidence this surface accepts.
Probe record #876: local-only-by-contract — the verification ran locally
through the standalone-copy test and the release battery, which is the
evidence this surface accepts.

AI-provenance: authored by the maintainer's coding agent in the release
session and reviewed by a fresh-eye subagent reviewer before publish.
