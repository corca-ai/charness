fix: preserve quality adapter intent during bootstrap

Closes #507
Classification: bug
Jtbd: Maintainers need quality adapter bootstrap to preserve an existing consumer-owned adapter and its normalized intent, comments, deliberate absences, and custom commands unless an explicit migration is requested.
Boundary: `skills/public/quality/scripts/bootstrap_adapter.py` owns the operator-facing lifecycle boundary; `scripts/quality_bootstrap_lib.py` owns state construction; `scripts/quality_bootstrap_lifecycle.py` owns no-op, conflict, advisory, and migration authorization; source/plugin parity remains a packaging boundary.
Resolution Brief: Default bootstrap is now an unchanged no-op when normalized intent matches, a byte-preserving conflict with requested changes and next actions when semantic intent differs, and an explicit `--migrate` rewrite that retains comment text and reports migration changes. Unsafe output/report aliases and uninterpreted YAML are refused before writes.
Implementation: Moved lifecycle authorization into `scripts/quality_bootstrap_lifecycle.py`, made YAML comment parsing quote-aware for mapping and list values, added the `--migrate` CLI contract and operator guidance, synchronized the checked-in plugin, and split focused lifecycle tests from absence/refill tests.
Root Cause: The bootstrap caller treated every generated difference as an authorized update, while the renderer discarded comments and the YAML loader could silently omit uninterpreted lines; the generic write planner detected only byte difference and had no consumer-owned authorization policy.
Debug Artifact: charness-artifacts/debug/2026-08-05-debug-review-followup.md
Siblings: #481 decision: retain its deliberate-absence repair as a separate predecessor and close the existing-adapter rewrite gap here; proof: explicit absence remains covered by the original absence suite and #507 adds byte-stable conflict/migration coverage. Markdown-preview bootstrap decision: keep its generated-writer lifecycle separate; proof: the duplicate ratchet records the parallel boundary as intentional and both focused consumers remain green. Quality-runner receipt decision (#502): keep runner-summary ownership separate from adapter lifecycle; proof: the existing receipt-focused suite and current broad gate remain green.
Prevention: Existing adapters must default to no write when normalized intent is unchanged or conflicting; only `--migrate` may rewrite a conflict. Future bootstrap writers must reject uninterpreted YAML and path aliases before writing, preserve comments through an explicit migration report, and carry focused source/plugin parity and consumer-fixture tests.
Critique #507: charness-artifacts/critique/2026-08-05-issue-507-resolution-critique.md
Behavior #507: Confirmed through the distinct locked closeout channel — the repository closeout completed its sync, packaging, public-skill, duplicate-ratchet, broad 7,126-test, and mutation-coverage phases, while the seeded CLI fixture matrix separately read back unchanged, conflict, and migration outcomes; this is distinct from the implementation-focused bootstrap test channel.
AI-provenance: Agent-authored direct-commit carrier; causal debug, implementation contract, quality review, two bounded critique rounds with clean boundary fingerprints, synchronized probe records, deterministic gates, and the locked closeout evidence are recorded in the referenced artifacts.
