# Resolution Brief — corca-ai/charness#439

Classification: deferred-work

**Reporter JTBD**: Stop the 200-line-cap pinning trap on high-traffic public
skills so the next behavioral edit expresses the change instead of fighting the
cap — starting with the worst re-grower, `impl` (184→195 across four commits).

**Proposed boundary**:

- in scope: one P2 split-or-delete slice on `skills/public/impl/SKILL.md`
  (currently 195/200 lines), plus the plugin-mirror sync, skill validators, and
  ownership-overlap check the issue names as verification.
- out of scope: `spec` (194), `critique` (194), `announcement` (194) — the
  issue's own weak direction says decide their shape only after the `impl`
  slice lands; batching all four blind is explicitly rejected.

**Non-goals**:

- No prose shaving and no displacement into `references/` — both are the P2
  violations this issue exists to prevent.
- No behavior change to the impl workflow itself; this is a concept-surface
  reorganization.

**Concept analysis (for the split option)**: `impl` currently carries two
separable concepts:

1. *Build the slice* — contract ingest, honest-contract upkeep, smallest
   meaningful unit (workflow steps 1–3, bootstrap, adapter policy).
2. *Prove and close the slice* — verification ladder, truth-surface sync, stop
   gate/critique binding, closeout vocabulary and output shape (workflow steps
   4–7, `## Output Shape`, `## Closeout Vocabulary`, `## Guardrails` — roughly
   75 body lines).

The two concepts are also consumed at different times (while coding vs at
closeout), so a split matches progressive-disclosure timing, not just line
counts.

**Acceptance checks**:

- `impl/SKILL.md` lands with real headroom (not re-pinned within 5% of cap).
- `python3 scripts/validate_skills.py` passes; skill ownership-overlap check
  passes; plugin mirror synced.
- The resolution records the decision for the remaining three skills
  (follow-up or explicit defer), so the backlog framing of the issue is
  dispositioned, not dropped.

**Open decisions**:

- Split vs delete: split the prove-and-close concept into its own public skill
  surface, or delete overflow concepts outright (e.g. `## Worktree Readiness`,
  `## Continuation Default`) which yields only ~15 lines and does not change
  the re-growth dynamic.
- If split: the new skill's name and routing description (it will be loaded at
  slice-closeout time by the same agent that ran `impl`).
- Close scope: close #439 after the `impl` slice with the remaining three
  routed to a follow-up, or keep #439 open as the backlog tracker.

**Operator decisions (2026-07-16, in-session)**: split, new skill named
`prove` (boundary discussion: prove = slice-scoped reversible-work closeout
ledger; quality/hotl/issue/release/critique boundaries recorded in the prove
SKILL.md concept block). Close scope: close after the `impl` slice with a
follow-up issue for `spec`/`critique`/`announcement`. Vocabulary
mechanization into a describe-first script was dropped for this slice: the
portable-authoring rule requires emittable tokens verbatim in a core
`## Closeout Vocabulary` H2, so single ownership in `prove` is the dedup
mechanism instead.

**Autonomous vs pause**: paused; resumed after the operator decisions above.
