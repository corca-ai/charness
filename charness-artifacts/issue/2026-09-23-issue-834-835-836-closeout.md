Closes #834
Closes #835
Closes #836

Classification #834: bug
Classification #835: bug
Classification #836: bug

Critique #834 #835 #836: charness-artifacts/critique/task-run-834-836-release-critique.md

JTBD: Task-run lanes that hit a scope mismatch after editing must keep their
tested candidate for rescope instead of reverting it; contract-heavy lanes need
a visible per-lane no-progress budget instead of an env-only kill; new-directory
scopes expressed as globs must warn like unmatched exact scopes instead of
failing at preflight.
Boundary: Post-edit scope mismatch never restores scoped files and emits the
typed `scope mismatch - real owner` request; a zero-match glob warns
(`unmatched-glob-scope`) and admits only pattern-matching lane-created paths on
refresh with frozen directory matches; `--no-progress-seconds` accepts finite
`>= 0` (`0` disables) and is recorded in `progress_guard`.
Resolution brief: split pre-edit/post-edit carrier directives; record
`candidate-self-reverted` with a first-scoped-diff recovery pointer when an
EDITING lane ends BLOCKED unchanged; plumb `--no-progress-seconds` from CLI
through plan, attempts, and guard receipt (`budget_source`); return empty-glob
specs instead of raising and warn them in scope closure.
Root cause: unconditional pre-edit scope directive read as undo-after-edit
(#834); env-only no-progress budget treating prompt-named contract reading as
no progress (#835); asymmetric unmatched-scope handling, fatal for globs while
warning-only for exact paths (#836).
Debug artifact: none (carrier defects reproduced from issue reports against the
lane code; pinned by new behavior tests).
Siblings: decision = bundle #834 + #835 + #836 as one task-run repair release
and remove none; proof = all three are lane-carrier defects in scripts/task_run/
with per-issue behavior tests, so one release carries the complete fix;
population = 3; removed = 0; deferred = dry-run scope-warning reachability on
dirty trees (critique Valid-but-Defer follow-up).
Implementation: scripts/task_run/{task_run_scope,task_run_plan,task_run_progress,task_run_attempts,task_run_lane_runner,task_run_completion,task_run}.py,
root launcher task-run parser, docs/cli-reference.md, tests
test_task_run_scope.py (warn-and-admit), test_task_run_scope_closure_831.py
(unchanged), test_task_run_implementation_lane.py (flag override/validation,
prompt split, self-revert truth table).
Prevention: Zero-match-glob admission pinned by warn-and-admit tests;
flag precedence and validation pinned by override/rejection tests; self-revert
shape pinned by backstop unit tests and the changed-line gate; ruff complexity
and statement caps hold on the touched modules.
Behavior #834: Confirmed via behavior test channel —
test_post_edit_scope_mismatch_keeps_candidate_and_types_request and
test_self_revert_backstop_flags_editing_blocked_unchanged pass on the shipped
tree, pinning the keep-candidate directive and the EDITING+BLOCKED+unchanged
detection (pre-edit BLOCKED stays unflagged).
Behavior #835: Confirmed via behavior test channel —
test_no_progress_flag_overrides_env_and_records_source and
test_no_progress_seconds_rejects_nonfinite_and_negative pass on the shipped
tree, pinning flag-over-env precedence, receipt recording, and boundary
rejection.
Behavior #836: Confirmed via behavior test channel —
test_zero_match_glob_warns_and_admits_lane_created_paths and
test_zero_match_glob_without_lane_output_warns_instead_of_preflight pass on the
shipped tree, pinning warn-only scope_warnings and refresh admission of
lane-created matching paths.
AI-provenance: Authored by an agentic coding session (Muse Code) from behavior
tests and the task-run-834-836 critique; a human operator reviews before
publication.
