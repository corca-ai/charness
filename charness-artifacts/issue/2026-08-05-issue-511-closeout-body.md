Closes #511
Classification: bug
Jtbd: Maintainers need the quality nose inventory to disclose which roots were requested, actually scanned, and missing, so an optional default layout cannot be reported as a completed clone measurement.
Boundary: `skills/public/quality/scripts/inventory_nose_clones.py` owns inventory scope and query receipts; `nose_inventory_scope_lib.py` owns root resolution and status classification; the quality adapter owns consumer-provided roots; `run-quality.sh` owns measured receipt aggregation; source/plugin parity remains a packaging boundary.
Resolution Brief: Optional default roots are filtered and disclosed, adapter roots are validated and resolved before the query, explicit invalid or absent scope is inapplicable or blocked with a non-success exit, partial scans refuse a baseline, and the quality runner counts only measured PASS phases in its completion receipt.
Implementation: Added the scope-resolution and receipt library, wired adapter-owned `nose_inventory_paths`, preserved explicit-path and monkeypatched query seams, added path-traversal and Windows-root rejection coverage, split runner scope tests, synchronized the plugin export and operator/config references, and refreshed the D47 companion probes after the inventory declaration changed. Coverage repairs are in `e16e05037faea6beef894b311ad1528d52654131`; the final quality-record update is in `0a02d940e6622742c8ef494ab4147e75b06b44e4`.
Root Cause: The inventory entrypoint unconditionally passed its Charness-specific default roots to `nose`, collapsed missing-root and query failures into an empty successful payload, and the runner treated every phase label as measured even when the producer had not established a measurement.
Debug Artifact: charness-artifacts/debug/2026-08-05-debug-review-followup-2.md
Siblings: #507 decision: keep adapter lifecycle authorization separate from inventory scope resolution; proof: the bootstrap lifecycle suite and this slice's scope matrix remain independently green. #502 decision: keep quality-runner receipt aggregation separate from producer query status; proof: `test_quality_runner_nose_scope.py` proves only measured PASS phases enter the receipt while producer tests own scope statuses. D47 decision: retain the marker rule unarmed and refresh its companion probes rather than rewriting frozen artifacts; proof: the 60-test probe suite and both recorded JSON reports reproduce the current corpus.
Prevention: Every filesystem-backed inventory must resolve and report requested/scanned/missing scope before claiming counts; absent optional roots must be non-measurements, query failures must carry a distinct exit/status pair, partial scans must not write or compare a baseline, and consumers must aggregate only measured PASS receipts. Adapter path validation and source/plugin parity remain deterministic gates.
Critique #511: charness-artifacts/critique/2026-08-05-issue-511-resolution-critique.md
Behavior #511: Confirmed through the distinct verification-locked closeout channel — the broad repository suite passed under fresh mutation instrumentation, all packaging/integration/probe gates passed, the resolved-base changed-line consumer proved 5/5 eligible files with `blocking: []`, the local pre-push gate passed 85 checks, and remote Quality Core run [30996843171](https://github.com/corca-ai/charness/actions/runs/30996843171) completed successfully for `0a02d940e6622742c8ef494ab4147e75b06b44e4`. This verdict is not the focused scope-test channel that produced the implementation.
AI-provenance: Agent-authored closeout body; causal debug, living implementation contract, quality review, two bounded fresh-eye rounds with clean boundary fingerprints, repaired-surface acceptance under the review cap, synchronized probes, deterministic gates, resolved-base changed-line proof, pre-push verification, remote CI readback, and the verification-locked closeout are recorded in the referenced artifacts.
