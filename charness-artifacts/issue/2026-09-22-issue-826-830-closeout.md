Closes #826
Closes #827
Closes #828
Closes #829
Closes #830

Critique #826 #827 #828 #829 #830: charness-artifacts/critique/v898-critique.md

JTBD: #826 — a lane whose candidate deletes tracked files must still checkpoint so the work stays reviewable. #827 — operators need an inspect-without-changing lane free of require-change and writer-conflict gates. #828 — goal runs must distinguish hard edges from conflict-avoidance, hold a parallel frontier, and amend edges mid-run. #829 — a transient model-stream idle stall must retry, and result.json must separate a crash from a finished lane needing scope review. #830 — a lane change silently discarding persisted data must never pass as completed.
Root Cause: #826 — the checkpoint stager assumed additions/modifications only and dropped deletions. #827 — every lane forced require-change semantics with no report-only shape. #828 — one untyped edge shape, a single-point cursor, and no amendment kinds. #829 — no stall failure kind and no retry path; crash and needs-review shared one outcome. #830 — no diff scan for destructive persistence statements.
Debug Artifact: tests/charness_cli/test_task_run_checkpoint_deletions.py, test_task_run_report_only.py, test_task_run_829_lane_resilience.py, test_task_run_persistence_lens.py, tests/quality_gates/test_goal_828_dependency_frontier.py, tests/quality_gates/test_issue_goal_run_binding.py, plus the release lane log at /tmp/release-lane14.log (89 passed).
Siblings: Decision — bundle-close #826-#830 in v8.9.8, each fixed at its own layer and verified on the shipped tree. Proof — every per-issue behavior test listed under Debug Artifact passes on the shipped tree and the full release lane passes 89 tests with zero failures.
Prevention: #826 — deletion cases pinned by checkpoint tests and the changed-line gate. #827 — the report-only contract pinned by tests and recorded in docs/agent-task-runs.md. #828 — typed edges, frontier, and amendment kinds pinned by contract tests on the achieve and issue sides. #829 — stall classification, retry accounting, and the needs-review disposition pinned by resilience tests; every receipt carries failure kind, retryable, and message. #830 — the fail-closed lens blocks added and removed destructive statements (unreadable diff blocks), pinned by lens tests; the green-to-red change is named in docs/agent-task-runs.md.
Behavior #826: Confirmed via behavior test channel — test_task_run_checkpoint_deletions.py passes on the shipped tree, exercising delete-including candidates end to end.
Behavior #827: Confirmed via behavior test channel — test_task_run_report_only.py passes on the shipped tree, delivering a stdout report with no change required.
Behavior #828: Confirmed via behavior test channel — test_goal_828_dependency_frontier.py and test_issue_goal_run_binding.py pass on the shipped tree, exercising typed edges, frontier hold, and mid-run amendment.
Behavior #829: Confirmed via behavior test channel — test_task_run_829_lane_resilience.py passes on the shipped tree, exercising idle-stall retry and the crash versus needs-review split.
Behavior #830: Confirmed via behavior test channel — test_task_run_persistence_lens.py passes on the shipped tree, blocking DROP, TRUNCATE, and unscoped-DELETE candidates while advisory shapes stay non-blocking.
AI-provenance: This carrier was authored by an agentic coding session (Muse Code) from the shipped tree's behavior tests, lane receipts, and the v8.9.8 release critique; a human operator reviews the release record before publication.
