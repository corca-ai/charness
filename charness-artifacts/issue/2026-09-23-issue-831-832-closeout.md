Closes #831
Closes #832

Critique #831 #832: charness-artifacts/critique/v8100-critique.md

JTBD: #831 — a lane that discovers a coherent behavior seam outside its declared scope must finish with a reviewable disposition instead of burning a full model run and forcing manual prompt/scope reconstruction and relaunch. #832 — secret environment values handed to a task-run lane must not reach any surface where any local process can read them.
Boundary: #831 — warning-only dispositions plus an operator-approved same-candidate rescope; no automatic scope expansion, no mid-run write preemption, refresh never admits lane-created directories. #832 — lane child env only; explicit user commands (`worktree exec`) and executor auth keeps are out of the scrub.
Resolution brief: #831 — preflight `scope_warnings`, typed `scope_extension_request` parsed from the lane's scope-mismatch blocker, and `rescope_result` re-validation. #832 — secret-pattern env names dropped from the lane child env except per-executor auth keeps and `CHARNESS_TASK_RUN_KEEP_SECRET_ENV`.
Implementation: #831 — scripts/task_run/{scope,plan,py,lane_runner,completion_next_step,support}.py, docs/agent-task-runs.md, tests/charness_cli/test_task_run_scope_closure_831.py (commits a7dff71c0, 331b0200f). #832 — scripts/task_run/{runtime,py,support}.py, docs/agent-task-runs.md, tests/charness_cli/test_task_run_lane_env_832.py (commit 373d6093e).
Prevention: #831 — frozen directory matches, unmatched-literal warnings, and the typed extension request pinned by closure tests and the changed-line gate. #832 — the scrub pinned by lane-env tests; the receipt records scrubbed/kept names, never values.
Behavior #831: Confirmed via behavior test channel — test_task_run_scope_closure_831.py passes on the shipped tree, exercising preflight warnings, extension-request parsing, and same-candidate re-validation without relaunch.
Behavior #832: Confirmed via behavior test channel — test_task_run_lane_env_832.py passes on the shipped tree, exercising secret-name drops, executor auth keeps, the operator override, and a names-only receipt through a live lane run.
AI-provenance: This carrier was authored by an agentic coding session (Muse Code) from the shipped tree's behavior tests, lane receipts, and the v8.10.0 release critique; a human operator reviews the release record before publication.
