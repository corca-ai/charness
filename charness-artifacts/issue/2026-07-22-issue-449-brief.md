# Resolution Brief — corca-ai/charness#449

**Classification**: feature

**Reporter JTBD**: After publication, a release operator needs a durable public
release readback from a machine- and credential-distinct observer.

**Proposed boundary**:

- In scope: a CI-side release observer and its discoverable durable record.
- Out of scope: changing the existing publisher-host observer or treating a CI
  success state as terminal behavior proof.

**Non-goals**:

- Releasing or publishing this change in this slice.

**Acceptance checks**:

- The selected workflow trigger, record destination/retention, and cross-link
  contract are explicit and testable.
- A release closeout can discover both the local and machine-distinct records.

**Open decisions**:

- Trigger: GitHub `release` event versus a post-publish dispatch.
- Record destination and retention: workflow artifact, release asset, or a
  repository-backed record.
- Cross-link direction: how the immutable local observer record and later CI
  record reference one another.

**Autonomous vs pause**: pausing for user discussion because open decisions are
non-empty.

AI-provenance: agent-drafted pause state for issue #449; no resolution or
closeout is claimed.
