Closes #504
Classification: bug
Jtbd: An achieve closeout must reject a wrong, missing, or malformed owning-goal identity before the retro artifact or any derived summary, lesson index, event, or output-directory write occurs, while ordinary session and release retros remain goal-free.
Boundary: `scripts/retro_persistence_lib.py` owns goal-aware identity validation and pre-write ordering; the achieve/retro contract supplies `--goal-path` for goal closeout; release and ordinary session callers intentionally omit it; final achieve evidence binding remains defense in depth.
Resolution Brief: Add an opt-in `--goal-path` contract at the shared persistence write owner. Require exactly one matching top-level `Goal:` field, canonicalize successful slug input to the repo-relative goal path, refuse mismatches before every side effect, and preserve the legacy goal-free path.
Implementation: Landed in `9768f95dcde783fd128e3ad21fb8f6d2973760c1` (`fix: bind retro persistence to owning goals`) with defensive coverage in `c655e9aa006c70ec1c5846b3ab32175b8ceb0f08`. The change adds library and CLI goal identity transport, exact preamble parsing, canonical output, full-tree no-write coverage, achieve/retro caller guidance, final-consumer binding alignment, synchronized source/plugin mirrors, and current focused proof of 115 tests.
Root Cause: The shared writer owned the first local writes but had no goal-aware contract; achieve could discover a wrong-owner retro only in its later evidence consumer, after the retro and derived state had changed. A universal requirement would incorrectly break intentional session and release persistence, so the repair makes goal awareness explicit at the owning boundary.
Debug Artifact: charness-artifacts/debug/2026-08-04-debug-review-followup-3.md
Siblings: Session/release decision: remain goal-free; proof: `test_goal_aware_library_accepts_exact_slug_and_legacy_mode_stays_goal_free` exercises goal-aware canonicalization and the omitted-flag legacy path, and `publish_release_retro.py` remains an intentional direct library caller without `goal_path`. Achieve-consumer decision: retain final evidence binding as defense in depth rather than moving the write owner back to the reader; proof: `goal_artifact_closeout_evidence.py` still derives and checks goal tokens while the shared writer rejects before writes.
Prevention: Goal closeout instructions in `skills/public/achieve/SKILL.md`, `skills/public/retro/SKILL.md`, and `trigger-and-persistence.md` require the canonical `--goal-path` and one matching top-level `Goal:` field; the shared library validates before artifact, summary/index, event, or output-directory writes; focused direct-library/CLI, malformed-identity, no-write, canonicalization, and legacy tests plus exact source/plugin parity protect the contract.
Critique #504: charness-artifacts/critique/2026-08-05-issue-504-resolution-closeout-critique.md
Behavior #504: local-only-by-contract — the distinct focused test channel (`python3 scripts/run_standing_pytest.py --repo-root . --mode read-only --pytest-target tests/quality_gates/test_retro_persistence.py --pytest-target tests/quality_gates/test_goal_artifact_lib.py --pytest-target tests/quality_gates/test_goal_disposition_gate.py`) passed 115 tests and exercises matching identity, wrong/missing/malformed refusal before writes, canonicalization, and goal-free session behavior. This does not claim live-agent/installed-host invocation, provider behavior, or that every future agent supplies `--goal-path`; those are outside the local contract.
AI-provenance: Agent-authored direct-commit closeout carrier; the live issue read, causal review, implementation history, focused proof, synchronized source/plugin checks, delegated four-lens resolution critique with clean boundary fingerprints, and the final GitHub adapter readback are recorded in the referenced artifacts and commands.
