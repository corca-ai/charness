# Achieve Goal: Resolve Charness issues 294-298

Status: active
Created: 2026-06-04
Activation: `/goal @charness-artifacts/goals/2026-06-04-issue-294-298.md`

This file is the living goal scratchpad. It becomes active only when the user
runs the activation command.

## Active Operating Frame

- Current slice: Slice 3 - #295 closeout test-selection cost policy.
- Next action: design and implement #295 phase-sensitive closeout runner
  behavior using the Slice 2 verification cadence as the cost boundary.
- Verification cadence: issue-classification and focused current-diff proof
  before each slice locks; `run_slice_closeout.py --verification-lock` at final
  closeout; no pre-lock broad pytest rerun unless changed-surface evidence says
  the focused set is insufficient.
- Slice review packet: before fresh-eye slice critique, provide intent, changed
  files and owning/generated surfaces, expected invariants, tests/proof,
  non-claims, out-of-scope lines, and reviewer questions.
- History boundary: keep this frame current; move completed detail to
  `## Slice Log`, `## Final Verification`, and `## Auto-Retro`.

## Goal

Resolve the current Charness issue band #294-#298 as one auditable maintenance
goal:

- close-intended targets: #294, #295, #297, and #298;
- context-only target: #296, already closed before this goal was shaped;
- result shape: focused repo changes, synchronized generated/export surfaces
  when touched, deterministic validation, natural carrier commits with close
  keywords, direct push closeout verification, final critique, and retro
  dispositions.

## Non-Goals

- Do not reopen, relitigate, or re-close #296; use it only as the predecessor
  context for #297.
- Do not open a PR for this goal; this machine owns the direct local work and
  push path.
- Do not manually close GitHub issues unless auto-close verification fails after
  the pushed carrier commit. Manual close-with-comment is fallback only.
- Do not claim installed-host, live Cautilus, or production proof unless the
  active run explicitly routes and records that proof.
- Do not absorb unrelated Ceal follow-ups or new sibling findings into this
  bundle; file them through `issue` or record them as explicit non-goals.
- Do not run broad pytest before every narrow slice merely because final
  closeout will need broad verification.

## Boundaries

- Repository target: `corca-ai/charness` from `origin`; issue backend:
  adapter-selected `gh`.
- Scope is intentionally bundled because the user selected the inclusive
  294-298 issue band; split only if the initial classification/brief step finds
  a product decision, host permission, or implementation conflict that blocks a
  coherent bundle.
- Bug-class candidates (#294 and #298) need causal/root-cause review before the
  fix slice. Feature/deferred-work candidates (#295 and #297) need a
  pre-mutation resolution brief; pause if the brief exposes open decisions.
- #294 may touch `debug`, `issue`, RCA/debug artifact validators, or their
  references. It must preserve legitimate diagnostic-only entries while adding a
  narrow hard validator floor for abstraction-up `diagnostic-only` entries that
  describe unresolved repo-level/deferred structural work without proof/reason
  or a follow-up issue/handoff anchor.
- #295 may touch quality/closeout policy, runner output, changed-surface
  planning, or tests. It must make `run_slice_closeout.py` phase-sensitive:
  final broad closeout refuses without `--verification-lock`; pre-lock broad
  proof remains possible but carries focused-proof recommendation and cost
  warning.
- #297 may touch critique/review evidence, adapter tier resolution, spawn-field
  recording, and validators. It must record requested reviewer tier plus host
  exposure state; use `applied` only when the host confirms it, otherwise record
  states such as `requested_fields_sent`, `metadata-hidden`, `unsupported`, or
  `host-defaulted`.
- #298 may touch handoff chunker merge-hint generation, policy consumption,
  ranking/proposal packets, issue closeout publication wording, achieve/retro
  closeout guidance, and tests. It must honor adapter broad-token policy without
  hardcoding Ceal label semantics, and it must separate issue-resolution carrier
  publication from later lifecycle/audit artifact publication.
- #298a script contract: scripts provide facts, never clearance. A false
  `broad_only_overlap` signal is not proof that a merge is good; unknown tokens
  mean the policy has no opinion; semantic fit, urgency, dependency, and operator
  value remain agent judgment.
- #298b helper/preflight scope is conditional: start with policy/reference
  wording; add a small advisory helper only if implementation shows repeated
  manual publication-boundary classification remains ambiguous.
- Generated/plugin/export mirrors touched by any slice must sync before
  validators.

## User Acceptance

- GitHub issue #294, #295, #297, and #298 each have a clear resolution ledger in
  the natural carrier commit that fixes them. A carrier may close one or more
  issues, but it must only include `Close #N` for issues actually resolved by
  that commit.
- GitHub issue #296 remains closed and is referenced only as prior context.
- No PR is used; direct push of carrier commits triggers GitHub auto-close, then
  `issue_tool.py verify-closeout --expect-state CLOSED` verifies issue state.
- The user can inspect the final goal artifact and see slice evidence,
  closeout proof, residual risks, non-claims, and retro dispositions without
  reconstructing the session transcript.
- The user can run the documented focused tests and final closeout gate from a
  clean checkout at the closing commit and get the same pass/fail posture.

## Agent Verification Plan

### Low-Cost Checks

- `python3 /home/hwidong/.codex/plugins/cache/local/charness/0.17.0/skills/issue/scripts/issue_tool.py resolve-invocation --repo-root . -- 294-298`
- `python3 scripts/check_changed_surfaces.py --repo-root .`
- Focused pytest for the touched helper/validator modules after each slice.
- Focused artifact/markdown validators for any changed `charness-artifacts/`
  proof files.
- `python3 scripts/check_skill_surface_preflight.py --repo-root . --path <skill-or-reference> --preview-delta <planned-lines>` before adding public skill prose.

### High-Confidence Checks

- For bug-class slices, a `debug`-style causal review or bound RCA artifact
  before mutation, plus sibling disposition evidence in the slice log.
- For feature/deferred-work slices, an issue-resolution brief before mutation
  and no unresolved open decisions before implementation.
- `python3 scripts/run_slice_closeout.py --repo-root . --skip-broad-pytest` as
  a pre-lock rehearsal when a slice spans generated surfaces or multiple
  validator families.
- `python3 scripts/run_slice_closeout.py --repo-root . --verification-lock` at
  final closeout after the mutation set and generated mirrors are locked.
- Standalone `critique` fresh-eye review for substantial slice bundles and
  final closeout, using the adapter-declared reviewer tier when the host exposes
  those fields.

### External Or Live Proof

- Real GitHub issue state was read at shaping through the adapter-selected
  `gh` backend; during the active run, refresh issue state before closeout.
- Direct push is part of this goal after local verification locks and each
  natural carrier commit carries the appropriate close keyword(s). After push,
  run issue closeout verification against GitHub state.
- PR proof is not planned. Manual issue close is fallback only after pushed
  carrier auto-close verification fails.
- No live Cautilus or installed-host proof is planned; if a slice proposes it,
  route through `quality` and the repo-owned Cautilus planner first.

## Slice Plan

| Slice | Objective | Why Now | Expected Evidence | Status |
| --- | --- | --- | --- | --- |
| 1 | Classify #294/#295/#297/#298, bind #296 as context only, and choose natural carrier commit units. | Prevent the bundle from bypassing issue-skill bug RCA, feature brief, carrier closeout, or split rules. | `issue_tool.py resolve-invocation`; refreshed `gh issue view` state including #298 comment; #294/#298 separate debug RCA plan; #295/#297 briefs; natural carrier plan; test pressure: none. | complete |
| 2 | Resolve #294 abstraction-up diagnostic-only disposition floor. | It hardens bug-class RCA closeout and may influence later issue closeout wording. | Separate #294 debug RCA; updated debug/issue guidance and narrow hard validator; focused tests for RCA/closeout parsing; carrier commit includes `Close #294` if complete. | complete |
| 3 | Resolve #295 closeout test-selection cost policy. | It shapes the verification cadence for the remaining slices and final lock. | `run_slice_closeout.py` final broad refusal without `--verification-lock`; pre-lock focused-proof recommendation/cost warning; focused tests avoid live-issue coupling; carrier commit includes `Close #295` if complete. | planned |
| 4 | Resolve #297 reviewer-tier evidence surface. | It closes the narrower follow-up left after #296 and supports required fresh-eye review evidence. | Requested tier plus host exposure state recorded by critique or sibling workflows; validator/regression proves adapter tier config is consumed or honestly reported; carrier commit includes `Close #297` if complete. | planned |
| 5 | Resolve #298a handoff broad-token merge-hint facts contract. | It fixes the original handoff chunker bug while preserving agent judgment. | Deterministic scripts expose broad/unknown token facts without clearance; validator rejects agent high-confidence proposals that ignore strong warnings; focused handoff tests cover false-negative guardrails without Ceal hardcoding. | planned |
| 6 | Resolve #298b carrier vs lifecycle artifact publication guidance. | The new #298 comment adds closeout waste from unnecessary docs-only second pushes. | `issue`, `issue/references/closeout-discipline.md`, `achieve`, and `retro` wording separate resolution carrier push from later lifecycle/audit artifact publication; helper added only if conditional scope triggers; carrier commit includes `Close #298` when both #298a/#298b are complete. | planned |
| 7 | Sync, verify, critique, push carriers, and verify issue closeout. | The bundle is only complete when proof, generated mirrors, carrier close keywords, direct push, GitHub state verification, critique, and retro dispositions are coherent. | Synced generated/export surfaces; `run_slice_closeout.py --verification-lock`; `issue_tool.py validate-closeout-draft`; direct push; `issue_tool.py verify-closeout --expect-state CLOSED`; final critique; retro with dispositions; final artifact status flip. | planned |

## Coordination Cues

Phase-appropriate routing for this run, deferred to `find-skills` (its
`--recommend-for-task` / `--recommendation-role --next-skill-id` recommendation
engine) — never a hard-coded phase-to-skill list here. `achieve` owns this slot
and the floors below; `find-skills` owns *which* skill answers a boundary. Fill
during the run:

- **Routing** — ask `find-skills` to recommend the skill for the current phase or
  boundary, and record the route it returns.
- **Gather step** — when `## Context Sources` names an external source
  (URL / Slack / Notion / Docs / Drive), add a `Gather:` line here pointing at the
  gathered asset, or write `Gather: n/a — <reason>` when no external context
  applies.
- **Release step** — when this run touches a release surface (a version bump or
  install-manifest edit), add a `Release:` line here pointing at the release
  proof, or write `Release: n/a — <reason>`.
- **Issue closeout step** — when this goal resolves tracked GitHub issues, add
  an `Issue closeout:` line naming the close-intended issue numbers, direct
  commit carrier(s), per-carrier close keywords, and
  `issue_tool.py validate-closeout-draft` / `verify-closeout` proof. If a
  tracked issue appears in `## Context Sources` as context only, use
  `Issue closeout: n/a — <reason>`.

- Routing: initial `find-skills` recommendation selected public skill
  `achieve`; public skill `issue` is the companion workflow for GitHub source
  of truth, issue classification, and closeout proof.
- Gather: n/a - GitHub issues are tracked issue sources handled through the
  adapter-selected `issue` backend; no Slack/Notion/Docs/Drive or arbitrary URL
  source is being converted into a separate gathered asset.
- Release: n/a - no version bump or install-manifest release surface is planned;
  reroute through `release` if an active slice touches release packaging.
- Issue closeout: planned for #294, #295, #297, and #298 through natural
  direct-commit carriers. Each carrier includes only the `Close #N` keywords for
  the issues it actually resolves; #296 is context-only. Run
  `issue_tool.py validate-closeout-draft` before push and
  `issue_tool.py verify-closeout --expect-state CLOSED` after the direct push.

Discuss before activation: CONFIRMED - the user selected the inclusive issue
band #294-#298; this goal treats #296 as already-closed context, bundles the
four open issues only if Slice 1 classification leaves no open decision, uses
natural direct-commit carriers with per-issue close keywords rather than PRs,
pushes from this machine after verification, and does not claim live installed
host or Cautilus proof.

## Slice Log

### Slice 1: Classification, RCA Substrates, And Carrier Plan

- Objective: Activate the goal, refresh GitHub issue state, classify #294-#298,
  bind #296 as context only, create bug-class debug substrates, run causal
  reviews, and decide natural carrier units before implementation.
- Why this approach: The `issue` resolve contract requires GitHub as source of
  truth, bug-class causal review before fix design, and feature/deferred-work
  briefs before mutation. Doing this first prevents the bundled goal from
  bypassing per-issue proof.
- Commits: none yet for code; this slice should commit active goal state plus
  debug artifacts only.
- What changed: Goal status flipped to `active`; added
  `charness-artifacts/debug/2026-06-04-294-abstraction-up-diagnostic-disposition.md`
  and
  `charness-artifacts/debug/2026-06-04-298-handoff-merge-hints-and-publication-boundary.md`.
- Classification:
  - #294: bug-class; #294 causal review confirmed the bug and bounded it to
    abstraction-up unresolved structural work, preserving ordinary bounded
    diagnostic-only entries.
  - #295: deferred-work / quality-evidence improvement. Resolution brief:
    reporter JTBD is to avoid paying pre-lock broad pytest for narrow slices
    while keeping final verification-lock proof; boundary is
    phase-sensitive runner behavior; open decisions: none; continue.
  - #296: context-only; already CLOSED and not a close target.
  - #297: deferred-work / evidence-surface improvement. Resolution brief:
    reporter JTBD is to distinguish requested reviewer tier from host exposure
    or application state; boundary is requested tier plus host exposure state,
    not provider-internal applied proof; open decisions: none; continue.
  - #298: bug-class; #298 causal review confirmed one workflow-boundary bug with
    two acceptance items: facts-not-clearance handoff merge evidence and
    carrier-vs-lifecycle publication guidance.
- Natural carrier plan:
  - Carrier A / #294: guidance + narrow validator + focused tests; include
    `Close #294` only if the carrier fully resolves #294.
  - Carrier B / #295: phase-sensitive `run_slice_closeout.py` policy/output and
    tests; include `Close #295` only if complete.
  - Carrier C / #297: reviewer-tier evidence state and tests; include
    `Close #297` only if complete.
  - Carrier D / #298: may be one or two commits depending on touched surfaces,
    but `Close #298` waits until both #298a and #298b are complete.
- Alternatives rejected: Reopening #296; using a PR carrier; forcing one commit
  per issue; claiming `broad_only_overlap: false` as merge clearance; creating
  the optional closeout publication helper before implementation proves it is
  needed.
- Targeted verification:
  - PASS: `check_goal_artifact.py --pursue-ready` on this goal before
    activation.
  - PASS: `issue_tool.py resolve-invocation --repo-root . -- 294-298`.
  - PASS: refreshed `gh issue view` for #294-#298; current states are #294,
    #295, #297, #298 OPEN and #296 CLOSED.
  - PASS: `validate_debug_artifact.py --repo-root .` after adding #294/#298
    debug artifacts.
  - PASS: #294 and #298 high-leverage causal reviews returned
    `Fresh-Eye Satisfaction: satisfied`.
- Test duplication pressure: none; no tests were added or expanded in Slice 1.
- Critique: #294 causal review consumed
  `charness-artifacts/debug/2026-06-04-294-abstraction-up-diagnostic-disposition.md`
  and recommended bundling guidance, validator, and focused tests while
  deferring historical artifact migration. #298 causal review consumed
  `charness-artifacts/debug/2026-06-04-298-handoff-merge-hints-and-publication-boundary.md`
  and recommended bundling handoff facts, carrier publication wording, and
  tests while deferring the optional helper unless ambiguity remains.
- Off-goal findings: none.
- Lessons carried forward: Treat script facts as evidence, never clearance; keep
  issue closeout proof tied to the resolution carrier; preserve debug learning
  in artifacts before mutation.
- Metrics: host token/time/tool-call totals unavailable; two high-leverage
  causal-review subagents were used for #294 and #298.

### Slice 2: #294 Abstraction-Up Diagnostic-Only Disposition Floor

- Objective: Resolve #294 by preventing abstraction-up sibling-search entries
  from using `same class, diagnostic-only for this slice` as false clearance for
  unresolved structural work.
- Why this approach: #294 is a bug-class RCA recurrence risk. A prose-only
  reminder would not stop the same closeout shape from passing again, but a
  narrow validator floor can reject the specific false-negative pattern while
  leaving semantic judgment with the agent.
- Carrier: direct-commit carrier for #294 planned from this slice; exact commit
  body is validated before commit and carries `Close #294`.
- What changed:
  - `scripts/artifact_validator.py` now treats sibling decision bullets as `-`
    or `*` markdown list items with a `decision:` field.
  - Abstraction-up entries marked `same class, diagnostic-only for this slice`
    must include a proof-backed no-action reason or a valid `follow-up:`
    identifier; unresolved structural wording without a follow-up fails.
  - Continuation scanning stops at the next `-` or `*` bullet, so a later
    sibling cannot accidentally supply the current entry's no-action reason.
  - `tests/test_debug_artifact.py` adds focused acceptance/rejection coverage,
    including ordinary same-layer diagnostic-only preservation and `*` bullet
    false-negative guards.
  - `skills/public/debug/references/sibling-search.md` and
    `skills/public/issue/references/causal-review.md` now state the
    abstraction-up diagnostic-only boundary.
  - Plugin mirror copies were synced under `plugins/charness/`.
  - `docs/public-skill-dogfood.json` records the debug/issue scenario review
    decision: maintained Cautilus scenarios stay unchanged; deterministic
    artifact-consumer coverage owns this semantic slice.
- Sibling disposition:
  - Same bug, fix now: debug artifact validator and public debug/issue sibling
    search guidance.
  - Diagnostic-only: ordinary same-layer diagnostic-only entries remain valid
    without a no-action reason, proven by focused regression coverage.
  - Deferred: historical artifact migration and semantic proof-quality scoring
    are not bundled; the validator is a minimum-structure floor, not a proof
    oracle.
- Targeted verification:
  - PASS: `pytest -q tests/test_debug_artifact.py` (`27 passed`).
  - PASS: `validate_debug_artifact.py --repo-root .`.
  - PASS: `python3 scripts/validate_critique_artifacts.py --repo-root .`.
  - PASS: `python3 scripts/check_duplicates.py --repo-root . --fail-on-match
    --require-git-file-listing` (`No duplicates found at threshold 0.98`).
  - PASS: `python3 scripts/check_python_lengths.py --repo-root . --paths
    scripts/artifact_validator.py tests/test_debug_artifact.py
    plugins/charness/scripts/artifact_validator.py`.
  - PASS: `python3 scripts/run_slice_closeout.py --repo-root .
    --skip-broad-pytest --ack-cautilus-skill-review`.
- Cautilus proof: planner reported `next_action: none`; scenario registry
  review was performed for public skills `debug` and `issue`, and deterministic
  validation owns this slice. No live Cautilus run is claimed.
- Skipped proof: pre-lock broad pytest was intentionally skipped by
  `run_slice_closeout.py`; final verification-lock proof remains reserved for
  the final mutation set.
- Test duplication pressure: focused tests expanded from 25 to 27 pass cases
  after critique found `*` bullet parser risk; duplicate scanner reported no
  near duplicates.
- Critique:
  - Packet:
    `charness-artifacts/critique/2026-06-04-111910-packet.md`.
  - Resolution critique:
    `charness-artifacts/critique/2026-06-04-issue-294-validator-closeout.md`.
  - Fresh-eye result: no Act Before Ship blockers. One Bundle Anyway parser
    concern around `*` bullets was folded into code and tests before closeout.
    Counterweight confirmed the integrated fix was sufficient and that broader
    close-ledger semantic parsing is over-worry for this slice.
- Non-claims: The validator does not prove semantic adequacy, urgency,
  dependency, value, or whether a closeout is safe. It only rejects a narrow
  structural false-clearance shape; the agent must still read the artifact and
  judge the proof.

## Context Sources

- `docs/handoff.md` current state: #295 and #296 were follow-ups from the
  portable skill closeout; #184, #293, #294 remain outside that completed goal.
- `charness-artifacts/retro/recent-lessons.md`: broad pytest cost, fresh-eye
  evidence, repeated VCS/check overhead, and stale live-issue-coupled tests are
  active lessons for this run.
- GitHub issue #294, OPEN:
  `https://github.com/corca-ai/charness/issues/294` - abstraction-up
  diagnostic-only same-class siblings need durable disposition.
- GitHub issue #295, OPEN:
  `https://github.com/corca-ai/charness/issues/295` - make closeout test
  selection cost explicit.
- GitHub issue #296, CLOSED:
  `https://github.com/corca-ai/charness/issues/296` - skill-specific subagent
  model/reasoning defaults, predecessor context for #297 only.
- GitHub issue #297, OPEN:
  `https://github.com/corca-ai/charness/issues/297` - record requested and
  applied reviewer-tier evidence.
- GitHub issue #298, OPEN, label `bug`:
  `https://github.com/corca-ai/charness/issues/298` - handoff chunker merge
  hints should honor broad-token adapter policy and preserve agent judgment.
- GitHub issue #298 comment, 2026-06-04:
  `https://github.com/corca-ai/charness/issues/298#issuecomment-4621505959` -
  carrier-vs-lifecycle publication boundary should prevent unnecessary
  docs-only second pushes after verified issue-resolution carriers.
- Critique prepare packet:
  `charness-artifacts/critique/2026-06-04-103148-packet.md`.

## Interview Decisions

- Mode family: artifact-only goal draft vs implementation continuation.
  Chosen: Before-phase draft now; implementation starts only after explicit
  `/goal` activation. Rejected: starting slices immediately from
  `$charness:achieve`, because `achieve` shapes and `/goal` pursues.
- Issue selector family: resolve every number literally vs preserve current
  GitHub state. Chosen: #294/#295/#297/#298 are close-intended open targets;
  #296 is closed context only. Rejected: reopening or re-closing #296.
- Bundle family: one inclusive issue-band goal vs split per issue immediately.
  Chosen: one bundled goal because the user selected #294-#298 and the issues
  share workflow-quality/closeout surfaces. Rejected: unconditional split, but
  Slice 1 must split if classifications expose conflicting decisions.
- Issue classification family: bug-class vs deferred-work. Chosen: #294 and
  #298 are bug-class and get separate debug RCA; #295 and #297 are
  deferred-work / quality-evidence improvements and get pre-mutation briefs.
  Rejected: treating #294 as cosmetic guidance or #298 as a single broad
  governance rewrite.
- Carrier family: one final carrier, PR carrier, per-issue carriers, or natural
  direct-commit carriers. Chosen: natural direct-commit carriers, each with the
  close keyword(s) for issues actually resolved by that commit, followed by
  direct push and GitHub closeout verification. Rejected: PRs, forced one-issue
  commits, and a single final catch-all carrier.
- Host axis: reviewer tier evidence varies by host metadata exposure and spawn
  schema. Chosen: record requested reviewer tier plus host exposure state, using
  `applied` only when host-confirmed and otherwise using states such as
  `requested_fields_sent`, `metadata-hidden`, `unsupported`, or
  `host-defaulted`.
  Rejected: asserting Codex-visible spawn fields are universal.
- Validation axis: pre-lock slice proof and final verification-lock proof vary
  by changed surface and mutation stability. Chosen: focused current-diff proof
  before lock, broad final proof after lock. Rejected: broad pytest after every
  small edit.
- #294 disposition family: guidance only, advisory validator, or narrow hard
  floor. Chosen: guidance plus narrow hard validator for abstraction-up
  `diagnostic-only` entries that describe unresolved structural work without
  proof/reason or a durable follow-up. Rejected: prose-only memory.
- #295 runner family: advisory only, phase-sensitive refusal, or pre-lock broad
  refusal. Chosen: final broad closeout refuses without `--verification-lock`;
  pre-lock broad remains possible with focused-proof recommendation and cost
  warning. Rejected: forbidding all pre-lock broad proof.
- #297 evidence family: requested-only, requested plus host exposure state, or
  applied-by-assertion. Chosen: requested plus host exposure state; `applied`
  only when host-confirmed. Rejected: equating sent fields with provider-applied
  behavior.
- #298a script contract family: script judgment, facts-only, or facts as
  clearance. Chosen: facts are evidence, never clearance; unknown tokens are
  policy-no-opinion; agent judges semantic fit, urgency, dependency, and
  operator value. Rejected: hardcoded Ceal label demotion and `safe_to_merge`
  style clearance.
- #298b implementation family: issue-only wording, issue/achieve/retro wording,
  or helper-first. Chosen: update `issue`, `issue/references`, `achieve`, and
  `retro`; helper/preflight remains conditional if repeated manual
  publication-boundary classification is still ambiguous.
- Target repo axis: `corca-ai/charness` is a single-point target resolved from
  `origin`; no cross-repo implementation is planned.

## Plan Critique Findings

- Provenance: Before-phase local synthesis plus bounded fresh-eye angle review
  from agents `019e9230-b39f-7ab1-8b06-a9663c56bda2` and
  `019e9230-db52-73a2-9721-c0ccc3612713`; parent-delegated counterweight agent
  `019e9233-4d07-77f0-9bd2-35a512e5172b`. Packet consumed:
  `charness-artifacts/critique/2026-06-04-103148-packet.md`.
- Counterweight triage complete: parent-delegated counterweight found the draft
  activation-ready after folded edits; no remaining Act Before Ship blockers
  beyond preserving Slice 1 as the classification/brief gate before
  implementation.
- Act Before Ship folded into plan: fill all activation sections before
  activation; classify each issue before implementation; keep #296 context-only;
  make #294/#298 bug RCA/classification gates rather than cosmetic doc fixes.
- Bundle Anyway folded into plan: bundled goal is acceptable because the open
  issues share closeout evidence, reviewer proof, handoff/goal governance, or
  validation economics; preserve issue-specific proof and slice boundaries.
- Over-Worry not folded: separate goals for #294/#295/#297 are unnecessary if
  Slice 1 preserves classification and the bundle escape hatch.
- Valid but Defer: broader governance-bundle scoring beyond #298 and retro-side
  prepare-packet integration are out of scope unless a slice proves they are
  required.
- Folded verification finding: use focused pre-lock proof per mutation slice and
  final verification-lock broad proof once; do not pay broad pytest before the
  mutation set is locked unless changed-surface evidence requires it.
- User discussion folded after initial critique: direct push is in scope on this
  machine; PR is out of scope; close keywords attach to natural carrier commits,
  not necessarily one issue per commit; active slices must route through
  `debug`, `impl`, `quality`, `issue`, `critique`, and `retro` as recommended by
  `find-skills`.
- User discussion folded for #298: keep #298 as one issue with two acceptance
  items (#298a handoff facts-only merge-hint contract, #298b carrier-vs-
  lifecycle publication guidance); avoid script false-clearance and leave
  semantic work-package judgment to agents.

## Off-Goal Findings

N/A - no off-goal findings have been surfaced during shaping.

## Final Verification

Not run. This goal is draft and has not executed slices.

## User Verification Instructions

After activation and completion, verify:

- `gh issue view 294 295 297 298 --repo corca-ai/charness` show the intended
  final `CLOSED` state after direct push of carrier commits.
- The final goal artifact lists exact focused tests, final closeout proof,
  critique evidence, retro dispositions, and non-claims.
- #296 remains closed and is not included in close keywords.
- #298 closeout ledger proves both #298a and #298b acceptance items.

## Auto-Retro

Not run. Retro is an After-phase step.
