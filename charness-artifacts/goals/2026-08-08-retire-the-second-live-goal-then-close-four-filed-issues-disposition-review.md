# Disposition Review — goal 2026-08-08-retire-the-second-live-goal-then-close-four-filed-issues

Rung-1b review of the Auto-Retro dispositions for this goal (binding + honesty
check; presence-only per the disposition gate). Source retro:
`charness-artifacts/retro/2026-08-08-retire-the-second-live-goal-retro.md`.

## Dispositions reviewed

1. **Budget two delegated rounds per verdict-logic slice, as a plan-level cost**
   → `applied`.
   - Bound to a real change: the successor artifact
     `charness-artifacts/goals/2026-08-09-close-the-copies-this-run-measured.md`
     carries it in `## Agent Verification Plan` with this run's own measurement as
     the reason. Honest: it is a committed artifact, and the measurement behind it
     is specific rather than an impression — eighteen blockers across ten rounds,
     none of them in a first diagnosis. VALID.

2. **Run the dup ratchet and length headroom EARLY in each slice** → `applied`.
   - Bound: the successor's `## Agent Verification Plan` Low-Cost Checks name both
     at slice start rather than at the commit boundary. Honest: this run hit three
     commit-boundary blocks (one dup hard-block, two length-cap failures), each of
     which forced a re-run of the closeout aggregate. The claim is about a cost
     that was paid three times, not a hypothetical. VALID.

3. **Equality-pinned probes over a mutable corpus** → `issue #561`.
   - Honest destination, carried on both required axes: `Structural pattern:` (an
     equality pin over a corpus ordinary work mutates converts every routine write
     into a hand re-record indistinguishable from laundering a rule regression) and
     `Triggering instance(s):` (two probes refreshed three times in seven days
     versus a third pinning `>= floor` and never refreshed). NOT folded in: the
     choice between equality and invariant is D47's owner's decision, and this goal
     inherited `#561` as an explicit non-claim. VALID.

4. **Owner-inspection locator pins with a measured 0/5 true-positive rate** →
   `issue #562`.
   - Honest destination with a measurement rather than an impression. NOT folded
     in, and the reason is this goal's own `## Non-Goals`: it is a proof-surface
     DELETION touching ~35 `sha256` references plus a schema bump, and this run's
     record — eighteen blockers, all in repairs — is the strongest evidence yet
     that such a slice needs its own budget and two rounds. VALID.

5. **The four traps no gate holds, written where the next session reads them** →
   `applied`.
   - Bound: the successor's `## Active Operating Frame` carries all four (opening
     a file is not sufficient; a repair inherits halves; a test that re-implements
     its subject is a copy of the rule; pin the source, not the generated mirror),
     and `charness-artifacts/retro/recent-lessons.md` was regenerated from the
     persisted retro. Honest about the mechanism, which the PREVIOUS goal's
     disposition review had to correct: the digest is GENERATED by a selection
     index, so the retro artifact is the durable owner and the digest slot is the
     selector's to give. Recorded here the same way rather than re-learned. VALID.

## Verdict

Five dispositions, all bound to a committed change or an honest tracked
destination; none is prose-only memory. The two `issue` routes are genuinely
out-of-scope — each is a proof-surface change with its own budget — and each
carries a measurement rather than an adjective.

One thing this review checked specifically, because the predecessor's review
caught it: disposition 5's claim about `recent-lessons.md` states the generated
mechanism rather than claiming the digest as a durable home. The correction made
one goal ago survived into this one, which is the first time a disposition-review
finding in this family has been carried forward rather than re-discovered.
