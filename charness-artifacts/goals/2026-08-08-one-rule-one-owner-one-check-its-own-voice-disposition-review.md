# Disposition Review — goal 2026-08-08-one-rule-one-owner-one-check-its-own-voice

Rung-1b review of the Auto-Retro dispositions for this goal (binding + honesty
check; presence-only per the disposition gate). Source retro:
`charness-artifacts/retro/2026-08-08-one-rule-one-owner-retro.md`.

## Dispositions reviewed

1. **Plan to the budget that produced quality — five slices, not nine** →
   `applied`.
   - Bound to a real change: the successor artifact
     `charness-artifacts/goals/2026-08-08-retire-the-second-live-goal-then-close-four-filed-issues.md`
     ships a five-row `## Slice Plan` and a `NOT claimed` paragraph naming `#562`,
     `#563`, `#560`, `#561` and the re-scoped original slice 4, so the cut is
     visible rather than implied. Honest: this is a committed artifact, not a
     stated intention. Its `## Non-Goals` also carries `Do not plan nine slices`
     with this goal's own five-of-eleven measurement as the reason. VALID.

2. **Verify the reviewer boundary BEFORE repairing** → `applied`.
   - Bound: this goal missed it on all three of its own slices and reconciled
     afterwards. The successor's `## Agent Verification Plan` names it as a
     High-Confidence check, and `#536`'s closeout executed it that way — windows
     `w-20260808T052344Z-3740093` and `w-20260808T053129Z-3760763`, each verified
     `clean` at the moment the reviewer returned and before any repair, recorded
     in `charness-artifacts/critique/2026-08-08-issue-536-resolution-critique.md`.
     Honest: an executed instance, not a rule restated. VALID.

3. **Equality-pinned probes over a mutable corpus** → `issue #561`.
   - Honest destination, and the classification is carried on both axes required
     by the retro-issue destination split: `Structural pattern:` (an equality pin
     over a corpus ordinary work mutates converts every routine write into a hand
     re-record indistinguishable from laundering a rule regression) and
     `Triggering instance(s):` (two probes refreshed three times in seven days,
     versus a third pinning `>= floor` and never refreshed). NOT folded into this
     goal: choosing between equality and invariant is D47's owner's decision, and
     taking it inside a message slice would have been the goal deciding a
     deferral it does not own. VALID.

4. **Owner-inspection locator pins with a measured 0/5 true-positive rate** →
   `issue #562`.
   - Honest destination with a real measurement rather than an impression: 6 of 20
     locators changed in one day, five prior re-stamps, all five incidental,
     `refreeze` a single command recording no basis. NOT folded in: the build is a
     proof-surface DELETION touching ~35 `sha256` references plus a schema bump,
     and this goal family's own record is that every proof-surface slice shipped a
     fix carrying the class it fixed — so it needs its own two rounds and its own
     budget. The successor's `## Non-Goals` says so explicitly. VALID.

5. **The two gate-proof lessons written where the next session reads them** →
   `applied`, with a narrower claim than first drafted.
   - Bound: `charness-artifacts/retro/recent-lessons.md` now carries the retro's
     `memory` bullet, which names both lessons (a substring pin cannot see an
     INVERSION; a live-repo-state test needs INJECTION) plus the sharpened form
     found at `#536`'s closeout. Honest about the mechanism: that digest is
     GENERATED from the retro corpus by a selection index, so this was achieved by
     persisting a retro artifact whose `Next Improvements` names them — not by
     editing the digest, which would have been overwritten. NOT claimed: that the
     selector will keep them across future refreshes; the durable owner is the
     retro artifact, and the digest slot is the selector's to give.

## Verdict

Five dispositions, all bound to a committed change or an honest tracked
destination; none is prose-only memory. The two `issue` routes are genuinely
out-of-scope for a goal whose center of gravity was one defect CLASS, and each
carries a measurement rather than an impression. Disposition 5 was narrowed
during this review after checking how `recent-lessons.md` is produced: the first
draft claimed the digest as the durable home, which is false — it is generated,
and the retro artifact is the owner. That correction is the review earning its
place.
