# Achieve Goal: Length warn tier + issue-body safety (#232) + host-hook install (#244/#245)

Status: active
Created: 2026-05-29
Activation: `/goal @charness-artifacts/goals/2026-05-29-length-warn-232-244-245.md`

This file is the living goal scratchpad. It becomes active only when the user
runs the activation command.

## Goal

Land the top-3 chunks from this session's chunked-routing ranking as one
coherent arc. (Provenance: the chunker unioned the live issue backlog with the
hand-written `docs/handoff.md` `## Next Session`, which surfaced **#232** — not
listed in the handoff doc itself — ranked it #2, deferred **#243**, and reshaped
chunk-1 from "pre-commit gate" to "warn-only". So this scope is *not* a literal
read of the handoff doc's three entries.) The arc:
(1) add an advisory file-length WARN tier to `check_python_lengths.py` for all
file classes (helpers 330/360, scripts 432/480, tests 720/800) so the saturated
codebase gets an early signal before the existing hard-fail;
(2) #232 make the issue skill pass body content via file/stdin instead of inline
shell-quoted `--body`, with guidance + tests covering
Korean/Markdown/backticks/quotes/dollar-signs/URLs;
(3) #244 auto-install the find-skills SessionStart routing hook and #245
de-duplicate SessionStart hooks by logical identity across checkouts.

## Non-Goals

- **#243** (usage-episode telemetry: no consumer + biased capture). Deferred out
  of this goal by interview decision — it is effectively a new feature
  (aggregator/report + capture-consistency) that would dominate a two-installer-
  fix cluster. Stays on the backlog as its own future goal.
- **Rank-4 chunk:** v0.12.0 real-host proof (Cautilus clean-machine smoke) and
  the residual backlog #242/#219, #233, #241, #237/#236, #184/#185.
- **Wiring `check_python_lengths` into `.githooks/pre-commit`.** Explicitly not
  chosen: pre-push already runs the hard gate, so the warn tier lands only in
  the existing full-gate/pre-push path.
- **A function-length warn tier.** Warn applies to *file* length only; function
  limits (100 / 150) stay hard-only.
- **Changing any existing hard-fail limit** (480 / 360 / 800 / 100 / 150). Only
  a new advisory warn band is added below each.
- **Changing the external `ceal` issue backend.** #232's fix is provider-
  agnostic and lives in the charness issue skill, not the host-mediated backend.

## Boundaries

- **Slice A (warn tier):** advisory only — `check_python_lengths.py` prints a
  `WARN` line for files in `[warn, limit]` but keeps exit 0; `> limit` still
  hard-fails unchanged. **The line MUST start with `WARN:` (or `WARN `)** — else
  `run-quality.sh`'s passing-gate surface filter
  (`^(WARNING|WARN|WEAK|ADVISORY)(:|[[:space:]])`, ~line 296) captures the
  advisory to the log but never shows it, silently defeating the warn tier.
  Warn thresholds (file length): skill helpers
  (`skills/public|support/*/scripts/*.py`) 330/360; repo `scripts/*.py`
  432/480; `tests/**/*.py` 720/800. The exported copy
  `plugins/charness/scripts/check_python_lengths.py` is a synced surface —
  regenerate it (mutate → **sync** → verify). Invocation stays via
  `run-quality.sh` (`check-python-lengths`) and pre-push; do **not** add to
  pre-commit.
- **Slice B (#232):** fix lives in `skills/public/issue/` (`issue_tool.py` /
  `issue_runtime.py` + `SKILL.md` guidance). Body must reach the backend via
  stdin or a temp file, never interpolated into a shell command string. Note:
  `issue_tool.py` has **no `create` subcommand today** — `SKILL.md` directs the
  agent to call `gh issue create` directly. The fix likely adds a `create`
  subcommand with `--body-file` (parallel to the existing `close-with-comment`),
  plus guidance against inline `--body`. `issue_tool.py` is already 352 lines
  (inside the 330 warn band), so a new `issue_create.py` module (parallel to
  `issue_close.py`) is **expected, not contingent** — `issue_tool.py` must stay
  ≤360. Provider-agnostic (works for `gh` and any host-mediated backend).
- **Slice C (#244):** add an adapter-gated, opt-in `find_skills_routing`
  host-hooks intent in `host_hook_install_lib.py`, pointing at the installed
  plugin's `session_start_find_skills.py`, matcher `startup|resume|clear`,
  parallel to usage-episodes. Keep the dumb-hook / skill-owns-routing split.
- **Slice D (#245):** de-duplicate SessionStart hooks by *logical identity*
  (script basename / intent) rather than exact absolute-path command string, so
  two checkouts do not double-install. Same file/seam as C
  (`_entries_match_command`); must not break the legitimate single-install case
  or the Codex TOML path (`host_hook_codex_toml_lib.py`).
- **Phase discipline:** mutate → sync (plugin export + generated surfaces) →
  verify (validators) → commit per slice. Meaningful `charness-artifacts/`
  changes commit with their slice.

## User Acceptance

What the user can do to verify completion directly.

- **A:** `python3 scripts/check_python_lengths.py --repo-root .` exits 0 but now
  prints a `WARN` line for each in-band file (e.g. `publish_release_cli.py` 360,
  `issue_tool.py` 352). A file pushed past its hard limit still fails. The
  exported plugin copy matches the canonical script.
- **B:** Creating an issue whose body mixes Korean text, a fenced code block,
  backticks, single/double quotes, `$VAR`, and a URL yields an issue body
  byte-identical to the input on read-back — no shell-quoting corruption. The
  issue `SKILL.md` warns against inline `--body` for non-trivial bodies.
- **C/D:** On a machine with two charness checkouts, `charness init`/`update`
  installs exactly one usage-episode SessionStart hook (no path-duplicate) and,
  when the find-skills routing intent is enabled, exactly one find-skills
  SessionStart hook pointing at the installed plugin's script.

## Agent Verification Plan

### Low-Cost Checks

- Per slice: targeted pytest for the touched surface —
  `tests/quality_gates/test_python_and_security_gates.py` (A); issue skill
  fixtures (B); host-hook installer tests (C/D).
- `python3 scripts/check_python_lengths.py --repo-root .`; `py_compile` + `ruff
  check` on changed files; `validate_skills.py` for skill edits.

### High-Confidence Checks

- Bundle boundary: `./scripts/run-quality.sh --read-only` (or the documented
  docs-only subset when the slice is docs-only).
- Plugin export drift: `sync_root_plugin_manifests.py` then `git diff --
  plugins/charness` clean; `check_duplicates.py --json` for new test code.

### External Or Live Proof

- **B:** issue body read-back uses the live `gh` backend when available;
  otherwise fixture/stub-verify and record the non-claim.
- **D:** cross-checkout dedup is best proven on a real two-checkout machine;
  if not exercised live, record it as live-pending rather than claimed.

## Slice Plan

| Slice | Objective | Why Now | Expected Evidence | Status |
| --- | --- | --- | --- | --- |
| A | `check_python_lengths` file-length WARN tier (all classes) + sync export | Ranked #1: guards the script edits in B/C/D; repo is saturated (~10 skill helpers in 330-360 band, ~30 files across all classes in band; `publish_release_cli.py` at the 360 limit) | check exits 0 with `WARN:`-prefixed lines for in-band files; hard-fail unchanged; tests updated; plugin copy synced | done (28 in-band WARN lines; exit 0; 28 gate tests green) |
| B | #232 issue body via stdin/file + `SKILL.md` guidance + tests | Protects all issue-driven work; `issue_tool.py` edit benefits from A's warn | body round-trips byte-identical for Korean/MD/backticks/quotes/`$`/URL; guidance present; fixtures green | done (`create --body-file`; 30 issue tests green; fresh-eye critique: no blockers) |
| C | #244 auto-install find-skills SessionStart hook (opt-in, adapter-gated) | Removes the manual hook step the 0.12.0 note still documents; defines clean-machine path | installer wires `session_start_find_skills.py` parallel to usage-episodes; installer tests green | planned |
| D | #245 dedup SessionStart hooks by logical identity | Same installer seam as C; stops double-recording across checkouts | second-checkout install adds no path-duplicate; single-install unaffected; tests green | planned |

## Slice Log

### Slice 1: A: check_python_lengths file-length WARN tier + sync

- Objective: Add advisory file-length WARN band (helpers 330/360, scripts 432/480, tests 720/800) to check_python_lengths.py: in-band files keep exit 0 but emit a line-start WARN: line; > limit still hard-fails. Sync exported plugin copy.
- Why this approach: Ranked #1; guards the B/C/D script edits. WARN: prefix is load-bearing for run-quality.sh's passing-gate surface filter (^(WARNING|WARN|WEAK|ADVISORY)(:|space)).
- Commits: pending (committed with this slice)
- What changed: scripts/check_python_lengths.py (warn thresholds + file_warn_for + validate_file_length returns optional WARN + main prints WARN lines/summary); plugins/charness/scripts/check_python_lengths.py (synced, identical); tests/quality_gates/test_python_and_security_gates.py (+2 tests: across-classes warn-band, no-warn-below-band).
- Alternatives rejected: Rejected unprefixed advisory (silently swallowed by surface filter); rejected wiring into pre-commit (out of goal — pre-push already runs hard gate); rejected function-length warn (file-length only per goal).
- Targeted verification: python3 scripts/check_python_lengths.py --repo-root . -> exit 0, 28 in-band WARN lines incl. issue_tool.py 352 & publish_release_cli.py 360; pytest tests/quality_gates/test_python_and_security_gates.py -> 28 passed; py_compile + ruff clean; export diff identical.
- Test duplication pressure: Slice adds 2 tests to test_python_and_security_gates.py: file now 659 lines (test warn floor 720 -> not in-band). check_duplicates.py --json -> 0 duplicates, exit 0.
- Critique: Deferred to per-slice resolution critique; warn-format blocker already folded in Before-phase (WARN: prefix).
- Off-goal findings:
- Lessons carried forward: issue_tool.py (352) & test_issue_skill.py (780) are both in-band -> Slice B must keep issue_tool.py <=360 (new issue_create.py module) and avoid pushing test_issue_skill.py past 800.
- Metrics:

### Slice 2: B: #232 issue body via --body-file + read-back verify + guidance

- Objective: Add issue_tool.py create subcommand that delivers the body via --body-file (never inline shell-quoted --body) so Korean/Markdown/backticks/fenced-code/quotes/$/URL content round-trips byte-identical; read the created body back and report body_verified. Update SKILL.md + issue-backend.md guidance against inline --body.
- Why this approach: #232 protects all issue-driven work; bodies were corrupted by inline shell-quoted --body. issue_tool.py was at 352 (warn band from Slice A) so logic lives in new modules to keep it <=360.
- Commits: pending (committed with this slice)
- What changed: NEW issue_backend.py (shared run_backend/resolve_op primitives); NEW issue_create.py (create_issue + command_create + register_create_subparser, body via --body-file, read-back body_verified tri-state); issue_close.py refactored to delegate primitives (195->142 lines); issue_tool.py +2 lines wiring create (now 355<=360); SKILL.md (steps 5/6 + 2 guardrails, kept at 200); references/issue-backend.md (+Body Safety section); NEW tests/quality_gates/test_issue_create.py (4 tests). Plugin export synced.
- Alternatives rejected: Rejected duplicating backend primitives into issue_create (extracted issue_backend.py instead); rejected putting create logic inline in issue_tool.py (would exceed 360 hard limit); rejected adding create tests to test_issue_skill.py (already 780/800 -> new file); rejected passing body inline via argv-only (file is clearer and matches close-with-comment's --body-file pattern).
- Targeted verification: pytest test_issue_skill.py+test_issue_create.py -> 30 passed (incl. byte-identical hostile-body round-trip asserting --body-file present & --body absent); validate_skills exit 0 (SKILL.md=200); check_python_lengths exit 0 (issue_tool.py 355); ruff + py_compile clean; check_doc_links exit 0; export parity OK for all 6 files.
- Test duplication pressure: New file test_issue_create.py=190 lines (test warn floor 720 -> not in-band). issue_create.py=222, issue_backend.py=98, issue_close.py=142 (all <360). check_duplicates.py --json -> 0 duplicate pairs, exit 0 (whole-file detector; block-level reuse of primitives not flagged).
- Critique: Fresh-eye subagent (general-purpose) reviewed correctness/refactor-safety/provider-agnosticism/edge-cases/recurrence. Verdict: safe to commit, no blockers. Folded 2 doc SHOULD-FIX/NICE into issue-backend.md: (1) body_verified:false can mean benign server-side newline normalization, inspect diff not assume regression; (2) label/milestone append is gh-flag-shaped, other backends handle their own syntax. Confirmed: no shell=True anywhere; issue_verify_closeout still resolves close primitives via re-exported aliases.
- Off-goal findings:
- Lessons carried forward: C/D touch host_hook_install_lib.py — different seam, no length pressure expected. Pattern: when a CLI entrypoint nears its file-length limit, push logic into a sibling module and keep the entrypoint a thin wiring layer.
- Metrics:

### Slice 3: C: #244 auto-install find-skills SessionStart hook (opt-in, adapter-gated)

- Objective: Add a find_skills_routing host-hooks intent so charness init/update auto-installs the find-skills SessionStart hook (session_start_find_skills.py, matcher startup|resume|clear) parallel to usage-episodes, opt-in via adapter. Keep the dumb-hook / skill-owns-routing split.
- Why this approach: #244: #240 shipped the routing script into the plugin but the installer never wired it, so the trigger only fired after a manual per-machine edit.
- Commits: pending (committed with this slice)
- What changed: host_hook_install_lib.py: generalized primitives (build_command script_relative; _event_entry/_install_json_event matcher; _record/_clear_state_entry state_key; _intent_for section) + reconcile_host_hooks lazily reconciles find-skills under actions['find_skills_routing']; host_hook_codex_toml_lib.py: marker param on block/scan/install/uninstall (default usage-episodes); NEW host_hook_find_skills.py (install/uninstall claude+codex + reconcile_find_skills_hooks, state key '<host>:find_skills_routing', TOML marker charness:find-skills-routing); tests +6. Plugin scripts synced.
- Alternatives rejected: Rejected full HookSpec generalization of all per-host funcs (would change reconcile payload shape + break back-compat tests, and blow the 480 hard limit). Rejected putting find-skills inline in install_lib (would exceed 480). Chose a sibling module reusing install_lib primitives (parallel to how host_hook_codex_toml_lib was carved out) — usage-episodes reconcile path byte-for-byte unchanged (zero regression). Lazy import in reconcile_host_hooks avoids the module-load cycle.
- Targeted verification: 29 host-hook tests green (23 existing usage-episodes untouched + 6 new find-skills: install-when-enabled w/ matcher startup|resume|clear, coexists-with-usage-episodes (2 distinct entries), idempotent, disabled-by-default, codex TOML distinct marker, uninstall-when-flipped). ruff + py_compile clean (incl. reconcile runner); export parity OK for all 3 host-hook scripts; check_python_lengths exit 0.
- Test duplication pressure: test_usage_episodes_host_hooks.py 478->550 (test warn floor 720 -> not in-band). check_duplicates.py --json -> 0 pairs. NOTE: host_hook_install_lib.py 425->436 ENTERED the advisory warn band [432,480] (Slice A's tier firing on this very change) — under the 480 hard limit; recorded as off-goal split candidate.
- Critique: Deferred to a combined installer critique after Slice D (C and D share the matching seam; one fresh-eye pass covers both).
- Off-goal findings: host_hook_install_lib.py at 436/480 (warn band) — future split candidate (extract detect_host_hook_actual + session_capture_status into host_hook_status.py). Not filed as issue yet; advisory only.
- Lessons carried forward: D must change the dedup matcher (_entries_match_command + find_charness_toml_block command match) from exact-command to logical identity (script basename), which now naturally dedups BOTH hooks per-kind since find-skills already has a distinct basename + TOML marker.
- Metrics:

## Context Sources

Durable references this goal was shaped from. A fresh session can reconstruct
the originating context by following them in order.

- `docs/handoff.md` — `## Next Session` ranked chunks 1–3 (this goal's scope).
- `charness-artifacts/retro/recent-lessons.md` — chunk-1 recurrence (warn-330 /
  fail-360, second recorded occurrence).
- `charness-artifacts/retro/2026-05-29-249-248-handoff-chunker-v2-closeout.md` —
  originating closeout retro.
- GitHub issues **#232, #244, #245** (live bodies fetched 2026-05-29).
- `scripts/check_python_lengths.py`, `scripts/run-quality.sh:415`,
  `.githooks/pre-commit`, `pyproject.toml` (ruff: `select=[E,F,I,C90]`,
  `ignore=[E501]` → no file/line-length rule).
- `scripts/host_hook_install_lib.py`, `scripts/session_start_find_skills.py`,
  `scripts/usage_episode_session_start.py`, `scripts/host_hook_codex_toml_lib.py`.
- `skills/public/issue/scripts/{issue_tool.py,issue_runtime.py}`,
  `skills/public/issue/SKILL.md`.

## Interview Decisions

- **#243 scope** — options {full / minimal slice / **defer**}. Chosen: defer out
  of goal. Rejected full/minimal because #243 is a new feature (consumer +
  capture-consistency) that would dominate and dilute a two-installer-fix
  cluster; it stays on the backlog.
- **Chunk-1 shape** — options {warn + pre-commit / **warn-only** / drop}. Chosen:
  warn-only. Rejected pre-commit wiring because pre-push already runs the hard
  gate, so the genuine gap is the missing early signal, not timing; rejected
  drop after evidence that ruff does **not** check file/function length here
  (E501 ignored, no PL rules) and the repo is saturated at the limit.
- **Warn scope** — options {skill-helpers-only / **all-classes**}. Chosen: all
  classes proportionally (helpers 330/360, scripts 432/480, tests 720/800).
  Rejected helpers-only because scripts/tests saturate the same way; the retro's
  330/360 is a subset of the broader band.

## Plan Critique Findings

_Critique plan:_ per-slice critique runs during the During phase; a
resolution/final critique runs in the After phase.

**Before-phase misunderstanding critique** (fresh-eye subagent, 2026-05-29).
Verdict: **safe to activate, no blockers**; reads of #232/#244/#245 all
confirmed against live issues + code. Findings:

- **Folded — WARN format (Slice A).** `run-quality.sh:296` only surfaces a
  passing gate's output when it matches `^(WARNING|WARN|WEAK|ADVISORY)(:|space)`.
  An advisory WARN with no prefix is captured but never shown → silent tier.
  Folded into Slice A Boundaries: the line must start with `WARN:`.
- **Folded — Slice B split is expected, not contingent.** `issue_tool.py` is
  already 352 lines and has no `create` subcommand (SKILL.md calls
  `gh issue create` directly); the #232 fix adds a `create`/`--body-file` path,
  near-certainly via a new `issue_create.py` module. Folded into Slice B
  Boundaries.
- **Folded — stale saturation figure.** Actual in-band today: ~10 skill helpers
  (330-360), ~30 files across all classes. Corrected in the Slice Plan rationale.
- **Folded — narrative provenance.** "top-3 ranked handoff chunks" was
  imprecise: #232 is not in `docs/handoff.md`; it was surfaced by the live-issue
  union and ranked #2. Goal statement now states this provenance.
- **Over-worry (not folded):** synced-export step is correct
  (`packaging_lib.py:243` full-tree copy via `sync_root_plugin_manifests.py`);
  Codex TOML path is isolated from the JSON `_entries_match_command` change;
  C/D same-file changes are additive with no hidden ordering. "warn but exit 0"
  is coherent with run-quality's capture model.

## Off-Goal Findings

Issues or deferred findings discovered during the run.

## Final Verification

_Pending — filled in the After phase (final gate result, proof levels run vs
skipped, residual risk, non-claims)._

## User Verification Instructions

_Pending — filled in the After phase (concrete operator steps to confirm each
slice's acceptance)._

## Auto-Retro

_Pending — `retro` runs in the After phase; substantive findings narrated inline
here and persisted to `charness-artifacts/retro/`._
