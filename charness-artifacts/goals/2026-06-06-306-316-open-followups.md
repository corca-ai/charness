# Achieve Goal: Resolve #306/#311/#314/#315/#316/#317 open follow-ups via dynamic workflow

Status: active
Created: 2026-06-06
Activation: `/goal @charness-artifacts/goals/2026-06-06-306-316-open-followups.md`

This file is the living goal scratchpad. It becomes active only when the user
runs the activation command.

## Active Operating Frame

- Current slice: shaped and active; all operator decisions locked (scope now 6
  issues — #317 added by operator request); launching the dynamic workflow.
- Next action: run the implementation workflow (S1→S6 sequential: #314, #306,
  #311, #317, #315, #316), then review (S7), then release (S8) in the main loop.
  Setup slices #311→#317 are sequenced (shared setup files); achieve slices
  #315→#316 are sequenced (shared template).
- Verification cadence: per-slice `run_slice_closeout.py --predict-commit`
  (pre-commit aggregate) at each commit; targeted pytest per changed surface at
  slice boundary; one final `./scripts/run-quality.sh --release` + one remote CI
  run at closeout. Local per-slice, one final remote proof (the #316 lesson,
  lived).
- Slice review packet: before fresh-eye slice critique, provide intent, changed
  files and owning/generated surfaces, expected invariants, tests/proof,
  non-claims, out-of-scope lines, and reviewer questions.
- History boundary: keep this frame current; move completed detail to
  `## Slice Log`, `## Final Verification`, and `## Auto-Retro`.

## Goal

Durably resolve the six OPEN follow-up issues in the 306–317 range and close
them on `main`, then cut a release:

- **#306** — stop the scheduled mutation gate's changed-line signal from
  generating ~2 self-healing auto-issues/day. Make subprocess-only CLI-script
  coverage reliably captured (so the recurring class stops reading as 0%) **and**
  cover that script class directly with in-process tests. The gate stays
  blocking — noise is removed by making the signal honest, not by demoting it.
- **#311** — make `setup`'s inspector flag an existing `AGENTS.md` as stale when
  it lacks the #303 adapter-first subagent-reviewer rule, and add that rule to
  the required-snippet set. `setup` keeps its never-rewrite-existing-body design;
  the operator is *told* the file is stale rather than having it rewritten.
- **#314** — wire the fast structural checkers (`validate_skill_ergonomics.py`,
  `check_boundary_bypass_ratchet.py`) into the per-slice surface `verify_commands`
  **and** reconcile the per-slice aggregate (`run_slice_closeout.py`) with the
  literal git pre-commit hook (`staged_commit_gate_plan.py`) so both commit-
  boundary paths run the same fast gate subset. Generalizes the closed #307.
- **#315** — seed visible closeout-evidence placeholders into the `achieve` goal
  artifact template (retro, host-log probe, disposition review, Auto-Retro
  dispositions) so an active agent sees the obligation from the start — while
  keeping the placeholders unable to satisfy the `complete` gate (literal
  `TODO`/`<path>`/`TBD` still fails).
- **#316** — make `achieve`'s external-publication/apply approval boundary
  explicit: approval is scoped to the phase/bundle that requested it; after an
  approved publish/CI/apply lane, done-early test-only quality continuation is
  **local by default**; per-slice remote CI is not assumed.
- **#317** — make `setup` seed a compact meaningful-slice commit-discipline block
  in greenfield `AGENTS.md` (commit meaningful work slices as they finish; treat
  meaningful `charness-artifacts/` changes as repo state; do not report a goal
  done while meaningful work is uncommitted unless deferral is explicit), and have
  the setup inspector flag an `AGENTS.md` that carries Charness goal routing but
  no commit discipline. Same "tell, don't rewrite" pattern as #311: existing
  bodies are not rewritten. Detailed rationale stays in setup references.

Ending: cut a release (version bump + publish) as the final slice; the final
push auto-closes the `Close #N` carriers and provides the single remote CI proof.

## Non-Goals

- Not reopening or re-verifying the six already-CLOSED issues in the range
  (#307, #308, #309, #310, #312, #313). Scope is the 6 open issues only
  (#306, #311, #314, #315, #316, #317).
- Not demoting the #306 changed-line signal to advisory (rejected option) — the
  gate stays blocking; honesty of the signal is the fix.
- Not rewriting existing `AGENTS.md` bodies for #311 or #317 (rejected option) —
  inspector staleness/commit-discipline flag only; greenfield template seeding is
  additive and safe.
- Not adding a fragile deterministic regex gate for #316's publication-boundary
  prose (a word-list over-fires across unrelated goals, the same trap named in
  the disposition-gate design). #316 lands as prose-contract + template language,
  with at most a non-blocking advisory.
- Not weakening the `achieve` complete-evidence gate while adding #315
  placeholders — the gate must still reject untouched placeholders.

## Boundaries

- Per-slice work is committed locally with a `Close #N` carrier; **no per-slice
  push**. The only push is the final release (S7), which carries the one remote
  CI proof and auto-closes the issues. This is the operator's chosen CI boundary
  and the lived form of #316.
- Portable skill packages (`skills/public/**`, `skills/support/**`) must NOT
  carry bare `#NNN` issue anchors in code/comments (the
  `portable_package_issue_anchor` ergonomics gate). Issue numbers live only in
  commit messages (`Close #N`) and this artifact.
- Any slice that edits `skills/public/**` must run
  `python3 scripts/sync_root_plugin_manifests.py --repo-root .` and stage the
  regenerated `plugins/` mirror with the source (the `check_staged_mirror_drift`
  pre-commit gate hard-blocks otherwise).
- The release (S7) is the only irreversible external side effect; it is operator-
  pre-approved and executed deliberately in the main loop (release skill), not
  inside the implementation workflow.
- Discuss before activation: RESOLVED on 2026-06-06 via operator decisions
  (recorded under `## Interview Decisions`). This goal bundles six tracked-issue
  closures, broad multi-surface scope (mutation tooling, setup, quality-gate
  economics, the `achieve` skill itself), and an irreversible release as the
  final slice. Operator decisions: scope = 6 open issues only (#317 added by
  explicit operator request after shaping); #306 = fix coverage + cover class
  (gate stays blocking); #311 = inspector staleness flag; #314 = wire fast
  checkers + reconcile both commit-boundary paths; #315 = visible placeholders
  that still fail the gate; #316 = prose/template boundary (no fragile new gate);
  #317 = greenfield commit-discipline block + inspector flag, same tell-don't-
  rewrite pattern as #311; goal CI boundary = local per-slice + one final remote
  CI; ending = cut a release. All consequential defaults were approved by the
  operator before activation.

## User Acceptance

What the user can do to verify completion directly:

- `gh issue list --state open` (or `gh issue view <N>`) shows
  #306, #311, #314, #315, #316, #317 CLOSED after the release push.
- `./scripts/run-quality.sh --release` passes on the final `main` state.
- #306: `python3 scripts/check_changed_line_mutation_coverage.py` (or the
  mutation workflow) no longer reports the recurring subprocess-only script class
  as `covers as 0%`; a focused test proves the class is now coverage-tracked.
- #311: re-running `setup` against a repo whose `AGENTS.md` predates #303 emits a
  staleness finding for the missing adapter-first reviewer rule; the body is not
  rewritten.
- #317: a freshly seeded `AGENTS.md` carries a compact commit-discipline block;
  re-running `setup` against an `AGENTS.md` that has Charness goal routing but no
  commit discipline emits a finding; the body is not rewritten.
- #314: `run_slice_closeout.py --predict-commit` and the git pre-commit hook
  (`staged_commit_gate_plan.py`) run the same fast checker subset on a touched
  surface; a test pins the agreement.
- #315: a freshly scaffolded goal artifact shows visible `TODO` placeholders for
  retro / host-log probe / disposition review / Auto-Retro, and
  `upsert_goal.py --status complete` still refuses a placeholder-only artifact.
- #316: `achieve` SKILL.md + `references/lifecycle.md` + the goal template state
  that publication approval is phase/bundle-scoped and continuation is local by
  default.
- The release tag + GitHub release exist and CI is green on the released SHA.

## Agent Verification Plan

### Low-Cost Checks

- Per slice: `python3 scripts/run_slice_closeout.py --repo-root . --predict-commit`
  (the pre-commit aggregate: ruff, `check_python_lengths`, attention-state,
  markdown, mirror-drift, `validate_skills`, doc-links) — runs automatically via
  the pre-commit hook on each `Close #N` commit.
- Per slice: targeted `pytest` over the changed surface's test module(s) only.
- Mirror sync check after any `skills/**` edit.

### High-Confidence Checks

- New/updated tests proving each issue's acceptance criteria (per-slice).
- One final `./scripts/run-quality.sh --release` on the fully bundled `main`
  state before the release push.
- Bounded fresh-eye review across the slice bundle (S6) with adversarial
  verification of findings; rung-2 disposition review at closeout.
- Re-verify THIS goal still closes after S4/#315 lands (the slice edits the very
  evidence-gate machinery this goal's own closeout depends on).

### External Or Live Proof

- One remote GitHub Actions CI run on the released SHA (the single remote proof
  per the chosen CI boundary). Release publish is itself live external proof.
- #306's durable effect (no more self-healing mutation auto-issues) is only fully
  observable over subsequent scheduled cron runs — recorded as a non-claim, not
  asserted as proven in-session.

## Slice Plan

| Slice | Objective | Why Now | Expected Evidence | Status |
| --- | --- | --- | --- | --- |
| S1 · #314 | Wire `validate_skill_ergonomics` + `check_boundary_bypass_ratchet` into per-slice surface `verify_commands`; reconcile `run_slice_closeout` and `staged_commit_gate_plan` to the same fast subset | Independent; makes the rest of the run's commit-boundary proof cheaper and consistent | `.agents/surfaces.json` + `staged_commit_gate_plan.py` updated; `test_staged_commit_gate_plan` green; both paths run the same fast checkers | planned |
| S2 · #306 | Capture subprocess-only CLI-script coverage (coverage.py multiprocessing/process_startup) + add in-process tests for the recurring script class; gate stays blocking | Independent; durably stops the ~2/day self-healing mutation auto-issues | coverage config/sitecustomize change; new in-process tests; the class no longer reads as 0% in `check_changed_line_mutation_coverage` | planned |
| S3 · #311 | Add the #303 adapter-first reviewer rule to `FRESH_EYE_COMPACT_REQUIRED_SNIPPETS`; inspector flags an older `AGENTS.md` as stale; no body rewrite | Setup surface; sequence before S4 (shared setup files) | updated snippet set + inspector finding; test proves stale detection; greenfield write-path behavior unchanged | planned |
| S4 · #317 | Seed a compact commit-discipline block in greenfield `AGENTS.md` (`setup_host_docs_lib.py`); inspector flags goal-routing-without-commit-discipline; no body rewrite | Touches the same setup template/inspector as S3; sequence after S3 | greenfield template block + inspector finding; test proves both seed and stale detection; setup references updated | planned |
| S5 · #315 | Seed visible closeout-evidence placeholders in the `achieve` goal template that still FAIL the complete gate | Touches the `achieve` template; sequence before S6 | template placeholders (retro/host-log/disposition/Auto-Retro); placeholder-only artifact fails `--status complete`; producer test updated; mirror synced | planned |
| S6 · #316 | Make the external-publication/apply approval boundary explicit in SKILL.md + `references/lifecycle.md` + goal-template `Boundaries` | Touches the `achieve` template/SKILL after S5 | boundary language in all three surfaces; producer test still green; mirror synced; no fragile new gate | planned |
| S7 · review | Bounded fresh-eye review across the slice bundle + adversarial verify; surface real fixes; rung-2 disposition review | risk boundary: validators, exports, public-skill surfaces all changed | review artifact under `charness-artifacts/critique/`; verified findings; per-improvement verdicts recorded | planned |
| S8 · release | Final `--release` gate on bundled state; cut release (version bump + publish); push (closes `Close #N`); one remote CI proof | operator chose release-at-end; single final CI | release artifact; pushed tag + GitHub release; issues closed; CI green | planned |

## Coordination Cues

Phase-appropriate routing for this run, deferred to `find-skills` — never a
hard-coded phase→skill list here. Fill/bind during the run:

- **Routing** — each phase routed via `find-skills` recommendation: `impl`/dynamic
  workflow for S1–S5 implementation, `critique` for S6 review, `quality` for the
  `--release` gate, `release` for S7, `issue` for closeout carriers, `retro` for
  the after-action review. Bind the executed route at closeout as `Routing:`.
- **Gather step** — `Gather: n/a — Context Sources are local repo artifacts and
  GitHub issue numbers reached via the gh-backed issue surface, not arbitrary
  external URLs requiring gather acquisition.`
- **Release step** — bind `Release:` to the S7 release artifact path/proof at
  closeout (this run cuts a release).
- **Issue closeout step** — `Issue closeout:` will name
  #306/#311/#314/#315/#316/#317, carrier `direct-commit` (`Close #N` on each slice
  commit; final release push auto-closes), with `issue_tool.py
  validate-closeout-draft` rehearsal proof.

## Slice Log

### Slice 1: wire fast checkers into surface verify + reconcile commit-boundary paths

- Objective: Run validate_skill_ergonomics and check_boundary_bypass_ratchet in per-slice surface verify_commands and make staged_commit_gate_plan agree with the aggregate on the fast subset
- Why this approach:
- Commits:
- What changed:
- Alternatives rejected:
- Targeted verification: pytest tests/quality_gates/test_staged_commit_gate_plan.py + test_surface_obligations.py (43 passed); validate_surfaces ok; run_slice_closeout --predict-commit ok (check-boundary-bypass-ratchet ran in pre-commit path); both checkers green on tree
- Test duplication pressure: test_precommit_plan_agrees_with_aggregate_fast_subset_for_skill_change / _for_test_change; test_fast_surface_verify_allowlist_keys_exist_in_some_surface (drift guard)
- Critique:
- Off-goal findings:
- Lessons carried forward:
- Metrics:

### Slice 2: scaffold in-process coverage (#306)

- Objective: Make subprocess-only scaffold CLI coverage honest so the mutation changed-line signal stops self-healing into ~2 auto-issues/day; gate stays blocking
- Why this approach:
- Commits:
- What changed:
- Alternatives rejected:
- Targeted verification: pytest tests/test_scaffold_inprocess_coverage.py (14 passed); release_only gate proof test_scaffold_changed_line_coverage.py (1 passed, 17s) confirms validator-fallback lines read covered via the gate's own run_test_coverage probe; non-weakening test proves an uncovered changed line still blocks; check_python_lengths 697 files OK; closeout predict-commit PASS
- Test duplication pressure: test_scaffold_changed_lines_read_covered_through_gate_probe + test_changed_line_gate_still_blocks_genuinely_uncovered_line
- Critique:
- Off-goal findings:
- Lessons carried forward:
- Metrics:

### Slice 3: setup inspector flags pre-303 AGENTS.md stale

- Objective: Add adapter-first reviewer-tier snippet to compact required set so an existing AGENTS.md missing the #303 rule is flagged stale without rewriting the body
- Why this approach:
- Commits:
- What changed:
- Alternatives rejected:
- Targeted verification: pytest tests/quality_gates/test_setup_inspect_policy.py (35 passed); reviewer_tier + normalize_host_docs suites green; run_slice_closeout --predict-commit PASS (mirror-drift, python-lengths, ruff)
- Test duplication pressure: test_setup_inspect_flags_compact_delegation_missing_adapter_first_reviewer_rule + companion accepts-with-rule test
- Critique:
- Off-goal findings:
- Lessons carried forward:
- Metrics:

### Slice 4: setup seeds meaningful-slice commit discipline (#317)

- Objective: Greenfield AGENTS.md seeds a compact commit-discipline block; inspector flags goal-routed AGENTS.md that lacks the rule (tell-don't-rewrite, mirrors #311).
- Why this approach:
- Commits:
- What changed:
- Alternatives rejected:
- Targeted verification: pytest test_setup_commit_discipline.py + test_setup_normalize_host_docs.py + test_setup_inspect_policy.py = 47 passed; run_slice_closeout --predict-commit ok (mirror-drift, boundary-bypass-ratchet, skill-ergonomics, check-markdown all PASS); check_python_lengths ok.
- Test duplication pressure: detector requires BOTH halves; artifact-only body still flags missing slice rule; no-goal-routing body not flagged; existing body byte-identical after inspection
- Critique:
- Off-goal findings:
- Lessons carried forward:
- Metrics:

## Context Sources

Durable references this goal was shaped from. A fresh session can reconstruct the
originating context by following them in order:

- Tracked issues (the contract): #306, #311, #314, #315, #316, #317 (bodies read
  in full at shaping; #317 added by operator request after shaping; the six closed
  siblings #307/#308/#309/#310/#312/#313 are out-of-scope context).
- `docs/handoff.md` — Next Session lists #311 and the open backlog; v0.22.0 is the
  current shipped baseline.
- `charness-artifacts/retro/recent-lessons.md` — repeat traps (release-helper
  persistence, mirror-read overlap, broad-pytest cost, closeout evidence chain).
- `charness-artifacts/critique/2026-06-05-v0.22.0-release-critique.md` — source of
  #309/#310/#311 (findings E/F/G).
- `charness-artifacts/retro/2026-06-05-abc-followups-closeout.md` — source of #314.
- Key implementation files per issue: `scripts/check_changed_line_mutation_coverage.py`,
  `scripts/mutation_sampling_lib.py` (#306); `scripts/setup_agent_docs_fresh_eye_lib.py`,
  `scripts/setup_host_docs_lib.py` (#311); `.agents/surfaces.json`,
  `scripts/run_slice_closeout.py`, `scripts/staged_commit_gate_plan.py` (#314);
  `skills/public/achieve/scripts/goal_artifact_template.md`,
  `skills/public/achieve/scripts/goal_artifact_closeout_evidence.py`,
  `skills/public/achieve/scripts/check_goal_artifact.py` (#315);
  `skills/public/achieve/SKILL.md`, `skills/public/achieve/references/lifecycle.md` (#316);
  `scripts/setup_host_docs_lib.py`, the setup inspector, and
  `skills/setup/references/{default-surfaces,agent-docs-policy}.md` (#317).

## Interview Decisions

Operator decisions taken upfront on 2026-06-06 (via `AskUserQuestion`), recorded
with the family considered, the chosen value, the rejected-alternatives reason,
and an anti-anchoring axis note.

1. **Scope.** Family: {5 open only · open + re-verify closed · all 11 fresh}.
   Chosen: **5 open only** (#306, #311, #314, #315, #316). Rejected: re-verifying
   the six closed issues adds cost without a signal that they regressed; reopening
   all 11 risks churning shipped work. `single-point: this goal's scope is a
   one-time backlog cut, not a system axis.`
2. **#306 direction.** Family: {fix coverage + cover class · demote to advisory ·
   both}. Chosen: **fix coverage + cover class, gate stays blocking**. Rejected:
   demoting to advisory gives up a real regression guard and only hides the noise.
   `single-point: a gate-honesty decision for this repo's mutation surface.`
3. **#311 direction.** Family: {inspector staleness flag · backfill existing
   AGENTS.md · won't-fix}. Chosen: **inspector staleness flag**. Rejected:
   backfilling breaks setup's long-standing never-rewrite-existing-body invariant;
   won't-fix leaves upgrading operators silently without the rule.
   `axis: host — AGENTS.md is a per-host operator surface, so the fix tells each
   host its file is stale rather than imposing one rewrite.`
4. **Goal CI boundary.** Family: {local per-slice + 1 final CI · push+watch CI per
   slice · local only, no remote CI}. Chosen: **local per-slice, 1 final remote
   CI**. Rejected: per-slice remote CI is exactly the over-publication #316 flags;
   local-only forgoes any in-run remote proof of the bundle.
   `axis: phase/bundle — external proof is scoped to the bundle boundary, the
   #316 principle this goal also implements.`
5. **#314 scope.** Family: {checkers + reconcile both paths · per-slice checkers
   only · reconcile paths only}. Chosen: **wire fast checkers AND reconcile both
   commit-boundary paths**. Rejected: either half alone leaves the aggregate and
   the literal pre-commit hook disagreeing about which gates ran.
   `single-point: quality-gate economics decision for this repo.`
6. **Ending / release.** Family: {land on main, defer release · cut a release at
   the end}. Chosen: **cut a release at the end**. Rejected: deferring would leave
   operator-facing setup/achieve/quality changes unreleased; the operator wants
   them shipped in this run. `single-point: explicit operator release decision.`
7. **#317 (added after shaping, decided by consistency).** Family: {seed
   greenfield block + inspector flag · inspector flag only · greenfield seed
   only · won't-fix}. Chosen: **seed greenfield commit-discipline block +
   inspector flag, no existing-body rewrite** — the same "tell, don't rewrite"
   pattern the operator already chose for #311. The operator requested #317's
   inclusion explicitly; the approach mirrors #311 and is additive/safe, so it
   was decided by consistency rather than a fresh question (the operator's
   standing "make all decisions upfront" directive). Rejected: a body rewrite
   would break setup's never-rewrite invariant; inspector-only would leave new
   repos without the rule; seed-only would leave upgraders unflagged.
   `single-point: setup operating-surface discipline decision for this repo.`
8. **Mode (stated, not asked).** Implementation-continuation, not artifact-only:
   the operator's prose ("dynamic workflow 로 전부 구현하는 골") names execution.
   The goal is shaped here; activation (`/goal`) remains the operator's explicit
   step.

## Plan Critique Findings

Before-phase critique (self-authored at shaping; bounded fresh-eye review runs in
S6 and a rung-2 disposition review at closeout):

- **Folded blocker — self-modifying closeout machinery.** S4/#315 edits the very
  complete-evidence gate this goal's own closeout depends on. Folded into the
  High-Confidence plan: after S4 lands, re-verify a probe goal still closes and
  that placeholders still fail; S4's out-of-scope line forbids weakening the gate.
- **Folded blocker — portable issue-anchor gate.** Adding `#NNN` to `skills/**`
  scripts would trip `portable_package_issue_anchor`. Folded into Boundaries +
  every implementer prompt: issue numbers only in commit messages.
- **Folded blocker — plugin mirror drift.** Editing `skills/public/achieve/**`
  (S4/S5) without syncing `plugins/` hard-blocks the commit. Folded into
  Boundaries: sync + stage the mirror before each such commit.
- **Folded blocker — #316 over-eager gate.** A deterministic regex for the
  publication-boundary prose would over-fire across unrelated goals (the word-list
  trap the disposition gate already documents). Folded into Non-Goals + S5 scope:
  prose-contract + template only, at most a non-blocking advisory.
- **Over-worry raised, not folded — cross-slice workflow conflict.** Running 5
  slices via one workflow could collide on shared files. Mitigated, not a blocker:
  implementation slices run sequentially and #315/#316 (shared template) are
  ordered S4→S5. No worktree isolation needed.
- **Over-worry raised, not folded — release mid-autonomous-run.** Mitigated:
  release is operator-pre-approved, executed deliberately in the main loop after
  the bundled `--release` gate passes; the #312 resume/closeout release-tooling
  gaps are already CLOSED/fixed.
- Reviewer provenance: this Before-phase pass is self-authored; independent
  fresh-eye coverage is the S6 bounded review + the closeout disposition review.

## Off-Goal Findings

Issues or deferred findings discovered during the run.

## Final Verification

## User Verification Instructions

## Auto-Retro
