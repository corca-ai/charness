# Tool Manifests

Source policy for external tools lives in `integrations/tools/` manifests.
Generated lock state lives in `../locks/` and is not source-of-truth policy.

Schema: [`manifest.schema.json`](./manifest.schema.json).
