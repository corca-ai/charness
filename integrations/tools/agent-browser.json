{
  "schema_version": "1",
  "tool_id": "agent-browser",
  "kind": "external_binary_with_skill",
  "display_name": "agent-browser",
  "summary": "Browser automation CLI for JS-rendered pages, DOM inspection, interactive browser debugging, and browser-mediated `gather` fallback when private SaaS acquisition cannot stay on an official API/export path.",
  "upstream_repo": "vercel-labs/agent-browser",
  "homepage": "https://github.com/vercel-labs/agent-browser",
  "platforms": [
    "linux",
    "macos"
  ],
  "status": "active",
  "lifecycle": {
    "install": {
      "mode": "manual",
      "docs_url": "https://github.com/vercel-labs/agent-browser",
      "install_url": "https://github.com/vercel-labs/agent-browser#installation",
      "notes": [
        "Recommended install paths are npm or Cargo.",
        "Run `agent-browser install` after installing the CLI so Chrome for Testing or an existing browser runtime is available."
      ]
    },
    "update": {
      "mode": "manual",
      "docs_url": "https://github.com/vercel-labs/agent-browser",
      "install_url": "https://github.com/vercel-labs/agent-browser#installation",
      "notes": [
        "Use the same package manager or release asset path that installed the current binary.",
        "Charness promotes this manual route to a package-manager update only when install provenance proves an npm or Cargo-managed binary."
      ]
    }
  },
  "checks": {
    "detect": {
      "commands": [
        "agent-browser --version"
      ],
      "success_criteria": [
        "exit_code:0"
      ],
      "failure_hint": "Install the CLI first, then run `agent-browser install` to prepare a browser runtime."
    },
    "healthcheck": {
      "commands": [
        "python3 scripts/agent_browser_runtime_guard.py --repo-root . --doctor-check"
      ],
      "success_criteria": [
        "exit_code:0",
        "stdout_contains:agent-browser runtime healthy"
      ],
      "failure_hint": "The CLI exists but the runtime is not healthy. Run `python3 scripts/agent_browser_runtime_guard.py --repo-root . --cleanup-orphans --execute` to remove orphan daemon trees, then reinstall or run `agent-browser install --with-deps` if the CLI itself is still unhealthy."
    }
  },
  "access_modes": [
    "binary",
    "human-only",
    "degraded"
  ],
  "config_layers": [
    {
      "layer_id": "agent-browser-saved-auth-state",
      "layer_type": "authenticated-binary",
      "summary": "Prefer imported auth state, persistent profiles, session persistence, or auth-vault login when browser-mediated private-source gather already has approved local state."
    },
    {
      "layer_id": "agent-browser-origin-headers",
      "layer_type": "authenticated-binary",
      "summary": "Use origin-scoped headers only when the source contract explicitly allows header-based read access through the local browser runtime."
    },
    {
      "layer_id": "agent-browser-manual-bootstrap",
      "layer_type": "operator-step",
      "summary": "Some private SaaS flows need a one-time manual or headed bootstrap before later headless gather can continue honestly."
    }
  ],
  "version_expectation": {
    "policy": "advisory",
    "constraint": "Prefer a recent upstream-supported release.",
    "detected_by": "stdout"
  },
  "package_managers": {
    "npm": {
      "package_name": "agent-browser",
      "notes": [
        "Upstream publishes the npm package as `agent-browser`; update with `npm install -g agent-browser@latest` only when the detected binary is managed by the active npm global prefix."
      ]
    },
    "cargo": {
      "package_name": "agent-browser",
      "notes": [
        "Upstream publishes the Cargo crate as `agent-browser`; update with `cargo install agent-browser --force` only when the detected binary is managed by Cargo."
      ]
    }
  },
  "supports_public_skills": [
    "gather"
  ],
  "intent_triggers": [
    "browser automation",
    "headless browser",
    "headless screenshot",
    "browser screenshot",
    "playwright",
    "js-rendered",
    "javascript-rendered",
    "dom inspection",
    "live dom",
    "browser debugging",
    "headed verification",
    "browser-mediated gather"
  ],
  "recommendation_role": "runtime",
  "support_skill_source": {
    "source_type": "upstream_repo",
    "path": "skills/agent-browser",
    "ref": "main",
    "notes": [
      "Prefer the upstream support skill surface when the host runtime can consume it.",
      "`charness` still owns the public `gather` fallback and degradation contract that decides when browser mediation is honest."
    ]
  },
  "degradation": {
    "when_missing": [
      "Fall back to plain HTTP or non-browser workflows when possible.",
      "For private SaaS gather, stop cleanly when browser auth/bootstrap is the missing boundary instead of pretending acquisition succeeded."
    ],
    "when_version_mismatch": [
      "Prefer operator guidance over silent retries when CLI behavior drifts."
    ]
  },
  "host_notes": [
    "Hosts may need a separate Chromium or Chrome installation if they do not use `agent-browser install`.",
    "Local desktop profile reuse is not equivalent to a remote or CI-like Linux runner; some flows require a one-time manual or headed bootstrap before later headless reuse is honest.",
    "Long-lived orphan `agent-browser` daemons are treated as runtime drift, not as a healthy installed state.",
    "Invocation-bound Chrome process/profile teardown is upstream `agent-browser` lifecycle ownership; Charness' runtime guard is downstream drift detection and owned orphan daemon-tree cleanup after residue already exists. Track upstream `vercel-labs/agent-browser#1334` for the broader interrupted-session leak, with adjacent Linux launch-zombie coverage in `#1401` and high-CPU temp-profile residue in `#1371`."
  ]
}
