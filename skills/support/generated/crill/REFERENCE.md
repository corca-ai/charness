# crill Support Reference

This generated reference records how `charness` consumes the upstream
support surface without copying it into the local taxonomy.

- upstream repo: `corca-ai/crill`
- upstream path: `.agents/skills/crill-operator/SKILL.md`
- sync strategy: `reference`
- support state: `upstream-consumed`
- access modes: `binary, human-only, degraded`

## Reuse Notes

- The upstream repo already carries operator guidance for real-device workflows.

## Host Notes

- The upstream tool currently assumes repo-local `uv` workflows and may need platform setup beyond a single binary install.

Regenerate this file through `scripts/sync_support.py` instead of
editing it by hand.
