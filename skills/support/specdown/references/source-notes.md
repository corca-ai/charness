# Source Notes

This support skill is vendored from the upstream `specdown` repo.

- `SKILL.md` comes from `cmd/specdown/skills/specdown/SKILL.md`
- workflow guides come from `docs/`
- detailed syntax and runtime references come from `specs/*.spec.md`, flattened to local `.md` files for support-skill packaging

When refreshing this bundle, preserve relative links so the checked-in support copy stays self-contained inside `skills/support/specdown/`.
