{
  "_purpose": "Declare every repo-owned Python command/helper surface that contains exit-zero attention-state terms. The gate prevents new no_adapter/disabled/not_configured/skipped/prose-review-required states from silently reading as clean pass states.",
  "_contract": "Every detected file must declare its exact states, at least one visibility class, evidence_terms that appear in the source, and a rationale. Use local_noop_not_closeout only for runtime/internal no-op payloads that are not standing quality or release closeout proof.",
  "_visibility_values": [
    "artifact_visible",
    "explicit_skip_summary",
    "hard_failure_or_blocker",
    "local_noop_not_closeout",
    "metric_or_trace",
    "operator_summary",
    "stdout_attention",
    "structured_warning",
    "support_trace",
    "terminal_payload"
  ],
  "files": {
    "skills/public/handoff/scripts/chunked_routing_issue_source.py": {
      "states": ["disabled"],
      "visibility": ["local_noop_not_closeout"],
      "evidence_terms": ["enabled", "doc-only fallback"],
      "rationale": "When the handoff adapter's issue_source is disabled (or no tracker resolves), build_issue_entries returns [] and the chunker falls back to doc-only chunking. This is a deliberate runtime no-op (the pickup still produces a valid handoff-only ranking), not a quality/release closeout signal; the parse payload surfaces issue_entry_count=0 so the doc-only fallback is visible."
    },
    "scripts/build_debug_seam_risk_index.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["skipped_artifacts"],
      "rationale": "Skipped debug artifacts are listed in the index payload with reasons and are not treated as clean seam-risk proof."
    },
    "skills/public/quality/scripts/changed_line_coverage_gate_lib.py": {
      "states": ["skipped"],
      "visibility": ["explicit_skip_summary"],
      "evidence_terms": ["gate skipped (non-blocking)"],
      "rationale": "Helper that builds the portable changed-line coverage gate's non-blocking skip reasons (no base SHA, no eligible change, no coverage report, or a stale fingerprint). Each skip carries an explicit `reason` surfaced by check_changed_line_coverage.py, so a missing/stale coverage source cannot read as a clean blocking-clear pass; teeth only fire on fresh coverage."
    },
    "skills/public/quality/scripts/check_changed_line_coverage.py": {
      "states": ["skipped"],
      "visibility": ["explicit_skip_summary", "terminal_payload"],
      "evidence_terms": ["skipped", "reason"],
      "rationale": "The portable changed-line coverage gate emits explicit non-blocking skips (exit 0) in the JSON/plain `reason` when there is no base SHA, no eligible change, no coverage report, or a stale fingerprint. The skip is surfaced in the terminal payload so a missing/old coverage source cannot read as a clean pass; an invalid adapter fails closed (exit 1)."
    },
    "skills/public/quality/scripts/check_standing_doc_provenance.py": {
      "states": ["skipped"],
      "visibility": ["hard_failure_or_blocker", "operator_summary"],
      "evidence_terms": ["skipped", "inert"],
      "rationale": "The only `skipped` string is the adapter-invalid stderr message, which exits 1 (a hard failure/blocker, not an exit-0 pass). The command's genuine exit-0 attention state is `inert` (opt-out: empty standing_doc_provenance.standing_docs), surfaced verbatim in the plain summary and the JSON `inert` field, so an opted-out repo cannot read as a silent clean pass."
    },
    "scripts/cautilus_adapter_lib.py": {
      "states": ["disabled"],
      "visibility": ["structured_warning", "terminal_payload"],
      "evidence_terms": ["disabled_reason", "Create .agents/cautilus-adapter.yaml"],
      "rationale": "Disabled Cautilus policy is exposed through adapter payloads and operator-facing next-action text."
    },
    "scripts/check_changed_line_mutation_coverage.py": {
      "states": ["skipped"],
      "visibility": ["explicit_skip_summary", "terminal_payload"],
      "evidence_terms": ["skipped (non-blocking)", "--skip-if-no-coverage"],
      "rationale": "The pre-push changed-line mutation-coverage teeth emit an explicit non-blocking skip (exit 0) in the JSON payload `reason` when there is no fresh coverage source for the current head (--skip-if-no-coverage / --require-fresh-coverage). The skip is surfaced in the terminal payload so a stale/absent coverage source cannot read as a clean blocking-clear pass; teeth only fire on a fresh coverage source."
    },
    "scripts/check_cli_skill_surface.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["status", "skipped"],
      "rationale": "The release/quality CLI surface check returns an explicit skipped status and reason when no relevant surface changed."
    },
    "scripts/check_command_docs.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["missing-contract"],
      "rationale": "Repos without command-doc contracts get an explicit skipped status and reason."
    },
    "scripts/check_coverage.py": {
      "states": ["disabled"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["doctor_status", "disabled"],
      "rationale": "Coverage helper tests and payloads keep disabled Cautilus policy explicit rather than silent."
    },
    "scripts/check_coverage_lib.py": {
      "states": ["disabled", "skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["disabled_check_payload", "skipped_healthcheck"],
      "rationale": "Coverage library preserves disabled tool state and skipped healthcheck state through shared lifecycle payloads."
    },
    "scripts/check_seed_fixture_budget.py": {
      "states": ["advisory-only"],
      "visibility": ["stdout_attention", "structured_warning"],
      "evidence_terms": ["advisory_only_no_pytest_temp_yet", "scope_classification"],
      "rationale": "Seed fixture budget gate reports advisory-only when no pytest tmp directory has been materialized yet; payload exposes the classification."
    },
    "scripts/check_mutation_score.py": {
      "states": ["skipped"],
      "visibility": ["operator_summary", "metric_or_trace"],
      "evidence_terms": ["skipped"],
      "rationale": "Mutation skipped workers remain counted in the score metrics and are rendered by the summary helper."
    },
    "scripts/check_mutation_score_summary_lib.py": {
      "states": ["skipped"],
      "visibility": ["operator_summary", "metric_or_trace"],
      "evidence_terms": ["Skipped:"],
      "rationale": "Mutation skipped workers are rendered in the printed score summary."
    },
    "scripts/check_upstream_support_drift.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["skipped ("],
      "rationale": "Skipped upstream checks are surfaced as per-record errors in the drift report."
    },
    "scripts/control_plane_lifecycle_lib.py": {
      "states": ["disabled", "skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["disabled by repo adapter", "not-configured", "skipped"],
      "rationale": "Tool lifecycle disabled states and intentionally absent optional healthchecks share explicit payloads for detect and healthcheck output."
    },
    "scripts/doctor.py": {
      "states": ["disabled", "skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["install_method", "disabled"],
      "rationale": "Doctor output reports disabled tool installs and skipped release probes as structured JSON."
    },
    "scripts/doctor_lib.py": {
      "states": ["disabled", "skipped"],
      "visibility": ["operator_summary", "terminal_payload"],
      "evidence_terms": ["doctor_status", "disabled"],
      "rationale": "Doctor library exposes disabled and skipped healthcheck states with next steps."
    },
    "scripts/filter_cosmic_ray_mutants.py": {
      "states": ["skipped"],
      "visibility": ["operator_summary", "metric_or_trace"],
      "evidence_terms": ["filtered", "skipped"],
      "rationale": "Filtered mutation work is summarized by inspected and skipped counts."
    },
    "scripts/github_actions_lib.py": {
      "states": ["skipped"],
      "visibility": ["metric_or_trace"],
      "evidence_terms": ["skipped"],
      "rationale": "Skipped workflow steps are parsed as source facts for downstream gates, not closeout proof."
    },
    "scripts/host_hook_find_skills.py": {
      "states": ["disabled"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["intent", "disabled"],
      "rationale": "find-skills routing hook install/uninstall library exposes the per-host find_skills_routing intent (enabled/disabled) in the reconcile payload under actions['find_skills_routing']; a disabled (opt-in default) intent uninstalls/no-ops and cannot read as a silent install."
    },
    "scripts/host_hook_install_lib.py": {
      "states": ["disabled"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["intent", "disabled"],
      "rationale": "Host-hook install/uninstall library exposes per-host intent (enabled/disabled) in reconcile and status payloads so adapter-driven no-op runs cannot read as silent install."
    },
    "scripts/host_hook_skill_anchor_guard.py": {
      "states": ["disabled"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["intent", "disabled"],
      "rationale": "Skill #N-anchor edit-time guard hook library exposes the per-host skill_anchor_edit_guard intent (enabled/disabled) in the reconcile payload under actions['skill_anchor_edit_guard']; a disabled (opt-in default) intent uninstalls/no-ops and the codex noop carries an explicit unsupported reason, so neither can read as a silent install."
    },
    "scripts/install_machine_local.py": {
      "states": ["skipped"],
      "visibility": ["operator_summary"],
      "evidence_terms": ["Codex marketplace update skipped."],
      "rationale": "Machine-local install output names skipped marketplace updates directly."
    },
    "scripts/install_tools.py": {
      "states": ["disabled", "skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["mode", "disabled"],
      "rationale": "Tool installation reports disabled adapter state as structured skipped output."
    },
    "skills/public/quality/scripts/inventory_nose_clones.py": {
      "states": ["skipped"],
      "visibility": ["stdout_attention", "terminal_payload"],
      "evidence_terms": ["ADVISORY: nose missing", "clone-family inventory skipped"],
      "rationale": "The `nose` clone-family inventory is advisory. When the binary is absent it prints an explicit advisory skip and exits zero, while document near-copy detection remains covered by the standing hard gate."
    },
    "skills/public/quality/scripts/inventory_doc_duplicates.py": {
      "states": ["skipped"],
      "visibility": ["stdout_attention", "terminal_payload"],
      "evidence_terms": ["ADVISORY: nose missing", "doc near-duplicate inventory skipped"],
      "rationale": "The nose Markdown doc near-duplicate inventory is advisory. Without --require-nose it prints an explicit advisory skip and exits zero when the binary is absent; the run-quality `doc-duplicates` phase passes --require-nose so a missing or pre-0.13.0 nose fails closed (the required tool) rather than silently skipping."
    },
    "scripts/plan_cautilus_proof.py": {
      "states": ["disabled"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["Repo policy is disabled"],
      "rationale": "Cautilus proof planning explicitly tells operators not to run disabled evaluator proof."
    },
    "scripts/plugin_preamble.py": {
      "states": ["disabled"],
      "visibility": ["stdout_attention"],
      "evidence_terms": ["RUNTIME_SELF_UPDATE: disabled"],
      "rationale": "Plugin preamble prints disabled runtime self-update status."
    },
    "scripts/retro_persistence_lib.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["summary_skipped_reason"],
      "rationale": "Retro persistence records skipped summary refresh reasons in payload state."
    },
    "scripts/run_cosmic_ray_mutation.py": {
      "states": ["disabled"],
      "visibility": ["local_noop_not_closeout", "stdout_attention"],
      "evidence_terms": ["defensive module-path restore disabled"],
      "rationale": "When no TOML parser (tomllib/tomli) is available the runner cannot read module-path, so the defensive restore is disabled and degrades to a no-op while the mutation run itself proceeds unaffected. A runtime no-op, not a quality/release closeout signal; the message is written to stdout so the disabled safety net is visible rather than silent."
    },
    "scripts/run_slice_closeout.py": {
      "states": ["disabled"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["cautilus_plan"],
      "rationale": "Slice closeout carries the Cautilus planner disabled state into closeout decisions; broad-pytest skip and lock states live in slice_closeout_broad_gate.py."
    },
    "scripts/slice_closeout_broad_gate.py": {
      "states": ["skipped"],
      "visibility": ["explicit_skip_summary", "terminal_payload"],
      "evidence_terms": ["skipped_broad_pytest_commands", "verification_lock_required"],
      "rationale": "Pre-lock slice closeout can explicitly skip broad pytest while preserving deterministic checks; the payload names skipped_broad_pytest_commands and the lock-required state so skipped broad proof cannot read as full closeout proof."
    },
    "scripts/classify_t_signal.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["skipped_reason"],
      "rationale": "T-signal classifier exposes skipped reasons (no_parent, shallow_clone, diff_unavailable) in its structured result so callers can attach classification_skipped to the durable episode record instead of silently emitting t_status none."
    },
    "scripts/slice_closeout_telemetry.py": {
      "states": ["disabled"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["status", "emitted"],
      "rationale": "Closeout-telemetry emission (spec achieve-efficiency E1) is on by default but returns a disabled status in the closeout payload's closeout_telemetry field when the operator sets CHARNESS_CLOSEOUT_TELEMETRY=off, so the off state stays visible in the terminal payload instead of being treated as a successful waste-signal capture. This is operational-waste telemetry, never quality or release closeout proof."
    },
    "scripts/slice_closeout_usage_episode.py": {
      "states": ["disabled", "no_adapter", "skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["status", "no_adapter", "classification_skipped"],
      "rationale": "Slice closeout usage emission reports disabled or missing adapter state in the closeout payload, and propagates the classifier's skipped reason as classification_skipped on the durable episode record instead of treating telemetry absence as successful capture."
    },
    "scripts/sync_support.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["status", "skipped"],
      "rationale": "Support sync returns explicit skipped status for non-materialized or irrelevant sync work."
    },
    "scripts/t_events_emit_lib.py": {
      "states": ["disabled", "no_adapter", "skipped"],
      "visibility": ["local_noop_not_closeout", "terminal_payload"],
      "evidence_terms": ["emitted", "reason"],
      "rationale": "T-event emission is runtime telemetry; no-op reasons are returned to callers and are not quality closeout proof."
    },
    "scripts/update_tools.py": {
      "states": ["disabled", "skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["mode", "disabled"],
      "rationale": "Tool update reports disabled adapter state as structured skipped output."
    },
    "scripts/usage_episode_session_start.py": {
      "states": ["disabled", "no_adapter"],
      "visibility": ["local_noop_not_closeout"],
      "evidence_terms": ["status", "no_adapter", "disabled"],
      "rationale": "SessionStart hook payload returns local no-op statuses (no_adapter, disabled) so optional capture can stay silent without ever being treated as closeout proof; statuses are surfaced via CHARNESS_SESSION_START_DEBUG diagnostics."
    },
    "scripts/validate_cautilus_proof.py": {
      "states": ["disabled"],
      "visibility": ["stdout_attention"],
      "evidence_terms": ["cautilus proof disabled by repo adapter"],
      "rationale": "Disabled Cautilus proof is printed as the validator outcome."
    },
    "scripts/validate_inventory_consumption.py": {
      "states": ["prose_review_status", "skipped"],
      "visibility": ["hard_failure_or_blocker", "explicit_skip_summary"],
      "evidence_terms": ["prose_review_status", "predates contract start"],
      "rationale": "The validator fails current artifacts missing prose-review engagement and explicitly reports grandfathered artifacts."
    },
    "scripts/validate_inventory_consumption_declaration.py": {
      "states": ["skipped"],
      "visibility": ["explicit_skip_summary"],
      "evidence_terms": ["opted-out", "skipped here"],
      "rationale": "Opted-out inventory declarations are handled by the companion coverage validator."
    },
    "scripts/validate_maintainer_setup.py": {
      "states": ["skipped"],
      "visibility": ["stdout_attention"],
      "evidence_terms": ["maintainer hook validation skipped"],
      "rationale": "Non-git worktrees get an explicit skipped maintainer-hook message."
    },
    "scripts/validate_usage_episodes.py": {
      "states": ["disabled", "no_adapter", "no_records", "skipped"],
      "visibility": ["structured_warning", "stdout_attention"],
      "evidence_terms": ["WARNING:", "usage_episodes_adapter_missing", "usage_episodes_no_records"],
      "rationale": "Usage episode opt-in absence and disabled state remain non-failing but visible in JSON and normal output."
    },
    "scripts/report_usage_episodes.py": {
      "states": ["disabled", "no_adapter", "no_records", "skipped"],
      "visibility": ["structured_warning", "stdout_attention"],
      "evidence_terms": ["WARNING:", "usage_episodes_adapter_missing", "usage_episodes_no_records", "Non-claims:"],
      "rationale": "Usage episode report absence, disabled state, and empty capture remain non-failing but visible, with explicit non-claims so summaries are not mistaken for product-success proof."
    },
    "scripts/report_usage_product_review.py": {
      "states": ["disabled", "no_adapter", "no_records"],
      "visibility": ["structured_warning", "stdout_attention"],
      "evidence_terms": ["usage_episodes_adapter_missing", "usage_episodes_no_records", "product-review report unavailable"],
      "rationale": "The product-review reporter reuses usage-episode adapter readiness states and renders them as explicit warnings or unavailable status instead of treating missing capture as last-seen product-review proof."
    },
    "scripts/usage_episode_product_review.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["classification_skipped"],
      "rationale": "Missed-detection candidate reporting treats `classification_skipped` as evidence in a reporter payload, not as a silent clean state."
    },
    "scripts/worktree_audit_lib.py": {
      "states": ["skipped"],
      "visibility": ["operator_summary", "terminal_payload"],
      "evidence_terms": ["skipped"],
      "rationale": "Worktree audit includes skipped counts in summaries."
    },
    "scripts/worktree_cleanup_lib.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["delete-branch", "skipped"],
      "rationale": "Cleanup actions report skipped action state and reason in the action payload."
    },
    "scripts/worktree_doctor_lib.py": {
      "states": ["skipped"],
      "visibility": ["operator_summary", "terminal_payload"],
      "evidence_terms": ["skipped:"],
      "rationale": "Worktree doctor renders skipped checks in operator output."
    },
    "scripts/worktree_doctor_state.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["SKIPPED"],
      "rationale": "The state module defines the canonical skipped value consumed by worktree doctor renderers."
    },
    "skills/public/gather/scripts/advise_google_workspace_path.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["doctor_status", "skipped"],
      "rationale": "Google Workspace advice reports skipped doctor status when the host capability is unavailable."
    },
    "skills/public/gather/scripts/advise_slack_path.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["doctor_status", "skipped"],
      "rationale": "Slack advice reports skipped doctor status when the host capability is unavailable."
    },
    "skills/public/gather/scripts/gather_public_url.py": {
      "states": ["skipped"],
      "visibility": ["support_trace"],
      "evidence_terms": ["attempt", "skipped"],
      "rationale": "Skipped acquisition stages are represented in the public URL gather attempt trace."
    },
    "skills/public/quality/references/find_inline_prompt_bulk.py": {
      "states": ["advisory-only"],
      "visibility": ["stdout_attention", "structured_warning"],
      "evidence_terms": ["advisory_only_no_canonical_prompt_asset_roots", "scope_classification"],
      "rationale": "Inline-prompt bulk helper marks default-glob scans as advisory-only when no canonical prompt_asset_roots are supplied; classification is in stdout and JSON payload."
    },
    "skills/public/quality/scripts/inventory_cli_ergonomics.py": {
      "states": ["advisory-only"],
      "visibility": ["stdout_attention", "structured_warning"],
      "evidence_terms": ["advisory-only", "scope_classification"],
      "rationale": "CLI ergonomics inventory prints an advisory-only scope_classification (carried from cli_ergonomics_lib) so green output cannot be read as enforcement."
    },
    "skills/public/quality/scripts/ci_recoverable_gates_lib.py": {
      "states": ["advisory-only"],
      "visibility": ["operator_summary", "stdout_attention"],
      "evidence_terms": ["advisory-only", "keep-local"],
      "rationale": "The CI-recoverability triage lens is advisory-only and always exit-zero (carried into inventory_ci_recoverable_gates.py): it classifies cost-ranked local gates into move-off-local candidates and keep-local. The keep-local list and the interpretation question are surfaced in the operator summary so a green run is never read as 'safe to move every gate off local' — moving a gate CI does not re-run is exactly what the lens refuses to recommend."
    },
    "skills/public/quality/scripts/skill_ergonomics_lib.py": {
      "states": ["prose_review_status"],
      "visibility": ["structured_warning"],
      "evidence_terms": ["prose_review_status", "advisories"],
      "rationale": "Skill ergonomics prose-review-required states produce advisory payloads consumed by quality artifacts."
    },
    "skills/public/quality/scripts/validate_skill_ergonomics.py": {
      "states": ["advisory-only", "disabled", "skipped"],
      "visibility": ["structured_warning", "stdout_attention"],
      "evidence_terms": ["WARNING:", "skill_ergonomics_gate_rules_empty"],
      "rationale": "Empty or disabled skill ergonomics enforcement remains exit-zero but emits warnings."
    },
    "skills/public/release/scripts/check_fresh_checkout_probes.py": {
      "states": ["not_configured"],
      "visibility": ["artifact_visible", "stdout_attention"],
      "evidence_terms": ["release adapter declares no fresh_checkout_probes"],
      "rationale": "Release fresh-checkout absence is written into the release artifact and plain output."
    },
    "skills/public/release/scripts/check_requested_review_gate.py": {
      "states": ["advisory-only", "not_configured"],
      "visibility": ["structured_warning", "stdout_attention"],
      "evidence_terms": ["WARNING:", "requested_review_commands is empty"],
      "rationale": "Release requested-review absence remains non-blocking only with an explicit warning."
    },
    "skills/public/release/scripts/init_adapter.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["review skipped because"],
      "rationale": "Generated release adapter patterns include explicit skipped-review phrases for gate parsing."
    },
    "skills/public/release/scripts/publish_release_artifact.py": {
      "states": ["not_configured"],
      "visibility": ["artifact_visible"],
      "evidence_terms": ["No repo-declared fresh checkout probes were configured"],
      "rationale": "Release helper writes not-configured fresh-checkout status into the release artifact."
    },
    "skills/public/release/scripts/publish_release_post_create.py": {
      "states": ["not_configured"],
      "visibility": ["terminal_payload", "local_noop_not_closeout"],
      "evidence_terms": ["refreshed", "failed"],
      "rationale": "run_post_publish_install_refresh returns status not_configured when the release adapter declares no post_publish_install_refresh command; all three states (refreshed/failed/not_configured) are surfaced in the terminal release payload under install_refresh, so a consumer-repo no-op (the authoring repo declares charness update) never silently reads as a clean closeout."
    },
    "skills/public/release/scripts/publish_release_retro.py": {
      "states": ["skipped"],
      "visibility": ["artifact_visible", "terminal_payload"],
      "evidence_terms": ["closeout", "retro trigger did not match"],
      "rationale": "Release-triggered retro closeout records skipped trigger evaluations in the release payload/artifact, while matched triggers persist a bounded retro artifact."
    },
    "skills/public/release/scripts/resolve_adapter.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["review skipped because"],
      "rationale": "Release adapter parsing preserves skipped-review patterns for requested-review gate decisions."
    },
    "skills/public/setup/scripts/seed_dependencies.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["status", "skipped"],
      "rationale": "Dependency seeding reports skipped dependency setup as structured payload status."
    },
    "skills/public/setup/scripts/seed_t_events_adapter.py": {
      "states": ["disabled"],
      "visibility": ["artifact_visible"],
      "evidence_terms": ["enabled: false"],
      "rationale": "Seeded T-events adapter starts disabled in checked-in config text so the opt-in state is visible."
    },
    "skills/support/markdown-preview/scripts/render_markdown_preview.py": {
      "states": ["disabled"],
      "visibility": ["structured_warning", "terminal_payload"],
      "evidence_terms": ["Markdown preview is disabled by config."],
      "rationale": "Markdown preview disabled state includes a warning in the render payload."
    },
    "skills/support/web-fetch/scripts/acquisition_trace_lib.py": {
      "states": ["skipped"],
      "visibility": ["support_trace"],
      "evidence_terms": ["status=\"skipped\""],
      "rationale": "Web-fetch skipped acquisition attempts are explicit trace entries."
    },
    "scripts/check_prescribed_skill_executed.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload", "hard_failure_or_blocker"],
      "evidence_terms": ["skipped", "host-blocked-subagent", "host-log-not-exposed", "evaluator-unavailable"],
      "rationale": "Shared closeout helper CLI surfaces each skipped required evidence name with an enum-bounded reason; absence of a recognized reason fails the gate."
    },
    "scripts/check_prescribed_skill_executed_lib.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload", "hard_failure_or_blocker"],
      "evidence_terms": ["skipped", "host-blocked-subagent", "host-log-not-exposed", "evaluator-unavailable"],
      "rationale": "Shared closeout helper library carries the same enum-bounded skip semantics into structured ok/blockers payloads consumed by achieve/issue/release closeout gates."
    },
    "skills/public/achieve/scripts/goal_artifact_lib.py": {
      "states": ["skipped"],
      "visibility": ["artifact_visible"],
      "evidence_terms": ["skipped:", "skipped: <enum>"],
      "rationale": "Achieve After-phase closeout writes per-evidence-name `skipped: <enum>: <detail>` lines into the goal artifact so the persisted reason stays operator-visible."
    },
    "skills/public/achieve/scripts/goal_artifact_closeout_evidence.py": {
      "states": ["skipped"],
      "visibility": ["artifact_visible"],
      "evidence_terms": ["skipped"],
      "rationale": "Sibling module split out of goal_artifact_lib; mirrors the same closeout-evidence skip semantics (per-evidence-name skip lines extracted from the goal artifact body and forwarded to the shared closeout helper)."
    },
    "skills/public/achieve/scripts/goal_artifact_closeout_delegation.py": {
      "states": ["skipped"],
      "visibility": ["artifact_visible", "hard_failure_or_blocker"],
      "evidence_terms": ["skipped: <reason>", "verified", "issue #N"],
      "rationale": "Orchestrator/sub-goal closeout-delegation gate: an orchestrator's delegated-proof checklist item resolves as verified, `skipped: <reason>`, or an issue reference; an unresolved item blocks the complete flip and the reason stays artifact-visible in the goal's `## Closeout Delegation` section."
    },
    "skills/public/achieve/scripts/goal_artifact_early_close_report.py": {
      "states": ["skipped"],
      "visibility": ["artifact_visible", "hard_failure_or_blocker"],
      "evidence_terms": ["early-close reports cannot be skipped"],
      "rationale": "Early-close report evidence is required when the goal records an early-close reason; a skipped report line is converted into an invalid skip so the complete gate blocks instead of silently accepting missing user-facing rationale."
    },
    "skills/public/achieve/scripts/goal_artifact_discussion.py": {
      "states": ["skipped"],
      "visibility": ["artifact_visible"],
      "evidence_terms": ["Discuss before activation", "discussion_summary_present"],
      "rationale": "Before-phase discussion readiness treats n/a/none summaries as absent when consequential activation decisions are present; a real summary must be visible in the goal artifact before pursue-ready passes."
    },
    "skills/public/achieve/scripts/describe_goal_closeout_shape.py": {
      "states": ["skipped"],
      "visibility": ["stdout_attention"],
      "evidence_terms": ["skipped: <reason>: <detail>", "skipped: <allowed-reason>"],
      "rationale": "Author-time shape SOURCE for the goal-closeout preflight: it prints the enforced `skipped: <enum>: <detail>` author syntax (the exact form `check_goal_artifact.py` accepts) to stdout so an author learns it before the flip. The `skipped` token is documentation of the author syntax surfaced verbatim to the operator, never a runtime exit-zero status of this script."
    },
    "skills/public/issue/scripts/issue_create.py": {
      "states": ["disabled", "skipped"],
      "visibility": ["terminal_payload"],
      "evidence_terms": ["verify_skipped", "verification disabled by caller", "body_verified"],
      "rationale": "issue create emits a stdout payload whose body_verified is null with an explicit verify_skipped/verify_error note when read-back is disabled (verify=False) or not feasible; an unverified write is surfaced to the operator, never silently treated as a confirmed write."
    },
    "skills/public/issue/scripts/issue_resolution_critique.py": {
      "states": ["skipped"],
      "visibility": ["terminal_payload", "hard_failure_or_blocker"],
      "evidence_terms": ["skipped_classification", "host-blocked-subagent"],
      "rationale": "Issue closeout verifier reports an explicit skipped_classification payload and refuses closure when a required resolution-critique skip lacks a recognized host-blocked reason."
    },
    "skills/public/release/scripts/publish_release_preflight.py": {
      "states": ["skipped"],
      "visibility": ["hard_failure_or_blocker"],
      "evidence_terms": ["host-blocked-subagent", "honestly skipped"],
      "rationale": "Release publish preflight refuses without an explicit `--critique-artifact` or `--critique-blocked <host-signal>`; the blocked path is recorded as a skipped standalone-critique reason in the shared closeout payload and never silently reads as release proof."
    },
    "skills/public/release/scripts/publish_release_adapter_preflight.py": {
      "states": ["not_configured", "not_evaluable"],
      "visibility": ["artifact_visible"],
      "evidence_terms": ["not_configured", "not_evaluable"],
      "rationale": "Release adapter focused preflight records not_configured/not_evaluable as release-artifact-visible non-claims when a changed adapter cannot run focused commands or lacks a previous tag for field diff."
    }
  }
}
